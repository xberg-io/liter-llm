# This file is auto-generated by alef — DO NOT EDIT.
# alef:hash:e5cb5821d8725e53ca31e816c57f73650394e9b76ff7ec0252ba6743c5541fa5
# To regenerate: alef generate
# To verify freshness: alef verify
"""Configuration options for the conversion API."""

from __future__ import annotations

from dataclasses import dataclass, field
from enum import Enum
from typing import TYPE_CHECKING, Any

from ._internal_bindings import (
    AssistantContent,  # noqa: TC001
    AssistantPart,  # noqa: TC001
    BatchStatus,  # noqa: TC001
    CacheBackend,  # noqa: TC001
    ContentPart,  # noqa: TC001
    EmbeddingContentPart,  # noqa: TC001
    EmbeddingFormat,  # noqa: TC001
    EmbeddingInput,  # noqa: TC001
    Enforcement,  # noqa: TC001
    FilePurpose,  # noqa: TC001
    FinishReason,  # noqa: TC001
    ImageDetail,  # noqa: TC001
    Message,  # noqa: TC001
    Modality,  # noqa: TC001
    ModerationInput,  # noqa: TC001
    OcrDocument,  # noqa: TC001
    ReasoningEffort,  # noqa: TC001
    RerankDocument,  # noqa: TC001
    ResponseFormat,  # noqa: TC001
    StopSequence,  # noqa: TC001
    ToolChoice,  # noqa: TC001
    ToolChoiceMode,  # noqa: TC001
    ToolType,  # noqa: TC001
    UserContent,  # noqa: TC001
)
from . import _internal_bindings

if TYPE_CHECKING:
    from ._internal_bindings import (
        ChatCompletionTool,  # noqa: F401
        FunctionCall,  # noqa: F401
        ToolCall,  # noqa: F401
    )


class AuthType(str, Enum):
    """Auth scheme used by a provider."""

    bearer = "bearer"

    api_key = "api_key"

    none = "none"

    unknown = "unknown"


class CircuitState(str, Enum):
    """Observable state of a circuit breaker."""

    closed = "closed"

    open = "open"

    half_open = "half_open"


class HealthStatus(str, Enum):
    """The result of a single health probe."""

    healthy = "healthy"

    unhealthy = "unhealthy"


class RefreshOutcome(str, Enum):
    """Result of a [`refresh_catalog`] call."""

    disabled = "disabled"

    from_cache = "from_cache"

    fetched = "fetched"


class StreamFormat(str, Enum):
    """The streaming wire format a provider uses for its response stream."""

    sse = "sse"

    aws_event_stream = "aws_event_stream"


@dataclass(frozen=True, slots=True)
class AssistantMessage:
    """Assistant's response to a user message."""

    content: AssistantContent | None = None

    """The assistant's response: plain text, structured parts, or absent."""

    name: str | None = None

    """Optional name for the assistant."""

    tool_calls: list[ToolCall] | None = None

    """Tool calls the model wants to execute, if any."""

    refusal: str | None = None

    """Refusal reason, if the model declined to respond per safety policies."""

    function_call: FunctionCall | None = None

    """Deprecated legacy function_call field; retained for API compatibility."""

    reasoning_content: str | None = None

    """Reasoning/thinking tokens returned by the provider, if any (e.g. DeepSeek R1, Qwen `reasoning_content`, or Anthropic extended thinking)."""

    @staticmethod
    def from_json(json_str: str) -> AssistantMessage:
        """Build an AssistantMessage from a JSON string, via the native binding."""
        return _from_native_assistant_message(_internal_bindings.AssistantMessage.from_json(json_str))


@dataclass(frozen=True, slots=True)
class AudioContent:
    """Audio content part for speech-capable models."""

    data: str = ""

    """Base64-encoded audio data."""

    format: str = ""

    """Audio format (e.g., "wav", "mp3", "ogg")."""

    @staticmethod
    def from_json(json_str: str) -> AudioContent:
        """Build an AudioContent from a JSON string, via the native binding."""
        return _from_native_audio_content(_internal_bindings.AudioContent.from_json(json_str))


@dataclass(frozen=True, slots=True)
class BatchListQuery:
    """Query parameters for listing batches."""

    limit: int | None = None

    """Maximum number of results to return. Defaults to 20."""

    after: str | None = None

    """Pagination cursor: return results after this batch ID."""

    @staticmethod
    def from_json(json_str: str) -> BatchListQuery:
        """Build a BatchListQuery from a JSON string, via the native binding."""
        return _from_native_batch_list_query(_internal_bindings.BatchListQuery.from_json(json_str))


@dataclass(frozen=True, slots=True)
class BatchListResponse:
    """Response from listing batches."""

    object: str = ""

    """Object type (always `"list"`)."""

    data: list[BatchObject] = field(default_factory=list)

    """List of batch objects."""

    has_more: bool | None = None

    """Whether more results are available."""

    first_id: str | None = None

    """First batch ID in the result set (for pagination)."""

    last_id: str | None = None

    """Last batch ID in the result set (for pagination)."""

    @staticmethod
    def from_json(json_str: str) -> BatchListResponse:
        """Build a BatchListResponse from a JSON string, via the native binding."""
        return _from_native_batch_list_response(_internal_bindings.BatchListResponse.from_json(json_str))


@dataclass(frozen=True, slots=True)
class BatchObject:
    """A batch job object."""

    id: str = ""

    """Unique batch ID."""

    object: str = ""

    """Object type (always `"batch"`)."""

    endpoint: str = ""

    """API endpoint (e.g., `"/v1/chat/completions"`)."""

    input_file_id: str = ""

    """ID of the input file."""

    completion_window: str = ""

    """Completion window (e.g., `"24h"`)."""

    status: BatchStatus | str = "validating"

    """Current job status."""

    output_file_id: str | None = None

    """ID of the output file (present when completed)."""

    error_file_id: str | None = None

    """ID of the error file (present if some requests failed)."""

    created_at: int = 0

    """Unix timestamp of batch creation."""

    completed_at: int | None = None

    """Unix timestamp of completion (if completed)."""

    failed_at: int | None = None

    """Unix timestamp of failure (if failed)."""

    expired_at: int | None = None

    """Unix timestamp of expiration (if expired)."""

    request_counts: BatchRequestCounts | None = None

    """Request processing counts."""

    metadata: str | None = None

    """Metadata attached to the batch."""

    @staticmethod
    def from_json(json_str: str) -> BatchObject:
        """Build a BatchObject from a JSON string, via the native binding."""
        return _from_native_batch_object(_internal_bindings.BatchObject.from_json(json_str))


@dataclass(frozen=True, slots=True)
class BatchRequestCounts:
    """Request processing counts for a batch."""

    total: int = 0

    """Total requests in the batch."""

    completed: int = 0

    """Completed requests."""

    failed: int = 0

    """Failed requests."""

    @staticmethod
    def from_json(json_str: str) -> BatchRequestCounts:
        """Build a BatchRequestCounts from a JSON string, via the native binding."""
        return _from_native_batch_request_counts(_internal_bindings.BatchRequestCounts.from_json(json_str))


@dataclass(frozen=True, slots=True)
class BedrockConfig:
    """AWS Bedrock configuration."""

    region: str | None = None

    """AWS region (e.g. `"us-east-1"`)."""

    cross_region_prefix: str | None = None

    """Cross-region inference profile prefix (e.g. `"us"`)."""

    access_key_id: str | None = None

    """Explicit AWS access key ID."""

    secret_access_key: str | None = None

    """Explicit AWS secret access key."""

    session_token: str | None = None

    """Explicit AWS session token (temporary credentials)."""

    @staticmethod
    def from_json(json_str: str) -> BedrockConfig:
        """Build a BedrockConfig from a JSON string, via the native binding."""
        return _from_native_bedrock_config(_internal_bindings.BedrockConfig.from_json(json_str))


@dataclass(frozen=True, slots=True)
class BudgetConfig:
    """Configuration for budget enforcement."""

    global_limit: float | None = None

    """Maximum total spend across all models, in USD.  `None` means unlimited."""

    model_limits: dict[str, float] = field(default_factory=dict)

    """Per-model spending limits in USD.  Models not listed here are only constrained by `global_limit`."""

    enforcement: Enforcement | str = "hard"

    """Whether to reject requests or merely warn when a limit is exceeded."""

    @staticmethod
    def from_json(json_str: str) -> BudgetConfig:
        """Build a BudgetConfig from a JSON string, via the native binding."""
        return _from_native_budget_config(_internal_bindings.BudgetConfig.from_json(json_str))


@dataclass(frozen=True, slots=True)
class CacheConfig:
    """Configuration for the response cache."""

    max_entries: int = 256

    """Maximum number of cached entries."""

    ttl: int = 300000

    """Time-to-live for each cached entry."""

    backend: CacheBackend | str = "memory"

    """Storage backend to use."""

    @staticmethod
    def from_json(json_str: str) -> CacheConfig:
        """Build a CacheConfig from a JSON string, via the native binding."""
        return _from_native_cache_config(_internal_bindings.CacheConfig.from_json(json_str))


@dataclass(frozen=True, slots=True)
class CatalogRefreshConfig:
    """Plain-data configuration for [`refresh_catalog`]."""

    enabled: bool = False

    """Runtime catalog refresh is entirely opt-in: when `false`, [`refresh_catalog`] is a no-op that returns `Ok(`[`RefreshOutcome::Disabled`]`)` without touching the network, the filesystem, or the overlay registry."""

    source_url: str = "https://github.com/xberg-io/liter-llm/releases/download/model-catalog/catalog.json"

    """Source URL to fetch `catalog.json` from. Must be `https`. Defaults to [`DEFAULT_CATALOG_URL`]; configurable so self-hosted mirrors work."""

    ttl_seconds: int = 86400

    """How long a cached `catalog.json` remains valid before a network refetch is attempted, in seconds."""

    cache_path: str | None = None

    """Filesystem path for the on-disk cache. `None` uses a default path under `std::env::temp_dir()`."""

    @staticmethod
    def from_json(json_str: str) -> CatalogRefreshConfig:
        """Build a CatalogRefreshConfig from a JSON string, via the native binding."""
        return _from_native_catalog_refresh_config(_internal_bindings.CatalogRefreshConfig.from_json(json_str))


@dataclass(frozen=True, slots=True)
class ChatCompletionChunk:
    """A streamed chunk of a chat completion response."""

    id: str = ""

    """Unique identifier for this stream."""

    object: str = ""

    """Always `"chat.completion.chunk"` from OpenAI-compatible APIs.  Stored as a plain `String` so non-standard provider values do not fail parsing."""

    created: int = 0

    """Unix timestamp of chunk creation."""

    model: str = ""

    """Model used to generate the chunk."""

    choices: list[StreamChoice] = field(default_factory=list)

    """Streaming choices (delta updates)."""

    usage: Usage | None = None

    """Token usage (typically only in the final chunk)."""

    system_fingerprint: str | None = None

    """Fingerprint of the system configuration (OpenAI-specific)."""

    service_tier: str | None = None

    """Service tier used (OpenAI-specific)."""

    @staticmethod
    def from_json(json_str: str) -> ChatCompletionChunk:
        """Build a ChatCompletionChunk from a JSON string, via the native binding."""
        return _from_native_chat_completion_chunk(_internal_bindings.ChatCompletionChunk.from_json(json_str))


@dataclass(frozen=True, slots=True)
class ChatCompletionRequest:
    """Chat completion request (compatible with OpenAI and similar APIs)."""

    model: str = ""

    """Model ID (e.g., `"gpt-4o-mini"`, `"claude-3-5-sonnet"`)."""

    messages: list[Message] = field(default_factory=list)

    """Conversation history from oldest to newest."""

    temperature: float | None = None

    """Sampling temperature. Higher increases randomness, lower is more deterministic. Defaults to 1.0."""

    top_p: float | None = None

    """Nucleus sampling parameter. Lower is more focused."""

    n: int | None = None

    """Number of chat completions to generate. Defaults to 1."""

    stream: bool | None = None

    """Whether to stream the response."""

    stop: StopSequence | None = None

    """Stop sequence(s) that halt token generation."""

    max_tokens: int | None = None

    """Max output tokens. Different from max_completion_tokens in some providers."""

    presence_penalty: float | None = None

    """Presence penalty in `[-2.0, 2.0]`. Positive discourages repeated topics."""

    frequency_penalty: float | None = None

    """Frequency penalty in `[-2.0, 2.0]`. Positive discourages repeated tokens."""

    logit_bias: dict[str, float] | None = None

    """Token bias map.  Uses `BTreeMap` (sorted keys) for deterministic serialization order -- important when hashing or signing requests."""

    user: str | None = None

    """User identifier for request tracking and abuse detection."""

    tools: list[ChatCompletionTool] | None = None

    """Tools the model can invoke."""

    tool_choice: ToolChoice | None = None

    """Tool usage mode (auto, required, none, or specific tool)."""

    parallel_tool_calls: bool | None = None

    """Whether the model can call multiple tools in parallel. Defaults to true."""

    response_format: ResponseFormat | str | None = None

    """Output format constraint (text, JSON, JSON schema)."""

    stream_options: StreamOptions | None = None

    """Streaming options (e.g., include_usage)."""

    seed: int | None = None

    """Random seed for reproducible outputs. Provider support varies."""

    reasoning_effort: ReasoningEffort | str | None = None

    """Reasoning effort level (minimal, low, medium, high, max) for extended-thinking models."""

    modalities: list[Modality | str] | None = None

    """Output modalities to request from the model."""

    logprobs: bool | None = None

    """Whether to return log probabilities of the output tokens."""

    top_logprobs: int | None = None

    """Number of most-likely tokens to return log probabilities for, `0..=20`. Requires `logprobs` to be `true`."""

    max_completion_tokens: int | None = None

    """Upper bound on generated tokens, including reasoning tokens."""

    service_tier: str | None = None

    """Latency tier to process the request under (e.g. `"auto"`, `"default"`, `"flex"`)."""

    store: bool | None = None

    """Whether to store the completion for later retrieval by the provider."""

    metadata: dict[str, str] | None = None

    """Developer-defined tags attached to the completion."""

    prediction: str | None = None

    """Predicted output, for latency reduction when much of the response is known ahead of time."""

    audio: str | None = None

    """Audio output parameters, required when `modalities` includes `audio`."""

    web_search_options: str | None = None

    """Web-search tool configuration for search-enabled models."""

    extra_body: str | None = None

    """Provider-specific extra parameters merged into the request body. Use for guardrails, safety settings, grounding config, etc."""

    @staticmethod
    def from_json(json_str: str) -> ChatCompletionRequest:
        """Build a ChatCompletionRequest from a JSON string, via the native binding."""
        return _from_native_chat_completion_request(_internal_bindings.ChatCompletionRequest.from_json(json_str))


@dataclass(frozen=True, slots=True)
class ChatCompletionResponse:
    """Chat completion response from the API."""

    id: str = ""

    """Unique identifier for this response."""

    object: str = ""

    """Always `"chat.completion"` from OpenAI-compatible APIs.  Stored as a plain `String` so non-standard provider values do not break deserialization."""

    created: int = 0

    """Unix timestamp of response creation."""

    model: str = ""

    """Model used to generate the response."""

    choices: list[Choice] = field(default_factory=list)

    """List of completion choices."""

    usage: Usage | None = None

    """Token usage statistics."""

    system_fingerprint: str | None = None

    """Fingerprint of the system configuration (OpenAI-specific)."""

    service_tier: str | None = None

    """Service tier used (OpenAI-specific)."""

    @staticmethod
    def from_json(json_str: str) -> ChatCompletionResponse:
        """Build a ChatCompletionResponse from a JSON string, via the native binding."""
        return _from_native_chat_completion_response(_internal_bindings.ChatCompletionResponse.from_json(json_str))


@dataclass(frozen=True, slots=True)
class Choice:
    """A single completion choice."""

    index: int = 0

    """Index of this choice in the choices array."""

    message: AssistantMessage | None = None

    """The assistant's message response."""

    finish_reason: FinishReason | str | None = None

    """Why the model stopped generating (stop, length, tool_calls, content_filter, etc.)."""

    logprobs: str | None = None

    """Per-token log probabilities, when the request asked for them."""

    @staticmethod
    def from_json(json_str: str) -> Choice:
        """Build a Choice from a JSON string, via the native binding."""
        return _from_native_choice(_internal_bindings.Choice.from_json(json_str))


@dataclass(frozen=True, slots=True)
class CreateBatchRequest:
    """Request to create a batch job."""

    input_file_id: str = ""

    """ID of the uploaded input file (JSONL format)."""

    endpoint: str = ""

    """API endpoint (e.g., `"/v1/chat/completions"`)."""

    completion_window: str = ""

    """Completion window (e.g., `"24h"`)."""

    metadata: str | None = None

    """Optional metadata to attach to the batch."""

    @staticmethod
    def from_json(json_str: str) -> CreateBatchRequest:
        """Build a CreateBatchRequest from a JSON string, via the native binding."""
        return _from_native_create_batch_request(_internal_bindings.CreateBatchRequest.from_json(json_str))


@dataclass(frozen=True, slots=True)
class CreateFileRequest:
    """Request to upload a file."""

    file: str = ""

    """Base64-encoded file data."""

    purpose: FilePurpose | str = "assistants"

    """Purpose for the file."""

    filename: str | None = None

    """Optional filename to associate with the upload."""

    @staticmethod
    def from_json(json_str: str) -> CreateFileRequest:
        """Build a CreateFileRequest from a JSON string, via the native binding."""
        return _from_native_create_file_request(_internal_bindings.CreateFileRequest.from_json(json_str))


@dataclass(frozen=True, slots=True)
class CreateImageRequest:
    """Request to create images from a text prompt."""

    prompt: str = ""

    """Text description of the image to generate."""

    model: str | None = None

    """Model ID (e.g., `"dall-e-3"`). Optional; API may use default if unset."""

    n: int | None = None

    """Number of images to generate. Defaults to 1."""

    size: str | None = None

    """Image size (e.g., `"1024x1024"`, `"1792x1024"`)."""

    quality: str | None = None

    """Image quality: `"standard"` or `"hd"`."""

    style: str | None = None

    """Style: `"natural"` or `"vivid"` (DALL-E 3 only)."""

    response_format: str | None = None

    """Response format: `"url"` or `"b64_json"`."""

    user: str | None = None

    """User identifier for request tracking."""

    @staticmethod
    def from_json(json_str: str) -> CreateImageRequest:
        """Build a CreateImageRequest from a JSON string, via the native binding."""
        return _from_native_create_image_request(_internal_bindings.CreateImageRequest.from_json(json_str))


@dataclass(frozen=True, slots=True)
class CreateResponseRequest:
    """Request to create a response via the OpenAI Responses API (`POST /responses`)."""

    model: str = ""

    """Model ID, as named by the OpenAI Responses API (e.g. `"gpt-5"`)."""

    input: str | None = None

    """Input data to process (e.g., a document to extract from)."""

    instructions: str | None = None

    """Instructions for processing the input."""

    tools: list[ResponseTool] | None = None

    """Available tools the model can use."""

    temperature: float | None = None

    """Sampling temperature in `[0.0, 2.0]`. Defaults to 1.0."""

    max_output_tokens: int | None = None

    """Maximum output tokens."""

    metadata: str | None = None

    """Optional metadata."""

    extra_body: str | None = None

    """Extra top-level parameters shallow-merged into the request body, OpenAI-Python style (`{**body, **extra_body}`) -- keys here override identically named fields above. Use it for OpenAI Responses fields this struct does not model directly, such as the top-level `reasoning.effort`."""

    stream: bool | None = None

    """Whether to stream the response."""

    @staticmethod
    def from_json(json_str: str) -> CreateResponseRequest:
        """Build a CreateResponseRequest from a JSON string, via the native binding."""
        return _from_native_create_response_request(_internal_bindings.CreateResponseRequest.from_json(json_str))


@dataclass(frozen=True, slots=True)
class CreateSpeechRequest:
    """Request to generate speech audio from text."""

    model: str = ""

    """Model ID (e.g., `"tts-1"`, `"tts-1-hd"`)."""

    input: str = ""

    """Text to synthesize into speech."""

    voice: str = ""

    """Voice name (e.g., `"alloy"`, `"echo"`, `"fable"`, `"onyx"`, `"nova"`, `"shimmer"`)."""

    response_format: str | None = None

    """Audio format (e.g., `"mp3"`, `"opus"`, `"aac"`, `"flac"`, `"wav"`, `"pcm"`)."""

    speed: float | None = None

    """Playback speed in `[0.25, 4.0]`. Defaults to 1.0."""

    @staticmethod
    def from_json(json_str: str) -> CreateSpeechRequest:
        """Build a CreateSpeechRequest from a JSON string, via the native binding."""
        return _from_native_create_speech_request(_internal_bindings.CreateSpeechRequest.from_json(json_str))


@dataclass(frozen=True, slots=True)
class CreateTranscriptionRequest:
    """Request to transcribe audio into text."""

    model: str = ""

    """Model ID (e.g., `"whisper-1"`)."""

    file: str = ""

    """Base64-encoded audio file data."""

    language: str | None = None

    """Language ISO-639-1 code (e.g., `"en"`, `"fr"`, `"de"`). Optional; model auto-detects."""

    prompt: str | None = None

    """Optional text to guide the model (improves accuracy for domain-specific terms)."""

    response_format: str | None = None

    """Output format (e.g., `"json"`, `"text"`, `"vtt"`, `"srt"`, `"verbose_json"`)."""

    temperature: float | None = None

    """Sampling temperature in `[0.0, 1.0]`. Higher increases variability. Defaults to 0."""

    @staticmethod
    def from_json(json_str: str) -> CreateTranscriptionRequest:
        """Build a CreateTranscriptionRequest from a JSON string, via the native binding."""
        return _from_native_create_transcription_request(_internal_bindings.CreateTranscriptionRequest.from_json(json_str))


@dataclass(frozen=True, slots=True)
class DeleteResponse:
    """Response from a delete operation."""

    id: str = ""

    """ID of the deleted resource."""

    object: str = ""

    """Object type."""

    deleted: bool = False

    """Confirmation that the resource was deleted."""

    @staticmethod
    def from_json(json_str: str) -> DeleteResponse:
        """Build a DeleteResponse from a JSON string, via the native binding."""
        return _from_native_delete_response(_internal_bindings.DeleteResponse.from_json(json_str))


@dataclass(frozen=True, slots=True)
class DeveloperMessage:
    """Developer message (system-like message for Claude models)."""

    content: str = ""

    """Developer-specific instructions or context."""

    name: str | None = None

    """Optional name for the developer message source."""

    @staticmethod
    def from_json(json_str: str) -> DeveloperMessage:
        """Build a DeveloperMessage from a JSON string, via the native binding."""
        return _from_native_developer_message(_internal_bindings.DeveloperMessage.from_json(json_str))


@dataclass(frozen=True, slots=True)
class DocumentContent:
    """PDF/document content part for vision-capable models."""

    data: str = ""

    """Base64-encoded document data or URL."""

    media_type: str = ""

    """MIME type (e.g., "application/pdf", "text/csv")."""

    @staticmethod
    def from_json(json_str: str) -> DocumentContent:
        """Build a DocumentContent from a JSON string, via the native binding."""
        return _from_native_document_content(_internal_bindings.DocumentContent.from_json(json_str))


@dataclass(frozen=True, slots=True)
class EmbeddingRequest:
    """Embedding request."""

    model: str = ""

    """Model ID (e.g., `"text-embedding-3-small"`)."""

    input: EmbeddingInput | None = None

    """Text, texts, or multimodal content to embed."""

    encoding_format: EmbeddingFormat | str | None = None

    """Output format: float (native) or base64."""

    dimensions: int | None = None

    """Requested embedding dimensions (if supported by the model)."""

    user: str | None = None

    """User identifier for request tracking."""

    @staticmethod
    def from_json(json_str: str) -> EmbeddingRequest:
        """Build an EmbeddingRequest from a JSON string, via the native binding."""
        return _from_native_embedding_request(_internal_bindings.EmbeddingRequest.from_json(json_str))


@dataclass(frozen=True, slots=True)
class FileListQuery:
    """Query parameters for listing files."""

    purpose: str | None = None

    """Filter by file purpose (e.g., `"batch"`, `"fine-tune"`)."""

    limit: int | None = None

    """Maximum number of results to return. Defaults to 20."""

    after: str | None = None

    """Pagination cursor: return results after this file ID."""

    @staticmethod
    def from_json(json_str: str) -> FileListQuery:
        """Build a FileListQuery from a JSON string, via the native binding."""
        return _from_native_file_list_query(_internal_bindings.FileListQuery.from_json(json_str))


@dataclass(frozen=True, slots=True)
class FileListResponse:
    """Response from listing files."""

    object: str = ""

    """Object type (always `"list"`)."""

    data: list[FileObject] = field(default_factory=list)

    """List of file objects."""

    has_more: bool | None = None

    """Whether more results are available."""

    @staticmethod
    def from_json(json_str: str) -> FileListResponse:
        """Build a FileListResponse from a JSON string, via the native binding."""
        return _from_native_file_list_response(_internal_bindings.FileListResponse.from_json(json_str))


@dataclass(frozen=True, slots=True)
class FileObject:
    """An uploaded file object."""

    id: str = ""

    """Unique file ID."""

    object: str = ""

    """Object type (always `"file"`)."""

    bytes: int = 0

    """File size in bytes."""

    created_at: int = 0

    """Unix timestamp of file creation."""

    filename: str = ""

    """Filename."""

    purpose: str = ""

    """File purpose."""

    status: str | None = None

    """Processing status (e.g., `"uploaded"`, `"processed"`)."""

    @staticmethod
    def from_json(json_str: str) -> FileObject:
        """Build a FileObject from a JSON string, via the native binding."""
        return _from_native_file_object(_internal_bindings.FileObject.from_json(json_str))


@dataclass(frozen=True, slots=True)
class FunctionMessage:
    """Deprecated legacy function-role message body."""

    content: str = ""

    name: str = ""

    @staticmethod
    def from_json(json_str: str) -> FunctionMessage:
        """Build a FunctionMessage from a JSON string, via the native binding."""
        return _from_native_function_message(_internal_bindings.FunctionMessage.from_json(json_str))


@dataclass(frozen=True, slots=True)
class Image:
    """A single generated image, returned as either a URL or base64 data."""

    url: str | None = None

    """Image URL (if response_format was "url")."""

    b64_json: str | None = None

    """Base64-encoded image data (if response_format was "b64_json")."""

    revised_prompt: str | None = None

    """The final prompt used to generate the image (DALL-E 3)."""

    @staticmethod
    def from_json(json_str: str) -> Image:
        """Build an Image from a JSON string, via the native binding."""
        return _from_native_image(_internal_bindings.Image.from_json(json_str))


@dataclass(frozen=True, slots=True)
class ImageUrl:
    """An image URL reference with optional detail level for processing."""

    url: str = ""

    """URL of the image (data URI or HTTP/HTTPS URL)."""

    detail: ImageDetail | str | None = None

    """Detail level: low (512x512), high (2x2 tiles), or auto (model-selected)."""

    @staticmethod
    def from_json(json_str: str) -> ImageUrl:
        """Build an ImageUrl from a JSON string, via the native binding."""
        return _from_native_image_url(_internal_bindings.ImageUrl.from_json(json_str))


@dataclass(frozen=True, slots=True)
class ImagesResponse:
    """Response containing generated images."""

    created: int = 0

    """Unix timestamp of image creation."""

    data: list[Image] = field(default_factory=list)

    """List of generated images."""

    @staticmethod
    def from_json(json_str: str) -> ImagesResponse:
        """Build an ImagesResponse from a JSON string, via the native binding."""
        return _from_native_images_response(_internal_bindings.ImagesResponse.from_json(json_str))


@dataclass(frozen=True, slots=True)
class InFlightLimitConfig:
    """Configuration for the global per-client in-flight request limit."""

    max_in_flight: int | None = None

    """Maximum simultaneously outstanding provider requests. `None` means unlimited."""

    @staticmethod
    def from_json(json_str: str) -> InFlightLimitConfig:
        """Build an InFlightLimitConfig from a JSON string, via the native binding."""
        return _from_native_in_flight_limit_config(_internal_bindings.InFlightLimitConfig.from_json(json_str))


@dataclass(frozen=True, slots=True)
class JsonSchemaFormat:
    """JSON Schema specification for constrained output."""

    name: str = ""

    """Name of the schema (must be unique in the request)."""

    description: str | None = None

    """Description of what the schema represents."""

    schema: str | None = None

    """JSON Schema object defining the output structure."""

    strict: bool | None = None

    """If true, enforce strict schema validation."""

    @staticmethod
    def from_json(json_str: str) -> JsonSchemaFormat:
        """Build a JsonSchemaFormat from a JSON string, via the native binding."""
        return _from_native_json_schema_format(_internal_bindings.JsonSchemaFormat.from_json(json_str))


@dataclass(frozen=True, slots=True)
class LlmBudgetConfig:
    """Budget enforcement configuration."""

    global_limit: float | None = None

    """Global spend limit in USD."""

    model_limits: dict[str, float] | None = None

    """Per-model spend limits in USD, keyed by model name."""

    enforcement: str | None = None

    """Enforcement mode: `"hard"` (reject over-budget requests) or `"soft"` (log only)."""

    @staticmethod
    def from_json(json_str: str) -> LlmBudgetConfig:
        """Build a LlmBudgetConfig from a JSON string, via the native binding."""
        return _from_native_llm_budget_config(_internal_bindings.LlmBudgetConfig.from_json(json_str))


@dataclass(frozen=True, slots=True)
class LlmCacheConfig:
    """Response cache configuration."""

    max_entries: int | None = None

    """Maximum number of cached entries."""

    ttl_seconds: int | None = None

    """Cache entry time-to-live, in seconds."""

    backend: str | None = None

    """Cache backend name (e.g. `"memory"`, or an `opendal` scheme)."""

    backend_config: dict[str, str] | None = None

    """Backend-specific configuration key/value pairs."""

    @staticmethod
    def from_json(json_str: str) -> LlmCacheConfig:
        """Build a LlmCacheConfig from a JSON string, via the native binding."""
        return _from_native_llm_cache_config(_internal_bindings.LlmCacheConfig.from_json(json_str))


@dataclass(frozen=True, slots=True)
class LlmConfig:
    """Canonical configuration for an LLM client."""

    model: str = ""

    """Model identifier (e.g. `"gpt-4o"`, `"bedrock/anthropic.claude-3-sonnet-20240229-v1:0"`)."""

    api_key: str | None = None

    """API key for authentication."""

    base_url: str | None = None

    """Override base URL. When set, all requests go here and provider auto-detection is skipped."""

    timeout_secs: int | None = None

    """Request timeout, in seconds."""

    max_retries: int | None = None

    """Maximum number of retries on 429 / 5xx responses."""

    temperature: float | None = None

    """Sampling temperature for requests built from this config."""

    max_tokens: int | None = None

    """Maximum number of tokens to generate for requests built from this config."""

    load_env: bool | None = None

    """Automatically load the API key from the provider's environment variable when no explicit key is provided (default: `true`)."""

    headers: dict[str, str] | None = None

    """Extra headers sent on every request."""

    providers: list[LlmProviderConfig] | None = None

    """Custom provider configurations, in addition to the built-in providers."""

    cache: LlmCacheConfig | None = None

    """Response cache configuration."""

    budget: LlmBudgetConfig | None = None

    """Budget enforcement configuration."""

    rate_limit: LlmRateLimitConfig | None = None

    """Per-model rate limiting configuration."""

    in_flight_limit: LlmInFlightLimitConfig | None = None

    """Global per-client in-flight provider request limit."""

    cost_tracking: bool | None = None

    """Enable per-request cost tracking."""

    tracing: bool | None = None

    """Enable OpenTelemetry-compatible tracing spans."""

    cooldown_secs: int | None = None

    """Cooldown duration after transient errors, in seconds."""

    health_check_secs: int | None = None

    """Background health check interval, in seconds."""

    bedrock: BedrockConfig | None = None

    """AWS Bedrock configuration (region, credentials, cross-region routing)."""

    @staticmethod
    def from_json(json_str: str) -> LlmConfig:
        """Build a LlmConfig from a JSON string, via the native binding."""
        return _from_native_llm_config(_internal_bindings.LlmConfig.from_json(json_str))


@dataclass(frozen=True, slots=True)
class LlmInFlightLimitConfig:
    """Global per-client in-flight provider request limit."""

    max_in_flight: int | None = None

    """Maximum simultaneously outstanding provider requests. `None` means unlimited."""

    @staticmethod
    def from_json(json_str: str) -> LlmInFlightLimitConfig:
        """Build a LlmInFlightLimitConfig from a JSON string, via the native binding."""
        return _from_native_llm_in_flight_limit_config(_internal_bindings.LlmInFlightLimitConfig.from_json(json_str))


@dataclass(frozen=True, slots=True)
class LlmProviderConfig:
    """A custom provider configuration entry."""

    name: str = ""

    """Provider name, used to key model prefix matching."""

    base_url: str = ""

    """Base URL for the provider's OpenAI-compatible API."""

    auth_header: str | None = None

    """Header name used to carry the API key (defaults to `Authorization` when unset)."""

    model_prefixes: list[str] = field(default_factory=list)

    """Model name prefixes routed to this provider (e.g. `["my-provider/"]`)."""

    @staticmethod
    def from_json(json_str: str) -> LlmProviderConfig:
        """Build a LlmProviderConfig from a JSON string, via the native binding."""
        return _from_native_llm_provider_config(_internal_bindings.LlmProviderConfig.from_json(json_str))


@dataclass(frozen=True, slots=True)
class LlmRateLimitConfig:
    """Per-model rate limiting configuration."""

    rpm: int | None = None

    """Requests per minute limit."""

    tpm: int | None = None

    """Tokens per minute limit."""

    window_seconds: int | None = None

    """Rate limit window, in seconds."""

    @staticmethod
    def from_json(json_str: str) -> LlmRateLimitConfig:
        """Build a LlmRateLimitConfig from a JSON string, via the native binding."""
        return _from_native_llm_rate_limit_config(_internal_bindings.LlmRateLimitConfig.from_json(json_str))


@dataclass(frozen=True, slots=True)
class ModelObject:
    """A model available from the API."""

    id: str = ""

    """Model ID (e.g., `"gpt-4o"`, `"claude-3-5-sonnet"`)."""

    object: str = ""

    """Always `"model"` from OpenAI-compatible APIs.  Stored as a plain `String` so non-standard provider values do not break deserialization. Defaults to empty when a provider omits the field."""

    created: int = 0

    """Unix timestamp of model creation (or release date)."""

    owned_by: str = ""

    """Organization or entity that owns the model. Defaults to empty when a provider omits the field."""

    @staticmethod
    def from_json(json_str: str) -> ModelObject:
        """Build a ModelObject from a JSON string, via the native binding."""
        return _from_native_model_object(_internal_bindings.ModelObject.from_json(json_str))


@dataclass(frozen=True, slots=True)
class ModelTier:
    """Public, FFI-friendly snapshot of a single context-window pricing tier, projected from [`P."""

    min_context_tokens: int = 0

    """The tier applies when the prompt/context token count is at least this value."""

    input_cost_per_token: float = 0.0

    """Cost in USD per input (prompt) token within this tier."""

    output_cost_per_token: float = 0.0

    """Cost in USD per output (completion) token within this tier."""

    cache_read_input_token_cost: float | None = None

    """Cost in USD per cached input token within this tier."""

    cache_creation_input_token_cost: float | None = None

    """Cost in USD per cache-write token within this tier."""

    input_cost_per_audio_token: float | None = None

    """Cost in USD per input audio token within this tier."""

    output_cost_per_audio_token: float | None = None

    """Cost in USD per output audio token within this tier."""

    output_cost_per_reasoning_token: float | None = None

    """Cost in USD per reasoning output token within this tier."""

    @staticmethod
    def from_json(json_str: str) -> ModelTier:
        """Build a ModelTier from a JSON string, via the native binding."""
        return _from_native_model_tier(_internal_bindings.ModelTier.from_json(json_str))


@dataclass(frozen=True, slots=True)
class ModelsListResponse:
    """Response listing available models."""

    object: str = ""

    """Always `"list"` from OpenAI-compatible APIs.  Stored as a plain `String` so non-standard provider values do not break deserialization. Defaults to empty when a provider omits the field."""

    data: list[ModelObject] = field(default_factory=list)

    """List of available models."""

    @staticmethod
    def from_json(json_str: str) -> ModelsListResponse:
        """Build a ModelsListResponse from a JSON string, via the native binding."""
        return _from_native_models_list_response(_internal_bindings.ModelsListResponse.from_json(json_str))


@dataclass(frozen=True, slots=True)
class ModerationCategories:
    """Boolean flags for each moderation category."""

    sexual: bool = False

    """Sexual content."""

    hate: bool = False

    """Hate speech."""

    harassment: bool = False

    """Harassment."""

    self_harm: bool = False

    """Self-harm content."""

    sexual_minors: bool = False

    """Sexual content involving minors."""

    hate_threatening: bool = False

    """Hate speech that threatens violence."""

    violence_graphic: bool = False

    """Graphic violence."""

    self_harm_intent: bool = False

    """Intent to self-harm."""

    self_harm_instructions: bool = False

    """Instructions for self-harm."""

    harassment_threatening: bool = False

    """Harassment that threatens violence."""

    violence: bool = False

    """Non-graphic violence."""

    @staticmethod
    def from_json(json_str: str) -> ModerationCategories:
        """Build a ModerationCategories from a JSON string, via the native binding."""
        return _from_native_moderation_categories(_internal_bindings.ModerationCategories.from_json(json_str))


@dataclass(frozen=True, slots=True)
class ModerationCategoryScores:
    """Confidence scores for each moderation category."""

    sexual: float = 0.0

    """Sexual content score."""

    hate: float = 0.0

    """Hate speech score."""

    harassment: float = 0.0

    """Harassment score."""

    self_harm: float = 0.0

    """Self-harm content score."""

    sexual_minors: float = 0.0

    """Sexual content involving minors score."""

    hate_threatening: float = 0.0

    """Hate speech that threatens violence score."""

    violence_graphic: float = 0.0

    """Graphic violence score."""

    self_harm_intent: float = 0.0

    """Intent to self-harm score."""

    self_harm_instructions: float = 0.0

    """Instructions for self-harm score."""

    harassment_threatening: float = 0.0

    """Harassment that threatens violence score."""

    violence: float = 0.0

    """Non-graphic violence score."""

    @staticmethod
    def from_json(json_str: str) -> ModerationCategoryScores:
        """Build a ModerationCategoryScores from a JSON string, via the native binding."""
        return _from_native_moderation_category_scores(_internal_bindings.ModerationCategoryScores.from_json(json_str))


@dataclass(frozen=True, slots=True)
class ModerationRequest:
    """Request to classify content for policy violations."""

    input: ModerationInput | None = None

    """Text or texts to check."""

    model: str | None = None

    """Model ID (e.g., `"text-moderation-latest"`). Optional; API uses default if unset."""

    @staticmethod
    def from_json(json_str: str) -> ModerationRequest:
        """Build a ModerationRequest from a JSON string, via the native binding."""
        return _from_native_moderation_request(_internal_bindings.ModerationRequest.from_json(json_str))


@dataclass(frozen=True, slots=True)
class ModerationResult:
    """A single moderation classification result."""

    flagged: bool

    """True if any category was flagged."""

    categories: ModerationCategories

    """Boolean flags for each moderation category."""

    category_scores: ModerationCategoryScores

    """Confidence scores for each category."""

    @staticmethod
    def from_json(json_str: str) -> ModerationResult:
        """Build a ModerationResult from a JSON string, via the native binding."""
        return _from_native_moderation_result(_internal_bindings.ModerationResult.from_json(json_str))


@dataclass(frozen=True, slots=True)
class OcrRequest:
    """An OCR request."""

    model: str = ""

    """The model/provider to use (e.g. `"mistral/mistral-ocr-latest"`)."""

    document: OcrDocument | None = None

    """The document to process (URL or base64)."""

    pages: list[int] | None = None

    """Specific pages to process (1-indexed). `None` means all pages."""

    include_image_base64: bool | None = None

    """Whether to include base64-encoded images of each processed page."""

    @staticmethod
    def from_json(json_str: str) -> OcrRequest:
        """Build an OcrRequest from a JSON string, via the native binding."""
        return _from_native_ocr_request(_internal_bindings.OcrRequest.from_json(json_str))


@dataclass(frozen=True, slots=True)
class PromptTokensDetails:
    """Breakdown of tokens used in the prompt portion of a request."""

    cached_tokens: int = 0

    """Cached tokens present in the prompt. Defaults to 0 when absent."""

    audio_tokens: int = 0

    """Audio input tokens present in the prompt. Defaults to 0 when absent."""

    @staticmethod
    def from_json(json_str: str) -> PromptTokensDetails:
        """Build a PromptTokensDetails from a JSON string, via the native binding."""
        return _from_native_prompt_tokens_details(_internal_bindings.PromptTokensDetails.from_json(json_str))


@dataclass(frozen=True, slots=True)
class RateLimitConfig:
    """Configuration for per-model rate limits."""

    rpm: int | None = None

    """Maximum requests per window.  `None` means unlimited."""

    tpm: int | None = None

    """Maximum tokens per window.  `None` means unlimited."""

    window: int = 60000

    """Fixed window duration (defaults to 60 s)."""

    @staticmethod
    def from_json(json_str: str) -> RateLimitConfig:
        """Build a RateLimitConfig from a JSON string, via the native binding."""
        return _from_native_rate_limit_config(_internal_bindings.RateLimitConfig.from_json(json_str))


@dataclass(frozen=True, slots=True)
class RerankRequest:
    """Request to rerank documents by relevance to a query."""

    model: str = ""

    """Model ID (e.g., `"cohere/rerank-english-v3.0"`)."""

    query: str = ""

    """The search query."""

    documents: list[RerankDocument] = field(default_factory=list)

    """Documents to rerank."""

    top_n: int | None = None

    """Return only the top N results. Optional."""

    return_documents: bool | None = None

    """Include the document content in results. Defaults to false."""

    @staticmethod
    def from_json(json_str: str) -> RerankRequest:
        """Build a RerankRequest from a JSON string, via the native binding."""
        return _from_native_rerank_request(_internal_bindings.RerankRequest.from_json(json_str))


@dataclass(frozen=True, slots=True)
class ResponseObject:
    """Response from a structured response request."""

    id: str = ""

    """Unique response ID."""

    object: str = ""

    """Object type (e.g., `"response"`)."""

    created_at: int = 0

    """Unix timestamp of response creation."""

    model: str = ""

    """Model used to generate the response."""

    status: str = ""

    """Status (e.g., `"succeeded"`, `"failed"`)."""

    output: list[ResponseOutputItem] = field(default_factory=list)

    """Output items from the response."""

    usage: ResponseUsage | None = None

    """Token usage."""

    error: str | None = None

    """Error details (if status is "failed")."""

    @staticmethod
    def from_json(json_str: str) -> ResponseObject:
        """Build a ResponseObject from a JSON string, via the native binding."""
        return _from_native_response_object(_internal_bindings.ResponseObject.from_json(json_str))


@dataclass(frozen=True, slots=True)
class ResponseOutputItem:
    """A single output item from the response."""

    item_type: str = ""

    """Output type (e.g., `"text"`, `"object"`, `"error"`)."""

    content: str | None = None

    """Output content (flattened into the object)."""

    @staticmethod
    def from_json(json_str: str) -> ResponseOutputItem:
        """Build a ResponseOutputItem from a JSON string, via the native binding."""
        return _from_native_response_output_item(_internal_bindings.ResponseOutputItem.from_json(json_str))


@dataclass(frozen=True, slots=True)
class ResponseTool:
    """A tool available for the response request."""

    tool_type: str = ""

    """Tool type (e.g., "extractor", "search")."""

    config: str | None = None

    """Tool configuration (flattened into the object)."""

    @staticmethod
    def from_json(json_str: str) -> ResponseTool:
        """Build a ResponseTool from a JSON string, via the native binding."""
        return _from_native_response_tool(_internal_bindings.ResponseTool.from_json(json_str))


@dataclass(frozen=True, slots=True)
class ResponseUsage:
    """Token usage for a response."""

    input_tokens: int = 0

    """Input tokens used."""

    output_tokens: int = 0

    """Output tokens used."""

    total_tokens: int = 0

    """Total tokens used."""

    @staticmethod
    def from_json(json_str: str) -> ResponseUsage:
        """Build a ResponseUsage from a JSON string, via the native binding."""
        return _from_native_response_usage(_internal_bindings.ResponseUsage.from_json(json_str))


@dataclass(frozen=True, slots=True)
class SearchRequest:
    """A search request."""

    model: str = ""

    """The model/provider to use (e.g. `"brave/web-search"`, `"tavily/search"`)."""

    query: str = ""

    """The search query string."""

    max_results: int | None = None

    """Maximum number of results to return."""

    search_domain_filter: list[str] | None = None

    """Domain filter -- restrict results to specific domains."""

    country: str | None = None

    """Country code for localized results (ISO 3166-1 alpha-2, e.g., `"US"`, `"FR"`)."""

    @staticmethod
    def from_json(json_str: str) -> SearchRequest:
        """Build a SearchRequest from a JSON string, via the native binding."""
        return _from_native_search_request(_internal_bindings.SearchRequest.from_json(json_str))


@dataclass(frozen=True, slots=True)
class SpecificFunction:
    """Name of the specific function to invoke."""

    name: str = ""

    """Function name."""

    @staticmethod
    def from_json(json_str: str) -> SpecificFunction:
        """Build a SpecificFunction from a JSON string, via the native binding."""
        return _from_native_specific_function(_internal_bindings.SpecificFunction.from_json(json_str))


@dataclass(frozen=True, slots=True)
class SpecificToolChoice:
    """Directive to call a specific tool."""

    choice_type: ToolType | str = "function"

    """Tool type (always "function")."""

    function: SpecificFunction | None = None

    """The specific function to invoke."""

    @staticmethod
    def from_json(json_str: str) -> SpecificToolChoice:
        """Build a SpecificToolChoice from a JSON string, via the native binding."""
        return _from_native_specific_tool_choice(_internal_bindings.SpecificToolChoice.from_json(json_str))


@dataclass(frozen=True, slots=True)
class StreamChoice:
    """A streaming choice with incremental delta."""

    index: int = 0

    """Index of this choice in the choices array."""

    delta: StreamDelta | None = None

    """Incremental update to the message (content, tool calls, etc.)."""

    finish_reason: FinishReason | str | None = None

    """Why the stream ended (present only in final chunk)."""

    @staticmethod
    def from_json(json_str: str) -> StreamChoice:
        """Build a StreamChoice from a JSON string, via the native binding."""
        return _from_native_stream_choice(_internal_bindings.StreamChoice.from_json(json_str))


@dataclass(frozen=True, slots=True)
class StreamDelta:
    """Incremental delta in a stream chunk."""

    role: str | None = None

    """Role (typically present only in the first chunk)."""

    content: str | None = None

    """Partial content chunk (e.g., a few words of the response)."""

    tool_calls: list[StreamToolCall] | None = None

    """Partial tool calls being streamed."""

    function_call: StreamFunctionCall | None = None

    """Deprecated legacy function_call delta; retained for API compatibility."""

    refusal: str | None = None

    """Partial refusal message."""

    reasoning_content: str | None = None

    """Partial reasoning/thinking tokens (OpenAI-compatible extension used by DeepSeek R1, Qwen, etc.)."""

    @staticmethod
    def from_json(json_str: str) -> StreamDelta:
        """Build a StreamDelta from a JSON string, via the native binding."""
        return _from_native_stream_delta(_internal_bindings.StreamDelta.from_json(json_str))


@dataclass(frozen=True, slots=True)
class StreamFunctionCall:
    """Partial function call details in a stream."""

    name: str | None = None

    """Function name (typically in the first chunk)."""

    arguments: str | None = None

    """Partial JSON arguments chunk."""

    @staticmethod
    def from_json(json_str: str) -> StreamFunctionCall:
        """Build a StreamFunctionCall from a JSON string, via the native binding."""
        return _from_native_stream_function_call(_internal_bindings.StreamFunctionCall.from_json(json_str))


@dataclass(frozen=True, slots=True)
class StreamOptions:
    """Options for streaming responses."""

    include_usage: bool | None = None

    """If true, include token usage in the final stream chunk."""

    @staticmethod
    def from_json(json_str: str) -> StreamOptions:
        """Build a StreamOptions from a JSON string, via the native binding."""
        return _from_native_stream_options(_internal_bindings.StreamOptions.from_json(json_str))


@dataclass(frozen=True, slots=True)
class StreamToolCall:
    """A streaming tool call being built incrementally."""

    index: int = 0

    """Index of this tool call in the tool_calls array."""

    id: str | None = None

    """Tool call ID (typically in the first chunk for this call)."""

    call_type: ToolType | str | None = None

    """Tool type (typically "function")."""

    function: StreamFunctionCall | None = None

    """Partial function name and arguments."""

    @staticmethod
    def from_json(json_str: str) -> StreamToolCall:
        """Build a StreamToolCall from a JSON string, via the native binding."""
        return _from_native_stream_tool_call(_internal_bindings.StreamToolCall.from_json(json_str))


@dataclass(frozen=True, slots=True)
class SystemMessage:
    """System message guiding model behavior for the entire conversation."""

    content: UserContent | None = None

    """Instructions or context that apply throughout the conversation."""

    name: str | None = None

    """Optional name for the system message source."""

    @staticmethod
    def from_json(json_str: str) -> SystemMessage:
        """Build a SystemMessage from a JSON string, via the native binding."""
        return _from_native_system_message(_internal_bindings.SystemMessage.from_json(json_str))


@dataclass(frozen=True, slots=True)
class ToolMessage:
    """Tool execution result returned to the model."""

    content: UserContent | None = None

    """Result of the tool execution as plain text or an array of content parts (text, images, documents, audio), mirroring [`UserMessage::content`]."""

    tool_call_id: str = ""

    """ID of the tool call this result responds to."""

    name: str | None = None

    """Optional tool/function name."""

    @staticmethod
    def from_json(json_str: str) -> ToolMessage:
        """Build a ToolMessage from a JSON string, via the native binding."""
        return _from_native_tool_message(_internal_bindings.ToolMessage.from_json(json_str))


@dataclass(frozen=True, slots=True)
class TranscriptionResponse:
    """Response from a transcription request."""

    text: str = ""

    """The transcribed text."""

    language: str | None = None

    """Detected language (ISO-639-1 code)."""

    duration: float | None = None

    """Total audio duration in seconds."""

    segments: list[TranscriptionSegment] | None = None

    """Detailed segment-level transcription (if response_format is "verbose_json")."""

    @staticmethod
    def from_json(json_str: str) -> TranscriptionResponse:
        """Build a TranscriptionResponse from a JSON string, via the native binding."""
        return _from_native_transcription_response(_internal_bindings.TranscriptionResponse.from_json(json_str))


@dataclass(frozen=True, slots=True)
class TranscriptionSegment:
    """A segment of transcribed audio with timing information."""

    id: int = 0

    """Segment index (0-based)."""

    start: float = 0.0

    """Start time in seconds."""

    end: float = 0.0

    """End time in seconds."""

    text: str = ""

    """Transcribed text for this segment."""

    @staticmethod
    def from_json(json_str: str) -> TranscriptionSegment:
        """Build a TranscriptionSegment from a JSON string, via the native binding."""
        return _from_native_transcription_segment(_internal_bindings.TranscriptionSegment.from_json(json_str))


@dataclass(frozen=True, slots=True)
class Usage:
    """Token-usage accounting returned by the provider on each completion / embedding call."""

    prompt_tokens: int = 0

    """Prompt tokens used. Defaults to 0 when absent (some providers omit this)."""

    completion_tokens: int = 0

    """Completion tokens used. Defaults to 0 when absent (e.g. embedding responses)."""

    total_tokens: int = 0

    """Total tokens used. Defaults to 0 when absent (some providers omit this)."""

    prompt_tokens_details: PromptTokensDetails | None = None

    """Breakdown of tokens used in the prompt, including cached tokens served at the provider's discounted cache-read rate. Absent when the provider does not return prompt-token details."""

    @staticmethod
    def from_json(json_str: str) -> Usage:
        """Build an Usage from a JSON string, via the native binding."""
        return _from_native_usage(_internal_bindings.Usage.from_json(json_str))


@dataclass(frozen=True, slots=True)
class UserMessage:
    """User message in the conversation."""

    content: UserContent | None = None

    """Message content as plain text or array of content parts (text, images, documents, audio)."""

    name: str | None = None

    """Optional name for the user."""

    @staticmethod
    def from_json(json_str: str) -> UserMessage:
        """Build an UserMessage from a JSON string, via the native binding."""
        return _from_native_user_message(_internal_bindings.UserMessage.from_json(json_str))


@dataclass(frozen=True, slots=True)
class WaitForBatchConfig:
    """Configuration for polling a batch until terminal status."""

    initial_interval_secs: float = 5

    """Initial interval between polls, in seconds."""

    max_interval_secs: float = 60

    """Maximum interval between polls (backoff plateau), in seconds."""

    backoff_multiplier: float = 1.5

    """Exponential backoff multiplier (e.g., 1.5 increases delay by 50% each poll)."""

    timeout_secs: float | None = None

    """Optional timeout in seconds -- polling fails if this duration is exceeded."""

    @staticmethod
    def from_json(json_str: str) -> WaitForBatchConfig:
        """Build a WaitForBatchConfig from a JSON string, via the native binding."""
        return _from_native_wait_for_batch_config(_internal_bindings.WaitForBatchConfig.from_json(json_str))


def _from_native_assistant_message(native: Any) -> AssistantMessage:
    """Build the public `AssistantMessage` dataclass from the native binding object."""
    return AssistantMessage(
        content=native.content,
        name=native.name,
        tool_calls=native.tool_calls,
        refusal=native.refusal,
        function_call=native.function_call,
        reasoning_content=native.reasoning_content,
    )


def _from_native_audio_content(native: Any) -> AudioContent:
    """Build the public `AudioContent` dataclass from the native binding object."""
    return AudioContent(
        data=native.data,
        format=native.format,
    )


def _from_native_batch_list_query(native: Any) -> BatchListQuery:
    """Build the public `BatchListQuery` dataclass from the native binding object."""
    return BatchListQuery(
        limit=native.limit,
        after=native.after,
    )


def _from_native_batch_list_response(native: Any) -> BatchListResponse:
    """Build the public `BatchListResponse` dataclass from the native binding object."""
    return BatchListResponse(
        object=native.object,
        data=[_from_native_batch_object(__v) for __v in native.data],
        has_more=native.has_more,
        first_id=native.first_id,
        last_id=native.last_id,
    )


def _from_native_batch_object(native: Any) -> BatchObject:
    """Build the public `BatchObject` dataclass from the native binding object."""
    return BatchObject(
        id=native.id,
        object=native.object,
        endpoint=native.endpoint,
        input_file_id=native.input_file_id,
        completion_window=native.completion_window,
        status=native.status,
        output_file_id=native.output_file_id,
        error_file_id=native.error_file_id,
        created_at=native.created_at,
        completed_at=native.completed_at,
        failed_at=native.failed_at,
        expired_at=native.expired_at,
        request_counts=(None if native.request_counts is None else _from_native_batch_request_counts(native.request_counts)),
        metadata=native.metadata,
    )


def _from_native_batch_request_counts(native: Any) -> BatchRequestCounts:
    """Build the public `BatchRequestCounts` dataclass from the native binding object."""
    return BatchRequestCounts(
        total=native.total,
        completed=native.completed,
        failed=native.failed,
    )


def _from_native_bedrock_config(native: Any) -> BedrockConfig:
    """Build the public `BedrockConfig` dataclass from the native binding object."""
    return BedrockConfig(
        region=native.region,
        cross_region_prefix=native.cross_region_prefix,
        access_key_id=native.access_key_id,
        secret_access_key=native.secret_access_key,
        session_token=native.session_token,
    )


def _from_native_budget_config(native: Any) -> BudgetConfig:
    """Build the public `BudgetConfig` dataclass from the native binding object."""
    return BudgetConfig(
        global_limit=native.global_limit,
        model_limits=native.model_limits,
        enforcement=native.enforcement,
    )


def _from_native_cache_config(native: Any) -> CacheConfig:
    """Build the public `CacheConfig` dataclass from the native binding object."""
    return CacheConfig(
        max_entries=native.max_entries,
        ttl=native.ttl,
        backend=native.backend,
    )


def _from_native_catalog_refresh_config(native: Any) -> CatalogRefreshConfig:
    """Build the public `CatalogRefreshConfig` dataclass from the native binding object."""
    return CatalogRefreshConfig(
        enabled=native.enabled,
        source_url=native.source_url,
        ttl_seconds=native.ttl_seconds,
        cache_path=native.cache_path,
    )


def _from_native_chat_completion_chunk(native: Any) -> ChatCompletionChunk:
    """Build the public `ChatCompletionChunk` dataclass from the native binding object."""
    return ChatCompletionChunk(
        id=native.id,
        object=native.object,
        created=native.created,
        model=native.model,
        choices=[_from_native_stream_choice(__v) for __v in native.choices],
        usage=(None if native.usage is None else _from_native_usage(native.usage)),
        system_fingerprint=native.system_fingerprint,
        service_tier=native.service_tier,
    )


def _from_native_chat_completion_request(native: Any) -> ChatCompletionRequest:
    """Build the public `ChatCompletionRequest` dataclass from the native binding object."""
    return ChatCompletionRequest(
        model=native.model,
        messages=native.messages,
        temperature=native.temperature,
        top_p=native.top_p,
        n=native.n,
        stream=native.stream,
        stop=native.stop,
        max_tokens=native.max_tokens,
        presence_penalty=native.presence_penalty,
        frequency_penalty=native.frequency_penalty,
        logit_bias=native.logit_bias,
        user=native.user,
        tools=native.tools,
        tool_choice=native.tool_choice,
        parallel_tool_calls=native.parallel_tool_calls,
        response_format=native.response_format,
        stream_options=(None if native.stream_options is None else _from_native_stream_options(native.stream_options)),
        seed=native.seed,
        reasoning_effort=native.reasoning_effort,
        modalities=native.modalities,
        logprobs=native.logprobs,
        top_logprobs=native.top_logprobs,
        max_completion_tokens=native.max_completion_tokens,
        service_tier=native.service_tier,
        store=native.store,
        metadata=native.metadata,
        prediction=native.prediction,
        audio=native.audio,
        web_search_options=native.web_search_options,
        extra_body=native.extra_body,
    )


def _from_native_chat_completion_response(native: Any) -> ChatCompletionResponse:
    """Build the public `ChatCompletionResponse` dataclass from the native binding object."""
    return ChatCompletionResponse(
        id=native.id,
        object=native.object,
        created=native.created,
        model=native.model,
        choices=[_from_native_choice(__v) for __v in native.choices],
        usage=(None if native.usage is None else _from_native_usage(native.usage)),
        system_fingerprint=native.system_fingerprint,
        service_tier=native.service_tier,
    )


def _from_native_choice(native: Any) -> Choice:
    """Build the public `Choice` dataclass from the native binding object."""
    return Choice(
        index=native.index,
        message=_from_native_assistant_message(native.message),
        finish_reason=native.finish_reason,
        logprobs=native.logprobs,
    )


def _from_native_create_batch_request(native: Any) -> CreateBatchRequest:
    """Build the public `CreateBatchRequest` dataclass from the native binding object."""
    return CreateBatchRequest(
        input_file_id=native.input_file_id,
        endpoint=native.endpoint,
        completion_window=native.completion_window,
        metadata=native.metadata,
    )


def _from_native_create_file_request(native: Any) -> CreateFileRequest:
    """Build the public `CreateFileRequest` dataclass from the native binding object."""
    return CreateFileRequest(
        file=native.file,
        purpose=native.purpose,
        filename=native.filename,
    )


def _from_native_create_image_request(native: Any) -> CreateImageRequest:
    """Build the public `CreateImageRequest` dataclass from the native binding object."""
    return CreateImageRequest(
        prompt=native.prompt,
        model=native.model,
        n=native.n,
        size=native.size,
        quality=native.quality,
        style=native.style,
        response_format=native.response_format,
        user=native.user,
    )


def _from_native_create_response_request(native: Any) -> CreateResponseRequest:
    """Build the public `CreateResponseRequest` dataclass from the native binding object."""
    return CreateResponseRequest(
        model=native.model,
        input=native.input,
        instructions=native.instructions,
        tools=(None if native.tools is None else [_from_native_response_tool(__v) for __v in native.tools]),
        temperature=native.temperature,
        max_output_tokens=native.max_output_tokens,
        metadata=native.metadata,
        extra_body=native.extra_body,
        stream=native.stream,
    )


def _from_native_create_speech_request(native: Any) -> CreateSpeechRequest:
    """Build the public `CreateSpeechRequest` dataclass from the native binding object."""
    return CreateSpeechRequest(
        model=native.model,
        input=native.input,
        voice=native.voice,
        response_format=native.response_format,
        speed=native.speed,
    )


def _from_native_create_transcription_request(native: Any) -> CreateTranscriptionRequest:
    """Build the public `CreateTranscriptionRequest` dataclass from the native binding object."""
    return CreateTranscriptionRequest(
        model=native.model,
        file=native.file,
        language=native.language,
        prompt=native.prompt,
        response_format=native.response_format,
        temperature=native.temperature,
    )


def _from_native_delete_response(native: Any) -> DeleteResponse:
    """Build the public `DeleteResponse` dataclass from the native binding object."""
    return DeleteResponse(
        id=native.id,
        object=native.object,
        deleted=native.deleted,
    )


def _from_native_developer_message(native: Any) -> DeveloperMessage:
    """Build the public `DeveloperMessage` dataclass from the native binding object."""
    return DeveloperMessage(
        content=native.content,
        name=native.name,
    )


def _from_native_document_content(native: Any) -> DocumentContent:
    """Build the public `DocumentContent` dataclass from the native binding object."""
    return DocumentContent(
        data=native.data,
        media_type=native.media_type,
    )


def _from_native_embedding_request(native: Any) -> EmbeddingRequest:
    """Build the public `EmbeddingRequest` dataclass from the native binding object."""
    return EmbeddingRequest(
        model=native.model,
        input=native.input,
        encoding_format=native.encoding_format,
        dimensions=native.dimensions,
        user=native.user,
    )


def _from_native_file_list_query(native: Any) -> FileListQuery:
    """Build the public `FileListQuery` dataclass from the native binding object."""
    return FileListQuery(
        purpose=native.purpose,
        limit=native.limit,
        after=native.after,
    )


def _from_native_file_list_response(native: Any) -> FileListResponse:
    """Build the public `FileListResponse` dataclass from the native binding object."""
    return FileListResponse(
        object=native.object,
        data=[_from_native_file_object(__v) for __v in native.data],
        has_more=native.has_more,
    )


def _from_native_file_object(native: Any) -> FileObject:
    """Build the public `FileObject` dataclass from the native binding object."""
    return FileObject(
        id=native.id,
        object=native.object,
        bytes=native.bytes,
        created_at=native.created_at,
        filename=native.filename,
        purpose=native.purpose,
        status=native.status,
    )


def _from_native_function_message(native: Any) -> FunctionMessage:
    """Build the public `FunctionMessage` dataclass from the native binding object."""
    return FunctionMessage(
        content=native.content,
        name=native.name,
    )


def _from_native_image(native: Any) -> Image:
    """Build the public `Image` dataclass from the native binding object."""
    return Image(
        url=native.url,
        b64_json=native.b64_json,
        revised_prompt=native.revised_prompt,
    )


def _from_native_image_url(native: Any) -> ImageUrl:
    """Build the public `ImageUrl` dataclass from the native binding object."""
    return ImageUrl(
        url=native.url,
        detail=native.detail,
    )


def _from_native_images_response(native: Any) -> ImagesResponse:
    """Build the public `ImagesResponse` dataclass from the native binding object."""
    return ImagesResponse(
        created=native.created,
        data=[_from_native_image(__v) for __v in native.data],
    )


def _from_native_in_flight_limit_config(native: Any) -> InFlightLimitConfig:
    """Build the public `InFlightLimitConfig` dataclass from the native binding object."""
    return InFlightLimitConfig(
        max_in_flight=native.max_in_flight,
    )


def _from_native_json_schema_format(native: Any) -> JsonSchemaFormat:
    """Build the public `JsonSchemaFormat` dataclass from the native binding object."""
    return JsonSchemaFormat(
        name=native.name,
        description=native.description,
        schema=native.schema,
        strict=native.strict,
    )


def _from_native_llm_budget_config(native: Any) -> LlmBudgetConfig:
    """Build the public `LlmBudgetConfig` dataclass from the native binding object."""
    return LlmBudgetConfig(
        global_limit=native.global_limit,
        model_limits=native.model_limits,
        enforcement=native.enforcement,
    )


def _from_native_llm_cache_config(native: Any) -> LlmCacheConfig:
    """Build the public `LlmCacheConfig` dataclass from the native binding object."""
    return LlmCacheConfig(
        max_entries=native.max_entries,
        ttl_seconds=native.ttl_seconds,
        backend=native.backend,
        backend_config=native.backend_config,
    )


def _from_native_llm_config(native: Any) -> LlmConfig:
    """Build the public `LlmConfig` dataclass from the native binding object."""
    return LlmConfig(
        model=native.model,
        api_key=native.api_key,
        base_url=native.base_url,
        timeout_secs=native.timeout_secs,
        max_retries=native.max_retries,
        temperature=native.temperature,
        max_tokens=native.max_tokens,
        load_env=native.load_env,
        headers=native.headers,
        providers=(None if native.providers is None else [_from_native_llm_provider_config(__v) for __v in native.providers]),
        cache=(None if native.cache is None else _from_native_llm_cache_config(native.cache)),
        budget=(None if native.budget is None else _from_native_llm_budget_config(native.budget)),
        rate_limit=(None if native.rate_limit is None else _from_native_llm_rate_limit_config(native.rate_limit)),
        in_flight_limit=(None if native.in_flight_limit is None else _from_native_llm_in_flight_limit_config(native.in_flight_limit)),
        cost_tracking=native.cost_tracking,
        tracing=native.tracing,
        cooldown_secs=native.cooldown_secs,
        health_check_secs=native.health_check_secs,
        bedrock=(None if native.bedrock is None else _from_native_bedrock_config(native.bedrock)),
    )


def _from_native_llm_in_flight_limit_config(native: Any) -> LlmInFlightLimitConfig:
    """Build the public `LlmInFlightLimitConfig` dataclass from the native binding object."""
    return LlmInFlightLimitConfig(
        max_in_flight=native.max_in_flight,
    )


def _from_native_llm_provider_config(native: Any) -> LlmProviderConfig:
    """Build the public `LlmProviderConfig` dataclass from the native binding object."""
    return LlmProviderConfig(
        name=native.name,
        base_url=native.base_url,
        auth_header=native.auth_header,
        model_prefixes=native.model_prefixes,
    )


def _from_native_llm_rate_limit_config(native: Any) -> LlmRateLimitConfig:
    """Build the public `LlmRateLimitConfig` dataclass from the native binding object."""
    return LlmRateLimitConfig(
        rpm=native.rpm,
        tpm=native.tpm,
        window_seconds=native.window_seconds,
    )


def _from_native_model_object(native: Any) -> ModelObject:
    """Build the public `ModelObject` dataclass from the native binding object."""
    return ModelObject(
        id=native.id,
        object=native.object,
        created=native.created,
        owned_by=native.owned_by,
    )


def _from_native_model_tier(native: Any) -> ModelTier:
    """Build the public `ModelTier` dataclass from the native binding object."""
    return ModelTier(
        min_context_tokens=native.min_context_tokens,
        input_cost_per_token=native.input_cost_per_token,
        output_cost_per_token=native.output_cost_per_token,
        cache_read_input_token_cost=native.cache_read_input_token_cost,
        cache_creation_input_token_cost=native.cache_creation_input_token_cost,
        input_cost_per_audio_token=native.input_cost_per_audio_token,
        output_cost_per_audio_token=native.output_cost_per_audio_token,
        output_cost_per_reasoning_token=native.output_cost_per_reasoning_token,
    )


def _from_native_models_list_response(native: Any) -> ModelsListResponse:
    """Build the public `ModelsListResponse` dataclass from the native binding object."""
    return ModelsListResponse(
        object=native.object,
        data=[_from_native_model_object(__v) for __v in native.data],
    )


def _from_native_moderation_categories(native: Any) -> ModerationCategories:
    """Build the public `ModerationCategories` dataclass from the native binding object."""
    return ModerationCategories(
        sexual=native.sexual,
        hate=native.hate,
        harassment=native.harassment,
        self_harm=native.self_harm,
        sexual_minors=native.sexual_minors,
        hate_threatening=native.hate_threatening,
        violence_graphic=native.violence_graphic,
        self_harm_intent=native.self_harm_intent,
        self_harm_instructions=native.self_harm_instructions,
        harassment_threatening=native.harassment_threatening,
        violence=native.violence,
    )


def _from_native_moderation_category_scores(native: Any) -> ModerationCategoryScores:
    """Build the public `ModerationCategoryScores` dataclass from the native binding object."""
    return ModerationCategoryScores(
        sexual=native.sexual,
        hate=native.hate,
        harassment=native.harassment,
        self_harm=native.self_harm,
        sexual_minors=native.sexual_minors,
        hate_threatening=native.hate_threatening,
        violence_graphic=native.violence_graphic,
        self_harm_intent=native.self_harm_intent,
        self_harm_instructions=native.self_harm_instructions,
        harassment_threatening=native.harassment_threatening,
        violence=native.violence,
    )


def _from_native_moderation_request(native: Any) -> ModerationRequest:
    """Build the public `ModerationRequest` dataclass from the native binding object."""
    return ModerationRequest(
        input=native.input,
        model=native.model,
    )


def _from_native_moderation_result(native: Any) -> ModerationResult:
    """Build the public `ModerationResult` dataclass from the native binding object."""
    return ModerationResult(
        flagged=native.flagged,
        categories=_from_native_moderation_categories(native.categories),
        category_scores=_from_native_moderation_category_scores(native.category_scores),
    )


def _from_native_ocr_request(native: Any) -> OcrRequest:
    """Build the public `OcrRequest` dataclass from the native binding object."""
    return OcrRequest(
        model=native.model,
        document=native.document,
        pages=native.pages,
        include_image_base64=native.include_image_base64,
    )


def _from_native_prompt_tokens_details(native: Any) -> PromptTokensDetails:
    """Build the public `PromptTokensDetails` dataclass from the native binding object."""
    return PromptTokensDetails(
        cached_tokens=native.cached_tokens,
        audio_tokens=native.audio_tokens,
    )


def _from_native_rate_limit_config(native: Any) -> RateLimitConfig:
    """Build the public `RateLimitConfig` dataclass from the native binding object."""
    return RateLimitConfig(
        rpm=native.rpm,
        tpm=native.tpm,
        window=native.window,
    )


def _from_native_rerank_request(native: Any) -> RerankRequest:
    """Build the public `RerankRequest` dataclass from the native binding object."""
    return RerankRequest(
        model=native.model,
        query=native.query,
        documents=native.documents,
        top_n=native.top_n,
        return_documents=native.return_documents,
    )


def _from_native_response_object(native: Any) -> ResponseObject:
    """Build the public `ResponseObject` dataclass from the native binding object."""
    return ResponseObject(
        id=native.id,
        object=native.object,
        created_at=native.created_at,
        model=native.model,
        status=native.status,
        output=[_from_native_response_output_item(__v) for __v in native.output],
        usage=(None if native.usage is None else _from_native_response_usage(native.usage)),
        error=native.error,
    )


def _from_native_response_output_item(native: Any) -> ResponseOutputItem:
    """Build the public `ResponseOutputItem` dataclass from the native binding object."""
    return ResponseOutputItem(
        item_type=native.item_type,
        content=native.content,
    )


def _from_native_response_tool(native: Any) -> ResponseTool:
    """Build the public `ResponseTool` dataclass from the native binding object."""
    return ResponseTool(
        tool_type=native.tool_type,
        config=native.config,
    )


def _from_native_response_usage(native: Any) -> ResponseUsage:
    """Build the public `ResponseUsage` dataclass from the native binding object."""
    return ResponseUsage(
        input_tokens=native.input_tokens,
        output_tokens=native.output_tokens,
        total_tokens=native.total_tokens,
    )


def _from_native_search_request(native: Any) -> SearchRequest:
    """Build the public `SearchRequest` dataclass from the native binding object."""
    return SearchRequest(
        model=native.model,
        query=native.query,
        max_results=native.max_results,
        search_domain_filter=native.search_domain_filter,
        country=native.country,
    )


def _from_native_specific_function(native: Any) -> SpecificFunction:
    """Build the public `SpecificFunction` dataclass from the native binding object."""
    return SpecificFunction(
        name=native.name,
    )


def _from_native_specific_tool_choice(native: Any) -> SpecificToolChoice:
    """Build the public `SpecificToolChoice` dataclass from the native binding object."""
    return SpecificToolChoice(
        choice_type=native.choice_type,
        function=_from_native_specific_function(native.function),
    )


def _from_native_stream_choice(native: Any) -> StreamChoice:
    """Build the public `StreamChoice` dataclass from the native binding object."""
    return StreamChoice(
        index=native.index,
        delta=_from_native_stream_delta(native.delta),
        finish_reason=native.finish_reason,
    )


def _from_native_stream_delta(native: Any) -> StreamDelta:
    """Build the public `StreamDelta` dataclass from the native binding object."""
    return StreamDelta(
        role=native.role,
        content=native.content,
        tool_calls=(None if native.tool_calls is None else [_from_native_stream_tool_call(__v) for __v in native.tool_calls]),
        function_call=(None if native.function_call is None else _from_native_stream_function_call(native.function_call)),
        refusal=native.refusal,
        reasoning_content=native.reasoning_content,
    )


def _from_native_stream_function_call(native: Any) -> StreamFunctionCall:
    """Build the public `StreamFunctionCall` dataclass from the native binding object."""
    return StreamFunctionCall(
        name=native.name,
        arguments=native.arguments,
    )


def _from_native_stream_options(native: Any) -> StreamOptions:
    """Build the public `StreamOptions` dataclass from the native binding object."""
    return StreamOptions(
        include_usage=native.include_usage,
    )


def _from_native_stream_tool_call(native: Any) -> StreamToolCall:
    """Build the public `StreamToolCall` dataclass from the native binding object."""
    return StreamToolCall(
        index=native.index,
        id=native.id,
        call_type=native.call_type,
        function=(None if native.function is None else _from_native_stream_function_call(native.function)),
    )


def _from_native_system_message(native: Any) -> SystemMessage:
    """Build the public `SystemMessage` dataclass from the native binding object."""
    return SystemMessage(
        content=native.content,
        name=native.name,
    )


def _from_native_tool_message(native: Any) -> ToolMessage:
    """Build the public `ToolMessage` dataclass from the native binding object."""
    return ToolMessage(
        content=native.content,
        tool_call_id=native.tool_call_id,
        name=native.name,
    )


def _from_native_transcription_response(native: Any) -> TranscriptionResponse:
    """Build the public `TranscriptionResponse` dataclass from the native binding object."""
    return TranscriptionResponse(
        text=native.text,
        language=native.language,
        duration=native.duration,
        segments=(None if native.segments is None else [_from_native_transcription_segment(__v) for __v in native.segments]),
    )


def _from_native_transcription_segment(native: Any) -> TranscriptionSegment:
    """Build the public `TranscriptionSegment` dataclass from the native binding object."""
    return TranscriptionSegment(
        id=native.id,
        start=native.start,
        end=native.end,
        text=native.text,
    )


def _from_native_usage(native: Any) -> Usage:
    """Build the public `Usage` dataclass from the native binding object."""
    return Usage(
        prompt_tokens=native.prompt_tokens,
        completion_tokens=native.completion_tokens,
        total_tokens=native.total_tokens,
        prompt_tokens_details=(None if native.prompt_tokens_details is None else _from_native_prompt_tokens_details(native.prompt_tokens_details)),
    )


def _from_native_user_message(native: Any) -> UserMessage:
    """Build the public `UserMessage` dataclass from the native binding object."""
    return UserMessage(
        content=native.content,
        name=native.name,
    )


def _from_native_wait_for_batch_config(native: Any) -> WaitForBatchConfig:
    """Build the public `WaitForBatchConfig` dataclass from the native binding object."""
    return WaitForBatchConfig(
        initial_interval_secs=native.initial_interval_secs,
        max_interval_secs=native.max_interval_secs,
        backoff_multiplier=native.backoff_multiplier,
        timeout_secs=native.timeout_secs,
    )
