// This file is auto-generated by alef. DO NOT EDIT.
// alef:hash:c424c5189652152b6e17a61ed02c993089e534994764553587cfd7de9a167991
#nullable enable

using System;
using Microsoft.Win32.SafeHandles;
using System.Runtime.InteropServices;
using System.Text.Json;
using System.Text.Json.Serialization;
using System.Collections.Generic;
using System.Threading.Tasks;
using System.Threading;
using System.Runtime.CompilerServices;
namespace LiterLlm;

internal sealed class DefaultClientSafeHandle : SafeHandle
{
    internal DefaultClientSafeHandle(IntPtr handle) : base(IntPtr.Zero, true)
    {
        SetHandle(handle);
    }

    public override bool IsInvalid => handle == IntPtr.Zero;

    protected override bool ReleaseHandle()
    {
        NativeMethods.DefaultClientFree(handle);
        return true;
    }
}
/// <summary>/// Default client implementation backed by `reqwest`./// /// The provider is resolved at construction time from `model_hint` (or/// defaults to OpenAI). However, individual requests can override the/// provider when their model string contains a prefix that clearly/// identifies a different provider (e.g. `"anthropic/claude-3"` will/// route to Anthropic even if the client was built without a hint)./// /// When the model prefix does not match any known provider, the/// construction-time provider is used as the fallback./// /// The provider is stored behind an [`Arc`] so it can be shared cheaply into/// async closures and streaming tasks that must be `'static`./// </summary>
public sealed class DefaultClient : IDisposable
{
    private static readonly JsonSerializerOptions JsonOptions = new()
    {
        Converters = { new JsonStringEnumConverter(JsonNamingPolicy.SnakeCaseLower) },
        DefaultIgnoreCondition = JsonIgnoreCondition.WhenWritingNull
    };
    private readonly DefaultClientSafeHandle _safeHandle;

    internal DefaultClient(IntPtr handle)
    {
        _safeHandle = new DefaultClientSafeHandle(handle);
    }

    internal IntPtr Handle => _safeHandle.DangerousGetHandle();

    public void Dispose() => _safeHandle.Dispose();


    public async Task<ChatCompletionResponse> Chat(ChatCompletionRequest req)
    {
        var reqJson = JsonSerializer.Serialize(req, JsonOptions);
        var reqHandle = NativeMethods.ChatCompletionRequestFromJson(reqJson);
        return await Task.Run(() =>
        {
            var nativeResult = NativeMethods.DefaultClientChat(
                Handle,
                reqHandle
            );
            if (nativeResult == IntPtr.Zero)
            {
                throw new LiterLlmException(0, "DefaultClientChat failed");
            }
            var jsonPtr = NativeMethods.ChatCompletionResponseToJson(nativeResult);
            var json = Marshal.PtrToStringUTF8(jsonPtr);
            NativeMethods.FreeString(jsonPtr);
            NativeMethods.ChatCompletionResponseFree(nativeResult);
            var returnValue = JsonSerializer.Deserialize<ChatCompletionResponse>(json ?? "null", JsonOptions)!;
            NativeMethods.ChatCompletionRequestFree(reqHandle);
            return returnValue;
        });
    }

    /// <summary>
    /// Streaming variant of ChatStream. Returns chunks as an asynchronous sequence.
    /// </summary>
    public async IAsyncEnumerable<ChatCompletionChunk> ChatStream(
        ChatCompletionRequest req,
        [EnumeratorCancellation] CancellationToken cancellationToken = default)
    {
        var reqJson = JsonSerializer.Serialize(req, JsonOptions);
        var reqHandle = NativeMethods.ChatCompletionRequestFromJson(reqJson);
        var streamHandle = NativeMethods.DefaultClientChatStreamStart(Handle, reqHandle);
        if (streamHandle == IntPtr.Zero)
        {
            NativeMethods.ChatCompletionRequestFree(reqHandle);
            var ec = NativeMethods.LastErrorCode();
            var ctxPtr = NativeMethods.LastErrorContext();
            var msg = Marshal.PtrToStringUTF8(ctxPtr) ?? "Unknown error";
            throw new LiterLlmException(ec, msg);
        }
        try
        {
            while (true)
            {
                cancellationToken.ThrowIfCancellationRequested();
                var chunkPtr = NativeMethods.DefaultClientChatStreamNext(streamHandle);
                if (chunkPtr == IntPtr.Zero)
                {
                    var ec = NativeMethods.LastErrorCode();
                    if (ec != 0)
                    {
                        var ctxPtr = NativeMethods.LastErrorContext();
                        var msg = Marshal.PtrToStringUTF8(ctxPtr) ?? "Unknown error";
                        throw new LiterLlmException(ec, msg);
                    }
                    yield break;
                }
                var jsonPtr = NativeMethods.ChatCompletionChunkToJson(chunkPtr);
                var json = Marshal.PtrToStringUTF8(jsonPtr);
                NativeMethods.FreeString(jsonPtr);
                NativeMethods.ChatCompletionChunkFree(chunkPtr);
                var chunk = JsonSerializer.Deserialize<ChatCompletionChunk>(json ?? "null", JsonOptions)!;
                yield return chunk;
                await Task.Yield();
            }
        }
        finally
        {
            NativeMethods.DefaultClientChatStreamFree(streamHandle);
            NativeMethods.ChatCompletionRequestFree(reqHandle);
        }
    }

    public async Task<EmbeddingResponse> Embed(EmbeddingRequest req)
    {
        var reqJson = JsonSerializer.Serialize(req, JsonOptions);
        var reqHandle = NativeMethods.EmbeddingRequestFromJson(reqJson);
        return await Task.Run(() =>
        {
            var nativeResult = NativeMethods.DefaultClientEmbed(
                Handle,
                reqHandle
            );
            if (nativeResult == IntPtr.Zero)
            {
                throw new LiterLlmException(0, "DefaultClientEmbed failed");
            }
            var jsonPtr = NativeMethods.EmbeddingResponseToJson(nativeResult);
            var json = Marshal.PtrToStringUTF8(jsonPtr);
            NativeMethods.FreeString(jsonPtr);
            NativeMethods.EmbeddingResponseFree(nativeResult);
            var returnValue = JsonSerializer.Deserialize<EmbeddingResponse>(json ?? "null", JsonOptions)!;
            NativeMethods.EmbeddingRequestFree(reqHandle);
            return returnValue;
        });
    }

    public async Task<ModelsListResponse> ListModels()
    {
        return await Task.Run(() =>
        {
            var nativeResult = NativeMethods.DefaultClientListModels(
                Handle
            );
            if (nativeResult == IntPtr.Zero)
            {
                throw new LiterLlmException(0, "DefaultClientListModels failed");
            }
            var jsonPtr = NativeMethods.ModelsListResponseToJson(nativeResult);
            var json = Marshal.PtrToStringUTF8(jsonPtr);
            NativeMethods.FreeString(jsonPtr);
            NativeMethods.ModelsListResponseFree(nativeResult);
            var returnValue = JsonSerializer.Deserialize<ModelsListResponse>(json ?? "null", JsonOptions)!;
            return returnValue;
        });
    }

    public async Task<ImagesResponse> ImageGenerate(CreateImageRequest req)
    {
        var reqJson = JsonSerializer.Serialize(req, JsonOptions);
        var reqHandle = NativeMethods.CreateImageRequestFromJson(reqJson);
        return await Task.Run(() =>
        {
            var nativeResult = NativeMethods.DefaultClientImageGenerate(
                Handle,
                reqHandle
            );
            if (nativeResult == IntPtr.Zero)
            {
                throw new LiterLlmException(0, "DefaultClientImageGenerate failed");
            }
            var jsonPtr = NativeMethods.ImagesResponseToJson(nativeResult);
            var json = Marshal.PtrToStringUTF8(jsonPtr);
            NativeMethods.FreeString(jsonPtr);
            NativeMethods.ImagesResponseFree(nativeResult);
            var returnValue = JsonSerializer.Deserialize<ImagesResponse>(json ?? "null", JsonOptions)!;
            NativeMethods.CreateImageRequestFree(reqHandle);
            return returnValue;
        });
    }

    public async Task<byte[]> Speech(CreateSpeechRequest req)
    {
        var reqJson = JsonSerializer.Serialize(req, JsonOptions);
        var reqHandle = NativeMethods.CreateSpeechRequestFromJson(reqJson);
        return await Task.Run(() =>
        {
            var rc = NativeMethods.DefaultClientSpeech(
                Handle,
                reqHandle,
                out var outPtr,
                out var outLen,
                out var outCap
            );
            if (rc != 0)
            {
                var ec = NativeMethods.LastErrorCode();
                var ctxPtr = NativeMethods.LastErrorContext();
                var msg = System.Runtime.InteropServices.Marshal.PtrToStringUTF8(ctxPtr) ?? "Unknown error";
                throw new LiterLlmException(ec, msg);
            }
            var result = new byte[(int)outLen];
            System.Runtime.InteropServices.Marshal.Copy(outPtr, result, 0, (int)outLen);
            NativeMethods.FreeBytes(outPtr, outLen, outCap);
            return result;
        });
    }


    public async Task<TranscriptionResponse> Transcribe(CreateTranscriptionRequest req)
    {
        var reqJson = JsonSerializer.Serialize(req, JsonOptions);
        var reqHandle = NativeMethods.CreateTranscriptionRequestFromJson(reqJson);
        return await Task.Run(() =>
        {
            var nativeResult = NativeMethods.DefaultClientTranscribe(
                Handle,
                reqHandle
            );
            if (nativeResult == IntPtr.Zero)
            {
                throw new LiterLlmException(0, "DefaultClientTranscribe failed");
            }
            var jsonPtr = NativeMethods.TranscriptionResponseToJson(nativeResult);
            var json = Marshal.PtrToStringUTF8(jsonPtr);
            NativeMethods.FreeString(jsonPtr);
            NativeMethods.TranscriptionResponseFree(nativeResult);
            var returnValue = JsonSerializer.Deserialize<TranscriptionResponse>(json ?? "null", JsonOptions)!;
            NativeMethods.CreateTranscriptionRequestFree(reqHandle);
            return returnValue;
        });
    }

    public async Task<ModerationResponse> Moderate(ModerationRequest req)
    {
        var reqJson = JsonSerializer.Serialize(req, JsonOptions);
        var reqHandle = NativeMethods.ModerationRequestFromJson(reqJson);
        return await Task.Run(() =>
        {
            var nativeResult = NativeMethods.DefaultClientModerate(
                Handle,
                reqHandle
            );
            if (nativeResult == IntPtr.Zero)
            {
                throw new LiterLlmException(0, "DefaultClientModerate failed");
            }
            var jsonPtr = NativeMethods.ModerationResponseToJson(nativeResult);
            var json = Marshal.PtrToStringUTF8(jsonPtr);
            NativeMethods.FreeString(jsonPtr);
            NativeMethods.ModerationResponseFree(nativeResult);
            var returnValue = JsonSerializer.Deserialize<ModerationResponse>(json ?? "null", JsonOptions)!;
            NativeMethods.ModerationRequestFree(reqHandle);
            return returnValue;
        });
    }

    public async Task<RerankResponse> Rerank(RerankRequest req)
    {
        var reqJson = JsonSerializer.Serialize(req, JsonOptions);
        var reqHandle = NativeMethods.RerankRequestFromJson(reqJson);
        return await Task.Run(() =>
        {
            var nativeResult = NativeMethods.DefaultClientRerank(
                Handle,
                reqHandle
            );
            if (nativeResult == IntPtr.Zero)
            {
                throw new LiterLlmException(0, "DefaultClientRerank failed");
            }
            var jsonPtr = NativeMethods.RerankResponseToJson(nativeResult);
            var json = Marshal.PtrToStringUTF8(jsonPtr);
            NativeMethods.FreeString(jsonPtr);
            NativeMethods.RerankResponseFree(nativeResult);
            var returnValue = JsonSerializer.Deserialize<RerankResponse>(json ?? "null", JsonOptions)!;
            NativeMethods.RerankRequestFree(reqHandle);
            return returnValue;
        });
    }

    public async Task<SearchResponse> Search(SearchRequest req)
    {
        var reqJson = JsonSerializer.Serialize(req, JsonOptions);
        var reqHandle = NativeMethods.SearchRequestFromJson(reqJson);
        return await Task.Run(() =>
        {
            var nativeResult = NativeMethods.DefaultClientSearch(
                Handle,
                reqHandle
            );
            if (nativeResult == IntPtr.Zero)
            {
                throw new LiterLlmException(0, "DefaultClientSearch failed");
            }
            var jsonPtr = NativeMethods.SearchResponseToJson(nativeResult);
            var json = Marshal.PtrToStringUTF8(jsonPtr);
            NativeMethods.FreeString(jsonPtr);
            NativeMethods.SearchResponseFree(nativeResult);
            var returnValue = JsonSerializer.Deserialize<SearchResponse>(json ?? "null", JsonOptions)!;
            NativeMethods.SearchRequestFree(reqHandle);
            return returnValue;
        });
    }

    public async Task<OcrResponse> Ocr(OcrRequest req)
    {
        var reqJson = JsonSerializer.Serialize(req, JsonOptions);
        var reqHandle = NativeMethods.OcrRequestFromJson(reqJson);
        return await Task.Run(() =>
        {
            var nativeResult = NativeMethods.DefaultClientOcr(
                Handle,
                reqHandle
            );
            if (nativeResult == IntPtr.Zero)
            {
                throw new LiterLlmException(0, "DefaultClientOcr failed");
            }
            var jsonPtr = NativeMethods.OcrResponseToJson(nativeResult);
            var json = Marshal.PtrToStringUTF8(jsonPtr);
            NativeMethods.FreeString(jsonPtr);
            NativeMethods.OcrResponseFree(nativeResult);
            var returnValue = JsonSerializer.Deserialize<OcrResponse>(json ?? "null", JsonOptions)!;
            NativeMethods.OcrRequestFree(reqHandle);
            return returnValue;
        });
    }

    public async Task<FileObject> CreateFile(CreateFileRequest req)
    {
        var reqJson = JsonSerializer.Serialize(req, JsonOptions);
        var reqHandle = NativeMethods.CreateFileRequestFromJson(reqJson);
        return await Task.Run(() =>
        {
            var nativeResult = NativeMethods.DefaultClientCreateFile(
                Handle,
                reqHandle
            );
            if (nativeResult == IntPtr.Zero)
            {
                throw new LiterLlmException(0, "DefaultClientCreateFile failed");
            }
            var jsonPtr = NativeMethods.FileObjectToJson(nativeResult);
            var json = Marshal.PtrToStringUTF8(jsonPtr);
            NativeMethods.FreeString(jsonPtr);
            NativeMethods.FileObjectFree(nativeResult);
            var returnValue = JsonSerializer.Deserialize<FileObject>(json ?? "null", JsonOptions)!;
            NativeMethods.CreateFileRequestFree(reqHandle);
            return returnValue;
        });
    }

    public async Task<FileObject> RetrieveFile(string fileId)
    {
        return await Task.Run(() =>
        {
            var nativeResult = NativeMethods.DefaultClientRetrieveFile(
                Handle,
                fileId
            );
            if (nativeResult == IntPtr.Zero)
            {
                throw new LiterLlmException(0, "DefaultClientRetrieveFile failed");
            }
            var jsonPtr = NativeMethods.FileObjectToJson(nativeResult);
            var json = Marshal.PtrToStringUTF8(jsonPtr);
            NativeMethods.FreeString(jsonPtr);
            NativeMethods.FileObjectFree(nativeResult);
            var returnValue = JsonSerializer.Deserialize<FileObject>(json ?? "null", JsonOptions)!;
            return returnValue;
        });
    }

    public async Task<DeleteResponse> DeleteFile(string fileId)
    {
        return await Task.Run(() =>
        {
            var nativeResult = NativeMethods.DefaultClientDeleteFile(
                Handle,
                fileId
            );
            if (nativeResult == IntPtr.Zero)
            {
                throw new LiterLlmException(0, "DefaultClientDeleteFile failed");
            }
            var jsonPtr = NativeMethods.DeleteResponseToJson(nativeResult);
            var json = Marshal.PtrToStringUTF8(jsonPtr);
            NativeMethods.FreeString(jsonPtr);
            NativeMethods.DeleteResponseFree(nativeResult);
            var returnValue = JsonSerializer.Deserialize<DeleteResponse>(json ?? "null", JsonOptions)!;
            return returnValue;
        });
    }

    public async Task<FileListResponse> ListFiles(FileListQuery? query)
    {
        var queryJson = query != null ? JsonSerializer.Serialize(query, JsonOptions) : "null";
        var queryHandle = NativeMethods.FileListQueryFromJson(queryJson);
        return await Task.Run(() =>
        {
            var nativeResult = NativeMethods.DefaultClientListFiles(
                Handle,
                queryHandle
            );
            if (nativeResult == IntPtr.Zero)
            {
                throw new LiterLlmException(0, "DefaultClientListFiles failed");
            }
            var jsonPtr = NativeMethods.FileListResponseToJson(nativeResult);
            var json = Marshal.PtrToStringUTF8(jsonPtr);
            NativeMethods.FreeString(jsonPtr);
            NativeMethods.FileListResponseFree(nativeResult);
            var returnValue = JsonSerializer.Deserialize<FileListResponse>(json ?? "null", JsonOptions)!;
            NativeMethods.FileListQueryFree(queryHandle);
            return returnValue;
        });
    }

    public async Task<byte[]> FileContent(string fileId)
    {
        return await Task.Run(() =>
        {
            var rc = NativeMethods.DefaultClientFileContent(
                Handle,
                fileId,
                out var outPtr,
                out var outLen,
                out var outCap
            );
            if (rc != 0)
            {
                var ec = NativeMethods.LastErrorCode();
                var ctxPtr = NativeMethods.LastErrorContext();
                var msg = System.Runtime.InteropServices.Marshal.PtrToStringUTF8(ctxPtr) ?? "Unknown error";
                throw new LiterLlmException(ec, msg);
            }
            var result = new byte[(int)outLen];
            System.Runtime.InteropServices.Marshal.Copy(outPtr, result, 0, (int)outLen);
            NativeMethods.FreeBytes(outPtr, outLen, outCap);
            return result;
        });
    }


    public async Task<BatchObject> CreateBatch(CreateBatchRequest req)
    {
        var reqJson = JsonSerializer.Serialize(req, JsonOptions);
        var reqHandle = NativeMethods.CreateBatchRequestFromJson(reqJson);
        return await Task.Run(() =>
        {
            var nativeResult = NativeMethods.DefaultClientCreateBatch(
                Handle,
                reqHandle
            );
            if (nativeResult == IntPtr.Zero)
            {
                throw new LiterLlmException(0, "DefaultClientCreateBatch failed");
            }
            var jsonPtr = NativeMethods.BatchObjectToJson(nativeResult);
            var json = Marshal.PtrToStringUTF8(jsonPtr);
            NativeMethods.FreeString(jsonPtr);
            NativeMethods.BatchObjectFree(nativeResult);
            var returnValue = JsonSerializer.Deserialize<BatchObject>(json ?? "null", JsonOptions)!;
            NativeMethods.CreateBatchRequestFree(reqHandle);
            return returnValue;
        });
    }

    public async Task<BatchObject> RetrieveBatch(string batchId)
    {
        return await Task.Run(() =>
        {
            var nativeResult = NativeMethods.DefaultClientRetrieveBatch(
                Handle,
                batchId
            );
            if (nativeResult == IntPtr.Zero)
            {
                throw new LiterLlmException(0, "DefaultClientRetrieveBatch failed");
            }
            var jsonPtr = NativeMethods.BatchObjectToJson(nativeResult);
            var json = Marshal.PtrToStringUTF8(jsonPtr);
            NativeMethods.FreeString(jsonPtr);
            NativeMethods.BatchObjectFree(nativeResult);
            var returnValue = JsonSerializer.Deserialize<BatchObject>(json ?? "null", JsonOptions)!;
            return returnValue;
        });
    }

    public async Task<BatchListResponse> ListBatches(BatchListQuery? query)
    {
        var queryJson = query != null ? JsonSerializer.Serialize(query, JsonOptions) : "null";
        var queryHandle = NativeMethods.BatchListQueryFromJson(queryJson);
        return await Task.Run(() =>
        {
            var nativeResult = NativeMethods.DefaultClientListBatches(
                Handle,
                queryHandle
            );
            if (nativeResult == IntPtr.Zero)
            {
                throw new LiterLlmException(0, "DefaultClientListBatches failed");
            }
            var jsonPtr = NativeMethods.BatchListResponseToJson(nativeResult);
            var json = Marshal.PtrToStringUTF8(jsonPtr);
            NativeMethods.FreeString(jsonPtr);
            NativeMethods.BatchListResponseFree(nativeResult);
            var returnValue = JsonSerializer.Deserialize<BatchListResponse>(json ?? "null", JsonOptions)!;
            NativeMethods.BatchListQueryFree(queryHandle);
            return returnValue;
        });
    }

    public async Task<BatchObject> CancelBatch(string batchId)
    {
        return await Task.Run(() =>
        {
            var nativeResult = NativeMethods.DefaultClientCancelBatch(
                Handle,
                batchId
            );
            if (nativeResult == IntPtr.Zero)
            {
                throw new LiterLlmException(0, "DefaultClientCancelBatch failed");
            }
            var jsonPtr = NativeMethods.BatchObjectToJson(nativeResult);
            var json = Marshal.PtrToStringUTF8(jsonPtr);
            NativeMethods.FreeString(jsonPtr);
            NativeMethods.BatchObjectFree(nativeResult);
            var returnValue = JsonSerializer.Deserialize<BatchObject>(json ?? "null", JsonOptions)!;
            return returnValue;
        });
    }

    public async Task<ResponseObject> CreateResponse(CreateResponseRequest req)
    {
        var reqJson = JsonSerializer.Serialize(req, JsonOptions);
        var reqHandle = NativeMethods.CreateResponseRequestFromJson(reqJson);
        return await Task.Run(() =>
        {
            var nativeResult = NativeMethods.DefaultClientCreateResponse(
                Handle,
                reqHandle
            );
            if (nativeResult == IntPtr.Zero)
            {
                throw new LiterLlmException(0, "DefaultClientCreateResponse failed");
            }
            var jsonPtr = NativeMethods.ResponseObjectToJson(nativeResult);
            var json = Marshal.PtrToStringUTF8(jsonPtr);
            NativeMethods.FreeString(jsonPtr);
            NativeMethods.ResponseObjectFree(nativeResult);
            var returnValue = JsonSerializer.Deserialize<ResponseObject>(json ?? "null", JsonOptions)!;
            NativeMethods.CreateResponseRequestFree(reqHandle);
            return returnValue;
        });
    }

    public async Task<ResponseObject> RetrieveResponse(string id)
    {
        return await Task.Run(() =>
        {
            var nativeResult = NativeMethods.DefaultClientRetrieveResponse(
                Handle,
                id
            );
            if (nativeResult == IntPtr.Zero)
            {
                throw new LiterLlmException(0, "DefaultClientRetrieveResponse failed");
            }
            var jsonPtr = NativeMethods.ResponseObjectToJson(nativeResult);
            var json = Marshal.PtrToStringUTF8(jsonPtr);
            NativeMethods.FreeString(jsonPtr);
            NativeMethods.ResponseObjectFree(nativeResult);
            var returnValue = JsonSerializer.Deserialize<ResponseObject>(json ?? "null", JsonOptions)!;
            return returnValue;
        });
    }

    public async Task<ResponseObject> CancelResponse(string id)
    {
        return await Task.Run(() =>
        {
            var nativeResult = NativeMethods.DefaultClientCancelResponse(
                Handle,
                id
            );
            if (nativeResult == IntPtr.Zero)
            {
                throw new LiterLlmException(0, "DefaultClientCancelResponse failed");
            }
            var jsonPtr = NativeMethods.ResponseObjectToJson(nativeResult);
            var json = Marshal.PtrToStringUTF8(jsonPtr);
            NativeMethods.FreeString(jsonPtr);
            NativeMethods.ResponseObjectFree(nativeResult);
            var returnValue = JsonSerializer.Deserialize<ResponseObject>(json ?? "null", JsonOptions)!;
            return returnValue;
        });
    }
}
