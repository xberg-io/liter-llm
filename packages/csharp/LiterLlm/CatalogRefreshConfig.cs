// This file is auto-generated by alef — DO NOT EDIT.
// alef:hash:f2d02d39ed23b63568387f22c991c70fd7b0d5c8f2f92cc028dcbaff825ca583
// To regenerate: alef generate
// To verify freshness: alef verify --exit-code
#nullable enable

using System;
using System.Collections.Generic;
using System.Text.Json;
using System.Text.Json.Serialization;

namespace LiterLlm;

/// <summary>
/// Plain-data configuration for `refresh_catalog`.
///
/// Deliberately FFI/binding-friendly: no `Duration` or `PathBuf`, just
/// primitives that translate directly across language boundaries.
/// </summary>
public sealed record CatalogRefreshConfig
{
    /// <summary>
    /// Runtime catalog refresh is entirely opt-in: when `false`,
    /// `refresh_catalog` is a no-op that returns
    /// `Ok(``RefreshOutcome.Disabled``)` without touching the network,
    /// the filesystem, or the overlay registry.
    /// </summary>
    [JsonPropertyName("enabled")]
    public bool Enabled { get; init; } = false;

    /// <summary>
    /// Source URL to fetch `catalog.json` from. Must be `https`. Defaults to
    /// `DEFAULT_CATALOG_URL`; configurable so self-hosted mirrors work.
    /// </summary>
    [JsonPropertyName("source_url")]
    public string SourceUrl { get; init; } = "";

    /// <summary>
    /// How long a cached `catalog.json` remains valid before a network
    /// refetch is attempted, in seconds.
    /// </summary>
    [JsonPropertyName("ttl_seconds")]
    public ulong TtlSeconds { get; init; } = 86400;

    /// <summary>
    /// Filesystem path for the on-disk cache. `None` uses a default path
    /// under `std.env.temp_dir()`.
    /// </summary>
    [JsonPropertyName("cache_path")]
    public string? CachePath { get; init; } = null;


    /// <summary>
    /// Parse a <see cref="CatalogRefreshConfig"/> from a JSON string.
    /// </summary>
    /// <exception cref="LiterLlmException">When the JSON cannot be deserialised.</exception>
    public static CatalogRefreshConfig FromJson(string json)
    {
        try
        {
            return JsonSerializer.Deserialize<CatalogRefreshConfig>(json, JsonOptions)
                ?? throw new LiterLlmException($"Failed to parse CatalogRefreshConfig from JSON: deserializer returned null");
        }
        catch (LiterLlmException)
        {
            throw;
        }
        catch (Exception e)
        {
            throw new LiterLlmException($"Failed to parse CatalogRefreshConfig from JSON: {e.Message}", e);
        }
    }

    private static readonly JsonSerializerOptions JsonOptions = new()
    {
        DefaultIgnoreCondition = JsonIgnoreCondition.WhenWritingDefault,
        Converters = { new JsonStringEnumConverter(JsonNamingPolicy.SnakeCaseLower), new ByteArrayJsonConverter() },
    };

    /// <summary>Options for serializing config/input objects to FFI. Strips nulls
    /// (nullable C# fields default to null and would override required Rust fields with
    /// non-deserialisable nulls) but preserves explicit false/0 so caller intent is kept.</summary>
    private static readonly JsonSerializerOptions JsonSerializationOptions = new()
    {
        DefaultIgnoreCondition = JsonIgnoreCondition.WhenWritingNull,
        Converters = { new JsonStringEnumConverter(JsonNamingPolicy.SnakeCaseLower), new ByteArrayJsonConverter() },
    };

    public static CatalogRefreshConfig Default()
    {
        var nativeResult = NativeMethods.CatalogRefreshConfigDefault();
        var jsonPtr = NativeMethods.CatalogRefreshConfigToJson(nativeResult);
        var json = global::System.Runtime.InteropServices.Marshal.PtrToStringUTF8(jsonPtr);
        NativeMethods.FreeString(jsonPtr);
        NativeMethods.CatalogRefreshConfigFree(nativeResult);
        return JsonSerializer.Deserialize<CatalogRefreshConfig>(json ?? "null", JsonOptions)!;
    }
}
