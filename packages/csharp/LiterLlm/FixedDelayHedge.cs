// This file is auto-generated by alef. DO NOT EDIT.
#nullable enable

using System;
using Microsoft.Win32.SafeHandles;
using System.Runtime.InteropServices;
using System.Text.Json;
using System.Text.Json.Serialization;

namespace LiterLlm;

internal sealed class FixedDelayHedgeSafeHandle : SafeHandle
{
    internal FixedDelayHedgeSafeHandle(IntPtr handle) : base(IntPtr.Zero, true)
    {
        SetHandle(handle);
    }

    public override bool IsInvalid => handle == IntPtr.Zero;

    protected override bool ReleaseHandle()
    {
        NativeMethods.FixedDelayHedgeFree(handle);
        return true;
    }
}
/// <summary>/// A simple `HedgePolicy` that fires hedges at fixed intervals./// </summary>
public sealed class FixedDelayHedge : IDisposable
{    private static readonly JsonSerializerOptions JsonOptions = new()
    {
        Converters = { new JsonStringEnumConverter(JsonNamingPolicy.SnakeCaseLower) },
        DefaultIgnoreCondition = JsonIgnoreCondition.WhenWritingDefault
    };

    private static readonly JsonSerializerOptions JsonSerializationOptions = new()
    {
        Converters = { new JsonStringEnumConverter(JsonNamingPolicy.SnakeCaseLower) },
    };
    private readonly FixedDelayHedgeSafeHandle _safeHandle;

    internal FixedDelayHedge(IntPtr handle)
    {
        _safeHandle = new FixedDelayHedgeSafeHandle(handle);
    }

    internal IntPtr Handle => _safeHandle.DangerousGetHandle();

    public void Dispose() => _safeHandle.Dispose();


    /// <summary>Creates a new <see cref="FixedDelayHedge"/> instance.</summary>
    public FixedDelayHedge(ulong? delay, uint maxAttempts)
    {
        var handle = NativeMethods.FixedDelayHedgeNew(delay, maxAttempts);
        if (handle == IntPtr.Zero)
        {
            var ec = NativeMethods.LastErrorCode();
            var ctxPtr = NativeMethods.LastErrorContext();
            var msg = System.Runtime.InteropServices.Marshal.PtrToStringUTF8(ctxPtr) ?? "Constructor failed";
            throw new LiterLlmException(ec, msg);
        }
        _safeHandle = new FixedDelayHedgeSafeHandle(handle);
    }

    public ulong?? DelayForAttempt(uint attempt, ulong? latencySoFar)
    {
        var nativeResult = NativeMethods.FixedDelayHedgeDelayForAttempt(
            Handle,
            attempt,
            latencySoFar
        );
        if (nativeResult == IntPtr.Zero)
        {
            return null;
        }
        var json = global::System.Runtime.InteropServices.Marshal.PtrToStringUTF8(nativeResult);
        NativeMethods.FreeString(nativeResult);
        var returnValue = JsonSerializer.Deserialize<ulong??>(json ?? "null", JsonOptions)!;
        return returnValue;
    }

    public uint MaxAttempts()
    {
        var nativeResult = NativeMethods.FixedDelayHedgeMaxAttempts(
            Handle
        );
        var returnValue = nativeResult;
        return returnValue;
    }
}
