// This file is auto-generated by alef — DO NOT EDIT.
// To regenerate: alef generate
// To verify freshness: alef verify --exit-code
package dev.kreuzberg.literllm;

import java.lang.foreign.MemorySegment;

/**
 * The value broadcast from a singleflight leader to all followers.
 *
 * {@code Arc&lt;LiterLlmError&gt;} is used because {@code LiterLlmError} is not {@code Clone} and
 * broadcast channels require {@code T: Clone}.  The {@code Arc} adds only a reference-count
 * bump per follower, which is negligible under the burst loads this layer targets.
 */
@SuppressWarnings("PMD")
public class SingleflightResult implements AutoCloseable {
    private final MemorySegment handle;

    SingleflightResult(MemorySegment handle) {
        this.handle = handle;
    }

    MemorySegment handle() {
        return this.handle;
    }
    @Override
    @SuppressWarnings("PMD.AvoidCatchingGenericException")
    public void close() {
        if (handle != null && !handle.equals(MemorySegment.NULL)) {
            try {
                NativeLib.LITERLLM_SINGLEFLIGHT_RESULT_FREE.invoke(handle);
            } catch (Throwable e) {
                throw new RuntimeException("Failed to free SingleflightResult: " + e.getMessage(), e);
            }
        }
    }
}
