// This file is auto-generated by alef — DO NOT EDIT.
// To regenerate: alef generate
// To verify freshness: alef verify --exit-code
package dev.kreuzberg.literllm;

import com.fasterxml.jackson.annotation.JsonCreator;
import com.fasterxml.jackson.annotation.JsonValue;

/**
 * The outcome of a guardrail check.
 */
@SuppressWarnings("PMD")
public enum GuardrailDecision {
  /**
   * The check passed. Continue to the next guardrail or to the inner service.
   */
  Allow("allow"),
  /**
   * The check failed. Short-circuit the request/response with this reason.
   *
   * {@code code} should be ≥ 1000 to avoid collision with HTTP status codes and
   * to facilitate cross-language error mapping.
   */
  Block("block"),
  /**
   * Rewrite the payload. The provided {@code new_payload} replaces the original
   * {@code request} or {@code response} before it reaches the next stage.
   *
   * For {@code OutputChunk} stage: {@code new_payload} replaces the chunk content.
   */
  Mutate("mutate");

  /** The string value. */
  private final String value;

  GuardrailDecision(final String value) {
    this.value = value;
  }

  /** Returns the string value. */
  @JsonValue
  public String getValue() {
    return value;
  }

  /** Creates an instance from a string value. */
  @JsonCreator
  public static GuardrailDecision fromValue(final String value) {
    for (GuardrailDecision e : values()) {
      if (e.value.equalsIgnoreCase(value)) {
        return e;
      }
    }
    throw new IllegalArgumentException("Unknown GuardrailDecision value: " + value);
  }

  /** Returns the wire-format string value (matches JSON serialization). */
  @Override
  public String toString() {
    return value;
  }
}
