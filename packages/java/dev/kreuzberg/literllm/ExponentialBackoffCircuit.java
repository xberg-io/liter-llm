// This file is auto-generated by alef — DO NOT EDIT.
// To regenerate: alef generate
// To verify freshness: alef verify --exit-code
package dev.kreuzberg.literllm;

import java.lang.foreign.MemorySegment;

/**
 * Circuit breaker with exponential backoff.
 *
 * Opens after {@code failure_threshold} consecutive failures.  After
 * {@code base_backoff} (doubled on each successive open → half-open → open cycle,
 * up to {@code max_backoff}), the circuit enters CircuitState.HalfOpen and
 * allows one probe request through.
 */
@SuppressWarnings("PMD")
public class ExponentialBackoffCircuit implements AutoCloseable {
  private final MemorySegment handle;

  ExponentialBackoffCircuit(MemorySegment handle) {
    this.handle = handle;
  }

  MemorySegment handle() {
    return this.handle;
  }

  public void recordSuccess() throws LiterLlmRsException {
    try {
      NativeLib.LITERLLM_EXPONENTIAL_BACKOFF_CIRCUIT_RECORD_SUCCESS.invoke(this.handle);
      checkLastFfiError();
    } catch (LiterLlmRsException ex) {
      throw ex;
    } catch (Throwable e) {
      throw new LiterLlmRsException("recordSuccess: failed", e);
    }
  }

  public void recordFailure() throws LiterLlmRsException {
    try {
      NativeLib.LITERLLM_EXPONENTIAL_BACKOFF_CIRCUIT_RECORD_FAILURE.invoke(this.handle);
      checkLastFfiError();
    } catch (LiterLlmRsException ex) {
      throw ex;
    } catch (Throwable e) {
      throw new LiterLlmRsException("recordFailure: failed", e);
    }
  }

  public boolean shouldAllow() throws LiterLlmRsException {
    try {
      var result =
          (int) NativeLib.LITERLLM_EXPONENTIAL_BACKOFF_CIRCUIT_SHOULD_ALLOW.invoke(this.handle);
      return result != 0;
    } catch (LiterLlmRsException ex) {
      throw ex;
    } catch (Throwable e) {
      throw new LiterLlmRsException("shouldAllow: failed", e);
    }
  }

  public CircuitState state() throws LiterLlmRsException {
    try {
      // Unsupported return shape for state
      throw new LiterLlmRsException("state: unsupported return shape", (Throwable) null);
    } catch (LiterLlmRsException ex) {
      throw ex;
    } catch (Throwable e) {
      throw new LiterLlmRsException("state: failed", e);
    }
  }
  /**
   * Release the probe slot without recording success or failure.
   *
   * Called by the ProbeGuard when the probe future is dropped before
   * completing (e.g. cancelled or panicked).
   */
  public void releaseProbeSlot() throws LiterLlmRsException {
    try {
      NativeLib.LITERLLM_EXPONENTIAL_BACKOFF_CIRCUIT_RELEASE_PROBE_SLOT.invoke(this.handle);
      checkLastFfiError();
    } catch (LiterLlmRsException ex) {
      throw ex;
    } catch (Throwable e) {
      throw new LiterLlmRsException("releaseProbeSlot: failed", e);
    }
  }
  /**
   * Create a new policy.
   *
   * - {@code failure_threshold}: consecutive failures required to open the circuit.
   * - {@code base_backoff}: initial half-open retry delay (doubles each open cycle,
   *   capped at 2 minutes).
   */
  public static ExponentialBackoffCircuit create(final int failureThreshold, final Long baseBackoff)
      throws LiterLlmRsException {
    try {
      var handle = (MemorySegment)
          NativeLib.LITERLLM_EXPONENTIAL_BACKOFF_CIRCUIT_NEW.invoke(failureThreshold, baseBackoff);
      if (handle == null || handle.equals(MemorySegment.NULL)) {
        throw new LiterLlmRsException("create returned null", (Throwable) null);
      }
      return new ExponentialBackoffCircuit(handle);
    } catch (Throwable e) {
      if (e instanceof LiterLlmRsException) throw (LiterLlmRsException) e;
      throw new RuntimeException("create failed: " + e.getMessage(), e);
    }
  }

  @Override
  @SuppressWarnings("PMD.AvoidCatchingGenericException")
  public void close() {
    if (handle != null && !handle.equals(MemorySegment.NULL)) {
      try {
        NativeLib.LITERLLM_EXPONENTIAL_BACKOFF_CIRCUIT_FREE.invoke(handle);
      } catch (Throwable e) {
        throw new RuntimeException(
            "Failed to free ExponentialBackoffCircuit: " + e.getMessage(), e);
      }
    }
  }

  // CPD-OFF — generated FFI boilerplate, identical across all opaque types by design.
  private static void checkLastFfiError() throws LiterLlmRsException {
    try {
      int code = (int) NativeLib.LITERLLM_LAST_ERROR_CODE.invoke();
      if (code == 0) {
        return;
      }
      MemorySegment ctxPtr = (MemorySegment) NativeLib.LITERLLM_LAST_ERROR_CONTEXT.invoke();
      String msg = ctxPtr.equals(MemorySegment.NULL)
          ? "unknown"
          : ctxPtr.reinterpret(Long.MAX_VALUE).getString(0);
      throw new LiterLlmRsException(code, msg);
    } catch (LiterLlmRsException e) {
      throw e;
    } catch (Throwable e) {
      throw new LiterLlmRsException("failed to read last error", e);
    }
  } // CPD-ON
}
