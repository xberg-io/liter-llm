// This file is auto-generated by alef — DO NOT EDIT.
// alef:hash:77c1bfd3e4c16565dde103417267de8e582e9f8497a8a4a87c23d0eec99d0b4d
// To regenerate: alef generate
// To verify freshness: alef verify --exit-code
// Issues & docs: https://github.com/kreuzberg-dev/alef
package dev.kreuzberg.literllm;

import java.lang.foreign.MemorySegment;
import java.lang.foreign.Arena;
import com.fasterxml.jackson.databind.ObjectMapper;
import java.util.Optional;

public class TowerLlmResponse implements AutoCloseable {
    private final MemorySegment handle;

    TowerLlmResponse(MemorySegment handle) {
        this.handle = handle;
    }

    MemorySegment handle() {
        return this.handle;
    }

    /**
     * Return the usage data from the response, if present.
     *
     * Streaming, model-list, and non-chat responses do not carry aggregated
     * usage data and always return {@code None}.
     */
    public Optional<Usage> usage() throws LiterLlmRsException {
        try (var arena = Arena.ofShared()) {
            MemorySegment resultPtr = (MemorySegment) NativeLib.LITERLLM_TOWER_LLM_RESPONSE_USAGE.invoke(this.handle);
            if (resultPtr.equals(MemorySegment.NULL)) {
                checkLastFfiError();
                return java.util.Optional.empty();
            }
            try {
                MemorySegment jsonPtr = (MemorySegment) NativeLib.LITERLLM_USAGE_TO_JSON.invoke(resultPtr);
                if (jsonPtr.equals(MemorySegment.NULL)) {
                    checkLastFfiError();
                    throw new LiterLlmRsException("usage: failed to serialize response", (Throwable) null);
                }
                String json = jsonPtr.reinterpret(Long.MAX_VALUE).getString(0);
                NativeLib.LITERLLM_FREE_STRING.invoke(jsonPtr);
                return java.util.Optional.of(STREAM_MAPPER.readValue(json, Usage.class));
            } finally {
                NativeLib.LITERLLM_USAGE_FREE.invoke(resultPtr);
            }
        } catch (Throwable e) {
            if (e instanceof LiterLlmRsException ex) { throw ex; }
            throw new LiterLlmRsException("usage: failed", e);
        }
    }
    @Override
    public void close() {
        if (handle != null && !handle.equals(MemorySegment.NULL)) {
            try {
                NativeLib.LITERLLM_TOWER_LLM_RESPONSE_FREE.invoke(handle);
            } catch (Throwable e) {
                throw new RuntimeException("Failed to free TowerLlmResponse: " + e.getMessage(), e);
            }
        }
    }

    private void checkLastFfiError() throws LiterLlmRsException {
        try {
            int code = (int) NativeLib.LITERLLM_LAST_ERROR_CODE.invoke();
            if (code == 0) { return; }
            MemorySegment ctxPtr = (MemorySegment) NativeLib.LITERLLM_LAST_ERROR_CONTEXT.invoke();
            String msg = ctxPtr.equals(MemorySegment.NULL) ? "unknown" : ctxPtr.reinterpret(Long.MAX_VALUE).getString(0);
            throw new LiterLlmRsException(code, msg);
        } catch (Throwable e) {
            if (e instanceof LiterLlmRsException ex) { throw ex; }
            throw new LiterLlmRsException("failed to read last error", e);
        }
    }
    private static final ObjectMapper STREAM_MAPPER = new ObjectMapper()
        .registerModule(new com.fasterxml.jackson.datatype.jdk8.Jdk8Module())
        .findAndRegisterModules()
        .setPropertyNamingStrategy(com.fasterxml.jackson.databind.PropertyNamingStrategies.SNAKE_CASE)
        .setSerializationInclusion(com.fasterxml.jackson.annotation.JsonInclude.Include.NON_NULL)
        .configure(com.fasterxml.jackson.databind.MapperFeature.ACCEPT_CASE_INSENSITIVE_ENUMS, true);
}
