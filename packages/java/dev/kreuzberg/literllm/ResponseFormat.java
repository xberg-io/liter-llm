// This file is auto-generated by alef — DO NOT EDIT.
// alef:hash:ee34d53dd7b53bbfa1e1d683dc961e1355a375403d7bca2db0da948bbbf6d364
// To regenerate: alef generate
// To verify freshness: alef verify --exit-code
package dev.kreuzberg.literllm;

import com.fasterxml.jackson.annotation.JsonProperty;
import com.fasterxml.jackson.annotation.JsonSubTypes;
import com.fasterxml.jackson.annotation.JsonTypeInfo;

/**
 * Wire format for the chat completions {@code response_format} field.
 *
 * # Provider mapping
 *
 * - **OpenAI** (and OpenAI-compatible providers): emitted verbatim as
 *   {@code {"type": "json_schema", "json_schema": {...}}} per the
 *   chat-completions spec.
 * - **Gemini / Vertex AI**: translated to
 *   {@code generationConfig.responseMimeType = "application/json"} and
 *   {@code generationConfig.responseSchema = &lt;schema&gt;}. The {@code name},
 *   {@code description}, and {@code strict} fields are dropped — Gemini's
 *   structured-output API does not consume them.
 * - **Anthropic**: no native JSON mode. A system instruction is
 *   prepended asking the model to respond with valid JSON.
 *   {@code strict} is advisory only; callers should still validate the
 *   returned JSON if the schema is load-bearing.
 */
@JsonTypeInfo(use = JsonTypeInfo.Id.NAME, property = "type", visible = false)
@JsonSubTypes({
  @JsonSubTypes.Type(value = ResponseFormat.Text.class, name = "text"),
  @JsonSubTypes.Type(value = ResponseFormat.JsonObject.class, name = "json_object"),
  @JsonSubTypes.Type(value = ResponseFormat.JsonSchema.class, name = "json_schema")
})
@com.fasterxml.jackson.annotation.JsonIgnoreProperties(ignoreUnknown = true)
public sealed interface ResponseFormat {

  /**
   * Plain text output (default).
   */
  record Text() implements ResponseFormat {}

  /**
   * Output must be valid JSON object (no schema validation).
   */
  record JsonObject() implements ResponseFormat {}

  /**
   * Output must conform to the specified JSON schema.
   */
  record JsonSchema(@JsonProperty("json_schema") JsonSchemaFormat jsonSchema)
      implements ResponseFormat {}
}
