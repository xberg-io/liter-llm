// This file is auto-generated by alef — DO NOT EDIT.
// alef:hash:52cff9ab632fc27fd4c70a61b4f9ae757316def7b11871ab370c512ecf93fcbe
// To regenerate: alef generate
// To verify freshness: alef verify --exit-code
// Issues & docs: https://github.com/kreuzberg-dev/alef
package dev.kreuzberg.literllm;

import java.lang.foreign.MemorySegment;
import java.lang.foreign.Arena;
import com.fasterxml.jackson.databind.ObjectMapper;
import java.util.Optional;

public class TowerLlmRequest implements AutoCloseable {
    private final MemorySegment handle;

    TowerLlmRequest(MemorySegment handle) {
        this.handle = handle;
    }

    MemorySegment handle() {
        return this.handle;
    }

    /**
     * OpenTelemetry GenAI {@code gen_ai.operation.name} value for this request.
     *
     * Maps each variant to one of the canonical GenAI semantic convention
     * operation names: {@code "chat"}, {@code "embeddings"}, or {@code "list_models"}.
     * Both streaming and non-streaming chat map to {@code "chat"}.
     */
    public String operationName() throws LiterLlmRsException {
        try (var arena = Arena.ofShared()) {
            MemorySegment resultPtr = (MemorySegment) NativeLib.LITERLLM_TOWER_LLM_REQUEST_OPERATION_NAME.invoke(this.handle);
            if (resultPtr.equals(MemorySegment.NULL)) {
                checkLastFfiError();
                return null;
            }
            try {
                String result = resultPtr.reinterpret(Long.MAX_VALUE).getString(0);
                return result;
            } finally {
                NativeLib.LITERLLM_FREE_STRING.invoke(resultPtr);
            }
        } catch (Throwable e) {
            if (e instanceof LiterLlmRsException ex) { throw ex; }
            throw new LiterLlmRsException("operationName: failed", e);
        }
    }
    /**
     * Human-readable name of the request type; used as a span / metric label.
     */
    public String requestType() throws LiterLlmRsException {
        try (var arena = Arena.ofShared()) {
            MemorySegment resultPtr = (MemorySegment) NativeLib.LITERLLM_TOWER_LLM_REQUEST_REQUEST_TYPE.invoke(this.handle);
            if (resultPtr.equals(MemorySegment.NULL)) {
                checkLastFfiError();
                return null;
            }
            try {
                String result = resultPtr.reinterpret(Long.MAX_VALUE).getString(0);
                return result;
            } finally {
                NativeLib.LITERLLM_FREE_STRING.invoke(resultPtr);
            }
        } catch (Throwable e) {
            if (e instanceof LiterLlmRsException ex) { throw ex; }
            throw new LiterLlmRsException("requestType: failed", e);
        }
    }
    /**
     * Return the model name embedded in the request, if any.
     */
    public Optional<String> model() throws LiterLlmRsException {
        try (var arena = Arena.ofShared()) {
            MemorySegment resultPtr = (MemorySegment) NativeLib.LITERLLM_TOWER_LLM_REQUEST_MODEL.invoke(this.handle);
            if (resultPtr.equals(MemorySegment.NULL)) {
                return java.util.Optional.empty();
            }
            try {
                String result = resultPtr.reinterpret(Long.MAX_VALUE).getString(0);
                return java.util.Optional.of(result);
            } finally {
                NativeLib.LITERLLM_FREE_STRING.invoke(resultPtr);
            }
        } catch (Throwable e) {
            if (e instanceof LiterLlmRsException ex) { throw ex; }
            throw new LiterLlmRsException("model: failed", e);
        }
    }
    @Override
    public void close() {
        if (handle != null && !handle.equals(MemorySegment.NULL)) {
            try {
                NativeLib.LITERLLM_TOWER_LLM_REQUEST_FREE.invoke(handle);
            } catch (Throwable e) {
                throw new RuntimeException("Failed to free TowerLlmRequest: " + e.getMessage(), e);
            }
        }
    }

    private void checkLastFfiError() throws LiterLlmRsException {
        try {
            int code = (int) NativeLib.LITERLLM_LAST_ERROR_CODE.invoke();
            if (code == 0) { return; }
            MemorySegment ctxPtr = (MemorySegment) NativeLib.LITERLLM_LAST_ERROR_CONTEXT.invoke();
            String msg = ctxPtr.equals(MemorySegment.NULL) ? "unknown" : ctxPtr.reinterpret(Long.MAX_VALUE).getString(0);
            throw new LiterLlmRsException(code, msg);
        } catch (Throwable e) {
            if (e instanceof LiterLlmRsException ex) { throw ex; }
            throw new LiterLlmRsException("failed to read last error", e);
        }
    }
    private static final ObjectMapper STREAM_MAPPER = new ObjectMapper()
        .registerModule(new com.fasterxml.jackson.datatype.jdk8.Jdk8Module())
        .findAndRegisterModules()
        .setPropertyNamingStrategy(com.fasterxml.jackson.databind.PropertyNamingStrategies.SNAKE_CASE)
        .setSerializationInclusion(com.fasterxml.jackson.annotation.JsonInclude.Include.NON_NULL)
        .configure(com.fasterxml.jackson.databind.MapperFeature.ACCEPT_CASE_INSENSITIVE_ENUMS, true);
}
