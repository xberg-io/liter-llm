// Generated by alef. Do not edit by hand.
// alef:hash:1213a045f6c720bcab8727df8faa87409c027be1ac51394a1bf33f9c83c5d07c
@file:Suppress(
    "ktlint:standard:trailing-comma-on-call-site",
    "ktlint:standard:trailing-comma-on-declaration-site",
    "ktlint:standard:spacing-between-declarations-with-comments",
    "ktlint:standard:spacing-between-declarations-with-annotations",
    "ktlint:standard:when-entry-bracing",
    "ktlint:standard:blank-line-between-when-conditions",
    "ktlint:standard:blank-line-before-declaration",
    "ktlint:standard:chain-method-continuation",
    "ktlint:standard:annotation",
    "ktlint:standard:max-line-length",
    "ktlint:standard:no-semi",
    "ktlint:standard:statement-wrapping",
    "MaxLineLength",
    "TooManyFunctions",
    "FunctionParameterNaming",
    "LongParameterList",
    "CyclomaticComplexMethod",
    "LongMethod",
    "MagicNumber",
    "NestedBlockDepth",
    "ReturnCount",
    "UnusedParameter",
)

package dev.kreuzberg.literllm.android

/**
 * Content shape for assistant messages.
 *
 * `#[serde(untagged)]` means providers returning a plain scalar string for the `content` field
 * still deserialise correctly into `AssistantContent.Text(_)`. Providers returning an array of
 * typed parts (e.g. after an image-generation or audio-synthesis request) deserialise into
 * `AssistantContent.Parts(_)`.
 */
@com.fasterxml.jackson.databind.annotation.JsonDeserialize(
    using = AssistantContentDeserializer::class
)
@com.fasterxml.jackson.databind.annotation.JsonSerialize(using = AssistantContentSerializer::class)
sealed class AssistantContent {
    /** Plain text response (the common case for text-only models). */
    data class Text(val value: String) : AssistantContent()

    /** Structured parts — text, refusals, output images, output audio. */
    data class Parts(val value: List<AssistantPart>) : AssistantContent()
}

private class AssistantContentDeserializer :
    com.fasterxml.jackson.databind.deser.std.StdDeserializer<AssistantContent>(
        AssistantContent::class.java
    ) {
    @Suppress("LongMethod")
    override fun deserialize(
        parser: com.fasterxml.jackson.core.JsonParser,
        ctx: com.fasterxml.jackson.databind.DeserializationContext,
    ): AssistantContent {
        val node = parser.codec.readTree<com.fasterxml.jackson.databind.JsonNode>(parser)
        if (node.isTextual) return AssistantContent.Text(node.asText())
        if (node.isArray)
            return run {
                val javaType =
                    ctx.typeFactory.constructCollectionType(
                        List::class.java,
                        AssistantPart::class.java,
                    )
                @Suppress("UNCHECKED_CAST")
                AssistantContent.Parts(
                    ctx.readTreeAsValue<List<AssistantPart>>(node, javaType) as List<AssistantPart>
                )
            }
        throw com.fasterxml.jackson.databind.exc.InvalidFormatException(
            parser,
            "Cannot deserialize AssistantContent: no matching variant for JSON shape",
            null,
            AssistantContent::class.java,
        )
    }
}

private class AssistantContentSerializer :
    com.fasterxml.jackson.databind.ser.std.StdSerializer<AssistantContent>(
        AssistantContent::class.java
    ) {
    @Suppress("LongMethod")
    override fun serialize(
        value: AssistantContent,
        gen: com.fasterxml.jackson.core.JsonGenerator,
        provider: com.fasterxml.jackson.databind.SerializerProvider,
    ) {
        @Suppress("UNCHECKED_CAST")
        val mapper =
            (gen.codec as? com.fasterxml.jackson.databind.ObjectMapper)
                ?: com.fasterxml.jackson.databind.ObjectMapper().findAndRegisterModules()
        when (value) {
            is AssistantContent.Text -> mapper.writeValue(gen, value.value)
            is AssistantContent.Parts -> {
                gen.writeStartArray()
                val elemSerializer = provider.findValueSerializer(AssistantPart::class.java)
                for (elem in value.value) {
                    elemSerializer.serialize(elem, gen, provider)
                }
                gen.writeEndArray()
            }
        }
    }
}
