// Generated by alef. Do not edit by hand.
// alef:hash:c7947a41084f71e69944233535fbeda5cd13b37144078f1895b6b57a7802d3fe
@file:Suppress(
    "ktlint:standard:trailing-comma-on-call-site",
    "ktlint:standard:trailing-comma-on-declaration-site",
    "ktlint:standard:spacing-between-declarations-with-comments",
    "ktlint:standard:spacing-between-declarations-with-annotations",
    "ktlint:standard:when-entry-bracing",
    "ktlint:standard:blank-line-between-when-conditions",
    "ktlint:standard:blank-line-before-declaration",
    "ktlint:standard:chain-method-continuation",
    "ktlint:standard:annotation",
    "ktlint:standard:max-line-length",
    "ktlint:standard:no-semi",
    "ktlint:standard:statement-wrapping",
    "MaxLineLength",
    "TooManyFunctions",
    "FunctionParameterNaming",
    "LongParameterList",
    "CyclomaticComplexMethod",
    "LongMethod",
    "MagicNumber",
    "NestedBlockDepth",
    "ReturnCount",
    "UnusedParameter",
)

package io.xberg.literllm.android

/**
 * Errors from `refresh_catalog` and `install_catalog_overlay_from_str`.
 *
 * On every variant, the overlay registry is left untouched: a previously
 * installed overlay (or the embedded catalog, if none was ever installed)
 * remains active. This is the air-gap-safety contract — a failed refresh
 * never degrades pricing/model-info availability.
 */
sealed class CatalogRefreshError(message: String) : Exception(message) {
    object Disabled : CatalogRefreshError("catalog refresh is disabled")
    data class InsecureUrl(
        val url: String,
    ) : CatalogRefreshError("insecure catalog source URL {url:?}: only https is allowed")
    data class Fetch(
        val url: String,
        override val message: String,
    ) : CatalogRefreshError("failed to fetch catalog from {url}: {message}")
    data class Parse(
        override val message: String,
    ) : CatalogRefreshError("failed to parse catalog JSON: {message}")
    data class Cache(
        val path: String,
        override val message: String,
    ) : CatalogRefreshError("catalog cache I/O error at {path}: {message}")
}
