// This file is auto-generated by alef — DO NOT EDIT.
// alef:hash:fa9f177d99c49595aef4c8330291fac50c616e390ee21c5d14278aa4c48ca2b0
// To regenerate: alef generate
// To verify freshness: alef verify --exit-code
// Issues & docs: https://github.com/kreuzberg-dev/alef
import XCTest
import Foundation
import LiterLlm
import RustBridge

/// E2e tests for category: chat.
final class ChatTests: XCTestCase {
    func testDeveloperMessage() async throws {
        // Chat request that includes a developer role message alongside user messages
        let _client = try DefaultClient(apiKey: "test-key", baseUrl: ProcessInfo.processInfo.environment["MOCK_SERVER_URL"]! + "/fixtures/developer_message")
        let result = try await _client.chat(nil)
        XCTAssertEqual(result.choices().count, 1)
        XCTAssertEqual((result.choices()[0].message().content()?.toString() ?? "").trimmingCharacters(in: .whitespaces), "s[::-1]")
        XCTAssertEqual(String(describing: result.choices()[0].finish_reason()).trimmingCharacters(in: .whitespaces), "stop")
    }

    func testEdgeChatMaxTokens() async throws {
        // Chat request with max_tokens=1 terminates with length finish_reason
        let _client = try DefaultClient(apiKey: "test-key", baseUrl: ProcessInfo.processInfo.environment["MOCK_SERVER_URL"]! + "/fixtures/edge_chat_max_tokens")
        let result = try await _client.chat(nil)
        XCTAssertEqual(String(describing: result.choices()[0].finish_reason()).trimmingCharacters(in: .whitespaces), "length")
        XCTAssertNotNil(result.choices()[0].message().content(), "expected non-nil value")
    }

    func testEdgeChatSystemOnly() async throws {
        // Chat request with system message and user message
        let _client = try DefaultClient(apiKey: "test-key", baseUrl: ProcessInfo.processInfo.environment["MOCK_SERVER_URL"]! + "/fixtures/edge_chat_system_only")
        let result = try await _client.chat(nil)
        XCTAssertNotNil(result.choices()[0].message().content(), "expected non-nil value")
    }

    func testEdgeChatTemperatureZero() async throws {
        // Chat request with temperature=0 for deterministic responses
        let _client = try DefaultClient(apiKey: "test-key", baseUrl: ProcessInfo.processInfo.environment["MOCK_SERVER_URL"]! + "/fixtures/edge_chat_temperature_zero")
        let result = try await _client.chat(nil)
        XCTAssertNotNil(result.choices()[0].message().content(), "expected non-nil value")
    }

    func testFinishReasonContentFilter() async throws {
        // Chat response stopped by content filter with finish_reason of content_filter and null content
        let _client = try DefaultClient(apiKey: "test-key", baseUrl: ProcessInfo.processInfo.environment["MOCK_SERVER_URL"]! + "/fixtures/finish_reason_content_filter")
        let result = try await _client.chat(nil)
        XCTAssertEqual(result.choices().count, 1)
        XCTAssertEqual(String(describing: result.choices()[0].finish_reason()).trimmingCharacters(in: .whitespaces), "content_filter")
    }

    func testFinishReasonLength() async throws {
        // Chat response truncated due to max_tokens limit with finish_reason of length
        let _client = try DefaultClient(apiKey: "test-key", baseUrl: ProcessInfo.processInfo.environment["MOCK_SERVER_URL"]! + "/fixtures/finish_reason_length")
        let result = try await _client.chat(nil)
        XCTAssertEqual(result.choices().count, 1)
        XCTAssertEqual(String(describing: result.choices()[0].finish_reason()).trimmingCharacters(in: .whitespaces), "length")
    }

    func testMultiTurnConversation() async throws {
        // Multi-turn conversation with system, user, assistant, and follow-up user messages
        let _client = try DefaultClient(apiKey: "test-key", baseUrl: ProcessInfo.processInfo.environment["MOCK_SERVER_URL"]! + "/fixtures/multi_turn_conversation")
        let result = try await _client.chat(nil)
        XCTAssertEqual(result.choices().count, 1)
        XCTAssertEqual((result.choices()[0].message().content()?.toString() ?? "").trimmingCharacters(in: .whitespaces), "4 + 4 equals 8.")
        XCTAssertEqual(String(describing: result.choices()[0].finish_reason()).trimmingCharacters(in: .whitespaces), "stop")
    }

    func testParallelToolCalls() async throws {
        // Chat request that results in parallel tool calls in the response
        let _client = try DefaultClient(apiKey: "test-key", baseUrl: ProcessInfo.processInfo.environment["MOCK_SERVER_URL"]! + "/fixtures/parallel_tool_calls")
        let result = try await _client.chat(nil)
        XCTAssertEqual(result.choices().count, 1)
        XCTAssertNotNil(result.choices()[0].message().tool_calls(), "expected non-nil value")
        XCTAssertEqual((result.choices[0]().message().tool_calls()?.count ?? 0), 2)
        XCTAssertEqual(String(describing: result.choices()[0].finish_reason()).trimmingCharacters(in: .whitespaces), "tool_calls")
    }

    func testResponseFormatJsonObject() async throws {
        // Chat request with response_format json_object that returns valid JSON content
        let _client = try DefaultClient(apiKey: "test-key", baseUrl: ProcessInfo.processInfo.environment["MOCK_SERVER_URL"]! + "/fixtures/response_format_json_object")
        let result = try await _client.chat(nil)
        XCTAssertEqual(result.choices().count, 1)
        XCTAssertNotNil(result.choices()[0].message().content(), "expected non-nil value")
        XCTAssertEqual(String(describing: result.choices()[0].finish_reason()).trimmingCharacters(in: .whitespaces), "stop")
    }

    func testResponseFormatJsonSchema() async throws {
        // Chat request with response_format json_schema that validates the output structure
        let _client = try DefaultClient(apiKey: "test-key", baseUrl: ProcessInfo.processInfo.environment["MOCK_SERVER_URL"]! + "/fixtures/response_format_json_schema")
        let result = try await _client.chat(nil)
        XCTAssertEqual(result.choices().count, 1)
        XCTAssertNotNil(result.choices()[0].message().content(), "expected non-nil value")
        XCTAssertEqual(String(describing: result.choices()[0].finish_reason()).trimmingCharacters(in: .whitespaces), "stop")
    }

    func testSeedParameter() async throws {
        // Chat request with seed parameter for deterministic output; response includes system_fingerprint
        let _client = try DefaultClient(apiKey: "test-key", baseUrl: ProcessInfo.processInfo.environment["MOCK_SERVER_URL"]! + "/fixtures/seed_parameter")
        let result = try await _client.chat(nil)
        XCTAssertEqual(result.choices().count, 1)
        XCTAssertEqual(String(describing: result.choices()[0].finish_reason()).trimmingCharacters(in: .whitespaces), "stop")
        XCTAssertNotNil(result.system_fingerprint(), "expected non-nil value")
    }

    func testStopSequences() async throws {
        // Chat request with custom stop sequences that terminates generation at a stop token
        let _client = try DefaultClient(apiKey: "test-key", baseUrl: ProcessInfo.processInfo.environment["MOCK_SERVER_URL"]! + "/fixtures/stop_sequences")
        let result = try await _client.chat(nil)
        XCTAssertEqual(result.choices().count, 1)
        XCTAssertEqual(String(describing: result.choices()[0].finish_reason()).trimmingCharacters(in: .whitespaces), "stop")
    }

    func testToolChoiceRequired() async throws {
        // Chat request with tool_choice set to required forces the model to call a tool
        let _client = try DefaultClient(apiKey: "test-key", baseUrl: ProcessInfo.processInfo.environment["MOCK_SERVER_URL"]! + "/fixtures/tool_choice_required")
        let result = try await _client.chat(nil)
        XCTAssertEqual(result.choices().count, 1)
        XCTAssertNotNil(result.choices()[0].message().tool_calls(), "expected non-nil value")
        XCTAssertEqual(result.choices()[0].message().tool_calls()[0].function().name().toString().trimmingCharacters(in: .whitespaces), "get_weather")
        XCTAssertEqual(String(describing: result.choices()[0].finish_reason()).trimmingCharacters(in: .whitespaces), "tool_calls")
    }

    func testToolChoiceSpecific() async throws {
        // Chat request with tool_choice specifying a particular function to call
        let _client = try DefaultClient(apiKey: "test-key", baseUrl: ProcessInfo.processInfo.environment["MOCK_SERVER_URL"]! + "/fixtures/tool_choice_specific")
        let result = try await _client.chat(nil)
        XCTAssertEqual(result.choices().count, 1)
        XCTAssertNotNil(result.choices()[0].message().tool_calls(), "expected non-nil value")
        XCTAssertEqual(result.choices()[0].message().tool_calls()[0].function().name().toString().trimmingCharacters(in: .whitespaces), "get_weather")
        XCTAssertEqual(String(describing: result.choices()[0].finish_reason()).trimmingCharacters(in: .whitespaces), "tool_calls")
    }

}
