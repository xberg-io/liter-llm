// This file is auto-generated by alef — DO NOT EDIT.
// alef:hash:61090fda501af46a87bae1117dd901d2442e9898c26548c991608f68f13a8363
// To regenerate: alef generate
// To verify freshness: alef verify --exit-code
// Issues & docs: https://github.com/kreuzberg-dev/alef
import XCTest
import Foundation
import LiterLlm
import RustBridge

/// E2e tests for category: files.
final class FilesTests: XCTestCase {
    func testEdgeFileEmptyList() async throws {
        // List files when no files have been uploaded
        let _client = try DefaultClient(apiKey: "test-key", baseUrl: ProcessInfo.processInfo.environment["MOCK_SERVER_URL"]! + "/fixtures/edge_file_empty_list")
        let result = try await _client.listFiles(nil)
        XCTAssertEqual(result.data().count, 0)
    }

    func testEdgeFileLargeUpload() async throws {
        // Upload a large file successfully
        let _request = try createFileRequestFromJson("{\"file\":\"eyJwcm9tcHQiOiAibGFyZ2UgdHJhaW5pbmcgZGF0YSJ9Cg==\",\"filename\":\"large_training_data.jsonl\",\"purpose\":\"fine-tune\"}")
        let _client = try DefaultClient(apiKey: "test-key", baseUrl: ProcessInfo.processInfo.environment["MOCK_SERVER_URL"]! + "/fixtures/edge_file_large_upload")
        let result = try await _client.createFile(_request)
        XCTAssertFalse(result.id().toString().isEmpty, "expected non-empty value")
    }

    func testErrorFileAuth401() async throws {
        // 401 Unauthorized when listing files with invalid API key
        let _client = try DefaultClient(apiKey: "test-key", baseUrl: ProcessInfo.processInfo.environment["MOCK_SERVER_URL"]! + "/fixtures/error_file_auth_401")
        do {
            _ = try await _client.listFiles(nil)
            XCTFail("expected to throw")
        } catch {
            // success
        }
    }

    func testErrorFileBadPurpose() async throws {
        // 400 Bad Request when uploading a file with invalid purpose
        let _request = try createFileRequestFromJson("{\"file\":\"data.jsonl\",\"purpose\":\"invalid-purpose\"}")
        let _client = try DefaultClient(apiKey: "test-key", baseUrl: ProcessInfo.processInfo.environment["MOCK_SERVER_URL"]! + "/fixtures/error_file_bad_purpose")
        do {
            _ = try await _client.createFile(_request)
            XCTFail("expected to throw")
        } catch {
            // success
        }
    }

    func testErrorFileNotFound() async throws {
        // 404 Not Found when retrieving a nonexistent file
        let _client = try DefaultClient(apiKey: "test-key", baseUrl: ProcessInfo.processInfo.environment["MOCK_SERVER_URL"]! + "/fixtures/error_file_not_found")
        do {
            _ = try await _client.retrieveFile("file-nonexistent")
            XCTFail("expected to throw")
        } catch {
            // success
        }
    }

    func testSmokeCreateFile() async throws {
        // Upload a file for use with the API
        let _request = try createFileRequestFromJson("{\"file\":\"eyJwcm9tcHQiOiAiaGVsbG8ifQo=\",\"filename\":\"training_data.jsonl\",\"purpose\":\"fine-tune\"}")
        let _client = try DefaultClient(apiKey: "test-key", baseUrl: ProcessInfo.processInfo.environment["MOCK_SERVER_URL"]! + "/fixtures/smoke_create_file")
        let result = try await _client.createFile(_request)
        XCTAssertFalse(result.id().toString().isEmpty, "expected non-empty value")
    }

    func testSmokeDeleteFile() async throws {
        // Delete an uploaded file
        let _client = try DefaultClient(apiKey: "test-key", baseUrl: ProcessInfo.processInfo.environment["MOCK_SERVER_URL"]! + "/fixtures/smoke_delete_file")
        let result = try await _client.deleteFile("file-abc123")
        XCTAssertFalse(result.id().toString().isEmpty, "expected non-empty value")
        XCTAssertEqual(result.deleted(), true)
    }

    func testSmokeFileContent() async throws {
        // Retrieve the content of an uploaded file
        let _client = try DefaultClient(apiKey: "test-key", baseUrl: ProcessInfo.processInfo.environment["MOCK_SERVER_URL"]! + "/fixtures/smoke_file_content")
        let result = try await _client.fileContent("file-abc123")
        XCTAssertFalse(result.isEmpty, "expected non-empty value")
    }

    func testSmokeListFiles() async throws {
        // List all uploaded files
        let _client = try DefaultClient(apiKey: "test-key", baseUrl: ProcessInfo.processInfo.environment["MOCK_SERVER_URL"]! + "/fixtures/smoke_list_files")
        let result = try await _client.listFiles(nil)
        XCTAssertEqual(result.data().count, 2)
    }

    func testSmokeRetrieveFile() async throws {
        // Retrieve metadata for an uploaded file
        let _client = try DefaultClient(apiKey: "test-key", baseUrl: ProcessInfo.processInfo.environment["MOCK_SERVER_URL"]! + "/fixtures/smoke_retrieve_file")
        let result = try await _client.retrieveFile("file-abc123")
        XCTAssertFalse(result.id().toString().isEmpty, "expected non-empty value")
    }

}
