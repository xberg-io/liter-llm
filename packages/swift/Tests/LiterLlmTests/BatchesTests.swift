import Foundation
import LiterLlm
import RustBridge
// This file is auto-generated by alef — DO NOT EDIT.
// alef:hash:aac9e5e5195ffb873d05726979afa3f34a2154970509e0784db45439799d79ae
// To regenerate: alef generate
// To verify freshness: alef verify --exit-code
// Issues & docs: https://github.com/kreuzberg-dev/alef
import XCTest

/// E2e tests for category: batches.
final class BatchesTests: XCTestCase {
  func testEdgeBatchAlreadyCancelled() async throws {
    // Attempt to cancel an already-cancelled batch
    let _client = try DefaultClient(
      apiKey: "test-key",
      baseUrl: ProcessInfo.processInfo.environment["MOCK_SERVER_URL"]!
        + "/fixtures/edge_batch_already_cancelled")
    do {
      _ = try await _client.cancelBatch("batch-cancelled001")
      XCTFail("expected to throw")
    } catch {
      // success
    }
  }

  func testEdgeBatchEmptyList() async throws {
    // List batches when no batches exist
    let _client = try DefaultClient(
      apiKey: "test-key",
      baseUrl: ProcessInfo.processInfo.environment["MOCK_SERVER_URL"]!
        + "/fixtures/edge_batch_empty_list")
    let result = try await _client.listBatches(nil)
    XCTAssertEqual(result.data().count, 0)
  }

  func testErrorBatchAuth401() async throws {
    // 401 Unauthorized when creating a batch with invalid API key
    let _request = try createBatchRequestFromJson(
      "{\"completion_window\":\"24h\",\"endpoint\":\"/v1/chat/completions\",\"input_file_id\":\"file-abc123\"}"
    )
    let _client = try DefaultClient(
      apiKey: "test-key",
      baseUrl: ProcessInfo.processInfo.environment["MOCK_SERVER_URL"]!
        + "/fixtures/error_batch_auth_401")
    do {
      _ = try await _client.createBatch(_request)
      XCTFail("expected to throw")
    } catch {
      // success
    }
  }

  func testErrorBatchInvalidFile() async throws {
    // 400 Bad Request when creating a batch with invalid input file
    let _request = try createBatchRequestFromJson(
      "{\"completion_window\":\"24h\",\"endpoint\":\"/v1/chat/completions\",\"input_file_id\":\"file-wrong-purpose\"}"
    )
    let _client = try DefaultClient(
      apiKey: "test-key",
      baseUrl: ProcessInfo.processInfo.environment["MOCK_SERVER_URL"]!
        + "/fixtures/error_batch_invalid_file")
    do {
      _ = try await _client.createBatch(_request)
      XCTFail("expected to throw")
    } catch {
      // success
    }
  }

  func testErrorBatchNotFound() async throws {
    // 404 Not Found when retrieving a nonexistent batch
    let _client = try DefaultClient(
      apiKey: "test-key",
      baseUrl: ProcessInfo.processInfo.environment["MOCK_SERVER_URL"]!
        + "/fixtures/error_batch_not_found")
    do {
      _ = try await _client.retrieveBatch("batch-nonexistent")
      XCTFail("expected to throw")
    } catch {
      // success
    }
  }

  func testSmokeBatchCompleted() async throws {
    // Retrieve a completed batch job with output file
    let _client = try DefaultClient(
      apiKey: "test-key",
      baseUrl: ProcessInfo.processInfo.environment["MOCK_SERVER_URL"]!
        + "/fixtures/smoke_batch_completed")
    let result = try await _client.retrieveBatch("batch-ghi789")
    XCTAssertFalse(result.id().toString().isEmpty, "expected non-empty value")
    XCTAssertEqual(
      result.status().toString().toString().trimmingCharacters(in: CharacterSet.whitespaces),
      "completed")
  }

  func testSmokeCancelBatch() async throws {
    // Cancel a running batch job
    let _client = try DefaultClient(
      apiKey: "test-key",
      baseUrl: ProcessInfo.processInfo.environment["MOCK_SERVER_URL"]!
        + "/fixtures/smoke_cancel_batch")
    let result = try await _client.cancelBatch("batch-def456")
    XCTAssertFalse(result.id().toString().isEmpty, "expected non-empty value")
    XCTAssertEqual(
      result.status().toString().toString().trimmingCharacters(in: CharacterSet.whitespaces),
      "cancelling")
  }

  func testSmokeCreateBatch() async throws {
    // Create a new batch processing job
    let _request = try createBatchRequestFromJson(
      "{\"completion_window\":\"24h\",\"endpoint\":\"/v1/chat/completions\",\"input_file_id\":\"file-abc123\"}"
    )
    let _client = try DefaultClient(
      apiKey: "test-key",
      baseUrl: ProcessInfo.processInfo.environment["MOCK_SERVER_URL"]!
        + "/fixtures/smoke_create_batch")
    let result = try await _client.createBatch(_request)
    XCTAssertFalse(result.id().toString().isEmpty, "expected non-empty value")
    XCTAssertEqual(
      result.status().toString().trimmingCharacters(in: CharacterSet.whitespaces), "validating")
  }

  func testSmokeListBatches() async throws {
    // List all batch jobs
    let _client = try DefaultClient(
      apiKey: "test-key",
      baseUrl: ProcessInfo.processInfo.environment["MOCK_SERVER_URL"]!
        + "/fixtures/smoke_list_batches")
    let result = try await _client.listBatches(nil)
    XCTAssertEqual(result.data().count, 2)
  }

  func testSmokeRetrieveBatch() async throws {
    // Retrieve the status of a batch job
    let _client = try DefaultClient(
      apiKey: "test-key",
      baseUrl: ProcessInfo.processInfo.environment["MOCK_SERVER_URL"]!
        + "/fixtures/smoke_retrieve_batch")
    let result = try await _client.retrieveBatch("batch-abc123")
    XCTAssertFalse(result.id().toString().isEmpty, "expected non-empty value")
    XCTAssertEqual(
      result.status().toString().toString().trimmingCharacters(in: CharacterSet.whitespaces),
      "in_progress")
  }

}
