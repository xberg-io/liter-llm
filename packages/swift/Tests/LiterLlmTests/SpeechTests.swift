// This file is auto-generated by alef — DO NOT EDIT.
// alef:hash:f494b9be29f08f7bbd28471f920ccd87b74d72ecc96a5e5f1db2268b553af360
// To regenerate: alef generate
// To verify freshness: alef verify --exit-code
// Issues & docs: https://github.com/kreuzberg-dev/alef
import XCTest
import Foundation
import LiterLlm
import RustBridge

/// E2e tests for category: speech.
final class SpeechTests: XCTestCase {
    func testEdgeSpeechAllVoices() async throws {
        // Text-to-speech with specific voice selection
        let _client = try DefaultClient(apiKey: "test-key", baseUrl: ProcessInfo.processInfo.environment["MOCK_SERVER_URL"]! + "/fixtures/edge_speech_all_voices")
        let result = try await _client.speech("Hello world")
        XCTAssertFalse(result.audio().toString().isEmpty, "expected non-empty value")
    }

    func testEdgeSpeechLongInput() async throws {
        // Speech generation with a very long input text
        let _client = try DefaultClient(apiKey: "test-key", baseUrl: ProcessInfo.processInfo.environment["MOCK_SERVER_URL"]! + "/fixtures/edge_speech_long_input")
        let result = try await _client.speech("This is a long input text. This is a long input text. This is a long input text. This is a long input text. This is a long input text. This is a long input text. This is a long input text. This is a long input text. This is a long input text. This is a long input text. This is a long input text. This is a long input text. This is a long input text. This is a long input text. This is a long input text. This is a long input text. This is a long input text. This is a long input text. This is a long input text. This is a long input text. This is a long input text. This is a long input text. This is a long input text. This is a long input text. This is a long input text. This is a long input text. This is a long input text. This is a long input text. This is a long input text. This is a long input text. This is a long input text. This is a long input text. This is a long input text. This is a long input text. This is a long input text. This is a long input text. This is a long input text. This is a long input text. This is a long input text. This is a long input text. This is a long input text. This is a long input text. This is a long input text. This is a long input text. This is a long input text. This is a long input text. This is a long input text. This is a long input text. This is a long input text. This is a long input text. End of input.")
        XCTAssertFalse(result.audio().toString().isEmpty, "expected non-empty value")
    }

    func testErrorSpeechAuth401() async throws {
        // 401 Unauthorized for speech generation with invalid API key
        let _client = try DefaultClient(apiKey: "test-key", baseUrl: ProcessInfo.processInfo.environment["MOCK_SERVER_URL"]! + "/fixtures/error_speech_auth_401")
        do {
            _ = try await _client.speech("Hello")
            XCTFail("expected to throw")
        } catch {
            // success
        }
    }

    func testErrorSpeechBadModel() async throws {
        // 400 Bad Request for speech with unsupported model
        let _client = try DefaultClient(apiKey: "test-key", baseUrl: ProcessInfo.processInfo.environment["MOCK_SERVER_URL"]! + "/fixtures/error_speech_bad_model")
        do {
            _ = try await _client.speech("Hello")
            XCTFail("expected to throw")
        } catch {
            // success
        }
    }

    func testSmokeSpeechBasic() async throws {
        // Basic text-to-speech generation
        let _client = try DefaultClient(apiKey: "test-key", baseUrl: ProcessInfo.processInfo.environment["MOCK_SERVER_URL"]! + "/fixtures/smoke_speech_basic")
        let result = try await _client.speech("Hello, world!")
        XCTAssertFalse(result.audio().toString().isEmpty, "expected non-empty value")
    }

    func testSmokeSpeechMp3Format() async throws {
        // Text-to-speech with explicit MP3 response format
        let _client = try DefaultClient(apiKey: "test-key", baseUrl: ProcessInfo.processInfo.environment["MOCK_SERVER_URL"]! + "/fixtures/smoke_speech_mp3_format")
        let result = try await _client.speech("The quick brown fox jumps over the lazy dog.")
        XCTAssertFalse(result.audio().toString().isEmpty, "expected non-empty value")
    }

}
