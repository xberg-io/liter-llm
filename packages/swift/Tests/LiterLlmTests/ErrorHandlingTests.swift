// This file is auto-generated by alef — DO NOT EDIT.
// alef:hash:783efbbb4889a383818e4a2333198effd86ad5cd0fb1dd2ee780278b5eaf1cf3
// To regenerate: alef generate
// To verify freshness: alef verify --exit-code
// Issues & docs: https://github.com/kreuzberg-dev/alef
import XCTest
import Foundation
import LiterLlm
import RustBridge

/// E2e tests for category: error-handling.
final class ErrorHandlingTests: XCTestCase {
    func testAnthropicErrorAuth() async throws {
        // 401 Authentication error returned by the Anthropic API when the API key is invalid
        let _client = try DefaultClient(apiKey: "test-key", baseUrl: ProcessInfo.processInfo.environment["MOCK_SERVER_URL"]! + "/fixtures/anthropic_error_auth")
        do {
            _ = try await _client.chat(nil)
            XCTFail("expected to throw")
        } catch {
            // success
        }
    }

    func testAuth401() async throws {
        // 401 Unauthorized error when API key is invalid or missing
        let _client = try DefaultClient(apiKey: "test-key", baseUrl: ProcessInfo.processInfo.environment["MOCK_SERVER_URL"]! + "/fixtures/auth_401")
        do {
            _ = try await _client.chat(nil)
            XCTFail("expected to throw")
        } catch {
            // success
        }
    }

    func testAzureErrorAuth() async throws {
        // Azure OpenAI returns a 401 Unauthorized error when the API key is missing or invalid — uses Azure's error envelope shape with code AccessDenied
        let _client = try DefaultClient(apiKey: "test-key", baseUrl: ProcessInfo.processInfo.environment["MOCK_SERVER_URL"]! + "/fixtures/azure_error_auth")
        do {
            _ = try await _client.chat(nil)
            XCTFail("expected to throw")
        } catch {
            // success
        }
    }

    func testBadRequest400() async throws {
        // 400 Bad Request error when a parameter value is invalid
        let _client = try DefaultClient(apiKey: "test-key", baseUrl: ProcessInfo.processInfo.environment["MOCK_SERVER_URL"]! + "/fixtures/bad_request_400")
        do {
            _ = try await _client.chat(nil)
            XCTFail("expected to throw")
        } catch {
            // success
        }
    }

    func testBedrockErrorAuth() async throws {
        // AWS Bedrock returns 403 Forbidden (not 401) when credentials are missing, expired, or the IAM role lacks bedrock:InvokeModel permission — verifies the error is mapped to Authentication
        let _client = try DefaultClient(apiKey: "test-key", baseUrl: ProcessInfo.processInfo.environment["MOCK_SERVER_URL"]! + "/fixtures/bedrock_error_auth")
        do {
            _ = try await _client.chat(nil)
            XCTFail("expected to throw")
        } catch {
            // success
        }
    }

    func testContentPolicyViolation() async throws {
        // 400 error when a request is rejected due to content policy
        let _client = try DefaultClient(apiKey: "test-key", baseUrl: ProcessInfo.processInfo.environment["MOCK_SERVER_URL"]! + "/fixtures/content_policy_violation")
        do {
            _ = try await _client.chat(nil)
            XCTFail("expected to throw")
        } catch {
            // success
        }
    }

    func testContextWindowExceeded() async throws {
        // 400 error when the prompt exceeds the model's maximum context length
        let _client = try DefaultClient(apiKey: "test-key", baseUrl: ProcessInfo.processInfo.environment["MOCK_SERVER_URL"]! + "/fixtures/context_window_exceeded")
        do {
            _ = try await _client.chat(nil)
            XCTFail("expected to throw")
        } catch {
            // success
        }
    }

    func testEmptyResponseBody() async throws {
        // 200 OK response with an empty JSON object body, missing required fields
        let _client = try DefaultClient(apiKey: "test-key", baseUrl: ProcessInfo.processInfo.environment["MOCK_SERVER_URL"]! + "/fixtures/empty_response_body")
        do {
            _ = try await _client.chat(nil)
            XCTFail("expected to throw")
        } catch {
            // success
        }
    }

    func testForbidden403() async throws {
        // 403 Forbidden error when the API key does not have access to the requested resource
        let _client = try DefaultClient(apiKey: "test-key", baseUrl: ProcessInfo.processInfo.environment["MOCK_SERVER_URL"]! + "/fixtures/forbidden_403")
        do {
            _ = try await _client.chat(nil)
            XCTFail("expected to throw")
        } catch {
            // success
        }
    }

    func testGatewayTimeout504() async throws {
        // 504 Gateway Timeout error when the upstream service times out
        let _client = try DefaultClient(apiKey: "test-key", baseUrl: ProcessInfo.processInfo.environment["MOCK_SERVER_URL"]! + "/fixtures/gateway_timeout_504")
        do {
            _ = try await _client.chat(nil)
            XCTFail("expected to throw")
        } catch {
            // success
        }
    }

    func testGithubCopilotErrorAuth() async throws {
        // 401 Authentication error returned by the GitHub Copilot API when the token is invalid or expired
        let _client = try DefaultClient(apiKey: "test-key", baseUrl: ProcessInfo.processInfo.environment["MOCK_SERVER_URL"]! + "/fixtures/github_copilot_error_auth")
        do {
            _ = try await _client.chat(nil)
            XCTFail("expected to throw")
        } catch {
            // success
        }
    }

    func testNotFound404() async throws {
        // 404 Not Found error when requesting a model that does not exist
        let _client = try DefaultClient(apiKey: "test-key", baseUrl: ProcessInfo.processInfo.environment["MOCK_SERVER_URL"]! + "/fixtures/not_found_404")
        do {
            _ = try await _client.chat(nil)
            XCTFail("expected to throw")
        } catch {
            // success
        }
    }

    func testRateLimit429() async throws {
        // 429 Too Many Requests error when the rate limit is exceeded
        let _client = try DefaultClient(apiKey: "test-key", baseUrl: ProcessInfo.processInfo.environment["MOCK_SERVER_URL"]! + "/fixtures/rate_limit_429")
        do {
            _ = try await _client.chat(nil)
            XCTFail("expected to throw")
        } catch {
            // success
        }
    }

    func testServerError500() async throws {
        // 500 Internal Server Error from the upstream API
        let _client = try DefaultClient(apiKey: "test-key", baseUrl: ProcessInfo.processInfo.environment["MOCK_SERVER_URL"]! + "/fixtures/server_error_500")
        do {
            _ = try await _client.chat(nil)
            XCTFail("expected to throw")
        } catch {
            // success
        }
    }

    func testServiceUnavailable502() async throws {
        // 502 Bad Gateway error when the upstream service is unavailable
        let _client = try DefaultClient(apiKey: "test-key", baseUrl: ProcessInfo.processInfo.environment["MOCK_SERVER_URL"]! + "/fixtures/service_unavailable_502")
        do {
            _ = try await _client.chat(nil)
            XCTFail("expected to throw")
        } catch {
            // success
        }
    }

    func testTimeoutError() async throws {
        // 408 Request Timeout error when the API request takes too long to complete
        let _client = try DefaultClient(apiKey: "test-key", baseUrl: ProcessInfo.processInfo.environment["MOCK_SERVER_URL"]! + "/fixtures/timeout_error")
        do {
            _ = try await _client.chat(nil)
            XCTFail("expected to throw")
        } catch {
            // success
        }
    }

    func testVertexErrorAuth() async throws {
        // Google Vertex AI returns 401 Unauthorized when the OAuth2 token is missing, expired, or the service account lacks aiplatform.endpoints.predict permission — verifies the error is mapped to Authentication
        let _client = try DefaultClient(apiKey: "test-key", baseUrl: ProcessInfo.processInfo.environment["MOCK_SERVER_URL"]! + "/fixtures/vertex_error_auth")
        do {
            _ = try await _client.chat(nil)
            XCTFail("expected to throw")
        } catch {
            // success
        }
    }

}
