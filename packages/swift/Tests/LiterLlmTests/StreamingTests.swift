// This file is auto-generated by alef — DO NOT EDIT.
// alef:hash:aa9aa476ac41114a78222c7b95c7f4c1d1d59f48f0b20416c648bb18905d62da
// To regenerate: alef generate
// To verify freshness: alef verify --exit-code
// Issues & docs: https://github.com/kreuzberg-dev/alef
import XCTest
import Foundation
import LiterLlm
import RustBridge

/// E2e tests for category: streaming.
final class StreamingTests: XCTestCase {
    func testAnthropicStream() async throws {
        // Streaming chat completion via the Anthropic provider (claude-3-5-sonnet-20241022) yielding multiple SSE chunks
        let _client = try DefaultClient(apiKey: "test-key", baseUrl: ProcessInfo.processInfo.environment["MOCK_SERVER_URL"]! + "/fixtures/anthropic_stream")
        let result = try await _client.chatStream(nil)
        // skipped: field '{f}' not available on result type
        // skipped: field '{f}' not available on result type
        // skipped: field '{f}' not available on result type
    }

    func testAzureStream() async throws {
        // Streaming chat completion via Azure OpenAI — verifies the azure/ prefix routes correctly and SSE chunks are delivered in the standard OpenAI chat.completion.chunk shape
        let _client = try DefaultClient(apiKey: "test-key", baseUrl: ProcessInfo.processInfo.environment["MOCK_SERVER_URL"]! + "/fixtures/azure_stream")
        let result = try await _client.chatStream(nil)
        // skipped: field '{f}' not available on result type
        // skipped: field '{f}' not available on result type
        // skipped: field '{f}' not available on result type
    }

    func testBasicStream() async throws {
        // Streaming chat completion that produces content across multiple SSE chunks
        let _client = try DefaultClient(apiKey: "test-key", baseUrl: ProcessInfo.processInfo.environment["MOCK_SERVER_URL"]! + "/fixtures/basic_stream")
        let result = try await _client.chatStream(nil)
        // skipped: field '{f}' not available on result type
        // skipped: field '{f}' not available on result type
    }

    func testBedrockStream() async throws {
        // Streaming chat completion via the AWS Bedrock provider using the bedrock/ prefix — verifies SSE chunks are yielded and assembled correctly from the Converse streaming API
        let _client = try DefaultClient(apiKey: "test-key", baseUrl: ProcessInfo.processInfo.environment["MOCK_SERVER_URL"]! + "/fixtures/bedrock_stream")
        let result = try await _client.chatStream(nil)
        // skipped: field '{f}' not available on result type
        // skipped: field '{f}' not available on result type
        // skipped: field '{f}' not available on result type
    }

    func testEdgeStreamFunctionCall() async throws {
        // Streaming chat completion with tool/function call chunks
        let _client = try DefaultClient(apiKey: "test-key", baseUrl: ProcessInfo.processInfo.environment["MOCK_SERVER_URL"]! + "/fixtures/edge_stream_function_call")
        let result = try await _client.chatStream(nil)
        // skipped: field '{f}' not available on result type
    }

    func testEmptyStream() async throws {
        // Streaming chat completion that produces no content chunks before the DONE signal
        let _client = try DefaultClient(apiKey: "test-key", baseUrl: ProcessInfo.processInfo.environment["MOCK_SERVER_URL"]! + "/fixtures/empty_stream")
        let result = try await _client.chatStream(nil)
        // skipped: field '{f}' not available on result type
        // skipped: field '{f}' not available on result type
    }

    func testLocalStreamOllama() async throws {
        // Streaming chat completion via Ollama local provider with SSE chunks
        let _client = try DefaultClient(apiKey: "test-key", baseUrl: ProcessInfo.processInfo.environment["MOCK_SERVER_URL"]! + "/fixtures/local_stream_ollama")
        let result = try await _client.chatStream(nil)
        // skipped: field '{f}' not available on result type
        // skipped: field '{f}' not available on result type
        // skipped: field '{f}' not available on result type
    }

    func testStreamContentPolicyError() async throws {
        // 400 Bad Request error on stream due to content policy violation
        let _client = try DefaultClient(apiKey: "test-key", baseUrl: ProcessInfo.processInfo.environment["MOCK_SERVER_URL"]! + "/fixtures/stream_content_policy_error")
        do {
            _ = try await _client.chatStream(nil)
            XCTFail("expected to throw")
        } catch {
            // success
        }
    }

    func testStreamDoneSignal() async throws {
        // Verify that the [DONE] sentinel signal properly terminates the stream
        let _client = try DefaultClient(apiKey: "test-key", baseUrl: ProcessInfo.processInfo.environment["MOCK_SERVER_URL"]! + "/fixtures/stream_done_signal")
        let result = try await _client.chatStream(nil)
        // skipped: field '{f}' not available on result type
        // skipped: field '{f}' not available on result type
        // skipped: field '{f}' not available on result type
    }

    func testStreamError401() async throws {
        // 401 Unauthorized error on stream initiation before any chunks are received
        let _client = try DefaultClient(apiKey: "test-key", baseUrl: ProcessInfo.processInfo.environment["MOCK_SERVER_URL"]! + "/fixtures/stream_error_401")
        do {
            _ = try await _client.chatStream(nil)
            XCTFail("expected to throw")
        } catch {
            // success
        }
    }

    func testStreamMultipleChoices() async throws {
        // Streaming chat completion with multiple choice outputs (n > 1)
        let _client = try DefaultClient(apiKey: "test-key", baseUrl: ProcessInfo.processInfo.environment["MOCK_SERVER_URL"]! + "/fixtures/stream_multiple_choices")
        let result = try await _client.chatStream(nil)
    }

    func testStreamWithToolCalls() async throws {
        // Streaming chat completion where the assistant responds with a tool call across multiple chunks
        let _client = try DefaultClient(apiKey: "test-key", baseUrl: ProcessInfo.processInfo.environment["MOCK_SERVER_URL"]! + "/fixtures/stream_with_tool_calls")
        let result = try await _client.chatStream(nil)
        // skipped: field '{f}' not available on result type
        // skipped: field '{f}' not available on result type
        // skipped: field '{f}' not available on result type
        // skipped: field '{f}' not available on result type
    }

    func testStreamWithUsage() async throws {
        // Streaming chat completion that includes a usage summary in the final chunk
        let _client = try DefaultClient(apiKey: "test-key", baseUrl: ProcessInfo.processInfo.environment["MOCK_SERVER_URL"]! + "/fixtures/stream_with_usage")
        let result = try await _client.chatStream(nil)
        // skipped: field '{f}' not available on result type
        // skipped: field '{f}' not available on result type
        XCTAssertEqual(result.usage()?.total_tokens(), 18)
    }

    func testVertexStream() async throws {
        // Streaming chat completion via the Google Vertex AI provider using the vertex_ai/ prefix — verifies SSE chunks from the Gemini streaming endpoint are yielded and assembled correctly
        let _client = try DefaultClient(apiKey: "test-key", baseUrl: ProcessInfo.processInfo.environment["MOCK_SERVER_URL"]! + "/fixtures/vertex_stream")
        let result = try await _client.chatStream(nil)
        // skipped: field '{f}' not available on result type
        // skipped: field '{f}' not available on result type
        // skipped: field '{f}' not available on result type
    }

}
