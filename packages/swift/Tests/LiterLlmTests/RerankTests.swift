// This file is auto-generated by alef — DO NOT EDIT.
// alef:hash:3e2ca7e18d1c76646b149a90a68231ed9085fae08a9da2a703ad4a662fcdac6a
// To regenerate: alef generate
// To verify freshness: alef verify --exit-code
// Issues & docs: https://github.com/kreuzberg-dev/alef
import XCTest
import Foundation
import LiterLlm
import RustBridge

/// E2e tests for category: rerank.
final class RerankTests: XCTestCase {
    func testEdgeRerankEmptyQuery() async throws {
        // Reranking with an empty query string
        let _request = try rerankRequestFromJson("{\"documents\":[\"Some document\",\"Another document\"],\"model\":\"rerank-v3.5\",\"query\":\"\"}")
        let _client = try DefaultClient(apiKey: "test-key", baseUrl: ProcessInfo.processInfo.environment["MOCK_SERVER_URL"]! + "/fixtures/edge_rerank_empty_query")
        let result = try await _client.rerank(_request)
        XCTAssertEqual(result.results().count, 2)
    }

    func testEdgeRerankSingleDoc() async throws {
        // Reranking with only a single document
        let _request = try rerankRequestFromJson("{\"documents\":[\"Artificial intelligence is the simulation of human intelligence.\"],\"model\":\"rerank-v3.5\",\"query\":\"What is AI?\"}")
        let _client = try DefaultClient(apiKey: "test-key", baseUrl: ProcessInfo.processInfo.environment["MOCK_SERVER_URL"]! + "/fixtures/edge_rerank_single_doc")
        let result = try await _client.rerank(_request)
        XCTAssertEqual(result.results().count, 1)
        XCTAssertGreaterThan(result.results()[0].relevance_score(), 0.9)
    }

    func testErrorRerankAuth401() async throws {
        // 401 Unauthorized for reranking with invalid API key
        let _request = try rerankRequestFromJson("{\"documents\":[\"doc1\"],\"model\":\"rerank-v3.5\",\"query\":\"test\"}")
        let _client = try DefaultClient(apiKey: "test-key", baseUrl: ProcessInfo.processInfo.environment["MOCK_SERVER_URL"]! + "/fixtures/error_rerank_auth_401")
        do {
            _ = try await _client.rerank(_request)
            XCTFail("expected to throw")
        } catch {
            // success
        }
    }

    func testErrorRerankBadRequest() async throws {
        // 400 Bad Request for reranking with invalid model
        let _request = try rerankRequestFromJson("{\"documents\":[\"doc1\"],\"model\":\"nonexistent-rerank\",\"query\":\"test\"}")
        let _client = try DefaultClient(apiKey: "test-key", baseUrl: ProcessInfo.processInfo.environment["MOCK_SERVER_URL"]! + "/fixtures/error_rerank_bad_request")
        do {
            _ = try await _client.rerank(_request)
            XCTFail("expected to throw")
        } catch {
            // success
        }
    }

    func testSmokeRerankBasic() async throws {
        // Basic reranking of documents against a query
        let _request = try rerankRequestFromJson("{\"documents\":[\"Machine learning is a subset of AI.\",\"The weather is sunny today.\",\"Deep learning uses neural networks.\"],\"model\":\"rerank-v3.5\",\"query\":\"What is machine learning?\"}")
        let _client = try DefaultClient(apiKey: "test-key", baseUrl: ProcessInfo.processInfo.environment["MOCK_SERVER_URL"]! + "/fixtures/smoke_rerank_basic")
        let result = try await _client.rerank(_request)
        XCTAssertEqual(result.results().count, 3)
        XCTAssertGreaterThan(result.results()[0].relevance_score(), 0.9)
    }

    func testSmokeRerankReturnDocs() async throws {
        // Reranking with return_documents flag to include document text
        let _request = try rerankRequestFromJson("{\"documents\":[\"Rust is a systems programming language.\",\"Iron rusts when exposed to water.\"],\"model\":\"rerank-v3.5\",\"query\":\"What is Rust?\",\"return_documents\":true}")
        let _client = try DefaultClient(apiKey: "test-key", baseUrl: ProcessInfo.processInfo.environment["MOCK_SERVER_URL"]! + "/fixtures/smoke_rerank_return_docs")
        let result = try await _client.rerank(_request)
        XCTAssertEqual(result.results().count, 2)
        XCTAssertNotNil(result.results()[0].document(), "expected non-nil value")
        XCTAssertGreaterThan(result.results()[0].relevance_score(), 0.9)
    }

    func testSmokeRerankWithTopN() async throws {
        // Reranking with top_n parameter to limit results
        let _request = try rerankRequestFromJson("{\"documents\":[\"Python is a programming language.\",\"Cats are cute animals.\",\"Python was created by Guido van Rossum.\",\"The sun is a star.\"],\"model\":\"rerank-v3.5\",\"query\":\"What is Python?\",\"top_n\":2}")
        let _client = try DefaultClient(apiKey: "test-key", baseUrl: ProcessInfo.processInfo.environment["MOCK_SERVER_URL"]! + "/fixtures/smoke_rerank_with_top_n")
        let result = try await _client.rerank(_request)
        XCTAssertEqual(result.results().count, 2)
        XCTAssertGreaterThan(result.results()[0].relevance_score(), 0.9)
    }

}
