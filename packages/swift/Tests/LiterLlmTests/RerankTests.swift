// This file is auto-generated by alef — DO NOT EDIT.
// alef:hash:9fb5d82054c83027d20db3573b34fcfc84486c46aa99aa85cdc764cc6676afc2
// To regenerate: alef generate
// To verify freshness: alef verify --exit-code
// Issues & docs: https://github.com/kreuzberg-dev/alef
import XCTest
import Foundation
import LiterLlm
import RustBridge

/// E2e tests for category: rerank.
final class RerankTests: XCTestCase {
    func testEdgeRerankEmptyQuery() async throws {
        // Reranking with an empty query string
        try XCTSkipIf(true, "swift: json_object request construction requires options_via configuration (fixture: edge_rerank_empty_query)");
    }

    func testEdgeRerankSingleDoc() async throws {
        // Reranking with only a single document
        try XCTSkipIf(true, "swift: json_object request construction requires options_via configuration (fixture: edge_rerank_single_doc)");
    }

    func testErrorRerankAuth401() async throws {
        // 401 Unauthorized for reranking with invalid API key
        try XCTSkipIf(true, "swift: json_object request construction requires options_via configuration (fixture: error_rerank_auth_401)");
    }

    func testErrorRerankBadRequest() async throws {
        // 400 Bad Request for reranking with invalid model
        try XCTSkipIf(true, "swift: json_object request construction requires options_via configuration (fixture: error_rerank_bad_request)");
    }

    func testSmokeRerankBasic() async throws {
        // Basic reranking of documents against a query
        try XCTSkipIf(true, "swift: json_object request construction requires options_via configuration (fixture: smoke_rerank_basic)");
    }

    func testSmokeRerankReturnDocs() async throws {
        // Reranking with return_documents flag to include document text
        try XCTSkipIf(true, "swift: json_object request construction requires options_via configuration (fixture: smoke_rerank_return_docs)");
    }

    func testSmokeRerankWithTopN() async throws {
        // Reranking with top_n parameter to limit results
        try XCTSkipIf(true, "swift: json_object request construction requires options_via configuration (fixture: smoke_rerank_with_top_n)");
    }

}
