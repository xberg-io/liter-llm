// This file is auto-generated by alef — DO NOT EDIT.
// alef:hash:9cd481bc44a773a187a4ca1cf4630e9da60f12bcbc2da4f862db06d5f7b7453d
// To regenerate: alef generate
// To verify freshness: alef verify --exit-code
// Issues & docs: https://github.com/kreuzberg-dev/alef
import XCTest
import Foundation
import LiterLlm
import RustBridge

/// E2e tests for category: search.
final class SearchTests: XCTestCase {
    func testSearchBasic() async throws {
        // Basic web search request with a simple query
        let _client = try DefaultClient(apiKey: "test-key", baseUrl: ProcessInfo.processInfo.environment["MOCK_SERVER_URL"]! + "/fixtures/search_basic")
        let result = try await _client.search(nil)
    }

    func testSearchEmptyResults() async throws {
        // Web search with a query that returns no results
        let _client = try DefaultClient(apiKey: "test-key", baseUrl: ProcessInfo.processInfo.environment["MOCK_SERVER_URL"]! + "/fixtures/search_empty_results")
        let result = try await _client.search(nil)
    }

    func testSearchError400() async throws {
        // 400 Bad Request error when search query is empty
        let _client = try DefaultClient(apiKey: "test-key", baseUrl: ProcessInfo.processInfo.environment["MOCK_SERVER_URL"]! + "/fixtures/search_error_400")
        do {
            _ = try await _client.search(nil)
            XCTFail("expected to throw")
        } catch {
            // success
        }
    }

    func testSearchError401() async throws {
        // 401 Unauthorized error on web search due to invalid API credentials
        let _client = try DefaultClient(apiKey: "test-key", baseUrl: ProcessInfo.processInfo.environment["MOCK_SERVER_URL"]! + "/fixtures/search_error_401")
        do {
            _ = try await _client.search(nil)
            XCTFail("expected to throw")
        } catch {
            // success
        }
    }

    func testSearchWithMaxResults() async throws {
        // Search request with max_results parameter limiting response count
        let _client = try DefaultClient(apiKey: "test-key", baseUrl: ProcessInfo.processInfo.environment["MOCK_SERVER_URL"]! + "/fixtures/search_with_max_results")
        let result = try await _client.search(nil)
        XCTAssertEqual(result.results().count, 2)
    }

}
