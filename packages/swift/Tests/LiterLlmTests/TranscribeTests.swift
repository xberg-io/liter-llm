// This file is auto-generated by alef — DO NOT EDIT.
// alef:hash:1df1e9c42520411c9d25eac0e4d833733f13670b2b34f8e2ff2cad57d6a297f3
// To regenerate: alef generate
// To verify freshness: alef verify --exit-code
// Issues & docs: https://github.com/kreuzberg-dev/alef
import XCTest
import Foundation
import LiterLlm
import RustBridge

/// E2e tests for category: transcribe.
final class TranscribeTests: XCTestCase {
    func testEdgeTranscribeEmptyAudio() async throws {
        // Transcription of a silent or empty audio file returns empty text
        let _request = try createTranscriptionRequestFromJson("{\"file\":\"silence.mp3\",\"model\":\"whisper-1\"}")
        let _client = try DefaultClient(apiKey: "test-key", baseUrl: ProcessInfo.processInfo.environment["MOCK_SERVER_URL"]! + "/fixtures/edge_transcribe_empty_audio")
        let result = try await _client.transcribe(_request)
        XCTAssertTrue(result.text().toString().contains(""), "expected to contain: \("")")
    }

    func testEdgeTranscribeWithTimestamps() async throws {
        // Transcription with verbose JSON response format including timestamp segments
        let _request = try createTranscriptionRequestFromJson("{\"file\":\"audio.mp3\",\"model\":\"whisper-1\",\"response_format\":\"verbose_json\"}")
        let _client = try DefaultClient(apiKey: "test-key", baseUrl: ProcessInfo.processInfo.environment["MOCK_SERVER_URL"]! + "/fixtures/edge_transcribe_with_timestamps")
        let result = try await _client.transcribe(_request)
        XCTAssertFalse(result.text().toString().isEmpty, "expected non-empty value")
        XCTAssertEqual((result.segments()?.count ?? 0), 3)
        XCTAssertEqual(result.segments()[0].id(), 0)
    }

    func testErrorTranscribeAuth401() async throws {
        // 401 Unauthorized for transcription with invalid API key
        let _request = try createTranscriptionRequestFromJson("{\"file\":\"audio.mp3\",\"model\":\"whisper-1\"}")
        let _client = try DefaultClient(apiKey: "test-key", baseUrl: ProcessInfo.processInfo.environment["MOCK_SERVER_URL"]! + "/fixtures/error_transcribe_auth_401")
        do {
            _ = try await _client.transcribe(_request)
            XCTFail("expected to throw")
        } catch {
            // success
        }
    }

    func testErrorTranscribeBadFormat() async throws {
        // 400 Bad Request when audio format is unsupported
        let _request = try createTranscriptionRequestFromJson("{\"file\":\"audio.xyz\",\"model\":\"whisper-1\"}")
        let _client = try DefaultClient(apiKey: "test-key", baseUrl: ProcessInfo.processInfo.environment["MOCK_SERVER_URL"]! + "/fixtures/error_transcribe_bad_format")
        do {
            _ = try await _client.transcribe(_request)
            XCTFail("expected to throw")
        } catch {
            // success
        }
    }

    func testSmokeTranscribeBasic() async throws {
        // Basic audio transcription
        let _request = try createTranscriptionRequestFromJson("{\"file\":\"audio.mp3\",\"model\":\"whisper-1\"}")
        let _client = try DefaultClient(apiKey: "test-key", baseUrl: ProcessInfo.processInfo.environment["MOCK_SERVER_URL"]! + "/fixtures/smoke_transcribe_basic")
        let result = try await _client.transcribe(_request)
        XCTAssertTrue(result.text().toString().contains("Hello, this is a test transcription."), "expected to contain: \("Hello, this is a test transcription.")")
    }

    func testSmokeTranscribeWithLanguage() async throws {
        // Audio transcription with explicit language hint
        let _request = try createTranscriptionRequestFromJson("{\"file\":\"audio_de.mp3\",\"language\":\"de\",\"model\":\"whisper-1\"}")
        let _client = try DefaultClient(apiKey: "test-key", baseUrl: ProcessInfo.processInfo.environment["MOCK_SERVER_URL"]! + "/fixtures/smoke_transcribe_with_language")
        let result = try await _client.transcribe(_request)
        XCTAssertTrue(result.text().toString().contains("Hallo, dies ist ein Testtranskription."), "expected to contain: \("Hallo, dies ist ein Testtranskription.")")
    }

}
