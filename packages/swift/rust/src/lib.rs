// Generated by alef. Do not edit by hand.
// alef:hash:614a9a228c315e2e022431f3529f2dc8acdbb8c9b89123fe631385dfe0d5ad5f

#![allow(unused_variables, unreachable_code, unreachable_patterns, missing_docs)]
#![allow(
    clippy::collapsible_if,
    clippy::collapsible_match,
    clippy::needless_borrows_for_generic_args,
    clippy::field_reassign_with_default,
    clippy::too_many_arguments,
    clippy::clone_on_copy,
    clippy::unnecessary_cast,
    clippy::manual_flatten,
    clippy::match_single_binding,
    clippy::redundant_closure,
    clippy::useless_conversion,
    clippy::inherent_to_string,
    clippy::new_without_default
)]
#![allow(clippy::single_match)]

/// Process-wide tokio runtime shared across every swift-bridge async wrapper.
///
/// alef-emitted; see shims.rs for the rationale (orphaned reqwest connection
/// pools when each call creates and drops its own current-thread runtime).
fn __alef_tokio_runtime() -> &'static ::tokio::runtime::Runtime {
    use std::sync::OnceLock;
    static RT: OnceLock<::tokio::runtime::Runtime> = OnceLock::new();
    RT.get_or_init(|| {
        ::tokio::runtime::Builder::new_multi_thread()
            .enable_all()
            .build()
            .expect("build process-wide alef tokio runtime")
    })
}

#[swift_bridge::bridge]
mod ffi {
    extern "Rust" {
        type SystemMessage;
        #[swift_bridge(init)]
        fn new(content: UserContent, name: Option<String>) -> SystemMessage;
        fn content(&self) -> String;
        fn name(&self) -> Option<String>;
    }

    extern "Rust" {
        type UserMessage;
        #[swift_bridge(init)]
        fn new(content: UserContent, name: Option<String>) -> UserMessage;
        fn content(&self) -> String;
        fn name(&self) -> Option<String>;
    }

    extern "Rust" {
        type ImageUrl;
        #[swift_bridge(init)]
        fn new(url: String, detail: Option<ImageDetail>) -> ImageUrl;
        fn url(&self) -> String;
        fn detail(&self) -> Option<String>;
    }

    extern "Rust" {
        type DocumentContent;
        #[swift_bridge(init)]
        fn new(data: String, media_type: String) -> DocumentContent;
        fn data(&self) -> String;
        #[swift_bridge(swift_name = "mediaType")]
        fn media_type(&self) -> String;
    }

    extern "Rust" {
        type AudioContent;
        #[swift_bridge(init)]
        fn new(data: String, format: String) -> AudioContent;
        fn data(&self) -> String;
        fn format(&self) -> String;
    }

    extern "Rust" {
        type AssistantMessage;
        #[swift_bridge(init)]
        fn new(
            content: Option<AssistantContent>,
            name: Option<String>,
            tool_calls: Option<Vec<ToolCall>>,
            refusal: Option<String>,
            function_call: Option<FunctionCall>,
        ) -> AssistantMessage;
        fn content(&self) -> Option<String>;
        fn name(&self) -> Option<String>;
        #[swift_bridge(swift_name = "toolCalls")]
        fn tool_calls(&self) -> String;
        fn refusal(&self) -> Option<String>;
        #[swift_bridge(swift_name = "functionCall")]
        fn function_call(&self) -> Option<FunctionCall>;
    }

    extern "Rust" {
        #[swift_bridge(swift_name = "assistantMessageTextFromJson")]
        fn assistant_message_text_from_json(json: String) -> Result<String, String>;
        #[swift_bridge(swift_name = "assistantMessageRefusalTextFromJson")]
        fn assistant_message_refusal_text_from_json(json: String) -> Result<String, String>;
        #[swift_bridge(swift_name = "assistantMessageOutputImagesFromJson")]
        fn assistant_message_output_images_from_json(json: String) -> Result<String, String>;
        #[swift_bridge(swift_name = "assistantMessageOutputAudioFromJson")]
        fn assistant_message_output_audio_from_json(json: String) -> Result<String, String>;
    }

    extern "Rust" {
        type ToolMessage;
        #[swift_bridge(init)]
        fn new(content: String, tool_call_id: String, name: Option<String>) -> ToolMessage;
        fn content(&self) -> String;
        #[swift_bridge(swift_name = "toolCallId")]
        fn tool_call_id(&self) -> String;
        fn name(&self) -> Option<String>;
    }

    extern "Rust" {
        type DeveloperMessage;
        #[swift_bridge(init)]
        fn new(content: String, name: Option<String>) -> DeveloperMessage;
        fn content(&self) -> String;
        fn name(&self) -> Option<String>;
    }

    extern "Rust" {
        type FunctionMessage;
        #[swift_bridge(init)]
        fn new(content: String, name: String) -> FunctionMessage;
        fn content(&self) -> String;
        fn name(&self) -> String;
    }

    extern "Rust" {
        type ChatCompletionTool;
        #[swift_bridge(swift_name = "toolType")]
        fn tool_type(&self) -> String;
        fn function(&self) -> FunctionDefinition;
    }

    extern "Rust" {
        type FunctionDefinition;
        fn name(&self) -> String;
        fn description(&self) -> Option<String>;
        fn parameters(&self) -> Option<String>;
        fn strict(&self) -> Option<bool>;
    }

    extern "Rust" {
        type ToolCall;
        fn id(&self) -> String;
        #[swift_bridge(swift_name = "callType")]
        fn call_type(&self) -> String;
        fn function(&self) -> FunctionCall;
    }

    extern "Rust" {
        type FunctionCall;
        fn name(&self) -> String;
        fn arguments(&self) -> String;
    }

    extern "Rust" {
        type SpecificToolChoice;
        #[swift_bridge(init)]
        fn new(choice_type: ToolType, function: SpecificFunction) -> SpecificToolChoice;
        #[swift_bridge(swift_name = "choiceType")]
        fn choice_type(&self) -> String;
        fn function(&self) -> SpecificFunction;
    }

    extern "Rust" {
        type SpecificFunction;
        #[swift_bridge(init)]
        fn new(name: String) -> SpecificFunction;
        fn name(&self) -> String;
    }

    extern "Rust" {
        type JsonSchemaFormat;
        #[swift_bridge(init)]
        fn new(name: String, description: Option<String>, schema: String, strict: Option<bool>) -> JsonSchemaFormat;
        fn name(&self) -> String;
        fn description(&self) -> Option<String>;
        fn schema(&self) -> String;
        fn strict(&self) -> Option<bool>;
    }

    extern "Rust" {
        type Usage;
        #[swift_bridge(init)]
        fn new(
            prompt_tokens: u64,
            completion_tokens: u64,
            total_tokens: u64,
            prompt_tokens_details: Option<PromptTokensDetails>,
        ) -> Usage;
        #[swift_bridge(swift_name = "promptTokens")]
        fn prompt_tokens(&self) -> u64;
        #[swift_bridge(swift_name = "completionTokens")]
        fn completion_tokens(&self) -> u64;
        #[swift_bridge(swift_name = "totalTokens")]
        fn total_tokens(&self) -> u64;
        #[swift_bridge(swift_name = "promptTokensDetails")]
        fn prompt_tokens_details(&self) -> Option<PromptTokensDetails>;
    }

    extern "Rust" {
        type PromptTokensDetails;
        #[swift_bridge(init)]
        fn new(cached_tokens: u64, audio_tokens: u64) -> PromptTokensDetails;
        #[swift_bridge(swift_name = "cachedTokens")]
        fn cached_tokens(&self) -> u64;
        #[swift_bridge(swift_name = "audioTokens")]
        fn audio_tokens(&self) -> u64;
    }

    extern "Rust" {
        type ChatCompletionRequest;
        #[swift_bridge(init)]
        fn new(
            model: String,
            messages: Vec<Message>,
            temperature: Option<f64>,
            top_p: Option<f64>,
            n: Option<u32>,
            stream: Option<bool>,
            stop: Option<StopSequence>,
            max_tokens: Option<u64>,
            presence_penalty: Option<f64>,
            frequency_penalty: Option<f64>,
            logit_bias: String,
            user: Option<String>,
            tools: Option<Vec<ChatCompletionTool>>,
            tool_choice: Option<ToolChoice>,
            parallel_tool_calls: Option<bool>,
            response_format: Option<ResponseFormat>,
            stream_options: Option<StreamOptions>,
            seed: Option<i64>,
            reasoning_effort: Option<ReasoningEffort>,
            modalities: Option<Vec<Modality>>,
            extra_body: Option<String>,
        ) -> ChatCompletionRequest;
        fn model(&self) -> String;
        fn messages(&self) -> Vec<String>;
        fn temperature(&self) -> Option<f64>;
        #[swift_bridge(swift_name = "topP")]
        fn top_p(&self) -> Option<f64>;
        fn n(&self) -> Option<u32>;
        fn stream(&self) -> Option<bool>;
        fn stop(&self) -> Option<String>;
        #[swift_bridge(swift_name = "maxTokens")]
        fn max_tokens(&self) -> Option<u64>;
        #[swift_bridge(swift_name = "presencePenalty")]
        fn presence_penalty(&self) -> Option<f64>;
        #[swift_bridge(swift_name = "frequencyPenalty")]
        fn frequency_penalty(&self) -> Option<f64>;
        #[swift_bridge(swift_name = "logitBias")]
        fn logit_bias(&self) -> String;
        fn user(&self) -> Option<String>;
        fn tools(&self) -> String;
        #[swift_bridge(swift_name = "toolChoice")]
        fn tool_choice(&self) -> Option<String>;
        #[swift_bridge(swift_name = "parallelToolCalls")]
        fn parallel_tool_calls(&self) -> Option<bool>;
        #[swift_bridge(swift_name = "responseFormat")]
        fn response_format(&self) -> Option<String>;
        #[swift_bridge(swift_name = "streamOptions")]
        fn stream_options(&self) -> Option<StreamOptions>;
        fn seed(&self) -> Option<i64>;
        #[swift_bridge(swift_name = "reasoningEffort")]
        fn reasoning_effort(&self) -> Option<String>;
        fn modalities(&self) -> String;
        #[swift_bridge(swift_name = "extraBody")]
        fn extra_body(&self) -> Option<String>;
    }

    extern "Rust" {
        type StreamOptions;
        #[swift_bridge(init)]
        fn new(include_usage: Option<bool>) -> StreamOptions;
        #[swift_bridge(swift_name = "includeUsage")]
        fn include_usage(&self) -> Option<bool>;
    }

    extern "Rust" {
        type ChatCompletionResponse;
        #[swift_bridge(init)]
        fn new(
            id: String,
            object: String,
            created: u64,
            model: String,
            choices: Vec<Choice>,
            usage: Option<Usage>,
            system_fingerprint: Option<String>,
            service_tier: Option<String>,
        ) -> ChatCompletionResponse;
        fn id(&self) -> String;
        fn object(&self) -> String;
        fn created(&self) -> u64;
        fn model(&self) -> String;
        fn choices(&self) -> Vec<String>;
        fn usage(&self) -> Option<Usage>;
        #[swift_bridge(swift_name = "systemFingerprint")]
        fn system_fingerprint(&self) -> Option<String>;
        #[swift_bridge(swift_name = "serviceTier")]
        fn service_tier(&self) -> Option<String>;
    }

    extern "Rust" {
        type Choice;
        #[swift_bridge(init)]
        fn new(index: u32, message: AssistantMessage, finish_reason: Option<FinishReason>) -> Choice;
        fn index(&self) -> u32;
        fn message(&self) -> AssistantMessage;
        #[swift_bridge(swift_name = "finishReason")]
        fn finish_reason(&self) -> Option<String>;
    }

    extern "Rust" {
        type ChatCompletionChunk;
        #[swift_bridge(init)]
        fn new(
            id: String,
            object: String,
            created: u64,
            model: String,
            choices: Vec<StreamChoice>,
            usage: Option<Usage>,
            system_fingerprint: Option<String>,
            service_tier: Option<String>,
        ) -> ChatCompletionChunk;
        fn id(&self) -> String;
        fn object(&self) -> String;
        fn created(&self) -> u64;
        fn model(&self) -> String;
        fn choices(&self) -> Vec<String>;
        fn usage(&self) -> Option<Usage>;
        #[swift_bridge(swift_name = "systemFingerprint")]
        fn system_fingerprint(&self) -> Option<String>;
        #[swift_bridge(swift_name = "serviceTier")]
        fn service_tier(&self) -> Option<String>;
    }

    extern "Rust" {
        type StreamChoice;
        #[swift_bridge(init)]
        fn new(index: u32, delta: StreamDelta, finish_reason: Option<FinishReason>) -> StreamChoice;
        fn index(&self) -> u32;
        fn delta(&self) -> StreamDelta;
        #[swift_bridge(swift_name = "finishReason")]
        fn finish_reason(&self) -> Option<String>;
    }

    extern "Rust" {
        type StreamDelta;
        #[swift_bridge(init)]
        fn new(
            role: Option<String>,
            content: Option<String>,
            tool_calls: Option<Vec<StreamToolCall>>,
            function_call: Option<StreamFunctionCall>,
            refusal: Option<String>,
        ) -> StreamDelta;
        fn role(&self) -> Option<String>;
        fn content(&self) -> Option<String>;
        #[swift_bridge(swift_name = "toolCalls")]
        fn tool_calls(&self) -> String;
        #[swift_bridge(swift_name = "functionCall")]
        fn function_call(&self) -> Option<StreamFunctionCall>;
        fn refusal(&self) -> Option<String>;
    }

    extern "Rust" {
        type StreamToolCall;
        #[swift_bridge(init)]
        fn new(
            index: u32,
            id: Option<String>,
            call_type: Option<ToolType>,
            function: Option<StreamFunctionCall>,
        ) -> StreamToolCall;
        fn index(&self) -> u32;
        fn id(&self) -> Option<String>;
        #[swift_bridge(swift_name = "callType")]
        fn call_type(&self) -> Option<String>;
        fn function(&self) -> Option<StreamFunctionCall>;
    }

    extern "Rust" {
        type StreamFunctionCall;
        #[swift_bridge(init)]
        fn new(name: Option<String>, arguments: Option<String>) -> StreamFunctionCall;
        fn name(&self) -> Option<String>;
        fn arguments(&self) -> Option<String>;
    }

    extern "Rust" {
        type EmbeddingRequest;
        #[swift_bridge(init)]
        fn new(
            model: String,
            input: EmbeddingInput,
            encoding_format: Option<EmbeddingFormat>,
            dimensions: Option<u32>,
            user: Option<String>,
        ) -> EmbeddingRequest;
        fn model(&self) -> String;
        fn input(&self) -> String;
        #[swift_bridge(swift_name = "encodingFormat")]
        fn encoding_format(&self) -> Option<String>;
        fn dimensions(&self) -> Option<u32>;
        fn user(&self) -> Option<String>;
    }

    extern "Rust" {
        type EmbeddingResponse;
        fn object(&self) -> String;
        fn data(&self) -> Vec<String>;
        fn model(&self) -> String;
        fn usage(&self) -> Option<Usage>;
    }

    extern "Rust" {
        type EmbeddingObject;
        fn object(&self) -> String;
        fn embedding(&self) -> Vec<f64>;
        fn index(&self) -> u32;
    }

    extern "Rust" {
        type CreateImageRequest;
        #[swift_bridge(init)]
        fn new(
            prompt: String,
            model: Option<String>,
            n: Option<u32>,
            size: Option<String>,
            quality: Option<String>,
            style: Option<String>,
            response_format: Option<String>,
            user: Option<String>,
        ) -> CreateImageRequest;
        fn prompt(&self) -> String;
        fn model(&self) -> Option<String>;
        fn n(&self) -> Option<u32>;
        fn size(&self) -> Option<String>;
        fn quality(&self) -> Option<String>;
        fn style(&self) -> Option<String>;
        #[swift_bridge(swift_name = "responseFormat")]
        fn response_format(&self) -> Option<String>;
        fn user(&self) -> Option<String>;
    }

    extern "Rust" {
        type ImagesResponse;
        #[swift_bridge(init)]
        fn new(created: u64, data: Vec<Image>) -> ImagesResponse;
        fn created(&self) -> u64;
        fn data(&self) -> Vec<String>;
    }

    extern "Rust" {
        type Image;
        #[swift_bridge(init)]
        fn new(url: Option<String>, b64_json: Option<String>, revised_prompt: Option<String>) -> Image;
        fn url(&self) -> Option<String>;
        #[swift_bridge(swift_name = "b64Json")]
        fn b64_json(&self) -> Option<String>;
        #[swift_bridge(swift_name = "revisedPrompt")]
        fn revised_prompt(&self) -> Option<String>;
    }

    extern "Rust" {
        type DecodedDataUrl;
        #[swift_bridge(init)]
        fn new(mime: String, data: Vec<u8>) -> DecodedDataUrl;
        fn mime(&self) -> String;
        fn data(&self) -> Vec<u8>;
    }

    extern "Rust" {
        type CreateSpeechRequest;
        #[swift_bridge(init)]
        fn new(
            model: String,
            input: String,
            voice: String,
            response_format: Option<String>,
            speed: Option<f64>,
        ) -> CreateSpeechRequest;
        fn model(&self) -> String;
        fn input(&self) -> String;
        fn voice(&self) -> String;
        #[swift_bridge(swift_name = "responseFormat")]
        fn response_format(&self) -> Option<String>;
        fn speed(&self) -> Option<f64>;
    }

    extern "Rust" {
        type CreateTranscriptionRequest;
        #[swift_bridge(init)]
        fn new(
            model: String,
            file: String,
            language: Option<String>,
            prompt: Option<String>,
            response_format: Option<String>,
            temperature: Option<f64>,
        ) -> CreateTranscriptionRequest;
        fn model(&self) -> String;
        fn file(&self) -> String;
        fn language(&self) -> Option<String>;
        fn prompt(&self) -> Option<String>;
        #[swift_bridge(swift_name = "responseFormat")]
        fn response_format(&self) -> Option<String>;
        fn temperature(&self) -> Option<f64>;
    }

    extern "Rust" {
        type TranscriptionResponse;
        #[swift_bridge(init)]
        fn new(
            text: String,
            language: Option<String>,
            duration: Option<f64>,
            segments: Option<Vec<TranscriptionSegment>>,
        ) -> TranscriptionResponse;
        fn text(&self) -> String;
        fn language(&self) -> Option<String>;
        fn duration(&self) -> Option<f64>;
        fn segments(&self) -> String;
    }

    extern "Rust" {
        type TranscriptionSegment;
        #[swift_bridge(init)]
        fn new(id: u32, start: f64, end: f64, text: String) -> TranscriptionSegment;
        fn id(&self) -> u32;
        fn start(&self) -> f64;
        fn end(&self) -> f64;
        fn text(&self) -> String;
    }

    extern "Rust" {
        type ModerationRequest;
        #[swift_bridge(init)]
        fn new(input: ModerationInput, model: Option<String>) -> ModerationRequest;
        fn input(&self) -> String;
        fn model(&self) -> Option<String>;
    }

    extern "Rust" {
        type ModerationResponse;
        fn id(&self) -> String;
        fn model(&self) -> String;
        fn results(&self) -> Vec<String>;
    }

    extern "Rust" {
        type ModerationResult;
        fn flagged(&self) -> bool;
        fn categories(&self) -> ModerationCategories;
        #[swift_bridge(swift_name = "categoryScores")]
        fn category_scores(&self) -> ModerationCategoryScores;
    }

    extern "Rust" {
        type ModerationCategories;
        #[swift_bridge(init)]
        fn new(
            sexual: bool,
            hate: bool,
            harassment: bool,
            self_harm: bool,
            sexual_minors: bool,
            hate_threatening: bool,
            violence_graphic: bool,
            self_harm_intent: bool,
            self_harm_instructions: bool,
            harassment_threatening: bool,
            violence: bool,
        ) -> ModerationCategories;
        fn sexual(&self) -> bool;
        fn hate(&self) -> bool;
        fn harassment(&self) -> bool;
        #[swift_bridge(swift_name = "selfHarm")]
        fn self_harm(&self) -> bool;
        #[swift_bridge(swift_name = "sexualMinors")]
        fn sexual_minors(&self) -> bool;
        #[swift_bridge(swift_name = "hateThreatening")]
        fn hate_threatening(&self) -> bool;
        #[swift_bridge(swift_name = "violenceGraphic")]
        fn violence_graphic(&self) -> bool;
        #[swift_bridge(swift_name = "selfHarmIntent")]
        fn self_harm_intent(&self) -> bool;
        #[swift_bridge(swift_name = "selfHarmInstructions")]
        fn self_harm_instructions(&self) -> bool;
        #[swift_bridge(swift_name = "harassmentThreatening")]
        fn harassment_threatening(&self) -> bool;
        fn violence(&self) -> bool;
    }

    extern "Rust" {
        type ModerationCategoryScores;
        #[swift_bridge(init)]
        fn new(
            sexual: f64,
            hate: f64,
            harassment: f64,
            self_harm: f64,
            sexual_minors: f64,
            hate_threatening: f64,
            violence_graphic: f64,
            self_harm_intent: f64,
            self_harm_instructions: f64,
            harassment_threatening: f64,
            violence: f64,
        ) -> ModerationCategoryScores;
        fn sexual(&self) -> f64;
        fn hate(&self) -> f64;
        fn harassment(&self) -> f64;
        #[swift_bridge(swift_name = "selfHarm")]
        fn self_harm(&self) -> f64;
        #[swift_bridge(swift_name = "sexualMinors")]
        fn sexual_minors(&self) -> f64;
        #[swift_bridge(swift_name = "hateThreatening")]
        fn hate_threatening(&self) -> f64;
        #[swift_bridge(swift_name = "violenceGraphic")]
        fn violence_graphic(&self) -> f64;
        #[swift_bridge(swift_name = "selfHarmIntent")]
        fn self_harm_intent(&self) -> f64;
        #[swift_bridge(swift_name = "selfHarmInstructions")]
        fn self_harm_instructions(&self) -> f64;
        #[swift_bridge(swift_name = "harassmentThreatening")]
        fn harassment_threatening(&self) -> f64;
        fn violence(&self) -> f64;
    }

    extern "Rust" {
        type RerankRequest;
        #[swift_bridge(init)]
        fn new(
            model: String,
            query: String,
            documents: Vec<RerankDocument>,
            top_n: Option<u32>,
            return_documents: Option<bool>,
        ) -> RerankRequest;
        fn model(&self) -> String;
        fn query(&self) -> String;
        fn documents(&self) -> Vec<String>;
        #[swift_bridge(swift_name = "topN")]
        fn top_n(&self) -> Option<u32>;
        #[swift_bridge(swift_name = "returnDocuments")]
        fn return_documents(&self) -> Option<bool>;
    }

    extern "Rust" {
        type RerankResponse;
        fn id(&self) -> Option<String>;
        fn results(&self) -> Vec<RerankResult>;
        fn meta(&self) -> Option<String>;
    }

    extern "Rust" {
        type RerankResult;
        fn index(&self) -> u32;
        #[swift_bridge(swift_name = "relevanceScore")]
        fn relevance_score(&self) -> f64;
        fn document(&self) -> Option<RerankResultDocument>;
    }

    extern "Rust" {
        type RerankResultDocument;
        fn text(&self) -> String;
    }

    extern "Rust" {
        type SearchRequest;
        #[swift_bridge(init)]
        fn new(
            model: String,
            query: String,
            max_results: Option<u32>,
            search_domain_filter: Option<Vec<String>>,
            country: Option<String>,
        ) -> SearchRequest;
        fn model(&self) -> String;
        fn query(&self) -> String;
        #[swift_bridge(swift_name = "maxResults")]
        fn max_results(&self) -> Option<u32>;
        #[swift_bridge(swift_name = "searchDomainFilter")]
        fn search_domain_filter(&self) -> Option<Vec<String>>;
        fn country(&self) -> Option<String>;
    }

    extern "Rust" {
        type SearchResponse;
        fn results(&self) -> Vec<String>;
        fn model(&self) -> String;
    }

    extern "Rust" {
        type SearchResult;
        fn title(&self) -> String;
        fn url(&self) -> String;
        fn snippet(&self) -> String;
        fn date(&self) -> Option<String>;
    }

    extern "Rust" {
        type OcrRequest;
        #[swift_bridge(init)]
        fn new(
            model: String,
            document: OcrDocument,
            pages: Option<Vec<u32>>,
            include_image_base64: Option<bool>,
        ) -> OcrRequest;
        fn model(&self) -> String;
        fn document(&self) -> String;
        fn pages(&self) -> Option<Vec<u32>>;
        #[swift_bridge(swift_name = "includeImageBase64")]
        fn include_image_base64(&self) -> Option<bool>;
    }

    extern "Rust" {
        type OcrResponse;
        fn pages(&self) -> Vec<String>;
        fn model(&self) -> String;
        fn usage(&self) -> Option<Usage>;
    }

    extern "Rust" {
        type OcrPage;
        fn index(&self) -> u32;
        fn markdown(&self) -> String;
        fn images(&self) -> String;
        fn dimensions(&self) -> Option<PageDimensions>;
    }

    extern "Rust" {
        type OcrImage;
        fn id(&self) -> String;
        #[swift_bridge(swift_name = "imageBase64")]
        fn image_base64(&self) -> Option<String>;
    }

    extern "Rust" {
        type PageDimensions;
        #[swift_bridge(init)]
        fn new(width: u32, height: u32) -> PageDimensions;
        fn width(&self) -> u32;
        fn height(&self) -> u32;
    }

    extern "Rust" {
        type ModelsListResponse;
        #[swift_bridge(init)]
        fn new(object: String, data: Vec<ModelObject>) -> ModelsListResponse;
        fn object(&self) -> String;
        fn data(&self) -> Vec<String>;
    }

    extern "Rust" {
        type ModelObject;
        #[swift_bridge(init)]
        fn new(id: String, object: String, created: u64, owned_by: String) -> ModelObject;
        fn id(&self) -> String;
        fn object(&self) -> String;
        fn created(&self) -> u64;
        #[swift_bridge(swift_name = "ownedBy")]
        fn owned_by(&self) -> String;
    }

    extern "Rust" {
        type CreateFileRequest;
        #[swift_bridge(init)]
        fn new(file: String, purpose: FilePurpose, filename: Option<String>) -> CreateFileRequest;
        fn file(&self) -> String;
        fn purpose(&self) -> String;
        fn filename(&self) -> Option<String>;
    }

    extern "Rust" {
        type FileObject;
        #[swift_bridge(init)]
        fn new(
            id: String,
            object: String,
            bytes: u64,
            created_at: u64,
            filename: String,
            purpose: String,
            status: Option<String>,
        ) -> FileObject;
        fn id(&self) -> String;
        fn object(&self) -> String;
        fn bytes(&self) -> u64;
        #[swift_bridge(swift_name = "createdAt")]
        fn created_at(&self) -> u64;
        fn filename(&self) -> String;
        fn purpose(&self) -> String;
        fn status(&self) -> Option<String>;
    }

    extern "Rust" {
        type FileListResponse;
        #[swift_bridge(init)]
        fn new(object: String, data: Vec<FileObject>, has_more: Option<bool>) -> FileListResponse;
        fn object(&self) -> String;
        fn data(&self) -> Vec<String>;
        #[swift_bridge(swift_name = "hasMore")]
        fn has_more(&self) -> Option<bool>;
    }

    extern "Rust" {
        type FileListQuery;
        #[swift_bridge(init)]
        fn new(purpose: Option<String>, limit: Option<u32>, after: Option<String>) -> FileListQuery;
        fn purpose(&self) -> Option<String>;
        fn limit(&self) -> Option<u32>;
        fn after(&self) -> Option<String>;
    }

    extern "Rust" {
        type DeleteResponse;
        #[swift_bridge(init)]
        fn new(id: String, object: String, deleted: bool) -> DeleteResponse;
        fn id(&self) -> String;
        fn object(&self) -> String;
        fn deleted(&self) -> bool;
    }

    extern "Rust" {
        type CreateBatchRequest;
        #[swift_bridge(init)]
        fn new(
            input_file_id: String,
            endpoint: String,
            completion_window: String,
            metadata: Option<String>,
        ) -> CreateBatchRequest;
        #[swift_bridge(swift_name = "inputFileId")]
        fn input_file_id(&self) -> String;
        fn endpoint(&self) -> String;
        #[swift_bridge(swift_name = "completionWindow")]
        fn completion_window(&self) -> String;
        fn metadata(&self) -> Option<String>;
    }

    extern "Rust" {
        type BatchObject;
        #[swift_bridge(init)]
        fn new(
            id: String,
            object: String,
            endpoint: String,
            input_file_id: String,
            completion_window: String,
            status: BatchStatus,
            output_file_id: Option<String>,
            error_file_id: Option<String>,
            created_at: u64,
            completed_at: Option<u64>,
            failed_at: Option<u64>,
            expired_at: Option<u64>,
            request_counts: Option<BatchRequestCounts>,
            metadata: Option<String>,
        ) -> BatchObject;
        fn id(&self) -> String;
        fn object(&self) -> String;
        fn endpoint(&self) -> String;
        #[swift_bridge(swift_name = "inputFileId")]
        fn input_file_id(&self) -> String;
        #[swift_bridge(swift_name = "completionWindow")]
        fn completion_window(&self) -> String;
        fn status(&self) -> String;
        #[swift_bridge(swift_name = "outputFileId")]
        fn output_file_id(&self) -> Option<String>;
        #[swift_bridge(swift_name = "errorFileId")]
        fn error_file_id(&self) -> Option<String>;
        #[swift_bridge(swift_name = "createdAt")]
        fn created_at(&self) -> u64;
        #[swift_bridge(swift_name = "completedAt")]
        fn completed_at(&self) -> Option<u64>;
        #[swift_bridge(swift_name = "failedAt")]
        fn failed_at(&self) -> Option<u64>;
        #[swift_bridge(swift_name = "expiredAt")]
        fn expired_at(&self) -> Option<u64>;
        #[swift_bridge(swift_name = "requestCounts")]
        fn request_counts(&self) -> Option<BatchRequestCounts>;
        fn metadata(&self) -> Option<String>;
    }

    extern "Rust" {
        type BatchRequestCounts;
        #[swift_bridge(init)]
        fn new(total: u64, completed: u64, failed: u64) -> BatchRequestCounts;
        fn total(&self) -> u64;
        fn completed(&self) -> u64;
        fn failed(&self) -> u64;
    }

    extern "Rust" {
        type BatchListResponse;
        #[swift_bridge(init)]
        fn new(
            object: String,
            data: Vec<BatchObject>,
            has_more: Option<bool>,
            first_id: Option<String>,
            last_id: Option<String>,
        ) -> BatchListResponse;
        fn object(&self) -> String;
        fn data(&self) -> Vec<BatchObject>;
        #[swift_bridge(swift_name = "hasMore")]
        fn has_more(&self) -> Option<bool>;
        #[swift_bridge(swift_name = "firstId")]
        fn first_id(&self) -> Option<String>;
        #[swift_bridge(swift_name = "lastId")]
        fn last_id(&self) -> Option<String>;
    }

    extern "Rust" {
        type BatchListQuery;
        #[swift_bridge(init)]
        fn new(limit: Option<u32>, after: Option<String>) -> BatchListQuery;
        fn limit(&self) -> Option<u32>;
        fn after(&self) -> Option<String>;
    }

    extern "Rust" {
        type CreateResponseRequest;
        #[swift_bridge(init)]
        fn new(
            model: String,
            input: String,
            instructions: Option<String>,
            tools: Option<Vec<ResponseTool>>,
            temperature: Option<f64>,
            max_output_tokens: Option<u64>,
            metadata: Option<String>,
        ) -> CreateResponseRequest;
        fn model(&self) -> String;
        fn input(&self) -> String;
        fn instructions(&self) -> Option<String>;
        fn tools(&self) -> String;
        fn temperature(&self) -> Option<f64>;
        #[swift_bridge(swift_name = "maxOutputTokens")]
        fn max_output_tokens(&self) -> Option<u64>;
        fn metadata(&self) -> Option<String>;
    }

    extern "Rust" {
        type ResponseTool;
        #[swift_bridge(init)]
        fn new(tool_type: String, config: String) -> ResponseTool;
        #[swift_bridge(swift_name = "toolType")]
        fn tool_type(&self) -> String;
        fn config(&self) -> String;
    }

    extern "Rust" {
        type ResponseObject;
        #[swift_bridge(init)]
        fn new(
            id: String,
            object: String,
            created_at: u64,
            model: String,
            status: String,
            output: Vec<ResponseOutputItem>,
            usage: Option<ResponseUsage>,
            error: Option<String>,
        ) -> ResponseObject;
        fn id(&self) -> String;
        fn object(&self) -> String;
        #[swift_bridge(swift_name = "createdAt")]
        fn created_at(&self) -> u64;
        fn model(&self) -> String;
        fn status(&self) -> String;
        fn output(&self) -> Vec<ResponseOutputItem>;
        fn usage(&self) -> Option<ResponseUsage>;
        fn error(&self) -> Option<String>;
    }

    extern "Rust" {
        type ResponseOutputItem;
        #[swift_bridge(init)]
        fn new(item_type: String, content: String) -> ResponseOutputItem;
        #[swift_bridge(swift_name = "itemType")]
        fn item_type(&self) -> String;
        fn content(&self) -> String;
    }

    extern "Rust" {
        type ResponseUsage;
        #[swift_bridge(init)]
        fn new(input_tokens: u64, output_tokens: u64, total_tokens: u64) -> ResponseUsage;
        #[swift_bridge(swift_name = "inputTokens")]
        fn input_tokens(&self) -> u64;
        #[swift_bridge(swift_name = "outputTokens")]
        fn output_tokens(&self) -> u64;
        #[swift_bridge(swift_name = "totalTokens")]
        fn total_tokens(&self) -> u64;
    }

    extern "Rust" {
        type WaitForBatchConfig;
        #[swift_bridge(init)]
        fn new(
            initial_interval_secs: f64,
            max_interval_secs: f64,
            backoff_multiplier: f32,
            timeout_secs: Option<f64>,
        ) -> WaitForBatchConfig;
        #[swift_bridge(swift_name = "initialIntervalSecs")]
        fn initial_interval_secs(&self) -> f64;
        #[swift_bridge(swift_name = "maxIntervalSecs")]
        fn max_interval_secs(&self) -> f64;
        #[swift_bridge(swift_name = "backoffMultiplier")]
        fn backoff_multiplier(&self) -> f32;
        #[swift_bridge(swift_name = "timeoutSecs")]
        fn timeout_secs(&self) -> Option<f64>;
    }

    #[cfg(any(feature = "native-http", feature = "wasm-http"))]
    extern "Rust" {
        type DefaultClient;
    }

    #[cfg(any(feature = "native-http", feature = "wasm-http"))]
    extern "Rust" {
        #[swift_bridge(swift_name = "createDefaultClient")]
        fn create_default_client(api_key: String, base_url: Option<String>) -> Result<DefaultClient, String>;
    }

    #[cfg(any(feature = "native-http", feature = "wasm-http"))]
    extern "Rust" {
        #[swift_bridge(swift_name = "defaultClientChat")]
        fn default_client_chat(
            client: &DefaultClient,
            req: ChatCompletionRequest,
        ) -> Result<ChatCompletionResponse, String>;
        #[swift_bridge(swift_name = "defaultClientEmbed")]
        fn default_client_embed(client: &DefaultClient, req: EmbeddingRequest) -> Result<EmbeddingResponse, String>;
        #[swift_bridge(swift_name = "defaultClientListModels")]
        fn default_client_list_models(client: &DefaultClient) -> Result<ModelsListResponse, String>;
        #[swift_bridge(swift_name = "defaultClientImageGenerate")]
        fn default_client_image_generate(
            client: &DefaultClient,
            req: CreateImageRequest,
        ) -> Result<ImagesResponse, String>;
        #[swift_bridge(swift_name = "defaultClientSpeech")]
        fn default_client_speech(client: &DefaultClient, req: CreateSpeechRequest) -> Result<Vec<u8>, String>;
        #[swift_bridge(swift_name = "defaultClientTranscribe")]
        fn default_client_transcribe(
            client: &DefaultClient,
            req: CreateTranscriptionRequest,
        ) -> Result<TranscriptionResponse, String>;
        #[swift_bridge(swift_name = "defaultClientModerate")]
        fn default_client_moderate(
            client: &DefaultClient,
            req: ModerationRequest,
        ) -> Result<ModerationResponse, String>;
        #[swift_bridge(swift_name = "defaultClientRerank")]
        fn default_client_rerank(client: &DefaultClient, req: RerankRequest) -> Result<RerankResponse, String>;
        #[swift_bridge(swift_name = "defaultClientSearch")]
        fn default_client_search(client: &DefaultClient, req: SearchRequest) -> Result<SearchResponse, String>;
        #[swift_bridge(swift_name = "defaultClientOcr")]
        fn default_client_ocr(client: &DefaultClient, req: OcrRequest) -> Result<OcrResponse, String>;
        #[swift_bridge(swift_name = "defaultClientCreateFile")]
        fn default_client_create_file(client: &DefaultClient, req: CreateFileRequest) -> Result<FileObject, String>;
        #[swift_bridge(swift_name = "defaultClientRetrieveFile")]
        fn default_client_retrieve_file(client: &DefaultClient, file_id: String) -> Result<FileObject, String>;
        #[swift_bridge(swift_name = "defaultClientDeleteFile")]
        fn default_client_delete_file(client: &DefaultClient, file_id: String) -> Result<DeleteResponse, String>;
        #[swift_bridge(swift_name = "defaultClientListFiles")]
        fn default_client_list_files(
            client: &DefaultClient,
            query: Option<FileListQuery>,
        ) -> Result<FileListResponse, String>;
        #[swift_bridge(swift_name = "defaultClientFileContent")]
        fn default_client_file_content(client: &DefaultClient, file_id: String) -> Result<Vec<u8>, String>;
        #[swift_bridge(swift_name = "defaultClientCreateBatch")]
        fn default_client_create_batch(client: &DefaultClient, req: CreateBatchRequest) -> Result<BatchObject, String>;
        #[swift_bridge(swift_name = "defaultClientRetrieveBatch")]
        fn default_client_retrieve_batch(client: &DefaultClient, batch_id: String) -> Result<BatchObject, String>;
        #[swift_bridge(swift_name = "defaultClientListBatches")]
        fn default_client_list_batches(
            client: &DefaultClient,
            query: Option<BatchListQuery>,
        ) -> Result<BatchListResponse, String>;
        #[swift_bridge(swift_name = "defaultClientCancelBatch")]
        fn default_client_cancel_batch(client: &DefaultClient, batch_id: String) -> Result<BatchObject, String>;
        #[swift_bridge(swift_name = "defaultClientFetchBatchForPolling")]
        fn default_client_fetch_batch_for_polling(
            client: &DefaultClient,
            batch_id: String,
        ) -> Result<BatchObject, String>;
        #[swift_bridge(swift_name = "defaultClientWaitForBatch")]
        fn default_client_wait_for_batch(
            client: &DefaultClient,
            batch_id: String,
            config: WaitForBatchConfig,
        ) -> Result<BatchObject, String>;
        #[swift_bridge(swift_name = "defaultClientCreateResponse")]
        fn default_client_create_response(
            client: &DefaultClient,
            req: CreateResponseRequest,
        ) -> Result<ResponseObject, String>;
        #[swift_bridge(swift_name = "defaultClientRetrieveResponse")]
        fn default_client_retrieve_response(
            client: &DefaultClient,
            response_id: String,
        ) -> Result<ResponseObject, String>;
        #[swift_bridge(swift_name = "defaultClientCancelResponse")]
        fn default_client_cancel_response(
            client: &DefaultClient,
            response_id: String,
        ) -> Result<ResponseObject, String>;
    }

    extern "Rust" {
        type CustomProviderConfig;
        fn name(&self) -> String;
        #[swift_bridge(swift_name = "baseUrl")]
        fn base_url(&self) -> String;
        #[swift_bridge(swift_name = "authHeader")]
        fn auth_header(&self) -> String;
        #[swift_bridge(swift_name = "modelPrefixes")]
        fn model_prefixes(&self) -> Vec<String>;
    }

    extern "Rust" {
        type ProviderCapabilities;
        #[swift_bridge(init)]
        fn new(
            vision: bool,
            reasoning: bool,
            structured_output: bool,
            function_calling: bool,
            audio_in: bool,
            audio_out: bool,
            video_in: bool,
        ) -> ProviderCapabilities;
        fn vision(&self) -> bool;
        fn reasoning(&self) -> bool;
        #[swift_bridge(swift_name = "structuredOutput")]
        fn structured_output(&self) -> bool;
        #[swift_bridge(swift_name = "functionCalling")]
        fn function_calling(&self) -> bool;
        #[swift_bridge(swift_name = "audioIn")]
        fn audio_in(&self) -> bool;
        #[swift_bridge(swift_name = "audioOut")]
        fn audio_out(&self) -> bool;
        #[swift_bridge(swift_name = "videoIn")]
        fn video_in(&self) -> bool;
    }

    extern "Rust" {
        type ProviderConfig;
        fn name(&self) -> String;
        #[swift_bridge(swift_name = "displayName")]
        fn display_name(&self) -> Option<String>;
        #[swift_bridge(swift_name = "baseUrl")]
        fn base_url(&self) -> Option<String>;
        fn auth(&self) -> Option<AuthConfig>;
        fn endpoints(&self) -> Option<Vec<String>>;
        #[swift_bridge(swift_name = "modelPrefixes")]
        fn model_prefixes(&self) -> Option<Vec<String>>;
        #[swift_bridge(swift_name = "paramMappings")]
        fn param_mappings(&self) -> String;
    }

    extern "Rust" {
        type AuthConfig;
        #[swift_bridge(swift_name = "authType")]
        fn auth_type(&self) -> String;
        #[swift_bridge(swift_name = "envVar")]
        fn env_var(&self) -> Option<String>;
    }

    extern "Rust" {
        type BudgetConfig;
        #[swift_bridge(init)]
        fn new(global_limit: Option<f64>, model_limits: String, enforcement: Enforcement) -> BudgetConfig;
        #[swift_bridge(swift_name = "globalLimit")]
        fn global_limit(&self) -> Option<f64>;
        #[swift_bridge(swift_name = "modelLimits")]
        fn model_limits(&self) -> String;
        fn enforcement(&self) -> String;
    }

    extern "Rust" {
        type CacheConfig;
        #[swift_bridge(init)]
        fn new(max_entries: usize, ttl: u64, backend: CacheBackend) -> CacheConfig;
        #[swift_bridge(swift_name = "maxEntries")]
        fn max_entries(&self) -> usize;
        fn ttl(&self) -> u64;
        fn backend(&self) -> String;
    }

    extern "Rust" {
        type SingleflightResult;
        fn singleflight_result_noop(client: &SingleflightResult);
    }

    extern "Rust" {
        type RateLimitConfig;
        #[swift_bridge(init)]
        fn new(rpm: Option<u32>, tpm: Option<u64>, window: u64) -> RateLimitConfig;
        fn rpm(&self) -> Option<u32>;
        fn tpm(&self) -> Option<u64>;
        fn window(&self) -> u64;
    }

    extern "Rust" {
        type IntentPrototype;
        fn name(&self) -> String;
        fn embedding(&self) -> Vec<f64>;
        fn model(&self) -> String;
    }

    extern "Rust" {
        type Message;
        fn to_string(&self) -> String;
    }

    extern "Rust" {
        type UserContent;
        fn to_string(&self) -> String;
    }

    extern "Rust" {
        type ContentPart;
        fn to_string(&self) -> String;
    }

    extern "Rust" {
        type ImageDetail;
        fn to_string(&self) -> String;
    }

    extern "Rust" {
        type AssistantContent;
        fn to_string(&self) -> String;
    }

    extern "Rust" {
        type AssistantPart;
        fn to_string(&self) -> String;
    }

    extern "Rust" {
        type ToolType;
        fn to_string(&self) -> String;
    }

    extern "Rust" {
        type ToolChoice;
        fn to_string(&self) -> String;
    }

    extern "Rust" {
        type ToolChoiceMode;
        fn to_string(&self) -> String;
    }

    extern "Rust" {
        type ResponseFormat;
        fn to_string(&self) -> String;
    }

    extern "Rust" {
        type StopSequence;
        fn to_string(&self) -> String;
    }

    extern "Rust" {
        type Modality;
        fn to_string(&self) -> String;
    }

    extern "Rust" {
        type FinishReason;
        fn to_string(&self) -> String;
    }

    extern "Rust" {
        type ReasoningEffort;
        fn to_string(&self) -> String;
    }

    extern "Rust" {
        type EmbeddingFormat;
        fn to_string(&self) -> String;
    }

    extern "Rust" {
        type EmbeddingInput;
        fn to_string(&self) -> String;
    }

    extern "Rust" {
        type ModerationInput;
        fn to_string(&self) -> String;
    }

    extern "Rust" {
        type RerankDocument;
        fn to_string(&self) -> String;
    }

    extern "Rust" {
        type OcrDocument;
        fn to_string(&self) -> String;
    }

    extern "Rust" {
        type FilePurpose;
        fn to_string(&self) -> String;
    }

    extern "Rust" {
        type BatchStatus;
        fn to_string(&self) -> String;
    }

    extern "Rust" {
        type AuthHeaderFormat;
        fn to_string(&self) -> String;
    }

    extern "Rust" {
        type StreamFormat;
        fn to_string(&self) -> String;
    }

    extern "Rust" {
        type AuthType;
        fn to_string(&self) -> String;
    }

    extern "Rust" {
        type Enforcement;
        fn to_string(&self) -> String;
    }

    extern "Rust" {
        type CacheBackend;
        fn to_string(&self) -> String;
    }

    extern "Rust" {
        type CircuitState;
        fn to_string(&self) -> String;
    }

    extern "Rust" {
        type HealthStatus;
        fn to_string(&self) -> String;
    }

    extern "Rust" {
        #[swift_bridge(swift_name = "createClient")]
        fn create_client(
            api_key: String,
            base_url: Option<String>,
            timeout_secs: Option<u64>,
            max_retries: Option<u32>,
            model_hint: Option<String>,
        ) -> Result<DefaultClient, String>;
        #[swift_bridge(swift_name = "createClientFromJson")]
        fn create_client_from_json(json: String) -> Result<DefaultClient, String>;
        #[swift_bridge(swift_name = "encodeDataUrl")]
        fn encode_data_url(bytes: Vec<u8>, mime: Option<String>) -> String;
        #[swift_bridge(swift_name = "decodeDataUrl")]
        fn decode_data_url(url: String) -> Option<DecodedDataUrl>;
        #[swift_bridge(swift_name = "registerCustomProvider")]
        fn register_custom_provider(config: CustomProviderConfig) -> Result<(), String>;
        #[swift_bridge(swift_name = "unregisterCustomProvider")]
        fn unregister_custom_provider(name: String) -> Result<bool, String>;
        fn capabilities(provider_name: String) -> ProviderCapabilities;
        #[swift_bridge(swift_name = "complexProviderNames")]
        fn complex_provider_names() -> Result<Vec<String>, String>;
        #[swift_bridge(swift_name = "completionCost")]
        fn completion_cost(model: String, prompt_tokens: u64, completion_tokens: u64) -> String;
        #[swift_bridge(swift_name = "completionCostWithCache")]
        fn completion_cost_with_cache(
            model: String,
            prompt_tokens: u64,
            cached_tokens: u64,
            completion_tokens: u64,
        ) -> String;
        fn clear() -> ();
        #[swift_bridge(swift_name = "countTokens")]
        fn count_tokens(model: String, text: String) -> Result<usize, String>;
        #[swift_bridge(swift_name = "countRequestTokens")]
        fn count_request_tokens(model: String, req: ChatCompletionRequest) -> Result<usize, String>;
        #[swift_bridge(swift_name = "checkBound")]
        fn check_bound(context: String, current_len: usize, incoming: usize, limit: usize) -> Result<(), String>;
    }

    #[cfg(any(
        all(feature = "native-http", not(target_os = "windows")),
        all(feature = "native-http", target_os = "windows")
    ))]
    extern "Rust" {
        #[swift_bridge(swift_name = "ensureCryptoProvider")]
        fn ensure_crypto_provider() -> ();
    }

    extern "Rust" {
        type DefaultClientChatStreamStreamHandle;

        #[swift_bridge(swift_name = "defaultClientChatStreamStart")]
        fn default_client_chat_stream_start(
            client: &DefaultClient,
            req: &ChatCompletionRequest,
        ) -> Result<DefaultClientChatStreamStreamHandle, String>;
        fn next(self: &mut DefaultClientChatStreamStreamHandle) -> Result<String, String>;
    }

    extern "Rust" {
        #[swift_bridge(swift_name = "chatCompletionRequestFromJson")]
        fn chat_completion_request_from_json(json: String) -> Result<ChatCompletionRequest, String>;
        #[swift_bridge(swift_name = "chatCompletionChunkFromJson")]
        fn chat_completion_chunk_from_json(json: String) -> Result<ChatCompletionChunk, String>;
        #[swift_bridge(swift_name = "embeddingRequestFromJson")]
        fn embedding_request_from_json(json: String) -> Result<EmbeddingRequest, String>;
        #[swift_bridge(swift_name = "createImageRequestFromJson")]
        fn create_image_request_from_json(json: String) -> Result<CreateImageRequest, String>;
        #[swift_bridge(swift_name = "createSpeechRequestFromJson")]
        fn create_speech_request_from_json(json: String) -> Result<CreateSpeechRequest, String>;
        #[swift_bridge(swift_name = "createTranscriptionRequestFromJson")]
        fn create_transcription_request_from_json(json: String) -> Result<CreateTranscriptionRequest, String>;
        #[swift_bridge(swift_name = "moderationRequestFromJson")]
        fn moderation_request_from_json(json: String) -> Result<ModerationRequest, String>;
        #[swift_bridge(swift_name = "rerankRequestFromJson")]
        fn rerank_request_from_json(json: String) -> Result<RerankRequest, String>;
        #[swift_bridge(swift_name = "searchRequestFromJson")]
        fn search_request_from_json(json: String) -> Result<SearchRequest, String>;
        #[swift_bridge(swift_name = "ocrRequestFromJson")]
        fn ocr_request_from_json(json: String) -> Result<OcrRequest, String>;
        #[swift_bridge(swift_name = "createFileRequestFromJson")]
        fn create_file_request_from_json(json: String) -> Result<CreateFileRequest, String>;
        #[swift_bridge(swift_name = "fileListQueryFromJson")]
        fn file_list_query_from_json(json: String) -> Result<FileListQuery, String>;
        #[swift_bridge(swift_name = "createBatchRequestFromJson")]
        fn create_batch_request_from_json(json: String) -> Result<CreateBatchRequest, String>;
        #[swift_bridge(swift_name = "batchListQueryFromJson")]
        fn batch_list_query_from_json(json: String) -> Result<BatchListQuery, String>;
        #[swift_bridge(swift_name = "createResponseRequestFromJson")]
        fn create_response_request_from_json(json: String) -> Result<CreateResponseRequest, String>;
        #[swift_bridge(swift_name = "waitForBatchConfigFromJson")]
        fn wait_for_batch_config_from_json(json: String) -> Result<WaitForBatchConfig, String>;
        #[swift_bridge(swift_name = "customProviderConfigFromJson")]
        fn custom_provider_config_from_json(json: String) -> Result<CustomProviderConfig, String>;
    }
    extern "Rust" {
        #[swift_bridge(swift_name = "systemMessageFromJson")]
        fn system_message_from_json(json: String) -> Result<SystemMessage, String>;
        #[swift_bridge(swift_name = "userMessageFromJson")]
        fn user_message_from_json(json: String) -> Result<UserMessage, String>;
        #[swift_bridge(swift_name = "imageUrlFromJson")]
        fn image_url_from_json(json: String) -> Result<ImageUrl, String>;
        #[swift_bridge(swift_name = "documentContentFromJson")]
        fn document_content_from_json(json: String) -> Result<DocumentContent, String>;
        #[swift_bridge(swift_name = "audioContentFromJson")]
        fn audio_content_from_json(json: String) -> Result<AudioContent, String>;
        #[swift_bridge(swift_name = "assistantMessageFromJson")]
        fn assistant_message_from_json(json: String) -> Result<AssistantMessage, String>;
        #[swift_bridge(swift_name = "toolMessageFromJson")]
        fn tool_message_from_json(json: String) -> Result<ToolMessage, String>;
        #[swift_bridge(swift_name = "developerMessageFromJson")]
        fn developer_message_from_json(json: String) -> Result<DeveloperMessage, String>;
        #[swift_bridge(swift_name = "functionMessageFromJson")]
        fn function_message_from_json(json: String) -> Result<FunctionMessage, String>;
        #[swift_bridge(swift_name = "chatCompletionToolFromJson")]
        fn chat_completion_tool_from_json(json: String) -> Result<ChatCompletionTool, String>;
        #[swift_bridge(swift_name = "functionDefinitionFromJson")]
        fn function_definition_from_json(json: String) -> Result<FunctionDefinition, String>;
        #[swift_bridge(swift_name = "toolCallFromJson")]
        fn tool_call_from_json(json: String) -> Result<ToolCall, String>;
        #[swift_bridge(swift_name = "functionCallFromJson")]
        fn function_call_from_json(json: String) -> Result<FunctionCall, String>;
        #[swift_bridge(swift_name = "specificToolChoiceFromJson")]
        fn specific_tool_choice_from_json(json: String) -> Result<SpecificToolChoice, String>;
        #[swift_bridge(swift_name = "specificFunctionFromJson")]
        fn specific_function_from_json(json: String) -> Result<SpecificFunction, String>;
        #[swift_bridge(swift_name = "jsonSchemaFormatFromJson")]
        fn json_schema_format_from_json(json: String) -> Result<JsonSchemaFormat, String>;
        #[swift_bridge(swift_name = "usageFromJson")]
        fn usage_from_json(json: String) -> Result<Usage, String>;
        #[swift_bridge(swift_name = "promptTokensDetailsFromJson")]
        fn prompt_tokens_details_from_json(json: String) -> Result<PromptTokensDetails, String>;
        #[swift_bridge(swift_name = "streamOptionsFromJson")]
        fn stream_options_from_json(json: String) -> Result<StreamOptions, String>;
        #[swift_bridge(swift_name = "chatCompletionResponseFromJson")]
        fn chat_completion_response_from_json(json: String) -> Result<ChatCompletionResponse, String>;
        #[swift_bridge(swift_name = "choiceFromJson")]
        fn choice_from_json(json: String) -> Result<Choice, String>;
        #[swift_bridge(swift_name = "streamChoiceFromJson")]
        fn stream_choice_from_json(json: String) -> Result<StreamChoice, String>;
        #[swift_bridge(swift_name = "streamDeltaFromJson")]
        fn stream_delta_from_json(json: String) -> Result<StreamDelta, String>;
        #[swift_bridge(swift_name = "streamToolCallFromJson")]
        fn stream_tool_call_from_json(json: String) -> Result<StreamToolCall, String>;
        #[swift_bridge(swift_name = "streamFunctionCallFromJson")]
        fn stream_function_call_from_json(json: String) -> Result<StreamFunctionCall, String>;
        #[swift_bridge(swift_name = "embeddingResponseFromJson")]
        fn embedding_response_from_json(json: String) -> Result<EmbeddingResponse, String>;
        #[swift_bridge(swift_name = "embeddingObjectFromJson")]
        fn embedding_object_from_json(json: String) -> Result<EmbeddingObject, String>;
        #[swift_bridge(swift_name = "imagesResponseFromJson")]
        fn images_response_from_json(json: String) -> Result<ImagesResponse, String>;
        #[swift_bridge(swift_name = "imageFromJson")]
        fn image_from_json(json: String) -> Result<Image, String>;
        #[swift_bridge(swift_name = "decodedDataUrlFromJson")]
        fn decoded_data_url_from_json(json: String) -> Result<DecodedDataUrl, String>;
        #[swift_bridge(swift_name = "transcriptionResponseFromJson")]
        fn transcription_response_from_json(json: String) -> Result<TranscriptionResponse, String>;
        #[swift_bridge(swift_name = "transcriptionSegmentFromJson")]
        fn transcription_segment_from_json(json: String) -> Result<TranscriptionSegment, String>;
        #[swift_bridge(swift_name = "moderationResponseFromJson")]
        fn moderation_response_from_json(json: String) -> Result<ModerationResponse, String>;
        #[swift_bridge(swift_name = "moderationResultFromJson")]
        fn moderation_result_from_json(json: String) -> Result<ModerationResult, String>;
        #[swift_bridge(swift_name = "moderationCategoriesFromJson")]
        fn moderation_categories_from_json(json: String) -> Result<ModerationCategories, String>;
        #[swift_bridge(swift_name = "moderationCategoryScoresFromJson")]
        fn moderation_category_scores_from_json(json: String) -> Result<ModerationCategoryScores, String>;
        #[swift_bridge(swift_name = "rerankResponseFromJson")]
        fn rerank_response_from_json(json: String) -> Result<RerankResponse, String>;
        #[swift_bridge(swift_name = "rerankResultFromJson")]
        fn rerank_result_from_json(json: String) -> Result<RerankResult, String>;
        #[swift_bridge(swift_name = "rerankResultDocumentFromJson")]
        fn rerank_result_document_from_json(json: String) -> Result<RerankResultDocument, String>;
        #[swift_bridge(swift_name = "searchResponseFromJson")]
        fn search_response_from_json(json: String) -> Result<SearchResponse, String>;
        #[swift_bridge(swift_name = "searchResultFromJson")]
        fn search_result_from_json(json: String) -> Result<SearchResult, String>;
        #[swift_bridge(swift_name = "ocrResponseFromJson")]
        fn ocr_response_from_json(json: String) -> Result<OcrResponse, String>;
        #[swift_bridge(swift_name = "ocrPageFromJson")]
        fn ocr_page_from_json(json: String) -> Result<OcrPage, String>;
        #[swift_bridge(swift_name = "ocrImageFromJson")]
        fn ocr_image_from_json(json: String) -> Result<OcrImage, String>;
        #[swift_bridge(swift_name = "pageDimensionsFromJson")]
        fn page_dimensions_from_json(json: String) -> Result<PageDimensions, String>;
        #[swift_bridge(swift_name = "modelsListResponseFromJson")]
        fn models_list_response_from_json(json: String) -> Result<ModelsListResponse, String>;
        #[swift_bridge(swift_name = "modelObjectFromJson")]
        fn model_object_from_json(json: String) -> Result<ModelObject, String>;
        #[swift_bridge(swift_name = "fileObjectFromJson")]
        fn file_object_from_json(json: String) -> Result<FileObject, String>;
        #[swift_bridge(swift_name = "fileListResponseFromJson")]
        fn file_list_response_from_json(json: String) -> Result<FileListResponse, String>;
        #[swift_bridge(swift_name = "deleteResponseFromJson")]
        fn delete_response_from_json(json: String) -> Result<DeleteResponse, String>;
        #[swift_bridge(swift_name = "batchObjectFromJson")]
        fn batch_object_from_json(json: String) -> Result<BatchObject, String>;
        #[swift_bridge(swift_name = "batchRequestCountsFromJson")]
        fn batch_request_counts_from_json(json: String) -> Result<BatchRequestCounts, String>;
        #[swift_bridge(swift_name = "batchListResponseFromJson")]
        fn batch_list_response_from_json(json: String) -> Result<BatchListResponse, String>;
        #[swift_bridge(swift_name = "responseToolFromJson")]
        fn response_tool_from_json(json: String) -> Result<ResponseTool, String>;
        #[swift_bridge(swift_name = "responseObjectFromJson")]
        fn response_object_from_json(json: String) -> Result<ResponseObject, String>;
        #[swift_bridge(swift_name = "responseOutputItemFromJson")]
        fn response_output_item_from_json(json: String) -> Result<ResponseOutputItem, String>;
        #[swift_bridge(swift_name = "responseUsageFromJson")]
        fn response_usage_from_json(json: String) -> Result<ResponseUsage, String>;
        #[swift_bridge(swift_name = "providerCapabilitiesFromJson")]
        fn provider_capabilities_from_json(json: String) -> Result<ProviderCapabilities, String>;
        #[swift_bridge(swift_name = "providerConfigFromJson")]
        fn provider_config_from_json(json: String) -> Result<ProviderConfig, String>;
        #[swift_bridge(swift_name = "authConfigFromJson")]
        fn auth_config_from_json(json: String) -> Result<AuthConfig, String>;
        #[swift_bridge(swift_name = "budgetConfigFromJson")]
        fn budget_config_from_json(json: String) -> Result<BudgetConfig, String>;
        #[swift_bridge(swift_name = "cacheConfigFromJson")]
        fn cache_config_from_json(json: String) -> Result<CacheConfig, String>;
        #[swift_bridge(swift_name = "rateLimitConfigFromJson")]
        fn rate_limit_config_from_json(json: String) -> Result<RateLimitConfig, String>;
    }
    extern "Rust" {
        #[swift_bridge(swift_name = "messageFromJson")]
        fn message_from_json(json: String) -> Result<Message, String>;
        #[swift_bridge(swift_name = "userContentFromJson")]
        fn user_content_from_json(json: String) -> Result<UserContent, String>;
        #[swift_bridge(swift_name = "contentPartFromJson")]
        fn content_part_from_json(json: String) -> Result<ContentPart, String>;
        #[swift_bridge(swift_name = "imageDetailFromJson")]
        fn image_detail_from_json(json: String) -> Result<ImageDetail, String>;
        #[swift_bridge(swift_name = "assistantContentFromJson")]
        fn assistant_content_from_json(json: String) -> Result<AssistantContent, String>;
        #[swift_bridge(swift_name = "assistantPartFromJson")]
        fn assistant_part_from_json(json: String) -> Result<AssistantPart, String>;
        #[swift_bridge(swift_name = "toolTypeFromJson")]
        fn tool_type_from_json(json: String) -> Result<ToolType, String>;
        #[swift_bridge(swift_name = "toolChoiceFromJson")]
        fn tool_choice_from_json(json: String) -> Result<ToolChoice, String>;
        #[swift_bridge(swift_name = "toolChoiceModeFromJson")]
        fn tool_choice_mode_from_json(json: String) -> Result<ToolChoiceMode, String>;
        #[swift_bridge(swift_name = "responseFormatFromJson")]
        fn response_format_from_json(json: String) -> Result<ResponseFormat, String>;
        #[swift_bridge(swift_name = "stopSequenceFromJson")]
        fn stop_sequence_from_json(json: String) -> Result<StopSequence, String>;
        #[swift_bridge(swift_name = "modalityFromJson")]
        fn modality_from_json(json: String) -> Result<Modality, String>;
        #[swift_bridge(swift_name = "finishReasonFromJson")]
        fn finish_reason_from_json(json: String) -> Result<FinishReason, String>;
        #[swift_bridge(swift_name = "reasoningEffortFromJson")]
        fn reasoning_effort_from_json(json: String) -> Result<ReasoningEffort, String>;
        #[swift_bridge(swift_name = "embeddingFormatFromJson")]
        fn embedding_format_from_json(json: String) -> Result<EmbeddingFormat, String>;
        #[swift_bridge(swift_name = "embeddingInputFromJson")]
        fn embedding_input_from_json(json: String) -> Result<EmbeddingInput, String>;
        #[swift_bridge(swift_name = "moderationInputFromJson")]
        fn moderation_input_from_json(json: String) -> Result<ModerationInput, String>;
        #[swift_bridge(swift_name = "rerankDocumentFromJson")]
        fn rerank_document_from_json(json: String) -> Result<RerankDocument, String>;
        #[swift_bridge(swift_name = "ocrDocumentFromJson")]
        fn ocr_document_from_json(json: String) -> Result<OcrDocument, String>;
        #[swift_bridge(swift_name = "filePurposeFromJson")]
        fn file_purpose_from_json(json: String) -> Result<FilePurpose, String>;
        #[swift_bridge(swift_name = "batchStatusFromJson")]
        fn batch_status_from_json(json: String) -> Result<BatchStatus, String>;
        #[swift_bridge(swift_name = "authHeaderFormatFromJson")]
        fn auth_header_format_from_json(json: String) -> Result<AuthHeaderFormat, String>;
        #[swift_bridge(swift_name = "streamFormatFromJson")]
        fn stream_format_from_json(json: String) -> Result<StreamFormat, String>;
        #[swift_bridge(swift_name = "authTypeFromJson")]
        fn auth_type_from_json(json: String) -> Result<AuthType, String>;
        #[swift_bridge(swift_name = "enforcementFromJson")]
        fn enforcement_from_json(json: String) -> Result<Enforcement, String>;
        #[swift_bridge(swift_name = "cacheBackendFromJson")]
        fn cache_backend_from_json(json: String) -> Result<CacheBackend, String>;
    }
    extern "Rust" {
        // Phantom Vec<T> functions: swift-bridge-build must emit the full Vec support
        // C ABI symbols (__swift_bridge__$Vec_T$new, drop, push, pop, get, get_mut, as_ptr, len)
        // which the auto-generated Swift Vec<T> conformances reference.
        //
        // swift-bridge 0.1.59 only emits these when Vec<T> appears as a return type
        // in an extern block. Without these phantom functions, Swift linker fails when
        // trying to construct or manipulate Vec<T> of opaque types.
        //
        // These declarations are paired with phantom_impl functions below the bridge module.
        fn __alef_phantom_vec_system_message() -> Vec<SystemMessage>;
        fn __alef_phantom_vec_user_message() -> Vec<UserMessage>;
        fn __alef_phantom_vec_image_url() -> Vec<ImageUrl>;
        fn __alef_phantom_vec_document_content() -> Vec<DocumentContent>;
        fn __alef_phantom_vec_audio_content() -> Vec<AudioContent>;
        fn __alef_phantom_vec_assistant_message() -> Vec<AssistantMessage>;
        fn __alef_phantom_vec_tool_message() -> Vec<ToolMessage>;
        fn __alef_phantom_vec_developer_message() -> Vec<DeveloperMessage>;
        fn __alef_phantom_vec_function_message() -> Vec<FunctionMessage>;
        fn __alef_phantom_vec_chat_completion_tool() -> Vec<ChatCompletionTool>;
        fn __alef_phantom_vec_function_definition() -> Vec<FunctionDefinition>;
        fn __alef_phantom_vec_tool_call() -> Vec<ToolCall>;
        fn __alef_phantom_vec_function_call() -> Vec<FunctionCall>;
        fn __alef_phantom_vec_specific_tool_choice() -> Vec<SpecificToolChoice>;
        fn __alef_phantom_vec_specific_function() -> Vec<SpecificFunction>;
        fn __alef_phantom_vec_json_schema_format() -> Vec<JsonSchemaFormat>;
        fn __alef_phantom_vec_usage() -> Vec<Usage>;
        fn __alef_phantom_vec_prompt_tokens_details() -> Vec<PromptTokensDetails>;
        fn __alef_phantom_vec_chat_completion_request() -> Vec<ChatCompletionRequest>;
        fn __alef_phantom_vec_stream_options() -> Vec<StreamOptions>;
        fn __alef_phantom_vec_chat_completion_response() -> Vec<ChatCompletionResponse>;
        fn __alef_phantom_vec_choice() -> Vec<Choice>;
        fn __alef_phantom_vec_chat_completion_chunk() -> Vec<ChatCompletionChunk>;
        fn __alef_phantom_vec_stream_choice() -> Vec<StreamChoice>;
        fn __alef_phantom_vec_stream_delta() -> Vec<StreamDelta>;
        fn __alef_phantom_vec_stream_tool_call() -> Vec<StreamToolCall>;
        fn __alef_phantom_vec_stream_function_call() -> Vec<StreamFunctionCall>;
        fn __alef_phantom_vec_embedding_request() -> Vec<EmbeddingRequest>;
        fn __alef_phantom_vec_embedding_response() -> Vec<EmbeddingResponse>;
        fn __alef_phantom_vec_embedding_object() -> Vec<EmbeddingObject>;
        fn __alef_phantom_vec_create_image_request() -> Vec<CreateImageRequest>;
        fn __alef_phantom_vec_images_response() -> Vec<ImagesResponse>;
        fn __alef_phantom_vec_image() -> Vec<Image>;
        fn __alef_phantom_vec_decoded_data_url() -> Vec<DecodedDataUrl>;
        fn __alef_phantom_vec_create_speech_request() -> Vec<CreateSpeechRequest>;
        fn __alef_phantom_vec_create_transcription_request() -> Vec<CreateTranscriptionRequest>;
        fn __alef_phantom_vec_transcription_response() -> Vec<TranscriptionResponse>;
        fn __alef_phantom_vec_transcription_segment() -> Vec<TranscriptionSegment>;
        fn __alef_phantom_vec_moderation_request() -> Vec<ModerationRequest>;
        fn __alef_phantom_vec_moderation_response() -> Vec<ModerationResponse>;
        fn __alef_phantom_vec_moderation_result() -> Vec<ModerationResult>;
        fn __alef_phantom_vec_moderation_categories() -> Vec<ModerationCategories>;
        fn __alef_phantom_vec_moderation_category_scores() -> Vec<ModerationCategoryScores>;
        fn __alef_phantom_vec_rerank_request() -> Vec<RerankRequest>;
        fn __alef_phantom_vec_rerank_response() -> Vec<RerankResponse>;
        fn __alef_phantom_vec_rerank_result() -> Vec<RerankResult>;
        fn __alef_phantom_vec_rerank_result_document() -> Vec<RerankResultDocument>;
        fn __alef_phantom_vec_search_request() -> Vec<SearchRequest>;
        fn __alef_phantom_vec_search_response() -> Vec<SearchResponse>;
        fn __alef_phantom_vec_search_result() -> Vec<SearchResult>;
        fn __alef_phantom_vec_ocr_request() -> Vec<OcrRequest>;
        fn __alef_phantom_vec_ocr_response() -> Vec<OcrResponse>;
        fn __alef_phantom_vec_ocr_page() -> Vec<OcrPage>;
        fn __alef_phantom_vec_ocr_image() -> Vec<OcrImage>;
        fn __alef_phantom_vec_page_dimensions() -> Vec<PageDimensions>;
        fn __alef_phantom_vec_models_list_response() -> Vec<ModelsListResponse>;
        fn __alef_phantom_vec_model_object() -> Vec<ModelObject>;
        fn __alef_phantom_vec_create_file_request() -> Vec<CreateFileRequest>;
        fn __alef_phantom_vec_file_object() -> Vec<FileObject>;
        fn __alef_phantom_vec_file_list_response() -> Vec<FileListResponse>;
        fn __alef_phantom_vec_file_list_query() -> Vec<FileListQuery>;
        fn __alef_phantom_vec_delete_response() -> Vec<DeleteResponse>;
        fn __alef_phantom_vec_create_batch_request() -> Vec<CreateBatchRequest>;
        fn __alef_phantom_vec_batch_object() -> Vec<BatchObject>;
        fn __alef_phantom_vec_batch_request_counts() -> Vec<BatchRequestCounts>;
        fn __alef_phantom_vec_batch_list_response() -> Vec<BatchListResponse>;
        fn __alef_phantom_vec_batch_list_query() -> Vec<BatchListQuery>;
        fn __alef_phantom_vec_create_response_request() -> Vec<CreateResponseRequest>;
        fn __alef_phantom_vec_response_tool() -> Vec<ResponseTool>;
        fn __alef_phantom_vec_response_object() -> Vec<ResponseObject>;
        fn __alef_phantom_vec_response_output_item() -> Vec<ResponseOutputItem>;
        fn __alef_phantom_vec_response_usage() -> Vec<ResponseUsage>;
        fn __alef_phantom_vec_wait_for_batch_config() -> Vec<WaitForBatchConfig>;
        fn __alef_phantom_vec_custom_provider_config() -> Vec<CustomProviderConfig>;
        fn __alef_phantom_vec_provider_capabilities() -> Vec<ProviderCapabilities>;
        fn __alef_phantom_vec_provider_config() -> Vec<ProviderConfig>;
        fn __alef_phantom_vec_auth_config() -> Vec<AuthConfig>;
        fn __alef_phantom_vec_budget_config() -> Vec<BudgetConfig>;
        fn __alef_phantom_vec_cache_config() -> Vec<CacheConfig>;
        fn __alef_phantom_vec_singleflight_result() -> Vec<SingleflightResult>;
        fn __alef_phantom_vec_rate_limit_config() -> Vec<RateLimitConfig>;
        fn __alef_phantom_vec_intent_prototype() -> Vec<IntentPrototype>;
        fn __alef_phantom_vec_message() -> Vec<Message>;
        fn __alef_phantom_vec_user_content() -> Vec<UserContent>;
        fn __alef_phantom_vec_content_part() -> Vec<ContentPart>;
        fn __alef_phantom_vec_image_detail() -> Vec<ImageDetail>;
        fn __alef_phantom_vec_assistant_content() -> Vec<AssistantContent>;
        fn __alef_phantom_vec_assistant_part() -> Vec<AssistantPart>;
        fn __alef_phantom_vec_tool_type() -> Vec<ToolType>;
        fn __alef_phantom_vec_tool_choice() -> Vec<ToolChoice>;
        fn __alef_phantom_vec_tool_choice_mode() -> Vec<ToolChoiceMode>;
        fn __alef_phantom_vec_response_format() -> Vec<ResponseFormat>;
        fn __alef_phantom_vec_stop_sequence() -> Vec<StopSequence>;
        fn __alef_phantom_vec_modality() -> Vec<Modality>;
        fn __alef_phantom_vec_finish_reason() -> Vec<FinishReason>;
        fn __alef_phantom_vec_reasoning_effort() -> Vec<ReasoningEffort>;
        fn __alef_phantom_vec_embedding_format() -> Vec<EmbeddingFormat>;
        fn __alef_phantom_vec_embedding_input() -> Vec<EmbeddingInput>;
        fn __alef_phantom_vec_moderation_input() -> Vec<ModerationInput>;
        fn __alef_phantom_vec_rerank_document() -> Vec<RerankDocument>;
        fn __alef_phantom_vec_ocr_document() -> Vec<OcrDocument>;
        fn __alef_phantom_vec_file_purpose() -> Vec<FilePurpose>;
        fn __alef_phantom_vec_batch_status() -> Vec<BatchStatus>;
        fn __alef_phantom_vec_auth_header_format() -> Vec<AuthHeaderFormat>;
        fn __alef_phantom_vec_stream_format() -> Vec<StreamFormat>;
        fn __alef_phantom_vec_auth_type() -> Vec<AuthType>;
        fn __alef_phantom_vec_enforcement() -> Vec<Enforcement>;
        fn __alef_phantom_vec_cache_backend() -> Vec<CacheBackend>;
        fn __alef_phantom_vec_circuit_state() -> Vec<CircuitState>;
        fn __alef_phantom_vec_health_status() -> Vec<HealthStatus>;
    }
}

#[doc(hidden)]
pub fn __alef_phantom_vec_system_message() -> Vec<SystemMessage> {
    Vec::new()
}
#[doc(hidden)]
pub fn __alef_phantom_vec_user_message() -> Vec<UserMessage> {
    Vec::new()
}
#[doc(hidden)]
pub fn __alef_phantom_vec_image_url() -> Vec<ImageUrl> {
    Vec::new()
}
#[doc(hidden)]
pub fn __alef_phantom_vec_document_content() -> Vec<DocumentContent> {
    Vec::new()
}
#[doc(hidden)]
pub fn __alef_phantom_vec_audio_content() -> Vec<AudioContent> {
    Vec::new()
}
#[doc(hidden)]
pub fn __alef_phantom_vec_assistant_message() -> Vec<AssistantMessage> {
    Vec::new()
}
#[doc(hidden)]
pub fn __alef_phantom_vec_tool_message() -> Vec<ToolMessage> {
    Vec::new()
}
#[doc(hidden)]
pub fn __alef_phantom_vec_developer_message() -> Vec<DeveloperMessage> {
    Vec::new()
}
#[doc(hidden)]
pub fn __alef_phantom_vec_function_message() -> Vec<FunctionMessage> {
    Vec::new()
}
#[doc(hidden)]
pub fn __alef_phantom_vec_chat_completion_tool() -> Vec<ChatCompletionTool> {
    Vec::new()
}
#[doc(hidden)]
pub fn __alef_phantom_vec_function_definition() -> Vec<FunctionDefinition> {
    Vec::new()
}
#[doc(hidden)]
pub fn __alef_phantom_vec_tool_call() -> Vec<ToolCall> {
    Vec::new()
}
#[doc(hidden)]
pub fn __alef_phantom_vec_function_call() -> Vec<FunctionCall> {
    Vec::new()
}
#[doc(hidden)]
pub fn __alef_phantom_vec_specific_tool_choice() -> Vec<SpecificToolChoice> {
    Vec::new()
}
#[doc(hidden)]
pub fn __alef_phantom_vec_specific_function() -> Vec<SpecificFunction> {
    Vec::new()
}
#[doc(hidden)]
pub fn __alef_phantom_vec_json_schema_format() -> Vec<JsonSchemaFormat> {
    Vec::new()
}
#[doc(hidden)]
pub fn __alef_phantom_vec_usage() -> Vec<Usage> {
    Vec::new()
}
#[doc(hidden)]
pub fn __alef_phantom_vec_prompt_tokens_details() -> Vec<PromptTokensDetails> {
    Vec::new()
}
#[doc(hidden)]
pub fn __alef_phantom_vec_chat_completion_request() -> Vec<ChatCompletionRequest> {
    Vec::new()
}
#[doc(hidden)]
pub fn __alef_phantom_vec_stream_options() -> Vec<StreamOptions> {
    Vec::new()
}
#[doc(hidden)]
pub fn __alef_phantom_vec_chat_completion_response() -> Vec<ChatCompletionResponse> {
    Vec::new()
}
#[doc(hidden)]
pub fn __alef_phantom_vec_choice() -> Vec<Choice> {
    Vec::new()
}
#[doc(hidden)]
pub fn __alef_phantom_vec_chat_completion_chunk() -> Vec<ChatCompletionChunk> {
    Vec::new()
}
#[doc(hidden)]
pub fn __alef_phantom_vec_stream_choice() -> Vec<StreamChoice> {
    Vec::new()
}
#[doc(hidden)]
pub fn __alef_phantom_vec_stream_delta() -> Vec<StreamDelta> {
    Vec::new()
}
#[doc(hidden)]
pub fn __alef_phantom_vec_stream_tool_call() -> Vec<StreamToolCall> {
    Vec::new()
}
#[doc(hidden)]
pub fn __alef_phantom_vec_stream_function_call() -> Vec<StreamFunctionCall> {
    Vec::new()
}
#[doc(hidden)]
pub fn __alef_phantom_vec_embedding_request() -> Vec<EmbeddingRequest> {
    Vec::new()
}
#[doc(hidden)]
pub fn __alef_phantom_vec_embedding_response() -> Vec<EmbeddingResponse> {
    Vec::new()
}
#[doc(hidden)]
pub fn __alef_phantom_vec_embedding_object() -> Vec<EmbeddingObject> {
    Vec::new()
}
#[doc(hidden)]
pub fn __alef_phantom_vec_create_image_request() -> Vec<CreateImageRequest> {
    Vec::new()
}
#[doc(hidden)]
pub fn __alef_phantom_vec_images_response() -> Vec<ImagesResponse> {
    Vec::new()
}
#[doc(hidden)]
pub fn __alef_phantom_vec_image() -> Vec<Image> {
    Vec::new()
}
#[doc(hidden)]
pub fn __alef_phantom_vec_decoded_data_url() -> Vec<DecodedDataUrl> {
    Vec::new()
}
#[doc(hidden)]
pub fn __alef_phantom_vec_create_speech_request() -> Vec<CreateSpeechRequest> {
    Vec::new()
}
#[doc(hidden)]
pub fn __alef_phantom_vec_create_transcription_request() -> Vec<CreateTranscriptionRequest> {
    Vec::new()
}
#[doc(hidden)]
pub fn __alef_phantom_vec_transcription_response() -> Vec<TranscriptionResponse> {
    Vec::new()
}
#[doc(hidden)]
pub fn __alef_phantom_vec_transcription_segment() -> Vec<TranscriptionSegment> {
    Vec::new()
}
#[doc(hidden)]
pub fn __alef_phantom_vec_moderation_request() -> Vec<ModerationRequest> {
    Vec::new()
}
#[doc(hidden)]
pub fn __alef_phantom_vec_moderation_response() -> Vec<ModerationResponse> {
    Vec::new()
}
#[doc(hidden)]
pub fn __alef_phantom_vec_moderation_result() -> Vec<ModerationResult> {
    Vec::new()
}
#[doc(hidden)]
pub fn __alef_phantom_vec_moderation_categories() -> Vec<ModerationCategories> {
    Vec::new()
}
#[doc(hidden)]
pub fn __alef_phantom_vec_moderation_category_scores() -> Vec<ModerationCategoryScores> {
    Vec::new()
}
#[doc(hidden)]
pub fn __alef_phantom_vec_rerank_request() -> Vec<RerankRequest> {
    Vec::new()
}
#[doc(hidden)]
pub fn __alef_phantom_vec_rerank_response() -> Vec<RerankResponse> {
    Vec::new()
}
#[doc(hidden)]
pub fn __alef_phantom_vec_rerank_result() -> Vec<RerankResult> {
    Vec::new()
}
#[doc(hidden)]
pub fn __alef_phantom_vec_rerank_result_document() -> Vec<RerankResultDocument> {
    Vec::new()
}
#[doc(hidden)]
pub fn __alef_phantom_vec_search_request() -> Vec<SearchRequest> {
    Vec::new()
}
#[doc(hidden)]
pub fn __alef_phantom_vec_search_response() -> Vec<SearchResponse> {
    Vec::new()
}
#[doc(hidden)]
pub fn __alef_phantom_vec_search_result() -> Vec<SearchResult> {
    Vec::new()
}
#[doc(hidden)]
pub fn __alef_phantom_vec_ocr_request() -> Vec<OcrRequest> {
    Vec::new()
}
#[doc(hidden)]
pub fn __alef_phantom_vec_ocr_response() -> Vec<OcrResponse> {
    Vec::new()
}
#[doc(hidden)]
pub fn __alef_phantom_vec_ocr_page() -> Vec<OcrPage> {
    Vec::new()
}
#[doc(hidden)]
pub fn __alef_phantom_vec_ocr_image() -> Vec<OcrImage> {
    Vec::new()
}
#[doc(hidden)]
pub fn __alef_phantom_vec_page_dimensions() -> Vec<PageDimensions> {
    Vec::new()
}
#[doc(hidden)]
pub fn __alef_phantom_vec_models_list_response() -> Vec<ModelsListResponse> {
    Vec::new()
}
#[doc(hidden)]
pub fn __alef_phantom_vec_model_object() -> Vec<ModelObject> {
    Vec::new()
}
#[doc(hidden)]
pub fn __alef_phantom_vec_create_file_request() -> Vec<CreateFileRequest> {
    Vec::new()
}
#[doc(hidden)]
pub fn __alef_phantom_vec_file_object() -> Vec<FileObject> {
    Vec::new()
}
#[doc(hidden)]
pub fn __alef_phantom_vec_file_list_response() -> Vec<FileListResponse> {
    Vec::new()
}
#[doc(hidden)]
pub fn __alef_phantom_vec_file_list_query() -> Vec<FileListQuery> {
    Vec::new()
}
#[doc(hidden)]
pub fn __alef_phantom_vec_delete_response() -> Vec<DeleteResponse> {
    Vec::new()
}
#[doc(hidden)]
pub fn __alef_phantom_vec_create_batch_request() -> Vec<CreateBatchRequest> {
    Vec::new()
}
#[doc(hidden)]
pub fn __alef_phantom_vec_batch_object() -> Vec<BatchObject> {
    Vec::new()
}
#[doc(hidden)]
pub fn __alef_phantom_vec_batch_request_counts() -> Vec<BatchRequestCounts> {
    Vec::new()
}
#[doc(hidden)]
pub fn __alef_phantom_vec_batch_list_response() -> Vec<BatchListResponse> {
    Vec::new()
}
#[doc(hidden)]
pub fn __alef_phantom_vec_batch_list_query() -> Vec<BatchListQuery> {
    Vec::new()
}
#[doc(hidden)]
pub fn __alef_phantom_vec_create_response_request() -> Vec<CreateResponseRequest> {
    Vec::new()
}
#[doc(hidden)]
pub fn __alef_phantom_vec_response_tool() -> Vec<ResponseTool> {
    Vec::new()
}
#[doc(hidden)]
pub fn __alef_phantom_vec_response_object() -> Vec<ResponseObject> {
    Vec::new()
}
#[doc(hidden)]
pub fn __alef_phantom_vec_response_output_item() -> Vec<ResponseOutputItem> {
    Vec::new()
}
#[doc(hidden)]
pub fn __alef_phantom_vec_response_usage() -> Vec<ResponseUsage> {
    Vec::new()
}
#[doc(hidden)]
pub fn __alef_phantom_vec_wait_for_batch_config() -> Vec<WaitForBatchConfig> {
    Vec::new()
}
#[doc(hidden)]
pub fn __alef_phantom_vec_custom_provider_config() -> Vec<CustomProviderConfig> {
    Vec::new()
}
#[doc(hidden)]
pub fn __alef_phantom_vec_provider_capabilities() -> Vec<ProviderCapabilities> {
    Vec::new()
}
#[doc(hidden)]
pub fn __alef_phantom_vec_provider_config() -> Vec<ProviderConfig> {
    Vec::new()
}
#[doc(hidden)]
pub fn __alef_phantom_vec_auth_config() -> Vec<AuthConfig> {
    Vec::new()
}
#[doc(hidden)]
pub fn __alef_phantom_vec_budget_config() -> Vec<BudgetConfig> {
    Vec::new()
}
#[doc(hidden)]
pub fn __alef_phantom_vec_cache_config() -> Vec<CacheConfig> {
    Vec::new()
}
#[doc(hidden)]
pub fn __alef_phantom_vec_singleflight_result() -> Vec<SingleflightResult> {
    Vec::new()
}
#[doc(hidden)]
pub fn __alef_phantom_vec_rate_limit_config() -> Vec<RateLimitConfig> {
    Vec::new()
}
#[doc(hidden)]
pub fn __alef_phantom_vec_intent_prototype() -> Vec<IntentPrototype> {
    Vec::new()
}
#[doc(hidden)]
pub fn __alef_phantom_vec_message() -> Vec<Message> {
    Vec::new()
}
#[doc(hidden)]
pub fn __alef_phantom_vec_user_content() -> Vec<UserContent> {
    Vec::new()
}
#[doc(hidden)]
pub fn __alef_phantom_vec_content_part() -> Vec<ContentPart> {
    Vec::new()
}
#[doc(hidden)]
pub fn __alef_phantom_vec_image_detail() -> Vec<ImageDetail> {
    Vec::new()
}
#[doc(hidden)]
pub fn __alef_phantom_vec_assistant_content() -> Vec<AssistantContent> {
    Vec::new()
}
#[doc(hidden)]
pub fn __alef_phantom_vec_assistant_part() -> Vec<AssistantPart> {
    Vec::new()
}
#[doc(hidden)]
pub fn __alef_phantom_vec_tool_type() -> Vec<ToolType> {
    Vec::new()
}
#[doc(hidden)]
pub fn __alef_phantom_vec_tool_choice() -> Vec<ToolChoice> {
    Vec::new()
}
#[doc(hidden)]
pub fn __alef_phantom_vec_tool_choice_mode() -> Vec<ToolChoiceMode> {
    Vec::new()
}
#[doc(hidden)]
pub fn __alef_phantom_vec_response_format() -> Vec<ResponseFormat> {
    Vec::new()
}
#[doc(hidden)]
pub fn __alef_phantom_vec_stop_sequence() -> Vec<StopSequence> {
    Vec::new()
}
#[doc(hidden)]
pub fn __alef_phantom_vec_modality() -> Vec<Modality> {
    Vec::new()
}
#[doc(hidden)]
pub fn __alef_phantom_vec_finish_reason() -> Vec<FinishReason> {
    Vec::new()
}
#[doc(hidden)]
pub fn __alef_phantom_vec_reasoning_effort() -> Vec<ReasoningEffort> {
    Vec::new()
}
#[doc(hidden)]
pub fn __alef_phantom_vec_embedding_format() -> Vec<EmbeddingFormat> {
    Vec::new()
}
#[doc(hidden)]
pub fn __alef_phantom_vec_embedding_input() -> Vec<EmbeddingInput> {
    Vec::new()
}
#[doc(hidden)]
pub fn __alef_phantom_vec_moderation_input() -> Vec<ModerationInput> {
    Vec::new()
}
#[doc(hidden)]
pub fn __alef_phantom_vec_rerank_document() -> Vec<RerankDocument> {
    Vec::new()
}
#[doc(hidden)]
pub fn __alef_phantom_vec_ocr_document() -> Vec<OcrDocument> {
    Vec::new()
}
#[doc(hidden)]
pub fn __alef_phantom_vec_file_purpose() -> Vec<FilePurpose> {
    Vec::new()
}
#[doc(hidden)]
pub fn __alef_phantom_vec_batch_status() -> Vec<BatchStatus> {
    Vec::new()
}
#[doc(hidden)]
pub fn __alef_phantom_vec_auth_header_format() -> Vec<AuthHeaderFormat> {
    Vec::new()
}
#[doc(hidden)]
pub fn __alef_phantom_vec_stream_format() -> Vec<StreamFormat> {
    Vec::new()
}
#[doc(hidden)]
pub fn __alef_phantom_vec_auth_type() -> Vec<AuthType> {
    Vec::new()
}
#[doc(hidden)]
pub fn __alef_phantom_vec_enforcement() -> Vec<Enforcement> {
    Vec::new()
}
#[doc(hidden)]
pub fn __alef_phantom_vec_cache_backend() -> Vec<CacheBackend> {
    Vec::new()
}
#[doc(hidden)]
pub fn __alef_phantom_vec_circuit_state() -> Vec<CircuitState> {
    Vec::new()
}
#[doc(hidden)]
pub fn __alef_phantom_vec_health_status() -> Vec<HealthStatus> {
    Vec::new()
}

pub struct SystemMessage(pub liter_llm::types::SystemMessage);
impl SystemMessage {
    pub fn new(content: UserContent, name: Option<String>) -> SystemMessage {
        let mut __target: liter_llm::types::SystemMessage = ::std::default::Default::default();
        // alef: content (UserContent) is an enum; reverse From not generated — left at default
        if let Some(s) = name {
            // Try JSON parse first (handles enum/object values); on parse failure
            // treat the raw input as a JSON string scalar so plain `String` /
            // string-like enum fields don't end up empty for inputs that aren't
            // valid JSON tokens (e.g. `tts-1`).
            let __v = ::serde_json::from_str::<::serde_json::Value>(&s).unwrap_or(::serde_json::Value::String(s));
            if let Ok(t) = ::serde_json::from_value(__v) {
                __target.name = Some(t);
            }
        }
        SystemMessage(__target)
    }
    pub fn content(&self) -> String {
        serde_json::to_string(&self.0.content).unwrap_or_else(|_| "null".to_string())
    }
    pub fn name(&self) -> Option<String> {
        self.0.name.clone()
    }
}

pub struct UserMessage(pub liter_llm::types::UserMessage);
impl UserMessage {
    pub fn new(content: UserContent, name: Option<String>) -> UserMessage {
        let mut __target: liter_llm::types::UserMessage = ::std::default::Default::default();
        // alef: content (UserContent) is an enum; reverse From not generated — left at default
        if let Some(s) = name {
            // Try JSON parse first (handles enum/object values); on parse failure
            // treat the raw input as a JSON string scalar so plain `String` /
            // string-like enum fields don't end up empty for inputs that aren't
            // valid JSON tokens (e.g. `tts-1`).
            let __v = ::serde_json::from_str::<::serde_json::Value>(&s).unwrap_or(::serde_json::Value::String(s));
            if let Ok(t) = ::serde_json::from_value(__v) {
                __target.name = Some(t);
            }
        }
        UserMessage(__target)
    }
    pub fn content(&self) -> String {
        serde_json::to_string(&self.0.content).unwrap_or_else(|_| "null".to_string())
    }
    pub fn name(&self) -> Option<String> {
        self.0.name.clone()
    }
}

pub struct ImageUrl(pub liter_llm::types::ImageUrl);
impl ImageUrl {
    pub fn new(url: String, detail: Option<ImageDetail>) -> ImageUrl {
        let mut __target: liter_llm::types::ImageUrl = ::std::default::Default::default();
        {
            // Try JSON parse first (handles enum/object values); on parse failure
            // treat the raw input as a JSON string scalar so plain `String` /
            // string-like enum fields don't end up empty for inputs that aren't
            // valid JSON tokens (e.g. `tts-1`).
            let __v =
                ::serde_json::from_str::<::serde_json::Value>(&url).unwrap_or(::serde_json::Value::String(url.clone()));
            if let Ok(t) = ::serde_json::from_value(__v) {
                __target.url = t;
            }
        }
        // alef: detail (ImageDetail) is an enum; reverse From not generated — left at default
        ImageUrl(__target)
    }
    pub fn url(&self) -> String {
        self.0.url.clone()
    }
    pub fn detail(&self) -> Option<String> {
        self.0.detail.clone().map(|w| ImageDetail::from(w).to_string())
    }
}

pub struct DocumentContent(pub liter_llm::types::DocumentContent);
impl DocumentContent {
    pub fn new(data: String, media_type: String) -> DocumentContent {
        let mut __target: liter_llm::types::DocumentContent = ::std::default::Default::default();
        {
            // Try JSON parse first (handles enum/object values); on parse failure
            // treat the raw input as a JSON string scalar so plain `String` /
            // string-like enum fields don't end up empty for inputs that aren't
            // valid JSON tokens (e.g. `tts-1`).
            let __v = ::serde_json::from_str::<::serde_json::Value>(&data)
                .unwrap_or(::serde_json::Value::String(data.clone()));
            if let Ok(t) = ::serde_json::from_value(__v) {
                __target.data = t;
            }
        }
        {
            // Try JSON parse first (handles enum/object values); on parse failure
            // treat the raw input as a JSON string scalar so plain `String` /
            // string-like enum fields don't end up empty for inputs that aren't
            // valid JSON tokens (e.g. `tts-1`).
            let __v = ::serde_json::from_str::<::serde_json::Value>(&media_type)
                .unwrap_or(::serde_json::Value::String(media_type.clone()));
            if let Ok(t) = ::serde_json::from_value(__v) {
                __target.media_type = t;
            }
        }
        DocumentContent(__target)
    }
    pub fn data(&self) -> String {
        self.0.data.clone()
    }
    pub fn media_type(&self) -> String {
        self.0.media_type.clone()
    }
}

pub struct AudioContent(pub liter_llm::types::AudioContent);
impl AudioContent {
    pub fn new(data: String, format: String) -> AudioContent {
        let mut __target: liter_llm::types::AudioContent = ::std::default::Default::default();
        {
            // Try JSON parse first (handles enum/object values); on parse failure
            // treat the raw input as a JSON string scalar so plain `String` /
            // string-like enum fields don't end up empty for inputs that aren't
            // valid JSON tokens (e.g. `tts-1`).
            let __v = ::serde_json::from_str::<::serde_json::Value>(&data)
                .unwrap_or(::serde_json::Value::String(data.clone()));
            if let Ok(t) = ::serde_json::from_value(__v) {
                __target.data = t;
            }
        }
        {
            // Try JSON parse first (handles enum/object values); on parse failure
            // treat the raw input as a JSON string scalar so plain `String` /
            // string-like enum fields don't end up empty for inputs that aren't
            // valid JSON tokens (e.g. `tts-1`).
            let __v = ::serde_json::from_str::<::serde_json::Value>(&format)
                .unwrap_or(::serde_json::Value::String(format.clone()));
            if let Ok(t) = ::serde_json::from_value(__v) {
                __target.format = t;
            }
        }
        AudioContent(__target)
    }
    pub fn data(&self) -> String {
        self.0.data.clone()
    }
    pub fn format(&self) -> String {
        self.0.format.clone()
    }
}

pub struct AssistantMessage(pub liter_llm::types::AssistantMessage);
impl AssistantMessage {
    pub fn new(
        content: Option<AssistantContent>,
        name: Option<String>,
        tool_calls: Option<Vec<ToolCall>>,
        refusal: Option<String>,
        function_call: Option<FunctionCall>,
    ) -> AssistantMessage {
        let mut __target: liter_llm::types::AssistantMessage = ::std::default::Default::default();
        // alef: content (AssistantContent) is an enum; reverse From not generated — left at default
        if let Some(s) = name {
            // Try JSON parse first (handles enum/object values); on parse failure
            // treat the raw input as a JSON string scalar so plain `String` /
            // string-like enum fields don't end up empty for inputs that aren't
            // valid JSON tokens (e.g. `tts-1`).
            let __v = ::serde_json::from_str::<::serde_json::Value>(&s).unwrap_or(::serde_json::Value::String(s));
            if let Ok(t) = ::serde_json::from_value(__v) {
                __target.name = Some(t);
            }
        }
        if let Some(v) = tool_calls {
            __target.tool_calls = Some(v.into_iter().map(|w| w.0).collect());
        }
        if let Some(s) = refusal {
            // Try JSON parse first (handles enum/object values); on parse failure
            // treat the raw input as a JSON string scalar so plain `String` /
            // string-like enum fields don't end up empty for inputs that aren't
            // valid JSON tokens (e.g. `tts-1`).
            let __v = ::serde_json::from_str::<::serde_json::Value>(&s).unwrap_or(::serde_json::Value::String(s));
            if let Ok(t) = ::serde_json::from_value(__v) {
                __target.refusal = Some(t);
            }
        }
        if let Some(w) = function_call {
            __target.function_call = Some(w.0);
        }
        AssistantMessage(__target)
    }
    pub fn content(&self) -> Option<String> {
        self.0
            .content
            .clone()
            .map(|w| serde_json::to_string(&w).unwrap_or_else(|_| "null".to_string()))
    }
    pub fn name(&self) -> Option<String> {
        self.0.name.clone()
    }
    pub fn tool_calls(&self) -> String {
        serde_json::to_string(&self.0.tool_calls.as_ref().map(|v| {
            v.iter()
                .map(|elem| serde_json::to_string(elem).unwrap_or_else(|_| "null".to_string()))
                .collect::<Vec<_>>()
        }))
        .expect("serializable enum vec")
    }
    pub fn refusal(&self) -> Option<String> {
        self.0.refusal.clone()
    }
    pub fn function_call(&self) -> Option<FunctionCall> {
        self.0.function_call.clone().map(FunctionCall)
    }
}

pub fn assistant_message_text_from_json(json: String) -> Result<String, String> {
    let __self: liter_llm::types::AssistantMessage =
        serde_json::from_str(&json).map_err(|e| format!("Failed to deserialize AssistantMessage: {}", e))?;
    let __result = __self.text();
    serde_json::to_string(&__result).map_err(|e| format!("Failed to serialize result: {}", e))
}

pub fn assistant_message_refusal_text_from_json(json: String) -> Result<String, String> {
    let __self: liter_llm::types::AssistantMessage =
        serde_json::from_str(&json).map_err(|e| format!("Failed to deserialize AssistantMessage: {}", e))?;
    let __result = __self.refusal_text();
    serde_json::to_string(&__result).map_err(|e| format!("Failed to serialize result: {}", e))
}

pub fn assistant_message_output_images_from_json(json: String) -> Result<String, String> {
    let __self: liter_llm::types::AssistantMessage =
        serde_json::from_str(&json).map_err(|e| format!("Failed to deserialize AssistantMessage: {}", e))?;
    let __result = __self.output_images();
    serde_json::to_string(&__result).map_err(|e| format!("Failed to serialize result: {}", e))
}

pub fn assistant_message_output_audio_from_json(json: String) -> Result<String, String> {
    let __self: liter_llm::types::AssistantMessage =
        serde_json::from_str(&json).map_err(|e| format!("Failed to deserialize AssistantMessage: {}", e))?;
    let __result = __self.output_audio();
    serde_json::to_string(&__result).map_err(|e| format!("Failed to serialize result: {}", e))
}

pub struct ToolMessage(pub liter_llm::types::ToolMessage);
impl ToolMessage {
    pub fn new(content: String, tool_call_id: String, name: Option<String>) -> ToolMessage {
        let mut __target: liter_llm::types::ToolMessage = ::std::default::Default::default();
        {
            // Try JSON parse first (handles enum/object values); on parse failure
            // treat the raw input as a JSON string scalar so plain `String` /
            // string-like enum fields don't end up empty for inputs that aren't
            // valid JSON tokens (e.g. `tts-1`).
            let __v = ::serde_json::from_str::<::serde_json::Value>(&content)
                .unwrap_or(::serde_json::Value::String(content.clone()));
            if let Ok(t) = ::serde_json::from_value(__v) {
                __target.content = t;
            }
        }
        {
            // Try JSON parse first (handles enum/object values); on parse failure
            // treat the raw input as a JSON string scalar so plain `String` /
            // string-like enum fields don't end up empty for inputs that aren't
            // valid JSON tokens (e.g. `tts-1`).
            let __v = ::serde_json::from_str::<::serde_json::Value>(&tool_call_id)
                .unwrap_or(::serde_json::Value::String(tool_call_id.clone()));
            if let Ok(t) = ::serde_json::from_value(__v) {
                __target.tool_call_id = t;
            }
        }
        if let Some(s) = name {
            // Try JSON parse first (handles enum/object values); on parse failure
            // treat the raw input as a JSON string scalar so plain `String` /
            // string-like enum fields don't end up empty for inputs that aren't
            // valid JSON tokens (e.g. `tts-1`).
            let __v = ::serde_json::from_str::<::serde_json::Value>(&s).unwrap_or(::serde_json::Value::String(s));
            if let Ok(t) = ::serde_json::from_value(__v) {
                __target.name = Some(t);
            }
        }
        ToolMessage(__target)
    }
    pub fn content(&self) -> String {
        self.0.content.clone()
    }
    pub fn tool_call_id(&self) -> String {
        self.0.tool_call_id.clone()
    }
    pub fn name(&self) -> Option<String> {
        self.0.name.clone()
    }
}

pub struct DeveloperMessage(pub liter_llm::types::DeveloperMessage);
impl DeveloperMessage {
    pub fn new(content: String, name: Option<String>) -> DeveloperMessage {
        let mut __target: liter_llm::types::DeveloperMessage = ::std::default::Default::default();
        {
            // Try JSON parse first (handles enum/object values); on parse failure
            // treat the raw input as a JSON string scalar so plain `String` /
            // string-like enum fields don't end up empty for inputs that aren't
            // valid JSON tokens (e.g. `tts-1`).
            let __v = ::serde_json::from_str::<::serde_json::Value>(&content)
                .unwrap_or(::serde_json::Value::String(content.clone()));
            if let Ok(t) = ::serde_json::from_value(__v) {
                __target.content = t;
            }
        }
        if let Some(s) = name {
            // Try JSON parse first (handles enum/object values); on parse failure
            // treat the raw input as a JSON string scalar so plain `String` /
            // string-like enum fields don't end up empty for inputs that aren't
            // valid JSON tokens (e.g. `tts-1`).
            let __v = ::serde_json::from_str::<::serde_json::Value>(&s).unwrap_or(::serde_json::Value::String(s));
            if let Ok(t) = ::serde_json::from_value(__v) {
                __target.name = Some(t);
            }
        }
        DeveloperMessage(__target)
    }
    pub fn content(&self) -> String {
        self.0.content.clone()
    }
    pub fn name(&self) -> Option<String> {
        self.0.name.clone()
    }
}

pub struct FunctionMessage(pub liter_llm::types::FunctionMessage);
impl FunctionMessage {
    pub fn new(content: String, name: String) -> FunctionMessage {
        let mut __target: liter_llm::types::FunctionMessage = ::std::default::Default::default();
        {
            // Try JSON parse first (handles enum/object values); on parse failure
            // treat the raw input as a JSON string scalar so plain `String` /
            // string-like enum fields don't end up empty for inputs that aren't
            // valid JSON tokens (e.g. `tts-1`).
            let __v = ::serde_json::from_str::<::serde_json::Value>(&content)
                .unwrap_or(::serde_json::Value::String(content.clone()));
            if let Ok(t) = ::serde_json::from_value(__v) {
                __target.content = t;
            }
        }
        {
            // Try JSON parse first (handles enum/object values); on parse failure
            // treat the raw input as a JSON string scalar so plain `String` /
            // string-like enum fields don't end up empty for inputs that aren't
            // valid JSON tokens (e.g. `tts-1`).
            let __v = ::serde_json::from_str::<::serde_json::Value>(&name)
                .unwrap_or(::serde_json::Value::String(name.clone()));
            if let Ok(t) = ::serde_json::from_value(__v) {
                __target.name = t;
            }
        }
        FunctionMessage(__target)
    }
    pub fn content(&self) -> String {
        self.0.content.clone()
    }
    pub fn name(&self) -> String {
        self.0.name.clone()
    }
}

pub struct ChatCompletionTool(pub liter_llm::types::ChatCompletionTool);
impl ChatCompletionTool {
    pub fn tool_type(&self) -> String {
        ToolType::from(self.0.tool_type.clone()).to_string()
    }
    pub fn function(&self) -> FunctionDefinition {
        FunctionDefinition(self.0.function.clone())
    }
}

pub struct FunctionDefinition(pub liter_llm::types::FunctionDefinition);
impl FunctionDefinition {
    pub fn name(&self) -> String {
        self.0.name.clone()
    }
    pub fn description(&self) -> Option<String> {
        self.0.description.clone()
    }
    pub fn parameters(&self) -> Option<String> {
        self.0.parameters.as_ref().and_then(|v| serde_json::to_string(v).ok())
    }
    pub fn strict(&self) -> Option<bool> {
        self.0.strict.as_ref().and_then(|v| {
            ::serde_json::to_value(v)
                .ok()
                .and_then(|j| ::serde_json::from_value(j).ok())
        })
    }
}

pub struct ToolCall(pub liter_llm::types::ToolCall);
impl ToolCall {
    pub fn id(&self) -> String {
        self.0.id.clone()
    }
    pub fn call_type(&self) -> String {
        ToolType::from(self.0.call_type.clone()).to_string()
    }
    pub fn function(&self) -> FunctionCall {
        FunctionCall(self.0.function.clone())
    }
}

pub struct FunctionCall(pub liter_llm::types::FunctionCall);
impl FunctionCall {
    pub fn name(&self) -> String {
        self.0.name.clone()
    }
    pub fn arguments(&self) -> String {
        self.0.arguments.clone()
    }
}

pub struct SpecificToolChoice(pub liter_llm::types::SpecificToolChoice);
impl SpecificToolChoice {
    pub fn new(choice_type: ToolType, function: SpecificFunction) -> SpecificToolChoice {
        let mut __target: liter_llm::types::SpecificToolChoice = ::std::default::Default::default();
        // alef: choice_type (ToolType) is an enum; reverse From not generated — left at default
        __target.function = function.0;
        SpecificToolChoice(__target)
    }
    pub fn choice_type(&self) -> String {
        ToolType::from(self.0.choice_type.clone()).to_string()
    }
    pub fn function(&self) -> SpecificFunction {
        SpecificFunction(self.0.function.clone())
    }
}

pub struct SpecificFunction(pub liter_llm::types::SpecificFunction);
impl SpecificFunction {
    pub fn new(name: String) -> SpecificFunction {
        let mut __target: liter_llm::types::SpecificFunction = ::std::default::Default::default();
        {
            // Try JSON parse first (handles enum/object values); on parse failure
            // treat the raw input as a JSON string scalar so plain `String` /
            // string-like enum fields don't end up empty for inputs that aren't
            // valid JSON tokens (e.g. `tts-1`).
            let __v = ::serde_json::from_str::<::serde_json::Value>(&name)
                .unwrap_or(::serde_json::Value::String(name.clone()));
            if let Ok(t) = ::serde_json::from_value(__v) {
                __target.name = t;
            }
        }
        SpecificFunction(__target)
    }
    pub fn name(&self) -> String {
        self.0.name.clone()
    }
}

pub struct JsonSchemaFormat(pub liter_llm::types::JsonSchemaFormat);
impl JsonSchemaFormat {
    pub fn new(name: String, description: Option<String>, schema: String, strict: Option<bool>) -> JsonSchemaFormat {
        let mut __target: liter_llm::types::JsonSchemaFormat = ::std::default::Default::default();
        {
            // Try JSON parse first (handles enum/object values); on parse failure
            // treat the raw input as a JSON string scalar so plain `String` /
            // string-like enum fields don't end up empty for inputs that aren't
            // valid JSON tokens (e.g. `tts-1`).
            let __v = ::serde_json::from_str::<::serde_json::Value>(&name)
                .unwrap_or(::serde_json::Value::String(name.clone()));
            if let Ok(t) = ::serde_json::from_value(__v) {
                __target.name = t;
            }
        }
        if let Some(s) = description {
            // Try JSON parse first (handles enum/object values); on parse failure
            // treat the raw input as a JSON string scalar so plain `String` /
            // string-like enum fields don't end up empty for inputs that aren't
            // valid JSON tokens (e.g. `tts-1`).
            let __v = ::serde_json::from_str::<::serde_json::Value>(&s).unwrap_or(::serde_json::Value::String(s));
            if let Ok(t) = ::serde_json::from_value(__v) {
                __target.description = Some(t);
            }
        }
        {
            // Try JSON parse first (handles enum/object values); on parse failure
            // treat the raw input as a JSON string scalar so plain `String` /
            // string-like enum fields don't end up empty for inputs that aren't
            // valid JSON tokens (e.g. `tts-1`).
            let __v = ::serde_json::from_str::<::serde_json::Value>(&schema)
                .unwrap_or(::serde_json::Value::String(schema.clone()));
            if let Ok(t) = ::serde_json::from_value(__v) {
                __target.schema = t;
            }
        }
        __target.strict = strict;
        JsonSchemaFormat(__target)
    }
    pub fn name(&self) -> String {
        self.0.name.clone()
    }
    pub fn description(&self) -> Option<String> {
        self.0.description.clone()
    }
    pub fn schema(&self) -> String {
        serde_json::to_string(&self.0.schema).unwrap_or_default()
    }
    pub fn strict(&self) -> Option<bool> {
        self.0.strict.as_ref().and_then(|v| {
            ::serde_json::to_value(v)
                .ok()
                .and_then(|j| ::serde_json::from_value(j).ok())
        })
    }
}

pub struct Usage(pub liter_llm::types::Usage);
impl Usage {
    pub fn new(
        prompt_tokens: u64,
        completion_tokens: u64,
        total_tokens: u64,
        prompt_tokens_details: Option<PromptTokensDetails>,
    ) -> Usage {
        let mut __target: liter_llm::types::Usage = ::std::default::Default::default();
        __target.prompt_tokens = prompt_tokens;
        __target.completion_tokens = completion_tokens;
        __target.total_tokens = total_tokens;
        if let Some(w) = prompt_tokens_details {
            __target.prompt_tokens_details = Some(w.0);
        }
        Usage(__target)
    }
    pub fn prompt_tokens(&self) -> u64 {
        ::serde_json::to_value(&self.0.prompt_tokens)
            .ok()
            .and_then(|j| ::serde_json::from_value(j).ok())
            .unwrap_or_default()
    }
    pub fn completion_tokens(&self) -> u64 {
        ::serde_json::to_value(&self.0.completion_tokens)
            .ok()
            .and_then(|j| ::serde_json::from_value(j).ok())
            .unwrap_or_default()
    }
    pub fn total_tokens(&self) -> u64 {
        ::serde_json::to_value(&self.0.total_tokens)
            .ok()
            .and_then(|j| ::serde_json::from_value(j).ok())
            .unwrap_or_default()
    }
    pub fn prompt_tokens_details(&self) -> Option<PromptTokensDetails> {
        self.0.prompt_tokens_details.clone().map(PromptTokensDetails)
    }
}

pub struct PromptTokensDetails(pub liter_llm::types::PromptTokensDetails);
impl PromptTokensDetails {
    pub fn new(cached_tokens: u64, audio_tokens: u64) -> PromptTokensDetails {
        PromptTokensDetails(liter_llm::types::PromptTokensDetails {
            cached_tokens,
            audio_tokens,
        })
    }
    pub fn cached_tokens(&self) -> u64 {
        ::serde_json::to_value(&self.0.cached_tokens)
            .ok()
            .and_then(|j| ::serde_json::from_value(j).ok())
            .unwrap_or_default()
    }
    pub fn audio_tokens(&self) -> u64 {
        ::serde_json::to_value(&self.0.audio_tokens)
            .ok()
            .and_then(|j| ::serde_json::from_value(j).ok())
            .unwrap_or_default()
    }
}

pub struct ChatCompletionRequest(pub liter_llm::types::ChatCompletionRequest);
impl ChatCompletionRequest {
    pub fn new(
        model: String,
        messages: Vec<Message>,
        temperature: Option<f64>,
        top_p: Option<f64>,
        n: Option<u32>,
        stream: Option<bool>,
        stop: Option<StopSequence>,
        max_tokens: Option<u64>,
        presence_penalty: Option<f64>,
        frequency_penalty: Option<f64>,
        logit_bias: String,
        user: Option<String>,
        tools: Option<Vec<ChatCompletionTool>>,
        tool_choice: Option<ToolChoice>,
        parallel_tool_calls: Option<bool>,
        response_format: Option<ResponseFormat>,
        stream_options: Option<StreamOptions>,
        seed: Option<i64>,
        reasoning_effort: Option<ReasoningEffort>,
        modalities: Option<Vec<Modality>>,
        extra_body: Option<String>,
    ) -> ChatCompletionRequest {
        let mut __target: liter_llm::types::ChatCompletionRequest = ::std::default::Default::default();
        {
            // Try JSON parse first (handles enum/object values); on parse failure
            // treat the raw input as a JSON string scalar so plain `String` /
            // string-like enum fields don't end up empty for inputs that aren't
            // valid JSON tokens (e.g. `tts-1`).
            let __v = ::serde_json::from_str::<::serde_json::Value>(&model)
                .unwrap_or(::serde_json::Value::String(model.clone()));
            if let Ok(t) = ::serde_json::from_value(__v) {
                __target.model = t;
            }
        }
        // alef: messages (Vec<>) contains enum elements — left at default
        __target.temperature = temperature;
        __target.top_p = top_p;
        __target.n = n;
        __target.stream = stream;
        // alef: stop (StopSequence) is an enum; reverse From not generated — left at default
        __target.max_tokens = max_tokens;
        __target.presence_penalty = presence_penalty;
        __target.frequency_penalty = frequency_penalty;
        if let Ok(v) = ::serde_json::from_str::<::serde_json::Value>(&logit_bias) {
            if let Ok(t) = ::serde_json::from_value(v) {
                __target.logit_bias = t;
            }
        }
        if let Some(s) = user {
            // Try JSON parse first (handles enum/object values); on parse failure
            // treat the raw input as a JSON string scalar so plain `String` /
            // string-like enum fields don't end up empty for inputs that aren't
            // valid JSON tokens (e.g. `tts-1`).
            let __v = ::serde_json::from_str::<::serde_json::Value>(&s).unwrap_or(::serde_json::Value::String(s));
            if let Ok(t) = ::serde_json::from_value(__v) {
                __target.user = Some(t);
            }
        }
        if let Some(v) = tools {
            __target.tools = Some(v.into_iter().map(|w| w.0).collect());
        }
        // alef: tool_choice (ToolChoice) is an enum; reverse From not generated — left at default
        __target.parallel_tool_calls = parallel_tool_calls;
        // alef: response_format (ResponseFormat) is an enum; reverse From not generated — left at default
        if let Some(w) = stream_options {
            __target.stream_options = Some(w.0);
        }
        __target.seed = seed;
        // alef: reasoning_effort (ReasoningEffort) is an enum; reverse From not generated — left at default
        // alef: modalities (Vec<>) contains enum elements — left at default
        if let Some(s) = extra_body {
            // Try JSON parse first (handles enum/object values); on parse failure
            // treat the raw input as a JSON string scalar so plain `String` /
            // string-like enum fields don't end up empty for inputs that aren't
            // valid JSON tokens (e.g. `tts-1`).
            let __v = ::serde_json::from_str::<::serde_json::Value>(&s).unwrap_or(::serde_json::Value::String(s));
            if let Ok(t) = ::serde_json::from_value(__v) {
                __target.extra_body = Some(t);
            }
        }
        ChatCompletionRequest(__target)
    }
    pub fn model(&self) -> String {
        self.0.model.clone()
    }
    pub fn messages(&self) -> Vec<String> {
        self.0
            .messages
            .iter()
            .map(|elem| serde_json::to_string(elem).unwrap_or_else(|_| "null".to_string()))
            .collect()
    }
    pub fn temperature(&self) -> Option<f64> {
        self.0.temperature.as_ref().and_then(|v| {
            ::serde_json::to_value(v)
                .ok()
                .and_then(|j| ::serde_json::from_value(j).ok())
        })
    }
    pub fn top_p(&self) -> Option<f64> {
        self.0.top_p.as_ref().and_then(|v| {
            ::serde_json::to_value(v)
                .ok()
                .and_then(|j| ::serde_json::from_value(j).ok())
        })
    }
    pub fn n(&self) -> Option<u32> {
        self.0.n.as_ref().and_then(|v| {
            ::serde_json::to_value(v)
                .ok()
                .and_then(|j| ::serde_json::from_value(j).ok())
        })
    }
    pub fn stream(&self) -> Option<bool> {
        self.0.stream.as_ref().and_then(|v| {
            ::serde_json::to_value(v)
                .ok()
                .and_then(|j| ::serde_json::from_value(j).ok())
        })
    }
    pub fn stop(&self) -> Option<String> {
        self.0
            .stop
            .clone()
            .map(|w| serde_json::to_string(&w).unwrap_or_else(|_| "null".to_string()))
    }
    pub fn max_tokens(&self) -> Option<u64> {
        self.0.max_tokens.as_ref().and_then(|v| {
            ::serde_json::to_value(v)
                .ok()
                .and_then(|j| ::serde_json::from_value(j).ok())
        })
    }
    pub fn presence_penalty(&self) -> Option<f64> {
        self.0.presence_penalty.as_ref().and_then(|v| {
            ::serde_json::to_value(v)
                .ok()
                .and_then(|j| ::serde_json::from_value(j).ok())
        })
    }
    pub fn frequency_penalty(&self) -> Option<f64> {
        self.0.frequency_penalty.as_ref().and_then(|v| {
            ::serde_json::to_value(v)
                .ok()
                .and_then(|j| ::serde_json::from_value(j).ok())
        })
    }
    pub fn logit_bias(&self) -> String {
        serde_json::to_string(&self.0.logit_bias).expect("serializable logit_bias")
    }
    pub fn user(&self) -> Option<String> {
        self.0.user.clone()
    }
    pub fn tools(&self) -> String {
        serde_json::to_string(&self.0.tools.as_ref().map(|v| {
            v.iter()
                .map(|elem| serde_json::to_string(elem).unwrap_or_else(|_| "null".to_string()))
                .collect::<Vec<_>>()
        }))
        .expect("serializable enum vec")
    }
    pub fn tool_choice(&self) -> Option<String> {
        self.0
            .tool_choice
            .clone()
            .map(|w| serde_json::to_string(&w).unwrap_or_else(|_| "null".to_string()))
    }
    pub fn parallel_tool_calls(&self) -> Option<bool> {
        self.0.parallel_tool_calls.as_ref().and_then(|v| {
            ::serde_json::to_value(v)
                .ok()
                .and_then(|j| ::serde_json::from_value(j).ok())
        })
    }
    pub fn response_format(&self) -> Option<String> {
        self.0
            .response_format
            .clone()
            .map(|w| serde_json::to_string(&w).unwrap_or_else(|_| "null".to_string()))
    }
    pub fn stream_options(&self) -> Option<StreamOptions> {
        self.0.stream_options.clone().map(StreamOptions)
    }
    pub fn seed(&self) -> Option<i64> {
        self.0.seed.as_ref().and_then(|v| {
            ::serde_json::to_value(v)
                .ok()
                .and_then(|j| ::serde_json::from_value(j).ok())
        })
    }
    pub fn reasoning_effort(&self) -> Option<String> {
        self.0
            .reasoning_effort
            .clone()
            .map(|w| ReasoningEffort::from(w).to_string())
    }
    pub fn modalities(&self) -> String {
        serde_json::to_string(&self.0.modalities.as_ref().map(|v| {
            v.iter()
                .map(|elem| Modality::from(elem.clone()).to_string())
                .collect::<Vec<_>>()
        }))
        .expect("serializable enum vec")
    }
    pub fn extra_body(&self) -> Option<String> {
        self.0.extra_body.as_ref().and_then(|v| serde_json::to_string(v).ok())
    }
}

pub struct StreamOptions(pub liter_llm::types::StreamOptions);
impl StreamOptions {
    pub fn new(include_usage: Option<bool>) -> StreamOptions {
        StreamOptions(liter_llm::types::StreamOptions { include_usage })
    }
    pub fn include_usage(&self) -> Option<bool> {
        self.0.include_usage.as_ref().and_then(|v| {
            ::serde_json::to_value(v)
                .ok()
                .and_then(|j| ::serde_json::from_value(j).ok())
        })
    }
}

pub struct ChatCompletionResponse(pub liter_llm::types::ChatCompletionResponse);
impl ChatCompletionResponse {
    pub fn new(
        id: String,
        object: String,
        created: u64,
        model: String,
        choices: Vec<Choice>,
        usage: Option<Usage>,
        system_fingerprint: Option<String>,
        service_tier: Option<String>,
    ) -> ChatCompletionResponse {
        let mut __target: liter_llm::types::ChatCompletionResponse = ::std::default::Default::default();
        {
            // Try JSON parse first (handles enum/object values); on parse failure
            // treat the raw input as a JSON string scalar so plain `String` /
            // string-like enum fields don't end up empty for inputs that aren't
            // valid JSON tokens (e.g. `tts-1`).
            let __v =
                ::serde_json::from_str::<::serde_json::Value>(&id).unwrap_or(::serde_json::Value::String(id.clone()));
            if let Ok(t) = ::serde_json::from_value(__v) {
                __target.id = t;
            }
        }
        {
            // Try JSON parse first (handles enum/object values); on parse failure
            // treat the raw input as a JSON string scalar so plain `String` /
            // string-like enum fields don't end up empty for inputs that aren't
            // valid JSON tokens (e.g. `tts-1`).
            let __v = ::serde_json::from_str::<::serde_json::Value>(&object)
                .unwrap_or(::serde_json::Value::String(object.clone()));
            if let Ok(t) = ::serde_json::from_value(__v) {
                __target.object = t;
            }
        }
        __target.created = created;
        {
            // Try JSON parse first (handles enum/object values); on parse failure
            // treat the raw input as a JSON string scalar so plain `String` /
            // string-like enum fields don't end up empty for inputs that aren't
            // valid JSON tokens (e.g. `tts-1`).
            let __v = ::serde_json::from_str::<::serde_json::Value>(&model)
                .unwrap_or(::serde_json::Value::String(model.clone()));
            if let Ok(t) = ::serde_json::from_value(__v) {
                __target.model = t;
            }
        }
        __target.choices = choices.into_iter().map(|w| w.0).collect();
        if let Some(w) = usage {
            __target.usage = Some(w.0);
        }
        if let Some(s) = system_fingerprint {
            // Try JSON parse first (handles enum/object values); on parse failure
            // treat the raw input as a JSON string scalar so plain `String` /
            // string-like enum fields don't end up empty for inputs that aren't
            // valid JSON tokens (e.g. `tts-1`).
            let __v = ::serde_json::from_str::<::serde_json::Value>(&s).unwrap_or(::serde_json::Value::String(s));
            if let Ok(t) = ::serde_json::from_value(__v) {
                __target.system_fingerprint = Some(t);
            }
        }
        if let Some(s) = service_tier {
            // Try JSON parse first (handles enum/object values); on parse failure
            // treat the raw input as a JSON string scalar so plain `String` /
            // string-like enum fields don't end up empty for inputs that aren't
            // valid JSON tokens (e.g. `tts-1`).
            let __v = ::serde_json::from_str::<::serde_json::Value>(&s).unwrap_or(::serde_json::Value::String(s));
            if let Ok(t) = ::serde_json::from_value(__v) {
                __target.service_tier = Some(t);
            }
        }
        ChatCompletionResponse(__target)
    }
    pub fn id(&self) -> String {
        self.0.id.clone()
    }
    pub fn object(&self) -> String {
        self.0.object.clone()
    }
    pub fn created(&self) -> u64 {
        ::serde_json::to_value(&self.0.created)
            .ok()
            .and_then(|j| ::serde_json::from_value(j).ok())
            .unwrap_or_default()
    }
    pub fn model(&self) -> String {
        self.0.model.clone()
    }
    pub fn choices(&self) -> Vec<String> {
        self.0
            .choices
            .iter()
            .map(|elem| serde_json::to_string(elem).unwrap_or_else(|_| "null".to_string()))
            .collect()
    }
    pub fn usage(&self) -> Option<Usage> {
        self.0.usage.clone().map(Usage)
    }
    pub fn system_fingerprint(&self) -> Option<String> {
        self.0.system_fingerprint.clone()
    }
    pub fn service_tier(&self) -> Option<String> {
        self.0.service_tier.clone()
    }
}

pub struct Choice(pub liter_llm::types::Choice);
impl Choice {
    pub fn new(index: u32, message: AssistantMessage, finish_reason: Option<FinishReason>) -> Choice {
        let mut __target: liter_llm::types::Choice = ::std::default::Default::default();
        __target.index = index;
        __target.message = message.0;
        // alef: finish_reason (FinishReason) is an enum; reverse From not generated — left at default
        Choice(__target)
    }
    pub fn index(&self) -> u32 {
        ::serde_json::to_value(&self.0.index)
            .ok()
            .and_then(|j| ::serde_json::from_value(j).ok())
            .unwrap_or_default()
    }
    pub fn message(&self) -> AssistantMessage {
        AssistantMessage(self.0.message.clone())
    }
    pub fn finish_reason(&self) -> Option<String> {
        self.0.finish_reason.clone().map(|w| FinishReason::from(w).to_string())
    }
}

pub struct ChatCompletionChunk(pub liter_llm::types::ChatCompletionChunk);
impl ChatCompletionChunk {
    pub fn new(
        id: String,
        object: String,
        created: u64,
        model: String,
        choices: Vec<StreamChoice>,
        usage: Option<Usage>,
        system_fingerprint: Option<String>,
        service_tier: Option<String>,
    ) -> ChatCompletionChunk {
        let mut __target: liter_llm::types::ChatCompletionChunk = ::std::default::Default::default();
        {
            // Try JSON parse first (handles enum/object values); on parse failure
            // treat the raw input as a JSON string scalar so plain `String` /
            // string-like enum fields don't end up empty for inputs that aren't
            // valid JSON tokens (e.g. `tts-1`).
            let __v =
                ::serde_json::from_str::<::serde_json::Value>(&id).unwrap_or(::serde_json::Value::String(id.clone()));
            if let Ok(t) = ::serde_json::from_value(__v) {
                __target.id = t;
            }
        }
        {
            // Try JSON parse first (handles enum/object values); on parse failure
            // treat the raw input as a JSON string scalar so plain `String` /
            // string-like enum fields don't end up empty for inputs that aren't
            // valid JSON tokens (e.g. `tts-1`).
            let __v = ::serde_json::from_str::<::serde_json::Value>(&object)
                .unwrap_or(::serde_json::Value::String(object.clone()));
            if let Ok(t) = ::serde_json::from_value(__v) {
                __target.object = t;
            }
        }
        __target.created = created;
        {
            // Try JSON parse first (handles enum/object values); on parse failure
            // treat the raw input as a JSON string scalar so plain `String` /
            // string-like enum fields don't end up empty for inputs that aren't
            // valid JSON tokens (e.g. `tts-1`).
            let __v = ::serde_json::from_str::<::serde_json::Value>(&model)
                .unwrap_or(::serde_json::Value::String(model.clone()));
            if let Ok(t) = ::serde_json::from_value(__v) {
                __target.model = t;
            }
        }
        __target.choices = choices.into_iter().map(|w| w.0).collect();
        if let Some(w) = usage {
            __target.usage = Some(w.0);
        }
        if let Some(s) = system_fingerprint {
            // Try JSON parse first (handles enum/object values); on parse failure
            // treat the raw input as a JSON string scalar so plain `String` /
            // string-like enum fields don't end up empty for inputs that aren't
            // valid JSON tokens (e.g. `tts-1`).
            let __v = ::serde_json::from_str::<::serde_json::Value>(&s).unwrap_or(::serde_json::Value::String(s));
            if let Ok(t) = ::serde_json::from_value(__v) {
                __target.system_fingerprint = Some(t);
            }
        }
        if let Some(s) = service_tier {
            // Try JSON parse first (handles enum/object values); on parse failure
            // treat the raw input as a JSON string scalar so plain `String` /
            // string-like enum fields don't end up empty for inputs that aren't
            // valid JSON tokens (e.g. `tts-1`).
            let __v = ::serde_json::from_str::<::serde_json::Value>(&s).unwrap_or(::serde_json::Value::String(s));
            if let Ok(t) = ::serde_json::from_value(__v) {
                __target.service_tier = Some(t);
            }
        }
        ChatCompletionChunk(__target)
    }
    pub fn id(&self) -> String {
        self.0.id.clone()
    }
    pub fn object(&self) -> String {
        self.0.object.clone()
    }
    pub fn created(&self) -> u64 {
        ::serde_json::to_value(&self.0.created)
            .ok()
            .and_then(|j| ::serde_json::from_value(j).ok())
            .unwrap_or_default()
    }
    pub fn model(&self) -> String {
        self.0.model.clone()
    }
    pub fn choices(&self) -> Vec<String> {
        self.0
            .choices
            .iter()
            .map(|elem| serde_json::to_string(elem).unwrap_or_else(|_| "null".to_string()))
            .collect()
    }
    pub fn usage(&self) -> Option<Usage> {
        self.0.usage.clone().map(Usage)
    }
    pub fn system_fingerprint(&self) -> Option<String> {
        self.0.system_fingerprint.clone()
    }
    pub fn service_tier(&self) -> Option<String> {
        self.0.service_tier.clone()
    }
}

pub struct StreamChoice(pub liter_llm::types::StreamChoice);
impl StreamChoice {
    pub fn new(index: u32, delta: StreamDelta, finish_reason: Option<FinishReason>) -> StreamChoice {
        let mut __target: liter_llm::types::StreamChoice = ::std::default::Default::default();
        __target.index = index;
        __target.delta = delta.0;
        // alef: finish_reason (FinishReason) is an enum; reverse From not generated — left at default
        StreamChoice(__target)
    }
    pub fn index(&self) -> u32 {
        ::serde_json::to_value(&self.0.index)
            .ok()
            .and_then(|j| ::serde_json::from_value(j).ok())
            .unwrap_or_default()
    }
    pub fn delta(&self) -> StreamDelta {
        StreamDelta(self.0.delta.clone())
    }
    pub fn finish_reason(&self) -> Option<String> {
        self.0.finish_reason.clone().map(|w| FinishReason::from(w).to_string())
    }
}

pub struct StreamDelta(pub liter_llm::types::StreamDelta);
impl StreamDelta {
    pub fn new(
        role: Option<String>,
        content: Option<String>,
        tool_calls: Option<Vec<StreamToolCall>>,
        function_call: Option<StreamFunctionCall>,
        refusal: Option<String>,
    ) -> StreamDelta {
        let mut __target: liter_llm::types::StreamDelta = ::std::default::Default::default();
        if let Some(s) = role {
            // Try JSON parse first (handles enum/object values); on parse failure
            // treat the raw input as a JSON string scalar so plain `String` /
            // string-like enum fields don't end up empty for inputs that aren't
            // valid JSON tokens (e.g. `tts-1`).
            let __v = ::serde_json::from_str::<::serde_json::Value>(&s).unwrap_or(::serde_json::Value::String(s));
            if let Ok(t) = ::serde_json::from_value(__v) {
                __target.role = Some(t);
            }
        }
        if let Some(s) = content {
            // Try JSON parse first (handles enum/object values); on parse failure
            // treat the raw input as a JSON string scalar so plain `String` /
            // string-like enum fields don't end up empty for inputs that aren't
            // valid JSON tokens (e.g. `tts-1`).
            let __v = ::serde_json::from_str::<::serde_json::Value>(&s).unwrap_or(::serde_json::Value::String(s));
            if let Ok(t) = ::serde_json::from_value(__v) {
                __target.content = Some(t);
            }
        }
        if let Some(v) = tool_calls {
            __target.tool_calls = Some(v.into_iter().map(|w| w.0).collect());
        }
        if let Some(w) = function_call {
            __target.function_call = Some(w.0);
        }
        if let Some(s) = refusal {
            // Try JSON parse first (handles enum/object values); on parse failure
            // treat the raw input as a JSON string scalar so plain `String` /
            // string-like enum fields don't end up empty for inputs that aren't
            // valid JSON tokens (e.g. `tts-1`).
            let __v = ::serde_json::from_str::<::serde_json::Value>(&s).unwrap_or(::serde_json::Value::String(s));
            if let Ok(t) = ::serde_json::from_value(__v) {
                __target.refusal = Some(t);
            }
        }
        StreamDelta(__target)
    }
    pub fn role(&self) -> Option<String> {
        self.0.role.clone()
    }
    pub fn content(&self) -> Option<String> {
        self.0.content.clone()
    }
    pub fn tool_calls(&self) -> String {
        serde_json::to_string(&self.0.tool_calls.as_ref().map(|v| {
            v.iter()
                .map(|elem| serde_json::to_string(elem).unwrap_or_else(|_| "null".to_string()))
                .collect::<Vec<_>>()
        }))
        .expect("serializable enum vec")
    }
    pub fn function_call(&self) -> Option<StreamFunctionCall> {
        self.0.function_call.clone().map(StreamFunctionCall)
    }
    pub fn refusal(&self) -> Option<String> {
        self.0.refusal.clone()
    }
}

pub struct StreamToolCall(pub liter_llm::types::StreamToolCall);
impl StreamToolCall {
    pub fn new(
        index: u32,
        id: Option<String>,
        call_type: Option<ToolType>,
        function: Option<StreamFunctionCall>,
    ) -> StreamToolCall {
        let mut __target: liter_llm::types::StreamToolCall = ::std::default::Default::default();
        __target.index = index;
        if let Some(s) = id {
            // Try JSON parse first (handles enum/object values); on parse failure
            // treat the raw input as a JSON string scalar so plain `String` /
            // string-like enum fields don't end up empty for inputs that aren't
            // valid JSON tokens (e.g. `tts-1`).
            let __v = ::serde_json::from_str::<::serde_json::Value>(&s).unwrap_or(::serde_json::Value::String(s));
            if let Ok(t) = ::serde_json::from_value(__v) {
                __target.id = Some(t);
            }
        }
        // alef: call_type (ToolType) is an enum; reverse From not generated — left at default
        if let Some(w) = function {
            __target.function = Some(w.0);
        }
        StreamToolCall(__target)
    }
    pub fn index(&self) -> u32 {
        ::serde_json::to_value(&self.0.index)
            .ok()
            .and_then(|j| ::serde_json::from_value(j).ok())
            .unwrap_or_default()
    }
    pub fn id(&self) -> Option<String> {
        self.0.id.clone()
    }
    pub fn call_type(&self) -> Option<String> {
        self.0.call_type.clone().map(|w| ToolType::from(w).to_string())
    }
    pub fn function(&self) -> Option<StreamFunctionCall> {
        self.0.function.clone().map(StreamFunctionCall)
    }
}

pub struct StreamFunctionCall(pub liter_llm::types::StreamFunctionCall);
impl StreamFunctionCall {
    pub fn new(name: Option<String>, arguments: Option<String>) -> StreamFunctionCall {
        let mut __target: liter_llm::types::StreamFunctionCall = ::std::default::Default::default();
        if let Some(s) = name {
            // Try JSON parse first (handles enum/object values); on parse failure
            // treat the raw input as a JSON string scalar so plain `String` /
            // string-like enum fields don't end up empty for inputs that aren't
            // valid JSON tokens (e.g. `tts-1`).
            let __v = ::serde_json::from_str::<::serde_json::Value>(&s).unwrap_or(::serde_json::Value::String(s));
            if let Ok(t) = ::serde_json::from_value(__v) {
                __target.name = Some(t);
            }
        }
        if let Some(s) = arguments {
            // Try JSON parse first (handles enum/object values); on parse failure
            // treat the raw input as a JSON string scalar so plain `String` /
            // string-like enum fields don't end up empty for inputs that aren't
            // valid JSON tokens (e.g. `tts-1`).
            let __v = ::serde_json::from_str::<::serde_json::Value>(&s).unwrap_or(::serde_json::Value::String(s));
            if let Ok(t) = ::serde_json::from_value(__v) {
                __target.arguments = Some(t);
            }
        }
        StreamFunctionCall(__target)
    }
    pub fn name(&self) -> Option<String> {
        self.0.name.clone()
    }
    pub fn arguments(&self) -> Option<String> {
        self.0.arguments.clone()
    }
}

pub struct EmbeddingRequest(pub liter_llm::types::EmbeddingRequest);
impl EmbeddingRequest {
    pub fn new(
        model: String,
        input: EmbeddingInput,
        encoding_format: Option<EmbeddingFormat>,
        dimensions: Option<u32>,
        user: Option<String>,
    ) -> EmbeddingRequest {
        let mut __target: liter_llm::types::EmbeddingRequest = ::std::default::Default::default();
        {
            // Try JSON parse first (handles enum/object values); on parse failure
            // treat the raw input as a JSON string scalar so plain `String` /
            // string-like enum fields don't end up empty for inputs that aren't
            // valid JSON tokens (e.g. `tts-1`).
            let __v = ::serde_json::from_str::<::serde_json::Value>(&model)
                .unwrap_or(::serde_json::Value::String(model.clone()));
            if let Ok(t) = ::serde_json::from_value(__v) {
                __target.model = t;
            }
        }
        // alef: input (EmbeddingInput) is an enum; reverse From not generated — left at default
        // alef: encoding_format (EmbeddingFormat) is an enum; reverse From not generated — left at default
        __target.dimensions = dimensions;
        if let Some(s) = user {
            // Try JSON parse first (handles enum/object values); on parse failure
            // treat the raw input as a JSON string scalar so plain `String` /
            // string-like enum fields don't end up empty for inputs that aren't
            // valid JSON tokens (e.g. `tts-1`).
            let __v = ::serde_json::from_str::<::serde_json::Value>(&s).unwrap_or(::serde_json::Value::String(s));
            if let Ok(t) = ::serde_json::from_value(__v) {
                __target.user = Some(t);
            }
        }
        EmbeddingRequest(__target)
    }
    pub fn model(&self) -> String {
        self.0.model.clone()
    }
    pub fn input(&self) -> String {
        serde_json::to_string(&self.0.input).unwrap_or_else(|_| "null".to_string())
    }
    pub fn encoding_format(&self) -> Option<String> {
        self.0
            .encoding_format
            .clone()
            .map(|w| EmbeddingFormat::from(w).to_string())
    }
    pub fn dimensions(&self) -> Option<u32> {
        self.0.dimensions.as_ref().and_then(|v| {
            ::serde_json::to_value(v)
                .ok()
                .and_then(|j| ::serde_json::from_value(j).ok())
        })
    }
    pub fn user(&self) -> Option<String> {
        self.0.user.clone()
    }
}

pub struct EmbeddingResponse(pub liter_llm::types::EmbeddingResponse);
impl EmbeddingResponse {
    pub fn object(&self) -> String {
        self.0.object.clone()
    }
    pub fn data(&self) -> Vec<String> {
        self.0
            .data
            .iter()
            .map(|elem| serde_json::to_string(elem).unwrap_or_else(|_| "null".to_string()))
            .collect()
    }
    pub fn model(&self) -> String {
        self.0.model.clone()
    }
    pub fn usage(&self) -> Option<Usage> {
        self.0.usage.clone().map(Usage)
    }
}

pub struct EmbeddingObject(pub liter_llm::types::EmbeddingObject);
impl EmbeddingObject {
    pub fn object(&self) -> String {
        self.0.object.clone()
    }
    pub fn embedding(&self) -> Vec<f64> {
        ::serde_json::to_value(&self.0.embedding)
            .ok()
            .and_then(|j| ::serde_json::from_value(j).ok())
            .unwrap_or_default()
    }
    pub fn index(&self) -> u32 {
        ::serde_json::to_value(&self.0.index)
            .ok()
            .and_then(|j| ::serde_json::from_value(j).ok())
            .unwrap_or_default()
    }
}

pub struct CreateImageRequest(pub liter_llm::types::CreateImageRequest);
impl CreateImageRequest {
    pub fn new(
        prompt: String,
        model: Option<String>,
        n: Option<u32>,
        size: Option<String>,
        quality: Option<String>,
        style: Option<String>,
        response_format: Option<String>,
        user: Option<String>,
    ) -> CreateImageRequest {
        let mut __target: liter_llm::types::CreateImageRequest = ::std::default::Default::default();
        {
            // Try JSON parse first (handles enum/object values); on parse failure
            // treat the raw input as a JSON string scalar so plain `String` /
            // string-like enum fields don't end up empty for inputs that aren't
            // valid JSON tokens (e.g. `tts-1`).
            let __v = ::serde_json::from_str::<::serde_json::Value>(&prompt)
                .unwrap_or(::serde_json::Value::String(prompt.clone()));
            if let Ok(t) = ::serde_json::from_value(__v) {
                __target.prompt = t;
            }
        }
        if let Some(s) = model {
            // Try JSON parse first (handles enum/object values); on parse failure
            // treat the raw input as a JSON string scalar so plain `String` /
            // string-like enum fields don't end up empty for inputs that aren't
            // valid JSON tokens (e.g. `tts-1`).
            let __v = ::serde_json::from_str::<::serde_json::Value>(&s).unwrap_or(::serde_json::Value::String(s));
            if let Ok(t) = ::serde_json::from_value(__v) {
                __target.model = Some(t);
            }
        }
        __target.n = n;
        if let Some(s) = size {
            // Try JSON parse first (handles enum/object values); on parse failure
            // treat the raw input as a JSON string scalar so plain `String` /
            // string-like enum fields don't end up empty for inputs that aren't
            // valid JSON tokens (e.g. `tts-1`).
            let __v = ::serde_json::from_str::<::serde_json::Value>(&s).unwrap_or(::serde_json::Value::String(s));
            if let Ok(t) = ::serde_json::from_value(__v) {
                __target.size = Some(t);
            }
        }
        if let Some(s) = quality {
            // Try JSON parse first (handles enum/object values); on parse failure
            // treat the raw input as a JSON string scalar so plain `String` /
            // string-like enum fields don't end up empty for inputs that aren't
            // valid JSON tokens (e.g. `tts-1`).
            let __v = ::serde_json::from_str::<::serde_json::Value>(&s).unwrap_or(::serde_json::Value::String(s));
            if let Ok(t) = ::serde_json::from_value(__v) {
                __target.quality = Some(t);
            }
        }
        if let Some(s) = style {
            // Try JSON parse first (handles enum/object values); on parse failure
            // treat the raw input as a JSON string scalar so plain `String` /
            // string-like enum fields don't end up empty for inputs that aren't
            // valid JSON tokens (e.g. `tts-1`).
            let __v = ::serde_json::from_str::<::serde_json::Value>(&s).unwrap_or(::serde_json::Value::String(s));
            if let Ok(t) = ::serde_json::from_value(__v) {
                __target.style = Some(t);
            }
        }
        if let Some(s) = response_format {
            // Try JSON parse first (handles enum/object values); on parse failure
            // treat the raw input as a JSON string scalar so plain `String` /
            // string-like enum fields don't end up empty for inputs that aren't
            // valid JSON tokens (e.g. `tts-1`).
            let __v = ::serde_json::from_str::<::serde_json::Value>(&s).unwrap_or(::serde_json::Value::String(s));
            if let Ok(t) = ::serde_json::from_value(__v) {
                __target.response_format = Some(t);
            }
        }
        if let Some(s) = user {
            // Try JSON parse first (handles enum/object values); on parse failure
            // treat the raw input as a JSON string scalar so plain `String` /
            // string-like enum fields don't end up empty for inputs that aren't
            // valid JSON tokens (e.g. `tts-1`).
            let __v = ::serde_json::from_str::<::serde_json::Value>(&s).unwrap_or(::serde_json::Value::String(s));
            if let Ok(t) = ::serde_json::from_value(__v) {
                __target.user = Some(t);
            }
        }
        CreateImageRequest(__target)
    }
    pub fn prompt(&self) -> String {
        self.0.prompt.clone()
    }
    pub fn model(&self) -> Option<String> {
        self.0.model.clone()
    }
    pub fn n(&self) -> Option<u32> {
        self.0.n.as_ref().and_then(|v| {
            ::serde_json::to_value(v)
                .ok()
                .and_then(|j| ::serde_json::from_value(j).ok())
        })
    }
    pub fn size(&self) -> Option<String> {
        self.0.size.clone()
    }
    pub fn quality(&self) -> Option<String> {
        self.0.quality.clone()
    }
    pub fn style(&self) -> Option<String> {
        self.0.style.clone()
    }
    pub fn response_format(&self) -> Option<String> {
        self.0.response_format.clone()
    }
    pub fn user(&self) -> Option<String> {
        self.0.user.clone()
    }
}

pub struct ImagesResponse(pub liter_llm::types::ImagesResponse);
impl ImagesResponse {
    pub fn new(created: u64, data: Vec<Image>) -> ImagesResponse {
        let mut __target: liter_llm::types::ImagesResponse = ::std::default::Default::default();
        __target.created = created;
        __target.data = data.into_iter().map(|w| w.0).collect();
        ImagesResponse(__target)
    }
    pub fn created(&self) -> u64 {
        ::serde_json::to_value(&self.0.created)
            .ok()
            .and_then(|j| ::serde_json::from_value(j).ok())
            .unwrap_or_default()
    }
    pub fn data(&self) -> Vec<String> {
        self.0
            .data
            .iter()
            .map(|elem| serde_json::to_string(elem).unwrap_or_else(|_| "null".to_string()))
            .collect()
    }
}

pub struct Image(pub liter_llm::types::Image);
impl Image {
    pub fn new(url: Option<String>, b64_json: Option<String>, revised_prompt: Option<String>) -> Image {
        let mut __target: liter_llm::types::Image = ::std::default::Default::default();
        if let Some(s) = url {
            // Try JSON parse first (handles enum/object values); on parse failure
            // treat the raw input as a JSON string scalar so plain `String` /
            // string-like enum fields don't end up empty for inputs that aren't
            // valid JSON tokens (e.g. `tts-1`).
            let __v = ::serde_json::from_str::<::serde_json::Value>(&s).unwrap_or(::serde_json::Value::String(s));
            if let Ok(t) = ::serde_json::from_value(__v) {
                __target.url = Some(t);
            }
        }
        if let Some(s) = b64_json {
            // Try JSON parse first (handles enum/object values); on parse failure
            // treat the raw input as a JSON string scalar so plain `String` /
            // string-like enum fields don't end up empty for inputs that aren't
            // valid JSON tokens (e.g. `tts-1`).
            let __v = ::serde_json::from_str::<::serde_json::Value>(&s).unwrap_or(::serde_json::Value::String(s));
            if let Ok(t) = ::serde_json::from_value(__v) {
                __target.b64_json = Some(t);
            }
        }
        if let Some(s) = revised_prompt {
            // Try JSON parse first (handles enum/object values); on parse failure
            // treat the raw input as a JSON string scalar so plain `String` /
            // string-like enum fields don't end up empty for inputs that aren't
            // valid JSON tokens (e.g. `tts-1`).
            let __v = ::serde_json::from_str::<::serde_json::Value>(&s).unwrap_or(::serde_json::Value::String(s));
            if let Ok(t) = ::serde_json::from_value(__v) {
                __target.revised_prompt = Some(t);
            }
        }
        Image(__target)
    }
    pub fn url(&self) -> Option<String> {
        self.0.url.clone()
    }
    pub fn b64_json(&self) -> Option<String> {
        self.0.b64_json.clone()
    }
    pub fn revised_prompt(&self) -> Option<String> {
        self.0.revised_prompt.clone()
    }
}

pub struct DecodedDataUrl(pub liter_llm::image::DecodedDataUrl);
impl DecodedDataUrl {
    pub fn new(mime: String, data: Vec<u8>) -> DecodedDataUrl {
        let mut __target: liter_llm::image::DecodedDataUrl = ::std::default::Default::default();
        {
            // Try JSON parse first (handles enum/object values); on parse failure
            // treat the raw input as a JSON string scalar so plain `String` /
            // string-like enum fields don't end up empty for inputs that aren't
            // valid JSON tokens (e.g. `tts-1`).
            let __v = ::serde_json::from_str::<::serde_json::Value>(&mime)
                .unwrap_or(::serde_json::Value::String(mime.clone()));
            if let Ok(t) = ::serde_json::from_value(__v) {
                __target.mime = t;
            }
        }
        __target.data = data.into();
        DecodedDataUrl(__target)
    }
    pub fn mime(&self) -> String {
        self.0.mime.clone()
    }
    pub fn data(&self) -> Vec<u8> {
        self.0.data.to_vec()
    }
}

pub struct CreateSpeechRequest(pub liter_llm::types::CreateSpeechRequest);
impl CreateSpeechRequest {
    pub fn new(
        model: String,
        input: String,
        voice: String,
        response_format: Option<String>,
        speed: Option<f64>,
    ) -> CreateSpeechRequest {
        let mut __target: liter_llm::types::CreateSpeechRequest = ::std::default::Default::default();
        {
            // Try JSON parse first (handles enum/object values); on parse failure
            // treat the raw input as a JSON string scalar so plain `String` /
            // string-like enum fields don't end up empty for inputs that aren't
            // valid JSON tokens (e.g. `tts-1`).
            let __v = ::serde_json::from_str::<::serde_json::Value>(&model)
                .unwrap_or(::serde_json::Value::String(model.clone()));
            if let Ok(t) = ::serde_json::from_value(__v) {
                __target.model = t;
            }
        }
        {
            // Try JSON parse first (handles enum/object values); on parse failure
            // treat the raw input as a JSON string scalar so plain `String` /
            // string-like enum fields don't end up empty for inputs that aren't
            // valid JSON tokens (e.g. `tts-1`).
            let __v = ::serde_json::from_str::<::serde_json::Value>(&input)
                .unwrap_or(::serde_json::Value::String(input.clone()));
            if let Ok(t) = ::serde_json::from_value(__v) {
                __target.input = t;
            }
        }
        {
            // Try JSON parse first (handles enum/object values); on parse failure
            // treat the raw input as a JSON string scalar so plain `String` /
            // string-like enum fields don't end up empty for inputs that aren't
            // valid JSON tokens (e.g. `tts-1`).
            let __v = ::serde_json::from_str::<::serde_json::Value>(&voice)
                .unwrap_or(::serde_json::Value::String(voice.clone()));
            if let Ok(t) = ::serde_json::from_value(__v) {
                __target.voice = t;
            }
        }
        if let Some(s) = response_format {
            // Try JSON parse first (handles enum/object values); on parse failure
            // treat the raw input as a JSON string scalar so plain `String` /
            // string-like enum fields don't end up empty for inputs that aren't
            // valid JSON tokens (e.g. `tts-1`).
            let __v = ::serde_json::from_str::<::serde_json::Value>(&s).unwrap_or(::serde_json::Value::String(s));
            if let Ok(t) = ::serde_json::from_value(__v) {
                __target.response_format = Some(t);
            }
        }
        __target.speed = speed;
        CreateSpeechRequest(__target)
    }
    pub fn model(&self) -> String {
        self.0.model.clone()
    }
    pub fn input(&self) -> String {
        self.0.input.clone()
    }
    pub fn voice(&self) -> String {
        self.0.voice.clone()
    }
    pub fn response_format(&self) -> Option<String> {
        self.0.response_format.clone()
    }
    pub fn speed(&self) -> Option<f64> {
        self.0.speed.as_ref().and_then(|v| {
            ::serde_json::to_value(v)
                .ok()
                .and_then(|j| ::serde_json::from_value(j).ok())
        })
    }
}

pub struct CreateTranscriptionRequest(pub liter_llm::types::CreateTranscriptionRequest);
impl CreateTranscriptionRequest {
    pub fn new(
        model: String,
        file: String,
        language: Option<String>,
        prompt: Option<String>,
        response_format: Option<String>,
        temperature: Option<f64>,
    ) -> CreateTranscriptionRequest {
        let mut __target: liter_llm::types::CreateTranscriptionRequest = ::std::default::Default::default();
        {
            // Try JSON parse first (handles enum/object values); on parse failure
            // treat the raw input as a JSON string scalar so plain `String` /
            // string-like enum fields don't end up empty for inputs that aren't
            // valid JSON tokens (e.g. `tts-1`).
            let __v = ::serde_json::from_str::<::serde_json::Value>(&model)
                .unwrap_or(::serde_json::Value::String(model.clone()));
            if let Ok(t) = ::serde_json::from_value(__v) {
                __target.model = t;
            }
        }
        {
            // Try JSON parse first (handles enum/object values); on parse failure
            // treat the raw input as a JSON string scalar so plain `String` /
            // string-like enum fields don't end up empty for inputs that aren't
            // valid JSON tokens (e.g. `tts-1`).
            let __v = ::serde_json::from_str::<::serde_json::Value>(&file)
                .unwrap_or(::serde_json::Value::String(file.clone()));
            if let Ok(t) = ::serde_json::from_value(__v) {
                __target.file = t;
            }
        }
        if let Some(s) = language {
            // Try JSON parse first (handles enum/object values); on parse failure
            // treat the raw input as a JSON string scalar so plain `String` /
            // string-like enum fields don't end up empty for inputs that aren't
            // valid JSON tokens (e.g. `tts-1`).
            let __v = ::serde_json::from_str::<::serde_json::Value>(&s).unwrap_or(::serde_json::Value::String(s));
            if let Ok(t) = ::serde_json::from_value(__v) {
                __target.language = Some(t);
            }
        }
        if let Some(s) = prompt {
            // Try JSON parse first (handles enum/object values); on parse failure
            // treat the raw input as a JSON string scalar so plain `String` /
            // string-like enum fields don't end up empty for inputs that aren't
            // valid JSON tokens (e.g. `tts-1`).
            let __v = ::serde_json::from_str::<::serde_json::Value>(&s).unwrap_or(::serde_json::Value::String(s));
            if let Ok(t) = ::serde_json::from_value(__v) {
                __target.prompt = Some(t);
            }
        }
        if let Some(s) = response_format {
            // Try JSON parse first (handles enum/object values); on parse failure
            // treat the raw input as a JSON string scalar so plain `String` /
            // string-like enum fields don't end up empty for inputs that aren't
            // valid JSON tokens (e.g. `tts-1`).
            let __v = ::serde_json::from_str::<::serde_json::Value>(&s).unwrap_or(::serde_json::Value::String(s));
            if let Ok(t) = ::serde_json::from_value(__v) {
                __target.response_format = Some(t);
            }
        }
        __target.temperature = temperature;
        CreateTranscriptionRequest(__target)
    }
    pub fn model(&self) -> String {
        self.0.model.clone()
    }
    pub fn file(&self) -> String {
        self.0.file.clone()
    }
    pub fn language(&self) -> Option<String> {
        self.0.language.clone()
    }
    pub fn prompt(&self) -> Option<String> {
        self.0.prompt.clone()
    }
    pub fn response_format(&self) -> Option<String> {
        self.0.response_format.clone()
    }
    pub fn temperature(&self) -> Option<f64> {
        self.0.temperature.as_ref().and_then(|v| {
            ::serde_json::to_value(v)
                .ok()
                .and_then(|j| ::serde_json::from_value(j).ok())
        })
    }
}

pub struct TranscriptionResponse(pub liter_llm::types::TranscriptionResponse);
impl TranscriptionResponse {
    pub fn new(
        text: String,
        language: Option<String>,
        duration: Option<f64>,
        segments: Option<Vec<TranscriptionSegment>>,
    ) -> TranscriptionResponse {
        let mut __target: liter_llm::types::TranscriptionResponse = ::std::default::Default::default();
        {
            // Try JSON parse first (handles enum/object values); on parse failure
            // treat the raw input as a JSON string scalar so plain `String` /
            // string-like enum fields don't end up empty for inputs that aren't
            // valid JSON tokens (e.g. `tts-1`).
            let __v = ::serde_json::from_str::<::serde_json::Value>(&text)
                .unwrap_or(::serde_json::Value::String(text.clone()));
            if let Ok(t) = ::serde_json::from_value(__v) {
                __target.text = t;
            }
        }
        if let Some(s) = language {
            // Try JSON parse first (handles enum/object values); on parse failure
            // treat the raw input as a JSON string scalar so plain `String` /
            // string-like enum fields don't end up empty for inputs that aren't
            // valid JSON tokens (e.g. `tts-1`).
            let __v = ::serde_json::from_str::<::serde_json::Value>(&s).unwrap_or(::serde_json::Value::String(s));
            if let Ok(t) = ::serde_json::from_value(__v) {
                __target.language = Some(t);
            }
        }
        __target.duration = duration;
        if let Some(v) = segments {
            __target.segments = Some(v.into_iter().map(|w| w.0).collect());
        }
        TranscriptionResponse(__target)
    }
    pub fn text(&self) -> String {
        self.0.text.clone()
    }
    pub fn language(&self) -> Option<String> {
        self.0.language.clone()
    }
    pub fn duration(&self) -> Option<f64> {
        self.0.duration.as_ref().and_then(|v| {
            ::serde_json::to_value(v)
                .ok()
                .and_then(|j| ::serde_json::from_value(j).ok())
        })
    }
    pub fn segments(&self) -> String {
        serde_json::to_string(&self.0.segments.as_ref().map(|v| {
            v.iter()
                .map(|elem| serde_json::to_string(elem).unwrap_or_else(|_| "null".to_string()))
                .collect::<Vec<_>>()
        }))
        .expect("serializable enum vec")
    }
}

pub struct TranscriptionSegment(pub liter_llm::types::TranscriptionSegment);
impl TranscriptionSegment {
    pub fn new(id: u32, start: f64, end: f64, text: String) -> TranscriptionSegment {
        let mut __target: liter_llm::types::TranscriptionSegment = ::std::default::Default::default();
        __target.id = id;
        __target.start = start;
        __target.end = end;
        {
            // Try JSON parse first (handles enum/object values); on parse failure
            // treat the raw input as a JSON string scalar so plain `String` /
            // string-like enum fields don't end up empty for inputs that aren't
            // valid JSON tokens (e.g. `tts-1`).
            let __v = ::serde_json::from_str::<::serde_json::Value>(&text)
                .unwrap_or(::serde_json::Value::String(text.clone()));
            if let Ok(t) = ::serde_json::from_value(__v) {
                __target.text = t;
            }
        }
        TranscriptionSegment(__target)
    }
    pub fn id(&self) -> u32 {
        ::serde_json::to_value(&self.0.id)
            .ok()
            .and_then(|j| ::serde_json::from_value(j).ok())
            .unwrap_or_default()
    }
    pub fn start(&self) -> f64 {
        ::serde_json::to_value(&self.0.start)
            .ok()
            .and_then(|j| ::serde_json::from_value(j).ok())
            .unwrap_or_default()
    }
    pub fn end(&self) -> f64 {
        ::serde_json::to_value(&self.0.end)
            .ok()
            .and_then(|j| ::serde_json::from_value(j).ok())
            .unwrap_or_default()
    }
    pub fn text(&self) -> String {
        self.0.text.clone()
    }
}

pub struct ModerationRequest(pub liter_llm::types::ModerationRequest);
impl ModerationRequest {
    pub fn new(input: ModerationInput, model: Option<String>) -> ModerationRequest {
        let mut __target: liter_llm::types::ModerationRequest = ::std::default::Default::default();
        // alef: input (ModerationInput) is an enum; reverse From not generated — left at default
        if let Some(s) = model {
            // Try JSON parse first (handles enum/object values); on parse failure
            // treat the raw input as a JSON string scalar so plain `String` /
            // string-like enum fields don't end up empty for inputs that aren't
            // valid JSON tokens (e.g. `tts-1`).
            let __v = ::serde_json::from_str::<::serde_json::Value>(&s).unwrap_or(::serde_json::Value::String(s));
            if let Ok(t) = ::serde_json::from_value(__v) {
                __target.model = Some(t);
            }
        }
        ModerationRequest(__target)
    }
    pub fn input(&self) -> String {
        serde_json::to_string(&self.0.input).unwrap_or_else(|_| "null".to_string())
    }
    pub fn model(&self) -> Option<String> {
        self.0.model.clone()
    }
}

pub struct ModerationResponse(pub liter_llm::types::ModerationResponse);
impl ModerationResponse {
    pub fn id(&self) -> String {
        self.0.id.clone()
    }
    pub fn model(&self) -> String {
        self.0.model.clone()
    }
    pub fn results(&self) -> Vec<String> {
        self.0
            .results
            .iter()
            .map(|elem| serde_json::to_string(elem).unwrap_or_else(|_| "null".to_string()))
            .collect()
    }
}

pub struct ModerationResult(pub liter_llm::types::ModerationResult);
impl ModerationResult {
    pub fn flagged(&self) -> bool {
        ::serde_json::to_value(&self.0.flagged)
            .ok()
            .and_then(|j| ::serde_json::from_value(j).ok())
            .unwrap_or_default()
    }
    pub fn categories(&self) -> ModerationCategories {
        ModerationCategories(self.0.categories.clone())
    }
    pub fn category_scores(&self) -> ModerationCategoryScores {
        ModerationCategoryScores(self.0.category_scores.clone())
    }
}

pub struct ModerationCategories(pub liter_llm::types::ModerationCategories);
impl ModerationCategories {
    pub fn new(
        sexual: bool,
        hate: bool,
        harassment: bool,
        self_harm: bool,
        sexual_minors: bool,
        hate_threatening: bool,
        violence_graphic: bool,
        self_harm_intent: bool,
        self_harm_instructions: bool,
        harassment_threatening: bool,
        violence: bool,
    ) -> ModerationCategories {
        ModerationCategories(liter_llm::types::ModerationCategories {
            sexual,
            hate,
            harassment,
            self_harm,
            sexual_minors,
            hate_threatening,
            violence_graphic,
            self_harm_intent,
            self_harm_instructions,
            harassment_threatening,
            violence,
        })
    }
    pub fn sexual(&self) -> bool {
        ::serde_json::to_value(&self.0.sexual)
            .ok()
            .and_then(|j| ::serde_json::from_value(j).ok())
            .unwrap_or_default()
    }
    pub fn hate(&self) -> bool {
        ::serde_json::to_value(&self.0.hate)
            .ok()
            .and_then(|j| ::serde_json::from_value(j).ok())
            .unwrap_or_default()
    }
    pub fn harassment(&self) -> bool {
        ::serde_json::to_value(&self.0.harassment)
            .ok()
            .and_then(|j| ::serde_json::from_value(j).ok())
            .unwrap_or_default()
    }
    pub fn self_harm(&self) -> bool {
        ::serde_json::to_value(&self.0.self_harm)
            .ok()
            .and_then(|j| ::serde_json::from_value(j).ok())
            .unwrap_or_default()
    }
    pub fn sexual_minors(&self) -> bool {
        ::serde_json::to_value(&self.0.sexual_minors)
            .ok()
            .and_then(|j| ::serde_json::from_value(j).ok())
            .unwrap_or_default()
    }
    pub fn hate_threatening(&self) -> bool {
        ::serde_json::to_value(&self.0.hate_threatening)
            .ok()
            .and_then(|j| ::serde_json::from_value(j).ok())
            .unwrap_or_default()
    }
    pub fn violence_graphic(&self) -> bool {
        ::serde_json::to_value(&self.0.violence_graphic)
            .ok()
            .and_then(|j| ::serde_json::from_value(j).ok())
            .unwrap_or_default()
    }
    pub fn self_harm_intent(&self) -> bool {
        ::serde_json::to_value(&self.0.self_harm_intent)
            .ok()
            .and_then(|j| ::serde_json::from_value(j).ok())
            .unwrap_or_default()
    }
    pub fn self_harm_instructions(&self) -> bool {
        ::serde_json::to_value(&self.0.self_harm_instructions)
            .ok()
            .and_then(|j| ::serde_json::from_value(j).ok())
            .unwrap_or_default()
    }
    pub fn harassment_threatening(&self) -> bool {
        ::serde_json::to_value(&self.0.harassment_threatening)
            .ok()
            .and_then(|j| ::serde_json::from_value(j).ok())
            .unwrap_or_default()
    }
    pub fn violence(&self) -> bool {
        ::serde_json::to_value(&self.0.violence)
            .ok()
            .and_then(|j| ::serde_json::from_value(j).ok())
            .unwrap_or_default()
    }
}

pub struct ModerationCategoryScores(pub liter_llm::types::ModerationCategoryScores);
impl ModerationCategoryScores {
    pub fn new(
        sexual: f64,
        hate: f64,
        harassment: f64,
        self_harm: f64,
        sexual_minors: f64,
        hate_threatening: f64,
        violence_graphic: f64,
        self_harm_intent: f64,
        self_harm_instructions: f64,
        harassment_threatening: f64,
        violence: f64,
    ) -> ModerationCategoryScores {
        ModerationCategoryScores(liter_llm::types::ModerationCategoryScores {
            sexual,
            hate,
            harassment,
            self_harm,
            sexual_minors,
            hate_threatening,
            violence_graphic,
            self_harm_intent,
            self_harm_instructions,
            harassment_threatening,
            violence,
        })
    }
    pub fn sexual(&self) -> f64 {
        ::serde_json::to_value(&self.0.sexual)
            .ok()
            .and_then(|j| ::serde_json::from_value(j).ok())
            .unwrap_or_default()
    }
    pub fn hate(&self) -> f64 {
        ::serde_json::to_value(&self.0.hate)
            .ok()
            .and_then(|j| ::serde_json::from_value(j).ok())
            .unwrap_or_default()
    }
    pub fn harassment(&self) -> f64 {
        ::serde_json::to_value(&self.0.harassment)
            .ok()
            .and_then(|j| ::serde_json::from_value(j).ok())
            .unwrap_or_default()
    }
    pub fn self_harm(&self) -> f64 {
        ::serde_json::to_value(&self.0.self_harm)
            .ok()
            .and_then(|j| ::serde_json::from_value(j).ok())
            .unwrap_or_default()
    }
    pub fn sexual_minors(&self) -> f64 {
        ::serde_json::to_value(&self.0.sexual_minors)
            .ok()
            .and_then(|j| ::serde_json::from_value(j).ok())
            .unwrap_or_default()
    }
    pub fn hate_threatening(&self) -> f64 {
        ::serde_json::to_value(&self.0.hate_threatening)
            .ok()
            .and_then(|j| ::serde_json::from_value(j).ok())
            .unwrap_or_default()
    }
    pub fn violence_graphic(&self) -> f64 {
        ::serde_json::to_value(&self.0.violence_graphic)
            .ok()
            .and_then(|j| ::serde_json::from_value(j).ok())
            .unwrap_or_default()
    }
    pub fn self_harm_intent(&self) -> f64 {
        ::serde_json::to_value(&self.0.self_harm_intent)
            .ok()
            .and_then(|j| ::serde_json::from_value(j).ok())
            .unwrap_or_default()
    }
    pub fn self_harm_instructions(&self) -> f64 {
        ::serde_json::to_value(&self.0.self_harm_instructions)
            .ok()
            .and_then(|j| ::serde_json::from_value(j).ok())
            .unwrap_or_default()
    }
    pub fn harassment_threatening(&self) -> f64 {
        ::serde_json::to_value(&self.0.harassment_threatening)
            .ok()
            .and_then(|j| ::serde_json::from_value(j).ok())
            .unwrap_or_default()
    }
    pub fn violence(&self) -> f64 {
        ::serde_json::to_value(&self.0.violence)
            .ok()
            .and_then(|j| ::serde_json::from_value(j).ok())
            .unwrap_or_default()
    }
}

pub struct RerankRequest(pub liter_llm::types::RerankRequest);
impl RerankRequest {
    pub fn new(
        model: String,
        query: String,
        documents: Vec<RerankDocument>,
        top_n: Option<u32>,
        return_documents: Option<bool>,
    ) -> RerankRequest {
        let mut __target: liter_llm::types::RerankRequest = ::std::default::Default::default();
        {
            // Try JSON parse first (handles enum/object values); on parse failure
            // treat the raw input as a JSON string scalar so plain `String` /
            // string-like enum fields don't end up empty for inputs that aren't
            // valid JSON tokens (e.g. `tts-1`).
            let __v = ::serde_json::from_str::<::serde_json::Value>(&model)
                .unwrap_or(::serde_json::Value::String(model.clone()));
            if let Ok(t) = ::serde_json::from_value(__v) {
                __target.model = t;
            }
        }
        {
            // Try JSON parse first (handles enum/object values); on parse failure
            // treat the raw input as a JSON string scalar so plain `String` /
            // string-like enum fields don't end up empty for inputs that aren't
            // valid JSON tokens (e.g. `tts-1`).
            let __v = ::serde_json::from_str::<::serde_json::Value>(&query)
                .unwrap_or(::serde_json::Value::String(query.clone()));
            if let Ok(t) = ::serde_json::from_value(__v) {
                __target.query = t;
            }
        }
        // alef: documents (Vec<>) contains enum elements — left at default
        __target.top_n = top_n;
        __target.return_documents = return_documents;
        RerankRequest(__target)
    }
    pub fn model(&self) -> String {
        self.0.model.clone()
    }
    pub fn query(&self) -> String {
        self.0.query.clone()
    }
    pub fn documents(&self) -> Vec<String> {
        self.0
            .documents
            .iter()
            .map(|elem| serde_json::to_string(elem).unwrap_or_else(|_| "null".to_string()))
            .collect()
    }
    pub fn top_n(&self) -> Option<u32> {
        self.0.top_n.as_ref().and_then(|v| {
            ::serde_json::to_value(v)
                .ok()
                .and_then(|j| ::serde_json::from_value(j).ok())
        })
    }
    pub fn return_documents(&self) -> Option<bool> {
        self.0.return_documents.as_ref().and_then(|v| {
            ::serde_json::to_value(v)
                .ok()
                .and_then(|j| ::serde_json::from_value(j).ok())
        })
    }
}

pub struct RerankResponse(pub liter_llm::types::RerankResponse);
impl RerankResponse {
    pub fn id(&self) -> Option<String> {
        self.0.id.clone()
    }
    pub fn results(&self) -> Vec<RerankResult> {
        self.0.results.iter().map(|elem| RerankResult(elem.clone())).collect()
    }
    pub fn meta(&self) -> Option<String> {
        self.0.meta.as_ref().and_then(|v| serde_json::to_string(v).ok())
    }
}

pub struct RerankResult(pub liter_llm::types::RerankResult);
impl RerankResult {
    pub fn index(&self) -> u32 {
        ::serde_json::to_value(&self.0.index)
            .ok()
            .and_then(|j| ::serde_json::from_value(j).ok())
            .unwrap_or_default()
    }
    pub fn relevance_score(&self) -> f64 {
        ::serde_json::to_value(&self.0.relevance_score)
            .ok()
            .and_then(|j| ::serde_json::from_value(j).ok())
            .unwrap_or_default()
    }
    pub fn document(&self) -> Option<RerankResultDocument> {
        self.0.document.clone().map(RerankResultDocument)
    }
}

pub struct RerankResultDocument(pub liter_llm::types::RerankResultDocument);
impl RerankResultDocument {
    pub fn text(&self) -> String {
        self.0.text.clone()
    }
}

pub struct SearchRequest(pub liter_llm::types::SearchRequest);
impl SearchRequest {
    pub fn new(
        model: String,
        query: String,
        max_results: Option<u32>,
        search_domain_filter: Option<Vec<String>>,
        country: Option<String>,
    ) -> SearchRequest {
        let mut __target: liter_llm::types::SearchRequest = ::std::default::Default::default();
        {
            // Try JSON parse first (handles enum/object values); on parse failure
            // treat the raw input as a JSON string scalar so plain `String` /
            // string-like enum fields don't end up empty for inputs that aren't
            // valid JSON tokens (e.g. `tts-1`).
            let __v = ::serde_json::from_str::<::serde_json::Value>(&model)
                .unwrap_or(::serde_json::Value::String(model.clone()));
            if let Ok(t) = ::serde_json::from_value(__v) {
                __target.model = t;
            }
        }
        {
            // Try JSON parse first (handles enum/object values); on parse failure
            // treat the raw input as a JSON string scalar so plain `String` /
            // string-like enum fields don't end up empty for inputs that aren't
            // valid JSON tokens (e.g. `tts-1`).
            let __v = ::serde_json::from_str::<::serde_json::Value>(&query)
                .unwrap_or(::serde_json::Value::String(query.clone()));
            if let Ok(t) = ::serde_json::from_value(__v) {
                __target.query = t;
            }
        }
        __target.max_results = max_results;
        if let Ok(__v) = ::serde_json::to_value(search_domain_filter) {
            if let Ok(t) = ::serde_json::from_value(__v) {
                __target.search_domain_filter = t;
            }
        }
        if let Some(s) = country {
            // Try JSON parse first (handles enum/object values); on parse failure
            // treat the raw input as a JSON string scalar so plain `String` /
            // string-like enum fields don't end up empty for inputs that aren't
            // valid JSON tokens (e.g. `tts-1`).
            let __v = ::serde_json::from_str::<::serde_json::Value>(&s).unwrap_or(::serde_json::Value::String(s));
            if let Ok(t) = ::serde_json::from_value(__v) {
                __target.country = Some(t);
            }
        }
        SearchRequest(__target)
    }
    pub fn model(&self) -> String {
        self.0.model.clone()
    }
    pub fn query(&self) -> String {
        self.0.query.clone()
    }
    pub fn max_results(&self) -> Option<u32> {
        self.0.max_results.as_ref().and_then(|v| {
            ::serde_json::to_value(v)
                .ok()
                .and_then(|j| ::serde_json::from_value(j).ok())
        })
    }
    pub fn search_domain_filter(&self) -> Option<Vec<String>> {
        self.0.search_domain_filter.as_ref().and_then(|v| {
            ::serde_json::to_value(v)
                .ok()
                .and_then(|j| ::serde_json::from_value(j).ok())
        })
    }
    pub fn country(&self) -> Option<String> {
        self.0.country.clone()
    }
}

pub struct SearchResponse(pub liter_llm::types::SearchResponse);
impl SearchResponse {
    pub fn results(&self) -> Vec<String> {
        self.0
            .results
            .iter()
            .map(|elem| serde_json::to_string(elem).unwrap_or_else(|_| "null".to_string()))
            .collect()
    }
    pub fn model(&self) -> String {
        self.0.model.clone()
    }
}

pub struct SearchResult(pub liter_llm::types::SearchResult);
impl SearchResult {
    pub fn title(&self) -> String {
        self.0.title.clone()
    }
    pub fn url(&self) -> String {
        self.0.url.clone()
    }
    pub fn snippet(&self) -> String {
        self.0.snippet.clone()
    }
    pub fn date(&self) -> Option<String> {
        self.0.date.clone()
    }
}

pub struct OcrRequest(pub liter_llm::types::OcrRequest);
impl OcrRequest {
    pub fn new(
        model: String,
        document: OcrDocument,
        pages: Option<Vec<u32>>,
        include_image_base64: Option<bool>,
    ) -> OcrRequest {
        let mut __target: liter_llm::types::OcrRequest = ::std::default::Default::default();
        {
            // Try JSON parse first (handles enum/object values); on parse failure
            // treat the raw input as a JSON string scalar so plain `String` /
            // string-like enum fields don't end up empty for inputs that aren't
            // valid JSON tokens (e.g. `tts-1`).
            let __v = ::serde_json::from_str::<::serde_json::Value>(&model)
                .unwrap_or(::serde_json::Value::String(model.clone()));
            if let Ok(t) = ::serde_json::from_value(__v) {
                __target.model = t;
            }
        }
        // alef: document (OcrDocument) is an enum; reverse From not generated — left at default
        if let Ok(__v) = ::serde_json::to_value(pages) {
            if let Ok(t) = ::serde_json::from_value(__v) {
                __target.pages = t;
            }
        }
        __target.include_image_base64 = include_image_base64;
        OcrRequest(__target)
    }
    pub fn model(&self) -> String {
        self.0.model.clone()
    }
    pub fn document(&self) -> String {
        serde_json::to_string(&self.0.document).unwrap_or_else(|_| "null".to_string())
    }
    pub fn pages(&self) -> Option<Vec<u32>> {
        self.0.pages.as_ref().and_then(|v| {
            ::serde_json::to_value(v)
                .ok()
                .and_then(|j| ::serde_json::from_value(j).ok())
        })
    }
    pub fn include_image_base64(&self) -> Option<bool> {
        self.0.include_image_base64.as_ref().and_then(|v| {
            ::serde_json::to_value(v)
                .ok()
                .and_then(|j| ::serde_json::from_value(j).ok())
        })
    }
}

pub struct OcrResponse(pub liter_llm::types::OcrResponse);
impl OcrResponse {
    pub fn pages(&self) -> Vec<String> {
        self.0
            .pages
            .iter()
            .map(|elem| serde_json::to_string(elem).unwrap_or_else(|_| "null".to_string()))
            .collect()
    }
    pub fn model(&self) -> String {
        self.0.model.clone()
    }
    pub fn usage(&self) -> Option<Usage> {
        self.0.usage.clone().map(Usage)
    }
}

pub struct OcrPage(pub liter_llm::types::OcrPage);
impl OcrPage {
    pub fn index(&self) -> u32 {
        ::serde_json::to_value(&self.0.index)
            .ok()
            .and_then(|j| ::serde_json::from_value(j).ok())
            .unwrap_or_default()
    }
    pub fn markdown(&self) -> String {
        self.0.markdown.clone()
    }
    pub fn images(&self) -> String {
        serde_json::to_string(&self.0.images.as_ref().map(|v| {
            v.iter()
                .map(|elem| serde_json::to_string(elem).unwrap_or_else(|_| "null".to_string()))
                .collect::<Vec<_>>()
        }))
        .expect("serializable enum vec")
    }
    pub fn dimensions(&self) -> Option<PageDimensions> {
        self.0.dimensions.clone().map(PageDimensions)
    }
}

pub struct OcrImage(pub liter_llm::types::OcrImage);
impl OcrImage {
    pub fn id(&self) -> String {
        self.0.id.clone()
    }
    pub fn image_base64(&self) -> Option<String> {
        self.0.image_base64.clone()
    }
}

pub struct PageDimensions(pub liter_llm::types::PageDimensions);
impl PageDimensions {
    pub fn new(width: u32, height: u32) -> PageDimensions {
        PageDimensions(liter_llm::types::PageDimensions { width, height })
    }
    pub fn width(&self) -> u32 {
        ::serde_json::to_value(&self.0.width)
            .ok()
            .and_then(|j| ::serde_json::from_value(j).ok())
            .unwrap_or_default()
    }
    pub fn height(&self) -> u32 {
        ::serde_json::to_value(&self.0.height)
            .ok()
            .and_then(|j| ::serde_json::from_value(j).ok())
            .unwrap_or_default()
    }
}

pub struct ModelsListResponse(pub liter_llm::types::ModelsListResponse);
impl ModelsListResponse {
    pub fn new(object: String, data: Vec<ModelObject>) -> ModelsListResponse {
        let mut __target: liter_llm::types::ModelsListResponse = ::std::default::Default::default();
        {
            // Try JSON parse first (handles enum/object values); on parse failure
            // treat the raw input as a JSON string scalar so plain `String` /
            // string-like enum fields don't end up empty for inputs that aren't
            // valid JSON tokens (e.g. `tts-1`).
            let __v = ::serde_json::from_str::<::serde_json::Value>(&object)
                .unwrap_or(::serde_json::Value::String(object.clone()));
            if let Ok(t) = ::serde_json::from_value(__v) {
                __target.object = t;
            }
        }
        __target.data = data.into_iter().map(|w| w.0).collect();
        ModelsListResponse(__target)
    }
    pub fn object(&self) -> String {
        self.0.object.clone()
    }
    pub fn data(&self) -> Vec<String> {
        self.0
            .data
            .iter()
            .map(|elem| serde_json::to_string(elem).unwrap_or_else(|_| "null".to_string()))
            .collect()
    }
}

pub struct ModelObject(pub liter_llm::types::ModelObject);
impl ModelObject {
    pub fn new(id: String, object: String, created: u64, owned_by: String) -> ModelObject {
        let mut __target: liter_llm::types::ModelObject = ::std::default::Default::default();
        {
            // Try JSON parse first (handles enum/object values); on parse failure
            // treat the raw input as a JSON string scalar so plain `String` /
            // string-like enum fields don't end up empty for inputs that aren't
            // valid JSON tokens (e.g. `tts-1`).
            let __v =
                ::serde_json::from_str::<::serde_json::Value>(&id).unwrap_or(::serde_json::Value::String(id.clone()));
            if let Ok(t) = ::serde_json::from_value(__v) {
                __target.id = t;
            }
        }
        {
            // Try JSON parse first (handles enum/object values); on parse failure
            // treat the raw input as a JSON string scalar so plain `String` /
            // string-like enum fields don't end up empty for inputs that aren't
            // valid JSON tokens (e.g. `tts-1`).
            let __v = ::serde_json::from_str::<::serde_json::Value>(&object)
                .unwrap_or(::serde_json::Value::String(object.clone()));
            if let Ok(t) = ::serde_json::from_value(__v) {
                __target.object = t;
            }
        }
        __target.created = created;
        {
            // Try JSON parse first (handles enum/object values); on parse failure
            // treat the raw input as a JSON string scalar so plain `String` /
            // string-like enum fields don't end up empty for inputs that aren't
            // valid JSON tokens (e.g. `tts-1`).
            let __v = ::serde_json::from_str::<::serde_json::Value>(&owned_by)
                .unwrap_or(::serde_json::Value::String(owned_by.clone()));
            if let Ok(t) = ::serde_json::from_value(__v) {
                __target.owned_by = t;
            }
        }
        ModelObject(__target)
    }
    pub fn id(&self) -> String {
        self.0.id.clone()
    }
    pub fn object(&self) -> String {
        self.0.object.clone()
    }
    pub fn created(&self) -> u64 {
        ::serde_json::to_value(&self.0.created)
            .ok()
            .and_then(|j| ::serde_json::from_value(j).ok())
            .unwrap_or_default()
    }
    pub fn owned_by(&self) -> String {
        self.0.owned_by.clone()
    }
}

pub struct CreateFileRequest(pub liter_llm::types::CreateFileRequest);
impl CreateFileRequest {
    pub fn new(file: String, purpose: FilePurpose, filename: Option<String>) -> CreateFileRequest {
        let mut __target: liter_llm::types::CreateFileRequest = ::std::default::Default::default();
        {
            // Try JSON parse first (handles enum/object values); on parse failure
            // treat the raw input as a JSON string scalar so plain `String` /
            // string-like enum fields don't end up empty for inputs that aren't
            // valid JSON tokens (e.g. `tts-1`).
            let __v = ::serde_json::from_str::<::serde_json::Value>(&file)
                .unwrap_or(::serde_json::Value::String(file.clone()));
            if let Ok(t) = ::serde_json::from_value(__v) {
                __target.file = t;
            }
        }
        // alef: purpose (FilePurpose) is an enum; reverse From not generated — left at default
        if let Some(s) = filename {
            // Try JSON parse first (handles enum/object values); on parse failure
            // treat the raw input as a JSON string scalar so plain `String` /
            // string-like enum fields don't end up empty for inputs that aren't
            // valid JSON tokens (e.g. `tts-1`).
            let __v = ::serde_json::from_str::<::serde_json::Value>(&s).unwrap_or(::serde_json::Value::String(s));
            if let Ok(t) = ::serde_json::from_value(__v) {
                __target.filename = Some(t);
            }
        }
        CreateFileRequest(__target)
    }
    pub fn file(&self) -> String {
        self.0.file.clone()
    }
    pub fn purpose(&self) -> String {
        FilePurpose::from(self.0.purpose.clone()).to_string()
    }
    pub fn filename(&self) -> Option<String> {
        self.0.filename.clone()
    }
}

pub struct FileObject(pub liter_llm::types::FileObject);
impl FileObject {
    pub fn new(
        id: String,
        object: String,
        bytes: u64,
        created_at: u64,
        filename: String,
        purpose: String,
        status: Option<String>,
    ) -> FileObject {
        let mut __target: liter_llm::types::FileObject = ::std::default::Default::default();
        {
            // Try JSON parse first (handles enum/object values); on parse failure
            // treat the raw input as a JSON string scalar so plain `String` /
            // string-like enum fields don't end up empty for inputs that aren't
            // valid JSON tokens (e.g. `tts-1`).
            let __v =
                ::serde_json::from_str::<::serde_json::Value>(&id).unwrap_or(::serde_json::Value::String(id.clone()));
            if let Ok(t) = ::serde_json::from_value(__v) {
                __target.id = t;
            }
        }
        {
            // Try JSON parse first (handles enum/object values); on parse failure
            // treat the raw input as a JSON string scalar so plain `String` /
            // string-like enum fields don't end up empty for inputs that aren't
            // valid JSON tokens (e.g. `tts-1`).
            let __v = ::serde_json::from_str::<::serde_json::Value>(&object)
                .unwrap_or(::serde_json::Value::String(object.clone()));
            if let Ok(t) = ::serde_json::from_value(__v) {
                __target.object = t;
            }
        }
        __target.bytes = bytes;
        __target.created_at = created_at;
        {
            // Try JSON parse first (handles enum/object values); on parse failure
            // treat the raw input as a JSON string scalar so plain `String` /
            // string-like enum fields don't end up empty for inputs that aren't
            // valid JSON tokens (e.g. `tts-1`).
            let __v = ::serde_json::from_str::<::serde_json::Value>(&filename)
                .unwrap_or(::serde_json::Value::String(filename.clone()));
            if let Ok(t) = ::serde_json::from_value(__v) {
                __target.filename = t;
            }
        }
        {
            // Try JSON parse first (handles enum/object values); on parse failure
            // treat the raw input as a JSON string scalar so plain `String` /
            // string-like enum fields don't end up empty for inputs that aren't
            // valid JSON tokens (e.g. `tts-1`).
            let __v = ::serde_json::from_str::<::serde_json::Value>(&purpose)
                .unwrap_or(::serde_json::Value::String(purpose.clone()));
            if let Ok(t) = ::serde_json::from_value(__v) {
                __target.purpose = t;
            }
        }
        if let Some(s) = status {
            // Try JSON parse first (handles enum/object values); on parse failure
            // treat the raw input as a JSON string scalar so plain `String` /
            // string-like enum fields don't end up empty for inputs that aren't
            // valid JSON tokens (e.g. `tts-1`).
            let __v = ::serde_json::from_str::<::serde_json::Value>(&s).unwrap_or(::serde_json::Value::String(s));
            if let Ok(t) = ::serde_json::from_value(__v) {
                __target.status = Some(t);
            }
        }
        FileObject(__target)
    }
    pub fn id(&self) -> String {
        self.0.id.clone()
    }
    pub fn object(&self) -> String {
        self.0.object.clone()
    }
    pub fn bytes(&self) -> u64 {
        ::serde_json::to_value(&self.0.bytes)
            .ok()
            .and_then(|j| ::serde_json::from_value(j).ok())
            .unwrap_or_default()
    }
    pub fn created_at(&self) -> u64 {
        ::serde_json::to_value(&self.0.created_at)
            .ok()
            .and_then(|j| ::serde_json::from_value(j).ok())
            .unwrap_or_default()
    }
    pub fn filename(&self) -> String {
        self.0.filename.clone()
    }
    pub fn purpose(&self) -> String {
        self.0.purpose.clone()
    }
    pub fn status(&self) -> Option<String> {
        self.0.status.clone()
    }
}

pub struct FileListResponse(pub liter_llm::types::FileListResponse);
impl FileListResponse {
    pub fn new(object: String, data: Vec<FileObject>, has_more: Option<bool>) -> FileListResponse {
        let mut __target: liter_llm::types::FileListResponse = ::std::default::Default::default();
        {
            // Try JSON parse first (handles enum/object values); on parse failure
            // treat the raw input as a JSON string scalar so plain `String` /
            // string-like enum fields don't end up empty for inputs that aren't
            // valid JSON tokens (e.g. `tts-1`).
            let __v = ::serde_json::from_str::<::serde_json::Value>(&object)
                .unwrap_or(::serde_json::Value::String(object.clone()));
            if let Ok(t) = ::serde_json::from_value(__v) {
                __target.object = t;
            }
        }
        __target.data = data.into_iter().map(|w| w.0).collect();
        __target.has_more = has_more;
        FileListResponse(__target)
    }
    pub fn object(&self) -> String {
        self.0.object.clone()
    }
    pub fn data(&self) -> Vec<String> {
        self.0
            .data
            .iter()
            .map(|elem| serde_json::to_string(elem).unwrap_or_else(|_| "null".to_string()))
            .collect()
    }
    pub fn has_more(&self) -> Option<bool> {
        self.0.has_more.as_ref().and_then(|v| {
            ::serde_json::to_value(v)
                .ok()
                .and_then(|j| ::serde_json::from_value(j).ok())
        })
    }
}

pub struct FileListQuery(pub liter_llm::types::FileListQuery);
impl FileListQuery {
    pub fn new(purpose: Option<String>, limit: Option<u32>, after: Option<String>) -> FileListQuery {
        let mut __target: liter_llm::types::FileListQuery = ::std::default::Default::default();
        if let Some(s) = purpose {
            // Try JSON parse first (handles enum/object values); on parse failure
            // treat the raw input as a JSON string scalar so plain `String` /
            // string-like enum fields don't end up empty for inputs that aren't
            // valid JSON tokens (e.g. `tts-1`).
            let __v = ::serde_json::from_str::<::serde_json::Value>(&s).unwrap_or(::serde_json::Value::String(s));
            if let Ok(t) = ::serde_json::from_value(__v) {
                __target.purpose = Some(t);
            }
        }
        __target.limit = limit;
        if let Some(s) = after {
            // Try JSON parse first (handles enum/object values); on parse failure
            // treat the raw input as a JSON string scalar so plain `String` /
            // string-like enum fields don't end up empty for inputs that aren't
            // valid JSON tokens (e.g. `tts-1`).
            let __v = ::serde_json::from_str::<::serde_json::Value>(&s).unwrap_or(::serde_json::Value::String(s));
            if let Ok(t) = ::serde_json::from_value(__v) {
                __target.after = Some(t);
            }
        }
        FileListQuery(__target)
    }
    pub fn purpose(&self) -> Option<String> {
        self.0.purpose.clone()
    }
    pub fn limit(&self) -> Option<u32> {
        self.0.limit.as_ref().and_then(|v| {
            ::serde_json::to_value(v)
                .ok()
                .and_then(|j| ::serde_json::from_value(j).ok())
        })
    }
    pub fn after(&self) -> Option<String> {
        self.0.after.clone()
    }
}

pub struct DeleteResponse(pub liter_llm::types::DeleteResponse);
impl DeleteResponse {
    pub fn new(id: String, object: String, deleted: bool) -> DeleteResponse {
        let mut __target: liter_llm::types::DeleteResponse = ::std::default::Default::default();
        {
            // Try JSON parse first (handles enum/object values); on parse failure
            // treat the raw input as a JSON string scalar so plain `String` /
            // string-like enum fields don't end up empty for inputs that aren't
            // valid JSON tokens (e.g. `tts-1`).
            let __v =
                ::serde_json::from_str::<::serde_json::Value>(&id).unwrap_or(::serde_json::Value::String(id.clone()));
            if let Ok(t) = ::serde_json::from_value(__v) {
                __target.id = t;
            }
        }
        {
            // Try JSON parse first (handles enum/object values); on parse failure
            // treat the raw input as a JSON string scalar so plain `String` /
            // string-like enum fields don't end up empty for inputs that aren't
            // valid JSON tokens (e.g. `tts-1`).
            let __v = ::serde_json::from_str::<::serde_json::Value>(&object)
                .unwrap_or(::serde_json::Value::String(object.clone()));
            if let Ok(t) = ::serde_json::from_value(__v) {
                __target.object = t;
            }
        }
        __target.deleted = deleted;
        DeleteResponse(__target)
    }
    pub fn id(&self) -> String {
        self.0.id.clone()
    }
    pub fn object(&self) -> String {
        self.0.object.clone()
    }
    pub fn deleted(&self) -> bool {
        ::serde_json::to_value(&self.0.deleted)
            .ok()
            .and_then(|j| ::serde_json::from_value(j).ok())
            .unwrap_or_default()
    }
}

pub struct CreateBatchRequest(pub liter_llm::types::CreateBatchRequest);
impl CreateBatchRequest {
    pub fn new(
        input_file_id: String,
        endpoint: String,
        completion_window: String,
        metadata: Option<String>,
    ) -> CreateBatchRequest {
        let mut __target: liter_llm::types::CreateBatchRequest = ::std::default::Default::default();
        {
            // Try JSON parse first (handles enum/object values); on parse failure
            // treat the raw input as a JSON string scalar so plain `String` /
            // string-like enum fields don't end up empty for inputs that aren't
            // valid JSON tokens (e.g. `tts-1`).
            let __v = ::serde_json::from_str::<::serde_json::Value>(&input_file_id)
                .unwrap_or(::serde_json::Value::String(input_file_id.clone()));
            if let Ok(t) = ::serde_json::from_value(__v) {
                __target.input_file_id = t;
            }
        }
        {
            // Try JSON parse first (handles enum/object values); on parse failure
            // treat the raw input as a JSON string scalar so plain `String` /
            // string-like enum fields don't end up empty for inputs that aren't
            // valid JSON tokens (e.g. `tts-1`).
            let __v = ::serde_json::from_str::<::serde_json::Value>(&endpoint)
                .unwrap_or(::serde_json::Value::String(endpoint.clone()));
            if let Ok(t) = ::serde_json::from_value(__v) {
                __target.endpoint = t;
            }
        }
        {
            // Try JSON parse first (handles enum/object values); on parse failure
            // treat the raw input as a JSON string scalar so plain `String` /
            // string-like enum fields don't end up empty for inputs that aren't
            // valid JSON tokens (e.g. `tts-1`).
            let __v = ::serde_json::from_str::<::serde_json::Value>(&completion_window)
                .unwrap_or(::serde_json::Value::String(completion_window.clone()));
            if let Ok(t) = ::serde_json::from_value(__v) {
                __target.completion_window = t;
            }
        }
        if let Some(s) = metadata {
            // Try JSON parse first (handles enum/object values); on parse failure
            // treat the raw input as a JSON string scalar so plain `String` /
            // string-like enum fields don't end up empty for inputs that aren't
            // valid JSON tokens (e.g. `tts-1`).
            let __v = ::serde_json::from_str::<::serde_json::Value>(&s).unwrap_or(::serde_json::Value::String(s));
            if let Ok(t) = ::serde_json::from_value(__v) {
                __target.metadata = Some(t);
            }
        }
        CreateBatchRequest(__target)
    }
    pub fn input_file_id(&self) -> String {
        self.0.input_file_id.clone()
    }
    pub fn endpoint(&self) -> String {
        self.0.endpoint.clone()
    }
    pub fn completion_window(&self) -> String {
        self.0.completion_window.clone()
    }
    pub fn metadata(&self) -> Option<String> {
        self.0.metadata.as_ref().and_then(|v| serde_json::to_string(v).ok())
    }
}

pub struct BatchObject(pub liter_llm::types::BatchObject);
impl BatchObject {
    pub fn new(
        id: String,
        object: String,
        endpoint: String,
        input_file_id: String,
        completion_window: String,
        status: BatchStatus,
        output_file_id: Option<String>,
        error_file_id: Option<String>,
        created_at: u64,
        completed_at: Option<u64>,
        failed_at: Option<u64>,
        expired_at: Option<u64>,
        request_counts: Option<BatchRequestCounts>,
        metadata: Option<String>,
    ) -> BatchObject {
        let mut __target: liter_llm::types::BatchObject = ::std::default::Default::default();
        {
            // Try JSON parse first (handles enum/object values); on parse failure
            // treat the raw input as a JSON string scalar so plain `String` /
            // string-like enum fields don't end up empty for inputs that aren't
            // valid JSON tokens (e.g. `tts-1`).
            let __v =
                ::serde_json::from_str::<::serde_json::Value>(&id).unwrap_or(::serde_json::Value::String(id.clone()));
            if let Ok(t) = ::serde_json::from_value(__v) {
                __target.id = t;
            }
        }
        {
            // Try JSON parse first (handles enum/object values); on parse failure
            // treat the raw input as a JSON string scalar so plain `String` /
            // string-like enum fields don't end up empty for inputs that aren't
            // valid JSON tokens (e.g. `tts-1`).
            let __v = ::serde_json::from_str::<::serde_json::Value>(&object)
                .unwrap_or(::serde_json::Value::String(object.clone()));
            if let Ok(t) = ::serde_json::from_value(__v) {
                __target.object = t;
            }
        }
        {
            // Try JSON parse first (handles enum/object values); on parse failure
            // treat the raw input as a JSON string scalar so plain `String` /
            // string-like enum fields don't end up empty for inputs that aren't
            // valid JSON tokens (e.g. `tts-1`).
            let __v = ::serde_json::from_str::<::serde_json::Value>(&endpoint)
                .unwrap_or(::serde_json::Value::String(endpoint.clone()));
            if let Ok(t) = ::serde_json::from_value(__v) {
                __target.endpoint = t;
            }
        }
        {
            // Try JSON parse first (handles enum/object values); on parse failure
            // treat the raw input as a JSON string scalar so plain `String` /
            // string-like enum fields don't end up empty for inputs that aren't
            // valid JSON tokens (e.g. `tts-1`).
            let __v = ::serde_json::from_str::<::serde_json::Value>(&input_file_id)
                .unwrap_or(::serde_json::Value::String(input_file_id.clone()));
            if let Ok(t) = ::serde_json::from_value(__v) {
                __target.input_file_id = t;
            }
        }
        {
            // Try JSON parse first (handles enum/object values); on parse failure
            // treat the raw input as a JSON string scalar so plain `String` /
            // string-like enum fields don't end up empty for inputs that aren't
            // valid JSON tokens (e.g. `tts-1`).
            let __v = ::serde_json::from_str::<::serde_json::Value>(&completion_window)
                .unwrap_or(::serde_json::Value::String(completion_window.clone()));
            if let Ok(t) = ::serde_json::from_value(__v) {
                __target.completion_window = t;
            }
        }
        // alef: status (BatchStatus) is an enum; reverse From not generated — left at default
        if let Some(s) = output_file_id {
            // Try JSON parse first (handles enum/object values); on parse failure
            // treat the raw input as a JSON string scalar so plain `String` /
            // string-like enum fields don't end up empty for inputs that aren't
            // valid JSON tokens (e.g. `tts-1`).
            let __v = ::serde_json::from_str::<::serde_json::Value>(&s).unwrap_or(::serde_json::Value::String(s));
            if let Ok(t) = ::serde_json::from_value(__v) {
                __target.output_file_id = Some(t);
            }
        }
        if let Some(s) = error_file_id {
            // Try JSON parse first (handles enum/object values); on parse failure
            // treat the raw input as a JSON string scalar so plain `String` /
            // string-like enum fields don't end up empty for inputs that aren't
            // valid JSON tokens (e.g. `tts-1`).
            let __v = ::serde_json::from_str::<::serde_json::Value>(&s).unwrap_or(::serde_json::Value::String(s));
            if let Ok(t) = ::serde_json::from_value(__v) {
                __target.error_file_id = Some(t);
            }
        }
        __target.created_at = created_at;
        __target.completed_at = completed_at;
        __target.failed_at = failed_at;
        __target.expired_at = expired_at;
        if let Some(w) = request_counts {
            __target.request_counts = Some(w.0);
        }
        if let Some(s) = metadata {
            // Try JSON parse first (handles enum/object values); on parse failure
            // treat the raw input as a JSON string scalar so plain `String` /
            // string-like enum fields don't end up empty for inputs that aren't
            // valid JSON tokens (e.g. `tts-1`).
            let __v = ::serde_json::from_str::<::serde_json::Value>(&s).unwrap_or(::serde_json::Value::String(s));
            if let Ok(t) = ::serde_json::from_value(__v) {
                __target.metadata = Some(t);
            }
        }
        BatchObject(__target)
    }
    pub fn id(&self) -> String {
        self.0.id.clone()
    }
    pub fn object(&self) -> String {
        self.0.object.clone()
    }
    pub fn endpoint(&self) -> String {
        self.0.endpoint.clone()
    }
    pub fn input_file_id(&self) -> String {
        self.0.input_file_id.clone()
    }
    pub fn completion_window(&self) -> String {
        self.0.completion_window.clone()
    }
    pub fn status(&self) -> String {
        BatchStatus::from(self.0.status.clone()).to_string()
    }
    pub fn output_file_id(&self) -> Option<String> {
        self.0.output_file_id.clone()
    }
    pub fn error_file_id(&self) -> Option<String> {
        self.0.error_file_id.clone()
    }
    pub fn created_at(&self) -> u64 {
        ::serde_json::to_value(&self.0.created_at)
            .ok()
            .and_then(|j| ::serde_json::from_value(j).ok())
            .unwrap_or_default()
    }
    pub fn completed_at(&self) -> Option<u64> {
        self.0.completed_at.as_ref().and_then(|v| {
            ::serde_json::to_value(v)
                .ok()
                .and_then(|j| ::serde_json::from_value(j).ok())
        })
    }
    pub fn failed_at(&self) -> Option<u64> {
        self.0.failed_at.as_ref().and_then(|v| {
            ::serde_json::to_value(v)
                .ok()
                .and_then(|j| ::serde_json::from_value(j).ok())
        })
    }
    pub fn expired_at(&self) -> Option<u64> {
        self.0.expired_at.as_ref().and_then(|v| {
            ::serde_json::to_value(v)
                .ok()
                .and_then(|j| ::serde_json::from_value(j).ok())
        })
    }
    pub fn request_counts(&self) -> Option<BatchRequestCounts> {
        self.0.request_counts.clone().map(BatchRequestCounts)
    }
    pub fn metadata(&self) -> Option<String> {
        self.0.metadata.as_ref().and_then(|v| serde_json::to_string(v).ok())
    }
}

pub struct BatchRequestCounts(pub liter_llm::types::BatchRequestCounts);
impl BatchRequestCounts {
    pub fn new(total: u64, completed: u64, failed: u64) -> BatchRequestCounts {
        BatchRequestCounts(liter_llm::types::BatchRequestCounts {
            total,
            completed,
            failed,
        })
    }
    pub fn total(&self) -> u64 {
        ::serde_json::to_value(&self.0.total)
            .ok()
            .and_then(|j| ::serde_json::from_value(j).ok())
            .unwrap_or_default()
    }
    pub fn completed(&self) -> u64 {
        ::serde_json::to_value(&self.0.completed)
            .ok()
            .and_then(|j| ::serde_json::from_value(j).ok())
            .unwrap_or_default()
    }
    pub fn failed(&self) -> u64 {
        ::serde_json::to_value(&self.0.failed)
            .ok()
            .and_then(|j| ::serde_json::from_value(j).ok())
            .unwrap_or_default()
    }
}

pub struct BatchListResponse(pub liter_llm::types::BatchListResponse);
impl BatchListResponse {
    pub fn new(
        object: String,
        data: Vec<BatchObject>,
        has_more: Option<bool>,
        first_id: Option<String>,
        last_id: Option<String>,
    ) -> BatchListResponse {
        let mut __target: liter_llm::types::BatchListResponse = ::std::default::Default::default();
        {
            // Try JSON parse first (handles enum/object values); on parse failure
            // treat the raw input as a JSON string scalar so plain `String` /
            // string-like enum fields don't end up empty for inputs that aren't
            // valid JSON tokens (e.g. `tts-1`).
            let __v = ::serde_json::from_str::<::serde_json::Value>(&object)
                .unwrap_or(::serde_json::Value::String(object.clone()));
            if let Ok(t) = ::serde_json::from_value(__v) {
                __target.object = t;
            }
        }
        __target.data = data.into_iter().map(|w| w.0).collect();
        __target.has_more = has_more;
        if let Some(s) = first_id {
            // Try JSON parse first (handles enum/object values); on parse failure
            // treat the raw input as a JSON string scalar so plain `String` /
            // string-like enum fields don't end up empty for inputs that aren't
            // valid JSON tokens (e.g. `tts-1`).
            let __v = ::serde_json::from_str::<::serde_json::Value>(&s).unwrap_or(::serde_json::Value::String(s));
            if let Ok(t) = ::serde_json::from_value(__v) {
                __target.first_id = Some(t);
            }
        }
        if let Some(s) = last_id {
            // Try JSON parse first (handles enum/object values); on parse failure
            // treat the raw input as a JSON string scalar so plain `String` /
            // string-like enum fields don't end up empty for inputs that aren't
            // valid JSON tokens (e.g. `tts-1`).
            let __v = ::serde_json::from_str::<::serde_json::Value>(&s).unwrap_or(::serde_json::Value::String(s));
            if let Ok(t) = ::serde_json::from_value(__v) {
                __target.last_id = Some(t);
            }
        }
        BatchListResponse(__target)
    }
    pub fn object(&self) -> String {
        self.0.object.clone()
    }
    pub fn data(&self) -> Vec<BatchObject> {
        self.0.data.iter().map(|elem| BatchObject(elem.clone())).collect()
    }
    pub fn has_more(&self) -> Option<bool> {
        self.0.has_more.as_ref().and_then(|v| {
            ::serde_json::to_value(v)
                .ok()
                .and_then(|j| ::serde_json::from_value(j).ok())
        })
    }
    pub fn first_id(&self) -> Option<String> {
        self.0.first_id.clone()
    }
    pub fn last_id(&self) -> Option<String> {
        self.0.last_id.clone()
    }
}

pub struct BatchListQuery(pub liter_llm::types::BatchListQuery);
impl BatchListQuery {
    pub fn new(limit: Option<u32>, after: Option<String>) -> BatchListQuery {
        let mut __target: liter_llm::types::BatchListQuery = ::std::default::Default::default();
        __target.limit = limit;
        if let Some(s) = after {
            // Try JSON parse first (handles enum/object values); on parse failure
            // treat the raw input as a JSON string scalar so plain `String` /
            // string-like enum fields don't end up empty for inputs that aren't
            // valid JSON tokens (e.g. `tts-1`).
            let __v = ::serde_json::from_str::<::serde_json::Value>(&s).unwrap_or(::serde_json::Value::String(s));
            if let Ok(t) = ::serde_json::from_value(__v) {
                __target.after = Some(t);
            }
        }
        BatchListQuery(__target)
    }
    pub fn limit(&self) -> Option<u32> {
        self.0.limit.as_ref().and_then(|v| {
            ::serde_json::to_value(v)
                .ok()
                .and_then(|j| ::serde_json::from_value(j).ok())
        })
    }
    pub fn after(&self) -> Option<String> {
        self.0.after.clone()
    }
}

pub struct CreateResponseRequest(pub liter_llm::types::CreateResponseRequest);
impl CreateResponseRequest {
    pub fn new(
        model: String,
        input: String,
        instructions: Option<String>,
        tools: Option<Vec<ResponseTool>>,
        temperature: Option<f64>,
        max_output_tokens: Option<u64>,
        metadata: Option<String>,
    ) -> CreateResponseRequest {
        let mut __target: liter_llm::types::CreateResponseRequest = ::std::default::Default::default();
        {
            // Try JSON parse first (handles enum/object values); on parse failure
            // treat the raw input as a JSON string scalar so plain `String` /
            // string-like enum fields don't end up empty for inputs that aren't
            // valid JSON tokens (e.g. `tts-1`).
            let __v = ::serde_json::from_str::<::serde_json::Value>(&model)
                .unwrap_or(::serde_json::Value::String(model.clone()));
            if let Ok(t) = ::serde_json::from_value(__v) {
                __target.model = t;
            }
        }
        {
            // Try JSON parse first (handles enum/object values); on parse failure
            // treat the raw input as a JSON string scalar so plain `String` /
            // string-like enum fields don't end up empty for inputs that aren't
            // valid JSON tokens (e.g. `tts-1`).
            let __v = ::serde_json::from_str::<::serde_json::Value>(&input)
                .unwrap_or(::serde_json::Value::String(input.clone()));
            if let Ok(t) = ::serde_json::from_value(__v) {
                __target.input = t;
            }
        }
        if let Some(s) = instructions {
            // Try JSON parse first (handles enum/object values); on parse failure
            // treat the raw input as a JSON string scalar so plain `String` /
            // string-like enum fields don't end up empty for inputs that aren't
            // valid JSON tokens (e.g. `tts-1`).
            let __v = ::serde_json::from_str::<::serde_json::Value>(&s).unwrap_or(::serde_json::Value::String(s));
            if let Ok(t) = ::serde_json::from_value(__v) {
                __target.instructions = Some(t);
            }
        }
        if let Some(v) = tools {
            __target.tools = Some(v.into_iter().map(|w| w.0).collect());
        }
        __target.temperature = temperature;
        __target.max_output_tokens = max_output_tokens;
        if let Some(s) = metadata {
            // Try JSON parse first (handles enum/object values); on parse failure
            // treat the raw input as a JSON string scalar so plain `String` /
            // string-like enum fields don't end up empty for inputs that aren't
            // valid JSON tokens (e.g. `tts-1`).
            let __v = ::serde_json::from_str::<::serde_json::Value>(&s).unwrap_or(::serde_json::Value::String(s));
            if let Ok(t) = ::serde_json::from_value(__v) {
                __target.metadata = Some(t);
            }
        }
        CreateResponseRequest(__target)
    }
    pub fn model(&self) -> String {
        self.0.model.clone()
    }
    pub fn input(&self) -> String {
        serde_json::to_string(&self.0.input).unwrap_or_default()
    }
    pub fn instructions(&self) -> Option<String> {
        self.0.instructions.clone()
    }
    pub fn tools(&self) -> String {
        serde_json::to_string(&self.0.tools.as_ref().map(|v| {
            v.iter()
                .map(|elem| serde_json::to_string(elem).unwrap_or_else(|_| "null".to_string()))
                .collect::<Vec<_>>()
        }))
        .expect("serializable enum vec")
    }
    pub fn temperature(&self) -> Option<f64> {
        self.0.temperature.as_ref().and_then(|v| {
            ::serde_json::to_value(v)
                .ok()
                .and_then(|j| ::serde_json::from_value(j).ok())
        })
    }
    pub fn max_output_tokens(&self) -> Option<u64> {
        self.0.max_output_tokens.as_ref().and_then(|v| {
            ::serde_json::to_value(v)
                .ok()
                .and_then(|j| ::serde_json::from_value(j).ok())
        })
    }
    pub fn metadata(&self) -> Option<String> {
        self.0.metadata.as_ref().and_then(|v| serde_json::to_string(v).ok())
    }
}

pub struct ResponseTool(pub liter_llm::types::ResponseTool);
impl ResponseTool {
    pub fn new(tool_type: String, config: String) -> ResponseTool {
        let mut __target: liter_llm::types::ResponseTool = ::std::default::Default::default();
        {
            // Try JSON parse first (handles enum/object values); on parse failure
            // treat the raw input as a JSON string scalar so plain `String` /
            // string-like enum fields don't end up empty for inputs that aren't
            // valid JSON tokens (e.g. `tts-1`).
            let __v = ::serde_json::from_str::<::serde_json::Value>(&tool_type)
                .unwrap_or(::serde_json::Value::String(tool_type.clone()));
            if let Ok(t) = ::serde_json::from_value(__v) {
                __target.tool_type = t;
            }
        }
        {
            // Try JSON parse first (handles enum/object values); on parse failure
            // treat the raw input as a JSON string scalar so plain `String` /
            // string-like enum fields don't end up empty for inputs that aren't
            // valid JSON tokens (e.g. `tts-1`).
            let __v = ::serde_json::from_str::<::serde_json::Value>(&config)
                .unwrap_or(::serde_json::Value::String(config.clone()));
            if let Ok(t) = ::serde_json::from_value(__v) {
                __target.config = t;
            }
        }
        ResponseTool(__target)
    }
    pub fn tool_type(&self) -> String {
        self.0.tool_type.clone()
    }
    pub fn config(&self) -> String {
        serde_json::to_string(&self.0.config).unwrap_or_default()
    }
}

pub struct ResponseObject(pub liter_llm::types::ResponseObject);
impl ResponseObject {
    pub fn new(
        id: String,
        object: String,
        created_at: u64,
        model: String,
        status: String,
        output: Vec<ResponseOutputItem>,
        usage: Option<ResponseUsage>,
        error: Option<String>,
    ) -> ResponseObject {
        let mut __target: liter_llm::types::ResponseObject = ::std::default::Default::default();
        {
            // Try JSON parse first (handles enum/object values); on parse failure
            // treat the raw input as a JSON string scalar so plain `String` /
            // string-like enum fields don't end up empty for inputs that aren't
            // valid JSON tokens (e.g. `tts-1`).
            let __v =
                ::serde_json::from_str::<::serde_json::Value>(&id).unwrap_or(::serde_json::Value::String(id.clone()));
            if let Ok(t) = ::serde_json::from_value(__v) {
                __target.id = t;
            }
        }
        {
            // Try JSON parse first (handles enum/object values); on parse failure
            // treat the raw input as a JSON string scalar so plain `String` /
            // string-like enum fields don't end up empty for inputs that aren't
            // valid JSON tokens (e.g. `tts-1`).
            let __v = ::serde_json::from_str::<::serde_json::Value>(&object)
                .unwrap_or(::serde_json::Value::String(object.clone()));
            if let Ok(t) = ::serde_json::from_value(__v) {
                __target.object = t;
            }
        }
        __target.created_at = created_at;
        {
            // Try JSON parse first (handles enum/object values); on parse failure
            // treat the raw input as a JSON string scalar so plain `String` /
            // string-like enum fields don't end up empty for inputs that aren't
            // valid JSON tokens (e.g. `tts-1`).
            let __v = ::serde_json::from_str::<::serde_json::Value>(&model)
                .unwrap_or(::serde_json::Value::String(model.clone()));
            if let Ok(t) = ::serde_json::from_value(__v) {
                __target.model = t;
            }
        }
        {
            // Try JSON parse first (handles enum/object values); on parse failure
            // treat the raw input as a JSON string scalar so plain `String` /
            // string-like enum fields don't end up empty for inputs that aren't
            // valid JSON tokens (e.g. `tts-1`).
            let __v = ::serde_json::from_str::<::serde_json::Value>(&status)
                .unwrap_or(::serde_json::Value::String(status.clone()));
            if let Ok(t) = ::serde_json::from_value(__v) {
                __target.status = t;
            }
        }
        __target.output = output.into_iter().map(|w| w.0).collect();
        if let Some(w) = usage {
            __target.usage = Some(w.0);
        }
        if let Some(s) = error {
            // Try JSON parse first (handles enum/object values); on parse failure
            // treat the raw input as a JSON string scalar so plain `String` /
            // string-like enum fields don't end up empty for inputs that aren't
            // valid JSON tokens (e.g. `tts-1`).
            let __v = ::serde_json::from_str::<::serde_json::Value>(&s).unwrap_or(::serde_json::Value::String(s));
            if let Ok(t) = ::serde_json::from_value(__v) {
                __target.error = Some(t);
            }
        }
        ResponseObject(__target)
    }
    pub fn id(&self) -> String {
        self.0.id.clone()
    }
    pub fn object(&self) -> String {
        self.0.object.clone()
    }
    pub fn created_at(&self) -> u64 {
        ::serde_json::to_value(&self.0.created_at)
            .ok()
            .and_then(|j| ::serde_json::from_value(j).ok())
            .unwrap_or_default()
    }
    pub fn model(&self) -> String {
        self.0.model.clone()
    }
    pub fn status(&self) -> String {
        self.0.status.clone()
    }
    pub fn output(&self) -> Vec<ResponseOutputItem> {
        self.0
            .output
            .iter()
            .map(|elem| ResponseOutputItem(elem.clone()))
            .collect()
    }
    pub fn usage(&self) -> Option<ResponseUsage> {
        self.0.usage.clone().map(ResponseUsage)
    }
    pub fn error(&self) -> Option<String> {
        self.0.error.as_ref().and_then(|v| serde_json::to_string(v).ok())
    }
}

pub struct ResponseOutputItem(pub liter_llm::types::ResponseOutputItem);
impl ResponseOutputItem {
    pub fn new(item_type: String, content: String) -> ResponseOutputItem {
        let mut __target: liter_llm::types::ResponseOutputItem = ::std::default::Default::default();
        {
            // Try JSON parse first (handles enum/object values); on parse failure
            // treat the raw input as a JSON string scalar so plain `String` /
            // string-like enum fields don't end up empty for inputs that aren't
            // valid JSON tokens (e.g. `tts-1`).
            let __v = ::serde_json::from_str::<::serde_json::Value>(&item_type)
                .unwrap_or(::serde_json::Value::String(item_type.clone()));
            if let Ok(t) = ::serde_json::from_value(__v) {
                __target.item_type = t;
            }
        }
        {
            // Try JSON parse first (handles enum/object values); on parse failure
            // treat the raw input as a JSON string scalar so plain `String` /
            // string-like enum fields don't end up empty for inputs that aren't
            // valid JSON tokens (e.g. `tts-1`).
            let __v = ::serde_json::from_str::<::serde_json::Value>(&content)
                .unwrap_or(::serde_json::Value::String(content.clone()));
            if let Ok(t) = ::serde_json::from_value(__v) {
                __target.content = t;
            }
        }
        ResponseOutputItem(__target)
    }
    pub fn item_type(&self) -> String {
        self.0.item_type.clone()
    }
    pub fn content(&self) -> String {
        serde_json::to_string(&self.0.content).unwrap_or_default()
    }
}

pub struct ResponseUsage(pub liter_llm::types::ResponseUsage);
impl ResponseUsage {
    pub fn new(input_tokens: u64, output_tokens: u64, total_tokens: u64) -> ResponseUsage {
        ResponseUsage(liter_llm::types::ResponseUsage {
            input_tokens,
            output_tokens,
            total_tokens,
        })
    }
    pub fn input_tokens(&self) -> u64 {
        ::serde_json::to_value(&self.0.input_tokens)
            .ok()
            .and_then(|j| ::serde_json::from_value(j).ok())
            .unwrap_or_default()
    }
    pub fn output_tokens(&self) -> u64 {
        ::serde_json::to_value(&self.0.output_tokens)
            .ok()
            .and_then(|j| ::serde_json::from_value(j).ok())
            .unwrap_or_default()
    }
    pub fn total_tokens(&self) -> u64 {
        ::serde_json::to_value(&self.0.total_tokens)
            .ok()
            .and_then(|j| ::serde_json::from_value(j).ok())
            .unwrap_or_default()
    }
}

pub struct WaitForBatchConfig(pub liter_llm::client::WaitForBatchConfig);
impl WaitForBatchConfig {
    pub fn new(
        initial_interval_secs: f64,
        max_interval_secs: f64,
        backoff_multiplier: f32,
        timeout_secs: Option<f64>,
    ) -> WaitForBatchConfig {
        WaitForBatchConfig(liter_llm::client::WaitForBatchConfig {
            initial_interval_secs,
            max_interval_secs,
            backoff_multiplier,
            timeout_secs,
        })
    }
    pub fn initial_interval_secs(&self) -> f64 {
        ::serde_json::to_value(&self.0.initial_interval_secs)
            .ok()
            .and_then(|j| ::serde_json::from_value(j).ok())
            .unwrap_or_default()
    }
    pub fn max_interval_secs(&self) -> f64 {
        ::serde_json::to_value(&self.0.max_interval_secs)
            .ok()
            .and_then(|j| ::serde_json::from_value(j).ok())
            .unwrap_or_default()
    }
    pub fn backoff_multiplier(&self) -> f32 {
        ::serde_json::to_value(&self.0.backoff_multiplier)
            .ok()
            .and_then(|j| ::serde_json::from_value(j).ok())
            .unwrap_or_default()
    }
    pub fn timeout_secs(&self) -> Option<f64> {
        self.0.timeout_secs.as_ref().and_then(|v| {
            ::serde_json::to_value(v)
                .ok()
                .and_then(|j| ::serde_json::from_value(j).ok())
        })
    }
}

#[cfg(any(feature = "native-http", feature = "wasm-http"))]
pub struct DefaultClient(pub liter_llm::client::DefaultClient);

#[cfg(any(feature = "native-http", feature = "wasm-http"))]
pub fn create_default_client(api_key: String, base_url: Option<String>) -> Result<DefaultClient, String> {
    let mut config = liter_llm::ClientConfig::new(api_key);
    if let Some(url) = base_url {
        config.base_url = Some(url);
    }
    liter_llm::client::DefaultClient::new(config, None)
        .map_err(|e| e.to_string())
        .map(DefaultClient)
}

#[allow(unused_imports)]
use liter_llm::client::BatchClient;
#[allow(unused_imports)]
use liter_llm::client::BatchRetriever;
#[allow(unused_imports)]
use liter_llm::client::FileClient;
#[allow(unused_imports)]
use liter_llm::client::LlmClient;
#[allow(unused_imports)]
use liter_llm::client::ResponseClient;

#[cfg(any(feature = "native-http", feature = "wasm-http"))]
pub fn default_client_chat(
    client: &DefaultClient,
    req: ChatCompletionRequest,
) -> Result<ChatCompletionResponse, String> {
    crate::__alef_tokio_runtime().block_on(async {
        client
            .0
            .chat(req.0)
            .await
            .map_err(|e| e.to_string())
            .map(ChatCompletionResponse)
    })
}
#[cfg(any(feature = "native-http", feature = "wasm-http"))]
pub fn default_client_embed(client: &DefaultClient, req: EmbeddingRequest) -> Result<EmbeddingResponse, String> {
    crate::__alef_tokio_runtime().block_on(async {
        client
            .0
            .embed(req.0)
            .await
            .map_err(|e| e.to_string())
            .map(EmbeddingResponse)
    })
}
#[cfg(any(feature = "native-http", feature = "wasm-http"))]
pub fn default_client_list_models(client: &DefaultClient) -> Result<ModelsListResponse, String> {
    crate::__alef_tokio_runtime().block_on(async {
        client
            .0
            .list_models()
            .await
            .map_err(|e| e.to_string())
            .map(ModelsListResponse)
    })
}
#[cfg(any(feature = "native-http", feature = "wasm-http"))]
pub fn default_client_image_generate(
    client: &DefaultClient,
    req: CreateImageRequest,
) -> Result<ImagesResponse, String> {
    crate::__alef_tokio_runtime().block_on(async {
        client
            .0
            .image_generate(req.0)
            .await
            .map_err(|e| e.to_string())
            .map(ImagesResponse)
    })
}
#[cfg(any(feature = "native-http", feature = "wasm-http"))]
pub fn default_client_speech(client: &DefaultClient, req: CreateSpeechRequest) -> Result<Vec<u8>, String> {
    crate::__alef_tokio_runtime().block_on(async {
        client
            .0
            .speech(req.0)
            .await
            .map_err(|e| e.to_string())
            .map(|b| b.to_vec())
    })
}
#[cfg(any(feature = "native-http", feature = "wasm-http"))]
pub fn default_client_transcribe(
    client: &DefaultClient,
    req: CreateTranscriptionRequest,
) -> Result<TranscriptionResponse, String> {
    crate::__alef_tokio_runtime().block_on(async {
        client
            .0
            .transcribe(req.0)
            .await
            .map_err(|e| e.to_string())
            .map(TranscriptionResponse)
    })
}
#[cfg(any(feature = "native-http", feature = "wasm-http"))]
pub fn default_client_moderate(client: &DefaultClient, req: ModerationRequest) -> Result<ModerationResponse, String> {
    crate::__alef_tokio_runtime().block_on(async {
        client
            .0
            .moderate(req.0)
            .await
            .map_err(|e| e.to_string())
            .map(ModerationResponse)
    })
}
#[cfg(any(feature = "native-http", feature = "wasm-http"))]
pub fn default_client_rerank(client: &DefaultClient, req: RerankRequest) -> Result<RerankResponse, String> {
    crate::__alef_tokio_runtime().block_on(async {
        client
            .0
            .rerank(req.0)
            .await
            .map_err(|e| e.to_string())
            .map(RerankResponse)
    })
}
#[cfg(any(feature = "native-http", feature = "wasm-http"))]
pub fn default_client_search(client: &DefaultClient, req: SearchRequest) -> Result<SearchResponse, String> {
    crate::__alef_tokio_runtime().block_on(async {
        client
            .0
            .search(req.0)
            .await
            .map_err(|e| e.to_string())
            .map(SearchResponse)
    })
}
#[cfg(any(feature = "native-http", feature = "wasm-http"))]
pub fn default_client_ocr(client: &DefaultClient, req: OcrRequest) -> Result<OcrResponse, String> {
    crate::__alef_tokio_runtime()
        .block_on(async { client.0.ocr(req.0).await.map_err(|e| e.to_string()).map(OcrResponse) })
}
#[cfg(any(feature = "native-http", feature = "wasm-http"))]
pub fn default_client_create_file(client: &DefaultClient, req: CreateFileRequest) -> Result<FileObject, String> {
    crate::__alef_tokio_runtime().block_on(async {
        client
            .0
            .create_file(req.0)
            .await
            .map_err(|e| e.to_string())
            .map(FileObject)
    })
}
#[cfg(any(feature = "native-http", feature = "wasm-http"))]
pub fn default_client_retrieve_file(client: &DefaultClient, file_id: String) -> Result<FileObject, String> {
    crate::__alef_tokio_runtime().block_on(async {
        client
            .0
            .retrieve_file(&file_id)
            .await
            .map_err(|e| e.to_string())
            .map(FileObject)
    })
}
#[cfg(any(feature = "native-http", feature = "wasm-http"))]
pub fn default_client_delete_file(client: &DefaultClient, file_id: String) -> Result<DeleteResponse, String> {
    crate::__alef_tokio_runtime().block_on(async {
        client
            .0
            .delete_file(&file_id)
            .await
            .map_err(|e| e.to_string())
            .map(DeleteResponse)
    })
}
#[cfg(any(feature = "native-http", feature = "wasm-http"))]
pub fn default_client_list_files(
    client: &DefaultClient,
    query: Option<FileListQuery>,
) -> Result<FileListResponse, String> {
    crate::__alef_tokio_runtime().block_on(async {
        client
            .0
            .list_files(query.map(|v| v.0))
            .await
            .map_err(|e| e.to_string())
            .map(FileListResponse)
    })
}
#[cfg(any(feature = "native-http", feature = "wasm-http"))]
pub fn default_client_file_content(client: &DefaultClient, file_id: String) -> Result<Vec<u8>, String> {
    crate::__alef_tokio_runtime().block_on(async {
        client
            .0
            .file_content(&file_id)
            .await
            .map_err(|e| e.to_string())
            .map(|b| b.to_vec())
    })
}
#[cfg(any(feature = "native-http", feature = "wasm-http"))]
pub fn default_client_create_batch(client: &DefaultClient, req: CreateBatchRequest) -> Result<BatchObject, String> {
    crate::__alef_tokio_runtime().block_on(async {
        client
            .0
            .create_batch(req.0)
            .await
            .map_err(|e| e.to_string())
            .map(BatchObject)
    })
}
#[cfg(any(feature = "native-http", feature = "wasm-http"))]
pub fn default_client_retrieve_batch(client: &DefaultClient, batch_id: String) -> Result<BatchObject, String> {
    crate::__alef_tokio_runtime().block_on(async {
        client
            .0
            .retrieve_batch(&batch_id)
            .await
            .map_err(|e| e.to_string())
            .map(BatchObject)
    })
}
#[cfg(any(feature = "native-http", feature = "wasm-http"))]
pub fn default_client_list_batches(
    client: &DefaultClient,
    query: Option<BatchListQuery>,
) -> Result<BatchListResponse, String> {
    crate::__alef_tokio_runtime().block_on(async {
        client
            .0
            .list_batches(query.map(|v| v.0))
            .await
            .map_err(|e| e.to_string())
            .map(BatchListResponse)
    })
}
#[cfg(any(feature = "native-http", feature = "wasm-http"))]
pub fn default_client_cancel_batch(client: &DefaultClient, batch_id: String) -> Result<BatchObject, String> {
    crate::__alef_tokio_runtime().block_on(async {
        client
            .0
            .cancel_batch(&batch_id)
            .await
            .map_err(|e| e.to_string())
            .map(BatchObject)
    })
}
#[cfg(any(feature = "native-http", feature = "wasm-http"))]
pub fn default_client_fetch_batch_for_polling(client: &DefaultClient, batch_id: String) -> Result<BatchObject, String> {
    crate::__alef_tokio_runtime().block_on(async {
        client
            .0
            .fetch_batch_for_polling(&batch_id)
            .await
            .map_err(|e| e.to_string())
            .map(BatchObject)
    })
}
#[cfg(any(feature = "native-http", feature = "wasm-http"))]
pub fn default_client_wait_for_batch(
    client: &DefaultClient,
    batch_id: String,
    config: WaitForBatchConfig,
) -> Result<BatchObject, String> {
    crate::__alef_tokio_runtime().block_on(async {
        client
            .0
            .wait_for_batch(&batch_id, config.0)
            .await
            .map_err(|e| e.to_string())
            .map(BatchObject)
    })
}
#[cfg(any(feature = "native-http", feature = "wasm-http"))]
pub fn default_client_create_response(
    client: &DefaultClient,
    req: CreateResponseRequest,
) -> Result<ResponseObject, String> {
    crate::__alef_tokio_runtime().block_on(async {
        client
            .0
            .create_response(req.0)
            .await
            .map_err(|e| e.to_string())
            .map(ResponseObject)
    })
}
#[cfg(any(feature = "native-http", feature = "wasm-http"))]
pub fn default_client_retrieve_response(client: &DefaultClient, response_id: String) -> Result<ResponseObject, String> {
    crate::__alef_tokio_runtime().block_on(async {
        client
            .0
            .retrieve_response(&response_id)
            .await
            .map_err(|e| e.to_string())
            .map(ResponseObject)
    })
}
#[cfg(any(feature = "native-http", feature = "wasm-http"))]
pub fn default_client_cancel_response(client: &DefaultClient, response_id: String) -> Result<ResponseObject, String> {
    crate::__alef_tokio_runtime().block_on(async {
        client
            .0
            .cancel_response(&response_id)
            .await
            .map_err(|e| e.to_string())
            .map(ResponseObject)
    })
}

pub struct CustomProviderConfig(pub liter_llm::provider::custom::CustomProviderConfig);
impl CustomProviderConfig {
    pub fn name(&self) -> String {
        self.0.name.clone()
    }
    pub fn base_url(&self) -> String {
        self.0.base_url.clone()
    }
    pub fn auth_header(&self) -> String {
        serde_json::to_string(&self.0.auth_header).unwrap_or_else(|_| "null".to_string())
    }
    pub fn model_prefixes(&self) -> Vec<String> {
        ::serde_json::to_value(&self.0.model_prefixes)
            .ok()
            .and_then(|j| ::serde_json::from_value(j).ok())
            .unwrap_or_default()
    }
}

pub struct ProviderCapabilities(pub liter_llm::provider::ProviderCapabilities);
impl ProviderCapabilities {
    pub fn new(
        vision: bool,
        reasoning: bool,
        structured_output: bool,
        function_calling: bool,
        audio_in: bool,
        audio_out: bool,
        video_in: bool,
    ) -> ProviderCapabilities {
        ProviderCapabilities(liter_llm::provider::ProviderCapabilities {
            vision,
            reasoning,
            structured_output,
            function_calling,
            audio_in,
            audio_out,
            video_in,
        })
    }
    pub fn vision(&self) -> bool {
        ::serde_json::to_value(&self.0.vision)
            .ok()
            .and_then(|j| ::serde_json::from_value(j).ok())
            .unwrap_or_default()
    }
    pub fn reasoning(&self) -> bool {
        ::serde_json::to_value(&self.0.reasoning)
            .ok()
            .and_then(|j| ::serde_json::from_value(j).ok())
            .unwrap_or_default()
    }
    pub fn structured_output(&self) -> bool {
        ::serde_json::to_value(&self.0.structured_output)
            .ok()
            .and_then(|j| ::serde_json::from_value(j).ok())
            .unwrap_or_default()
    }
    pub fn function_calling(&self) -> bool {
        ::serde_json::to_value(&self.0.function_calling)
            .ok()
            .and_then(|j| ::serde_json::from_value(j).ok())
            .unwrap_or_default()
    }
    pub fn audio_in(&self) -> bool {
        ::serde_json::to_value(&self.0.audio_in)
            .ok()
            .and_then(|j| ::serde_json::from_value(j).ok())
            .unwrap_or_default()
    }
    pub fn audio_out(&self) -> bool {
        ::serde_json::to_value(&self.0.audio_out)
            .ok()
            .and_then(|j| ::serde_json::from_value(j).ok())
            .unwrap_or_default()
    }
    pub fn video_in(&self) -> bool {
        ::serde_json::to_value(&self.0.video_in)
            .ok()
            .and_then(|j| ::serde_json::from_value(j).ok())
            .unwrap_or_default()
    }
}

pub struct ProviderConfig(pub liter_llm::provider::ProviderConfig);
impl ProviderConfig {
    pub fn name(&self) -> String {
        self.0.name.clone()
    }
    pub fn display_name(&self) -> Option<String> {
        self.0.display_name.clone()
    }
    pub fn base_url(&self) -> Option<String> {
        self.0.base_url.clone()
    }
    pub fn auth(&self) -> Option<AuthConfig> {
        self.0.auth.clone().map(AuthConfig)
    }
    pub fn endpoints(&self) -> Option<Vec<String>> {
        self.0.endpoints.as_ref().and_then(|v| {
            ::serde_json::to_value(v)
                .ok()
                .and_then(|j| ::serde_json::from_value(j).ok())
        })
    }
    pub fn model_prefixes(&self) -> Option<Vec<String>> {
        self.0.model_prefixes.as_ref().and_then(|v| {
            ::serde_json::to_value(v)
                .ok()
                .and_then(|j| ::serde_json::from_value(j).ok())
        })
    }
    pub fn param_mappings(&self) -> String {
        serde_json::to_string(&self.0.param_mappings).expect("serializable param_mappings")
    }
}

pub struct AuthConfig(pub liter_llm::provider::AuthConfig);
impl AuthConfig {
    pub fn auth_type(&self) -> String {
        AuthType::from(self.0.auth_type.clone()).to_string()
    }
    pub fn env_var(&self) -> Option<String> {
        self.0.env_var.clone()
    }
}

pub struct BudgetConfig(pub liter_llm::BudgetConfig);
impl BudgetConfig {
    pub fn new(global_limit: Option<f64>, model_limits: String, enforcement: Enforcement) -> BudgetConfig {
        let mut __target: liter_llm::BudgetConfig = ::std::default::Default::default();
        __target.global_limit = global_limit;
        if let Ok(v) = ::serde_json::from_str::<::serde_json::Value>(&model_limits) {
            if let Ok(t) = ::serde_json::from_value(v) {
                __target.model_limits = t;
            }
        }
        // alef: enforcement (Enforcement) is an enum; reverse From not generated — left at default
        BudgetConfig(__target)
    }
    pub fn global_limit(&self) -> Option<f64> {
        self.0.global_limit.as_ref().and_then(|v| {
            ::serde_json::to_value(v)
                .ok()
                .and_then(|j| ::serde_json::from_value(j).ok())
        })
    }
    pub fn model_limits(&self) -> String {
        serde_json::to_string(&self.0.model_limits).expect("serializable model_limits")
    }
    pub fn enforcement(&self) -> String {
        Enforcement::from(self.0.enforcement.clone()).to_string()
    }
}

pub struct CacheConfig(pub liter_llm::CacheConfig);
impl CacheConfig {
    pub fn new(max_entries: usize, ttl: u64, backend: CacheBackend) -> CacheConfig {
        let mut __target: liter_llm::CacheConfig = ::std::default::Default::default();
        __target.max_entries = max_entries;
        __target.ttl = std::time::Duration::from_millis(ttl);
        // alef: backend (CacheBackend) is an enum; reverse From not generated — left at default
        CacheConfig(__target)
    }
    pub fn max_entries(&self) -> usize {
        ::serde_json::to_value(&self.0.max_entries)
            .ok()
            .and_then(|j| ::serde_json::from_value(j).ok())
            .unwrap_or_default()
    }
    pub fn ttl(&self) -> u64 {
        self.0.ttl.as_millis() as u64
    }
    pub fn backend(&self) -> String {
        serde_json::to_string(&self.0.backend).unwrap_or_else(|_| "null".to_string())
    }
}

pub struct SingleflightResult(pub liter_llm::tower::SingleflightResult);

pub struct RateLimitConfig(pub liter_llm::RateLimitConfig);
impl RateLimitConfig {
    pub fn new(rpm: Option<u32>, tpm: Option<u64>, window: u64) -> RateLimitConfig {
        let mut __target: liter_llm::RateLimitConfig = ::std::default::Default::default();
        __target.rpm = rpm;
        __target.tpm = tpm;
        __target.window = std::time::Duration::from_millis(window);
        RateLimitConfig(__target)
    }
    pub fn rpm(&self) -> Option<u32> {
        self.0.rpm.as_ref().and_then(|v| {
            ::serde_json::to_value(v)
                .ok()
                .and_then(|j| ::serde_json::from_value(j).ok())
        })
    }
    pub fn tpm(&self) -> Option<u64> {
        self.0.tpm.as_ref().and_then(|v| {
            ::serde_json::to_value(v)
                .ok()
                .and_then(|j| ::serde_json::from_value(j).ok())
        })
    }
    pub fn window(&self) -> u64 {
        self.0.window.as_millis() as u64
    }
}

pub struct IntentPrototype(pub liter_llm::tower::IntentPrototype);
impl IntentPrototype {
    pub fn name(&self) -> String {
        format!("{:?}", &self.0.name)
    }
    pub fn embedding(&self) -> Vec<f64> {
        self.0.embedding.clone()
    }
    pub fn model(&self) -> String {
        format!("{:?}", &self.0.model)
    }
}

pub fn singleflight_result_noop(client: &SingleflightResult) {
    // No-op method for swift-bridge destructor synthesis
}

pub enum Message {
    System,
    User,
    Assistant,
    Tool,
    Developer,
    Function,
}

impl From<liter_llm::types::Message> for Message {
    fn from(val: liter_llm::types::Message) -> Self {
        match val {
            liter_llm::types::Message::System(..) => Self::System,
            liter_llm::types::Message::User(..) => Self::User,
            liter_llm::types::Message::Assistant(..) => Self::Assistant,
            liter_llm::types::Message::Tool(..) => Self::Tool,
            liter_llm::types::Message::Developer(..) => Self::Developer,
            liter_llm::types::Message::Function(..) => Self::Function,
        }
    }
}

impl Message {
    pub fn to_string(&self) -> String {
        match self {
            Self::System => "system".to_string(),
            Self::User => "user".to_string(),
            Self::Assistant => "assistant".to_string(),
            Self::Tool => "tool".to_string(),
            Self::Developer => "developer".to_string(),
            Self::Function => "function".to_string(),
        }
    }
}

pub enum UserContent {
    Text,
    Parts,
}

impl From<liter_llm::types::UserContent> for UserContent {
    fn from(val: liter_llm::types::UserContent) -> Self {
        match val {
            liter_llm::types::UserContent::Text(..) => Self::Text,
            liter_llm::types::UserContent::Parts(..) => Self::Parts,
        }
    }
}

impl UserContent {
    pub fn to_string(&self) -> String {
        match self {
            Self::Text => "Text".to_string(),
            Self::Parts => "Parts".to_string(),
        }
    }
}

pub enum ContentPart {
    Text,
    ImageUrl,
    Document,
    InputAudio,
}

impl From<liter_llm::types::ContentPart> for ContentPart {
    fn from(val: liter_llm::types::ContentPart) -> Self {
        match val {
            liter_llm::types::ContentPart::Text { .. } => Self::Text,
            liter_llm::types::ContentPart::ImageUrl { .. } => Self::ImageUrl,
            liter_llm::types::ContentPart::Document { .. } => Self::Document,
            liter_llm::types::ContentPart::InputAudio { .. } => Self::InputAudio,
        }
    }
}

impl ContentPart {
    pub fn to_string(&self) -> String {
        match self {
            Self::Text => "text".to_string(),
            Self::ImageUrl => "image_url".to_string(),
            Self::Document => "document".to_string(),
            Self::InputAudio => "input_audio".to_string(),
        }
    }
}

pub enum ImageDetail {
    Low,
    High,
    Auto,
}

impl From<liter_llm::types::ImageDetail> for ImageDetail {
    fn from(val: liter_llm::types::ImageDetail) -> Self {
        match val {
            liter_llm::types::ImageDetail::Low => Self::Low,
            liter_llm::types::ImageDetail::High => Self::High,
            liter_llm::types::ImageDetail::Auto => Self::Auto,
        }
    }
}

impl ImageDetail {
    pub fn to_string(&self) -> String {
        match self {
            Self::Low => "low".to_string(),
            Self::High => "high".to_string(),
            Self::Auto => "auto".to_string(),
        }
    }
}

pub enum AssistantContent {
    Text,
    Parts,
}

impl From<liter_llm::types::AssistantContent> for AssistantContent {
    fn from(val: liter_llm::types::AssistantContent) -> Self {
        match val {
            liter_llm::types::AssistantContent::Text(..) => Self::Text,
            liter_llm::types::AssistantContent::Parts(..) => Self::Parts,
        }
    }
}

impl AssistantContent {
    pub fn to_string(&self) -> String {
        match self {
            Self::Text => "Text".to_string(),
            Self::Parts => "Parts".to_string(),
        }
    }
}

pub enum AssistantPart {
    Text,
    Refusal,
    OutputImage,
    OutputAudio,
}

impl From<liter_llm::types::AssistantPart> for AssistantPart {
    fn from(val: liter_llm::types::AssistantPart) -> Self {
        match val {
            liter_llm::types::AssistantPart::Text { .. } => Self::Text,
            liter_llm::types::AssistantPart::Refusal { .. } => Self::Refusal,
            liter_llm::types::AssistantPart::OutputImage { .. } => Self::OutputImage,
            liter_llm::types::AssistantPart::OutputAudio { .. } => Self::OutputAudio,
        }
    }
}

impl AssistantPart {
    pub fn to_string(&self) -> String {
        match self {
            Self::Text => "text".to_string(),
            Self::Refusal => "refusal".to_string(),
            Self::OutputImage => "output_image".to_string(),
            Self::OutputAudio => "output_audio".to_string(),
        }
    }
}

pub enum ToolType {
    Function,
}

impl From<liter_llm::types::ToolType> for ToolType {
    fn from(val: liter_llm::types::ToolType) -> Self {
        match val {
            liter_llm::types::ToolType::Function => Self::Function,
        }
    }
}

impl ToolType {
    pub fn to_string(&self) -> String {
        match self {
            Self::Function => "function".to_string(),
        }
    }
}

pub enum ToolChoice {
    Mode,
    Specific,
}

impl From<liter_llm::types::ToolChoice> for ToolChoice {
    fn from(val: liter_llm::types::ToolChoice) -> Self {
        match val {
            liter_llm::types::ToolChoice::Mode(..) => Self::Mode,
            liter_llm::types::ToolChoice::Specific(..) => Self::Specific,
        }
    }
}

impl ToolChoice {
    pub fn to_string(&self) -> String {
        match self {
            Self::Mode => "Mode".to_string(),
            Self::Specific => "Specific".to_string(),
        }
    }
}

pub enum ToolChoiceMode {
    Auto,
    Required,
    None,
}

impl From<liter_llm::types::ToolChoiceMode> for ToolChoiceMode {
    fn from(val: liter_llm::types::ToolChoiceMode) -> Self {
        match val {
            liter_llm::types::ToolChoiceMode::Auto => Self::Auto,
            liter_llm::types::ToolChoiceMode::Required => Self::Required,
            liter_llm::types::ToolChoiceMode::None => Self::None,
        }
    }
}

impl ToolChoiceMode {
    pub fn to_string(&self) -> String {
        match self {
            Self::Auto => "auto".to_string(),
            Self::Required => "required".to_string(),
            Self::None => "none".to_string(),
        }
    }
}

pub enum ResponseFormat {
    Text,
    JsonObject,
    JsonSchema,
}

impl From<liter_llm::types::ResponseFormat> for ResponseFormat {
    fn from(val: liter_llm::types::ResponseFormat) -> Self {
        match val {
            liter_llm::types::ResponseFormat::Text => Self::Text,
            liter_llm::types::ResponseFormat::JsonObject => Self::JsonObject,
            liter_llm::types::ResponseFormat::JsonSchema { .. } => Self::JsonSchema,
        }
    }
}

impl ResponseFormat {
    pub fn to_string(&self) -> String {
        match self {
            Self::Text => "text".to_string(),
            Self::JsonObject => "json_object".to_string(),
            Self::JsonSchema => "json_schema".to_string(),
        }
    }
}

pub enum StopSequence {
    Single,
    Multiple,
}

impl From<liter_llm::types::StopSequence> for StopSequence {
    fn from(val: liter_llm::types::StopSequence) -> Self {
        match val {
            liter_llm::types::StopSequence::Single(..) => Self::Single,
            liter_llm::types::StopSequence::Multiple(..) => Self::Multiple,
        }
    }
}

impl StopSequence {
    pub fn to_string(&self) -> String {
        match self {
            Self::Single => "Single".to_string(),
            Self::Multiple => "Multiple".to_string(),
        }
    }
}

pub enum Modality {
    Text,
    Audio,
    Image,
}

impl From<liter_llm::types::Modality> for Modality {
    fn from(val: liter_llm::types::Modality) -> Self {
        match val {
            liter_llm::types::Modality::Text => Self::Text,
            liter_llm::types::Modality::Audio => Self::Audio,
            liter_llm::types::Modality::Image => Self::Image,
        }
    }
}

impl Modality {
    pub fn to_string(&self) -> String {
        match self {
            Self::Text => "text".to_string(),
            Self::Audio => "audio".to_string(),
            Self::Image => "image".to_string(),
        }
    }
}

pub enum FinishReason {
    Stop,
    Length,
    ToolCalls,
    ContentFilter,
    FunctionCall,
    Other,
}

impl From<liter_llm::types::FinishReason> for FinishReason {
    fn from(val: liter_llm::types::FinishReason) -> Self {
        match val {
            liter_llm::types::FinishReason::Stop => Self::Stop,
            liter_llm::types::FinishReason::Length => Self::Length,
            liter_llm::types::FinishReason::ToolCalls => Self::ToolCalls,
            liter_llm::types::FinishReason::ContentFilter => Self::ContentFilter,
            liter_llm::types::FinishReason::FunctionCall => Self::FunctionCall,
            liter_llm::types::FinishReason::Other => Self::Other,
        }
    }
}

impl FinishReason {
    pub fn to_string(&self) -> String {
        match self {
            Self::Stop => "stop".to_string(),
            Self::Length => "length".to_string(),
            Self::ToolCalls => "tool_calls".to_string(),
            Self::ContentFilter => "content_filter".to_string(),
            Self::FunctionCall => "function_call".to_string(),
            Self::Other => "other".to_string(),
        }
    }
}

pub enum ReasoningEffort {
    Low,
    Medium,
    High,
}

impl From<liter_llm::types::ReasoningEffort> for ReasoningEffort {
    fn from(val: liter_llm::types::ReasoningEffort) -> Self {
        match val {
            liter_llm::types::ReasoningEffort::Low => Self::Low,
            liter_llm::types::ReasoningEffort::Medium => Self::Medium,
            liter_llm::types::ReasoningEffort::High => Self::High,
        }
    }
}

impl ReasoningEffort {
    pub fn to_string(&self) -> String {
        match self {
            Self::Low => "low".to_string(),
            Self::Medium => "medium".to_string(),
            Self::High => "high".to_string(),
        }
    }
}

pub enum EmbeddingFormat {
    Float,
    Base64,
}

impl From<liter_llm::types::EmbeddingFormat> for EmbeddingFormat {
    fn from(val: liter_llm::types::EmbeddingFormat) -> Self {
        match val {
            liter_llm::types::EmbeddingFormat::Float => Self::Float,
            liter_llm::types::EmbeddingFormat::Base64 => Self::Base64,
        }
    }
}

impl EmbeddingFormat {
    pub fn to_string(&self) -> String {
        match self {
            Self::Float => "float".to_string(),
            Self::Base64 => "base64".to_string(),
        }
    }
}

pub enum EmbeddingInput {
    Single,
    Multiple,
}

impl From<liter_llm::types::EmbeddingInput> for EmbeddingInput {
    fn from(val: liter_llm::types::EmbeddingInput) -> Self {
        match val {
            liter_llm::types::EmbeddingInput::Single(..) => Self::Single,
            liter_llm::types::EmbeddingInput::Multiple(..) => Self::Multiple,
        }
    }
}

impl EmbeddingInput {
    pub fn to_string(&self) -> String {
        match self {
            Self::Single => "Single".to_string(),
            Self::Multiple => "Multiple".to_string(),
        }
    }
}

pub enum ModerationInput {
    Single,
    Multiple,
}

impl From<liter_llm::types::ModerationInput> for ModerationInput {
    fn from(val: liter_llm::types::ModerationInput) -> Self {
        match val {
            liter_llm::types::ModerationInput::Single(..) => Self::Single,
            liter_llm::types::ModerationInput::Multiple(..) => Self::Multiple,
        }
    }
}

impl ModerationInput {
    pub fn to_string(&self) -> String {
        match self {
            Self::Single => "Single".to_string(),
            Self::Multiple => "Multiple".to_string(),
        }
    }
}

pub enum RerankDocument {
    Text,
    Object,
}

impl From<liter_llm::types::RerankDocument> for RerankDocument {
    fn from(val: liter_llm::types::RerankDocument) -> Self {
        match val {
            liter_llm::types::RerankDocument::Text(..) => Self::Text,
            liter_llm::types::RerankDocument::Object { .. } => Self::Object,
        }
    }
}

impl RerankDocument {
    pub fn to_string(&self) -> String {
        match self {
            Self::Text => "Text".to_string(),
            Self::Object => "Object".to_string(),
        }
    }
}

pub enum OcrDocument {
    Url,
    Base64,
}

impl From<liter_llm::types::OcrDocument> for OcrDocument {
    fn from(val: liter_llm::types::OcrDocument) -> Self {
        match val {
            liter_llm::types::OcrDocument::Url { .. } => Self::Url,
            liter_llm::types::OcrDocument::Base64 { .. } => Self::Base64,
        }
    }
}

impl OcrDocument {
    pub fn to_string(&self) -> String {
        match self {
            Self::Url => "document_url".to_string(),
            Self::Base64 => "base64".to_string(),
        }
    }
}

pub enum FilePurpose {
    Assistants,
    Batch,
    FineTune,
    Vision,
}

impl From<liter_llm::types::FilePurpose> for FilePurpose {
    fn from(val: liter_llm::types::FilePurpose) -> Self {
        match val {
            liter_llm::types::FilePurpose::Assistants => Self::Assistants,
            liter_llm::types::FilePurpose::Batch => Self::Batch,
            liter_llm::types::FilePurpose::FineTune => Self::FineTune,
            liter_llm::types::FilePurpose::Vision => Self::Vision,
        }
    }
}

impl FilePurpose {
    pub fn to_string(&self) -> String {
        match self {
            Self::Assistants => "assistants".to_string(),
            Self::Batch => "batch".to_string(),
            Self::FineTune => "fine-tune".to_string(),
            Self::Vision => "vision".to_string(),
        }
    }
}

pub enum BatchStatus {
    Validating,
    Failed,
    InProgress,
    Finalizing,
    Completed,
    Expired,
    Cancelling,
    Cancelled,
}

impl From<liter_llm::types::BatchStatus> for BatchStatus {
    fn from(val: liter_llm::types::BatchStatus) -> Self {
        match val {
            liter_llm::types::BatchStatus::Validating => Self::Validating,
            liter_llm::types::BatchStatus::Failed => Self::Failed,
            liter_llm::types::BatchStatus::InProgress => Self::InProgress,
            liter_llm::types::BatchStatus::Finalizing => Self::Finalizing,
            liter_llm::types::BatchStatus::Completed => Self::Completed,
            liter_llm::types::BatchStatus::Expired => Self::Expired,
            liter_llm::types::BatchStatus::Cancelling => Self::Cancelling,
            liter_llm::types::BatchStatus::Cancelled => Self::Cancelled,
        }
    }
}

impl BatchStatus {
    pub fn to_string(&self) -> String {
        match self {
            Self::Validating => "validating".to_string(),
            Self::Failed => "failed".to_string(),
            Self::InProgress => "in_progress".to_string(),
            Self::Finalizing => "finalizing".to_string(),
            Self::Completed => "completed".to_string(),
            Self::Expired => "expired".to_string(),
            Self::Cancelling => "cancelling".to_string(),
            Self::Cancelled => "cancelled".to_string(),
        }
    }
}

pub enum AuthHeaderFormat {
    Bearer,
    ApiKey,
    None,
}

impl From<liter_llm::provider::custom::AuthHeaderFormat> for AuthHeaderFormat {
    fn from(val: liter_llm::provider::custom::AuthHeaderFormat) -> Self {
        match val {
            liter_llm::provider::custom::AuthHeaderFormat::Bearer => Self::Bearer,
            liter_llm::provider::custom::AuthHeaderFormat::ApiKey(..) => Self::ApiKey,
            liter_llm::provider::custom::AuthHeaderFormat::None => Self::None,
        }
    }
}

impl AuthHeaderFormat {
    pub fn to_string(&self) -> String {
        match self {
            Self::Bearer => "Bearer".to_string(),
            Self::ApiKey => "ApiKey".to_string(),
            Self::None => "None".to_string(),
        }
    }
}

pub enum StreamFormat {
    Sse,
    AwsEventStream,
}

impl From<liter_llm::provider::StreamFormat> for StreamFormat {
    fn from(val: liter_llm::provider::StreamFormat) -> Self {
        match val {
            liter_llm::provider::StreamFormat::Sse => Self::Sse,
            liter_llm::provider::StreamFormat::AwsEventStream => Self::AwsEventStream,
        }
    }
}

impl StreamFormat {
    pub fn to_string(&self) -> String {
        match self {
            Self::Sse => "sse".to_string(),
            Self::AwsEventStream => "aws_event_stream".to_string(),
        }
    }
}

pub enum AuthType {
    Bearer,
    ApiKey,
    None,
    Unknown,
}

impl From<liter_llm::provider::AuthType> for AuthType {
    fn from(val: liter_llm::provider::AuthType) -> Self {
        match val {
            liter_llm::provider::AuthType::Bearer => Self::Bearer,
            liter_llm::provider::AuthType::ApiKey => Self::ApiKey,
            liter_llm::provider::AuthType::None => Self::None,
            liter_llm::provider::AuthType::Unknown => Self::Unknown,
        }
    }
}

impl AuthType {
    pub fn to_string(&self) -> String {
        match self {
            Self::Bearer => "bearer".to_string(),
            Self::ApiKey => "api-key".to_string(),
            Self::None => "none".to_string(),
            Self::Unknown => "unknown".to_string(),
        }
    }
}

pub enum Enforcement {
    Hard,
    Soft,
}

impl From<liter_llm::Enforcement> for Enforcement {
    fn from(val: liter_llm::Enforcement) -> Self {
        match val {
            liter_llm::Enforcement::Hard => Self::Hard,
            liter_llm::Enforcement::Soft => Self::Soft,
        }
    }
}

impl Enforcement {
    pub fn to_string(&self) -> String {
        match self {
            Self::Hard => "Hard".to_string(),
            Self::Soft => "Soft".to_string(),
        }
    }
}

pub enum CacheBackend {
    Memory,
    OpenDal,
}

impl From<liter_llm::CacheBackend> for CacheBackend {
    fn from(val: liter_llm::CacheBackend) -> Self {
        match val {
            liter_llm::CacheBackend::Memory => Self::Memory,
            #[cfg(feature = "opendal-cache")]
            liter_llm::CacheBackend::OpenDal { .. } => Self::OpenDal,
            _ => unreachable!("bridge enum variant of CacheBackend not exposed in binding"),
        }
    }
}

impl CacheBackend {
    pub fn to_string(&self) -> String {
        match self {
            Self::Memory => "memory".to_string(),
            Self::OpenDal => "open_dal".to_string(),
        }
    }
}

pub enum CircuitState {
    Closed,
    Open,
    HalfOpen,
}

impl From<liter_llm::tower::CircuitState> for CircuitState {
    fn from(val: liter_llm::tower::CircuitState) -> Self {
        match val {
            liter_llm::tower::CircuitState::Closed => Self::Closed,
            liter_llm::tower::CircuitState::Open => Self::Open,
            liter_llm::tower::CircuitState::HalfOpen => Self::HalfOpen,
        }
    }
}

impl CircuitState {
    pub fn to_string(&self) -> String {
        match self {
            Self::Closed => "Closed".to_string(),
            Self::Open => "Open".to_string(),
            Self::HalfOpen => "HalfOpen".to_string(),
        }
    }
}

pub enum HealthStatus {
    Healthy,
    Unhealthy,
}

impl From<liter_llm::tower::HealthStatus> for HealthStatus {
    fn from(val: liter_llm::tower::HealthStatus) -> Self {
        match val {
            liter_llm::tower::HealthStatus::Healthy => Self::Healthy,
            liter_llm::tower::HealthStatus::Unhealthy => Self::Unhealthy,
        }
    }
}

impl HealthStatus {
    pub fn to_string(&self) -> String {
        match self {
            Self::Healthy => "Healthy".to_string(),
            Self::Unhealthy => "Unhealthy".to_string(),
        }
    }
}

pub fn create_client(
    api_key: String,
    base_url: Option<String>,
    timeout_secs: Option<u64>,
    max_retries: Option<u32>,
    model_hint: Option<String>,
) -> Result<DefaultClient, String> {
    liter_llm::create_client(api_key, base_url, timeout_secs, max_retries, model_hint)
        .map_err(|e| e.to_string())
        .map(DefaultClient)
}

pub fn create_client_from_json(json: String) -> Result<DefaultClient, String> {
    liter_llm::create_client_from_json(&json)
        .map_err(|e| e.to_string())
        .map(DefaultClient)
}

pub fn encode_data_url(bytes: Vec<u8>, mime: Option<String>) -> String {
    liter_llm::image::encode_data_url(&bytes, mime.as_deref()).to_string()
}

pub fn decode_data_url(url: String) -> Option<DecodedDataUrl> {
    (liter_llm::image::decode_data_url(&url)).map(DecodedDataUrl)
}

pub fn register_custom_provider(config: CustomProviderConfig) -> Result<(), String> {
    liter_llm::provider::custom::register_custom_provider(config.0).map_err(|e| e.to_string())
}

pub fn unregister_custom_provider(name: String) -> Result<bool, String> {
    liter_llm::provider::custom::unregister_custom_provider(&name).map_err(|e| e.to_string())
}

pub fn capabilities(provider_name: String) -> ProviderCapabilities {
    ProviderCapabilities(liter_llm::provider::capabilities(&provider_name))
}

pub fn complex_provider_names() -> Result<Vec<String>, String> {
    liter_llm::provider::complex_provider_names()
        .map_err(|e| e.to_string())
        .map(|v| v.iter().map(|s| s.to_string()).collect::<Vec<_>>())
}

pub fn completion_cost(model: String, prompt_tokens: u64, completion_tokens: u64) -> String {
    serde_json::to_string(&(liter_llm::completion_cost(&model, prompt_tokens, completion_tokens)))
        .expect("serializable return")
}

pub fn completion_cost_with_cache(
    model: String,
    prompt_tokens: u64,
    cached_tokens: u64,
    completion_tokens: u64,
) -> String {
    serde_json::to_string(
        &(liter_llm::completion_cost_with_cache(&model, prompt_tokens, cached_tokens, completion_tokens)),
    )
    .expect("serializable return")
}

pub fn clear() {
    liter_llm::guardrail::registry::clear();
}

pub fn count_tokens(model: String, text: String) -> Result<usize, String> {
    liter_llm::count_tokens(&model, &text).map_err(|e| e.to_string())
}

pub fn count_request_tokens(model: String, req: ChatCompletionRequest) -> Result<usize, String> {
    liter_llm::count_request_tokens(&model, &req.0).map_err(|e| e.to_string())
}

pub fn check_bound(context: String, current_len: usize, incoming: usize, limit: usize) -> Result<(), String> {
    liter_llm::util::bounds::check_bound(&context, current_len, incoming, limit).map_err(|e| e.to_string())
}

#[cfg(any(
    all(feature = "native-http", not(target_os = "windows")),
    all(feature = "native-http", target_os = "windows")
))]
pub fn ensure_crypto_provider() {
    liter_llm::ensure_crypto_provider();
}

/// Opaque handle holding a reference to the process-wide tokio runtime
/// and a boxed `ChatCompletionChunk` stream.
///
/// Created by `default_client_chat_stream_start`, advanced via `next()`. Drop runs when the
/// Swift handle goes out of scope (swift-bridge generates the matching
/// `deinit`), so explicit cleanup from Swift is unnecessary.
///
/// Items are JSON-encoded at the bridge boundary because swift-bridge's
/// `Option<OpaqueRust>` support varies across versions, while `Result<String,
/// String>` is well-tested. An empty string `""` is the EOF sentinel -
/// no valid JSON value is the empty string.
#[allow(clippy::type_complexity)]
pub struct DefaultClientChatStreamStreamHandle {
    _rt: ::tokio::runtime::Handle,
    stream: ::std::sync::Mutex<
        Option<
            ::futures_util::stream::BoxStream<
                'static,
                Result<liter_llm::ChatCompletionChunk, Box<dyn ::std::error::Error + Send + Sync + 'static>>,
            >,
        >,
    >,
}
/// Start a streaming `DefaultClient::chat_stream` request.
///
/// Returns a fresh `DefaultClientChatStreamStreamHandle` whose ownership transfers to the
/// Swift caller (swift-bridge boxes the handle internally).
pub fn default_client_chat_stream_start(
    client: &DefaultClient,
    req: &ChatCompletionRequest,
) -> Result<DefaultClientChatStreamStreamHandle, String> {
    use ::futures_util::StreamExt;
    let rt = crate::__alef_tokio_runtime();
    let raw = rt.block_on(async { client.0.chat_stream(req.0.clone()).await.map_err(|e| e.to_string()) })?;
    let erased: ::futures_util::stream::BoxStream<
        'static,
        Result<liter_llm::ChatCompletionChunk, Box<dyn ::std::error::Error + Send + Sync + 'static>>,
    > = Box::pin(raw.map(|r| r.map_err(|e| Box::new(e) as Box<dyn ::std::error::Error + Send + Sync + 'static>)));
    Ok(DefaultClientChatStreamStreamHandle {
        _rt: rt.handle().clone(),
        stream: ::std::sync::Mutex::new(Some(erased)),
    })
}
#[allow(clippy::should_implement_trait)]
impl DefaultClientChatStreamStreamHandle {
    /// Advance the stream and return the next chunk JSON, or `""` on clean
    /// end-of-stream. Returns `Err(message)` on a stream-level error.
    pub fn next(&mut self) -> Result<String, String> {
        let mut guard = self
            .stream
            .lock()
            .map_err(|_| "DefaultClientChatStreamStreamHandle::next: stream mutex poisoned".to_string())?;
        let stream = match guard.as_mut() {
            Some(s) => s,
            None => return Ok(String::new()),
        };
        use ::futures_util::StreamExt;
        match crate::__alef_tokio_runtime().block_on(stream.next()) {
            Some(Ok(item)) => ::serde_json::to_string(&item).map_err(|e| e.to_string()),
            Some(Err(e)) => {
                *guard = None;
                Err(e.to_string())
            }
            None => {
                *guard = None;
                Ok(String::new())
            }
        }
    }
}
pub fn chat_completion_request_from_json(json: String) -> Result<ChatCompletionRequest, String> {
    serde_json::from_str::<liter_llm::types::ChatCompletionRequest>(&json)
        .map(ChatCompletionRequest)
        .map_err(|e| e.to_string())
}
pub fn chat_completion_chunk_from_json(json: String) -> Result<ChatCompletionChunk, String> {
    serde_json::from_str::<liter_llm::types::ChatCompletionChunk>(&json)
        .map(ChatCompletionChunk)
        .map_err(|e| e.to_string())
}
pub fn embedding_request_from_json(json: String) -> Result<EmbeddingRequest, String> {
    serde_json::from_str::<liter_llm::types::EmbeddingRequest>(&json)
        .map(EmbeddingRequest)
        .map_err(|e| e.to_string())
}
pub fn create_image_request_from_json(json: String) -> Result<CreateImageRequest, String> {
    serde_json::from_str::<liter_llm::types::CreateImageRequest>(&json)
        .map(CreateImageRequest)
        .map_err(|e| e.to_string())
}
pub fn create_speech_request_from_json(json: String) -> Result<CreateSpeechRequest, String> {
    serde_json::from_str::<liter_llm::types::CreateSpeechRequest>(&json)
        .map(CreateSpeechRequest)
        .map_err(|e| e.to_string())
}
pub fn create_transcription_request_from_json(json: String) -> Result<CreateTranscriptionRequest, String> {
    serde_json::from_str::<liter_llm::types::CreateTranscriptionRequest>(&json)
        .map(CreateTranscriptionRequest)
        .map_err(|e| e.to_string())
}
pub fn moderation_request_from_json(json: String) -> Result<ModerationRequest, String> {
    serde_json::from_str::<liter_llm::types::ModerationRequest>(&json)
        .map(ModerationRequest)
        .map_err(|e| e.to_string())
}
pub fn rerank_request_from_json(json: String) -> Result<RerankRequest, String> {
    serde_json::from_str::<liter_llm::types::RerankRequest>(&json)
        .map(RerankRequest)
        .map_err(|e| e.to_string())
}
pub fn search_request_from_json(json: String) -> Result<SearchRequest, String> {
    serde_json::from_str::<liter_llm::types::SearchRequest>(&json)
        .map(SearchRequest)
        .map_err(|e| e.to_string())
}
pub fn ocr_request_from_json(json: String) -> Result<OcrRequest, String> {
    serde_json::from_str::<liter_llm::types::OcrRequest>(&json)
        .map(OcrRequest)
        .map_err(|e| e.to_string())
}
pub fn create_file_request_from_json(json: String) -> Result<CreateFileRequest, String> {
    serde_json::from_str::<liter_llm::types::CreateFileRequest>(&json)
        .map(CreateFileRequest)
        .map_err(|e| e.to_string())
}
pub fn file_list_query_from_json(json: String) -> Result<FileListQuery, String> {
    serde_json::from_str::<liter_llm::types::FileListQuery>(&json)
        .map(FileListQuery)
        .map_err(|e| e.to_string())
}
pub fn create_batch_request_from_json(json: String) -> Result<CreateBatchRequest, String> {
    serde_json::from_str::<liter_llm::types::CreateBatchRequest>(&json)
        .map(CreateBatchRequest)
        .map_err(|e| e.to_string())
}
pub fn batch_list_query_from_json(json: String) -> Result<BatchListQuery, String> {
    serde_json::from_str::<liter_llm::types::BatchListQuery>(&json)
        .map(BatchListQuery)
        .map_err(|e| e.to_string())
}
pub fn create_response_request_from_json(json: String) -> Result<CreateResponseRequest, String> {
    serde_json::from_str::<liter_llm::types::CreateResponseRequest>(&json)
        .map(CreateResponseRequest)
        .map_err(|e| e.to_string())
}
pub fn wait_for_batch_config_from_json(json: String) -> Result<WaitForBatchConfig, String> {
    serde_json::from_str::<liter_llm::client::WaitForBatchConfig>(&json)
        .map(WaitForBatchConfig)
        .map_err(|e| e.to_string())
}
pub fn custom_provider_config_from_json(json: String) -> Result<CustomProviderConfig, String> {
    serde_json::from_str::<liter_llm::provider::custom::CustomProviderConfig>(&json)
        .map(CustomProviderConfig)
        .map_err(|e| e.to_string())
}
pub fn system_message_from_json(json: String) -> Result<SystemMessage, String> {
    serde_json::from_str::<liter_llm::types::SystemMessage>(&json)
        .map(SystemMessage)
        .map_err(|e| e.to_string())
}
pub fn user_message_from_json(json: String) -> Result<UserMessage, String> {
    serde_json::from_str::<liter_llm::types::UserMessage>(&json)
        .map(UserMessage)
        .map_err(|e| e.to_string())
}
pub fn image_url_from_json(json: String) -> Result<ImageUrl, String> {
    serde_json::from_str::<liter_llm::types::ImageUrl>(&json)
        .map(ImageUrl)
        .map_err(|e| e.to_string())
}
pub fn document_content_from_json(json: String) -> Result<DocumentContent, String> {
    serde_json::from_str::<liter_llm::types::DocumentContent>(&json)
        .map(DocumentContent)
        .map_err(|e| e.to_string())
}
pub fn audio_content_from_json(json: String) -> Result<AudioContent, String> {
    serde_json::from_str::<liter_llm::types::AudioContent>(&json)
        .map(AudioContent)
        .map_err(|e| e.to_string())
}
pub fn assistant_message_from_json(json: String) -> Result<AssistantMessage, String> {
    serde_json::from_str::<liter_llm::types::AssistantMessage>(&json)
        .map(AssistantMessage)
        .map_err(|e| e.to_string())
}
pub fn tool_message_from_json(json: String) -> Result<ToolMessage, String> {
    serde_json::from_str::<liter_llm::types::ToolMessage>(&json)
        .map(ToolMessage)
        .map_err(|e| e.to_string())
}
pub fn developer_message_from_json(json: String) -> Result<DeveloperMessage, String> {
    serde_json::from_str::<liter_llm::types::DeveloperMessage>(&json)
        .map(DeveloperMessage)
        .map_err(|e| e.to_string())
}
pub fn function_message_from_json(json: String) -> Result<FunctionMessage, String> {
    serde_json::from_str::<liter_llm::types::FunctionMessage>(&json)
        .map(FunctionMessage)
        .map_err(|e| e.to_string())
}
pub fn chat_completion_tool_from_json(json: String) -> Result<ChatCompletionTool, String> {
    serde_json::from_str::<liter_llm::types::ChatCompletionTool>(&json)
        .map(ChatCompletionTool)
        .map_err(|e| e.to_string())
}
pub fn function_definition_from_json(json: String) -> Result<FunctionDefinition, String> {
    serde_json::from_str::<liter_llm::types::FunctionDefinition>(&json)
        .map(FunctionDefinition)
        .map_err(|e| e.to_string())
}
pub fn tool_call_from_json(json: String) -> Result<ToolCall, String> {
    serde_json::from_str::<liter_llm::types::ToolCall>(&json)
        .map(ToolCall)
        .map_err(|e| e.to_string())
}
pub fn function_call_from_json(json: String) -> Result<FunctionCall, String> {
    serde_json::from_str::<liter_llm::types::FunctionCall>(&json)
        .map(FunctionCall)
        .map_err(|e| e.to_string())
}
pub fn specific_tool_choice_from_json(json: String) -> Result<SpecificToolChoice, String> {
    serde_json::from_str::<liter_llm::types::SpecificToolChoice>(&json)
        .map(SpecificToolChoice)
        .map_err(|e| e.to_string())
}
pub fn specific_function_from_json(json: String) -> Result<SpecificFunction, String> {
    serde_json::from_str::<liter_llm::types::SpecificFunction>(&json)
        .map(SpecificFunction)
        .map_err(|e| e.to_string())
}
pub fn json_schema_format_from_json(json: String) -> Result<JsonSchemaFormat, String> {
    serde_json::from_str::<liter_llm::types::JsonSchemaFormat>(&json)
        .map(JsonSchemaFormat)
        .map_err(|e| e.to_string())
}
pub fn usage_from_json(json: String) -> Result<Usage, String> {
    serde_json::from_str::<liter_llm::types::Usage>(&json)
        .map(Usage)
        .map_err(|e| e.to_string())
}
pub fn prompt_tokens_details_from_json(json: String) -> Result<PromptTokensDetails, String> {
    serde_json::from_str::<liter_llm::types::PromptTokensDetails>(&json)
        .map(PromptTokensDetails)
        .map_err(|e| e.to_string())
}
pub fn stream_options_from_json(json: String) -> Result<StreamOptions, String> {
    serde_json::from_str::<liter_llm::types::StreamOptions>(&json)
        .map(StreamOptions)
        .map_err(|e| e.to_string())
}
pub fn chat_completion_response_from_json(json: String) -> Result<ChatCompletionResponse, String> {
    serde_json::from_str::<liter_llm::types::ChatCompletionResponse>(&json)
        .map(ChatCompletionResponse)
        .map_err(|e| e.to_string())
}
pub fn choice_from_json(json: String) -> Result<Choice, String> {
    serde_json::from_str::<liter_llm::types::Choice>(&json)
        .map(Choice)
        .map_err(|e| e.to_string())
}
pub fn stream_choice_from_json(json: String) -> Result<StreamChoice, String> {
    serde_json::from_str::<liter_llm::types::StreamChoice>(&json)
        .map(StreamChoice)
        .map_err(|e| e.to_string())
}
pub fn stream_delta_from_json(json: String) -> Result<StreamDelta, String> {
    serde_json::from_str::<liter_llm::types::StreamDelta>(&json)
        .map(StreamDelta)
        .map_err(|e| e.to_string())
}
pub fn stream_tool_call_from_json(json: String) -> Result<StreamToolCall, String> {
    serde_json::from_str::<liter_llm::types::StreamToolCall>(&json)
        .map(StreamToolCall)
        .map_err(|e| e.to_string())
}
pub fn stream_function_call_from_json(json: String) -> Result<StreamFunctionCall, String> {
    serde_json::from_str::<liter_llm::types::StreamFunctionCall>(&json)
        .map(StreamFunctionCall)
        .map_err(|e| e.to_string())
}
pub fn embedding_response_from_json(json: String) -> Result<EmbeddingResponse, String> {
    serde_json::from_str::<liter_llm::types::EmbeddingResponse>(&json)
        .map(EmbeddingResponse)
        .map_err(|e| e.to_string())
}
pub fn embedding_object_from_json(json: String) -> Result<EmbeddingObject, String> {
    serde_json::from_str::<liter_llm::types::EmbeddingObject>(&json)
        .map(EmbeddingObject)
        .map_err(|e| e.to_string())
}
pub fn images_response_from_json(json: String) -> Result<ImagesResponse, String> {
    serde_json::from_str::<liter_llm::types::ImagesResponse>(&json)
        .map(ImagesResponse)
        .map_err(|e| e.to_string())
}
pub fn image_from_json(json: String) -> Result<Image, String> {
    serde_json::from_str::<liter_llm::types::Image>(&json)
        .map(Image)
        .map_err(|e| e.to_string())
}
pub fn decoded_data_url_from_json(json: String) -> Result<DecodedDataUrl, String> {
    serde_json::from_str::<liter_llm::image::DecodedDataUrl>(&json)
        .map(DecodedDataUrl)
        .map_err(|e| e.to_string())
}
pub fn transcription_response_from_json(json: String) -> Result<TranscriptionResponse, String> {
    serde_json::from_str::<liter_llm::types::TranscriptionResponse>(&json)
        .map(TranscriptionResponse)
        .map_err(|e| e.to_string())
}
pub fn transcription_segment_from_json(json: String) -> Result<TranscriptionSegment, String> {
    serde_json::from_str::<liter_llm::types::TranscriptionSegment>(&json)
        .map(TranscriptionSegment)
        .map_err(|e| e.to_string())
}
pub fn moderation_response_from_json(json: String) -> Result<ModerationResponse, String> {
    serde_json::from_str::<liter_llm::types::ModerationResponse>(&json)
        .map(ModerationResponse)
        .map_err(|e| e.to_string())
}
pub fn moderation_result_from_json(json: String) -> Result<ModerationResult, String> {
    serde_json::from_str::<liter_llm::types::ModerationResult>(&json)
        .map(ModerationResult)
        .map_err(|e| e.to_string())
}
pub fn moderation_categories_from_json(json: String) -> Result<ModerationCategories, String> {
    serde_json::from_str::<liter_llm::types::ModerationCategories>(&json)
        .map(ModerationCategories)
        .map_err(|e| e.to_string())
}
pub fn moderation_category_scores_from_json(json: String) -> Result<ModerationCategoryScores, String> {
    serde_json::from_str::<liter_llm::types::ModerationCategoryScores>(&json)
        .map(ModerationCategoryScores)
        .map_err(|e| e.to_string())
}
pub fn rerank_response_from_json(json: String) -> Result<RerankResponse, String> {
    serde_json::from_str::<liter_llm::types::RerankResponse>(&json)
        .map(RerankResponse)
        .map_err(|e| e.to_string())
}
pub fn rerank_result_from_json(json: String) -> Result<RerankResult, String> {
    serde_json::from_str::<liter_llm::types::RerankResult>(&json)
        .map(RerankResult)
        .map_err(|e| e.to_string())
}
pub fn rerank_result_document_from_json(json: String) -> Result<RerankResultDocument, String> {
    serde_json::from_str::<liter_llm::types::RerankResultDocument>(&json)
        .map(RerankResultDocument)
        .map_err(|e| e.to_string())
}
pub fn search_response_from_json(json: String) -> Result<SearchResponse, String> {
    serde_json::from_str::<liter_llm::types::SearchResponse>(&json)
        .map(SearchResponse)
        .map_err(|e| e.to_string())
}
pub fn search_result_from_json(json: String) -> Result<SearchResult, String> {
    serde_json::from_str::<liter_llm::types::SearchResult>(&json)
        .map(SearchResult)
        .map_err(|e| e.to_string())
}
pub fn ocr_response_from_json(json: String) -> Result<OcrResponse, String> {
    serde_json::from_str::<liter_llm::types::OcrResponse>(&json)
        .map(OcrResponse)
        .map_err(|e| e.to_string())
}
pub fn ocr_page_from_json(json: String) -> Result<OcrPage, String> {
    serde_json::from_str::<liter_llm::types::OcrPage>(&json)
        .map(OcrPage)
        .map_err(|e| e.to_string())
}
pub fn ocr_image_from_json(json: String) -> Result<OcrImage, String> {
    serde_json::from_str::<liter_llm::types::OcrImage>(&json)
        .map(OcrImage)
        .map_err(|e| e.to_string())
}
pub fn page_dimensions_from_json(json: String) -> Result<PageDimensions, String> {
    serde_json::from_str::<liter_llm::types::PageDimensions>(&json)
        .map(PageDimensions)
        .map_err(|e| e.to_string())
}
pub fn models_list_response_from_json(json: String) -> Result<ModelsListResponse, String> {
    serde_json::from_str::<liter_llm::types::ModelsListResponse>(&json)
        .map(ModelsListResponse)
        .map_err(|e| e.to_string())
}
pub fn model_object_from_json(json: String) -> Result<ModelObject, String> {
    serde_json::from_str::<liter_llm::types::ModelObject>(&json)
        .map(ModelObject)
        .map_err(|e| e.to_string())
}
pub fn file_object_from_json(json: String) -> Result<FileObject, String> {
    serde_json::from_str::<liter_llm::types::FileObject>(&json)
        .map(FileObject)
        .map_err(|e| e.to_string())
}
pub fn file_list_response_from_json(json: String) -> Result<FileListResponse, String> {
    serde_json::from_str::<liter_llm::types::FileListResponse>(&json)
        .map(FileListResponse)
        .map_err(|e| e.to_string())
}
pub fn delete_response_from_json(json: String) -> Result<DeleteResponse, String> {
    serde_json::from_str::<liter_llm::types::DeleteResponse>(&json)
        .map(DeleteResponse)
        .map_err(|e| e.to_string())
}
pub fn batch_object_from_json(json: String) -> Result<BatchObject, String> {
    serde_json::from_str::<liter_llm::types::BatchObject>(&json)
        .map(BatchObject)
        .map_err(|e| e.to_string())
}
pub fn batch_request_counts_from_json(json: String) -> Result<BatchRequestCounts, String> {
    serde_json::from_str::<liter_llm::types::BatchRequestCounts>(&json)
        .map(BatchRequestCounts)
        .map_err(|e| e.to_string())
}
pub fn batch_list_response_from_json(json: String) -> Result<BatchListResponse, String> {
    serde_json::from_str::<liter_llm::types::BatchListResponse>(&json)
        .map(BatchListResponse)
        .map_err(|e| e.to_string())
}
pub fn response_tool_from_json(json: String) -> Result<ResponseTool, String> {
    serde_json::from_str::<liter_llm::types::ResponseTool>(&json)
        .map(ResponseTool)
        .map_err(|e| e.to_string())
}
pub fn response_object_from_json(json: String) -> Result<ResponseObject, String> {
    serde_json::from_str::<liter_llm::types::ResponseObject>(&json)
        .map(ResponseObject)
        .map_err(|e| e.to_string())
}
pub fn response_output_item_from_json(json: String) -> Result<ResponseOutputItem, String> {
    serde_json::from_str::<liter_llm::types::ResponseOutputItem>(&json)
        .map(ResponseOutputItem)
        .map_err(|e| e.to_string())
}
pub fn response_usage_from_json(json: String) -> Result<ResponseUsage, String> {
    serde_json::from_str::<liter_llm::types::ResponseUsage>(&json)
        .map(ResponseUsage)
        .map_err(|e| e.to_string())
}
pub fn provider_capabilities_from_json(json: String) -> Result<ProviderCapabilities, String> {
    serde_json::from_str::<liter_llm::provider::ProviderCapabilities>(&json)
        .map(ProviderCapabilities)
        .map_err(|e| e.to_string())
}
pub fn provider_config_from_json(json: String) -> Result<ProviderConfig, String> {
    serde_json::from_str::<liter_llm::provider::ProviderConfig>(&json)
        .map(ProviderConfig)
        .map_err(|e| e.to_string())
}
pub fn auth_config_from_json(json: String) -> Result<AuthConfig, String> {
    serde_json::from_str::<liter_llm::provider::AuthConfig>(&json)
        .map(AuthConfig)
        .map_err(|e| e.to_string())
}
pub fn budget_config_from_json(json: String) -> Result<BudgetConfig, String> {
    serde_json::from_str::<liter_llm::BudgetConfig>(&json)
        .map(BudgetConfig)
        .map_err(|e| e.to_string())
}
pub fn cache_config_from_json(json: String) -> Result<CacheConfig, String> {
    serde_json::from_str::<liter_llm::CacheConfig>(&json)
        .map(CacheConfig)
        .map_err(|e| e.to_string())
}
pub fn rate_limit_config_from_json(json: String) -> Result<RateLimitConfig, String> {
    serde_json::from_str::<liter_llm::RateLimitConfig>(&json)
        .map(RateLimitConfig)
        .map_err(|e| e.to_string())
}
pub fn message_from_json(json: String) -> Result<Message, String> {
    serde_json::from_str::<liter_llm::types::Message>(&json)
        .map(Message::from)
        .map_err(|e| e.to_string())
}
pub fn user_content_from_json(json: String) -> Result<UserContent, String> {
    serde_json::from_str::<liter_llm::types::UserContent>(&json)
        .map(UserContent::from)
        .map_err(|e| e.to_string())
}
pub fn content_part_from_json(json: String) -> Result<ContentPart, String> {
    serde_json::from_str::<liter_llm::types::ContentPart>(&json)
        .map(ContentPart::from)
        .map_err(|e| e.to_string())
}
pub fn image_detail_from_json(json: String) -> Result<ImageDetail, String> {
    serde_json::from_str::<liter_llm::types::ImageDetail>(&json)
        .map(ImageDetail::from)
        .map_err(|e| e.to_string())
}
pub fn assistant_content_from_json(json: String) -> Result<AssistantContent, String> {
    serde_json::from_str::<liter_llm::types::AssistantContent>(&json)
        .map(AssistantContent::from)
        .map_err(|e| e.to_string())
}
pub fn assistant_part_from_json(json: String) -> Result<AssistantPart, String> {
    serde_json::from_str::<liter_llm::types::AssistantPart>(&json)
        .map(AssistantPart::from)
        .map_err(|e| e.to_string())
}
pub fn tool_type_from_json(json: String) -> Result<ToolType, String> {
    serde_json::from_str::<liter_llm::types::ToolType>(&json)
        .map(ToolType::from)
        .map_err(|e| e.to_string())
}
pub fn tool_choice_from_json(json: String) -> Result<ToolChoice, String> {
    serde_json::from_str::<liter_llm::types::ToolChoice>(&json)
        .map(ToolChoice::from)
        .map_err(|e| e.to_string())
}
pub fn tool_choice_mode_from_json(json: String) -> Result<ToolChoiceMode, String> {
    serde_json::from_str::<liter_llm::types::ToolChoiceMode>(&json)
        .map(ToolChoiceMode::from)
        .map_err(|e| e.to_string())
}
pub fn response_format_from_json(json: String) -> Result<ResponseFormat, String> {
    serde_json::from_str::<liter_llm::types::ResponseFormat>(&json)
        .map(ResponseFormat::from)
        .map_err(|e| e.to_string())
}
pub fn stop_sequence_from_json(json: String) -> Result<StopSequence, String> {
    serde_json::from_str::<liter_llm::types::StopSequence>(&json)
        .map(StopSequence::from)
        .map_err(|e| e.to_string())
}
pub fn modality_from_json(json: String) -> Result<Modality, String> {
    serde_json::from_str::<liter_llm::types::Modality>(&json)
        .map(Modality::from)
        .map_err(|e| e.to_string())
}
pub fn finish_reason_from_json(json: String) -> Result<FinishReason, String> {
    serde_json::from_str::<liter_llm::types::FinishReason>(&json)
        .map(FinishReason::from)
        .map_err(|e| e.to_string())
}
pub fn reasoning_effort_from_json(json: String) -> Result<ReasoningEffort, String> {
    serde_json::from_str::<liter_llm::types::ReasoningEffort>(&json)
        .map(ReasoningEffort::from)
        .map_err(|e| e.to_string())
}
pub fn embedding_format_from_json(json: String) -> Result<EmbeddingFormat, String> {
    serde_json::from_str::<liter_llm::types::EmbeddingFormat>(&json)
        .map(EmbeddingFormat::from)
        .map_err(|e| e.to_string())
}
pub fn embedding_input_from_json(json: String) -> Result<EmbeddingInput, String> {
    serde_json::from_str::<liter_llm::types::EmbeddingInput>(&json)
        .map(EmbeddingInput::from)
        .map_err(|e| e.to_string())
}
pub fn moderation_input_from_json(json: String) -> Result<ModerationInput, String> {
    serde_json::from_str::<liter_llm::types::ModerationInput>(&json)
        .map(ModerationInput::from)
        .map_err(|e| e.to_string())
}
pub fn rerank_document_from_json(json: String) -> Result<RerankDocument, String> {
    serde_json::from_str::<liter_llm::types::RerankDocument>(&json)
        .map(RerankDocument::from)
        .map_err(|e| e.to_string())
}
pub fn ocr_document_from_json(json: String) -> Result<OcrDocument, String> {
    serde_json::from_str::<liter_llm::types::OcrDocument>(&json)
        .map(OcrDocument::from)
        .map_err(|e| e.to_string())
}
pub fn file_purpose_from_json(json: String) -> Result<FilePurpose, String> {
    serde_json::from_str::<liter_llm::types::FilePurpose>(&json)
        .map(FilePurpose::from)
        .map_err(|e| e.to_string())
}
pub fn batch_status_from_json(json: String) -> Result<BatchStatus, String> {
    serde_json::from_str::<liter_llm::types::BatchStatus>(&json)
        .map(BatchStatus::from)
        .map_err(|e| e.to_string())
}
pub fn auth_header_format_from_json(json: String) -> Result<AuthHeaderFormat, String> {
    serde_json::from_str::<liter_llm::provider::custom::AuthHeaderFormat>(&json)
        .map(AuthHeaderFormat::from)
        .map_err(|e| e.to_string())
}
pub fn stream_format_from_json(json: String) -> Result<StreamFormat, String> {
    serde_json::from_str::<liter_llm::provider::StreamFormat>(&json)
        .map(StreamFormat::from)
        .map_err(|e| e.to_string())
}
pub fn auth_type_from_json(json: String) -> Result<AuthType, String> {
    serde_json::from_str::<liter_llm::provider::AuthType>(&json)
        .map(AuthType::from)
        .map_err(|e| e.to_string())
}
pub fn enforcement_from_json(json: String) -> Result<Enforcement, String> {
    serde_json::from_str::<liter_llm::Enforcement>(&json)
        .map(Enforcement::from)
        .map_err(|e| e.to_string())
}
pub fn cache_backend_from_json(json: String) -> Result<CacheBackend, String> {
    serde_json::from_str::<liter_llm::CacheBackend>(&json)
        .map(CacheBackend::from)
        .map_err(|e| e.to_string())
}
