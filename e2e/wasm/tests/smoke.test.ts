// This file is auto-generated by alef — DO NOT EDIT.
// alef:hash:12ba226655dd71043ff98454d0da03d9d72e6a16c135bd7421fb9a2b83defb7d
// To regenerate: alef generate
// To verify freshness: alef verify --exit-code

import { describe, expect, it } from "vitest";
import { createClient, WasmChatCompletionRequest, WasmEmbeddingRequest, WasmChatCompletionTool, WasmFunctionDefinition, WasmStreamOptions, WasmFinishReason } from "@xberg-io/liter-llm-wasm";

async function _alefE2eDecompressAndParseJson(response: Response): Promise<unknown> {
	const contentEncoding = response.headers.get("content-encoding");
	let buffer = await response.arrayBuffer();

	if (contentEncoding === "gzip") {
		const { gunzipSync } = await import("zlib");
		buffer = gunzipSync(new Uint8Array(buffer)).buffer;
	} else if (contentEncoding === "br") {
		const { brotliDecompressSync } = await import("zlib");
		buffer = brotliDecompressSync(new Uint8Array(buffer)).buffer;
	}

	const text = new TextDecoder().decode(buffer);
	return JSON.parse(text);
}

function _alefE2eText(value: unknown): string {
	return value == null ? "" : String(value);
}

function _alefE2eItemTexts(item: unknown): string[] {
	if (item == null || typeof item !== "object") {
		return [_alefE2eText(item)];
	}
	const record = item as Record<string, unknown>;
	const itemsText = Array.isArray(record.items) ? record.items.map(_alefE2eText).join(" ") : "";
	return [_alefE2eText(item), _alefE2eText(record.kind), _alefE2eText(record.name), _alefE2eText(record.source), _alefE2eText(record.alias), _alefE2eText(record.text), _alefE2eText(record.signature), itemsText];
}

function _alefE2eFormatMetadataDisplay(fm: unknown): string {
	if (fm == null) return "";
	if (typeof fm !== "object") return String(fm);
	const record = fm as Record<string, unknown>;
	const formatType = record.format_type;

	// FormatMetadata is a tagged union: { format_type: 'image', image: { format: 'PNG', ... }, ... }
	// Extract the display string based on the variant type
	if (formatType === "image" && typeof record.image === "object") {
		const imageData = record.image as Record<string, unknown>;
		if (typeof imageData.format === "string") return imageData.format;
	}

	// Fallback: return format_type variant name
	if (typeof record.format_type === "string") return record.format_type;
	return "";
}


describe("smoke", () => {

	it("anthropic_chat: Basic chat completion via the Anthropic provider (claude-3-5-sonnet-20241022) with system and user messages", async () => {
		const client = createClient('test-key', `${process.env.MOCK_SERVER_URL}/fixtures/anthropic_chat`);
		const result = await client.chat((() => { const _u0 = WasmChatCompletionRequest.default(); _u0.maxTokens = 16n; _u0.messages = [{ content: "You are a helpful assistant.", role: "system" }, { content: "Say hello in one word.", role: "user" }]; _u0.model = "anthropic/claude-3-5-sonnet-20241022"; _u0.temperature = 0; return _u0; })());
    expect(result.choices.length).toBe(1);
    expect((result.choices[0].message.content ?? "").trim()).toBe("Hello!");
    expect(result.choices[0].finishReason).toBe("stop");
    expect(result.model.trim()).toBe("claude-3-5-sonnet-20241022");
    expect(Number(result.usage.totalTokens)).toBe(23);
	}, 30000);
	it("azure_chat: Chat completion via Azure OpenAI with the azure/ prefix for provider routing — verifies the prefix is stripped before dispatching and the response is normalised to the standard OpenAI chat completion shape", async () => {
		const client = createClient('test-key', `${process.env.MOCK_SERVER_URL}/fixtures/azure_chat`);
		const result = await client.chat((() => { const _u0 = WasmChatCompletionRequest.default(); _u0.maxTokens = 16n; _u0.messages = [{ content: "Say hello", role: "user" }]; _u0.model = "azure/gpt-4"; _u0.temperature = 0; return _u0; })());
    expect(result.choices.length).toBe(1);
    expect((result.choices[0].message.content ?? "").trim()).toBe("Hello!");
    expect(result.choices[0].finishReason).toBe("stop");
    expect(result.model.trim()).toBe("gpt-4");
    expect(Number(result.usage.totalTokens)).toBe(13);
	}, 30000);
	it("azure_embed: Embedding request via Azure OpenAI using the azure/ provider prefix — response follows the standard OpenAI embeddings shape that Azure returns unchanged", async () => {
		const client = createClient('test-key', `${process.env.MOCK_SERVER_URL}/fixtures/azure_embed`);
		const result = await client.embed((() => { const _u0 = WasmEmbeddingRequest.default(); _u0.input = "Hello world"; _u0.model = "azure/text-embedding-ada-002"; return _u0; })());
    expect(result.data.length).toBe(1);
    expect(result.data[0].embedding.length).toBe(1480);
	}, 30000);
	it("basic_chat: Basic chat completion with a single user message", async () => {
		const client = createClient('test-key', `${process.env.MOCK_SERVER_URL}/fixtures/basic_chat`);
		const result = await client.chat((() => { const _u0 = WasmChatCompletionRequest.default(); _u0.messages = [{ content: "Say hello", role: "user" }]; _u0.model = "gpt-4"; _u0.temperature = 0; return _u0; })());
    expect(result.choices.length).toBe(1);
    expect((result.choices[0].message.content ?? "").trim()).toBe("Hello!");
    expect(result.choices[0].finishReason).toBe("stop");
    expect(Number(result.usage.totalTokens)).toBe(15);
    expect(result.model.trim()).toBe("gpt-4");
	}, 30000);
	it("basic_embed: Basic embedding request for a single input string", async () => {
		const client = createClient('test-key', `${process.env.MOCK_SERVER_URL}/fixtures/basic_embed`);
		const result = await client.embed((() => { const _u0 = WasmEmbeddingRequest.default(); _u0.input = "Hello world"; _u0.model = "text-embedding-3-small"; return _u0; })());
    expect(result.data.length).toBe(1);
    expect(result.data[0].embedding.length).toBe(5);
	}, 30000);
	it("basic_list_models: List available models from the API", async () => {
		const client = createClient('test-key', `${process.env.MOCK_SERVER_URL}/fixtures/basic_list_models`);
		const result = await client.listModels();
    expect(result.data.length).toBeGreaterThanOrEqual(1);
	}, 30000);
	it("bedrock_chat: Basic chat completion via the AWS Bedrock provider using the bedrock/ prefix for routing — verifies the prefix is stripped before dispatching and the Converse API response is normalised to the standard OpenAI chat completion shape", async () => {
		const client = createClient('test-key', `${process.env.MOCK_SERVER_URL}/fixtures/bedrock_chat`);
		const result = await client.chat((() => { const _u0 = WasmChatCompletionRequest.default(); _u0.maxTokens = 16n; _u0.messages = [{ content: "Say hello in one word.", role: "user" }]; _u0.model = "bedrock/anthropic.claude-3-sonnet-20240229-v1:0"; _u0.temperature = 0; return _u0; })());
    expect(result.choices.length).toBe(1);
    expect((result.choices[0].message.content ?? "").trim()).toBe("Hello!");
    expect(result.choices[0].finishReason).toBe("stop");
    expect(result.model.trim()).toBe("anthropic.claude-3-sonnet-20240229-v1:0");
    expect(Number(result.usage.totalTokens)).toBe(15);
	}, 30000);
	it("github_copilot_chat: Basic chat completion via the GitHub Copilot provider (gpt-4o) with a single user message", async () => {
		const client = createClient('test-key', `${process.env.MOCK_SERVER_URL}/fixtures/github_copilot_chat`);
		const result = await client.chat((() => { const _u0 = WasmChatCompletionRequest.default(); _u0.maxTokens = 16n; _u0.messages = [{ content: "Say hello in one word.", role: "user" }]; _u0.model = "github_copilot/gpt-4o"; _u0.temperature = 0; return _u0; })());
    expect(result.choices.length).toBe(1);
    expect((result.choices[0].message.content ?? "").trim()).toBe("Hello!");
    expect(result.choices[0].finishReason).toBe("stop");
    expect(result.model.trim()).toBe("gpt-4o");
    expect(Number(result.usage.totalTokens)).toBe(14);
	}, 30000);
	it("smoke_cache_memory: Test in-memory caching by sending identical requests twice", async () => {
		const apiKey = process.env.OPENAI_API_KEY;
    const baseUrl = apiKey ? undefined : `${process.env.MOCK_SERVER_URL}/fixtures/smoke_cache_memory`;
    console.log(`smoke_cache_memory: ${apiKey ? 'using real API (OPENAI_API_KEY is set)' : 'using mock server (OPENAI_API_KEY not set)'}`);
    const client = createClient(apiKey ?? 'test-key', baseUrl);
		const result = await client.chat((() => { const _u0 = WasmChatCompletionRequest.default(); _u0.maxTokens = 5n; _u0.messages = [{ content: "What is 2+2? Answer with just the number.", role: "user" }]; _u0.model = "openai/gpt-4o-mini"; return _u0; })());
    {
        const _v = result.choices;
        if (typeof _v === "string" || Array.isArray(_v)) {
            expect(_v.length).toBeGreaterThan(0);
        } else {
            expect(_v).toBeDefined();
            expect(_v).not.toBeNull();
        }
    }
	}, 30000);
	it("smoke_chat_anthropic: Basic chat completion against real Anthropic API", async () => {
		const apiKey = process.env.ANTHROPIC_API_KEY;
    const baseUrl = apiKey ? undefined : `${process.env.MOCK_SERVER_URL}/fixtures/smoke_chat_anthropic`;
    console.log(`smoke_chat_anthropic: ${apiKey ? 'using real API (ANTHROPIC_API_KEY is set)' : 'using mock server (ANTHROPIC_API_KEY not set)'}`);
    const client = createClient(apiKey ?? 'test-key', baseUrl);
		const result = await client.chat((() => { const _u0 = WasmChatCompletionRequest.default(); _u0.maxTokens = 10n; _u0.messages = [{ content: "Say hello in exactly one word.", role: "user" }]; _u0.model = "anthropic/claude-sonnet-4-20250514"; return _u0; })());
    {
        const _v = result.choices;
        if (typeof _v === "string" || Array.isArray(_v)) {
            expect(_v.length).toBeGreaterThan(0);
        } else {
            expect(_v).toBeDefined();
            expect(_v).not.toBeNull();
        }
    }
    {
        const _v = result.usage;
        if (typeof _v === "string" || Array.isArray(_v)) {
            expect(_v.length).toBeGreaterThan(0);
        } else {
            expect(_v).toBeDefined();
            expect(_v).not.toBeNull();
        }
    }
	}, 30000);
	it("smoke_chat_gemini: Basic chat completion against real Google Gemini API", async () => {
		const apiKey = process.env.GEMINI_API_KEY;
    const baseUrl = apiKey ? undefined : `${process.env.MOCK_SERVER_URL}/fixtures/smoke_chat_gemini`;
    console.log(`smoke_chat_gemini: ${apiKey ? 'using real API (GEMINI_API_KEY is set)' : 'using mock server (GEMINI_API_KEY not set)'}`);
    const client = createClient(apiKey ?? 'test-key', baseUrl);
		const result = await client.chat((() => { const _u0 = WasmChatCompletionRequest.default(); _u0.maxTokens = 10n; _u0.messages = [{ content: "Say hello in exactly one word.", role: "user" }]; _u0.model = "gemini/gemini-2.5-flash-lite"; return _u0; })());
    {
        const _v = result.choices;
        if (typeof _v === "string" || Array.isArray(_v)) {
            expect(_v.length).toBeGreaterThan(0);
        } else {
            expect(_v).toBeDefined();
            expect(_v).not.toBeNull();
        }
    }
    {
        const _v = result.usage;
        if (typeof _v === "string" || Array.isArray(_v)) {
            expect(_v.length).toBeGreaterThan(0);
        } else {
            expect(_v).toBeDefined();
            expect(_v).not.toBeNull();
        }
    }
	}, 30000);
	it("smoke_chat_openai: Basic chat completion against real OpenAI API", async () => {
		const apiKey = process.env.OPENAI_API_KEY;
    const baseUrl = apiKey ? undefined : `${process.env.MOCK_SERVER_URL}/fixtures/smoke_chat_openai`;
    console.log(`smoke_chat_openai: ${apiKey ? 'using real API (OPENAI_API_KEY is set)' : 'using mock server (OPENAI_API_KEY not set)'}`);
    const client = createClient(apiKey ?? 'test-key', baseUrl);
		const result = await client.chat((() => { const _u0 = WasmChatCompletionRequest.default(); _u0.maxTokens = 10n; _u0.messages = [{ content: "Say hello in exactly one word.", role: "user" }]; _u0.model = "openai/gpt-4o-mini"; return _u0; })());
    {
        const _v = result.choices;
        if (typeof _v === "string" || Array.isArray(_v)) {
            expect(_v.length).toBeGreaterThan(0);
        } else {
            expect(_v).toBeDefined();
            expect(_v).not.toBeNull();
        }
    }
    {
        const _v = result.usage;
        if (typeof _v === "string" || Array.isArray(_v)) {
            expect(_v.length).toBeGreaterThan(0);
        } else {
            expect(_v).toBeDefined();
            expect(_v).not.toBeNull();
        }
    }
	}, 30000);
	it("smoke_embed_openai: Embeddings request against real OpenAI API", async () => {
		const apiKey = process.env.OPENAI_API_KEY;
    const baseUrl = apiKey ? undefined : `${process.env.MOCK_SERVER_URL}/fixtures/smoke_embed_openai`;
    console.log(`smoke_embed_openai: ${apiKey ? 'using real API (OPENAI_API_KEY is set)' : 'using mock server (OPENAI_API_KEY not set)'}`);
    const client = createClient(apiKey ?? 'test-key', baseUrl);
		const result = await client.embed((() => { const _u0 = WasmEmbeddingRequest.default(); _u0.input = ["Hello world"]; _u0.model = "openai/text-embedding-3-small"; return _u0; })());
    {
        const _v = result.data;
        if (typeof _v === "string" || Array.isArray(_v)) {
            expect(_v.length).toBeGreaterThan(0);
        } else {
            expect(_v).toBeDefined();
            expect(_v).not.toBeNull();
        }
    }
	}, 30000);
	it("smoke_list_models_openai: List models against real OpenAI API", async () => {
		const apiKey = process.env.OPENAI_API_KEY;
    const baseUrl = apiKey ? undefined : `${process.env.MOCK_SERVER_URL}/fixtures/smoke_list_models_openai`;
    console.log(`smoke_list_models_openai: ${apiKey ? 'using real API (OPENAI_API_KEY is set)' : 'using mock server (OPENAI_API_KEY not set)'}`);
    const client = createClient(apiKey ?? 'test-key', baseUrl);
		const result = await client.listModels();
    expect(result.data.length).toBeGreaterThanOrEqual(1);
	}, 30000);
	it("smoke_provider_routing: Test provider routing by sending requests to OpenAI and Anthropic", async () => {
		const apiKey = process.env.OPENAI_API_KEY;
    const baseUrl = apiKey ? undefined : `${process.env.MOCK_SERVER_URL}/fixtures/smoke_provider_routing`;
    console.log(`smoke_provider_routing: ${apiKey ? 'using real API (OPENAI_API_KEY is set)' : 'using mock server (OPENAI_API_KEY not set)'}`);
    const client = createClient(apiKey ?? 'test-key', baseUrl);
		const result = await client.chat((() => { const _u0 = WasmChatCompletionRequest.default(); _u0.maxTokens = 5n; _u0.messages = [{ content: "Say hi.", role: "user" }]; _u0.model = "openai/gpt-4o-mini"; return _u0; })());
    {
        const _v = result.choices;
        if (typeof _v === "string" || Array.isArray(_v)) {
            expect(_v.length).toBeGreaterThan(0);
        } else {
            expect(_v).toBeDefined();
            expect(_v).not.toBeNull();
        }
    }
	}, 30000);
	it("smoke_streaming_openai: Chat streaming against real OpenAI API, verifies chunks received", async () => {
		const apiKey = process.env.OPENAI_API_KEY;
    const baseUrl = apiKey ? undefined : `${process.env.MOCK_SERVER_URL}/fixtures/smoke_streaming_openai`;
    console.log(`smoke_streaming_openai: ${apiKey ? 'using real API (OPENAI_API_KEY is set)' : 'using mock server (OPENAI_API_KEY not set)'}`);
    const client = createClient(apiKey ?? 'test-key', baseUrl);
		const stream = await client.chatStream((() => { const _u0 = WasmChatCompletionRequest.default(); _u0.maxTokens = 50n; _u0.messages = [{ content: "Count from 1 to 5.", role: "user" }]; _u0.model = "openai/gpt-4o-mini"; return _u0; })());
		const chunks: any[] = [];
    while (true) { const _chunk = await stream.next(); if (_chunk == null) break; chunks.push(_chunk); }
    expect(chunks.length).toBeGreaterThanOrEqual(1);
	}, 30000);
	it("vertex_chat: Basic chat completion via the Google Vertex AI provider using the vertex_ai/ prefix for routing — verifies the prefix is stripped before dispatching and the Gemini response is normalised to the standard OpenAI chat completion shape", async () => {
		const client = createClient('test-key', `${process.env.MOCK_SERVER_URL}/fixtures/vertex_chat`);
		const result = await client.chat((() => { const _u0 = WasmChatCompletionRequest.default(); _u0.maxTokens = 16n; _u0.messages = [{ content: "Say hello in one word.", role: "user" }]; _u0.model = "vertex_ai/gemini-2.0-flash"; _u0.temperature = 0; return _u0; })());
    expect(result.choices.length).toBe(1);
    expect((result.choices[0].message.content ?? "").trim()).toBe("Hello!");
    expect(result.choices[0].finishReason).toBe("stop");
    expect(result.model.trim()).toBe("gemini-2.0-flash");
    expect(Number(result.usage.totalTokens)).toBe(13);
	}, 30000);
	it("vertex_embed: Embedding request via Google Vertex AI using the vertex_ai/ provider prefix and the text-embedding-005 model — response follows the standard OpenAI embeddings shape", async () => {
		const client = createClient('test-key', `${process.env.MOCK_SERVER_URL}/fixtures/vertex_embed`);
		const result = await client.embed((() => { const _u0 = WasmEmbeddingRequest.default(); _u0.input = "Hello"; _u0.model = "vertex_ai/text-embedding-005"; return _u0; })());
    expect(result.data.length).toBe(1);
    expect(result.data[0].embedding.length).toBe(160);
	}, 30000);
});
