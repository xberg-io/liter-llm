// This file is auto-generated by alef — DO NOT EDIT.
// alef:hash:d5acd6193dec98d5f42f7fe0535936dad95b6dc3ee4c55e66aa6d33226b8fae8
// To regenerate: alef generate
// To verify freshness: alef verify --exit-code

import { describe, expect, it } from "vitest";
import { createClient, WasmChatCompletionRequest, WasmCreateImageRequest, WasmChatCompletionTool, WasmFunctionDefinition, WasmStreamOptions } from "@xberg-io/liter-llm-wasm";

async function _alefE2eDecompressAndParseJson(response: Response): Promise<unknown> {
	const contentEncoding = response.headers.get("content-encoding");
	let buffer = await response.arrayBuffer();

	if (contentEncoding === "gzip") {
		const { gunzipSync } = await import("zlib");
		buffer = gunzipSync(new Uint8Array(buffer)).buffer;
	} else if (contentEncoding === "br") {
		const { brotliDecompressSync } = await import("zlib");
		buffer = brotliDecompressSync(new Uint8Array(buffer)).buffer;
	}

	const text = new TextDecoder().decode(buffer);
	return JSON.parse(text);
}

function _alefE2eText(value: unknown): string {
	return value == null ? "" : String(value);
}

function _alefE2eItemTexts(item: unknown): string[] {
	if (item == null || typeof item !== "object") {
		return [_alefE2eText(item)];
	}
	const record = item as Record<string, unknown>;
	const itemsText = Array.isArray(record.items) ? record.items.map(_alefE2eText).join(" ") : "";
	return [_alefE2eText(item), _alefE2eText(record.kind), _alefE2eText(record.name), _alefE2eText(record.source), _alefE2eText(record.alias), _alefE2eText(record.text), _alefE2eText(record.signature), itemsText];
}

function _alefE2eFormatMetadataDisplay(fm: unknown): string {
	if (fm == null) return "";
	if (typeof fm !== "object") return String(fm);
	const record = fm as Record<string, unknown>;
	const formatType = record.format_type;

	// FormatMetadata is a tagged union: { format_type: 'image', image: { format: 'PNG', ... }, ... }
	// Extract the display string based on the variant type
	if (formatType === "image" && typeof record.image === "object") {
		const imageData = record.image as Record<string, unknown>;
		if (typeof imageData.format === "string") return imageData.format;
	}

	// Fallback: return format_type variant name
	if (typeof record.format_type === "string") return record.format_type;
	return "";
}


describe("image-generate", () => {

	it("edge_image_b64_response: Image generation returning base64-encoded data instead of URL", async () => {
		const client = createClient('test-key', `${process.env.MOCK_SERVER_URL}/fixtures/edge_image_b64_response`);
		const result = await client.imageGenerate((() => { const _u0 = WasmCreateImageRequest.default(); _u0.model = "dall-e-3"; _u0.n = 1; _u0.prompt = "A blue circle"; _u0.responseFormat = "b64_json"; _u0.size = "1024x1024"; return _u0; })());
    expect(result.data.length).toBe(1);
    {
        const _v = result.data[0].b64Json;
        if (typeof _v === "string" || Array.isArray(_v)) {
            expect(_v.length).toBeGreaterThan(0);
        } else {
            expect(_v).toBeDefined();
            expect(_v).not.toBeNull();
        }
    }
	}, 30000);
	it("edge_image_empty_prompt: Image generation with an empty prompt returns 400", async () => {
		const client = createClient('test-key', `${process.env.MOCK_SERVER_URL}/fixtures/edge_image_empty_prompt`);
		await expect(async () => {
			await client.imageGenerate((() => { const _u0 = WasmCreateImageRequest.default(); _u0.model = "dall-e-3"; _u0.n = 1; _u0.prompt = ""; _u0.size = "1024x1024"; return _u0; })());
		}).rejects.toThrow();
	}, 30000);
	it("edge_image_multiple_n: Image generation requesting multiple images with n=3", async () => {
		const client = createClient('test-key', `${process.env.MOCK_SERVER_URL}/fixtures/edge_image_multiple_n`);
		const result = await client.imageGenerate((() => { const _u0 = WasmCreateImageRequest.default(); _u0.model = "dall-e-3"; _u0.n = 3; _u0.prompt = "A cat"; return _u0; })());
    expect(result.data.length).toBe(3);
    {
        const _v = result.data[0].url;
        if (typeof _v === "string" || Array.isArray(_v)) {
            expect(_v.length).toBeGreaterThan(0);
        } else {
            expect(_v).toBeDefined();
            expect(_v).not.toBeNull();
        }
    }
    {
        const _v = result.data[1].url;
        if (typeof _v === "string" || Array.isArray(_v)) {
            expect(_v.length).toBeGreaterThan(0);
        } else {
            expect(_v).toBeDefined();
            expect(_v).not.toBeNull();
        }
    }
    {
        const _v = result.data[2].url;
        if (typeof _v === "string" || Array.isArray(_v)) {
            expect(_v.length).toBeGreaterThan(0);
        } else {
            expect(_v).toBeDefined();
            expect(_v).not.toBeNull();
        }
    }
	}, 30000);
	it("error_image_auth_401: 401 Unauthorized when generating images with invalid API key", async () => {
		const client = createClient('test-key', `${process.env.MOCK_SERVER_URL}/fixtures/error_image_auth_401`);
		await expect(async () => {
			await client.imageGenerate((() => { const _u0 = WasmCreateImageRequest.default(); _u0.model = "dall-e-3"; _u0.n = 1; _u0.prompt = "A cat"; _u0.size = "1024x1024"; return _u0; })());
		}).rejects.toThrow();
	}, 30000);
	it("error_image_bad_request: 400 Bad Request when image generation parameters are invalid", async () => {
		const client = createClient('test-key', `${process.env.MOCK_SERVER_URL}/fixtures/error_image_bad_request`);
		await expect(async () => {
			await client.imageGenerate((() => { const _u0 = WasmCreateImageRequest.default(); _u0.model = "dall-e-3"; _u0.n = 1; _u0.prompt = "A cat"; _u0.size = "9999x9999"; return _u0; })());
		}).rejects.toThrow();
	}, 30000);
	it("error_image_rate_limit: 429 Rate limit exceeded for image generation", async () => {
		const client = createClient('test-key', `${process.env.MOCK_SERVER_URL}/fixtures/error_image_rate_limit`);
		await expect(async () => {
			await client.imageGenerate((() => { const _u0 = WasmCreateImageRequest.default(); _u0.model = "dall-e-3"; _u0.n = 1; _u0.prompt = "A cat"; _u0.size = "1024x1024"; return _u0; })());
		}).rejects.toThrow();
	}, 30000);
	it("smoke_image_basic: Basic image generation with a text prompt", async () => {
		const client = createClient('test-key', `${process.env.MOCK_SERVER_URL}/fixtures/smoke_image_basic`);
		const result = await client.imageGenerate((() => { const _u0 = WasmCreateImageRequest.default(); _u0.model = "dall-e-3"; _u0.n = 1; _u0.prompt = "A white cat sitting on a windowsill"; _u0.size = "1024x1024"; return _u0; })());
    expect(result.data.length).toBe(1);
    {
        const _v = result.data[0].url;
        if (typeof _v === "string" || Array.isArray(_v)) {
            expect(_v.length).toBeGreaterThan(0);
        } else {
            expect(_v).toBeDefined();
            expect(_v).not.toBeNull();
        }
    }
	}, 30000);
	it("smoke_image_multiple: Image generation requesting multiple images", async () => {
		const client = createClient('test-key', `${process.env.MOCK_SERVER_URL}/fixtures/smoke_image_multiple`);
		const result = await client.imageGenerate((() => { const _u0 = WasmCreateImageRequest.default(); _u0.model = "dall-e-2"; _u0.n = 3; _u0.prompt = "A red bicycle"; _u0.size = "256x256"; return _u0; })());
    expect(result.data.length).toBe(3);
    {
        const _v = result.data[0].url;
        if (typeof _v === "string" || Array.isArray(_v)) {
            expect(_v.length).toBeGreaterThan(0);
        } else {
            expect(_v).toBeDefined();
            expect(_v).not.toBeNull();
        }
    }
	}, 30000);
	it("smoke_image_with_size: Image generation with explicit size parameter", async () => {
		const client = createClient('test-key', `${process.env.MOCK_SERVER_URL}/fixtures/smoke_image_with_size`);
		const result = await client.imageGenerate((() => { const _u0 = WasmCreateImageRequest.default(); _u0.model = "dall-e-3"; _u0.n = 1; _u0.prompt = "A sunset over mountains"; _u0.size = "1792x1024"; return _u0; })());
    expect(result.data.length).toBe(1);
    {
        const _v = result.data[0].url;
        if (typeof _v === "string" || Array.isArray(_v)) {
            expect(_v.length).toBeGreaterThan(0);
        } else {
            expect(_v).toBeDefined();
            expect(_v).not.toBeNull();
        }
    }
	}, 30000);
});
