import { initSync } from "liter_llm";
// This file is auto-generated by alef — DO NOT EDIT.
// alef:hash:de1ec574fcaf790df6d7c09af9a5f8b3603757140753bcb61715db981cbb8c93
// To regenerate: alef generate
// To verify freshness: alef verify --exit-code
// Issues & docs: https://github.com/kreuzberg-dev/alef

import { describe, expect, it } from "vitest";
import {
  createClient,
  WasmChatCompletionRequest,
  WasmChatCompletionRequestUpdate,
} from "liter_llm";
import { fileURLToPath } from "url";
import { dirname, join } from "path";
import { readFileSync } from "fs";
const __filename = fileURLToPath(import.meta.url);
const __dirname = dirname(__filename);
const testDocumentsDir = join(__dirname, "..", "..", "..", "test_documents");
globalThis.process.chdir(testDocumentsDir);
const wasmUrl = await import.meta.resolve("liter_llm/liter_llm_wasm_bg.wasm");
const wasmPath = fileURLToPath(wasmUrl);
const wasmBuffer = readFileSync(wasmPath);
initSync(wasmBuffer);

function _alefE2eText(value: unknown): string {
  return value == null ? "" : String(value);
}

function _alefE2eItemTexts(item: unknown): string[] {
  if (item == null || typeof item !== "object") {
    return [_alefE2eText(item)];
  }
  const record = item as Record<string, unknown>;
  const itemsText = Array.isArray(record.items) ? record.items.map(_alefE2eText).join(" ") : "";
  return [
    _alefE2eText(item),
    _alefE2eText(record.kind),
    _alefE2eText(record.name),
    _alefE2eText(record.source),
    _alefE2eText(record.alias),
    _alefE2eText(record.text),
    _alefE2eText(record.signature),
    itemsText,
  ];
}

describe("embed", () => {
  it("batch_embed: Embedding request with multiple input strings returns one embedding object per input", async () => {
    const client = createClient("test-key", `${process.env.MOCK_SERVER_URL}/fixtures/batch_embed`);
    const result = await client.embed(
      (() => {
        const _u = new WasmChatCompletionRequestUpdate();
        _u.input = ["Hello", "World"];
        _u.model = "text-embedding-3-small";
        return WasmChatCompletionRequest.fromUpdate(_u);
      })(),
    );
    expect(result.data.length).toBe(2);
    expect(result.data["0"].embedding.length).toBe(5);
  });
  it("edge_embed_batch_input: Embedding request with multiple inputs returns multiple embedding objects", async () => {
    const client = createClient(
      "test-key",
      `${process.env.MOCK_SERVER_URL}/fixtures/edge_embed_batch_input`,
    );
    const result = await client.embed(
      (() => {
        const _u = new WasmChatCompletionRequestUpdate();
        _u.input = ["Hello world", "Goodbye world"];
        _u.model = "text-embedding-3-small";
        return WasmChatCompletionRequest.fromUpdate(_u);
      })(),
    );
    expect(result.data.length).toBe(2);
    expect(result.data["0"].index).toBe(0);
    expect(result.data["1"].index).toBe(1);
  });
  it("edge_embed_empty_input: Embedding request with empty string input returns empty data array", async () => {
    const client = createClient(
      "test-key",
      `${process.env.MOCK_SERVER_URL}/fixtures/edge_embed_empty_input`,
    );
    const result = await client.embed(
      (() => {
        const _u = new WasmChatCompletionRequestUpdate();
        _u.input = "";
        _u.model = "text-embedding-3-small";
        return WasmChatCompletionRequest.fromUpdate(_u);
      })(),
    );
    expect(result.data.length).toBe(0);
  });
  it("embed_encoding_format: Embedding request with explicit encoding_format of float returns float array embeddings", async () => {
    const client = createClient(
      "test-key",
      `${process.env.MOCK_SERVER_URL}/fixtures/embed_encoding_format`,
    );
    const result = await client.embed(
      (() => {
        const _u = new WasmChatCompletionRequestUpdate();
        _u.encodingFormat = "float";
        _u.input = "Test input";
        _u.model = "text-embedding-3-small";
        return WasmChatCompletionRequest.fromUpdate(_u);
      })(),
    );
    expect(result.data.length).toBe(1);
    expect(result.data["0"].embedding.length).toBe(5);
  });
  it("embed_error_401: 401 Unauthorized error on embedding request when API key is invalid", async () => {
    const client = createClient(
      "test-key",
      `${process.env.MOCK_SERVER_URL}/fixtures/embed_error_401`,
    );
    await expect(async () => {
      await client.embed(
        (() => {
          const _u = new WasmChatCompletionRequestUpdate();
          _u.input = "Hello world";
          _u.model = "text-embedding-3-small";
          return WasmChatCompletionRequest.fromUpdate(_u);
        })(),
      );
    }).rejects.toThrow();
  });
  it("embed_with_dimensions: Embedding request with explicit dimensions parameter returns embeddings of the requested size", async () => {
    const client = createClient(
      "test-key",
      `${process.env.MOCK_SERVER_URL}/fixtures/embed_with_dimensions`,
    );
    const result = await client.embed(
      (() => {
        const _u = new WasmChatCompletionRequestUpdate();
        _u.dimensions = 256;
        _u.input = "Hello world";
        _u.model = "text-embedding-3-small";
        return WasmChatCompletionRequest.fromUpdate(_u);
      })(),
    );
    expect(result.data.length).toBe(1);
    expect(result.data["0"].embedding.length).toBe(8);
  });
  it("local_embed_ollama: Embedding request via Ollama local provider with all-minilm model", async () => {
    const client = createClient(
      "test-key",
      `${process.env.MOCK_SERVER_URL}/fixtures/local_embed_ollama`,
    );
    const result = await client.embed(
      (() => {
        const _u = new WasmChatCompletionRequestUpdate();
        _u.input = "The quick brown fox jumps over the lazy dog";
        _u.model = "ollama/all-minilm";
        return WasmChatCompletionRequest.fromUpdate(_u);
      })(),
    );
    expect(result.data.length).toBe(1);
    expect(result.data["0"].embedding.length).toBe(32);
  });
});
