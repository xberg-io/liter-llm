// This file is auto-generated by alef — DO NOT EDIT.
// alef:hash:8d2f6b79df8816028b8e6ab188eacc825db704afb9b7e13d20284c691f820716
// To regenerate: alef generate
// To verify freshness: alef verify --exit-code
// Issues & docs: https://github.com/kreuzberg-dev/alef

import { describe, expect, it } from "vitest";
import {
  createClient,
  WasmChatCompletionRequest,
  WasmChatCompletionRequestUpdate,
} from "liter_llm";
function _alefE2eText(value: unknown): string {
  return value == null ? "" : String(value);
}

function _alefE2eItemTexts(item: unknown): string[] {
  if (item == null || typeof item !== "object") {
    return [_alefE2eText(item)];
  }
  const record = item as Record<string, unknown>;
  const itemsText = Array.isArray(record.items) ? record.items.map(_alefE2eText).join(" ") : "";
  return [
    _alefE2eText(item),
    _alefE2eText(record.kind),
    _alefE2eText(record.name),
    _alefE2eText(record.source),
    _alefE2eText(record.alias),
    _alefE2eText(record.text),
    _alefE2eText(record.signature),
    itemsText,
  ];
}

describe("chat", () => {
  it("developer_message: Chat request that includes a developer role message alongside user messages", async () => {
    const client = createClient(
      "test-key",
      `${process.env.MOCK_SERVER_URL}/fixtures/developer_message`,
    );
    const result = await client.chat(
      (() => {
        const _u = new WasmChatCompletionRequestUpdate();
        _u.messages = [
          {
            content: "You are a coding assistant. Always respond with concise code examples.",
            role: "developer",
          },
          { content: "How do I reverse a string in Python?", role: "user" },
        ];
        _u.model = "gpt-4";
        return WasmChatCompletionRequest.fromUpdate(_u);
      })(),
    );
    expect(result.choices.length).toBe(1);
    expect((result.choices["0"].message.content ?? "").trim()).toBe("s[::-1]");
    expect((result.choices["0"].finishReason ?? "").trim()).toBe("stop");
  });
  it("edge_chat_max_tokens: Chat request with max_tokens=1 terminates with length finish_reason", async () => {
    const client = createClient(
      "test-key",
      `${process.env.MOCK_SERVER_URL}/fixtures/edge_chat_max_tokens`,
    );
    const result = await client.chat(
      (() => {
        const _u = new WasmChatCompletionRequestUpdate();
        _u.maxTokens = 1;
        _u.messages = [{ content: "Write a story", role: "user" }];
        _u.model = "gpt-4";
        return WasmChatCompletionRequest.fromUpdate(_u);
      })(),
    );
    expect((result.choices["0"].finishReason ?? "").trim()).toBe("length");
    expect(result.choices["0"].message.content).toBeTruthy();
  });
  it("edge_chat_system_only: Chat request with system message and user message", async () => {
    const client = createClient(
      "test-key",
      `${process.env.MOCK_SERVER_URL}/fixtures/edge_chat_system_only`,
    );
    const result = await client.chat(
      (() => {
        const _u = new WasmChatCompletionRequestUpdate();
        _u.messages = [
          { content: "You are a helpful and concise assistant", role: "system" },
          { content: "Hi", role: "user" },
        ];
        _u.model = "gpt-4";
        return WasmChatCompletionRequest.fromUpdate(_u);
      })(),
    );
    expect(result.choices["0"].message.content).toBeTruthy();
  });
  it("edge_chat_temperature_zero: Chat request with temperature=0 for deterministic responses", async () => {
    const client = createClient(
      "test-key",
      `${process.env.MOCK_SERVER_URL}/fixtures/edge_chat_temperature_zero`,
    );
    const result = await client.chat(
      (() => {
        const _u = new WasmChatCompletionRequestUpdate();
        _u.messages = [{ content: "Say hello", role: "user" }];
        _u.model = "gpt-4";
        _u.temperature = 0;
        return WasmChatCompletionRequest.fromUpdate(_u);
      })(),
    );
    expect(result.choices["0"].message.content).toBeTruthy();
  });
  it("finish_reason_content_filter: Chat response stopped by content filter with finish_reason of content_filter and null content", async () => {
    const client = createClient(
      "test-key",
      `${process.env.MOCK_SERVER_URL}/fixtures/finish_reason_content_filter`,
    );
    const result = await client.chat(
      (() => {
        const _u = new WasmChatCompletionRequestUpdate();
        _u.messages = [{ content: "Tell me something controversial", role: "user" }];
        _u.model = "gpt-4";
        return WasmChatCompletionRequest.fromUpdate(_u);
      })(),
    );
    expect(result.choices.length).toBe(1);
    expect((result.choices["0"].finishReason ?? "").trim()).toBe("content_filter");
  });
  it("finish_reason_length: Chat response truncated due to max_tokens limit with finish_reason of length", async () => {
    const client = createClient(
      "test-key",
      `${process.env.MOCK_SERVER_URL}/fixtures/finish_reason_length`,
    );
    const result = await client.chat(
      (() => {
        const _u = new WasmChatCompletionRequestUpdate();
        _u.maxTokens = 5;
        _u.messages = [{ content: "Tell me a long story", role: "user" }];
        _u.model = "gpt-4";
        return WasmChatCompletionRequest.fromUpdate(_u);
      })(),
    );
    expect(result.choices.length).toBe(1);
    expect((result.choices["0"].finishReason ?? "").trim()).toBe("length");
  });
  it("multi_turn_conversation: Multi-turn conversation with system, user, assistant, and follow-up user messages", async () => {
    const client = createClient(
      "test-key",
      `${process.env.MOCK_SERVER_URL}/fixtures/multi_turn_conversation`,
    );
    const result = await client.chat(
      (() => {
        const _u = new WasmChatCompletionRequestUpdate();
        _u.messages = [
          { content: "You are a helpful assistant.", role: "system" },
          { content: "What is 2 + 2?", role: "user" },
          { content: "2 + 2 equals 4.", role: "assistant" },
          { content: "And what is 4 + 4?", role: "user" },
        ];
        _u.model = "gpt-4";
        return WasmChatCompletionRequest.fromUpdate(_u);
      })(),
    );
    expect(result.choices.length).toBe(1);
    expect((result.choices["0"].message.content ?? "").trim()).toBe("4 + 4 equals 8.");
    expect((result.choices["0"].finishReason ?? "").trim()).toBe("stop");
  });
  it("parallel_tool_calls: Chat request that results in parallel tool calls in the response", async () => {
    const client = createClient(
      "test-key",
      `${process.env.MOCK_SERVER_URL}/fixtures/parallel_tool_calls`,
    );
    const result = await client.chat(
      (() => {
        const _u = new WasmChatCompletionRequestUpdate();
        _u.messages = [{ content: "What is the weather in NYC and London?", role: "user" }];
        _u.model = "gpt-4";
        _u.parallelToolCalls = true;
        _u.tools = [
          {
            function: {
              description: "Get the current weather for a given location",
              name: "get_weather",
              parameters: {
                properties: { location: { description: "The city name", type: "string" } },
                required: ["location"],
                type: "object",
              },
            },
            type: "function",
          },
        ];
        return WasmChatCompletionRequest.fromUpdate(_u);
      })(),
    );
    expect(result.choices.length).toBe(1);
    expect(result.choices["0"].message.toolCalls).toBeTruthy();
    expect(result.choices["0"].message.toolCalls.length).toBe(2);
    expect((result.choices["0"].finishReason ?? "").trim()).toBe("tool_calls");
  });
  it("response_format_json_object: Chat request with response_format json_object that returns valid JSON content", async () => {
    const client = createClient(
      "test-key",
      `${process.env.MOCK_SERVER_URL}/fixtures/response_format_json_object`,
    );
    const result = await client.chat(
      (() => {
        const _u = new WasmChatCompletionRequestUpdate();
        _u.messages = [
          { content: "Respond with JSON only.", role: "system" },
          { content: "Give me a user object with name and age fields.", role: "user" },
        ];
        _u.model = "gpt-4";
        _u.responseFormat = { type: "json_object" };
        return WasmChatCompletionRequest.fromUpdate(_u);
      })(),
    );
    expect(result.choices.length).toBe(1);
    expect(result.choices["0"].message.content).toBeTruthy();
    expect((result.choices["0"].finishReason ?? "").trim()).toBe("stop");
  });
  it("response_format_json_schema: Chat request with response_format json_schema that validates the output structure", async () => {
    const client = createClient(
      "test-key",
      `${process.env.MOCK_SERVER_URL}/fixtures/response_format_json_schema`,
    );
    const result = await client.chat(
      (() => {
        const _u = new WasmChatCompletionRequestUpdate();
        _u.messages = [{ content: "What is the temperature in Paris today?", role: "user" }];
        _u.model = "gpt-4";
        _u.responseFormat = {
          jsonSchema: {
            name: "weather",
            schema: {
              properties: { temp: { type: "number" } },
              required: ["temp"],
              type: "object",
            },
          },
          type: "json_schema",
        };
        return WasmChatCompletionRequest.fromUpdate(_u);
      })(),
    );
    expect(result.choices.length).toBe(1);
    expect(result.choices["0"].message.content).toBeTruthy();
    expect((result.choices["0"].finishReason ?? "").trim()).toBe("stop");
  });
  it("seed_parameter: Chat request with seed parameter for deterministic output; response includes system_fingerprint", async () => {
    const client = createClient(
      "test-key",
      `${process.env.MOCK_SERVER_URL}/fixtures/seed_parameter`,
    );
    const result = await client.chat(
      (() => {
        const _u = new WasmChatCompletionRequestUpdate();
        _u.messages = [{ content: "Pick a random number", role: "user" }];
        _u.model = "gpt-4";
        _u.seed = 42;
        return WasmChatCompletionRequest.fromUpdate(_u);
      })(),
    );
    expect(result.choices.length).toBe(1);
    expect((result.choices["0"].finishReason ?? "").trim()).toBe("stop");
    expect(result.systemFingerprint).toBeTruthy();
  });
  it("stop_sequences: Chat request with custom stop sequences that terminates generation at a stop token", async () => {
    const client = createClient(
      "test-key",
      `${process.env.MOCK_SERVER_URL}/fixtures/stop_sequences`,
    );
    const result = await client.chat(
      (() => {
        const _u = new WasmChatCompletionRequestUpdate();
        _u.messages = [{ content: "List items until you see STOP", role: "user" }];
        _u.model = "gpt-4";
        _u.stop = ["STOP", "END"];
        return WasmChatCompletionRequest.fromUpdate(_u);
      })(),
    );
    expect(result.choices.length).toBe(1);
    expect((result.choices["0"].finishReason ?? "").trim()).toBe("stop");
  });
  it("tool_choice_required: Chat request with tool_choice set to required forces the model to call a tool", async () => {
    const client = createClient(
      "test-key",
      `${process.env.MOCK_SERVER_URL}/fixtures/tool_choice_required`,
    );
    const result = await client.chat(
      (() => {
        const _u = new WasmChatCompletionRequestUpdate();
        _u.messages = [{ content: "What is the weather today?", role: "user" }];
        _u.model = "gpt-4";
        _u.toolChoice = "required";
        _u.tools = [
          {
            function: {
              description: "Get the current weather for a given location",
              name: "get_weather",
              parameters: {
                properties: { location: { description: "The city name", type: "string" } },
                required: ["location"],
                type: "object",
              },
            },
            type: "function",
          },
        ];
        return WasmChatCompletionRequest.fromUpdate(_u);
      })(),
    );
    expect(result.choices.length).toBe(1);
    expect(result.choices["0"].message.toolCalls).toBeTruthy();
    expect(result.choices["0"].message.toolCalls["0"].function.name.trim()).toBe("get_weather");
    expect((result.choices["0"].finishReason ?? "").trim()).toBe("tool_calls");
  });
  it("tool_choice_specific: Chat request with tool_choice specifying a particular function to call", async () => {
    const client = createClient(
      "test-key",
      `${process.env.MOCK_SERVER_URL}/fixtures/tool_choice_specific`,
    );
    const result = await client.chat(
      (() => {
        const _u = new WasmChatCompletionRequestUpdate();
        _u.messages = [{ content: "What is the weather in Paris?", role: "user" }];
        _u.model = "gpt-4";
        _u.toolChoice = { function: { name: "get_weather" }, type: "function" };
        _u.tools = [
          {
            function: {
              description: "Get the current weather for a given location",
              name: "get_weather",
              parameters: {
                properties: { location: { description: "The city name", type: "string" } },
                required: ["location"],
                type: "object",
              },
            },
            type: "function",
          },
          {
            function: {
              description: "Search the web for information",
              name: "search_web",
              parameters: {
                properties: { query: { description: "The search query", type: "string" } },
                required: ["query"],
                type: "object",
              },
            },
            type: "function",
          },
        ];
        return WasmChatCompletionRequest.fromUpdate(_u);
      })(),
    );
    expect(result.choices.length).toBe(1);
    expect(result.choices["0"].message.toolCalls).toBeTruthy();
    expect(result.choices["0"].message.toolCalls["0"].function.name.trim()).toBe("get_weather");
    expect((result.choices["0"].finishReason ?? "").trim()).toBe("tool_calls");
  });
});
