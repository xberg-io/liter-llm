// This file is auto-generated by alef — DO NOT EDIT.
// alef:hash:35ea5a986a303bb6684f1269fab95b4ae6fc063d878a3b3b3a397b106f1f9092
// To regenerate: alef generate
// To verify freshness: alef verify --exit-code
// Issues & docs: https://github.com/kreuzberg-dev/alef

import { describe, expect, it } from "vitest";
import {
  createClient,
  WasmChatCompletionRequest,
  WasmFunctionDefinition,
  WasmChatCompletionTool,
  WasmStreamOptions,
  WasmFinishReason,
} from "liter_llm";
function _alefE2eText(value: unknown): string {
  return value == null ? "" : String(value);
}

function _alefE2eItemTexts(item: unknown): string[] {
  if (item == null || typeof item !== "object") {
    return [_alefE2eText(item)];
  }
  const record = item as Record<string, unknown>;
  const itemsText = Array.isArray(record.items) ? record.items.map(_alefE2eText).join(" ") : "";
  return [
    _alefE2eText(item),
    _alefE2eText(record.kind),
    _alefE2eText(record.name),
    _alefE2eText(record.source),
    _alefE2eText(record.alias),
    _alefE2eText(record.text),
    _alefE2eText(record.signature),
    itemsText,
  ];
}

describe("error-handling", () => {
  it("anthropic_error_auth: 401 Authentication error returned by the Anthropic API when the API key is invalid", async () => {
    const client = createClient(
      "test-key",
      `${process.env.MOCK_SERVER_URL}/fixtures/anthropic_error_auth`,
    );
    await expect(async () => {
      await client.chat(
        (() => {
          const _u = WasmChatCompletionRequest.default();
          _u.messages = [{ content: "Hello", role: "user" }];
          _u.model = "anthropic/claude-3-5-sonnet-20241022";
          return _u;
        })(),
      );
    }).rejects.toThrow();
  });
  it("auth_401: 401 Unauthorized error when API key is invalid or missing", async () => {
    const client = createClient("test-key", `${process.env.MOCK_SERVER_URL}/fixtures/auth_401`);
    await expect(async () => {
      await client.chat(
        (() => {
          const _u = WasmChatCompletionRequest.default();
          _u.messages = [{ content: "Hello", role: "user" }];
          _u.model = "gpt-4";
          return _u;
        })(),
      );
    }).rejects.toThrow();
  });
  it("azure_error_auth: Azure OpenAI returns a 401 Unauthorized error when the API key is missing or invalid — uses Azure's error envelope shape with code AccessDenied", async () => {
    const client = createClient(
      "test-key",
      `${process.env.MOCK_SERVER_URL}/fixtures/azure_error_auth`,
    );
    await expect(async () => {
      await client.chat(
        (() => {
          const _u = WasmChatCompletionRequest.default();
          _u.messages = [{ content: "Hello", role: "user" }];
          _u.model = "azure/gpt-4";
          return _u;
        })(),
      );
    }).rejects.toThrow();
  });
  it("bad_request_400: 400 Bad Request error when a parameter value is invalid", async () => {
    const client = createClient(
      "test-key",
      `${process.env.MOCK_SERVER_URL}/fixtures/bad_request_400`,
    );
    await expect(async () => {
      await client.chat(
        (() => {
          const _u = WasmChatCompletionRequest.default();
          _u.messages = [{ content: "Hello", role: "user" }];
          _u.model = "gpt-4";
          _u.temperature = 5.0;
          return _u;
        })(),
      );
    }).rejects.toThrow();
  });
  it("bedrock_error_auth: AWS Bedrock returns 403 Forbidden (not 401) when credentials are missing, expired, or the IAM role lacks bedrock:InvokeModel permission — verifies the error is mapped to Authentication", async () => {
    const client = createClient(
      "test-key",
      `${process.env.MOCK_SERVER_URL}/fixtures/bedrock_error_auth`,
    );
    await expect(async () => {
      await client.chat(
        (() => {
          const _u = WasmChatCompletionRequest.default();
          _u.messages = [{ content: "Hello", role: "user" }];
          _u.model = "bedrock/anthropic.claude-3-sonnet-20240229-v1:0";
          return _u;
        })(),
      );
    }).rejects.toThrow();
  });
  it("content_policy_violation: 400 error when a request is rejected due to content policy", async () => {
    const client = createClient(
      "test-key",
      `${process.env.MOCK_SERVER_URL}/fixtures/content_policy_violation`,
    );
    await expect(async () => {
      await client.chat(
        (() => {
          const _u = WasmChatCompletionRequest.default();
          _u.messages = [{ content: "Generate harmful content", role: "user" }];
          _u.model = "gpt-4";
          return _u;
        })(),
      );
    }).rejects.toThrow();
  });
  it("context_window_exceeded: 400 error when the prompt exceeds the model's maximum context length", async () => {
    const client = createClient(
      "test-key",
      `${process.env.MOCK_SERVER_URL}/fixtures/context_window_exceeded`,
    );
    await expect(async () => {
      await client.chat(
        (() => {
          const _u = WasmChatCompletionRequest.default();
          _u.messages = [
            { content: "Very long prompt that exceeds the context window...", role: "user" },
          ];
          _u.model = "gpt-4";
          return _u;
        })(),
      );
    }).rejects.toThrow();
  });
  it("empty_response_body: 200 OK response with an empty JSON object body, missing required fields", async () => {
    const client = createClient(
      "test-key",
      `${process.env.MOCK_SERVER_URL}/fixtures/empty_response_body`,
    );
    await expect(async () => {
      await client.chat(
        (() => {
          const _u = WasmChatCompletionRequest.default();
          _u.messages = [{ content: "Hello", role: "user" }];
          _u.model = "gpt-4";
          return _u;
        })(),
      );
    }).rejects.toThrow();
  });
  it("forbidden_403: 403 Forbidden error when the API key does not have access to the requested resource", async () => {
    const client = createClient(
      "test-key",
      `${process.env.MOCK_SERVER_URL}/fixtures/forbidden_403`,
    );
    await expect(async () => {
      await client.chat(
        (() => {
          const _u = WasmChatCompletionRequest.default();
          _u.messages = [{ content: "Hello", role: "user" }];
          _u.model = "gpt-4";
          return _u;
        })(),
      );
    }).rejects.toThrow();
  });
  it("gateway_timeout_504: 504 Gateway Timeout error when the upstream service times out", async () => {
    const client = createClient(
      "test-key",
      `${process.env.MOCK_SERVER_URL}/fixtures/gateway_timeout_504`,
    );
    await expect(async () => {
      await client.chat(
        (() => {
          const _u = WasmChatCompletionRequest.default();
          _u.messages = [{ content: "Hello", role: "user" }];
          _u.model = "gpt-4";
          return _u;
        })(),
      );
    }).rejects.toThrow();
  });
  it("github_copilot_error_auth: 401 Authentication error returned by the GitHub Copilot API when the token is invalid or expired", async () => {
    const client = createClient(
      "test-key",
      `${process.env.MOCK_SERVER_URL}/fixtures/github_copilot_error_auth`,
    );
    await expect(async () => {
      await client.chat(
        (() => {
          const _u = WasmChatCompletionRequest.default();
          _u.messages = [{ content: "Hello", role: "user" }];
          _u.model = "github_copilot/gpt-4o";
          return _u;
        })(),
      );
    }).rejects.toThrow();
  });
  it("not_found_404: 404 Not Found error when requesting a model that does not exist", async () => {
    const client = createClient(
      "test-key",
      `${process.env.MOCK_SERVER_URL}/fixtures/not_found_404`,
    );
    await expect(async () => {
      await client.chat(
        (() => {
          const _u = WasmChatCompletionRequest.default();
          _u.messages = [{ content: "Hello", role: "user" }];
          _u.model = "gpt-99";
          return _u;
        })(),
      );
    }).rejects.toThrow();
  });
  it("rate_limit_429: 429 Too Many Requests error when the rate limit is exceeded", async () => {
    const client = createClient(
      "test-key",
      `${process.env.MOCK_SERVER_URL}/fixtures/rate_limit_429`,
    );
    await expect(async () => {
      await client.chat(
        (() => {
          const _u = WasmChatCompletionRequest.default();
          _u.messages = [{ content: "Hello", role: "user" }];
          _u.model = "gpt-4";
          return _u;
        })(),
      );
    }).rejects.toThrow();
  });
  it("server_error_500: 500 Internal Server Error from the upstream API", async () => {
    const client = createClient(
      "test-key",
      `${process.env.MOCK_SERVER_URL}/fixtures/server_error_500`,
    );
    await expect(async () => {
      await client.chat(
        (() => {
          const _u = WasmChatCompletionRequest.default();
          _u.messages = [{ content: "Hello", role: "user" }];
          _u.model = "gpt-4";
          return _u;
        })(),
      );
    }).rejects.toThrow();
  });
  it("service_unavailable_502: 502 Bad Gateway error when the upstream service is unavailable", async () => {
    const client = createClient(
      "test-key",
      `${process.env.MOCK_SERVER_URL}/fixtures/service_unavailable_502`,
    );
    await expect(async () => {
      await client.chat(
        (() => {
          const _u = WasmChatCompletionRequest.default();
          _u.messages = [{ content: "Hello", role: "user" }];
          _u.model = "gpt-4";
          return _u;
        })(),
      );
    }).rejects.toThrow();
  });
  it("timeout_error: 408 Request Timeout error when the API request takes too long to complete", async () => {
    const client = createClient(
      "test-key",
      `${process.env.MOCK_SERVER_URL}/fixtures/timeout_error`,
    );
    await expect(async () => {
      await client.chat(
        (() => {
          const _u = WasmChatCompletionRequest.default();
          _u.messages = [{ content: "Hello", role: "user" }];
          _u.model = "gpt-4";
          return _u;
        })(),
      );
    }).rejects.toThrow();
  });
  it("vertex_error_auth: Google Vertex AI returns 401 Unauthorized when the OAuth2 token is missing, expired, or the service account lacks aiplatform.endpoints.predict permission — verifies the error is mapped to Authentication", async () => {
    const client = createClient(
      "test-key",
      `${process.env.MOCK_SERVER_URL}/fixtures/vertex_error_auth`,
    );
    await expect(async () => {
      await client.chat(
        (() => {
          const _u = WasmChatCompletionRequest.default();
          _u.messages = [{ content: "Hello", role: "user" }];
          _u.model = "vertex_ai/gemini-2.0-flash";
          return _u;
        })(),
      );
    }).rejects.toThrow();
  });
});
