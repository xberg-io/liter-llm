// This file is auto-generated by alef — DO NOT EDIT.
// alef:hash:2eacffc5b80eeec10c72a99761a47505daba01d50b2fcc1838a97e9b09701bab
// To regenerate: alef generate
// To verify freshness: alef verify --exit-code
// Issues & docs: https://github.com/kreuzberg-dev/alef

import { describe, expect, it } from "vitest";
import {
  createClient,
  WasmChatCompletionRequest,
  WasmCreateBatchRequest,
  WasmChatCompletionTool,
  WasmFunctionDefinition,
  WasmStreamOptions,
  WasmBatchStatus,
} from "liter_llm";
function _alefE2eText(value: unknown): string {
  return value == null ? "" : String(value);
}

function _alefE2eItemTexts(item: unknown): string[] {
  if (item == null || typeof item !== "object") {
    return [_alefE2eText(item)];
  }
  const record = item as Record<string, unknown>;
  const itemsText = Array.isArray(record.items) ? record.items.map(_alefE2eText).join(" ") : "";
  return [
    _alefE2eText(item),
    _alefE2eText(record.kind),
    _alefE2eText(record.name),
    _alefE2eText(record.source),
    _alefE2eText(record.alias),
    _alefE2eText(record.text),
    _alefE2eText(record.signature),
    itemsText,
  ];
}

describe("batches", () => {
  it("edge_batch_already_cancelled: Attempt to cancel an already-cancelled batch", async () => {
    const client = createClient(
      "test-key",
      `${process.env.MOCK_SERVER_URL}/fixtures/edge_batch_already_cancelled`,
    );
    await expect(async () => {
      await client.cancelBatch("batch-cancelled001");
    }).rejects.toThrow();
  });
  it("edge_batch_empty_list: List batches when no batches exist", async () => {
    const client = createClient(
      "test-key",
      `${process.env.MOCK_SERVER_URL}/fixtures/edge_batch_empty_list`,
    );
    const result = await client.listBatches(undefined);
    expect(result.data.length).toBe(0);
  });
  it("error_batch_auth_401: 401 Unauthorized when creating a batch with invalid API key", async () => {
    const client = createClient(
      "test-key",
      `${process.env.MOCK_SERVER_URL}/fixtures/error_batch_auth_401`,
    );
    await expect(async () => {
      await client.createBatch(
        (() => {
          const _u = WasmCreateBatchRequest.default();
          _u.completionWindow = "24h";
          _u.endpoint = "/v1/chat/completions";
          _u.inputFileId = "file-abc123";
          return _u;
        })(),
      );
    }).rejects.toThrow();
  });
  it("error_batch_invalid_file: 400 Bad Request when creating a batch with invalid input file", async () => {
    const client = createClient(
      "test-key",
      `${process.env.MOCK_SERVER_URL}/fixtures/error_batch_invalid_file`,
    );
    await expect(async () => {
      await client.createBatch(
        (() => {
          const _u = WasmCreateBatchRequest.default();
          _u.completionWindow = "24h";
          _u.endpoint = "/v1/chat/completions";
          _u.inputFileId = "file-wrong-purpose";
          return _u;
        })(),
      );
    }).rejects.toThrow();
  });
  it("error_batch_not_found: 404 Not Found when retrieving a nonexistent batch", async () => {
    const client = createClient(
      "test-key",
      `${process.env.MOCK_SERVER_URL}/fixtures/error_batch_not_found`,
    );
    await expect(async () => {
      await client.retrieveBatch("batch-nonexistent");
    }).rejects.toThrow();
  });
  it("smoke_batch_completed: Retrieve a completed batch job with output file", async () => {
    const client = createClient(
      "test-key",
      `${process.env.MOCK_SERVER_URL}/fixtures/smoke_batch_completed`,
    );
    const result = await client.retrieveBatch("batch-ghi789");
    {
      const _v = result.id;
      if (typeof _v === "string" || Array.isArray(_v)) {
        expect(_v.length).toBeGreaterThan(0);
      } else {
        expect(_v).toBeDefined();
        expect(_v).not.toBeNull();
      }
    }
    expect(result.status).toBe("completed");
  });
  it("smoke_cancel_batch: Cancel a running batch job", async () => {
    const client = createClient(
      "test-key",
      `${process.env.MOCK_SERVER_URL}/fixtures/smoke_cancel_batch`,
    );
    const result = await client.cancelBatch("batch-def456");
    {
      const _v = result.id;
      if (typeof _v === "string" || Array.isArray(_v)) {
        expect(_v.length).toBeGreaterThan(0);
      } else {
        expect(_v).toBeDefined();
        expect(_v).not.toBeNull();
      }
    }
    expect(result.status).toBe("cancelling");
  });
  it("smoke_create_batch: Create a new batch processing job", async () => {
    const client = createClient(
      "test-key",
      `${process.env.MOCK_SERVER_URL}/fixtures/smoke_create_batch`,
    );
    const result = await client.createBatch(
      (() => {
        const _u = WasmCreateBatchRequest.default();
        _u.completionWindow = "24h";
        _u.endpoint = "/v1/chat/completions";
        _u.inputFileId = "file-abc123";
        return _u;
      })(),
    );
    {
      const _v = result.id;
      if (typeof _v === "string" || Array.isArray(_v)) {
        expect(_v.length).toBeGreaterThan(0);
      } else {
        expect(_v).toBeDefined();
        expect(_v).not.toBeNull();
      }
    }
    expect(result.status).toBe("validating");
  });
  it("smoke_list_batches: List all batch jobs", async () => {
    const client = createClient(
      "test-key",
      `${process.env.MOCK_SERVER_URL}/fixtures/smoke_list_batches`,
    );
    const result = await client.listBatches(undefined);
    expect(result.data.length).toBe(2);
  });
  it("smoke_retrieve_batch: Retrieve the status of a batch job", async () => {
    const client = createClient(
      "test-key",
      `${process.env.MOCK_SERVER_URL}/fixtures/smoke_retrieve_batch`,
    );
    const result = await client.retrieveBatch("batch-abc123");
    {
      const _v = result.id;
      if (typeof _v === "string" || Array.isArray(_v)) {
        expect(_v.length).toBeGreaterThan(0);
      } else {
        expect(_v).toBeDefined();
        expect(_v).not.toBeNull();
      }
    }
    expect(result.status).toBe("in_progress");
  });
});
