import { initSync } from "liter_llm";
// This file is auto-generated by alef — DO NOT EDIT.
// alef:hash:c8f794dfd9232795a0ff5bb097cef3275dab7e932c903ee12b43e084e1b84d14
// To regenerate: alef generate
// To verify freshness: alef verify --exit-code
// Issues & docs: https://github.com/kreuzberg-dev/alef

import { describe, expect, it } from "vitest";
import {
  createClient,
  WasmChatCompletionRequest,
  WasmChatCompletionRequestUpdate,
} from "liter_llm";
import { fileURLToPath } from "url";
import { dirname, join } from "path";
import { readFileSync } from "fs";
const __filename = fileURLToPath(import.meta.url);
const __dirname = dirname(__filename);
const testDocumentsDir = join(__dirname, "..", "..", "..", "test_documents");
globalThis.process.chdir(testDocumentsDir);
const wasmUrl = await import.meta.resolve("liter_llm/kreuzberg_wasm_bg.wasm");
const wasmPath = fileURLToPath(wasmUrl);
const wasmBuffer = readFileSync(wasmPath);
initSync(wasmBuffer);

function _alefE2eText(value: unknown): string {
  return value == null ? "" : String(value);
}

function _alefE2eItemTexts(item: unknown): string[] {
  if (item == null || typeof item !== "object") {
    return [_alefE2eText(item)];
  }
  const record = item as Record<string, unknown>;
  const itemsText = Array.isArray(record.items) ? record.items.map(_alefE2eText).join(" ") : "";
  return [
    _alefE2eText(item),
    _alefE2eText(record.kind),
    _alefE2eText(record.name),
    _alefE2eText(record.source),
    _alefE2eText(record.alias),
    _alefE2eText(record.text),
    _alefE2eText(record.signature),
    itemsText,
  ];
}

describe("contract", () => {
  it("binding_api_parity: Verify all bindings expose the full API surface — constructor accepts all config options and every method exists", async () => {
    const client = createClient(
      "test-key",
      `${process.env.MOCK_SERVER_URL}/fixtures/binding_api_parity`,
    );
    const result = await client.chat(
      (() => {
        const _u = new WasmChatCompletionRequestUpdate();
        _u.messages = [{ content: "Contract test", role: "user" }];
        _u.model = "openai/gpt-4o";
        return WasmChatCompletionRequest.fromUpdate(_u);
      })(),
    );
    expect(result.choices.length).toBe(1);
    expect((result.choices["0"].message.content ?? "").trim()).toBe("OK");
    expect((result.choices["0"].finishReason ?? "").trim()).toBe("stop");
    expect(result.usage.totalTokens).toBe(6);
    expect(result.model.trim()).toBe("gpt-4o");
  });
  it("contract_ocr: Verify ocr() method exists in all bindings", async () => {
    const client = createClient("test-key", `${process.env.MOCK_SERVER_URL}/fixtures/contract_ocr`);
    await client.ocr(
      (() => {
        const _u = new WasmChatCompletionRequestUpdate();
        _u.document = { type: "document_url", url: "https://example.com/contract-test.pdf" };
        _u.model = "mistral/mistral-ocr-latest";
        return WasmChatCompletionRequest.fromUpdate(_u);
      })(),
    );
  });
  it("contract_search: Verify search() method exists in all bindings", async () => {
    const client = createClient(
      "test-key",
      `${process.env.MOCK_SERVER_URL}/fixtures/contract_search`,
    );
    await client.search(
      (() => {
        const _u = new WasmChatCompletionRequestUpdate();
        _u.model = "brave/web-search";
        _u.query = "contract test query";
        return WasmChatCompletionRequest.fromUpdate(_u);
      })(),
    );
  });
});
