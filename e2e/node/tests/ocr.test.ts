// This file is auto-generated by alef — DO NOT EDIT.
// alef:hash:0ad579c4e6402a59f477d57e2df40488d9809a1a212e52fe47279587ff8c442e
// To regenerate: alef generate
// To verify freshness: alef verify --exit-code
// Issues & docs: https://github.com/sample_crate-dev/alef

import { describe, expect, it } from 'vitest';import { createClient } from '@kreuzberg/liter-llm-node';
function _alefE2eText(value: unknown): string {
  return value == null ? "" : String(value);
}

function _alefE2eItemTexts(item: unknown): string[] {
  if (item == null || typeof item !== "object") {
    return [_alefE2eText(item)];
  }
  const record = item as Record<string, unknown>;
  const itemsText = Array.isArray(record.items) ? record.items.map(_alefE2eText).join(" ") : "";
  return [_alefE2eText(item), _alefE2eText(record.kind), _alefE2eText(record.name), _alefE2eText(record.source), _alefE2eText(record.alias), _alefE2eText(record.text), _alefE2eText(record.signature), itemsText];
}

function _alefE2eFormatMetadataDisplay(fm: unknown): string {
  if (fm == null) return "";
  if (typeof fm !== "object") return String(fm);
  const record = fm as Record<string, unknown>;
  const formatType = record.format_type;

  // FormatMetadata is a tagged union: { format_type: 'image', image: { format: 'PNG', ... }, ... }
  // Extract the display string based on the variant type
  if (formatType === "image" && typeof record.image === "object") {
    const imageData = record.image as Record<string, unknown>;
    if (typeof imageData.format === "string") return imageData.format;
  }

  // Fallback: return format_type variant name
  if (typeof record.format_type === "string") return record.format_type;
  return "";
}


describe('ocr', () => {  it('ocr_error_400: 400 Bad Request error when OCR input has an invalid image format', async () => {    const client = createClient('test-key', `${process.env.MOCK_SERVER_URL}/fixtures/ocr_error_400`);    await expect(async () => {      await client.ocr({ document: { type: "document_url", url: "invalid://url" }, model: "mistral/mistral-ocr-latest" });    }).rejects.toThrow();
  }, 30000);
  it('ocr_error_401: 401 Unauthorized error on OCR request due to invalid API credentials', async () => {    const client = createClient('test-key', `${process.env.MOCK_SERVER_URL}/fixtures/ocr_error_401`);    await expect(async () => {      await client.ocr({ document: { type: "document_url", url: "https://example.com/doc.pdf" }, model: "mistral/mistral-ocr-latest" });    }).rejects.toThrow();
  }, 30000);
  it('ocr_multi_page: OCR request returning multiple pages of document content', async () => {    const client = createClient('test-key', `${process.env.MOCK_SERVER_URL}/fixtures/ocr_multi_page`);    const result = await client.ocr({ document: { type: "document_url", url: "https://example.com/multipage.pdf" }, model: "mistral/mistral-ocr-latest" });    expect(result.pages.length).toBe(2);
    expect(result.pages[0].index).toBe(0);
    expect(result.pages[1].index).toBe(1);

  }, 30000);
  it('ocr_url_document: OCR request with a document URL input', async () => {    const client = createClient('test-key', `${process.env.MOCK_SERVER_URL}/fixtures/ocr_url_document`);    await client.ocr({ document: { type: "document_url", url: "https://example.com/doc.pdf" }, model: "mistral/mistral-ocr-latest" });
  }, 30000);
});
