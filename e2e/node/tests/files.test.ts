// This file is auto-generated by alef — DO NOT EDIT.
// alef:hash:de9738d1a2c1e8c5eacecbd4da06878683f3650e2b3bcc2ce0cb163d94f425f0
// To regenerate: alef generate
// To verify freshness: alef verify --exit-code
// Issues & docs: https://github.com/sample_crate-dev/alef

import { describe, expect, it } from 'vitest';import { createClient } from '@kreuzberg/liter-llm-node';
function _alefE2eText(value: unknown): string {
  return value == null ? "" : String(value);
}

function _alefE2eItemTexts(item: unknown): string[] {
  if (item == null || typeof item !== "object") {
    return [_alefE2eText(item)];
  }
  const record = item as Record<string, unknown>;
  const itemsText = Array.isArray(record.items) ? record.items.map(_alefE2eText).join(" ") : "";
  return [_alefE2eText(item), _alefE2eText(record.kind), _alefE2eText(record.name), _alefE2eText(record.source), _alefE2eText(record.alias), _alefE2eText(record.text), _alefE2eText(record.signature), itemsText];
}

function _alefE2eFormatMetadataDisplay(fm: unknown): string {
  if (fm == null) return "";
  if (typeof fm !== "object") return String(fm);
  const record = fm as Record<string, unknown>;
  const formatType = record.format_type;

  // FormatMetadata is a tagged union: { format_type: 'image', image: { format: 'PNG', ... }, ... }
  // Extract the display string based on the variant type
  if (formatType === "image" && typeof record.image === "object") {
    const imageData = record.image as Record<string, unknown>;
    if (typeof imageData.format === "string") return imageData.format;
  }

  // Fallback: return format_type variant name
  if (typeof record.format_type === "string") return record.format_type;
  return "";
}


describe('files', () => {  it('edge_file_empty_list: List files when no files have been uploaded', async () => {    const client = createClient('test-key', `${process.env.MOCK_SERVER_URL}/fixtures/edge_file_empty_list`);    const result = await client.listFiles(undefined);    expect(result.data.length).toBe(0);

  }, 30000);
  it('edge_file_large_upload: Upload a large file successfully', async () => {    const client = createClient('test-key', `${process.env.MOCK_SERVER_URL}/fixtures/edge_file_large_upload`);    const result = await client.createFile({ file: "eyJwcm9tcHQiOiAibGFyZ2UgdHJhaW5pbmcgZGF0YSJ9Cg==", filename: "large_training_data.jsonl", purpose: "fine-tune" });    {
        const _v = result.id;
        if (typeof _v === "string" || Array.isArray(_v)) {
            expect(_v.length).toBeGreaterThan(0);
        } else {
            expect(_v).toBeDefined();
            expect(_v).not.toBeNull();
        }
    }

  }, 30000);
  it('error_file_auth_401: 401 Unauthorized when listing files with invalid API key', async () => {    const client = createClient('test-key', `${process.env.MOCK_SERVER_URL}/fixtures/error_file_auth_401`);    await expect(async () => {      await client.listFiles(undefined);    }).rejects.toThrow();
  }, 30000);
  it('error_file_bad_purpose: 400 Bad Request when uploading a file with invalid purpose', async () => {    const client = createClient('test-key', `${process.env.MOCK_SERVER_URL}/fixtures/error_file_bad_purpose`);    await expect(async () => {      await client.createFile({ file: "data.jsonl", purpose: "invalid-purpose" });    }).rejects.toThrow();
  }, 30000);
  it('error_file_not_found: 404 Not Found when retrieving a nonexistent file', async () => {    const client = createClient('test-key', `${process.env.MOCK_SERVER_URL}/fixtures/error_file_not_found`);    await expect(async () => {      await client.retrieveFile("file-nonexistent");    }).rejects.toThrow();
  }, 30000);
  it('smoke_create_file: Upload a file for use with the API', async () => {    const client = createClient('test-key', `${process.env.MOCK_SERVER_URL}/fixtures/smoke_create_file`);    const result = await client.createFile({ file: "eyJwcm9tcHQiOiAiaGVsbG8ifQo=", filename: "training_data.jsonl", purpose: "fine-tune" });    {
        const _v = result.id;
        if (typeof _v === "string" || Array.isArray(_v)) {
            expect(_v.length).toBeGreaterThan(0);
        } else {
            expect(_v).toBeDefined();
            expect(_v).not.toBeNull();
        }
    }

  }, 30000);
  it('smoke_delete_file: Delete an uploaded file', async () => {    const client = createClient('test-key', `${process.env.MOCK_SERVER_URL}/fixtures/smoke_delete_file`);    const result = await client.deleteFile("file-abc123");    {
        const _v = result.id;
        if (typeof _v === "string" || Array.isArray(_v)) {
            expect(_v.length).toBeGreaterThan(0);
        } else {
            expect(_v).toBeDefined();
            expect(_v).not.toBeNull();
        }
    }
    expect(result.deleted).toBe(true);

  }, 30000);
  it('smoke_file_content: Retrieve the content of an uploaded file', async () => {    const client = createClient('test-key', `${process.env.MOCK_SERVER_URL}/fixtures/smoke_file_content`);    const result = await client.fileContent("file-abc123");    expect(result.length).toBeGreaterThan(0);

  }, 30000);
  it('smoke_list_files: List all uploaded files', async () => {    const client = createClient('test-key', `${process.env.MOCK_SERVER_URL}/fixtures/smoke_list_files`);    const result = await client.listFiles(undefined);    expect(result.data.length).toBe(2);

  }, 30000);
  it('smoke_retrieve_file: Retrieve metadata for an uploaded file', async () => {    const client = createClient('test-key', `${process.env.MOCK_SERVER_URL}/fixtures/smoke_retrieve_file`);    const result = await client.retrieveFile("file-abc123");    {
        const _v = result.id;
        if (typeof _v === "string" || Array.isArray(_v)) {
            expect(_v.length).toBeGreaterThan(0);
        } else {
            expect(_v).toBeDefined();
            expect(_v).not.toBeNull();
        }
    }

  }, 30000);
});
