// This file is auto-generated by alef — DO NOT EDIT.
// alef:hash:eb6cc4f5c4e0a8310b37a5dcf77d397ee1e9b23bf2aa67b466e559838d61abc3
// To regenerate: alef generate
// To verify freshness: alef verify --exit-code
// Issues & docs: https://github.com/kreuzberg-dev/alef

import { describe, expect, it } from "vitest";
import { createClient } from "@kreuzberg/liter-llm-node";
function _alefE2eText(value: unknown): string {
  return value == null ? "" : String(value);
}

function _alefE2eItemTexts(item: unknown): string[] {
  if (item == null || typeof item !== "object") {
    return [_alefE2eText(item)];
  }
  const record = item as Record<string, unknown>;
  const itemsText = Array.isArray(record.items) ? record.items.map(_alefE2eText).join(" ") : "";
  return [
    _alefE2eText(item),
    _alefE2eText(record.kind),
    _alefE2eText(record.name),
    _alefE2eText(record.source),
    _alefE2eText(record.alias),
    _alefE2eText(record.text),
    _alefE2eText(record.signature),
    itemsText,
  ];
}

describe("cache", () => {
  it("cache_hit: Tests that identical chat requests return cached response", async () => {
    const client = createClient("test-key", `${process.env.MOCK_SERVER_URL}/fixtures/cache_hit`);
    await client.chat({ messages: [{ content: "Hello", role: "user" }], model: "gpt-4" }); // skipped: field 'cache_hit' not available on result type
  });
  it("cache_miss_ttl: Tests that cache expires after TTL", async () => {
    const client = createClient(
      "test-key",
      `${process.env.MOCK_SERVER_URL}/fixtures/cache_miss_ttl`,
    );
    await client.chat({ messages: [{ content: "Hello", role: "user" }], model: "gpt-4" }); // skipped: field 'cache_hit' not available on result type
  });
  it("cache_opendal_memory: Cache hit with OpenDAL memory backend returns cached response on repeat request", async () => {
    const client = createClient(
      "test-key",
      `${process.env.MOCK_SERVER_URL}/fixtures/cache_opendal_memory`,
    );
    await client.chat({ messages: [{ content: "Hello", role: "user" }], model: "openai/gpt-4o" }); // skipped: field 'cache_hit' not available on result type
  });
  it("cache_stream_bypass: Tests that streaming requests bypass cache entirely", async () => {
    const client = createClient(
      "test-key",
      `${process.env.MOCK_SERVER_URL}/fixtures/cache_stream_bypass`,
    );
    await client.chatStream({ messages: [{ content: "Hello", role: "user" }], model: "gpt-4" }); // skipped: field 'cache_bypassed' not available on result type
  });
});
