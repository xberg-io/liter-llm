// This file is auto-generated by alef — DO NOT EDIT.
// alef:hash:9a6e94386d8a58656779a53565013dd1754ed706a423955d86280140a736bb05
// To regenerate: alef generate
// To verify freshness: alef verify --exit-code
//! E2e tests for category: speech

use liter_llm::BatchClient;
use liter_llm::FileClient;
use liter_llm::LlmClient;
use liter_llm::ResponseClient;
mod common;
mod mock_server;
#[allow(unused_imports)]
use mock_server::{MockRoute, MockServer};

#[tokio::test]
async fn test_edge_speech_all_voices() {
    // Text-to-speech with specific voice selection
    let mock_route = MockRoute {
        path: "/audio/speech",
        method: "POST",
        status: 200,
        body: r#"{"_binary":true,"_content_type":"audio/mpeg","_description":"Mock binary audio data for nova voice"}"#
            .to_string(),
        is_streaming: false,
        stream_chunks: vec![],
        headers: vec![],
        delay_ms: None,
    };
    let mock_server = MockServer::start(vec![mock_route]).await;
    let request_json: serde_json::Value =
        serde_json::from_str(r#"{"input":"Hello world","model":"tts-1","voice":"nova"}"#).unwrap();
    let request = serde_json::from_value(request_json).unwrap();
    let client =
        liter_llm::create_client("test-key".to_string(), Some(mock_server.url.clone()), None, None, None).unwrap();
    let result = client.speech(request).await.expect("should succeed");
    assert!(!result.is_empty(), "expected non-empty value");
}

#[tokio::test]
async fn test_edge_speech_long_input() {
    // Speech generation with a very long input text
    let mock_route = MockRoute {
        path: "/audio/speech",
        method: "POST",
        status: 200,
        body: r#"{"_binary":true,"_content_type":"audio/mpeg","_description":"Mock binary audio data for long text"}"#
            .to_string(),
        is_streaming: false,
        stream_chunks: vec![],
        headers: vec![],
        delay_ms: None,
    };
    let mock_server = MockServer::start(vec![mock_route]).await;
    let request_json: serde_json::Value = serde_json::from_str(r#"{"input":"This is a long input text. This is a long input text. This is a long input text. This is a long input text. This is a long input text. This is a long input text. This is a long input text. This is a long input text. This is a long input text. This is a long input text. This is a long input text. This is a long input text. This is a long input text. This is a long input text. This is a long input text. This is a long input text. This is a long input text. This is a long input text. This is a long input text. This is a long input text. This is a long input text. This is a long input text. This is a long input text. This is a long input text. This is a long input text. This is a long input text. This is a long input text. This is a long input text. This is a long input text. This is a long input text. This is a long input text. This is a long input text. This is a long input text. This is a long input text. This is a long input text. This is a long input text. This is a long input text. This is a long input text. This is a long input text. This is a long input text. This is a long input text. This is a long input text. This is a long input text. This is a long input text. This is a long input text. This is a long input text. This is a long input text. This is a long input text. This is a long input text. This is a long input text. End of input.","model":"tts-1","voice":"echo"}"#).unwrap();
    let request = serde_json::from_value(request_json).unwrap();
    let client =
        liter_llm::create_client("test-key".to_string(), Some(mock_server.url.clone()), None, None, None).unwrap();
    let result = client.speech(request).await.expect("should succeed");
    assert!(!result.is_empty(), "expected non-empty value");
}

#[tokio::test]
async fn test_error_speech_auth_401() {
    // 401 Unauthorized for speech generation with invalid API key
    let mock_route = MockRoute {
        path: "/audio/speech",
        method: "POST",
        status: 401,
        body: r#"{"error":{"code":"invalid_api_key","message":"Incorrect API key provided.","param":null,"type":"invalid_request_error"}}"#.to_string(),
        is_streaming: false,
        stream_chunks: vec![],
        headers: vec![
        ],
        delay_ms: None,
    };
    let mock_server = MockServer::start(vec![mock_route]).await;
    let request_json: serde_json::Value =
        serde_json::from_str(r#"{"input":"Hello","model":"tts-1","voice":"alloy"}"#).unwrap();
    let request = serde_json::from_value(request_json).unwrap();
    let client =
        liter_llm::create_client("test-key".to_string(), Some(mock_server.url.clone()), None, None, None).unwrap();
    let result = client.speech(request).await;
    assert!(result.is_err(), "expected call to fail");
    {
        let __e = result.as_ref().err().unwrap();
        assert!(
            format!("{:?}", __e).contains("Authentication") || __e.to_string().contains("Authentication"),
            "error message mismatch"
        );
    }
    assert_eq!(
        {
            let __err = result.as_ref().err().unwrap();
            __err.status_code()
        },
        401,
        "equals assertion failed"
    );
}

#[tokio::test]
async fn test_error_speech_bad_model() {
    // 400 Bad Request for speech with unsupported model
    let mock_route = MockRoute {
        path: "/audio/speech",
        method: "POST",
        status: 400,
        body: r#"{"error":{"code":"model_not_found","message":"The model 'tts-nonexistent' does not exist.","param":"model","type":"invalid_request_error"}}"#.to_string(),
        is_streaming: false,
        stream_chunks: vec![],
        headers: vec![
        ],
        delay_ms: None,
    };
    let mock_server = MockServer::start(vec![mock_route]).await;
    let request_json: serde_json::Value =
        serde_json::from_str(r#"{"input":"Hello","model":"tts-nonexistent","voice":"alloy"}"#).unwrap();
    let request = serde_json::from_value(request_json).unwrap();
    let client =
        liter_llm::create_client("test-key".to_string(), Some(mock_server.url.clone()), None, None, None).unwrap();
    let result = client.speech(request).await;
    assert!(result.is_err(), "expected call to fail");
    {
        let __e = result.as_ref().err().unwrap();
        assert!(
            format!("{:?}", __e).contains("BadRequest") || __e.to_string().contains("BadRequest"),
            "error message mismatch"
        );
    }
    assert_eq!(
        {
            let __err = result.as_ref().err().unwrap();
            __err.status_code()
        },
        400,
        "equals assertion failed"
    );
}

#[tokio::test]
async fn test_smoke_speech_basic() {
    // Basic text-to-speech generation
    let mock_route = MockRoute {
        path: "/audio/speech",
        method: "POST",
        status: 200,
        body: r#"{"_binary":true,"_content_type":"audio/mpeg","_description":"Mock binary audio data"}"#.to_string(),
        is_streaming: false,
        stream_chunks: vec![],
        headers: vec![],
        delay_ms: None,
    };
    let mock_server = MockServer::start(vec![mock_route]).await;
    let request_json: serde_json::Value =
        serde_json::from_str(r#"{"input":"Hello, world!","model":"tts-1","voice":"alloy"}"#).unwrap();
    let request = serde_json::from_value(request_json).unwrap();
    let client =
        liter_llm::create_client("test-key".to_string(), Some(mock_server.url.clone()), None, None, None).unwrap();
    let result = client.speech(request).await.expect("should succeed");
    assert!(!result.is_empty(), "expected non-empty value");
}

#[tokio::test]
async fn test_smoke_speech_mp3_format() {
    // Text-to-speech with explicit MP3 response format
    let mock_route = MockRoute {
        path: "/audio/speech",
        method: "POST",
        status: 200,
        body: r#"{"_binary":true,"_content_type":"audio/mpeg","_description":"Mock MP3 audio data"}"#.to_string(),
        is_streaming: false,
        stream_chunks: vec![],
        headers: vec![],
        delay_ms: None,
    };
    let mock_server = MockServer::start(vec![mock_route]).await;
    let request_json: serde_json::Value = serde_json::from_str(r#"{"input":"The quick brown fox jumps over the lazy dog.","model":"tts-1-hd","response_format":"mp3","speed":1.0,"voice":"nova"}"#).unwrap();
    let request = serde_json::from_value(request_json).unwrap();
    let client =
        liter_llm::create_client("test-key".to_string(), Some(mock_server.url.clone()), None, None, None).unwrap();
    let result = client.speech(request).await.expect("should succeed");
    assert!(!result.is_empty(), "expected non-empty value");
}
