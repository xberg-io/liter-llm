// This file is auto-generated by alef — DO NOT EDIT.
// alef:hash:27084cd5b0be08912d8f7bf09b56ba4eac9d9fa289ea545abcb9676a0e7e35b6
// To regenerate: alef generate
// To verify freshness: alef verify --exit-code
// Issues & docs: https://example.invalid/alef
//! E2e tests for category: files

use liter_llm::BatchClient;
use liter_llm::FileClient;
use liter_llm::LlmClient;
use liter_llm::ResponseClient;
mod common;
mod mock_server;
#[allow(unused_imports)]
use mock_server::{MockRoute, MockServer};

#[tokio::test]
async fn test_edge_file_empty_list() {
    // List files when no files have been uploaded
    let mock_route = MockRoute {
        path: "/files",
        method: "GET",
        status: 200,
        body: r#"{"data":[],"object":"list"}"#.to_string(),
        is_streaming: false,
        stream_chunks: vec![],
        headers: vec![],
        delay_ms: None,
    };
    let mock_server = MockServer::start(vec![mock_route]).await;
    let client =
        liter_llm::create_client("test-key".to_string(), Some(mock_server.url.clone()), None, None, None).unwrap();
    let result = client.list_files(None).await.expect("should succeed");
    assert_eq!(
        result.data.len(),
        0,
        "expected exactly 0 elements, got {}",
        result.data.len()
    );
}

#[tokio::test]
async fn test_edge_file_large_upload() {
    // Upload a large file successfully
    let mock_route = MockRoute {
        path: "/files",
        method: "POST",
        status: 200,
        body: r#"{"bytes":536870912,"created_at":1711000000,"filename":"large_training_data.jsonl","id":"file-large001","object":"file","purpose":"fine-tune","status":"processed"}"#.to_string(),
        is_streaming: false,
        stream_chunks: vec![],
        headers: vec![
        ],
        delay_ms: None,
    };
    let mock_server = MockServer::start(vec![mock_route]).await;
    let request_json = serde_json::json!({"file": "eyJwcm9tcHQiOiAibGFyZ2UgdHJhaW5pbmcgZGF0YSJ9Cg==", "filename": "large_training_data.jsonl", "purpose": "fine-tune"});
    let request = serde_json::from_value(request_json).unwrap();
    let client =
        liter_llm::create_client("test-key".to_string(), Some(mock_server.url.clone()), None, None, None).unwrap();
    let result = client.create_file(request).await.expect("should succeed");
    assert!(!result.id.is_empty(), "expected non-empty value");
}

#[tokio::test]
async fn test_error_file_auth_401() {
    // 401 Unauthorized when listing files with invalid API key
    let mock_route = MockRoute {
        path: "/files",
        method: "GET",
        status: 401,
        body: r#"{"error":{"code":"invalid_api_key","message":"Incorrect API key provided.","param":null,"type":"invalid_request_error"}}"#.to_string(),
        is_streaming: false,
        stream_chunks: vec![],
        headers: vec![
        ],
        delay_ms: None,
    };
    let mock_server = MockServer::start(vec![mock_route]).await;
    let client =
        liter_llm::create_client("test-key".to_string(), Some(mock_server.url.clone()), None, None, None).unwrap();
    let result = client.list_files(None).await;
    assert!(result.is_err(), "expected call to fail");
    {
        let __e = result.as_ref().err().unwrap();
        assert!(
            format!("{:?}", __e).contains("Authentication") || __e.to_string().contains("Authentication"),
            "error message mismatch"
        );
    }
    assert_eq!(
        {
            let __err = result.as_ref().err().unwrap();
            __err.status_code()
        },
        401,
        "equals assertion failed"
    );
}

#[tokio::test]
async fn test_error_file_not_found() {
    // 404 Not Found when retrieving a nonexistent file
    let mock_route = MockRoute {
        path: "/files",
        method: "GET",
        status: 404,
        body: r#"{"error":{"code":null,"message":"No such File object: file-nonexistent","param":null,"type":"invalid_request_error"}}"#.to_string(),
        is_streaming: false,
        stream_chunks: vec![],
        headers: vec![
        ],
        delay_ms: None,
    };
    let mock_server = MockServer::start(vec![mock_route]).await;
    let file_id = r#"file-nonexistent"#;
    let client =
        liter_llm::create_client("test-key".to_string(), Some(mock_server.url.clone()), None, None, None).unwrap();
    let result = client.retrieve_file(file_id).await;
    assert!(result.is_err(), "expected call to fail");
    {
        let __e = result.as_ref().err().unwrap();
        assert!(
            format!("{:?}", __e).contains("NotFound") || __e.to_string().contains("NotFound"),
            "error message mismatch"
        );
    }
    assert_eq!(
        {
            let __err = result.as_ref().err().unwrap();
            __err.status_code()
        },
        404,
        "equals assertion failed"
    );
}

#[tokio::test]
async fn test_smoke_create_file() {
    // Upload a file for use with the API
    let mock_route = MockRoute {
        path: "/files",
        method: "POST",
        status: 200,
        body: r#"{"bytes":1024,"created_at":1711000000,"filename":"training_data.jsonl","id":"file-abc123","object":"file","purpose":"fine-tune","status":"processed"}"#.to_string(),
        is_streaming: false,
        stream_chunks: vec![],
        headers: vec![
        ],
        delay_ms: None,
    };
    let mock_server = MockServer::start(vec![mock_route]).await;
    let request_json = serde_json::json!({"file": "eyJwcm9tcHQiOiAiaGVsbG8ifQo=", "filename": "training_data.jsonl", "purpose": "fine-tune"});
    let request = serde_json::from_value(request_json).unwrap();
    let client =
        liter_llm::create_client("test-key".to_string(), Some(mock_server.url.clone()), None, None, None).unwrap();
    let result = client.create_file(request).await.expect("should succeed");
    assert!(!result.id.is_empty(), "expected non-empty value");
}

#[tokio::test]
async fn test_smoke_delete_file() {
    // Delete an uploaded file
    let mock_route = MockRoute {
        path: "/files",
        method: "DELETE",
        status: 200,
        body: r#"{"deleted":true,"id":"file-abc123","object":"file"}"#.to_string(),
        is_streaming: false,
        stream_chunks: vec![],
        headers: vec![],
        delay_ms: None,
    };
    let mock_server = MockServer::start(vec![mock_route]).await;
    let file_id = r#"file-abc123"#;
    let client =
        liter_llm::create_client("test-key".to_string(), Some(mock_server.url.clone()), None, None, None).unwrap();
    let result = client.delete_file(file_id).await.expect("should succeed");
    assert!(!result.id.is_empty(), "expected non-empty value");
    assert!(result.deleted, "equals assertion failed");
}

#[tokio::test]
async fn test_smoke_file_content() {
    // Retrieve the content of an uploaded file
    let mock_route = MockRoute {
        path: "/files",
        method: "GET",
        status: 200,
        body: r#"{"_binary":true,"_content_type":"application/octet-stream","_description":"Mock file content bytes"}"#
            .to_string(),
        is_streaming: false,
        stream_chunks: vec![],
        headers: vec![],
        delay_ms: None,
    };
    let mock_server = MockServer::start(vec![mock_route]).await;
    let file_id = r#"file-abc123"#;
    let client =
        liter_llm::create_client("test-key".to_string(), Some(mock_server.url.clone()), None, None, None).unwrap();
    let result = client.file_content(file_id).await.expect("should succeed");
    assert!(!result.is_empty(), "expected non-empty value");
}

#[tokio::test]
async fn test_smoke_list_files() {
    // List all uploaded files
    let mock_route = MockRoute {
        path: "/files",
        method: "GET",
        status: 200,
        body: r#"{"data":[{"bytes":1024,"created_at":1711000000,"filename":"training_data.jsonl","id":"file-abc123","object":"file","purpose":"fine-tune","status":"processed"},{"bytes":2048,"created_at":1711000100,"filename":"batch_input.jsonl","id":"file-def456","object":"file","purpose":"batch","status":"processed"}],"object":"list"}"#.to_string(),
        is_streaming: false,
        stream_chunks: vec![],
        headers: vec![
        ],
        delay_ms: None,
    };
    let mock_server = MockServer::start(vec![mock_route]).await;
    let client =
        liter_llm::create_client("test-key".to_string(), Some(mock_server.url.clone()), None, None, None).unwrap();
    let result = client.list_files(None).await.expect("should succeed");
    assert_eq!(
        result.data.len(),
        2,
        "expected exactly 2 elements, got {}",
        result.data.len()
    );
}

#[tokio::test]
async fn test_smoke_retrieve_file() {
    // Retrieve metadata for an uploaded file
    let mock_route = MockRoute {
        path: "/files",
        method: "GET",
        status: 200,
        body: r#"{"bytes":1024,"created_at":1711000000,"filename":"training_data.jsonl","id":"file-abc123","object":"file","purpose":"fine-tune","status":"processed"}"#.to_string(),
        is_streaming: false,
        stream_chunks: vec![],
        headers: vec![
        ],
        delay_ms: None,
    };
    let mock_server = MockServer::start(vec![mock_route]).await;
    let file_id = r#"file-abc123"#;
    let client =
        liter_llm::create_client("test-key".to_string(), Some(mock_server.url.clone()), None, None, None).unwrap();
    let result = client.retrieve_file(file_id).await.expect("should succeed");
    assert!(!result.id.is_empty(), "expected non-empty value");
}
