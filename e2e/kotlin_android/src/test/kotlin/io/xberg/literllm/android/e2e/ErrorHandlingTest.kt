// This file is auto-generated by alef — DO NOT EDIT.
// alef:hash:4f25a8b7b3dfd89a506245762ff0a9c88ee0f1210dca8ef7077492f4c0f2854b
// To regenerate: alef generate
// To verify freshness: alef verify --exit-code
package io.xberg.literllm.android.e2e

import org.junit.jupiter.api.Test
import kotlin.test.assertEquals
import kotlin.test.assertTrue
import kotlin.test.assertFalse
import kotlin.test.assertFailsWith
import kotlinx.coroutines.runBlocking
import io.xberg.literllm.android.LiterLlm
import com.fasterxml.jackson.databind.ObjectMapper
import com.fasterxml.jackson.datatype.jdk8.Jdk8Module
import com.fasterxml.jackson.module.kotlin.registerKotlinModule
import io.xberg.literllm.android.ChatCompletionRequest

/** E2e tests for category: error-handling. */
class ErrorHandlingTest {

    companion object {
        init {
            try {
                System.loadLibrary("literllm_jni")
            } catch (e: UnsatisfiedLinkError) {
                System.err.println("Failed to load literllm_jni library: ${e.message}")
                val libPath = System.getProperty("java.library.path")
                System.err.println("java.library.path: $libPath")
                throw e
            }
        }
        private val MAPPER = ObjectMapper().registerModule(Jdk8Module()).registerKotlinModule().setPropertyNamingStrategy(com.fasterxml.jackson.databind.PropertyNamingStrategies.SNAKE_CASE)
    }
    @Test
    fun testAnthropicErrorAuth() = runBlocking {
        // 401 Authentication error returned by the Anthropic API when the API key is invalid
        assertFailsWith<Exception> {
            val request = MAPPER.readValue("{\"messages\":[{\"content\":\"Hello\",\"role\":\"user\"}],\"model\":\"anthropic/claude-3-5-sonnet-20241022\"}", ChatCompletionRequest::class.java)
            val client = LiterLlm.createClient(apiKey = "test-key", baseUrl = System.getProperty("mockServer.anthropic_error_auth", (System.getProperty("mockServerUrl", System.getenv("MOCK_SERVER_URL") ?: "") ?: "") + "/fixtures/anthropic_error_auth"))
            client.chat(request)
            client.close()
        }
        Unit
    }

    @Test
    fun testAuth401() = runBlocking {
        // 401 Unauthorized error when API key is invalid or missing
        assertFailsWith<Exception> {
            val request = MAPPER.readValue("{\"messages\":[{\"content\":\"Hello\",\"role\":\"user\"}],\"model\":\"gpt-4\"}", ChatCompletionRequest::class.java)
            val client = LiterLlm.createClient(apiKey = "test-key", baseUrl = System.getProperty("mockServer.auth_401", (System.getProperty("mockServerUrl", System.getenv("MOCK_SERVER_URL") ?: "") ?: "") + "/fixtures/auth_401"))
            client.chat(request)
            client.close()
        }
        Unit
    }

    @Test
    fun testAzureErrorAuth() = runBlocking {
        // Azure OpenAI returns a 401 Unauthorized error when the API key is missing or invalid — uses Azure's error envelope shape with code AccessDenied
        assertFailsWith<Exception> {
            val request = MAPPER.readValue("{\"messages\":[{\"content\":\"Hello\",\"role\":\"user\"}],\"model\":\"azure/gpt-4\"}", ChatCompletionRequest::class.java)
            val client = LiterLlm.createClient(apiKey = "test-key", baseUrl = System.getProperty("mockServer.azure_error_auth", (System.getProperty("mockServerUrl", System.getenv("MOCK_SERVER_URL") ?: "") ?: "") + "/fixtures/azure_error_auth"))
            client.chat(request)
            client.close()
        }
        Unit
    }

    @Test
    fun testBadRequest400() = runBlocking {
        // 400 Bad Request error when a parameter value is invalid
        assertFailsWith<Exception> {
            val request = MAPPER.readValue("{\"messages\":[{\"content\":\"Hello\",\"role\":\"user\"}],\"model\":\"gpt-4\",\"temperature\":5.0}", ChatCompletionRequest::class.java)
            val client = LiterLlm.createClient(apiKey = "test-key", baseUrl = System.getProperty("mockServer.bad_request_400", (System.getProperty("mockServerUrl", System.getenv("MOCK_SERVER_URL") ?: "") ?: "") + "/fixtures/bad_request_400"))
            client.chat(request)
            client.close()
        }
        Unit
    }

    @Test
    fun testBedrockErrorAuth() = runBlocking {
        // AWS Bedrock returns 403 Forbidden (not 401) when credentials are missing, expired, or the IAM role lacks bedrock:InvokeModel permission — verifies the error is mapped to Authentication
        assertFailsWith<Exception> {
            val request = MAPPER.readValue("{\"messages\":[{\"content\":\"Hello\",\"role\":\"user\"}],\"model\":\"bedrock/anthropic.claude-3-sonnet-20240229-v1:0\"}", ChatCompletionRequest::class.java)
            val client = LiterLlm.createClient(apiKey = "test-key", baseUrl = System.getProperty("mockServer.bedrock_error_auth", (System.getProperty("mockServerUrl", System.getenv("MOCK_SERVER_URL") ?: "") ?: "") + "/fixtures/bedrock_error_auth"))
            client.chat(request)
            client.close()
        }
        Unit
    }

    @Test
    fun testContentPolicyViolation() = runBlocking {
        // 400 error when a request is rejected due to content policy
        assertFailsWith<Exception> {
            val request = MAPPER.readValue("{\"messages\":[{\"content\":\"Generate harmful content\",\"role\":\"user\"}],\"model\":\"gpt-4\"}", ChatCompletionRequest::class.java)
            val client = LiterLlm.createClient(apiKey = "test-key", baseUrl = System.getProperty("mockServer.content_policy_violation", (System.getProperty("mockServerUrl", System.getenv("MOCK_SERVER_URL") ?: "") ?: "") + "/fixtures/content_policy_violation"))
            client.chat(request)
            client.close()
        }
        Unit
    }

    @Test
    fun testContextWindowExceeded() = runBlocking {
        // 400 error when the prompt exceeds the model's maximum context length
        assertFailsWith<Exception> {
            val request = MAPPER.readValue("{\"messages\":[{\"content\":\"Very long prompt that exceeds the context window...\",\"role\":\"user\"}],\"model\":\"gpt-4\"}", ChatCompletionRequest::class.java)
            val client = LiterLlm.createClient(apiKey = "test-key", baseUrl = System.getProperty("mockServer.context_window_exceeded", (System.getProperty("mockServerUrl", System.getenv("MOCK_SERVER_URL") ?: "") ?: "") + "/fixtures/context_window_exceeded"))
            client.chat(request)
            client.close()
        }
        Unit
    }

    @Test
    fun testEmptyResponseBody() = runBlocking {
        // 200 OK response with an empty JSON object body, missing required fields
        assertFailsWith<Exception> {
            val request = MAPPER.readValue("{\"messages\":[{\"content\":\"Hello\",\"role\":\"user\"}],\"model\":\"gpt-4\"}", ChatCompletionRequest::class.java)
            val client = LiterLlm.createClient(apiKey = "test-key", baseUrl = System.getProperty("mockServer.empty_response_body", (System.getProperty("mockServerUrl", System.getenv("MOCK_SERVER_URL") ?: "") ?: "") + "/fixtures/empty_response_body"))
            client.chat(request)
            client.close()
        }
        Unit
    }

    @Test
    fun testForbidden403() = runBlocking {
        // 403 Forbidden error when the API key does not have access to the requested resource
        assertFailsWith<Exception> {
            val request = MAPPER.readValue("{\"messages\":[{\"content\":\"Hello\",\"role\":\"user\"}],\"model\":\"gpt-4\"}", ChatCompletionRequest::class.java)
            val client = LiterLlm.createClient(apiKey = "test-key", baseUrl = System.getProperty("mockServer.forbidden_403", (System.getProperty("mockServerUrl", System.getenv("MOCK_SERVER_URL") ?: "") ?: "") + "/fixtures/forbidden_403"))
            client.chat(request)
            client.close()
        }
        Unit
    }

    @Test
    fun testGatewayTimeout504() = runBlocking {
        // 504 Gateway Timeout error when the upstream service times out
        assertFailsWith<Exception> {
            val request = MAPPER.readValue("{\"messages\":[{\"content\":\"Hello\",\"role\":\"user\"}],\"model\":\"gpt-4\"}", ChatCompletionRequest::class.java)
            val client = LiterLlm.createClient(apiKey = "test-key", baseUrl = System.getProperty("mockServer.gateway_timeout_504", (System.getProperty("mockServerUrl", System.getenv("MOCK_SERVER_URL") ?: "") ?: "") + "/fixtures/gateway_timeout_504"))
            client.chat(request)
            client.close()
        }
        Unit
    }

    @Test
    fun testGithubCopilotErrorAuth() = runBlocking {
        // 401 Authentication error returned by the GitHub Copilot API when the token is invalid or expired
        assertFailsWith<Exception> {
            val request = MAPPER.readValue("{\"messages\":[{\"content\":\"Hello\",\"role\":\"user\"}],\"model\":\"github_copilot/gpt-4o\"}", ChatCompletionRequest::class.java)
            val client = LiterLlm.createClient(apiKey = "test-key", baseUrl = System.getProperty("mockServer.github_copilot_error_auth", (System.getProperty("mockServerUrl", System.getenv("MOCK_SERVER_URL") ?: "") ?: "") + "/fixtures/github_copilot_error_auth"))
            client.chat(request)
            client.close()
        }
        Unit
    }

    @Test
    fun testNotFound404() = runBlocking {
        // 404 Not Found error when requesting a model that does not exist
        assertFailsWith<Exception> {
            val request = MAPPER.readValue("{\"messages\":[{\"content\":\"Hello\",\"role\":\"user\"}],\"model\":\"gpt-99\"}", ChatCompletionRequest::class.java)
            val client = LiterLlm.createClient(apiKey = "test-key", baseUrl = System.getProperty("mockServer.not_found_404", (System.getProperty("mockServerUrl", System.getenv("MOCK_SERVER_URL") ?: "") ?: "") + "/fixtures/not_found_404"))
            client.chat(request)
            client.close()
        }
        Unit
    }

    @Test
    fun testRateLimit429() = runBlocking {
        // 429 Too Many Requests error when the rate limit is exceeded
        assertFailsWith<Exception> {
            val request = MAPPER.readValue("{\"messages\":[{\"content\":\"Hello\",\"role\":\"user\"}],\"model\":\"gpt-4\"}", ChatCompletionRequest::class.java)
            val client = LiterLlm.createClient(apiKey = "test-key", baseUrl = System.getProperty("mockServer.rate_limit_429", (System.getProperty("mockServerUrl", System.getenv("MOCK_SERVER_URL") ?: "") ?: "") + "/fixtures/rate_limit_429"))
            client.chat(request)
            client.close()
        }
        Unit
    }

    @Test
    fun testServerError500() = runBlocking {
        // 500 Internal Server Error from the upstream API
        assertFailsWith<Exception> {
            val request = MAPPER.readValue("{\"messages\":[{\"content\":\"Hello\",\"role\":\"user\"}],\"model\":\"gpt-4\"}", ChatCompletionRequest::class.java)
            val client = LiterLlm.createClient(apiKey = "test-key", baseUrl = System.getProperty("mockServer.server_error_500", (System.getProperty("mockServerUrl", System.getenv("MOCK_SERVER_URL") ?: "") ?: "") + "/fixtures/server_error_500"))
            client.chat(request)
            client.close()
        }
        Unit
    }

    @Test
    fun testServiceUnavailable502() = runBlocking {
        // 502 Bad Gateway error when the upstream service is unavailable
        assertFailsWith<Exception> {
            val request = MAPPER.readValue("{\"messages\":[{\"content\":\"Hello\",\"role\":\"user\"}],\"model\":\"gpt-4\"}", ChatCompletionRequest::class.java)
            val client = LiterLlm.createClient(apiKey = "test-key", baseUrl = System.getProperty("mockServer.service_unavailable_502", (System.getProperty("mockServerUrl", System.getenv("MOCK_SERVER_URL") ?: "") ?: "") + "/fixtures/service_unavailable_502"))
            client.chat(request)
            client.close()
        }
        Unit
    }

    @Test
    fun testTimeoutError() = runBlocking {
        // 408 Request Timeout error when the API request takes too long to complete
        assertFailsWith<Exception> {
            val request = MAPPER.readValue("{\"messages\":[{\"content\":\"Hello\",\"role\":\"user\"}],\"model\":\"gpt-4\"}", ChatCompletionRequest::class.java)
            val client = LiterLlm.createClient(apiKey = "test-key", baseUrl = System.getProperty("mockServer.timeout_error", (System.getProperty("mockServerUrl", System.getenv("MOCK_SERVER_URL") ?: "") ?: "") + "/fixtures/timeout_error"))
            client.chat(request)
            client.close()
        }
        Unit
    }

    @Test
    fun testVertexErrorAuth() = runBlocking {
        // Google Vertex AI returns 401 Unauthorized when the OAuth2 token is missing, expired, or the service account lacks aiplatform.endpoints.predict permission — verifies the error is mapped to Authentication
        assertFailsWith<Exception> {
            val request = MAPPER.readValue("{\"messages\":[{\"content\":\"Hello\",\"role\":\"user\"}],\"model\":\"vertex_ai/gemini-2.0-flash\"}", ChatCompletionRequest::class.java)
            val client = LiterLlm.createClient(apiKey = "test-key", baseUrl = System.getProperty("mockServer.vertex_error_auth", (System.getProperty("mockServerUrl", System.getenv("MOCK_SERVER_URL") ?: "") ?: "") + "/fixtures/vertex_error_auth"))
            client.chat(request)
            client.close()
        }
        Unit
    }

}
