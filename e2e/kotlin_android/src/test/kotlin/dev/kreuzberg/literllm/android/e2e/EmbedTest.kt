// This file is auto-generated by alef — DO NOT EDIT.
// alef:hash:633261343872e302efa6a7b4a7e0c19aa28d94bc699fbee3af8bbaf95f603e9a
// To regenerate: alef generate
// To verify freshness: alef verify --exit-code
// Issues & docs: https://github.com/kreuzberg-dev/alef
package dev.kreuzberg.literllm.android.e2e

import org.junit.jupiter.api.Test
import kotlin.test.assertEquals
import kotlin.test.assertTrue
import kotlin.test.assertFalse
import kotlin.test.assertFailsWith
import kotlinx.coroutines.runBlocking
import dev.kreuzberg.literllm.android.LiterLlm
import com.fasterxml.jackson.databind.ObjectMapper
import com.fasterxml.jackson.datatype.jdk8.Jdk8Module
import dev.kreuzberg.literllm.android.EmbeddingRequest

/** E2e tests for category: embed. */
class EmbedTest {

    companion object {
        private val MAPPER = ObjectMapper().registerModule(Jdk8Module()).setPropertyNamingStrategy(com.fasterxml.jackson.databind.PropertyNamingStrategies.SNAKE_CASE)
    }
    @Test
    fun testBatchEmbed() = runBlocking {
        // Embedding request with multiple input strings returns one embedding object per input
        val request = MAPPER.readValue("{\"input\":[\"Hello\",\"World\"],\"model\":\"text-embedding-3-small\"}", EmbeddingRequest::class.java)
        val result = LiterLlm.embed(request)
        assertEquals(2, result.data().size, "expected exactly 2 elements")
        assertEquals(5, result.data().first().embedding().size, "expected exactly 5 elements")
    }

    @Test
    fun testEdgeEmbedBatchInput() = runBlocking {
        // Embedding request with multiple inputs returns multiple embedding objects
        val request = MAPPER.readValue("{\"input\":[\"Hello world\",\"Goodbye world\"],\"model\":\"text-embedding-3-small\"}", EmbeddingRequest::class.java)
        val result = LiterLlm.embed(request)
        assertEquals(2, result.data().size, "expected exactly 2 elements")
        assertEquals(0, result.data().first().index())
        assertEquals(1, result.data().get(1).index())
    }

    @Test
    fun testEdgeEmbedEmptyInput() = runBlocking {
        // Embedding request with empty string input returns empty data array
        val request = MAPPER.readValue("{\"input\":\"\",\"model\":\"text-embedding-3-small\"}", EmbeddingRequest::class.java)
        val result = LiterLlm.embed(request)
        assertEquals(0, result.data().size, "expected exactly 0 elements")
    }

    @Test
    fun testEmbedEncodingFormat() = runBlocking {
        // Embedding request with explicit encoding_format of float returns float array embeddings
        val request = MAPPER.readValue("{\"encoding_format\":\"float\",\"input\":\"Test input\",\"model\":\"text-embedding-3-small\"}", EmbeddingRequest::class.java)
        val result = LiterLlm.embed(request)
        assertEquals(1, result.data().size, "expected exactly 1 elements")
        assertEquals(5, result.data().first().embedding().size, "expected exactly 5 elements")
    }

    @Test
    fun testEmbedError401() = runBlocking {
        // 401 Unauthorized error on embedding request when API key is invalid
        val request = MAPPER.readValue("{\"input\":\"Hello world\",\"model\":\"text-embedding-3-small\"}", EmbeddingRequest::class.java)
        assertFailsWith<Exception> {
            LiterLlm.embed(request)
        }
    }

    @Test
    fun testEmbedWithDimensions() = runBlocking {
        // Embedding request with explicit dimensions parameter returns embeddings of the requested size
        val request = MAPPER.readValue("{\"dimensions\":256,\"input\":\"Hello world\",\"model\":\"text-embedding-3-small\"}", EmbeddingRequest::class.java)
        val result = LiterLlm.embed(request)
        assertEquals(1, result.data().size, "expected exactly 1 elements")
        assertEquals(8, result.data().first().embedding().size, "expected exactly 8 elements")
    }

    @Test
    fun testLocalEmbedOllama() = runBlocking {
        // Embedding request via Ollama local provider with all-minilm model
        val request = MAPPER.readValue("{\"input\":\"The quick brown fox jumps over the lazy dog\",\"model\":\"ollama/all-minilm\"}", EmbeddingRequest::class.java)
        val result = LiterLlm.embed(request)
        assertEquals(1, result.data().size, "expected exactly 1 elements")
        assertEquals(32, result.data().first().embedding().size, "expected exactly 32 elements")
    }

}
