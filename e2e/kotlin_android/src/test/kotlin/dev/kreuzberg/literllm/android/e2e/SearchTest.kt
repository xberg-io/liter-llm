// This file is auto-generated by alef — DO NOT EDIT.
// alef:hash:c5972fa61e7a381a22b8ec72ce139720db9ac67a9de347004bfe77898d550a3e
// To regenerate: alef generate
// To verify freshness: alef verify --exit-code
// Issues & docs: https://github.com/kreuzberg-dev/alef
package dev.kreuzberg.literllm.android.e2e

import org.junit.jupiter.api.Test
import kotlin.test.assertEquals
import kotlin.test.assertTrue
import kotlin.test.assertFalse
import kotlin.test.assertFailsWith
import kotlinx.coroutines.runBlocking
import dev.kreuzberg.literllm.android.LiterLlm
import com.fasterxml.jackson.databind.ObjectMapper
import com.fasterxml.jackson.datatype.jdk8.Jdk8Module
import dev.kreuzberg.literllm.android.SearchRequest

/** E2e tests for category: search. */
class SearchTest {

    companion object {
        private val MAPPER = ObjectMapper().registerModule(Jdk8Module()).setPropertyNamingStrategy(com.fasterxml.jackson.databind.PropertyNamingStrategies.SNAKE_CASE)
    }
    @Test
    fun testSearchBasic() = runBlocking {
        // Basic web search request with a simple query
        val request = MAPPER.readValue("{\"model\":\"brave/web-search\",\"query\":\"What is Rust programming language?\"}", SearchRequest::class.java)
        val result = LiterLlm.search(request)
    }

    @Test
    fun testSearchEmptyResults() = runBlocking {
        // Web search with a query that returns no results
        val request = MAPPER.readValue("{\"model\":\"brave/web-search\",\"query\":\"xyznonexistent12345xyz\"}", SearchRequest::class.java)
        val result = LiterLlm.search(request)
    }

    @Test
    fun testSearchError400() = runBlocking {
        // 400 Bad Request error when search query is empty
        val request = MAPPER.readValue("{\"model\":\"brave/web-search\",\"query\":\"\"}", SearchRequest::class.java)
        assertFailsWith<Exception> {
            LiterLlm.search(request)
        }
    }

    @Test
    fun testSearchError401() = runBlocking {
        // 401 Unauthorized error on web search due to invalid API credentials
        val request = MAPPER.readValue("{\"model\":\"brave/web-search\",\"query\":\"test\"}", SearchRequest::class.java)
        assertFailsWith<Exception> {
            LiterLlm.search(request)
        }
    }

    @Test
    fun testSearchWithMaxResults() = runBlocking {
        // Search request with max_results parameter limiting response count
        val request = MAPPER.readValue("{\"max_results\":2,\"model\":\"brave/web-search\",\"query\":\"Rust programming\"}", SearchRequest::class.java)
        val result = LiterLlm.search(request)
        assertEquals(2, result.results().size, "expected exactly 2 elements")
    }

}
