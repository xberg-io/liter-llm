// This file is auto-generated by alef — DO NOT EDIT.
// alef:hash:048c0ba9f60831e50bc723c3165ed702e322e764ed14a293844b8144dbce6f69
// To regenerate: alef generate
// To verify freshness: alef verify --exit-code
// Issues & docs: https://github.com/kreuzberg-dev/alef
package dev.kreuzberg.literllm.android.e2e

import org.junit.jupiter.api.Test
import kotlin.test.assertEquals
import kotlin.test.assertTrue
import kotlin.test.assertFalse
import kotlin.test.assertFailsWith
import kotlinx.coroutines.runBlocking
import dev.kreuzberg.literllm.android.LiterLlm
import com.fasterxml.jackson.databind.ObjectMapper
import com.fasterxml.jackson.datatype.jdk8.Jdk8Module
import dev.kreuzberg.literllm.android.CreateFileRequest

/** E2e tests for category: files. */
class FilesTest {

    companion object {
        private val MAPPER = ObjectMapper().registerModule(Jdk8Module()).setPropertyNamingStrategy(com.fasterxml.jackson.databind.PropertyNamingStrategies.SNAKE_CASE)
    }
    @Test
    fun testEdgeFileEmptyList() = runBlocking {
        // List files when no files have been uploaded
        val result = LiterLlm.listFiles()
        assertEquals(0, result.data().size, "expected exactly 0 elements")
    }

    @Test
    fun testEdgeFileLargeUpload() = runBlocking {
        // Upload a large file successfully
        val request = MAPPER.readValue("{\"file\":\"eyJwcm9tcHQiOiAibGFyZ2UgdHJhaW5pbmcgZGF0YSJ9Cg==\",\"filename\":\"large_training_data.jsonl\",\"purpose\":\"fine-tune\"}", CreateFileRequest::class.java)
        val result = LiterLlm.createFile(request)
        assertFalse(result.id().isEmpty(), "expected non-empty value")
    }

    @Test
    fun testErrorFileAuth401() = runBlocking {
        // 401 Unauthorized when listing files with invalid API key
        assertFailsWith<Exception> {
            LiterLlm.listFiles()
        }
    }

    @Test
    fun testErrorFileBadPurpose() = runBlocking {
        // 400 Bad Request when uploading a file with invalid purpose
        val request = MAPPER.readValue("{\"file\":\"data.jsonl\",\"purpose\":\"invalid-purpose\"}", CreateFileRequest::class.java)
        assertFailsWith<Exception> {
            LiterLlm.createFile(request)
        }
    }

    @Test
    fun testErrorFileNotFound() = runBlocking {
        // 404 Not Found when retrieving a nonexistent file
        assertFailsWith<Exception> {
            LiterLlm.retrieveFile("file-nonexistent")
        }
    }

    @Test
    fun testSmokeCreateFile() = runBlocking {
        // Upload a file for use with the API
        val request = MAPPER.readValue("{\"file\":\"eyJwcm9tcHQiOiAiaGVsbG8ifQo=\",\"filename\":\"training_data.jsonl\",\"purpose\":\"fine-tune\"}", CreateFileRequest::class.java)
        val result = LiterLlm.createFile(request)
        assertFalse(result.id().isEmpty(), "expected non-empty value")
    }

    @Test
    fun testSmokeDeleteFile() = runBlocking {
        // Delete an uploaded file
        val result = LiterLlm.deleteFile("file-abc123")
        assertFalse(result.id().isEmpty(), "expected non-empty value")
        assertEquals(true, result.deleted())
    }

    @Test
    fun testSmokeFileContent() = runBlocking {
        // Retrieve the content of an uploaded file
        val result = LiterLlm.fileContent("file-abc123")
        assertFalse(result.isEmpty(), "expected non-empty value")
    }

    @Test
    fun testSmokeListFiles() = runBlocking {
        // List all uploaded files
        val result = LiterLlm.listFiles()
        assertEquals(2, result.data().size, "expected exactly 2 elements")
    }

    @Test
    fun testSmokeRetrieveFile() = runBlocking {
        // Retrieve metadata for an uploaded file
        val result = LiterLlm.retrieveFile("file-abc123")
        assertFalse(result.id().isEmpty(), "expected non-empty value")
    }

}
