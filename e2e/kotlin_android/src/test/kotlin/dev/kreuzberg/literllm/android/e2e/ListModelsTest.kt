// This file is auto-generated by alef — DO NOT EDIT.
// alef:hash:056373c5a8b6c19070cad85994f925c985cf099de05b91d16b861bfcd55b91c6
// To regenerate: alef generate
// To verify freshness: alef verify --exit-code
// Issues & docs: https://github.com/kreuzberg-dev/alef
package dev.kreuzberg.literllm.android.e2e

import org.junit.jupiter.api.Test
import kotlin.test.assertEquals
import kotlin.test.assertTrue
import kotlin.test.assertFalse
import kotlin.test.assertFailsWith
import kotlinx.coroutines.runBlocking
import dev.kreuzberg.literllm.android.LiterLlm

/** E2e tests for category: list-models. */
class ListModelsTest {
    @Test
    fun testEmptyModelList() = runBlocking {
        // List models response returns an empty data array when no models are available
        val client = LiterLlm.createClient(apiKey = "test-key", baseUrl = System.getProperty("mockServer.empty_model_list", System.getProperty("mockServerUrl", System.getenv("MOCK_SERVER_URL") ?: "") + "/fixtures/empty_model_list"))
        val result = client.listModels()
        assertTrue(result.data.size >= 0, "expected at least 0 elements")
        assertEquals(0, result.data.size, "expected exactly 0 elements")
        client.close()
    }

    @Test
    fun testListModelsError401() = runBlocking {
        // 401 Unauthorized error on list models request when API key is invalid
        val client = LiterLlm.createClient(apiKey = "test-key", baseUrl = System.getProperty("mockServer.list_models_error_401", System.getProperty("mockServerUrl", System.getenv("MOCK_SERVER_URL") ?: "") + "/fixtures/list_models_error_401"))
        assertFailsWith<Exception> {
            client.listModels()
        }
        client.close()
    }

    @Test
    fun testListModelsError500() = runBlocking {
        // 500 Internal Server Error when listing models due to server failure
        val client = LiterLlm.createClient(apiKey = "test-key", baseUrl = System.getProperty("mockServer.list_models_error_500", System.getProperty("mockServerUrl", System.getenv("MOCK_SERVER_URL") ?: "") + "/fixtures/list_models_error_500"))
        assertFailsWith<Exception> {
            client.listModels()
        }
        client.close()
    }

    @Test
    fun testListModelsFiltered() = runBlocking {
        // List models response with multiple model objects
        val client = LiterLlm.createClient(apiKey = "test-key", baseUrl = System.getProperty("mockServer.list_models_filtered", System.getProperty("mockServerUrl", System.getenv("MOCK_SERVER_URL") ?: "") + "/fixtures/list_models_filtered"))
        val result = client.listModels()
        assertTrue(result.data.size >= 5, "expected at least 5 elements")
        assertEquals("model", result.data.first().`object`.trim())
        client.close()
    }

}
