// This file is auto-generated by alef — DO NOT EDIT.
// alef:hash:4fe8d3411dc9c937765211959f3ac9d4fc3049ccb38d0826d86e3bdb98f705cf
// To regenerate: alef generate
// To verify freshness: alef verify --exit-code

// E2e tests for category: list-models
package e2e_test

import (
	"os"
	"strings"
	"testing"

	"github.com/stretchr/testify/assert"

	pkg "github.com/xberg-io/liter-llm/packages/go"
)

func Test_EmptyModelList(t *testing.T) {
	// List models response returns an empty data array when no models are available
	mockURL := os.Getenv("MOCK_SERVER_URL") + "/fixtures/empty_model_list"
	client, clientErr := pkg.CreateClient("test-key", &mockURL, nil, nil, nil)
	if clientErr != nil {
		t.Fatalf("create client failed: %v", clientErr)
	}
	result, err := client.ListModels()
	if err != nil {
		t.Fatalf("call failed: %v", err)
	}
	assert.GreaterOrEqual(t, len(result.Data), 0, "expected at least 0 elements")
	assert.Equal(t, len(result.Data), 0, "expected exactly 0 elements")
}

func Test_ListModelsError401(t *testing.T) {
	// 401 Unauthorized error on list models request when API key is invalid
	mockURL := os.Getenv("MOCK_SERVER_URL") + "/fixtures/list_models_error_401"
	client, clientErr := pkg.CreateClient("test-key", &mockURL, nil, nil, nil)
	if clientErr != nil {
		t.Fatalf("create client failed: %v", clientErr)
	}
	_, err := client.ListModels()
	if err == nil {
		t.Errorf("expected an error, but call succeeded")
	}
}

func Test_ListModelsError500(t *testing.T) {
	// 500 Internal Server Error when listing models due to server failure
	mockURL := os.Getenv("MOCK_SERVER_URL") + "/fixtures/list_models_error_500"
	client, clientErr := pkg.CreateClient("test-key", &mockURL, nil, nil, nil)
	if clientErr != nil {
		t.Fatalf("create client failed: %v", clientErr)
	}
	_, err := client.ListModels()
	if err == nil {
		t.Errorf("expected an error, but call succeeded")
	}
}

func Test_ListModelsFiltered(t *testing.T) {
	// List models response with multiple model objects
	mockURL := os.Getenv("MOCK_SERVER_URL") + "/fixtures/list_models_filtered"
	client, clientErr := pkg.CreateClient("test-key", &mockURL, nil, nil, nil)
	if clientErr != nil {
		t.Fatalf("create client failed: %v", clientErr)
	}
	result, err := client.ListModels()
	if err != nil {
		t.Fatalf("call failed: %v", err)
	}
	assert.GreaterOrEqual(t, len(result.Data), 5, "expected at least 5 elements")
	if len(result.Data) > 0 {
		if strings.TrimSpace(string(result.Data[0].Object)) != `model` {
			t.Errorf("equals mismatch: got %v", result.Data[0].Object)
		}
	}
}
