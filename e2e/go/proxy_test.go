// This file is auto-generated by alef — DO NOT EDIT.
// alef:hash:5a56e18dc9a576cf0cb4e800f65293b26578b17ecf9ce9e253cfac0ea07c4abe
// To regenerate: alef generate
// To verify freshness: alef verify --exit-code
// Issues & docs: https://github.com/kreuzberg-dev/alef

// E2e tests for category: proxy
package e2e_test

import (
	"encoding/json"
	"os"
	"strings"
	"testing"

	"github.com/stretchr/testify/assert"

	pkg "github.com/kreuzberg-dev/liter-llm/packages/go"
)

func Test_ProxyAuthInvalid(t *testing.T) {
	// 401 Unauthorized when an invalid API key is provided through the proxy
	var request pkg.ChatCompletionRequest
	if err := json.Unmarshal([]byte(`{"messages":[{"content":"Hello","role":"user"}],"model":"openai/gpt-4o"}`), &request); err != nil {
		t.Fatalf("config parse failed: %v", err)
	}
	mockURL := os.Getenv("MOCK_SERVER_URL") + "/fixtures/proxy_auth_invalid"
	client, clientErr := pkg.CreateClient("test-key", &mockURL, nil, nil, nil)
	if clientErr != nil {
		t.Fatalf("create client failed: %v", clientErr)
	}
	_, err := client.Chat(request)
	if err == nil {
		t.Errorf("expected an error, but call succeeded")
	}
}

func Test_ProxyAuthMissing(t *testing.T) {
	// 401 Unauthorized when no API key is provided through the proxy
	var request pkg.ChatCompletionRequest
	if err := json.Unmarshal([]byte(`{"messages":[{"content":"Hello","role":"user"}],"model":"openai/gpt-4o"}`), &request); err != nil {
		t.Fatalf("config parse failed: %v", err)
	}
	mockURL := os.Getenv("MOCK_SERVER_URL") + "/fixtures/proxy_auth_missing"
	client, clientErr := pkg.CreateClient("test-key", &mockURL, nil, nil, nil)
	if clientErr != nil {
		t.Fatalf("create client failed: %v", clientErr)
	}
	_, err := client.Chat(request)
	if err == nil {
		t.Errorf("expected an error, but call succeeded")
	}
}

func Test_ProxyChatBasic(t *testing.T) {
	// Basic chat completion request routed through the proxy
	var request pkg.ChatCompletionRequest
	if err := json.Unmarshal([]byte(`{"messages":[{"content":"Say hello","role":"user"}],"model":"openai/gpt-4o"}`), &request); err != nil {
		t.Fatalf("config parse failed: %v", err)
	}
	mockURL := os.Getenv("MOCK_SERVER_URL") + "/fixtures/proxy_chat_basic"
	client, clientErr := pkg.CreateClient("test-key", &mockURL, nil, nil, nil)
	if clientErr != nil {
		t.Fatalf("create client failed: %v", clientErr)
	}
	result, err := client.Chat(request)
	if err != nil {
		t.Fatalf("call failed: %v", err)
	}
	var choices0MessageContent string
	if result.Choices[0].Message.Content != nil {
		choices0MessageContent = string(*result.Choices[0].Message.Content)
	}
	var choices0FinishReason string
	if result.Choices[0].FinishReason != nil {
		choices0FinishReason = string(*result.Choices[0].FinishReason)
	}
	assert.Equal(t, len(result.Choices), 1, "expected exactly 1 elements")
	if strings.TrimSpace(string(choices0MessageContent)) != `Hello!` {
		t.Errorf("equals mismatch: got %v", choices0MessageContent)
	}
	if strings.TrimSpace(string(choices0FinishReason)) != `stop` {
		t.Errorf("equals mismatch: got %v", choices0FinishReason)
	}
}

func Test_ProxyChatStreaming(t *testing.T) {
	// Streaming chat completion routed through the proxy
	t.Skip("non-HTTP fixture: Go binding does not expose a callable for the configured `[e2e.call]` function")
}

func Test_ProxyEmbeddings(t *testing.T) {
	// Embedding request routed through the proxy
	var request pkg.EmbeddingRequest
	if err := json.Unmarshal([]byte(`{"input":"Hello world","model":"openai/text-embedding-3-small"}`), &request); err != nil {
		t.Fatalf("config parse failed: %v", err)
	}
	mockURL := os.Getenv("MOCK_SERVER_URL") + "/fixtures/proxy_embeddings"
	client, clientErr := pkg.CreateClient("test-key", &mockURL, nil, nil, nil)
	if clientErr != nil {
		t.Fatalf("create client failed: %v", clientErr)
	}
	result, err := client.Embed(request)
	if err != nil {
		t.Fatalf("call failed: %v", err)
	}
	assert.Equal(t, len(result.Data), 1, "expected exactly 1 elements")
}

func Test_ProxyHealth(t *testing.T) {
	// Health check verifying proxy connectivity via list models
	mockURL := os.Getenv("MOCK_SERVER_URL") + "/fixtures/proxy_health"
	client, clientErr := pkg.CreateClient("test-key", &mockURL, nil, nil, nil)
	if clientErr != nil {
		t.Fatalf("create client failed: %v", clientErr)
	}
	result, err := client.ListModels()
	if err != nil {
		t.Fatalf("call failed: %v", err)
	}
	assert.GreaterOrEqual(t, len(result.Data), 1, "expected at least 1 elements")
}

func Test_ProxyImageGenerate(t *testing.T) {
	// Image generation request routed through the proxy
	var request pkg.CreateImageRequest
	if err := json.Unmarshal([]byte(`{"model":"dall-e-3","n":1,"prompt":"A sunset over the ocean","size":"1024x1024"}`), &request); err != nil {
		t.Fatalf("config parse failed: %v", err)
	}
	mockURL := os.Getenv("MOCK_SERVER_URL") + "/fixtures/proxy_image_generate"
	client, clientErr := pkg.CreateClient("test-key", &mockURL, nil, nil, nil)
	if clientErr != nil {
		t.Fatalf("create client failed: %v", clientErr)
	}
	result, err := client.ImageGenerate(request)
	if err != nil {
		t.Fatalf("call failed: %v", err)
	}
	assert.Equal(t, len(result.Data), 1, "expected exactly 1 elements")
	if len(result.Data) > 0 {
		if result.Data[0].URL == nil {
			t.Errorf("expected non-empty value")
		}
	}
}

func Test_ProxyModelsList(t *testing.T) {
	// List models request routed through the proxy
	mockURL := os.Getenv("MOCK_SERVER_URL") + "/fixtures/proxy_models_list"
	client, clientErr := pkg.CreateClient("test-key", &mockURL, nil, nil, nil)
	if clientErr != nil {
		t.Fatalf("create client failed: %v", clientErr)
	}
	result, err := client.ListModels()
	if err != nil {
		t.Fatalf("call failed: %v", err)
	}
	assert.GreaterOrEqual(t, len(result.Data), 1, "expected at least 1 elements")
}

func Test_ProxyModeration(t *testing.T) {
	// Content moderation request routed through the proxy
	var request pkg.ModerationRequest
	if err := json.Unmarshal([]byte(`{"input":"The weather is nice today.","model":"omni-moderation-latest"}`), &request); err != nil {
		t.Fatalf("config parse failed: %v", err)
	}
	mockURL := os.Getenv("MOCK_SERVER_URL") + "/fixtures/proxy_moderation"
	client, clientErr := pkg.CreateClient("test-key", &mockURL, nil, nil, nil)
	if clientErr != nil {
		t.Fatalf("create client failed: %v", clientErr)
	}
	result, err := client.Moderate(request)
	if err != nil {
		t.Fatalf("call failed: %v", err)
	}
	assert.Equal(t, len(result.Results), 1, "expected exactly 1 elements")
	if len(result.Results) > 0 {
		if result.Results[0].Flagged != false {
			t.Errorf("equals mismatch: got %v", result.Results[0].Flagged)
		}
	}
}

func Test_ProxyRerank(t *testing.T) {
	// Document reranking request routed through the proxy
	var request pkg.RerankRequest
	if err := json.Unmarshal([]byte(`{"documents":["Deep learning is a subset of machine learning using neural networks.","The stock market closed higher today."],"model":"rerank-v3.5","query":"What is deep learning?"}`), &request); err != nil {
		t.Fatalf("config parse failed: %v", err)
	}
	mockURL := os.Getenv("MOCK_SERVER_URL") + "/fixtures/proxy_rerank"
	client, clientErr := pkg.CreateClient("test-key", &mockURL, nil, nil, nil)
	if clientErr != nil {
		t.Fatalf("create client failed: %v", clientErr)
	}
	result, err := client.Rerank(request)
	if err != nil {
		t.Fatalf("call failed: %v", err)
	}
	assert.Equal(t, len(result.Results), 2, "expected exactly 2 elements")
	if len(result.Results) > 0 {
		if result.Results[0].RelevanceScore <= 0.9 {
			t.Errorf("expected > 0.9, got %v", result.Results[0].RelevanceScore)
		}
	}
}

func Test_ProxyUpstream429(t *testing.T) {
	// 429 Too Many Requests from upstream provider through the proxy
	var request pkg.ChatCompletionRequest
	if err := json.Unmarshal([]byte(`{"messages":[{"content":"Hello","role":"user"}],"model":"openai/gpt-4o"}`), &request); err != nil {
		t.Fatalf("config parse failed: %v", err)
	}
	mockURL := os.Getenv("MOCK_SERVER_URL") + "/fixtures/proxy_upstream_429"
	client, clientErr := pkg.CreateClient("test-key", &mockURL, nil, nil, nil)
	if clientErr != nil {
		t.Fatalf("create client failed: %v", clientErr)
	}
	_, err := client.Chat(request)
	if err == nil {
		t.Errorf("expected an error, but call succeeded")
	}
}

func Test_ProxyUpstream500(t *testing.T) {
	// 500 Internal Server Error from upstream provider through the proxy
	var request pkg.ChatCompletionRequest
	if err := json.Unmarshal([]byte(`{"messages":[{"content":"Hello","role":"user"}],"model":"openai/gpt-4o"}`), &request); err != nil {
		t.Fatalf("config parse failed: %v", err)
	}
	mockURL := os.Getenv("MOCK_SERVER_URL") + "/fixtures/proxy_upstream_500"
	client, clientErr := pkg.CreateClient("test-key", &mockURL, nil, nil, nil)
	if clientErr != nil {
		t.Fatalf("create client failed: %v", clientErr)
	}
	_, err := client.Chat(request)
	if err == nil {
		t.Errorf("expected an error, but call succeeded")
	}
}
