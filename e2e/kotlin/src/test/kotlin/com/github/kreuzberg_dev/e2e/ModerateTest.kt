// This file is auto-generated by alef — DO NOT EDIT.
// alef:hash:e2779efdc620ab3ee04957d571ba86b240a0857b6f8ed00a9a1f21b4a94c1da5
// To regenerate: alef generate
// To verify freshness: alef verify --exit-code
// Issues & docs: https://github.com/kreuzberg-dev/alef
package com.github.kreuzberg_dev.e2e

import org.junit.jupiter.api.Test
import kotlin.test.assertEquals
import kotlin.test.assertTrue
import kotlin.test.assertFalse
import kotlin.test.assertFailsWith
import com.github.kreuzberg_dev.LiterLlm
import com.fasterxml.jackson.databind.ObjectMapper
import com.fasterxml.jackson.datatype.jdk8.Jdk8Module
import com.github.kreuzberg_dev.ModerationRequest

/** E2e tests for category: moderate. */
class ModerateTest {

    companion object {
        private val MAPPER = ObjectMapper().registerModule(Jdk8Module())
    }
    @Test
    fun testEdgeModerateAllCategories() {
        // Moderation response with multiple categories flagged
        val request = MAPPER.readValue("{\"input\":\"Extremely harmful content targeting multiple categories\",\"model\":\"omni-moderation-latest\"}", ModerationRequest::class.java)
        val client = LiterLlm.createClient(apiKey = "test-key", baseUrl = System.getenv("MOCK_SERVER_URL") + "/fixtures/edge_moderate_all_categories")
        val result = client.moderate(request)
        assertEquals(1, result.results().size, "expected exactly 1 elements")
        assertEquals(true, result.results().first().flagged())
        client.close()
    }

    @Test
    fun testEdgeModerateEmptyInput() {
        // Moderation with empty string input
        val request = MAPPER.readValue("{\"input\":\"\",\"model\":\"omni-moderation-latest\"}", ModerationRequest::class.java)
        val client = LiterLlm.createClient(apiKey = "test-key", baseUrl = System.getenv("MOCK_SERVER_URL") + "/fixtures/edge_moderate_empty_input")
        val result = client.moderate(request)
        assertEquals(1, result.results().size, "expected exactly 1 elements")
        assertEquals(false, result.results().first().flagged())
        client.close()
    }

    @Test
    fun testErrorModerateAuth401() {
        // 401 Unauthorized for moderation with invalid API key
        val request = MAPPER.readValue("{\"input\":\"Hello\",\"model\":\"omni-moderation-latest\"}", ModerationRequest::class.java)
        val client = LiterLlm.createClient(apiKey = "test-key", baseUrl = System.getenv("MOCK_SERVER_URL") + "/fixtures/error_moderate_auth_401")
        assertFailsWith<Exception> {
            client.moderate(request)
        }
        client.close()
    }

    @Test
    fun testErrorModerateBadRequest() {
        // 400 Bad Request for moderation with invalid model
        val request = MAPPER.readValue("{\"input\":\"Hello\",\"model\":\"nonexistent-moderation\"}", ModerationRequest::class.java)
        val client = LiterLlm.createClient(apiKey = "test-key", baseUrl = System.getenv("MOCK_SERVER_URL") + "/fixtures/error_moderate_bad_request")
        assertFailsWith<Exception> {
            client.moderate(request)
        }
        client.close()
    }

    @Test
    fun testSmokeModerateBatch() {
        // Moderate multiple inputs in a single request
        val request = MAPPER.readValue("{\"input\":[\"Hello world\",\"Nice weather today\"],\"model\":\"omni-moderation-latest\"}", ModerationRequest::class.java)
        val client = LiterLlm.createClient(apiKey = "test-key", baseUrl = System.getenv("MOCK_SERVER_URL") + "/fixtures/smoke_moderate_batch")
        val result = client.moderate(request)
        assertEquals(2, result.results().size, "expected exactly 2 elements")
        assertEquals(false, result.results().first().flagged())
        client.close()
    }

    @Test
    fun testSmokeModerateFlagged() {
        // Moderation detects flagged content
        val request = MAPPER.readValue("{\"input\":\"I want to hurt someone very badly\",\"model\":\"omni-moderation-latest\"}", ModerationRequest::class.java)
        val client = LiterLlm.createClient(apiKey = "test-key", baseUrl = System.getenv("MOCK_SERVER_URL") + "/fixtures/smoke_moderate_flagged")
        val result = client.moderate(request)
        assertEquals(1, result.results().size, "expected exactly 1 elements")
        assertEquals(true, result.results().first().flagged())
        client.close()
    }

    @Test
    fun testSmokeModerateSingle() {
        // Moderate a single non-flagged input
        val request = MAPPER.readValue("{\"input\":\"The weather is nice today.\",\"model\":\"omni-moderation-latest\"}", ModerationRequest::class.java)
        val client = LiterLlm.createClient(apiKey = "test-key", baseUrl = System.getenv("MOCK_SERVER_URL") + "/fixtures/smoke_moderate_single")
        val result = client.moderate(request)
        assertEquals(1, result.results().size, "expected exactly 1 elements")
        assertEquals(false, result.results().first().flagged())
        client.close()
    }

}
