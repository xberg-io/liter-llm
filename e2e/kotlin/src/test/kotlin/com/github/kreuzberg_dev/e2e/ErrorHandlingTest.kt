// This file is auto-generated by alef — DO NOT EDIT.
// alef:hash:9846fc30fcc50262b0e302ba6947b8b7141ced36a1b8410fa8de2eaeeb9b1c94
// To regenerate: alef generate
// To verify freshness: alef verify --exit-code
// Issues & docs: https://github.com/kreuzberg-dev/alef
package com.github.kreuzberg_dev.e2e

import org.junit.jupiter.api.Test
import kotlin.test.assertEquals
import kotlin.test.assertTrue
import kotlin.test.assertFalse
import kotlin.test.assertFailsWith
import com.github.kreuzberg_dev.LiterLlm
import com.fasterxml.jackson.databind.ObjectMapper
import com.fasterxml.jackson.datatype.jdk8.Jdk8Module
import com.github.kreuzberg_dev.ChatCompletionRequest

/** E2e tests for category: error-handling. */
class ErrorHandlingTest {

    companion object {
        private val MAPPER = ObjectMapper().registerModule(Jdk8Module())
    }
    @Test
    fun testAnthropicErrorAuth() {
        // 401 Authentication error returned by the Anthropic API when the API key is invalid
        val request = MAPPER.readValue("{\"messages\":[{\"content\":\"Hello\",\"role\":\"user\"}],\"model\":\"anthropic/claude-3-5-sonnet-20241022\"}", ChatCompletionRequest::class.java)
        val client = LiterLlm.createClient(apiKey = "test-key", baseUrl = System.getenv("MOCK_SERVER_URL") + "/fixtures/anthropic_error_auth")
        assertFailsWith<Exception> {
            client.chat(request)
        }
        client.close()
    }

    @Test
    fun testAuth401() {
        // 401 Unauthorized error when API key is invalid or missing
        val request = MAPPER.readValue("{\"messages\":[{\"content\":\"Hello\",\"role\":\"user\"}],\"model\":\"gpt-4\"}", ChatCompletionRequest::class.java)
        val client = LiterLlm.createClient(apiKey = "test-key", baseUrl = System.getenv("MOCK_SERVER_URL") + "/fixtures/auth_401")
        assertFailsWith<Exception> {
            client.chat(request)
        }
        client.close()
    }

    @Test
    fun testAzureErrorAuth() {
        // Azure OpenAI returns a 401 Unauthorized error when the API key is missing or invalid — uses Azure's error envelope shape with code AccessDenied
        val request = MAPPER.readValue("{\"messages\":[{\"content\":\"Hello\",\"role\":\"user\"}],\"model\":\"azure/gpt-4\"}", ChatCompletionRequest::class.java)
        val client = LiterLlm.createClient(apiKey = "test-key", baseUrl = System.getenv("MOCK_SERVER_URL") + "/fixtures/azure_error_auth")
        assertFailsWith<Exception> {
            client.chat(request)
        }
        client.close()
    }

    @Test
    fun testBadRequest400() {
        // 400 Bad Request error when a parameter value is invalid
        val request = MAPPER.readValue("{\"messages\":[{\"content\":\"Hello\",\"role\":\"user\"}],\"model\":\"gpt-4\",\"temperature\":5.0}", ChatCompletionRequest::class.java)
        val client = LiterLlm.createClient(apiKey = "test-key", baseUrl = System.getenv("MOCK_SERVER_URL") + "/fixtures/bad_request_400")
        assertFailsWith<Exception> {
            client.chat(request)
        }
        client.close()
    }

    @Test
    fun testBedrockErrorAuth() {
        // AWS Bedrock returns 403 Forbidden (not 401) when credentials are missing, expired, or the IAM role lacks bedrock:InvokeModel permission — verifies the error is mapped to Authentication
        val request = MAPPER.readValue("{\"messages\":[{\"content\":\"Hello\",\"role\":\"user\"}],\"model\":\"bedrock/anthropic.claude-3-sonnet-20240229-v1:0\"}", ChatCompletionRequest::class.java)
        val client = LiterLlm.createClient(apiKey = "test-key", baseUrl = System.getenv("MOCK_SERVER_URL") + "/fixtures/bedrock_error_auth")
        assertFailsWith<Exception> {
            client.chat(request)
        }
        client.close()
    }

    @Test
    fun testContentPolicyViolation() {
        // 400 error when a request is rejected due to content policy
        val request = MAPPER.readValue("{\"messages\":[{\"content\":\"Generate harmful content\",\"role\":\"user\"}],\"model\":\"gpt-4\"}", ChatCompletionRequest::class.java)
        val client = LiterLlm.createClient(apiKey = "test-key", baseUrl = System.getenv("MOCK_SERVER_URL") + "/fixtures/content_policy_violation")
        assertFailsWith<Exception> {
            client.chat(request)
        }
        client.close()
    }

    @Test
    fun testContextWindowExceeded() {
        // 400 error when the prompt exceeds the model's maximum context length
        val request = MAPPER.readValue("{\"messages\":[{\"content\":\"Very long prompt that exceeds the context window...\",\"role\":\"user\"}],\"model\":\"gpt-4\"}", ChatCompletionRequest::class.java)
        val client = LiterLlm.createClient(apiKey = "test-key", baseUrl = System.getenv("MOCK_SERVER_URL") + "/fixtures/context_window_exceeded")
        assertFailsWith<Exception> {
            client.chat(request)
        }
        client.close()
    }

    @Test
    fun testEmptyResponseBody() {
        // 200 OK response with an empty JSON object body, missing required fields
        val request = MAPPER.readValue("{\"messages\":[{\"content\":\"Hello\",\"role\":\"user\"}],\"model\":\"gpt-4\"}", ChatCompletionRequest::class.java)
        val client = LiterLlm.createClient(apiKey = "test-key", baseUrl = System.getenv("MOCK_SERVER_URL") + "/fixtures/empty_response_body")
        assertFailsWith<Exception> {
            client.chat(request)
        }
        client.close()
    }

    @Test
    fun testForbidden403() {
        // 403 Forbidden error when the API key does not have access to the requested resource
        val request = MAPPER.readValue("{\"messages\":[{\"content\":\"Hello\",\"role\":\"user\"}],\"model\":\"gpt-4\"}", ChatCompletionRequest::class.java)
        val client = LiterLlm.createClient(apiKey = "test-key", baseUrl = System.getenv("MOCK_SERVER_URL") + "/fixtures/forbidden_403")
        assertFailsWith<Exception> {
            client.chat(request)
        }
        client.close()
    }

    @Test
    fun testGatewayTimeout504() {
        // 504 Gateway Timeout error when the upstream service times out
        val request = MAPPER.readValue("{\"messages\":[{\"content\":\"Hello\",\"role\":\"user\"}],\"model\":\"gpt-4\"}", ChatCompletionRequest::class.java)
        val client = LiterLlm.createClient(apiKey = "test-key", baseUrl = System.getenv("MOCK_SERVER_URL") + "/fixtures/gateway_timeout_504")
        assertFailsWith<Exception> {
            client.chat(request)
        }
        client.close()
    }

    @Test
    fun testGithubCopilotErrorAuth() {
        // 401 Authentication error returned by the GitHub Copilot API when the token is invalid or expired
        val request = MAPPER.readValue("{\"messages\":[{\"content\":\"Hello\",\"role\":\"user\"}],\"model\":\"github_copilot/gpt-4o\"}", ChatCompletionRequest::class.java)
        val client = LiterLlm.createClient(apiKey = "test-key", baseUrl = System.getenv("MOCK_SERVER_URL") + "/fixtures/github_copilot_error_auth")
        assertFailsWith<Exception> {
            client.chat(request)
        }
        client.close()
    }

    @Test
    fun testNotFound404() {
        // 404 Not Found error when requesting a model that does not exist
        val request = MAPPER.readValue("{\"messages\":[{\"content\":\"Hello\",\"role\":\"user\"}],\"model\":\"gpt-99\"}", ChatCompletionRequest::class.java)
        val client = LiterLlm.createClient(apiKey = "test-key", baseUrl = System.getenv("MOCK_SERVER_URL") + "/fixtures/not_found_404")
        assertFailsWith<Exception> {
            client.chat(request)
        }
        client.close()
    }

    @Test
    fun testRateLimit429() {
        // 429 Too Many Requests error when the rate limit is exceeded
        val request = MAPPER.readValue("{\"messages\":[{\"content\":\"Hello\",\"role\":\"user\"}],\"model\":\"gpt-4\"}", ChatCompletionRequest::class.java)
        val client = LiterLlm.createClient(apiKey = "test-key", baseUrl = System.getenv("MOCK_SERVER_URL") + "/fixtures/rate_limit_429")
        assertFailsWith<Exception> {
            client.chat(request)
        }
        client.close()
    }

    @Test
    fun testServerError500() {
        // 500 Internal Server Error from the upstream API
        val request = MAPPER.readValue("{\"messages\":[{\"content\":\"Hello\",\"role\":\"user\"}],\"model\":\"gpt-4\"}", ChatCompletionRequest::class.java)
        val client = LiterLlm.createClient(apiKey = "test-key", baseUrl = System.getenv("MOCK_SERVER_URL") + "/fixtures/server_error_500")
        assertFailsWith<Exception> {
            client.chat(request)
        }
        client.close()
    }

    @Test
    fun testServiceUnavailable502() {
        // 502 Bad Gateway error when the upstream service is unavailable
        val request = MAPPER.readValue("{\"messages\":[{\"content\":\"Hello\",\"role\":\"user\"}],\"model\":\"gpt-4\"}", ChatCompletionRequest::class.java)
        val client = LiterLlm.createClient(apiKey = "test-key", baseUrl = System.getenv("MOCK_SERVER_URL") + "/fixtures/service_unavailable_502")
        assertFailsWith<Exception> {
            client.chat(request)
        }
        client.close()
    }

    @Test
    fun testTimeoutError() {
        // 408 Request Timeout error when the API request takes too long to complete
        val request = MAPPER.readValue("{\"messages\":[{\"content\":\"Hello\",\"role\":\"user\"}],\"model\":\"gpt-4\"}", ChatCompletionRequest::class.java)
        val client = LiterLlm.createClient(apiKey = "test-key", baseUrl = System.getenv("MOCK_SERVER_URL") + "/fixtures/timeout_error")
        assertFailsWith<Exception> {
            client.chat(request)
        }
        client.close()
    }

    @Test
    fun testVertexErrorAuth() {
        // Google Vertex AI returns 401 Unauthorized when the OAuth2 token is missing, expired, or the service account lacks aiplatform.endpoints.predict permission — verifies the error is mapped to Authentication
        val request = MAPPER.readValue("{\"messages\":[{\"content\":\"Hello\",\"role\":\"user\"}],\"model\":\"vertex_ai/gemini-2.0-flash\"}", ChatCompletionRequest::class.java)
        val client = LiterLlm.createClient(apiKey = "test-key", baseUrl = System.getenv("MOCK_SERVER_URL") + "/fixtures/vertex_error_auth")
        assertFailsWith<Exception> {
            client.chat(request)
        }
        client.close()
    }

}
