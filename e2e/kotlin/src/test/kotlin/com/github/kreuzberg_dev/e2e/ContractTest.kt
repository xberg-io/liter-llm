// This file is auto-generated by alef — DO NOT EDIT.
// alef:hash:696ff78171730da2390dfccff9d0cd313cdb29ac56ae3ce4a49999389010dd04
// To regenerate: alef generate
// To verify freshness: alef verify --exit-code
// Issues & docs: https://github.com/kreuzberg-dev/alef
package com.github.kreuzberg_dev.e2e

import org.junit.jupiter.api.Test
import kotlin.test.assertEquals
import kotlin.test.assertTrue
import kotlin.test.assertFalse
import kotlin.test.assertFailsWith
import com.github.kreuzberg_dev.LiterLlm
import com.fasterxml.jackson.databind.ObjectMapper
import com.fasterxml.jackson.datatype.jdk8.Jdk8Module
import com.github.kreuzberg_dev.ChatCompletionRequest
import com.github.kreuzberg_dev.OcrRequest
import com.github.kreuzberg_dev.SearchRequest

/** E2e tests for category: contract. */
class ContractTest {

    companion object {
        private val MAPPER = ObjectMapper().registerModule(Jdk8Module())
    }
    @Test
    fun testBindingApiParity() {
        // Verify all bindings expose the full API surface — constructor accepts all config options and every method exists
        val request = MAPPER.readValue("{\"messages\":[{\"content\":\"Contract test\",\"role\":\"user\"}],\"model\":\"openai/gpt-4o\"}", ChatCompletionRequest::class.java)
        val client = LiterLlm.createClient(apiKey = "test-key", baseUrl = System.getenv("MOCK_SERVER_URL") + "/fixtures/binding_api_parity")
        val result = client.chat(request)
        assertEquals(1, result.choices().size, "expected exactly 1 elements")
        assertEquals("OK", result.choices().first().message().content().orEmpty().trim())
        assertEquals("stop", result.choices().first().finishReason().orEmpty().getValue().trim())
        assertEquals(6, result.usage()?.totalTokens()!!)
        assertEquals("gpt-4o", result.model().trim())
        client.close()
    }

    @Test
    fun testContractOcr() {
        // Verify ocr() method exists in all bindings
        val request = MAPPER.readValue("{\"document\":{\"type\":\"document_url\",\"url\":\"https://example.com/contract-test.pdf\"},\"model\":\"mistral/mistral-ocr-latest\"}", OcrRequest::class.java)
        val client = LiterLlm.createClient(apiKey = "test-key", baseUrl = System.getenv("MOCK_SERVER_URL") + "/fixtures/contract_ocr")
        val result = client.ocr(request)
        client.close()
    }

    @Test
    fun testContractSearch() {
        // Verify search() method exists in all bindings
        val request = MAPPER.readValue("{\"model\":\"brave/web-search\",\"query\":\"contract test query\"}", SearchRequest::class.java)
        val client = LiterLlm.createClient(apiKey = "test-key", baseUrl = System.getenv("MOCK_SERVER_URL") + "/fixtures/contract_search")
        val result = client.search(request)
        client.close()
    }

}
