// This file is auto-generated by alef — DO NOT EDIT.
// alef:hash:46f005e51555de2e0a822916acc00950725bc6e54171d151b101d57d25d0ed02
// To regenerate: alef generate
// To verify freshness: alef verify --exit-code
// Issues & docs: https://github.com/kreuzberg-dev/alef
package com.github.kreuzberg_dev.e2e

import org.junit.jupiter.api.Test
import kotlin.test.assertEquals
import kotlin.test.assertTrue
import kotlin.test.assertFalse
import kotlin.test.assertFailsWith
import com.github.kreuzberg_dev.LiterLlm
import com.fasterxml.jackson.databind.ObjectMapper
import com.fasterxml.jackson.datatype.jdk8.Jdk8Module
import com.github.kreuzberg_dev.CreateBatchRequest

/** E2e tests for category: batches. */
class BatchesTest {

    companion object {
        private val MAPPER = ObjectMapper().registerModule(Jdk8Module())
    }
    @Test
    fun testEdgeBatchAlreadyCancelled() {
        // Attempt to cancel an already-cancelled batch
        val client = LiterLlm.createClient(apiKey = "test-key", baseUrl = System.getenv("MOCK_SERVER_URL") + "/fixtures/edge_batch_already_cancelled")
        assertFailsWith<Exception> {
            client.cancelBatch("batch-cancelled001")
        }
        client.close()
    }

    @Test
    fun testEdgeBatchEmptyList() {
        // List batches when no batches exist
        val client = LiterLlm.createClient(apiKey = "test-key", baseUrl = System.getenv("MOCK_SERVER_URL") + "/fixtures/edge_batch_empty_list")
        val result = client.listBatches()
        assertEquals(0, result.data().size, "expected exactly 0 elements")
        client.close()
    }

    @Test
    fun testErrorBatchAuth401() {
        // 401 Unauthorized when creating a batch with invalid API key
        val request = MAPPER.readValue("{\"completion_window\":\"24h\",\"endpoint\":\"/v1/chat/completions\",\"input_file_id\":\"file-abc123\"}", CreateBatchRequest::class.java)
        val client = LiterLlm.createClient(apiKey = "test-key", baseUrl = System.getenv("MOCK_SERVER_URL") + "/fixtures/error_batch_auth_401")
        assertFailsWith<Exception> {
            client.createBatch(request)
        }
        client.close()
    }

    @Test
    fun testErrorBatchInvalidFile() {
        // 400 Bad Request when creating a batch with invalid input file
        val request = MAPPER.readValue("{\"completion_window\":\"24h\",\"endpoint\":\"/v1/chat/completions\",\"input_file_id\":\"file-wrong-purpose\"}", CreateBatchRequest::class.java)
        val client = LiterLlm.createClient(apiKey = "test-key", baseUrl = System.getenv("MOCK_SERVER_URL") + "/fixtures/error_batch_invalid_file")
        assertFailsWith<Exception> {
            client.createBatch(request)
        }
        client.close()
    }

    @Test
    fun testErrorBatchNotFound() {
        // 404 Not Found when retrieving a nonexistent batch
        val client = LiterLlm.createClient(apiKey = "test-key", baseUrl = System.getenv("MOCK_SERVER_URL") + "/fixtures/error_batch_not_found")
        assertFailsWith<Exception> {
            client.retrieveBatch("batch-nonexistent")
        }
        client.close()
    }

    @Test
    fun testSmokeBatchCompleted() {
        // Retrieve a completed batch job with output file
        val client = LiterLlm.createClient(apiKey = "test-key", baseUrl = System.getenv("MOCK_SERVER_URL") + "/fixtures/smoke_batch_completed")
        val result = client.retrieveBatch("batch-ghi789")
        assertFalse(result.id().isEmpty(), "expected non-empty value")
        assertEquals("completed", result.status().trim())
        client.close()
    }

    @Test
    fun testSmokeCancelBatch() {
        // Cancel a running batch job
        val client = LiterLlm.createClient(apiKey = "test-key", baseUrl = System.getenv("MOCK_SERVER_URL") + "/fixtures/smoke_cancel_batch")
        val result = client.cancelBatch("batch-def456")
        assertFalse(result.id().isEmpty(), "expected non-empty value")
        assertEquals("cancelling", result.status().trim())
        client.close()
    }

    @Test
    fun testSmokeCreateBatch() {
        // Create a new batch processing job
        val request = MAPPER.readValue("{\"completion_window\":\"24h\",\"endpoint\":\"/v1/chat/completions\",\"input_file_id\":\"file-abc123\"}", CreateBatchRequest::class.java)
        val client = LiterLlm.createClient(apiKey = "test-key", baseUrl = System.getenv("MOCK_SERVER_URL") + "/fixtures/smoke_create_batch")
        val result = client.createBatch(request)
        assertFalse(result.id().isEmpty(), "expected non-empty value")
        assertEquals("validating", result.status().trim())
        client.close()
    }

    @Test
    fun testSmokeListBatches() {
        // List all batch jobs
        val client = LiterLlm.createClient(apiKey = "test-key", baseUrl = System.getenv("MOCK_SERVER_URL") + "/fixtures/smoke_list_batches")
        val result = client.listBatches()
        assertEquals(2, result.data().size, "expected exactly 2 elements")
        client.close()
    }

    @Test
    fun testSmokeRetrieveBatch() {
        // Retrieve the status of a batch job
        val client = LiterLlm.createClient(apiKey = "test-key", baseUrl = System.getenv("MOCK_SERVER_URL") + "/fixtures/smoke_retrieve_batch")
        val result = client.retrieveBatch("batch-abc123")
        assertFalse(result.id().isEmpty(), "expected non-empty value")
        assertEquals("in_progress", result.status().trim())
        client.close()
    }

}
