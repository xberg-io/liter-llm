# This file is auto-generated by alef — DO NOT EDIT.
# alef:hash:f2be24356f224d7cafb965a88977ab27840a65ecb9a9ba6cce1110d593455279
# To regenerate: alef generate
# To verify freshness: alef verify --exit-code
# Issues & docs: https://github.com/sample_crate-dev/alef
# E2e tests for category: search
defmodule E2e.SearchTest do
  use ExUnit.Case, async: false

  describe "search_basic" do
    test "search_basic" do
      {:ok, client} = LiterLlm.create_client("test-key", base_url: (System.get_env("MOCK_SERVER_URL") || "") <> "/fixtures/search_basic")
      {:ok, result} = LiterLlm.defaultclient_search_async(client, "{\"model\":\"brave/web-search\",\"query\":\"What is Rust programming language?\"}")
    end
  end

  describe "search_empty_results" do
    test "search_empty_results" do
      {:ok, client} = LiterLlm.create_client("test-key", base_url: (System.get_env("MOCK_SERVER_URL") || "") <> "/fixtures/search_empty_results")
      {:ok, result} = LiterLlm.defaultclient_search_async(client, "{\"model\":\"brave/web-search\",\"query\":\"xyznonexistent12345xyz\"}")
    end
  end

  describe "search_error_400" do
    test "search_error_400" do
      {:ok, client} = LiterLlm.create_client("test-key", base_url: (System.get_env("MOCK_SERVER_URL") || "") <> "/fixtures/search_error_400")
      assert {:error, _} = LiterLlm.defaultclient_search_async(client, "{\"model\":\"brave/web-search\",\"query\":\"\"}")
    end
  end

  describe "search_error_401" do
    test "search_error_401" do
      {:ok, client} = LiterLlm.create_client("test-key", base_url: (System.get_env("MOCK_SERVER_URL") || "") <> "/fixtures/search_error_401")
      assert {:error, _} = LiterLlm.defaultclient_search_async(client, "{\"model\":\"brave/web-search\",\"query\":\"test\"}")
    end
  end

  describe "search_with_max_results" do
    test "search_with_max_results" do
      {:ok, client} = LiterLlm.create_client("test-key", base_url: (System.get_env("MOCK_SERVER_URL") || "") <> "/fixtures/search_with_max_results")
      {:ok, result} = LiterLlm.defaultclient_search_async(client, "{\"max_results\":2,\"model\":\"brave/web-search\",\"query\":\"Rust programming\"}")
      assert length(result.results) == 2
    end
  end
end
