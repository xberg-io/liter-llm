# This file is auto-generated by alef — DO NOT EDIT.
# alef:hash:e959f0e6385f761d1825ca0ff36f87f3c4a2a1b4d9f10e1dc5060623ecfed28b
# To regenerate: alef generate
# To verify freshness: alef verify --exit-code
# Issues & docs: https://github.com/kreuzberg-dev/alef
# E2e tests for category: smoke
defmodule E2e.SmokeTest do
  use ExUnit.Case, async: false

  describe "anthropic_chat" do
    test "anthropic_chat" do
      {:ok, client} = LiterLlm.create_client("test-key", (System.get_env("MOCK_SERVER_URL") || "") <> "/fixtures/anthropic_chat")
      {:ok, result} = LiterLlm.chat_async(client, nil)
      assert length(result.choices) == 1
      assert String.trim(Enum.at(result.choices, 0).message.content) == "Hello!"
      assert String.trim(Enum.at(result.choices, 0).finish_reason) == "stop"
      assert String.trim(result.model) == "claude-3-5-sonnet-20241022"
      assert result.usage.total_tokens == 23
    end
  end

  describe "azure_chat" do
    test "azure_chat" do
      {:ok, client} = LiterLlm.create_client("test-key", (System.get_env("MOCK_SERVER_URL") || "") <> "/fixtures/azure_chat")
      {:ok, result} = LiterLlm.chat_async(client, nil)
      assert length(result.choices) == 1
      assert String.trim(Enum.at(result.choices, 0).message.content) == "Hello!"
      assert String.trim(Enum.at(result.choices, 0).finish_reason) == "stop"
      assert String.trim(result.model) == "gpt-4"
      assert result.usage.total_tokens == 13
    end
  end

  describe "azure_embed" do
    test "azure_embed" do
      {:ok, client} = LiterLlm.create_client("test-key", (System.get_env("MOCK_SERVER_URL") || "") <> "/fixtures/azure_embed")
      {:ok, result} = LiterLlm.embed_async(client, "\"Hello world\"")
      assert length(result.data) == 1
      assert length(Enum.at(result.data, 0).embedding) == 1480
    end
  end

  describe "basic_chat" do
    test "basic_chat" do
      {:ok, client} = LiterLlm.create_client("test-key", (System.get_env("MOCK_SERVER_URL") || "") <> "/fixtures/basic_chat")
      {:ok, result} = LiterLlm.chat_async(client, nil)
      assert length(result.choices) == 1
      assert String.trim(Enum.at(result.choices, 0).message.content) == "Hello!"
      assert String.trim(Enum.at(result.choices, 0).finish_reason) == "stop"
      assert result.usage.total_tokens == 15
      assert String.trim(result.model) == "gpt-4"
    end
  end

  describe "basic_embed" do
    test "basic_embed" do
      {:ok, client} = LiterLlm.create_client("test-key", (System.get_env("MOCK_SERVER_URL") || "") <> "/fixtures/basic_embed")
      {:ok, result} = LiterLlm.embed_async(client, "\"Hello world\"")
      assert length(result.data) == 1
      assert length(Enum.at(result.data, 0).embedding) == 5
    end
  end

  describe "basic_list_models" do
    test "basic_list_models" do
      {:ok, client} = LiterLlm.create_client("test-key", (System.get_env("MOCK_SERVER_URL") || "") <> "/fixtures/basic_list_models")
      {:ok, result} = LiterLlm.list_models_async(client)
      assert length(result.data) >= 1
    end
  end

  describe "bedrock_chat" do
    test "bedrock_chat" do
      {:ok, client} = LiterLlm.create_client("test-key", (System.get_env("MOCK_SERVER_URL") || "") <> "/fixtures/bedrock_chat")
      {:ok, result} = LiterLlm.chat_async(client, nil)
      assert length(result.choices) == 1
      assert String.trim(Enum.at(result.choices, 0).message.content) == "Hello!"
      assert String.trim(Enum.at(result.choices, 0).finish_reason) == "stop"
      assert String.trim(result.model) == "anthropic.claude-3-sonnet-20240229-v1:0"
      assert result.usage.total_tokens == 15
    end
  end

  describe "github_copilot_chat" do
    test "github_copilot_chat" do
      {:ok, client} = LiterLlm.create_client("test-key", (System.get_env("MOCK_SERVER_URL") || "") <> "/fixtures/github_copilot_chat")
      {:ok, result} = LiterLlm.chat_async(client, nil)
      assert length(result.choices) == 1
      assert String.trim(Enum.at(result.choices, 0).message.content) == "Hello!"
      assert String.trim(Enum.at(result.choices, 0).finish_reason) == "stop"
      assert String.trim(result.model) == "gpt-4o"
      assert result.usage.total_tokens == 14
    end
  end

  describe "local_chat_ollama" do
    test "local_chat_ollama" do
      {:ok, client} = LiterLlm.create_client("test-key", (System.get_env("MOCK_SERVER_URL") || "") <> "/fixtures/local_chat_ollama")
      {:ok, result} = LiterLlm.chat_async(client, nil)
      assert result.choices != ""
    end
  end

  describe "local_list_models_ollama" do
    test "local_list_models_ollama" do
      {:ok, client} = LiterLlm.create_client("test-key", (System.get_env("MOCK_SERVER_URL") || "") <> "/fixtures/local_list_models_ollama")
      {:ok, result} = LiterLlm.list_models_async(client, %{"model" => "ollama/any"})
      assert length(result.data) >= 1
    end
  end

  describe "smoke_cache_memory" do
    test "smoke_cache_memory" do
      {:ok, client} = LiterLlm.create_client("test-key", (System.get_env("MOCK_SERVER_URL") || "") <> "/fixtures/smoke_cache_memory")
      {:ok, result} = LiterLlm.chat_async(client, nil)
      assert result.choices != ""
    end
  end

  describe "smoke_chat_anthropic" do
    test "smoke_chat_anthropic" do
      {:ok, client} = LiterLlm.create_client("test-key", (System.get_env("MOCK_SERVER_URL") || "") <> "/fixtures/smoke_chat_anthropic")
      {:ok, result} = LiterLlm.chat_async(client, nil)
      assert result.choices != ""
      assert result.usage != ""
    end
  end

  describe "smoke_chat_gemini" do
    test "smoke_chat_gemini" do
      {:ok, client} = LiterLlm.create_client("test-key", (System.get_env("MOCK_SERVER_URL") || "") <> "/fixtures/smoke_chat_gemini")
      {:ok, result} = LiterLlm.chat_async(client, nil)
      assert result.choices != ""
      assert result.usage != ""
    end
  end

  describe "smoke_chat_openai" do
    test "smoke_chat_openai" do
      {:ok, client} = LiterLlm.create_client("test-key", (System.get_env("MOCK_SERVER_URL") || "") <> "/fixtures/smoke_chat_openai")
      {:ok, result} = LiterLlm.chat_async(client, nil)
      assert result.choices != ""
      assert result.usage != ""
    end
  end

  describe "smoke_embed_openai" do
    test "smoke_embed_openai" do
      {:ok, client} = LiterLlm.create_client("test-key", (System.get_env("MOCK_SERVER_URL") || "") <> "/fixtures/smoke_embed_openai")
      {:ok, result} = LiterLlm.embed_async(client, "[\"Hello world\"]")
      assert result.data != ""
    end
  end

  describe "smoke_list_models_openai" do
    test "smoke_list_models_openai" do
      {:ok, client} = LiterLlm.create_client("test-key", (System.get_env("MOCK_SERVER_URL") || "") <> "/fixtures/smoke_list_models_openai")
      {:ok, result} = LiterLlm.list_models_async(client, %{"model" => "openai/gpt-4o-mini"})
      assert length(result.data) >= 1
    end
  end

  describe "smoke_provider_routing" do
    test "smoke_provider_routing" do
      {:ok, client} = LiterLlm.create_client("test-key", (System.get_env("MOCK_SERVER_URL") || "") <> "/fixtures/smoke_provider_routing")
      {:ok, result} = LiterLlm.chat_async(client, nil)
      assert result.choices != ""
    end
  end

  describe "smoke_streaming_openai" do
    test "smoke_streaming_openai" do
      {:ok, client} = LiterLlm.create_client("test-key", (System.get_env("MOCK_SERVER_URL") || "") <> "/fixtures/smoke_streaming_openai")
      {:ok, result} = LiterLlm.chat_stream_async(client, nil)
      # skipped: field 'chunks' not available on result type
    end
  end

  describe "vertex_chat" do
    test "vertex_chat" do
      {:ok, client} = LiterLlm.create_client("test-key", (System.get_env("MOCK_SERVER_URL") || "") <> "/fixtures/vertex_chat")
      {:ok, result} = LiterLlm.chat_async(client, nil)
      assert length(result.choices) == 1
      assert String.trim(Enum.at(result.choices, 0).message.content) == "Hello!"
      assert String.trim(Enum.at(result.choices, 0).finish_reason) == "stop"
      assert String.trim(result.model) == "gemini-2.0-flash"
      assert result.usage.total_tokens == 13
    end
  end

  describe "vertex_embed" do
    test "vertex_embed" do
      {:ok, client} = LiterLlm.create_client("test-key", (System.get_env("MOCK_SERVER_URL") || "") <> "/fixtures/vertex_embed")
      {:ok, result} = LiterLlm.embed_async(client, "\"Hello\"")
      assert length(result.data) == 1
      assert length(Enum.at(result.data, 0).embedding) == 160
    end
  end
end
