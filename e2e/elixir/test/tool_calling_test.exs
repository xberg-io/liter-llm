# This file is auto-generated by alef — DO NOT EDIT.
# alef:hash:56b20dad5628cc7d2667b111b90fe4965cf083ec88417449c2fd962aeb2cc1b7
# To regenerate: alef generate
# To verify freshness: alef verify --exit-code
# Issues & docs: https://github.com/kreuzberg-dev/alef
# E2e tests for category: tool-calling
defmodule E2e.ToolCallingTest do
  use ExUnit.Case, async: false

  describe "anthropic_tool_calling" do
    test "anthropic_tool_calling" do
      {:ok, client} = LiterLlm.create_client("test-key", (System.get_env("MOCK_SERVER_URL") || "") <> "/fixtures/anthropic_tool_calling")
      {:ok, result} = LiterLlm.chat_async(client, nil)
      assert length(result.choices) == 1
      assert Enum.at(result.choices, 0).message.tool_calls != ""
      assert String.trim(Enum.at(Enum.at(result.choices, 0).message.tool_calls, 0).function.name) == "get_weather"
      assert String.trim(Enum.at(result.choices, 0).finish_reason) == "tool_calls"
    end
  end

  describe "single_tool_call" do
    test "single_tool_call" do
      {:ok, client} = LiterLlm.create_client("test-key", (System.get_env("MOCK_SERVER_URL") || "") <> "/fixtures/single_tool_call")
      {:ok, result} = LiterLlm.chat_async(client, nil)
      assert length(result.choices) == 1
      assert Enum.at(result.choices, 0).message.tool_calls != ""
      assert String.trim(Enum.at(Enum.at(result.choices, 0).message.tool_calls, 0).function.name) == "get_weather"
      assert String.trim(Enum.at(result.choices, 0).finish_reason) == "tool_calls"
    end
  end
end
