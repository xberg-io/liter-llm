# This file is auto-generated by alef — DO NOT EDIT.
<<<<<<< Updated upstream
# alef:hash:fe64a8a06beeb01b5344005fa07dcdfdf3e244d772ace0f89dbba84b0541ca2d
=======
# alef:hash:ff0e10ad47404c645f09678fc9db55fb107af1c70816902b56b45b49d59c3b3b
>>>>>>> Stashed changes
# To regenerate: alef generate
# To verify freshness: alef verify --exit-code
# E2e tests for category: moderate
defmodule E2e.ModerateTest do
  use ExUnit.Case, async: false

  describe "edge_moderate_all_categories" do
    test "edge_moderate_all_categories" do
      {:ok, client} = LiterLlm.create_client("test-key", base_url: (System.get_env("MOCK_SERVER_URL") || "") <> "/fixtures/edge_moderate_all_categories")
      {:ok, result} = LiterLlm.defaultclient_moderate_async(client, "{\"input\":\"Extremely harmful content targeting multiple categories\",\"model\":\"omni-moderation-latest\"}")
      assert length(result.results) == 1
      assert Enum.at(result.results, 0).flagged == true
    end
  end

  describe "edge_moderate_empty_input" do
    test "edge_moderate_empty_input" do
      {:ok, client} = LiterLlm.create_client("test-key", base_url: (System.get_env("MOCK_SERVER_URL") || "") <> "/fixtures/edge_moderate_empty_input")
      {:ok, result} = LiterLlm.defaultclient_moderate_async(client, "{\"input\":\"\",\"model\":\"omni-moderation-latest\"}")
      assert length(result.results) == 1
      assert Enum.at(result.results, 0).flagged == false
    end
  end

  describe "error_moderate_auth_401" do
    test "error_moderate_auth_401" do
      {:ok, client} = LiterLlm.create_client("test-key", base_url: (System.get_env("MOCK_SERVER_URL") || "") <> "/fixtures/error_moderate_auth_401")
      assert {:error, _} = LiterLlm.defaultclient_moderate_async(client, "{\"input\":\"Hello\",\"model\":\"omni-moderation-latest\"}")
    end
  end

  describe "error_moderate_bad_request" do
    test "error_moderate_bad_request" do
      {:ok, client} = LiterLlm.create_client("test-key", base_url: (System.get_env("MOCK_SERVER_URL") || "") <> "/fixtures/error_moderate_bad_request")
      assert {:error, _} = LiterLlm.defaultclient_moderate_async(client, "{\"input\":\"Hello\",\"model\":\"nonexistent-moderation\"}")
    end
  end

  describe "smoke_moderate_batch" do
    test "smoke_moderate_batch" do
      {:ok, client} = LiterLlm.create_client("test-key", base_url: (System.get_env("MOCK_SERVER_URL") || "") <> "/fixtures/smoke_moderate_batch")
      {:ok, result} = LiterLlm.defaultclient_moderate_async(client, "{\"input\":[\"Hello world\",\"Nice weather today\"],\"model\":\"omni-moderation-latest\"}")
      assert length(result.results) == 2
      assert Enum.at(result.results, 0).flagged == false
    end
  end

  describe "smoke_moderate_flagged" do
    test "smoke_moderate_flagged" do
      {:ok, client} = LiterLlm.create_client("test-key", base_url: (System.get_env("MOCK_SERVER_URL") || "") <> "/fixtures/smoke_moderate_flagged")
      {:ok, result} = LiterLlm.defaultclient_moderate_async(client, "{\"input\":\"I want to hurt someone very badly\",\"model\":\"omni-moderation-latest\"}")
      assert length(result.results) == 1
      assert Enum.at(result.results, 0).flagged == true
    end
  end

  describe "smoke_moderate_single" do
    test "smoke_moderate_single" do
      {:ok, client} = LiterLlm.create_client("test-key", base_url: (System.get_env("MOCK_SERVER_URL") || "") <> "/fixtures/smoke_moderate_single")
      {:ok, result} = LiterLlm.defaultclient_moderate_async(client, "{\"input\":\"The weather is nice today.\",\"model\":\"omni-moderation-latest\"}")
      assert length(result.results) == 1
      assert Enum.at(result.results, 0).flagged == false
    end
  end
end
