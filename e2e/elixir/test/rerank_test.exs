# This file is auto-generated by alef — DO NOT EDIT.
# alef:hash:9852acf2ac44d3711997478a0dca285361e16ff6bf43d84ecb07df60cf84e7c6
# To regenerate: alef generate
# To verify freshness: alef verify --exit-code
# Issues & docs: https://github.com/kreuzberg-dev/alef
# E2e tests for category: rerank
defmodule E2e.RerankTest do
  use ExUnit.Case, async: false

  describe "edge_rerank_empty_query" do
    test "edge_rerank_empty_query" do
      {:ok, client} = LiterLlm.create_client("test-key", (System.get_env("MOCK_SERVER_URL") || "") <> "/fixtures/edge_rerank_empty_query")
      {:ok, result} = LiterLlm.rerank_async(client, nil)
      assert length(result.results) == 2
    end
  end

  describe "edge_rerank_single_doc" do
    test "edge_rerank_single_doc" do
      {:ok, client} = LiterLlm.create_client("test-key", (System.get_env("MOCK_SERVER_URL") || "") <> "/fixtures/edge_rerank_single_doc")
      {:ok, result} = LiterLlm.rerank_async(client, nil)
      assert length(result.results) == 1
      assert Enum.at(result.results, 0).relevance_score > 0.9
    end
  end

  describe "error_rerank_auth_401" do
    test "error_rerank_auth_401" do
      {:ok, client} = LiterLlm.create_client("test-key", (System.get_env("MOCK_SERVER_URL") || "") <> "/fixtures/error_rerank_auth_401")
      assert {:error, _} = LiterLlm.rerank_async(client, nil)
    end
  end

  describe "error_rerank_bad_request" do
    test "error_rerank_bad_request" do
      {:ok, client} = LiterLlm.create_client("test-key", (System.get_env("MOCK_SERVER_URL") || "") <> "/fixtures/error_rerank_bad_request")
      assert {:error, _} = LiterLlm.rerank_async(client, nil)
    end
  end

  describe "smoke_rerank_basic" do
    test "smoke_rerank_basic" do
      {:ok, client} = LiterLlm.create_client("test-key", (System.get_env("MOCK_SERVER_URL") || "") <> "/fixtures/smoke_rerank_basic")
      {:ok, result} = LiterLlm.rerank_async(client, nil)
      assert length(result.results) == 3
      assert Enum.at(result.results, 0).relevance_score > 0.9
    end
  end

  describe "smoke_rerank_return_docs" do
    test "smoke_rerank_return_docs" do
      {:ok, client} = LiterLlm.create_client("test-key", (System.get_env("MOCK_SERVER_URL") || "") <> "/fixtures/smoke_rerank_return_docs")
      {:ok, result} = LiterLlm.rerank_async(client, nil)
      assert length(result.results) == 2
      assert Enum.at(result.results, 0).document != ""
      assert Enum.at(result.results, 0).relevance_score > 0.9
    end
  end

  describe "smoke_rerank_with_top_n" do
    test "smoke_rerank_with_top_n" do
      {:ok, client} = LiterLlm.create_client("test-key", (System.get_env("MOCK_SERVER_URL") || "") <> "/fixtures/smoke_rerank_with_top_n")
      {:ok, result} = LiterLlm.rerank_async(client, nil)
      assert length(result.results) == 2
      assert Enum.at(result.results, 0).relevance_score > 0.9
    end
  end
end
