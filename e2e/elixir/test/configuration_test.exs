# This file is auto-generated by alef — DO NOT EDIT.
# alef:hash:921b2ad43a718ffb9c525b98cb457516c4be520d3c54c28631223c5955a29e56
# To regenerate: alef generate
# To verify freshness: alef verify --exit-code
# Issues & docs: https://github.com/sample_crate-dev/alef
# E2e tests for category: configuration
defmodule E2e.ConfigurationTest do
  use ExUnit.Case, async: false

  describe "custom_base_url" do
    test "custom_base_url" do
      {:ok, client} = LiterLlm.create_client("test-key", base_url: (System.get_env("MOCK_SERVER_URL") || "") <> "/fixtures/custom_base_url")
      {:ok, result} = LiterLlm.defaultclient_chat_async(client, "{\"messages\":[{\"content\":\"Hello\",\"role\":\"user\"}],\"model\":\"local-model\"}")
      assert length(result.choices) == 1
      assert String.trim(Enum.at(result.choices, 0).message.content) == "Hi there!"
      assert String.trim(to_string(Enum.at(result.choices, 0).finish_reason)) == "stop"
      assert String.trim(result.model) == "local-model"
    end
  end

  describe "extra_headers" do
    test "extra_headers" do
      {:ok, client} = LiterLlm.create_client("test-key", base_url: (System.get_env("MOCK_SERVER_URL") || "") <> "/fixtures/extra_headers")
      {:ok, result} = LiterLlm.defaultclient_chat_async(client, "{\"messages\":[{\"content\":\"Hello\",\"role\":\"user\"}],\"model\":\"gpt-4\"}")
      assert length(result.choices) == 1
      assert String.trim(to_string(Enum.at(result.choices, 0).finish_reason)) == "stop"
    end
  end

  describe "local_provider_llamacpp" do
    test "local_provider_llamacpp" do
      {:ok, client} = LiterLlm.create_client("test-key", base_url: (System.get_env("MOCK_SERVER_URL") || "") <> "/fixtures/local_provider_llamacpp")
      {:ok, result} = LiterLlm.defaultclient_chat_async(client, "{\"messages\":[{\"content\":\"Hello\",\"role\":\"user\"}],\"model\":\"llamacpp/my-model\"}")
      assert length(result.choices) == 1
      assert String.trim(Enum.at(result.choices, 0).message.content) == "Hi there! I'm running locally."
      assert String.trim(to_string(Enum.at(result.choices, 0).finish_reason)) == "stop"
      assert String.trim(result.model) == "my-model"
    end
  end

  describe "local_provider_ollama" do
    test "local_provider_ollama" do
      {:ok, client} = LiterLlm.create_client("test-key", base_url: (System.get_env("MOCK_SERVER_URL") || "") <> "/fixtures/local_provider_ollama")
      {:ok, result} = LiterLlm.defaultclient_chat_async(client, "{\"messages\":[{\"content\":\"Hello\",\"role\":\"user\"}],\"model\":\"ollama/qwen2:0.5b\"}")
      assert length(result.choices) == 1
      assert String.trim(Enum.at(result.choices, 0).message.content) == "Hello! How can I help you today?"
      assert String.trim(to_string(Enum.at(result.choices, 0).finish_reason)) == "stop"
      assert String.trim(result.model) == "qwen2:0.5b"
    end
  end

  describe "local_provider_vllm" do
    test "local_provider_vllm" do
      {:ok, client} = LiterLlm.create_client("test-key", base_url: (System.get_env("MOCK_SERVER_URL") || "") <> "/fixtures/local_provider_vllm")
      {:ok, result} = LiterLlm.defaultclient_chat_async(client, "{\"messages\":[{\"content\":\"Hello\",\"role\":\"user\"}],\"model\":\"vllm/meta-llama/Llama-3.2-1B\"}")
      assert length(result.choices) == 1
      assert String.trim(Enum.at(result.choices, 0).message.content) == "Hello! How may I assist you?"
      assert String.trim(to_string(Enum.at(result.choices, 0).finish_reason)) == "stop"
      assert String.trim(result.model) == "meta-llama/Llama-3.2-1B"
    end
  end
end
