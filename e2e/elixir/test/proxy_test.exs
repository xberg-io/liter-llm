# This file is auto-generated by alef — DO NOT EDIT.
# alef:hash:c92e03e771a9474aafe3f26e14ba1fb6ce82644e7199f7f7a52370da975783c2
# To regenerate: alef generate
# To verify freshness: alef verify --exit-code
# Issues & docs: https://github.com/kreuzberg-dev/alef
# E2e tests for category: proxy
defmodule E2e.ProxyTest do
  use ExUnit.Case, async: false

  describe "proxy_auth_invalid" do
    test "proxy_auth_invalid" do
      {:ok, client} = LiterLlm.create_client("test-key", (System.get_env("MOCK_SERVER_URL") || "") <> "/fixtures/proxy_auth_invalid")
      assert {:error, _} = LiterLlm.defaultclient_chat_async(client, "{\"messages\":[{\"content\":\"Hello\",\"role\":\"user\"}],\"model\":\"openai/gpt-4o\"}")
    end
  end

  describe "proxy_auth_missing" do
    test "proxy_auth_missing" do
      {:ok, client} = LiterLlm.create_client("test-key", (System.get_env("MOCK_SERVER_URL") || "") <> "/fixtures/proxy_auth_missing")
      assert {:error, _} = LiterLlm.defaultclient_chat_async(client, "{\"messages\":[{\"content\":\"Hello\",\"role\":\"user\"}],\"model\":\"openai/gpt-4o\"}")
    end
  end

  describe "proxy_chat_basic" do
    test "proxy_chat_basic" do
      {:ok, client} = LiterLlm.create_client("test-key", (System.get_env("MOCK_SERVER_URL") || "") <> "/fixtures/proxy_chat_basic")
      {:ok, result} = LiterLlm.defaultclient_chat_async(client, "{\"messages\":[{\"content\":\"Say hello\",\"role\":\"user\"}],\"model\":\"openai/gpt-4o\"}")
      assert length(result.choices) == 1
      assert String.trim(Enum.at(result.choices, 0).message.content) == "Hello!"
      assert String.trim(Enum.at(result.choices, 0).finish_reason) == "stop"
    end
  end

  describe "proxy_chat_streaming" do
    test "proxy_chat_streaming" do
      {:ok, client} = LiterLlm.create_client("test-key", (System.get_env("MOCK_SERVER_URL") || "") <> "/fixtures/proxy_chat_streaming")
      {:ok, result} = LiterLlm.defaultclient_chat_stream_async(client, "{\"messages\":[{\"content\":\"Count to 3\",\"role\":\"user\"}],\"model\":\"openai/gpt-4o\",\"stream\":true}")
      # skipped: field 'chunks' not available on result type
      # skipped: field 'stream_content' not available on result type
      # skipped: field 'stream_complete' not available on result type
    end
  end

  describe "proxy_embeddings" do
    test "proxy_embeddings" do
      {:ok, client} = LiterLlm.create_client("test-key", (System.get_env("MOCK_SERVER_URL") || "") <> "/fixtures/proxy_embeddings")
      {:ok, result} = LiterLlm.defaultclient_embed_async(client, "{\"input\":\"Hello world\",\"model\":\"openai/text-embedding-3-small\"}")
      assert length(result.data) == 1
    end
  end

  describe "proxy_health" do
    test "proxy_health" do
      {:ok, client} = LiterLlm.create_client("test-key", (System.get_env("MOCK_SERVER_URL") || "") <> "/fixtures/proxy_health")
      {:ok, result} = LiterLlm.defaultclient_list_models_async(client)
      assert length(result.data) >= 1
    end
  end

  describe "proxy_image_generate" do
    test "proxy_image_generate" do
      {:ok, client} = LiterLlm.create_client("test-key", (System.get_env("MOCK_SERVER_URL") || "") <> "/fixtures/proxy_image_generate")
      {:ok, result} = LiterLlm.defaultclient_image_generate_async(client, "{\"model\":\"dall-e-3\",\"n\":1,\"prompt\":\"A sunset over the ocean\",\"size\":\"1024x1024\"}")
      assert length(result.data) == 1
      assert Enum.at(result.data, 0).url != ""
    end
  end

  describe "proxy_models_list" do
    test "proxy_models_list" do
      {:ok, client} = LiterLlm.create_client("test-key", (System.get_env("MOCK_SERVER_URL") || "") <> "/fixtures/proxy_models_list")
      {:ok, result} = LiterLlm.defaultclient_list_models_async(client)
      assert length(result.data) >= 1
    end
  end

  describe "proxy_moderation" do
    test "proxy_moderation" do
      {:ok, client} = LiterLlm.create_client("test-key", (System.get_env("MOCK_SERVER_URL") || "") <> "/fixtures/proxy_moderation")
      {:ok, result} = LiterLlm.defaultclient_moderate_async(client, "{\"input\":\"The weather is nice today.\",\"model\":\"omni-moderation-latest\"}")
      assert length(result.results) == 1
      assert Enum.at(result.results, 0).flagged == false
    end
  end

  describe "proxy_rerank" do
    test "proxy_rerank" do
      {:ok, client} = LiterLlm.create_client("test-key", (System.get_env("MOCK_SERVER_URL") || "") <> "/fixtures/proxy_rerank")
      {:ok, result} = LiterLlm.defaultclient_rerank_async(client, "{\"documents\":[\"Deep learning is a subset of machine learning using neural networks.\",\"The stock market closed higher today.\"],\"model\":\"rerank-v3.5\",\"query\":\"What is deep learning?\"}")
      assert length(result.results) == 2
      assert Enum.at(result.results, 0).relevance_score > 0.9
    end
  end

  describe "proxy_upstream_429" do
    test "proxy_upstream_429" do
      {:ok, client} = LiterLlm.create_client("test-key", (System.get_env("MOCK_SERVER_URL") || "") <> "/fixtures/proxy_upstream_429")
      assert {:error, _} = LiterLlm.defaultclient_chat_async(client, "{\"messages\":[{\"content\":\"Hello\",\"role\":\"user\"}],\"model\":\"openai/gpt-4o\"}")
    end
  end

  describe "proxy_upstream_500" do
    test "proxy_upstream_500" do
      {:ok, client} = LiterLlm.create_client("test-key", (System.get_env("MOCK_SERVER_URL") || "") <> "/fixtures/proxy_upstream_500")
      assert {:error, _} = LiterLlm.defaultclient_chat_async(client, "{\"messages\":[{\"content\":\"Hello\",\"role\":\"user\"}],\"model\":\"openai/gpt-4o\"}")
    end
  end
end
