# This file is auto-generated by alef — DO NOT EDIT.
# alef:hash:a7c064e23690d6da0a7c0734d1219488b1ca34c78c93e3287518f9c69d67f638
# To regenerate: alef generate
# To verify freshness: alef verify --exit-code
# E2e tests for category: chat
defmodule E2e.ChatTest do
  use ExUnit.Case, async: false

  describe "developer_message" do
    test "developer_message" do
      {:ok, client} = LiterLlm.create_client("test-key", base_url: (System.get_env("MOCK_SERVER_URL") || "") <> "/fixtures/developer_message")
      {:ok, result} = LiterLlm.defaultclient_chat_async(client, "{\"messages\":[{\"content\":\"You are a coding assistant. Always respond with concise code examples.\",\"role\":\"developer\"},{\"content\":\"How do I reverse a string in Python?\",\"role\":\"user\"}],\"model\":\"gpt-4\"}")
      assert length(result.choices) == 1
      assert String.trim(Enum.at(result.choices, 0).message.content) == "s[::-1]"
      assert String.trim(to_string(Enum.at(result.choices, 0).finish_reason)) == "stop"
    end
  end

  describe "edge_chat_max_tokens" do
    test "edge_chat_max_tokens" do
      {:ok, client} = LiterLlm.create_client("test-key", base_url: (System.get_env("MOCK_SERVER_URL") || "") <> "/fixtures/edge_chat_max_tokens")
      {:ok, result} = LiterLlm.defaultclient_chat_async(client, "{\"max_tokens\":1,\"messages\":[{\"content\":\"Write a story\",\"role\":\"user\"}],\"model\":\"gpt-4\"}")
      assert String.trim(to_string(Enum.at(result.choices, 0).finish_reason)) == "length"
      assert Enum.at(result.choices, 0).message.content != ""
    end
  end

  describe "edge_chat_system_only" do
    test "edge_chat_system_only" do
      {:ok, client} = LiterLlm.create_client("test-key", base_url: (System.get_env("MOCK_SERVER_URL") || "") <> "/fixtures/edge_chat_system_only")
      {:ok, result} = LiterLlm.defaultclient_chat_async(client, "{\"messages\":[{\"content\":\"You are a helpful and concise assistant\",\"role\":\"system\"},{\"content\":\"Hi\",\"role\":\"user\"}],\"model\":\"gpt-4\"}")
      assert Enum.at(result.choices, 0).message.content != ""
    end
  end

  describe "edge_chat_temperature_zero" do
    test "edge_chat_temperature_zero" do
      {:ok, client} = LiterLlm.create_client("test-key", base_url: (System.get_env("MOCK_SERVER_URL") || "") <> "/fixtures/edge_chat_temperature_zero")
      {:ok, result} = LiterLlm.defaultclient_chat_async(client, "{\"messages\":[{\"content\":\"Say hello\",\"role\":\"user\"}],\"model\":\"gpt-4\",\"temperature\":0}")
      assert Enum.at(result.choices, 0).message.content != ""
    end
  end

  describe "finish_reason_content_filter" do
    test "finish_reason_content_filter" do
      {:ok, client} = LiterLlm.create_client("test-key", base_url: (System.get_env("MOCK_SERVER_URL") || "") <> "/fixtures/finish_reason_content_filter")
      {:ok, result} = LiterLlm.defaultclient_chat_async(client, "{\"messages\":[{\"content\":\"Tell me something controversial\",\"role\":\"user\"}],\"model\":\"gpt-4\"}")
      assert length(result.choices) == 1
      assert String.trim(to_string(Enum.at(result.choices, 0).finish_reason)) == "content_filter"
    end
  end

  describe "finish_reason_length" do
    test "finish_reason_length" do
      {:ok, client} = LiterLlm.create_client("test-key", base_url: (System.get_env("MOCK_SERVER_URL") || "") <> "/fixtures/finish_reason_length")
      {:ok, result} = LiterLlm.defaultclient_chat_async(client, "{\"max_tokens\":5,\"messages\":[{\"content\":\"Tell me a long story\",\"role\":\"user\"}],\"model\":\"gpt-4\"}")
      assert length(result.choices) == 1
      assert String.trim(to_string(Enum.at(result.choices, 0).finish_reason)) == "length"
    end
  end

  describe "multi_turn_conversation" do
    test "multi_turn_conversation" do
      {:ok, client} = LiterLlm.create_client("test-key", base_url: (System.get_env("MOCK_SERVER_URL") || "") <> "/fixtures/multi_turn_conversation")
      {:ok, result} = LiterLlm.defaultclient_chat_async(client, "{\"messages\":[{\"content\":\"You are a helpful assistant.\",\"role\":\"system\"},{\"content\":\"What is 2 + 2?\",\"role\":\"user\"},{\"content\":\"2 + 2 equals 4.\",\"role\":\"assistant\"},{\"content\":\"And what is 4 + 4?\",\"role\":\"user\"}],\"model\":\"gpt-4\"}")
      assert length(result.choices) == 1
      assert String.trim(Enum.at(result.choices, 0).message.content) == "4 + 4 equals 8."
      assert String.trim(to_string(Enum.at(result.choices, 0).finish_reason)) == "stop"
    end
  end

  describe "parallel_tool_calls" do
    test "parallel_tool_calls" do
      {:ok, client} = LiterLlm.create_client("test-key", base_url: (System.get_env("MOCK_SERVER_URL") || "") <> "/fixtures/parallel_tool_calls")
      {:ok, result} = LiterLlm.defaultclient_chat_async(client, "{\"messages\":[{\"content\":\"What is the weather in NYC and London?\",\"role\":\"user\"}],\"model\":\"gpt-4\",\"parallel_tool_calls\":true,\"tools\":[{\"function\":{\"description\":\"Get the current weather for a given location\",\"name\":\"get_weather\",\"parameters\":{\"properties\":{\"location\":{\"description\":\"The city name\",\"type\":\"string\"}},\"required\":[\"location\"],\"type\":\"object\"}},\"type\":\"function\"}]}")
      assert length(result.choices) == 1
      assert Enum.at(result.choices, 0).message.tool_calls != ""
      assert length(Enum.at(result.choices, 0).message.tool_calls) == 2
      assert String.trim(to_string(Enum.at(result.choices, 0).finish_reason)) == "tool_calls"
    end
  end

  describe "response_format_json_object" do
    test "response_format_json_object" do
      {:ok, client} = LiterLlm.create_client("test-key", base_url: (System.get_env("MOCK_SERVER_URL") || "") <> "/fixtures/response_format_json_object")
      {:ok, result} = LiterLlm.defaultclient_chat_async(client, "{\"messages\":[{\"content\":\"Respond with JSON only.\",\"role\":\"system\"},{\"content\":\"Give me a user object with name and age fields.\",\"role\":\"user\"}],\"model\":\"gpt-4\",\"response_format\":{\"type\":\"json_object\"}}")
      assert length(result.choices) == 1
      assert Enum.at(result.choices, 0).message.content != ""
      assert String.trim(to_string(Enum.at(result.choices, 0).finish_reason)) == "stop"
    end
  end

  describe "response_format_json_schema" do
    test "response_format_json_schema" do
      {:ok, client} = LiterLlm.create_client("test-key", base_url: (System.get_env("MOCK_SERVER_URL") || "") <> "/fixtures/response_format_json_schema")
      {:ok, result} = LiterLlm.defaultclient_chat_async(client, "{\"messages\":[{\"content\":\"What is the temperature in Paris today?\",\"role\":\"user\"}],\"model\":\"gpt-4\",\"response_format\":{\"json_schema\":{\"name\":\"weather\",\"schema\":{\"properties\":{\"temp\":{\"type\":\"number\"}},\"required\":[\"temp\"],\"type\":\"object\"}},\"type\":\"json_schema\"}}")
      assert length(result.choices) == 1
      assert Enum.at(result.choices, 0).message.content != ""
      assert String.trim(to_string(Enum.at(result.choices, 0).finish_reason)) == "stop"
    end
  end

  describe "seed_parameter" do
    test "seed_parameter" do
      {:ok, client} = LiterLlm.create_client("test-key", base_url: (System.get_env("MOCK_SERVER_URL") || "") <> "/fixtures/seed_parameter")
      {:ok, result} = LiterLlm.defaultclient_chat_async(client, "{\"messages\":[{\"content\":\"Pick a random number\",\"role\":\"user\"}],\"model\":\"gpt-4\",\"seed\":42}")
      assert length(result.choices) == 1
      assert String.trim(to_string(Enum.at(result.choices, 0).finish_reason)) == "stop"
      assert result.system_fingerprint != ""
    end
  end

  describe "stop_sequences" do
    test "stop_sequences" do
      {:ok, client} = LiterLlm.create_client("test-key", base_url: (System.get_env("MOCK_SERVER_URL") || "") <> "/fixtures/stop_sequences")
      {:ok, result} = LiterLlm.defaultclient_chat_async(client, "{\"messages\":[{\"content\":\"List items until you see STOP\",\"role\":\"user\"}],\"model\":\"gpt-4\",\"stop\":[\"STOP\",\"END\"]}")
      assert length(result.choices) == 1
      assert String.trim(to_string(Enum.at(result.choices, 0).finish_reason)) == "stop"
    end
  end

  describe "tool_choice_required" do
    test "tool_choice_required" do
      {:ok, client} = LiterLlm.create_client("test-key", base_url: (System.get_env("MOCK_SERVER_URL") || "") <> "/fixtures/tool_choice_required")
      {:ok, result} = LiterLlm.defaultclient_chat_async(client, "{\"messages\":[{\"content\":\"What is the weather today?\",\"role\":\"user\"}],\"model\":\"gpt-4\",\"tool_choice\":\"required\",\"tools\":[{\"function\":{\"description\":\"Get the current weather for a given location\",\"name\":\"get_weather\",\"parameters\":{\"properties\":{\"location\":{\"description\":\"The city name\",\"type\":\"string\"}},\"required\":[\"location\"],\"type\":\"object\"}},\"type\":\"function\"}]}")
      assert length(result.choices) == 1
      assert Enum.at(result.choices, 0).message.tool_calls != ""
      assert String.trim(Enum.at(Enum.at(result.choices, 0).message.tool_calls, 0).function.name) == "get_weather"
      assert String.trim(to_string(Enum.at(result.choices, 0).finish_reason)) == "tool_calls"
    end
  end

  describe "tool_choice_specific" do
    test "tool_choice_specific" do
      {:ok, client} = LiterLlm.create_client("test-key", base_url: (System.get_env("MOCK_SERVER_URL") || "") <> "/fixtures/tool_choice_specific")
      {:ok, result} = LiterLlm.defaultclient_chat_async(client, "{\"messages\":[{\"content\":\"What is the weather in Paris?\",\"role\":\"user\"}],\"model\":\"gpt-4\",\"tool_choice\":{\"function\":{\"name\":\"get_weather\"},\"type\":\"function\"},\"tools\":[{\"function\":{\"description\":\"Get the current weather for a given location\",\"name\":\"get_weather\",\"parameters\":{\"properties\":{\"location\":{\"description\":\"The city name\",\"type\":\"string\"}},\"required\":[\"location\"],\"type\":\"object\"}},\"type\":\"function\"},{\"function\":{\"description\":\"Search the web for information\",\"name\":\"search_web\",\"parameters\":{\"properties\":{\"query\":{\"description\":\"The search query\",\"type\":\"string\"}},\"required\":[\"query\"],\"type\":\"object\"}},\"type\":\"function\"}]}")
      assert length(result.choices) == 1
      assert Enum.at(result.choices, 0).message.tool_calls != ""
      assert String.trim(Enum.at(Enum.at(result.choices, 0).message.tool_calls, 0).function.name) == "get_weather"
      assert String.trim(to_string(Enum.at(result.choices, 0).finish_reason)) == "tool_calls"
    end
  end
end
