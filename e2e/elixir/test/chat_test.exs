# This file is auto-generated by alef — DO NOT EDIT.
# alef:hash:0c87af5afc13eac71e9b2ee19272a82016f12f615674e5e518bc5f94a87e7adc
# To regenerate: alef generate
# To verify freshness: alef verify --exit-code
# Issues & docs: https://github.com/kreuzberg-dev/alef
# E2e tests for category: chat
defmodule E2e.ChatTest do
  use ExUnit.Case, async: false

  describe "developer_message" do
    test "developer_message" do
      {:ok, client} = LiterLlm.create_client("test-key", (System.get_env("MOCK_SERVER_URL") || "") <> "/fixtures/developer_message")
      {:ok, result} = LiterLlm.chat_async(client, nil)
      assert length(result.choices) == 1
      assert String.trim(Enum.at(result.choices, 0).message.content) == "s[::-1]"
      assert String.trim(Enum.at(result.choices, 0).finish_reason) == "stop"
    end
  end

  describe "edge_chat_max_tokens" do
    test "edge_chat_max_tokens" do
      {:ok, client} = LiterLlm.create_client("test-key", (System.get_env("MOCK_SERVER_URL") || "") <> "/fixtures/edge_chat_max_tokens")
      {:ok, result} = LiterLlm.chat_async(client, nil)
      assert String.trim(Enum.at(result.choices, 0).finish_reason) == "length"
      assert Enum.at(result.choices, 0).message.content != ""
    end
  end

  describe "edge_chat_system_only" do
    test "edge_chat_system_only" do
      {:ok, client} = LiterLlm.create_client("test-key", (System.get_env("MOCK_SERVER_URL") || "") <> "/fixtures/edge_chat_system_only")
      {:ok, result} = LiterLlm.chat_async(client, nil)
      assert Enum.at(result.choices, 0).message.content != ""
    end
  end

  describe "edge_chat_temperature_zero" do
    test "edge_chat_temperature_zero" do
      {:ok, client} = LiterLlm.create_client("test-key", (System.get_env("MOCK_SERVER_URL") || "") <> "/fixtures/edge_chat_temperature_zero")
      {:ok, result} = LiterLlm.chat_async(client, nil)
      assert Enum.at(result.choices, 0).message.content != ""
    end
  end

  describe "finish_reason_content_filter" do
    test "finish_reason_content_filter" do
      {:ok, client} = LiterLlm.create_client("test-key", (System.get_env("MOCK_SERVER_URL") || "") <> "/fixtures/finish_reason_content_filter")
      {:ok, result} = LiterLlm.chat_async(client, nil)
      assert length(result.choices) == 1
      assert String.trim(Enum.at(result.choices, 0).finish_reason) == "content_filter"
    end
  end

  describe "finish_reason_length" do
    test "finish_reason_length" do
      {:ok, client} = LiterLlm.create_client("test-key", (System.get_env("MOCK_SERVER_URL") || "") <> "/fixtures/finish_reason_length")
      {:ok, result} = LiterLlm.chat_async(client, nil)
      assert length(result.choices) == 1
      assert String.trim(Enum.at(result.choices, 0).finish_reason) == "length"
    end
  end

  describe "multi_turn_conversation" do
    test "multi_turn_conversation" do
      {:ok, client} = LiterLlm.create_client("test-key", (System.get_env("MOCK_SERVER_URL") || "") <> "/fixtures/multi_turn_conversation")
      {:ok, result} = LiterLlm.chat_async(client, nil)
      assert length(result.choices) == 1
      assert String.trim(Enum.at(result.choices, 0).message.content) == "4 + 4 equals 8."
      assert String.trim(Enum.at(result.choices, 0).finish_reason) == "stop"
    end
  end

  describe "parallel_tool_calls" do
    test "parallel_tool_calls" do
      {:ok, client} = LiterLlm.create_client("test-key", (System.get_env("MOCK_SERVER_URL") || "") <> "/fixtures/parallel_tool_calls")
      {:ok, result} = LiterLlm.chat_async(client, nil)
      assert length(result.choices) == 1
      assert Enum.at(result.choices, 0).message.tool_calls != ""
      assert length(Enum.at(result.choices, 0).message.tool_calls) == 2
      assert String.trim(Enum.at(result.choices, 0).finish_reason) == "tool_calls"
    end
  end

  describe "response_format_json_object" do
    test "response_format_json_object" do
      {:ok, client} = LiterLlm.create_client("test-key", (System.get_env("MOCK_SERVER_URL") || "") <> "/fixtures/response_format_json_object")
      {:ok, result} = LiterLlm.chat_async(client, nil)
      assert length(result.choices) == 1
      assert Enum.at(result.choices, 0).message.content != ""
      assert String.trim(Enum.at(result.choices, 0).finish_reason) == "stop"
    end
  end

  describe "response_format_json_schema" do
    test "response_format_json_schema" do
      {:ok, client} = LiterLlm.create_client("test-key", (System.get_env("MOCK_SERVER_URL") || "") <> "/fixtures/response_format_json_schema")
      {:ok, result} = LiterLlm.chat_async(client, nil)
      assert length(result.choices) == 1
      assert Enum.at(result.choices, 0).message.content != ""
      assert String.trim(Enum.at(result.choices, 0).finish_reason) == "stop"
    end
  end

  describe "seed_parameter" do
    test "seed_parameter" do
      {:ok, client} = LiterLlm.create_client("test-key", (System.get_env("MOCK_SERVER_URL") || "") <> "/fixtures/seed_parameter")
      {:ok, result} = LiterLlm.chat_async(client, nil)
      assert length(result.choices) == 1
      assert String.trim(Enum.at(result.choices, 0).finish_reason) == "stop"
      assert result.system_fingerprint != ""
    end
  end

  describe "stop_sequences" do
    test "stop_sequences" do
      {:ok, client} = LiterLlm.create_client("test-key", (System.get_env("MOCK_SERVER_URL") || "") <> "/fixtures/stop_sequences")
      {:ok, result} = LiterLlm.chat_async(client, nil)
      assert length(result.choices) == 1
      assert String.trim(Enum.at(result.choices, 0).finish_reason) == "stop"
    end
  end

  describe "tool_choice_required" do
    test "tool_choice_required" do
      {:ok, client} = LiterLlm.create_client("test-key", (System.get_env("MOCK_SERVER_URL") || "") <> "/fixtures/tool_choice_required")
      {:ok, result} = LiterLlm.chat_async(client, nil)
      assert length(result.choices) == 1
      assert Enum.at(result.choices, 0).message.tool_calls != ""
      assert String.trim(Enum.at(Enum.at(result.choices, 0).message.tool_calls, 0).function.name) == "get_weather"
      assert String.trim(Enum.at(result.choices, 0).finish_reason) == "tool_calls"
    end
  end

  describe "tool_choice_specific" do
    test "tool_choice_specific" do
      {:ok, client} = LiterLlm.create_client("test-key", (System.get_env("MOCK_SERVER_URL") || "") <> "/fixtures/tool_choice_specific")
      {:ok, result} = LiterLlm.chat_async(client, nil)
      assert length(result.choices) == 1
      assert Enum.at(result.choices, 0).message.tool_calls != ""
      assert String.trim(Enum.at(Enum.at(result.choices, 0).message.tool_calls, 0).function.name) == "get_weather"
      assert String.trim(Enum.at(result.choices, 0).finish_reason) == "tool_calls"
    end
  end
end
