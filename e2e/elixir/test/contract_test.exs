# This file is auto-generated by alef — DO NOT EDIT.
# alef:hash:a4d454c0756a15ceddae0a8c1fb84ef9dcd2872078b108f8ce5789cc78893a8d
# To regenerate: alef generate
# To verify freshness: alef verify --exit-code
# E2e tests for category: contract
defmodule E2e.ContractTest do
  use ExUnit.Case, async: false

  describe "binding_api_parity" do
    test "binding_api_parity" do
      {:ok, client} = LiterLlm.create_client("test-key", base_url: (System.get_env("MOCK_SERVER_URL") || "") <> "/fixtures/binding_api_parity")
      {:ok, result} = LiterLlm.defaultclient_chat_async(client, "{\"messages\":[{\"content\":\"Contract test\",\"role\":\"user\"}],\"model\":\"openai/gpt-4o\"}")
      assert length(result.choices) == 1
      assert String.trim(((Enum.at(result.choices, 0).message.content && Enum.at(result.choices, 0).message.content.text) || "")) == "OK"
      assert String.trim(to_string(Enum.at(result.choices, 0).finish_reason)) == "stop"
      assert result.usage.total_tokens == 6
      assert String.trim(result.model) == "gpt-4o"
    end
  end

  describe "contract_ocr" do
    test "contract_ocr" do
      {:ok, client} = LiterLlm.create_client("test-key", base_url: (System.get_env("MOCK_SERVER_URL") || "") <> "/fixtures/contract_ocr")
      {:ok, result} = LiterLlm.defaultclient_ocr_async(client, "{\"document\":{\"type\":\"document_url\",\"url\":\"https://example.com/contract-test.pdf\"},\"model\":\"mistral/mistral-ocr-latest\"}")
    end
  end

  describe "contract_search" do
    test "contract_search" do
      {:ok, client} = LiterLlm.create_client("test-key", base_url: (System.get_env("MOCK_SERVER_URL") || "") <> "/fixtures/contract_search")
      {:ok, result} = LiterLlm.defaultclient_search_async(client, "{\"model\":\"brave/web-search\",\"query\":\"contract test query\"}")
    end
  end
end
