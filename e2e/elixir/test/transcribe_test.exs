# This file is auto-generated by alef — DO NOT EDIT.
# alef:hash:c071a6bcd6afff79855ab1fd91e9354629d0692430e1fb0062f85f88d6f826e6
# To regenerate: alef generate
# To verify freshness: alef verify --exit-code
# Issues & docs: https://github.com/kreuzberg-dev/alef
# E2e tests for category: transcribe
defmodule E2e.TranscribeTest do
  use ExUnit.Case, async: false

  describe "edge_transcribe_empty_audio" do
    test "edge_transcribe_empty_audio" do
      {:ok, client} = LiterLlm.create_client("test-key", (System.get_env("MOCK_SERVER_URL") || "") <> "/fixtures/edge_transcribe_empty_audio")
      {:ok, result} = LiterLlm.transcribe_async(client, nil)
      assert String.contains?(to_string(result.text), "")
    end
  end

  describe "edge_transcribe_with_timestamps" do
    test "edge_transcribe_with_timestamps" do
      {:ok, client} = LiterLlm.create_client("test-key", (System.get_env("MOCK_SERVER_URL") || "") <> "/fixtures/edge_transcribe_with_timestamps")
      {:ok, result} = LiterLlm.transcribe_async(client, nil)
      assert result.text != ""
      assert length(result.segments) == 3
      assert Enum.at(result.segments, 0).id == 0
    end
  end

  describe "error_transcribe_auth_401" do
    test "error_transcribe_auth_401" do
      {:ok, client} = LiterLlm.create_client("test-key", (System.get_env("MOCK_SERVER_URL") || "") <> "/fixtures/error_transcribe_auth_401")
      assert {:error, _} = LiterLlm.transcribe_async(client, nil)
    end
  end

  describe "error_transcribe_bad_format" do
    test "error_transcribe_bad_format" do
      {:ok, client} = LiterLlm.create_client("test-key", (System.get_env("MOCK_SERVER_URL") || "") <> "/fixtures/error_transcribe_bad_format")
      assert {:error, _} = LiterLlm.transcribe_async(client, nil)
    end
  end

  describe "smoke_transcribe_basic" do
    test "smoke_transcribe_basic" do
      {:ok, client} = LiterLlm.create_client("test-key", (System.get_env("MOCK_SERVER_URL") || "") <> "/fixtures/smoke_transcribe_basic")
      {:ok, result} = LiterLlm.transcribe_async(client, nil)
      assert String.contains?(to_string(result.text), "Hello, this is a test transcription.")
    end
  end

  describe "smoke_transcribe_with_language" do
    test "smoke_transcribe_with_language" do
      {:ok, client} = LiterLlm.create_client("test-key", (System.get_env("MOCK_SERVER_URL") || "") <> "/fixtures/smoke_transcribe_with_language")
      {:ok, result} = LiterLlm.transcribe_async(client, nil)
      assert String.contains?(to_string(result.text), "Hallo, dies ist ein Testtranskription.")
    end
  end
end
