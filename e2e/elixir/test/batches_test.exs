# This file is auto-generated by alef — DO NOT EDIT.
# alef:hash:c9239a2f8e44747900f991a399929f3ed95de16b7e5508d01cf790033e697705
# To regenerate: alef generate
# To verify freshness: alef verify --exit-code
# Issues & docs: https://github.com/kreuzberg-dev/alef
# E2e tests for category: batches
defmodule E2e.BatchesTest do
  use ExUnit.Case, async: false

  describe "edge_batch_already_cancelled" do
    test "edge_batch_already_cancelled" do
      {:ok, client} = LiterLlm.create_client("test-key", (System.get_env("MOCK_SERVER_URL") || "") <> "/fixtures/edge_batch_already_cancelled")
      assert {:error, _} = LiterLlm.cancel_batch_async(client, "batch-cancelled001")
    end
  end

  describe "edge_batch_empty_list" do
    test "edge_batch_empty_list" do
      {:ok, client} = LiterLlm.create_client("test-key", (System.get_env("MOCK_SERVER_URL") || "") <> "/fixtures/edge_batch_empty_list")
      {:ok, result} = LiterLlm.list_batches_async(client)
      assert length(result.data) == 0
    end
  end

  describe "error_batch_auth_401" do
    test "error_batch_auth_401" do
      {:ok, client} = LiterLlm.create_client("test-key", (System.get_env("MOCK_SERVER_URL") || "") <> "/fixtures/error_batch_auth_401")
      assert {:error, _} = LiterLlm.create_batch_async(client, nil)
    end
  end

  describe "error_batch_invalid_file" do
    test "error_batch_invalid_file" do
      {:ok, client} = LiterLlm.create_client("test-key", (System.get_env("MOCK_SERVER_URL") || "") <> "/fixtures/error_batch_invalid_file")
      assert {:error, _} = LiterLlm.create_batch_async(client, nil)
    end
  end

  describe "error_batch_not_found" do
    test "error_batch_not_found" do
      {:ok, client} = LiterLlm.create_client("test-key", (System.get_env("MOCK_SERVER_URL") || "") <> "/fixtures/error_batch_not_found")
      assert {:error, _} = LiterLlm.retrieve_batch_async(client, "batch-nonexistent")
    end
  end

  describe "smoke_batch_completed" do
    test "smoke_batch_completed" do
      {:ok, client} = LiterLlm.create_client("test-key", (System.get_env("MOCK_SERVER_URL") || "") <> "/fixtures/smoke_batch_completed")
      {:ok, result} = LiterLlm.retrieve_batch_async(client, "batch-ghi789")
      assert result.id != ""
      assert String.trim(result.status) == "completed"
    end
  end

  describe "smoke_cancel_batch" do
    test "smoke_cancel_batch" do
      {:ok, client} = LiterLlm.create_client("test-key", (System.get_env("MOCK_SERVER_URL") || "") <> "/fixtures/smoke_cancel_batch")
      {:ok, result} = LiterLlm.cancel_batch_async(client, "batch-def456")
      assert result.id != ""
      assert String.trim(result.status) == "cancelling"
    end
  end

  describe "smoke_create_batch" do
    test "smoke_create_batch" do
      {:ok, client} = LiterLlm.create_client("test-key", (System.get_env("MOCK_SERVER_URL") || "") <> "/fixtures/smoke_create_batch")
      {:ok, result} = LiterLlm.create_batch_async(client, nil)
      assert result.id != ""
      assert String.trim(result.status) == "validating"
    end
  end

  describe "smoke_list_batches" do
    test "smoke_list_batches" do
      {:ok, client} = LiterLlm.create_client("test-key", (System.get_env("MOCK_SERVER_URL") || "") <> "/fixtures/smoke_list_batches")
      {:ok, result} = LiterLlm.list_batches_async(client)
      assert length(result.data) == 2
    end
  end

  describe "smoke_retrieve_batch" do
    test "smoke_retrieve_batch" do
      {:ok, client} = LiterLlm.create_client("test-key", (System.get_env("MOCK_SERVER_URL") || "") <> "/fixtures/smoke_retrieve_batch")
      {:ok, result} = LiterLlm.retrieve_batch_async(client, "batch-abc123")
      assert result.id != ""
      assert String.trim(result.status) == "in_progress"
    end
  end
end
