// This file is auto-generated by alef — DO NOT EDIT.
// alef:hash:98c7145fad0ce5724fbfe5458ad488240c7f2ce919071c4ff21b0b15e7db783f
// To regenerate: alef generate
// To verify freshness: alef verify --exit-code
// Issues & docs: https://github.com/kreuzberg-dev/alef

package dev.kreuzberg.literllm.e2e;import org.junit.jupiter.api.Test;import static org.junit.jupiter.api.Assertions.*;
/** E2e tests for category: files. */
class FilesTest {

    @Test
    void testEdgeFileEmptyList() throws Exception {
        // List files when no files have been uploaded
        String mockUrl = System.getenv("MOCK_SERVER_URL") + "/fixtures/edge_file_empty_list";
        var client = LiterLlm.createClient("test-key", mockUrl, null, null, null);
        var result = client.listFiles();
assertEquals(0, result.data().size(), "expected exactly 0 elements");
    }


    @Test
    void testEdgeFileLargeUpload() throws Exception {
        // Upload a large file successfully
        String mockUrl = System.getenv("MOCK_SERVER_URL") + "/fixtures/edge_file_large_upload";
        var client = LiterLlm.createClient("test-key", mockUrl, null, null, null);
        var result = client.createFile("{\"file\":\"eyJwcm9tcHQiOiAibGFyZ2UgdHJhaW5pbmcgZGF0YSJ9Cg==\",\"filename\":\"large_training_data.jsonl\",\"purpose\":\"fine-tune\"}");
assertFalse(result.id().isEmpty(), "expected non-empty value");
    }


    @Test
    void testErrorFileAuth401() throws Exception {
        // 401 Unauthorized when listing files with invalid API key
        String mockUrl = System.getenv("MOCK_SERVER_URL") + "/fixtures/error_file_auth_401";
        var client = LiterLlm.createClient("test-key", mockUrl, null, null, null);
        assertThrows(Exception.class, () -> client.listFiles());
    }


    @Test
    void testErrorFileBadPurpose() throws Exception {
        // 400 Bad Request when uploading a file with invalid purpose
        String mockUrl = System.getenv("MOCK_SERVER_URL") + "/fixtures/error_file_bad_purpose";
        var client = LiterLlm.createClient("test-key", mockUrl, null, null, null);
        assertThrows(Exception.class, () -> client.createFile("{\"file\":\"data.jsonl\",\"purpose\":\"invalid-purpose\"}"));
    }


    @Test
    void testErrorFileNotFound() throws Exception {
        // 404 Not Found when retrieving a nonexistent file
        String mockUrl = System.getenv("MOCK_SERVER_URL") + "/fixtures/error_file_not_found";
        var client = LiterLlm.createClient("test-key", mockUrl, null, null, null);
        assertThrows(Exception.class, () -> client.retrieveFile("file-nonexistent"));
    }


    @Test
    void testSmokeCreateFile() throws Exception {
        // Upload a file for use with the API
        String mockUrl = System.getenv("MOCK_SERVER_URL") + "/fixtures/smoke_create_file";
        var client = LiterLlm.createClient("test-key", mockUrl, null, null, null);
        var result = client.createFile("{\"file\":\"eyJwcm9tcHQiOiAiaGVsbG8ifQo=\",\"filename\":\"training_data.jsonl\",\"purpose\":\"fine-tune\"}");
assertFalse(result.id().isEmpty(), "expected non-empty value");
    }


    @Test
    void testSmokeDeleteFile() throws Exception {
        // Delete an uploaded file
        String mockUrl = System.getenv("MOCK_SERVER_URL") + "/fixtures/smoke_delete_file";
        var client = LiterLlm.createClient("test-key", mockUrl, null, null, null);
        var result = client.deleteFile("file-abc123");
assertFalse(result.id().isEmpty(), "expected non-empty value");assertEquals(true, result.deleted());
    }


    @Test
    void testSmokeFileContent() throws Exception {
        // Retrieve the content of an uploaded file
        String mockUrl = System.getenv("MOCK_SERVER_URL") + "/fixtures/smoke_file_content";
        var client = LiterLlm.createClient("test-key", mockUrl, null, null, null);
        var result = client.fileContent("file-abc123");
assertFalse(result.content().isEmpty(), "expected non-empty value");
    }


    @Test
    void testSmokeListFiles() throws Exception {
        // List all uploaded files
        String mockUrl = System.getenv("MOCK_SERVER_URL") + "/fixtures/smoke_list_files";
        var client = LiterLlm.createClient("test-key", mockUrl, null, null, null);
        var result = client.listFiles();
assertEquals(2, result.data().size(), "expected exactly 2 elements");
    }


    @Test
    void testSmokeRetrieveFile() throws Exception {
        // Retrieve metadata for an uploaded file
        String mockUrl = System.getenv("MOCK_SERVER_URL") + "/fixtures/smoke_retrieve_file";
        var client = LiterLlm.createClient("test-key", mockUrl, null, null, null);
        var result = client.retrieveFile("file-abc123");
assertFalse(result.id().isEmpty(), "expected non-empty value");
    }

}
