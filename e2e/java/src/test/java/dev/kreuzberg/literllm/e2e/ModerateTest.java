// This file is auto-generated by alef — DO NOT EDIT.
// alef:hash:ee597170733dcbe22d1d469bfe28893f0f47f6693899168f37f1aabe8281efb7
// To regenerate: alef generate
// To verify freshness: alef verify --exit-code
// Issues & docs: https://github.com/kreuzberg-dev/alef

package dev.kreuzberg.literllm.e2e;

import org.junit.jupiter.api.Test;
import static org.junit.jupiter.api.Assertions.*;
import dev.kreuzberg.literllm.LiterLlm;
import dev.kreuzberg.literllm.ModerationRequest;
import java.util.Optional;

/** E2e tests for category: moderate. */
class ModerateTest {

    @Test
    void testEdgeModerateAllCategories() throws Exception {
        // Moderation response with multiple categories flagged
        var request = ModerationRequest.fromJson("{\"input\":\"Extremely harmful content targeting multiple categories\",\"model\":\"omni-moderation-latest\"}");

        String mockUrl = System.getenv("MOCK_SERVER_URL") + "/fixtures/edge_moderate_all_categories";
        var client = LiterLlm.createClient("test-key", mockUrl, null, null, null);
        var result = client.moderate(request);
assertEquals(1, result.results().size(), "expected exactly 1 elements");assertEquals(true, result.results().get(0).flagged());
    }


    @Test
    void testEdgeModerateEmptyInput() throws Exception {
        // Moderation with empty string input
        var request = ModerationRequest.fromJson("{\"input\":\"\",\"model\":\"omni-moderation-latest\"}");

        String mockUrl = System.getenv("MOCK_SERVER_URL") + "/fixtures/edge_moderate_empty_input";
        var client = LiterLlm.createClient("test-key", mockUrl, null, null, null);
        var result = client.moderate(request);
assertEquals(1, result.results().size(), "expected exactly 1 elements");assertEquals(false, result.results().get(0).flagged());
    }


    @Test
    void testErrorModerateAuth401() throws Exception {
        // 401 Unauthorized for moderation with invalid API key
        var request = ModerationRequest.fromJson("{\"input\":\"Hello\",\"model\":\"omni-moderation-latest\"}");

        String mockUrl = System.getenv("MOCK_SERVER_URL") + "/fixtures/error_moderate_auth_401";
        var client = LiterLlm.createClient("test-key", mockUrl, null, null, null);
        assertThrows(Exception.class, () -> client.moderate(request));
    }


    @Test
    void testErrorModerateBadRequest() throws Exception {
        // 400 Bad Request for moderation with invalid model
        var request = ModerationRequest.fromJson("{\"input\":\"Hello\",\"model\":\"nonexistent-moderation\"}");

        String mockUrl = System.getenv("MOCK_SERVER_URL") + "/fixtures/error_moderate_bad_request";
        var client = LiterLlm.createClient("test-key", mockUrl, null, null, null);
        assertThrows(Exception.class, () -> client.moderate(request));
    }


    @Test
    void testSmokeModerateBatch() throws Exception {
        // Moderate multiple inputs in a single request
        var request = ModerationRequest.fromJson("{\"input\":[\"Hello world\",\"Nice weather today\"],\"model\":\"omni-moderation-latest\"}");

        String mockUrl = System.getenv("MOCK_SERVER_URL") + "/fixtures/smoke_moderate_batch";
        var client = LiterLlm.createClient("test-key", mockUrl, null, null, null);
        var result = client.moderate(request);
assertEquals(2, result.results().size(), "expected exactly 2 elements");assertEquals(false, result.results().get(0).flagged());
    }


    @Test
    void testSmokeModerateFlagged() throws Exception {
        // Moderation detects flagged content
        var request = ModerationRequest.fromJson("{\"input\":\"I want to hurt someone very badly\",\"model\":\"omni-moderation-latest\"}");

        String mockUrl = System.getenv("MOCK_SERVER_URL") + "/fixtures/smoke_moderate_flagged";
        var client = LiterLlm.createClient("test-key", mockUrl, null, null, null);
        var result = client.moderate(request);
assertEquals(1, result.results().size(), "expected exactly 1 elements");assertEquals(true, result.results().get(0).flagged());
    }


    @Test
    void testSmokeModerateSingle() throws Exception {
        // Moderate a single non-flagged input
        var request = ModerationRequest.fromJson("{\"input\":\"The weather is nice today.\",\"model\":\"omni-moderation-latest\"}");

        String mockUrl = System.getenv("MOCK_SERVER_URL") + "/fixtures/smoke_moderate_single";
        var client = LiterLlm.createClient("test-key", mockUrl, null, null, null);
        var result = client.moderate(request);
assertEquals(1, result.results().size(), "expected exactly 1 elements");assertEquals(false, result.results().get(0).flagged());
    }

}
