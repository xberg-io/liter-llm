// This file is auto-generated by alef — DO NOT EDIT.
// alef:hash:abc77815caa6b6a8b4076667737831bfc19bec54aa28e3f1abb635db5d3b22ab
// To regenerate: alef generate
// To verify freshness: alef verify --exit-code
// Issues & docs: https://github.com/kreuzberg-dev/alef

package dev.kreuzberg.literllm.e2e;

import org.junit.jupiter.api.Test;
import static org.junit.jupiter.api.Assertions.*;
import dev.kreuzberg.literllm.LiterLlm;
import dev.kreuzberg.literllm.ChatCompletionRequest;
import dev.kreuzberg.literllm.OcrRequest;
import dev.kreuzberg.literllm.SearchRequest;
import java.util.Optional;

/** E2e tests for category: contract. */
class ContractTest {

    @Test
    void testBindingApiParity() throws Exception {
        // Verify all bindings expose the full API surface — constructor accepts all config options and every method exists
        var request = ChatCompletionRequest.fromJson("{\"messages\":[{\"content\":\"Contract test\",\"role\":\"user\"}],\"model\":\"openai/gpt-4o\"}");

        String mockUrl = System.getProperty("mockServerUrl", System.getenv("MOCK_SERVER_URL")) + "/fixtures/binding_api_parity";
        var client = LiterLlm.createClient("test-key", mockUrl, null, null, null);
        var result = client.chat(request);
assertEquals(1, result.choices().size(), "expected exactly 1 elements");assertEquals("OK", java.util.Optional.ofNullable(result.choices().get(0).message().content()).orElse("").trim());assertEquals("stop", java.util.Optional.ofNullable(result.choices().get(0).finishReason()).map(v -> v.getValue()).orElse("").trim());assertEquals(6, result.usage().totalTokens());assertEquals("gpt-4o", result.model().trim());
    }


    @Test
    void testContractOcr() throws Exception {
        // Verify ocr() method exists in all bindings
        var request = OcrRequest.fromJson("{\"document\":{\"type\":\"document_url\",\"url\":\"https://example.com/contract-test.pdf\"},\"model\":\"mistral/mistral-ocr-latest\"}");

        String mockUrl = System.getProperty("mockServerUrl", System.getenv("MOCK_SERVER_URL")) + "/fixtures/contract_ocr";
        var client = LiterLlm.createClient("test-key", mockUrl, null, null, null);
        var result = client.ocr(request);

    }


    @Test
    void testContractSearch() throws Exception {
        // Verify search() method exists in all bindings
        var request = SearchRequest.fromJson("{\"model\":\"brave/web-search\",\"query\":\"contract test query\"}");

        String mockUrl = System.getProperty("mockServerUrl", System.getenv("MOCK_SERVER_URL")) + "/fixtures/contract_search";
        var client = LiterLlm.createClient("test-key", mockUrl, null, null, null);
        var result = client.search(request);

    }

}
