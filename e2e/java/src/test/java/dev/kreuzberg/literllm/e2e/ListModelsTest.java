// This file is auto-generated by alef — DO NOT EDIT.
// alef:hash:10fc0ad2cbf2838cb24109b910924f9d5287aba785530ba4550e9668e11ced2b
// To regenerate: alef generate
// To verify freshness: alef verify --exit-code
// Issues & docs: https://github.com/kreuzberg-dev/alef

package dev.kreuzberg.literllm.e2e;

import org.junit.jupiter.api.Test;
import static org.junit.jupiter.api.Assertions.*;
import dev.kreuzberg.literllm.LiterLlm;

/** E2e tests for category: list-models. */
class ListModelsTest {

    @Test
    void testEmptyModelList() throws Exception {
        // List models response returns an empty data array when no models are available
        String mockUrl = System.getProperty("mockServerUrl", System.getenv("MOCK_SERVER_URL")) + "/fixtures/empty_model_list";
        var client = LiterLlm.createClient("test-key", mockUrl, null, null, null);
        var result = client.listModels();
assertTrue(result.data().size() >= 0, "expected at least 0 elements");assertEquals(0, result.data().size(), "expected exactly 0 elements");
    }


    @Test
    void testListModelsError401() throws Exception {
        // 401 Unauthorized error on list models request when API key is invalid
        String mockUrl = System.getProperty("mockServerUrl", System.getenv("MOCK_SERVER_URL")) + "/fixtures/list_models_error_401";
        var client = LiterLlm.createClient("test-key", mockUrl, null, null, null);
        assertThrows(Exception.class, () -> client.listModels());
    }


    @Test
    void testListModelsError500() throws Exception {
        // 500 Internal Server Error when listing models due to server failure
        String mockUrl = System.getProperty("mockServerUrl", System.getenv("MOCK_SERVER_URL")) + "/fixtures/list_models_error_500";
        var client = LiterLlm.createClient("test-key", mockUrl, null, null, null);
        assertThrows(Exception.class, () -> client.listModels());
    }


    @Test
    void testListModelsFiltered() throws Exception {
        // List models response with multiple model objects
        String mockUrl = System.getProperty("mockServerUrl", System.getenv("MOCK_SERVER_URL")) + "/fixtures/list_models_filtered";
        var client = LiterLlm.createClient("test-key", mockUrl, null, null, null);
        var result = client.listModels();
assertTrue(result.data().size() >= 5, "expected at least 5 elements");assertEquals("model", result.data().get(0).object().trim());
    }

}
