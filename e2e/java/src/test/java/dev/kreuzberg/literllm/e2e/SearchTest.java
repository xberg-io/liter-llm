// This file is auto-generated by alef — DO NOT EDIT.
// alef:hash:b050ea71ab4329ba71b72b70aea5962d680c103637f14f24038bbc3022ad41ab
// To regenerate: alef generate
// To verify freshness: alef verify --exit-code
// Issues & docs: https://github.com/kreuzberg-dev/alef

package dev.kreuzberg.literllm.e2e;

import org.junit.jupiter.api.Test;
import static org.junit.jupiter.api.Assertions.*;
import dev.kreuzberg.literllm.LiterLlm;
import dev.kreuzberg.literllm.SearchRequest;
import java.util.Optional;

/** E2e tests for category: search. */
class SearchTest {

    @Test
    void testSearchBasic() throws Exception {
        // Basic web search request with a simple query
        var request = SearchRequest.fromJson("{\"model\":\"brave/web-search\",\"query\":\"What is Rust programming language?\"}");

        String mockUrl = System.getProperty("mockServerUrl", System.getenv("MOCK_SERVER_URL")) + "/fixtures/search_basic";
        var client = LiterLlm.createClient("test-key", mockUrl, null, null, null);
        var result = client.search(request);

    }


    @Test
    void testSearchEmptyResults() throws Exception {
        // Web search with a query that returns no results
        var request = SearchRequest.fromJson("{\"model\":\"brave/web-search\",\"query\":\"xyznonexistent12345xyz\"}");

        String mockUrl = System.getProperty("mockServerUrl", System.getenv("MOCK_SERVER_URL")) + "/fixtures/search_empty_results";
        var client = LiterLlm.createClient("test-key", mockUrl, null, null, null);
        var result = client.search(request);

    }


    @Test
    void testSearchError400() throws Exception {
        // 400 Bad Request error when search query is empty
        // Wrap setup_lines + call_expr inside the lambda so error fixtures
        // catch failures at *any* step — including `<Type>.fromJson(...)`
        // calls that throw on malformed JSON (e.g. error fixtures with an
        // invalid enum value like `"purpose":"invalid-purpose"`). Mirrors
        // the C# `Assert.ThrowsAnyAsync(() => client.X(Type.FromJson(...)))`
        // pattern.
        assertThrows(Exception.class, () -> {
        var request = SearchRequest.fromJson("{\"model\":\"brave/web-search\",\"query\":\"\"}");

            String mockUrl = System.getProperty("mockServerUrl", System.getenv("MOCK_SERVER_URL")) + "/fixtures/search_error_400";
            var client = LiterLlm.createClient("test-key", mockUrl, null, null, null);
            client.search(request);
        });
    }


    @Test
    void testSearchError401() throws Exception {
        // 401 Unauthorized error on web search due to invalid API credentials
        // Wrap setup_lines + call_expr inside the lambda so error fixtures
        // catch failures at *any* step — including `<Type>.fromJson(...)`
        // calls that throw on malformed JSON (e.g. error fixtures with an
        // invalid enum value like `"purpose":"invalid-purpose"`). Mirrors
        // the C# `Assert.ThrowsAnyAsync(() => client.X(Type.FromJson(...)))`
        // pattern.
        assertThrows(Exception.class, () -> {
        var request = SearchRequest.fromJson("{\"model\":\"brave/web-search\",\"query\":\"test\"}");

            String mockUrl = System.getProperty("mockServerUrl", System.getenv("MOCK_SERVER_URL")) + "/fixtures/search_error_401";
            var client = LiterLlm.createClient("test-key", mockUrl, null, null, null);
            client.search(request);
        });
    }


    @Test
    void testSearchWithMaxResults() throws Exception {
        // Search request with max_results parameter limiting response count
        var request = SearchRequest.fromJson("{\"max_results\":2,\"model\":\"brave/web-search\",\"query\":\"Rust programming\"}");

        String mockUrl = System.getProperty("mockServerUrl", System.getenv("MOCK_SERVER_URL")) + "/fixtures/search_with_max_results";
        var client = LiterLlm.createClient("test-key", mockUrl, null, null, null);
        var result = client.search(request);
assertEquals(2, result.results().size(), "expected exactly 2 elements");
    }

}
