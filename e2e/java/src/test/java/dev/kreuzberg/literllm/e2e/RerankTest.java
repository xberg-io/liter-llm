// This file is auto-generated by alef — DO NOT EDIT.
// alef:hash:bdcecc074381a358ca8dc27f83d55069080fbbb91abe3ee2ea3b7c3f3edec8d4
// To regenerate: alef generate
// To verify freshness: alef verify --exit-code
// Issues & docs: https://github.com/kreuzberg-dev/alef

package dev.kreuzberg.literllm.e2e;

import org.junit.jupiter.api.Test;
import static org.junit.jupiter.api.Assertions.*;
import dev.kreuzberg.literllm.LiterLlm;

/** E2e tests for category: rerank. */
class RerankTest {

    @Test
    void testEdgeRerankEmptyQuery() throws Exception {
        // Reranking with an empty query string
        String mockUrl = System.getenv("MOCK_SERVER_URL") + "/fixtures/edge_rerank_empty_query";
        var client = LiterLlm.createClient("test-key", mockUrl, null, null, null);
        var result = client.rerank("{\"documents\":[\"Some document\",\"Another document\"],\"model\":\"rerank-v3.5\",\"query\":\"\"}");
assertEquals(2, result.results().size(), "expected exactly 2 elements");
    }


    @Test
    void testEdgeRerankSingleDoc() throws Exception {
        // Reranking with only a single document
        String mockUrl = System.getenv("MOCK_SERVER_URL") + "/fixtures/edge_rerank_single_doc";
        var client = LiterLlm.createClient("test-key", mockUrl, null, null, null);
        var result = client.rerank("{\"documents\":[\"Artificial intelligence is the simulation of human intelligence.\"],\"model\":\"rerank-v3.5\",\"query\":\"What is AI?\"}");
assertEquals(1, result.results().size(), "expected exactly 1 elements");assertTrue(result.results().get("0").relevanceScore() > 0.9d, "expected > 0.9d");
    }


    @Test
    void testErrorRerankAuth401() throws Exception {
        // 401 Unauthorized for reranking with invalid API key
        String mockUrl = System.getenv("MOCK_SERVER_URL") + "/fixtures/error_rerank_auth_401";
        var client = LiterLlm.createClient("test-key", mockUrl, null, null, null);
        assertThrows(Exception.class, () -> client.rerank("{\"documents\":[\"doc1\"],\"model\":\"rerank-v3.5\",\"query\":\"test\"}"));
    }


    @Test
    void testErrorRerankBadRequest() throws Exception {
        // 400 Bad Request for reranking with invalid model
        String mockUrl = System.getenv("MOCK_SERVER_URL") + "/fixtures/error_rerank_bad_request";
        var client = LiterLlm.createClient("test-key", mockUrl, null, null, null);
        assertThrows(Exception.class, () -> client.rerank("{\"documents\":[\"doc1\"],\"model\":\"nonexistent-rerank\",\"query\":\"test\"}"));
    }


    @Test
    void testSmokeRerankBasic() throws Exception {
        // Basic reranking of documents against a query
        String mockUrl = System.getenv("MOCK_SERVER_URL") + "/fixtures/smoke_rerank_basic";
        var client = LiterLlm.createClient("test-key", mockUrl, null, null, null);
        var result = client.rerank("{\"documents\":[\"Machine learning is a subset of AI.\",\"The weather is sunny today.\",\"Deep learning uses neural networks.\"],\"model\":\"rerank-v3.5\",\"query\":\"What is machine learning?\"}");
assertEquals(3, result.results().size(), "expected exactly 3 elements");assertTrue(result.results().get("0").relevanceScore() > 0.9d, "expected > 0.9d");
    }


    @Test
    void testSmokeRerankReturnDocs() throws Exception {
        // Reranking with return_documents flag to include document text
        String mockUrl = System.getenv("MOCK_SERVER_URL") + "/fixtures/smoke_rerank_return_docs";
        var client = LiterLlm.createClient("test-key", mockUrl, null, null, null);
        var result = client.rerank("{\"documents\":[\"Rust is a systems programming language.\",\"Iron rusts when exposed to water.\"],\"model\":\"rerank-v3.5\",\"query\":\"What is Rust?\",\"return_documents\":true}");
assertEquals(2, result.results().size(), "expected exactly 2 elements");assertFalse(java.util.Optional.ofNullable(result.results().get("0").document()).isEmpty(), "expected non-empty value");assertTrue(result.results().get("0").relevanceScore() > 0.9d, "expected > 0.9d");
    }


    @Test
    void testSmokeRerankWithTopN() throws Exception {
        // Reranking with top_n parameter to limit results
        String mockUrl = System.getenv("MOCK_SERVER_URL") + "/fixtures/smoke_rerank_with_top_n";
        var client = LiterLlm.createClient("test-key", mockUrl, null, null, null);
        var result = client.rerank("{\"documents\":[\"Python is a programming language.\",\"Cats are cute animals.\",\"Python was created by Guido van Rossum.\",\"The sun is a star.\"],\"model\":\"rerank-v3.5\",\"query\":\"What is Python?\",\"top_n\":2}");
assertEquals(2, result.results().size(), "expected exactly 2 elements");assertTrue(result.results().get("0").relevanceScore() > 0.9d, "expected > 0.9d");
    }

}
