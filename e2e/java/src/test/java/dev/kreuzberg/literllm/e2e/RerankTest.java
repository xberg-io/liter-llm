// This file is auto-generated by alef — DO NOT EDIT.
// To regenerate: alef generate
// To verify freshness: alef verify --exit-code
// Issues & docs: https://github.com/kreuzberg-dev/alef

package dev.kreuzberg.literllm.e2e;

import org.junit.jupiter.api.Test;
import static org.junit.jupiter.api.Assertions.*;
import dev.kreuzberg.literllm.LiterLlm;
import dev.kreuzberg.literllm.RerankRequest;
import java.util.Optional;
import dev.kreuzberg.literllm.JsonUtil;

/** E2e tests for category: rerank. */
class RerankTest {

    @Test
    void testEdgeRerankEmptyQuery() throws Exception {
        // Reranking with an empty query string
        var request = JsonUtil.fromJson("{\"documents\":[\"Some document\",\"Another document\"],\"model\":\"rerank-v3.5\",\"query\":\"\"}", RerankRequest.class);

        String mockServerUrl = System.getProperty("mockServerUrl"); if (mockServerUrl == null) { mockServerUrl = System.getenv("MOCK_SERVER_URL"); } String mockUrl = (mockServerUrl != null ? mockServerUrl : "http://localhost:8000") + "/fixtures/edge_rerank_empty_query";
        var client = LiterLlm.createClient("test-key", mockUrl, null, null, null);
        var result = client.rerank(request);
assertEquals(2, result.results().size(), "expected exactly 2 elements");
    }


    @Test
    void testEdgeRerankSingleDoc() throws Exception {
        // Reranking with only a single document
        var request = JsonUtil.fromJson("{\"documents\":[\"Artificial intelligence is the simulation of human intelligence.\"],\"model\":\"rerank-v3.5\",\"query\":\"What is AI?\"}", RerankRequest.class);

        String mockServerUrl = System.getProperty("mockServerUrl"); if (mockServerUrl == null) { mockServerUrl = System.getenv("MOCK_SERVER_URL"); } String mockUrl = (mockServerUrl != null ? mockServerUrl : "http://localhost:8000") + "/fixtures/edge_rerank_single_doc";
        var client = LiterLlm.createClient("test-key", mockUrl, null, null, null);
        var result = client.rerank(request);
assertEquals(1, result.results().size(), "expected exactly 1 elements");assertTrue(result.results().get(0).relevanceScore() > 0.9d, "expected > 0.9d");
    }


    @Test
    void testErrorRerankAuth401() throws Exception {
        // 401 Unauthorized for reranking with invalid API key
        // Wrap setup_lines + call_expr inside the lambda so error fixtures
        // catch failures at *any* step — including `<Type>.fromJson(...)`
        // calls that throw on malformed JSON (e.g. error fixtures with an
        // invalid enum value like `"purpose":"invalid-purpose"`). Mirrors
        // the C# `Assert.ThrowsAnyAsync(() => client.X(Type.FromJson(...)))`
        // pattern.
        assertThrows(Exception.class, () -> {
        var request = JsonUtil.fromJson("{\"documents\":[\"doc1\"],\"model\":\"rerank-v3.5\",\"query\":\"test\"}", RerankRequest.class);

            String mockServerUrl = System.getProperty("mockServerUrl"); if (mockServerUrl == null) { mockServerUrl = System.getenv("MOCK_SERVER_URL"); } String mockUrl = (mockServerUrl != null ? mockServerUrl : "http://localhost:8000") + "/fixtures/error_rerank_auth_401";
            var client = LiterLlm.createClient("test-key", mockUrl, null, null, null);
            client.rerank(request);
        });
    }


    @Test
    void testErrorRerankBadRequest() throws Exception {
        // 400 Bad Request for reranking with invalid model
        // Wrap setup_lines + call_expr inside the lambda so error fixtures
        // catch failures at *any* step — including `<Type>.fromJson(...)`
        // calls that throw on malformed JSON (e.g. error fixtures with an
        // invalid enum value like `"purpose":"invalid-purpose"`). Mirrors
        // the C# `Assert.ThrowsAnyAsync(() => client.X(Type.FromJson(...)))`
        // pattern.
        assertThrows(Exception.class, () -> {
        var request = JsonUtil.fromJson("{\"documents\":[\"doc1\"],\"model\":\"nonexistent-rerank\",\"query\":\"test\"}", RerankRequest.class);

            String mockServerUrl = System.getProperty("mockServerUrl"); if (mockServerUrl == null) { mockServerUrl = System.getenv("MOCK_SERVER_URL"); } String mockUrl = (mockServerUrl != null ? mockServerUrl : "http://localhost:8000") + "/fixtures/error_rerank_bad_request";
            var client = LiterLlm.createClient("test-key", mockUrl, null, null, null);
            client.rerank(request);
        });
    }


    @Test
    void testSmokeRerankBasic() throws Exception {
        // Basic reranking of documents against a query
        var request = JsonUtil.fromJson("{\"documents\":[\"Machine learning is a subset of AI.\",\"The weather is sunny today.\",\"Deep learning uses neural networks.\"],\"model\":\"rerank-v3.5\",\"query\":\"What is machine learning?\"}", RerankRequest.class);

        String mockServerUrl = System.getProperty("mockServerUrl"); if (mockServerUrl == null) { mockServerUrl = System.getenv("MOCK_SERVER_URL"); } String mockUrl = (mockServerUrl != null ? mockServerUrl : "http://localhost:8000") + "/fixtures/smoke_rerank_basic";
        var client = LiterLlm.createClient("test-key", mockUrl, null, null, null);
        var result = client.rerank(request);
assertEquals(3, result.results().size(), "expected exactly 3 elements");assertTrue(result.results().get(0).relevanceScore() > 0.9d, "expected > 0.9d");
    }


    @Test
    void testSmokeRerankReturnDocs() throws Exception {
        // Reranking with return_documents flag to include document text
        var request = JsonUtil.fromJson("{\"documents\":[\"Rust is a systems programming language.\",\"Iron rusts when exposed to water.\"],\"model\":\"rerank-v3.5\",\"query\":\"What is Rust?\",\"return_documents\":true}", RerankRequest.class);

        String mockServerUrl = System.getProperty("mockServerUrl"); if (mockServerUrl == null) { mockServerUrl = System.getenv("MOCK_SERVER_URL"); } String mockUrl = (mockServerUrl != null ? mockServerUrl : "http://localhost:8000") + "/fixtures/smoke_rerank_return_docs";
        var client = LiterLlm.createClient("test-key", mockUrl, null, null, null);
        var result = client.rerank(request);
assertEquals(2, result.results().size(), "expected exactly 2 elements");assertFalse(java.util.Optional.ofNullable(result.results().get(0).document()).isEmpty(), "expected non-empty value");assertTrue(result.results().get(0).relevanceScore() > 0.9d, "expected > 0.9d");
    }


    @Test
    void testSmokeRerankWithTopN() throws Exception {
        // Reranking with top_n parameter to limit results
        var request = JsonUtil.fromJson("{\"documents\":[\"Python is a programming language.\",\"Cats are cute animals.\",\"Python was created by Guido van Rossum.\",\"The sun is a star.\"],\"model\":\"rerank-v3.5\",\"query\":\"What is Python?\",\"top_n\":2}", RerankRequest.class);

        String mockServerUrl = System.getProperty("mockServerUrl"); if (mockServerUrl == null) { mockServerUrl = System.getenv("MOCK_SERVER_URL"); } String mockUrl = (mockServerUrl != null ? mockServerUrl : "http://localhost:8000") + "/fixtures/smoke_rerank_with_top_n";
        var client = LiterLlm.createClient("test-key", mockUrl, null, null, null);
        var result = client.rerank(request);
assertEquals(2, result.results().size(), "expected exactly 2 elements");assertTrue(result.results().get(0).relevanceScore() > 0.9d, "expected > 0.9d");
    }

}
