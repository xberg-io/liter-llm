// This file is auto-generated by alef — DO NOT EDIT.
// alef:hash:07562382b502a5b7ab1c75b472a27ccfc096d0dfe06c87c012ca58af37b50df9
// To regenerate: alef generate
// To verify freshness: alef verify --exit-code
// Issues & docs: https://github.com/sample_crate-dev/alef

package dev.kreuzberg.literllm.e2e;

import org.junit.jupiter.api.Test;
import static org.junit.jupiter.api.Assertions.*;
import dev.kreuzberg.literllm.LiterLlm;
import dev.kreuzberg.literllm.CreateBatchRequest;
import java.util.Optional;
import dev.kreuzberg.literllm.JsonUtil;

/** E2e tests for category: batches. */
class BatchesTest {

    @Test
    void testEdgeBatchAlreadyCancelled() throws Exception {
        // Attempt to cancel an already-cancelled batch
        // Wrap setup_lines + call_expr inside the lambda so error fixtures
        // catch failures at *any* step — including `<Type>.fromJson(...)`
        // calls that throw on malformed JSON (e.g. error fixtures with an
        // invalid enum value like `"purpose":"invalid-purpose"`). Mirrors
        // the C# `Assert.ThrowsAnyAsync(() => client.X(Type.FromJson(...)))`
        // pattern.
        assertThrows(Exception.class, () -> {
            String mockServerUrl = System.getProperty("mockServerUrl"); if (mockServerUrl == null) { mockServerUrl = System.getenv("MOCK_SERVER_URL"); } String mockUrl = (mockServerUrl != null ? mockServerUrl : "http://localhost:8000") + "/fixtures/edge_batch_already_cancelled";
            var client = LiterLlm.createClient("test-key", mockUrl, null, null, null);
            client.cancelBatch("batch-cancelled001");
        });

    }


    @Test
    void testEdgeBatchEmptyList() throws Exception {
        // List batches when no batches exist
        String mockServerUrl = System.getProperty("mockServerUrl"); if (mockServerUrl == null) { mockServerUrl = System.getenv("MOCK_SERVER_URL"); } String mockUrl = (mockServerUrl != null ? mockServerUrl : "http://localhost:8000") + "/fixtures/edge_batch_empty_list";
        var client = LiterLlm.createClient("test-key", mockUrl, null, null, null);
        var result = client.listBatches(null);
assertEquals(0, result.data().size(), "expected exactly 0 elements");

    }


    @Test
    void testErrorBatchAuth401() throws Exception {
        // 401 Unauthorized when creating a batch with invalid API key
        // Wrap setup_lines + call_expr inside the lambda so error fixtures
        // catch failures at *any* step — including `<Type>.fromJson(...)`
        // calls that throw on malformed JSON (e.g. error fixtures with an
        // invalid enum value like `"purpose":"invalid-purpose"`). Mirrors
        // the C# `Assert.ThrowsAnyAsync(() => client.X(Type.FromJson(...)))`
        // pattern.
        assertThrows(Exception.class, () -> {
        var request = JsonUtil.fromJson("{\"completion_window\":\"24h\",\"endpoint\":\"/v1/chat/completions\",\"input_file_id\":\"file-abc123\"}", CreateBatchRequest.class);

            String mockServerUrl = System.getProperty("mockServerUrl"); if (mockServerUrl == null) { mockServerUrl = System.getenv("MOCK_SERVER_URL"); } String mockUrl = (mockServerUrl != null ? mockServerUrl : "http://localhost:8000") + "/fixtures/error_batch_auth_401";
            var client = LiterLlm.createClient("test-key", mockUrl, null, null, null);
            client.createBatch(request);
        });

    }


    @Test
    void testErrorBatchInvalidFile() throws Exception {
        // 400 Bad Request when creating a batch with invalid input file
        // Wrap setup_lines + call_expr inside the lambda so error fixtures
        // catch failures at *any* step — including `<Type>.fromJson(...)`
        // calls that throw on malformed JSON (e.g. error fixtures with an
        // invalid enum value like `"purpose":"invalid-purpose"`). Mirrors
        // the C# `Assert.ThrowsAnyAsync(() => client.X(Type.FromJson(...)))`
        // pattern.
        assertThrows(Exception.class, () -> {
        var request = JsonUtil.fromJson("{\"completion_window\":\"24h\",\"endpoint\":\"/v1/chat/completions\",\"input_file_id\":\"file-wrong-purpose\"}", CreateBatchRequest.class);

            String mockServerUrl = System.getProperty("mockServerUrl"); if (mockServerUrl == null) { mockServerUrl = System.getenv("MOCK_SERVER_URL"); } String mockUrl = (mockServerUrl != null ? mockServerUrl : "http://localhost:8000") + "/fixtures/error_batch_invalid_file";
            var client = LiterLlm.createClient("test-key", mockUrl, null, null, null);
            client.createBatch(request);
        });

    }


    @Test
    void testErrorBatchNotFound() throws Exception {
        // 404 Not Found when retrieving a nonexistent batch
        // Wrap setup_lines + call_expr inside the lambda so error fixtures
        // catch failures at *any* step — including `<Type>.fromJson(...)`
        // calls that throw on malformed JSON (e.g. error fixtures with an
        // invalid enum value like `"purpose":"invalid-purpose"`). Mirrors
        // the C# `Assert.ThrowsAnyAsync(() => client.X(Type.FromJson(...)))`
        // pattern.
        assertThrows(Exception.class, () -> {
            String mockServerUrl = System.getProperty("mockServerUrl"); if (mockServerUrl == null) { mockServerUrl = System.getenv("MOCK_SERVER_URL"); } String mockUrl = (mockServerUrl != null ? mockServerUrl : "http://localhost:8000") + "/fixtures/error_batch_not_found";
            var client = LiterLlm.createClient("test-key", mockUrl, null, null, null);
            client.retrieveBatch("batch-nonexistent");
        });

    }


    @Test
    void testSmokeBatchCompleted() throws Exception {
        // Retrieve a completed batch job with output file
        String mockServerUrl = System.getProperty("mockServerUrl"); if (mockServerUrl == null) { mockServerUrl = System.getenv("MOCK_SERVER_URL"); } String mockUrl = (mockServerUrl != null ? mockServerUrl : "http://localhost:8000") + "/fixtures/smoke_batch_completed";
        var client = LiterLlm.createClient("test-key", mockUrl, null, null, null);
        var result = client.retrieveBatch("batch-ghi789");
assertFalse(result.id().isEmpty(), "expected non-empty value");assertEquals("completed", result.status().getValue().trim());

    }


    @Test
    void testSmokeCancelBatch() throws Exception {
        // Cancel a running batch job
        String mockServerUrl = System.getProperty("mockServerUrl"); if (mockServerUrl == null) { mockServerUrl = System.getenv("MOCK_SERVER_URL"); } String mockUrl = (mockServerUrl != null ? mockServerUrl : "http://localhost:8000") + "/fixtures/smoke_cancel_batch";
        var client = LiterLlm.createClient("test-key", mockUrl, null, null, null);
        var result = client.cancelBatch("batch-def456");
assertFalse(result.id().isEmpty(), "expected non-empty value");assertEquals("cancelling", result.status().getValue().trim());

    }


    @Test
    void testSmokeCreateBatch() throws Exception {
        // Create a new batch processing job
        var request = JsonUtil.fromJson("{\"completion_window\":\"24h\",\"endpoint\":\"/v1/chat/completions\",\"input_file_id\":\"file-abc123\"}", CreateBatchRequest.class);

        String mockServerUrl = System.getProperty("mockServerUrl"); if (mockServerUrl == null) { mockServerUrl = System.getenv("MOCK_SERVER_URL"); } String mockUrl = (mockServerUrl != null ? mockServerUrl : "http://localhost:8000") + "/fixtures/smoke_create_batch";
        var client = LiterLlm.createClient("test-key", mockUrl, null, null, null);
        var result = client.createBatch(request);
assertFalse(result.id().isEmpty(), "expected non-empty value");assertEquals("validating", result.status().getValue().trim());

    }


    @Test
    void testSmokeListBatches() throws Exception {
        // List all batch jobs
        String mockServerUrl = System.getProperty("mockServerUrl"); if (mockServerUrl == null) { mockServerUrl = System.getenv("MOCK_SERVER_URL"); } String mockUrl = (mockServerUrl != null ? mockServerUrl : "http://localhost:8000") + "/fixtures/smoke_list_batches";
        var client = LiterLlm.createClient("test-key", mockUrl, null, null, null);
        var result = client.listBatches(null);
assertEquals(2, result.data().size(), "expected exactly 2 elements");

    }


    @Test
    void testSmokeRetrieveBatch() throws Exception {
        // Retrieve the status of a batch job
        String mockServerUrl = System.getProperty("mockServerUrl"); if (mockServerUrl == null) { mockServerUrl = System.getenv("MOCK_SERVER_URL"); } String mockUrl = (mockServerUrl != null ? mockServerUrl : "http://localhost:8000") + "/fixtures/smoke_retrieve_batch";
        var client = LiterLlm.createClient("test-key", mockUrl, null, null, null);
        var result = client.retrieveBatch("batch-abc123");
assertFalse(result.id().isEmpty(), "expected non-empty value");assertEquals("in_progress", result.status().getValue().trim());

    }

}
