// This file is auto-generated by alef — DO NOT EDIT.
// alef:hash:98d6e9ae625515e430db0dc6c3070777c123a3388a234b56450086433f7ef5de
// To regenerate: alef generate
// To verify freshness: alef verify --exit-code
// Issues & docs: https://github.com/kreuzberg-dev/alef

package dev.kreuzberg.literllm.e2e;

import org.junit.jupiter.api.Test;
import static org.junit.jupiter.api.Assertions.*;
import dev.kreuzberg.literllm.LiterLlm;
import dev.kreuzberg.literllm.OcrRequest;
import java.util.Optional;

/** E2e tests for category: ocr. */
class OcrTest {

    @Test
    void testOcrError400() throws Exception {
        // 400 Bad Request error when OCR input has an invalid image format
        // Wrap setup_lines + call_expr inside the lambda so error fixtures
        // catch failures at *any* step — including `<Type>.fromJson(...)`
        // calls that throw on malformed JSON (e.g. error fixtures with an
        // invalid enum value like `"purpose":"invalid-purpose"`). Mirrors
        // the C# `Assert.ThrowsAnyAsync(() => client.X(Type.FromJson(...)))`
        // pattern.
        assertThrows(Exception.class, () -> {
        var request = OcrRequest.fromJson("{\"document\":{\"type\":\"document_url\",\"url\":\"invalid://url\"},\"model\":\"mistral/mistral-ocr-latest\"}");

            String mockUrl = System.getProperty("mockServerUrl", System.getenv("MOCK_SERVER_URL")) + "/fixtures/ocr_error_400";
            var client = LiterLlm.createClient("test-key", mockUrl, null, null, null);
            client.ocr(request);
        });
    }


    @Test
    void testOcrError401() throws Exception {
        // 401 Unauthorized error on OCR request due to invalid API credentials
        // Wrap setup_lines + call_expr inside the lambda so error fixtures
        // catch failures at *any* step — including `<Type>.fromJson(...)`
        // calls that throw on malformed JSON (e.g. error fixtures with an
        // invalid enum value like `"purpose":"invalid-purpose"`). Mirrors
        // the C# `Assert.ThrowsAnyAsync(() => client.X(Type.FromJson(...)))`
        // pattern.
        assertThrows(Exception.class, () -> {
        var request = OcrRequest.fromJson("{\"document\":{\"type\":\"document_url\",\"url\":\"https://example.com/doc.pdf\"},\"model\":\"mistral/mistral-ocr-latest\"}");

            String mockUrl = System.getProperty("mockServerUrl", System.getenv("MOCK_SERVER_URL")) + "/fixtures/ocr_error_401";
            var client = LiterLlm.createClient("test-key", mockUrl, null, null, null);
            client.ocr(request);
        });
    }


    @Test
    void testOcrMultiPage() throws Exception {
        // OCR request returning multiple pages of document content
        var request = OcrRequest.fromJson("{\"document\":{\"type\":\"document_url\",\"url\":\"https://example.com/multipage.pdf\"},\"model\":\"mistral/mistral-ocr-latest\"}");

        String mockUrl = System.getProperty("mockServerUrl", System.getenv("MOCK_SERVER_URL")) + "/fixtures/ocr_multi_page";
        var client = LiterLlm.createClient("test-key", mockUrl, null, null, null);
        var result = client.ocr(request);
assertEquals(2, result.pages().size(), "expected exactly 2 elements");assertEquals(0, result.pages().get(0).index());assertEquals(1, result.pages().get(1).index());
    }


    @Test
    void testOcrUrlDocument() throws Exception {
        // OCR request with a document URL input
        var request = OcrRequest.fromJson("{\"document\":{\"type\":\"document_url\",\"url\":\"https://example.com/doc.pdf\"},\"model\":\"mistral/mistral-ocr-latest\"}");

        String mockUrl = System.getProperty("mockServerUrl", System.getenv("MOCK_SERVER_URL")) + "/fixtures/ocr_url_document";
        var client = LiterLlm.createClient("test-key", mockUrl, null, null, null);
        var result = client.ocr(request);

    }

}
