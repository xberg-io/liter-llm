// This file is auto-generated by alef — DO NOT EDIT.
// alef:hash:4593b463c43e95a90ab5d977a4016ac02c9fb0e0d9a228c0db914f721bbecb83
// To regenerate: alef generate
// To verify freshness: alef verify --exit-code
// Issues & docs: https://github.com/kreuzberg-dev/alef

package dev.kreuzberg.literllm.e2e;

import org.junit.jupiter.api.Test;
import static org.junit.jupiter.api.Assertions.*;
import dev.kreuzberg.literllm.LiterLlm;
import dev.kreuzberg.literllm.ChatCompletionRequest;
import java.util.Optional;

/** E2e tests for category: configuration. */
class ConfigurationTest {

    @Test
    void testCustomBaseUrl() throws Exception {
        // Client configured with a custom base URL routes all requests to that endpoint
        var request = ChatCompletionRequest.fromJson("{\"messages\":[{\"content\":\"Hello\",\"role\":\"user\"}],\"model\":\"local-model\"}");

        String mockUrl = System.getProperty("mockServerUrl", System.getenv("MOCK_SERVER_URL")) + "/fixtures/custom_base_url";
        var client = LiterLlm.createClient("test-key", mockUrl, null, null, null);
        var result = client.chat(request);
assertEquals(1, result.choices().size(), "expected exactly 1 elements");assertEquals("Hi there!", java.util.Optional.ofNullable(result.choices().get(0).message().content()).orElse("").trim());assertEquals("stop", java.util.Optional.ofNullable(result.choices().get(0).finishReason()).map(v -> v.getValue()).orElse("").trim());assertEquals("local-model", result.model().trim());
    }


    @Test
    void testExtraHeaders() throws Exception {
        // Client configured with extra custom headers successfully completes a chat request
        var request = ChatCompletionRequest.fromJson("{\"messages\":[{\"content\":\"Hello\",\"role\":\"user\"}],\"model\":\"gpt-4\"}");

        String mockUrl = System.getProperty("mockServerUrl", System.getenv("MOCK_SERVER_URL")) + "/fixtures/extra_headers";
        var client = LiterLlm.createClient("test-key", mockUrl, null, null, null);
        var result = client.chat(request);
assertEquals(1, result.choices().size(), "expected exactly 1 elements");assertEquals("stop", java.util.Optional.ofNullable(result.choices().get(0).finishReason()).map(v -> v.getValue()).orElse("").trim());
    }


    @Test
    void testLocalProviderLlamacpp() throws Exception {
        // llamacpp local provider routes requests via llamacpp/ model prefix with no auth
        var request = ChatCompletionRequest.fromJson("{\"messages\":[{\"content\":\"Hello\",\"role\":\"user\"}],\"model\":\"llamacpp/my-model\"}");

        String mockUrl = System.getProperty("mockServerUrl", System.getenv("MOCK_SERVER_URL")) + "/fixtures/local_provider_llamacpp";
        var client = LiterLlm.createClient("test-key", mockUrl, null, null, null);
        var result = client.chat(request);
assertEquals(1, result.choices().size(), "expected exactly 1 elements");assertEquals("Hi there! I'm running locally.", java.util.Optional.ofNullable(result.choices().get(0).message().content()).orElse("").trim());assertEquals("stop", java.util.Optional.ofNullable(result.choices().get(0).finishReason()).map(v -> v.getValue()).orElse("").trim());assertEquals("my-model", result.model().trim());
    }


    @Test
    void testLocalProviderOllama() throws Exception {
        // Ollama local provider routes requests via ollama/ model prefix with no auth
        var request = ChatCompletionRequest.fromJson("{\"messages\":[{\"content\":\"Hello\",\"role\":\"user\"}],\"model\":\"ollama/qwen2:0.5b\"}");

        String mockUrl = System.getProperty("mockServerUrl", System.getenv("MOCK_SERVER_URL")) + "/fixtures/local_provider_ollama";
        var client = LiterLlm.createClient("test-key", mockUrl, null, null, null);
        var result = client.chat(request);
assertEquals(1, result.choices().size(), "expected exactly 1 elements");assertEquals("Hello! How can I help you today?", java.util.Optional.ofNullable(result.choices().get(0).message().content()).orElse("").trim());assertEquals("stop", java.util.Optional.ofNullable(result.choices().get(0).finishReason()).map(v -> v.getValue()).orElse("").trim());assertEquals("qwen2:0.5b", result.model().trim());
    }


    @Test
    void testLocalProviderVllm() throws Exception {
        // vLLM local provider routes requests via vllm/ model prefix with no auth
        var request = ChatCompletionRequest.fromJson("{\"messages\":[{\"content\":\"Hello\",\"role\":\"user\"}],\"model\":\"vllm/meta-llama/Llama-3.2-1B\"}");

        String mockUrl = System.getProperty("mockServerUrl", System.getenv("MOCK_SERVER_URL")) + "/fixtures/local_provider_vllm";
        var client = LiterLlm.createClient("test-key", mockUrl, null, null, null);
        var result = client.chat(request);
assertEquals(1, result.choices().size(), "expected exactly 1 elements");assertEquals("Hello! How may I assist you?", java.util.Optional.ofNullable(result.choices().get(0).message().content()).orElse("").trim());assertEquals("stop", java.util.Optional.ofNullable(result.choices().get(0).finishReason()).map(v -> v.getValue()).orElse("").trim());assertEquals("meta-llama/Llama-3.2-1B", result.model().trim());
    }

}
