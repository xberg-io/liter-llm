// This file is auto-generated by alef — DO NOT EDIT.
// alef:hash:4babe65b472c1b2063adce07ed24c6c194ec1b84a3bea5021c0916724f18cd51
// To regenerate: alef generate
// To verify freshness: alef verify --exit-code
// Issues & docs: https://github.com/kreuzberg-dev/alef

package dev.kreuzberg.literllm.e2e;

import org.junit.jupiter.api.Test;
import static org.junit.jupiter.api.Assertions.*;
import dev.kreuzberg.literllm.LiterLlm;
import dev.kreuzberg.literllm.CreateSpeechRequest;
import java.util.Optional;

/** E2e tests for category: speech. */
class SpeechTest {

    @Test
    void testEdgeSpeechAllVoices() throws Exception {
        // Text-to-speech with specific voice selection
        var request = CreateSpeechRequest.fromJson("{\"input\":\"Hello world\",\"model\":\"tts-1\",\"voice\":\"nova\"}");

        String mockUrl = System.getProperty("mockServerUrl", System.getenv("MOCK_SERVER_URL")) + "/fixtures/edge_speech_all_voices";
        var client = LiterLlm.createClient("test-key", mockUrl, null, null, null);
        var result = client.speech(request);
        assertNotNull(result, "expected non-null byte[] response");
        assertTrue(result.length > 0, "expected non-empty value");

    }


    @Test
    void testEdgeSpeechLongInput() throws Exception {
        // Speech generation with a very long input text
        var request = CreateSpeechRequest.fromJson("{\"input\":\"This is a long input text. This is a long input text. This is a long input text. This is a long input text. This is a long input text. This is a long input text. This is a long input text. This is a long input text. This is a long input text. This is a long input text. This is a long input text. This is a long input text. This is a long input text. This is a long input text. This is a long input text. This is a long input text. This is a long input text. This is a long input text. This is a long input text. This is a long input text. This is a long input text. This is a long input text. This is a long input text. This is a long input text. This is a long input text. This is a long input text. This is a long input text. This is a long input text. This is a long input text. This is a long input text. This is a long input text. This is a long input text. This is a long input text. This is a long input text. This is a long input text. This is a long input text. This is a long input text. This is a long input text. This is a long input text. This is a long input text. This is a long input text. This is a long input text. This is a long input text. This is a long input text. This is a long input text. This is a long input text. This is a long input text. This is a long input text. This is a long input text. This is a long input text. End of input.\",\"model\":\"tts-1\",\"voice\":\"echo\"}");

        String mockUrl = System.getProperty("mockServerUrl", System.getenv("MOCK_SERVER_URL")) + "/fixtures/edge_speech_long_input";
        var client = LiterLlm.createClient("test-key", mockUrl, null, null, null);
        var result = client.speech(request);
        assertNotNull(result, "expected non-null byte[] response");
        assertTrue(result.length > 0, "expected non-empty value");

    }


    @Test
    void testErrorSpeechAuth401() throws Exception {
        // 401 Unauthorized for speech generation with invalid API key
        // Wrap setup_lines + call_expr inside the lambda so error fixtures
        // catch failures at *any* step — including `<Type>.fromJson(...)`
        // calls that throw on malformed JSON (e.g. error fixtures with an
        // invalid enum value like `"purpose":"invalid-purpose"`). Mirrors
        // the C# `Assert.ThrowsAnyAsync(() => client.X(Type.FromJson(...)))`
        // pattern.
        assertThrows(Exception.class, () -> {
        var request = CreateSpeechRequest.fromJson("{\"input\":\"Hello\",\"model\":\"tts-1\",\"voice\":\"alloy\"}");

            String mockUrl = System.getProperty("mockServerUrl", System.getenv("MOCK_SERVER_URL")) + "/fixtures/error_speech_auth_401";
            var client = LiterLlm.createClient("test-key", mockUrl, null, null, null);
            client.speech(request);
        });
    }


    @Test
    void testErrorSpeechBadModel() throws Exception {
        // 400 Bad Request for speech with unsupported model
        // Wrap setup_lines + call_expr inside the lambda so error fixtures
        // catch failures at *any* step — including `<Type>.fromJson(...)`
        // calls that throw on malformed JSON (e.g. error fixtures with an
        // invalid enum value like `"purpose":"invalid-purpose"`). Mirrors
        // the C# `Assert.ThrowsAnyAsync(() => client.X(Type.FromJson(...)))`
        // pattern.
        assertThrows(Exception.class, () -> {
        var request = CreateSpeechRequest.fromJson("{\"input\":\"Hello\",\"model\":\"tts-nonexistent\",\"voice\":\"alloy\"}");

            String mockUrl = System.getProperty("mockServerUrl", System.getenv("MOCK_SERVER_URL")) + "/fixtures/error_speech_bad_model";
            var client = LiterLlm.createClient("test-key", mockUrl, null, null, null);
            client.speech(request);
        });
    }


    @Test
    void testSmokeSpeechBasic() throws Exception {
        // Basic text-to-speech generation
        var request = CreateSpeechRequest.fromJson("{\"input\":\"Hello, world!\",\"model\":\"tts-1\",\"voice\":\"alloy\"}");

        String mockUrl = System.getProperty("mockServerUrl", System.getenv("MOCK_SERVER_URL")) + "/fixtures/smoke_speech_basic";
        var client = LiterLlm.createClient("test-key", mockUrl, null, null, null);
        var result = client.speech(request);
        assertNotNull(result, "expected non-null byte[] response");
        assertTrue(result.length > 0, "expected non-empty value");

    }


    @Test
    void testSmokeSpeechMp3Format() throws Exception {
        // Text-to-speech with explicit MP3 response format
        var request = CreateSpeechRequest.fromJson("{\"input\":\"The quick brown fox jumps over the lazy dog.\",\"model\":\"tts-1-hd\",\"response_format\":\"mp3\",\"speed\":1.0,\"voice\":\"nova\"}");

        String mockUrl = System.getProperty("mockServerUrl", System.getenv("MOCK_SERVER_URL")) + "/fixtures/smoke_speech_mp3_format";
        var client = LiterLlm.createClient("test-key", mockUrl, null, null, null);
        var result = client.speech(request);
        assertNotNull(result, "expected non-null byte[] response");
        assertTrue(result.length > 0, "expected non-empty value");

    }

}
