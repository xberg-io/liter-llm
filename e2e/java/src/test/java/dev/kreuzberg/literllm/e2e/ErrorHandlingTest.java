// This file is auto-generated by alef — DO NOT EDIT.
// alef:hash:81fa95d7b006b3c09aea2dfdf2b15a6d1892e6e7dd65234dd0b6e5a86828e092
// To regenerate: alef generate
// To verify freshness: alef verify --exit-code
// Issues & docs: https://github.com/kreuzberg-dev/alef

package dev.kreuzberg.literllm.e2e;

import org.junit.jupiter.api.Test;
import static org.junit.jupiter.api.Assertions.*;
import dev.kreuzberg.literllm.LiterLlm;
import dev.kreuzberg.literllm.ChatCompletionRequest;
import java.util.Optional;

/** E2e tests for category: error-handling. */
class ErrorHandlingTest {

    @Test
    void testAnthropicErrorAuth() throws Exception {
        // 401 Authentication error returned by the Anthropic API when the API key is invalid
        var request = ChatCompletionRequest.fromJson("{\"messages\":[{\"content\":\"Hello\",\"role\":\"user\"}],\"model\":\"anthropic/claude-3-5-sonnet-20241022\"}");

        String mockUrl = System.getenv("MOCK_SERVER_URL") + "/fixtures/anthropic_error_auth";
        var client = LiterLlm.createClient("test-key", mockUrl, null, null, null);
        assertThrows(Exception.class, () -> client.chat(request));
    }


    @Test
    void testAuth401() throws Exception {
        // 401 Unauthorized error when API key is invalid or missing
        var request = ChatCompletionRequest.fromJson("{\"messages\":[{\"content\":\"Hello\",\"role\":\"user\"}],\"model\":\"gpt-4\"}");

        String mockUrl = System.getenv("MOCK_SERVER_URL") + "/fixtures/auth_401";
        var client = LiterLlm.createClient("test-key", mockUrl, null, null, null);
        assertThrows(Exception.class, () -> client.chat(request));
    }


    @Test
    void testAzureErrorAuth() throws Exception {
        // Azure OpenAI returns a 401 Unauthorized error when the API key is missing or invalid — uses Azure's error envelope shape with code AccessDenied
        var request = ChatCompletionRequest.fromJson("{\"messages\":[{\"content\":\"Hello\",\"role\":\"user\"}],\"model\":\"azure/gpt-4\"}");

        String mockUrl = System.getenv("MOCK_SERVER_URL") + "/fixtures/azure_error_auth";
        var client = LiterLlm.createClient("test-key", mockUrl, null, null, null);
        assertThrows(Exception.class, () -> client.chat(request));
    }


    @Test
    void testBadRequest400() throws Exception {
        // 400 Bad Request error when a parameter value is invalid
        var request = ChatCompletionRequest.fromJson("{\"messages\":[{\"content\":\"Hello\",\"role\":\"user\"}],\"model\":\"gpt-4\",\"temperature\":5.0}");

        String mockUrl = System.getenv("MOCK_SERVER_URL") + "/fixtures/bad_request_400";
        var client = LiterLlm.createClient("test-key", mockUrl, null, null, null);
        assertThrows(Exception.class, () -> client.chat(request));
    }


    @Test
    void testBedrockErrorAuth() throws Exception {
        // AWS Bedrock returns 403 Forbidden (not 401) when credentials are missing, expired, or the IAM role lacks bedrock:InvokeModel permission — verifies the error is mapped to Authentication
        var request = ChatCompletionRequest.fromJson("{\"messages\":[{\"content\":\"Hello\",\"role\":\"user\"}],\"model\":\"bedrock/anthropic.claude-3-sonnet-20240229-v1:0\"}");

        String mockUrl = System.getenv("MOCK_SERVER_URL") + "/fixtures/bedrock_error_auth";
        var client = LiterLlm.createClient("test-key", mockUrl, null, null, null);
        assertThrows(Exception.class, () -> client.chat(request));
    }


    @Test
    void testContentPolicyViolation() throws Exception {
        // 400 error when a request is rejected due to content policy
        var request = ChatCompletionRequest.fromJson("{\"messages\":[{\"content\":\"Generate harmful content\",\"role\":\"user\"}],\"model\":\"gpt-4\"}");

        String mockUrl = System.getenv("MOCK_SERVER_URL") + "/fixtures/content_policy_violation";
        var client = LiterLlm.createClient("test-key", mockUrl, null, null, null);
        assertThrows(Exception.class, () -> client.chat(request));
    }


    @Test
    void testContextWindowExceeded() throws Exception {
        // 400 error when the prompt exceeds the model's maximum context length
        var request = ChatCompletionRequest.fromJson("{\"messages\":[{\"content\":\"Very long prompt that exceeds the context window...\",\"role\":\"user\"}],\"model\":\"gpt-4\"}");

        String mockUrl = System.getenv("MOCK_SERVER_URL") + "/fixtures/context_window_exceeded";
        var client = LiterLlm.createClient("test-key", mockUrl, null, null, null);
        assertThrows(Exception.class, () -> client.chat(request));
    }


    @Test
    void testEmptyResponseBody() throws Exception {
        // 200 OK response with an empty JSON object body, missing required fields
        var request = ChatCompletionRequest.fromJson("{\"messages\":[{\"content\":\"Hello\",\"role\":\"user\"}],\"model\":\"gpt-4\"}");

        String mockUrl = System.getenv("MOCK_SERVER_URL") + "/fixtures/empty_response_body";
        var client = LiterLlm.createClient("test-key", mockUrl, null, null, null);
        assertThrows(Exception.class, () -> client.chat(request));
    }


    @Test
    void testForbidden403() throws Exception {
        // 403 Forbidden error when the API key does not have access to the requested resource
        var request = ChatCompletionRequest.fromJson("{\"messages\":[{\"content\":\"Hello\",\"role\":\"user\"}],\"model\":\"gpt-4\"}");

        String mockUrl = System.getenv("MOCK_SERVER_URL") + "/fixtures/forbidden_403";
        var client = LiterLlm.createClient("test-key", mockUrl, null, null, null);
        assertThrows(Exception.class, () -> client.chat(request));
    }


    @Test
    void testGatewayTimeout504() throws Exception {
        // 504 Gateway Timeout error when the upstream service times out
        var request = ChatCompletionRequest.fromJson("{\"messages\":[{\"content\":\"Hello\",\"role\":\"user\"}],\"model\":\"gpt-4\"}");

        String mockUrl = System.getenv("MOCK_SERVER_URL") + "/fixtures/gateway_timeout_504";
        var client = LiterLlm.createClient("test-key", mockUrl, null, null, null);
        assertThrows(Exception.class, () -> client.chat(request));
    }


    @Test
    void testGithubCopilotErrorAuth() throws Exception {
        // 401 Authentication error returned by the GitHub Copilot API when the token is invalid or expired
        var request = ChatCompletionRequest.fromJson("{\"messages\":[{\"content\":\"Hello\",\"role\":\"user\"}],\"model\":\"github_copilot/gpt-4o\"}");

        String mockUrl = System.getenv("MOCK_SERVER_URL") + "/fixtures/github_copilot_error_auth";
        var client = LiterLlm.createClient("test-key", mockUrl, null, null, null);
        assertThrows(Exception.class, () -> client.chat(request));
    }


    @Test
    void testNotFound404() throws Exception {
        // 404 Not Found error when requesting a model that does not exist
        var request = ChatCompletionRequest.fromJson("{\"messages\":[{\"content\":\"Hello\",\"role\":\"user\"}],\"model\":\"gpt-99\"}");

        String mockUrl = System.getenv("MOCK_SERVER_URL") + "/fixtures/not_found_404";
        var client = LiterLlm.createClient("test-key", mockUrl, null, null, null);
        assertThrows(Exception.class, () -> client.chat(request));
    }


    @Test
    void testRateLimit429() throws Exception {
        // 429 Too Many Requests error when the rate limit is exceeded
        var request = ChatCompletionRequest.fromJson("{\"messages\":[{\"content\":\"Hello\",\"role\":\"user\"}],\"model\":\"gpt-4\"}");

        String mockUrl = System.getenv("MOCK_SERVER_URL") + "/fixtures/rate_limit_429";
        var client = LiterLlm.createClient("test-key", mockUrl, null, null, null);
        assertThrows(Exception.class, () -> client.chat(request));
    }


    @Test
    void testServerError500() throws Exception {
        // 500 Internal Server Error from the upstream API
        var request = ChatCompletionRequest.fromJson("{\"messages\":[{\"content\":\"Hello\",\"role\":\"user\"}],\"model\":\"gpt-4\"}");

        String mockUrl = System.getenv("MOCK_SERVER_URL") + "/fixtures/server_error_500";
        var client = LiterLlm.createClient("test-key", mockUrl, null, null, null);
        assertThrows(Exception.class, () -> client.chat(request));
    }


    @Test
    void testServiceUnavailable502() throws Exception {
        // 502 Bad Gateway error when the upstream service is unavailable
        var request = ChatCompletionRequest.fromJson("{\"messages\":[{\"content\":\"Hello\",\"role\":\"user\"}],\"model\":\"gpt-4\"}");

        String mockUrl = System.getenv("MOCK_SERVER_URL") + "/fixtures/service_unavailable_502";
        var client = LiterLlm.createClient("test-key", mockUrl, null, null, null);
        assertThrows(Exception.class, () -> client.chat(request));
    }


    @Test
    void testTimeoutError() throws Exception {
        // 408 Request Timeout error when the API request takes too long to complete
        var request = ChatCompletionRequest.fromJson("{\"messages\":[{\"content\":\"Hello\",\"role\":\"user\"}],\"model\":\"gpt-4\"}");

        String mockUrl = System.getenv("MOCK_SERVER_URL") + "/fixtures/timeout_error";
        var client = LiterLlm.createClient("test-key", mockUrl, null, null, null);
        assertThrows(Exception.class, () -> client.chat(request));
    }


    @Test
    void testVertexErrorAuth() throws Exception {
        // Google Vertex AI returns 401 Unauthorized when the OAuth2 token is missing, expired, or the service account lacks aiplatform.endpoints.predict permission — verifies the error is mapped to Authentication
        var request = ChatCompletionRequest.fromJson("{\"messages\":[{\"content\":\"Hello\",\"role\":\"user\"}],\"model\":\"vertex_ai/gemini-2.0-flash\"}");

        String mockUrl = System.getenv("MOCK_SERVER_URL") + "/fixtures/vertex_error_auth";
        var client = LiterLlm.createClient("test-key", mockUrl, null, null, null);
        assertThrows(Exception.class, () -> client.chat(request));
    }

}
