// This file is auto-generated by alef — DO NOT EDIT.
// alef:hash:481ae3ed0647e89e827a8693e4b67ea6c5e982f29c16cc83f4950a957978e1a5
// To regenerate: alef generate
// To verify freshness: alef verify --exit-code
// Issues & docs: https://github.com/kreuzberg-dev/alef

package dev.kreuzberg.literllm.e2e;

import org.junit.jupiter.api.Test;
import static org.junit.jupiter.api.Assertions.*;
import dev.kreuzberg.literllm.LiterLlm;
import dev.kreuzberg.literllm.CreateImageRequest;
import java.util.Optional;

/** E2e tests for category: image-generate. */
class ImageGenerateTest {

    @Test
    void testEdgeImageB64Response() throws Exception {
        // Image generation returning base64-encoded data instead of URL
        var request = CreateImageRequest.fromJson("{\"model\":\"dall-e-3\",\"n\":1,\"prompt\":\"A blue circle\",\"response_format\":\"b64_json\",\"size\":\"1024x1024\"}");

        String mockUrl = System.getProperty("mockServerUrl", System.getenv("MOCK_SERVER_URL")) + "/fixtures/edge_image_b64_response";
        var client = LiterLlm.createClient("test-key", mockUrl, null, null, null);
        var result = client.imageGenerate(request);
assertEquals(1, result.data().size(), "expected exactly 1 elements");assertFalse(java.util.Optional.ofNullable(result.data().get(0).b64Json()).isEmpty(), "expected non-empty value");
    }


    @Test
    void testEdgeImageEmptyPrompt() throws Exception {
        // Image generation with an empty prompt returns 400
        var request = CreateImageRequest.fromJson("{\"model\":\"dall-e-3\",\"n\":1,\"prompt\":\"\",\"size\":\"1024x1024\"}");

        String mockUrl = System.getProperty("mockServerUrl", System.getenv("MOCK_SERVER_URL")) + "/fixtures/edge_image_empty_prompt";
        var client = LiterLlm.createClient("test-key", mockUrl, null, null, null);
        assertThrows(Exception.class, () -> client.imageGenerate(request));
    }


    @Test
    void testEdgeImageMultipleN() throws Exception {
        // Image generation requesting multiple images with n=3
        var request = CreateImageRequest.fromJson("{\"model\":\"dall-e-3\",\"n\":3,\"prompt\":\"A cat\"}");

        String mockUrl = System.getProperty("mockServerUrl", System.getenv("MOCK_SERVER_URL")) + "/fixtures/edge_image_multiple_n";
        var client = LiterLlm.createClient("test-key", mockUrl, null, null, null);
        var result = client.imageGenerate(request);
assertEquals(3, result.data().size(), "expected exactly 3 elements");assertFalse(java.util.Optional.ofNullable(result.data().get(0).url()).isEmpty(), "expected non-empty value");assertFalse(java.util.Optional.ofNullable(result.data().get(1).url()).isEmpty(), "expected non-empty value");assertFalse(java.util.Optional.ofNullable(result.data().get(2).url()).isEmpty(), "expected non-empty value");
    }


    @Test
    void testErrorImageAuth401() throws Exception {
        // 401 Unauthorized when generating images with invalid API key
        var request = CreateImageRequest.fromJson("{\"model\":\"dall-e-3\",\"n\":1,\"prompt\":\"A cat\",\"size\":\"1024x1024\"}");

        String mockUrl = System.getProperty("mockServerUrl", System.getenv("MOCK_SERVER_URL")) + "/fixtures/error_image_auth_401";
        var client = LiterLlm.createClient("test-key", mockUrl, null, null, null);
        assertThrows(Exception.class, () -> client.imageGenerate(request));
    }


    @Test
    void testErrorImageBadRequest() throws Exception {
        // 400 Bad Request when image generation parameters are invalid
        var request = CreateImageRequest.fromJson("{\"model\":\"dall-e-3\",\"n\":1,\"prompt\":\"A cat\",\"size\":\"9999x9999\"}");

        String mockUrl = System.getProperty("mockServerUrl", System.getenv("MOCK_SERVER_URL")) + "/fixtures/error_image_bad_request";
        var client = LiterLlm.createClient("test-key", mockUrl, null, null, null);
        assertThrows(Exception.class, () -> client.imageGenerate(request));
    }


    @Test
    void testErrorImageRateLimit() throws Exception {
        // 429 Rate limit exceeded for image generation
        var request = CreateImageRequest.fromJson("{\"model\":\"dall-e-3\",\"n\":1,\"prompt\":\"A cat\",\"size\":\"1024x1024\"}");

        String mockUrl = System.getProperty("mockServerUrl", System.getenv("MOCK_SERVER_URL")) + "/fixtures/error_image_rate_limit";
        var client = LiterLlm.createClient("test-key", mockUrl, null, null, null);
        assertThrows(Exception.class, () -> client.imageGenerate(request));
    }


    @Test
    void testSmokeImageBasic() throws Exception {
        // Basic image generation with a text prompt
        var request = CreateImageRequest.fromJson("{\"model\":\"dall-e-3\",\"n\":1,\"prompt\":\"A white cat sitting on a windowsill\",\"size\":\"1024x1024\"}");

        String mockUrl = System.getProperty("mockServerUrl", System.getenv("MOCK_SERVER_URL")) + "/fixtures/smoke_image_basic";
        var client = LiterLlm.createClient("test-key", mockUrl, null, null, null);
        var result = client.imageGenerate(request);
assertEquals(1, result.data().size(), "expected exactly 1 elements");assertFalse(java.util.Optional.ofNullable(result.data().get(0).url()).isEmpty(), "expected non-empty value");
    }


    @Test
    void testSmokeImageMultiple() throws Exception {
        // Image generation requesting multiple images
        var request = CreateImageRequest.fromJson("{\"model\":\"dall-e-2\",\"n\":3,\"prompt\":\"A red bicycle\",\"size\":\"256x256\"}");

        String mockUrl = System.getProperty("mockServerUrl", System.getenv("MOCK_SERVER_URL")) + "/fixtures/smoke_image_multiple";
        var client = LiterLlm.createClient("test-key", mockUrl, null, null, null);
        var result = client.imageGenerate(request);
assertEquals(3, result.data().size(), "expected exactly 3 elements");assertFalse(java.util.Optional.ofNullable(result.data().get(0).url()).isEmpty(), "expected non-empty value");
    }


    @Test
    void testSmokeImageWithSize() throws Exception {
        // Image generation with explicit size parameter
        var request = CreateImageRequest.fromJson("{\"model\":\"dall-e-3\",\"n\":1,\"prompt\":\"A sunset over mountains\",\"size\":\"1792x1024\"}");

        String mockUrl = System.getProperty("mockServerUrl", System.getenv("MOCK_SERVER_URL")) + "/fixtures/smoke_image_with_size";
        var client = LiterLlm.createClient("test-key", mockUrl, null, null, null);
        var result = client.imageGenerate(request);
assertEquals(1, result.data().size(), "expected exactly 1 elements");assertFalse(java.util.Optional.ofNullable(result.data().get(0).url()).isEmpty(), "expected non-empty value");
    }

}
