// This file is auto-generated by alef — DO NOT EDIT.
// alef:hash:cc8dd0c6361f7c56cc13786f00d2cdbb9827d52d95e1a93f4d4105f3fe0d3e19
// To regenerate: alef generate
// To verify freshness: alef verify --exit-code
// Issues & docs: https://github.com/kreuzberg-dev/alef

package dev.kreuzberg.literllm.e2e;

import org.junit.jupiter.api.Test;
import static org.junit.jupiter.api.Assertions.*;
import dev.kreuzberg.literllm.LiterLlm;

/** E2e tests for category: image-generate. */
class ImageGenerateTest {

    @Test
    void testEdgeImageB64Response() throws Exception {
        // Image generation returning base64-encoded data instead of URL
        String mockUrl = System.getenv("MOCK_SERVER_URL") + "/fixtures/edge_image_b64_response";
        var client = LiterLlm.createClient("test-key", mockUrl, null, null, null);
        var result = client.imageGenerate("{\"model\":\"dall-e-3\",\"n\":1,\"prompt\":\"A blue circle\",\"response_format\":\"b64_json\",\"size\":\"1024x1024\"}");
assertEquals(1, result.data().size(), "expected exactly 1 elements");assertFalse(java.util.Optional.ofNullable(result.data().get(0).b64Json()).isEmpty(), "expected non-empty value");
    }


    @Test
    void testEdgeImageEmptyPrompt() throws Exception {
        // Image generation with an empty prompt returns 400
        String mockUrl = System.getenv("MOCK_SERVER_URL") + "/fixtures/edge_image_empty_prompt";
        var client = LiterLlm.createClient("test-key", mockUrl, null, null, null);
        assertThrows(Exception.class, () -> client.imageGenerate("{\"model\":\"dall-e-3\",\"n\":1,\"prompt\":\"\",\"size\":\"1024x1024\"}"));
    }


    @Test
    void testEdgeImageMultipleN() throws Exception {
        // Image generation requesting multiple images with n=3
        String mockUrl = System.getenv("MOCK_SERVER_URL") + "/fixtures/edge_image_multiple_n";
        var client = LiterLlm.createClient("test-key", mockUrl, null, null, null);
        var result = client.imageGenerate("{\"model\":\"dall-e-3\",\"n\":3,\"prompt\":\"A cat\"}");
assertEquals(3, result.data().size(), "expected exactly 3 elements");assertFalse(java.util.Optional.ofNullable(result.data().get(0).url()).isEmpty(), "expected non-empty value");assertFalse(java.util.Optional.ofNullable(result.data().get(1).url()).isEmpty(), "expected non-empty value");assertFalse(java.util.Optional.ofNullable(result.data().get(2).url()).isEmpty(), "expected non-empty value");
    }


    @Test
    void testErrorImageAuth401() throws Exception {
        // 401 Unauthorized when generating images with invalid API key
        String mockUrl = System.getenv("MOCK_SERVER_URL") + "/fixtures/error_image_auth_401";
        var client = LiterLlm.createClient("test-key", mockUrl, null, null, null);
        assertThrows(Exception.class, () -> client.imageGenerate("{\"model\":\"dall-e-3\",\"n\":1,\"prompt\":\"A cat\",\"size\":\"1024x1024\"}"));
    }


    @Test
    void testErrorImageBadRequest() throws Exception {
        // 400 Bad Request when image generation parameters are invalid
        String mockUrl = System.getenv("MOCK_SERVER_URL") + "/fixtures/error_image_bad_request";
        var client = LiterLlm.createClient("test-key", mockUrl, null, null, null);
        assertThrows(Exception.class, () -> client.imageGenerate("{\"model\":\"dall-e-3\",\"n\":1,\"prompt\":\"A cat\",\"size\":\"9999x9999\"}"));
    }


    @Test
    void testErrorImageRateLimit() throws Exception {
        // 429 Rate limit exceeded for image generation
        String mockUrl = System.getenv("MOCK_SERVER_URL") + "/fixtures/error_image_rate_limit";
        var client = LiterLlm.createClient("test-key", mockUrl, null, null, null);
        assertThrows(Exception.class, () -> client.imageGenerate("{\"model\":\"dall-e-3\",\"n\":1,\"prompt\":\"A cat\",\"size\":\"1024x1024\"}"));
    }


    @Test
    void testSmokeImageBasic() throws Exception {
        // Basic image generation with a text prompt
        String mockUrl = System.getenv("MOCK_SERVER_URL") + "/fixtures/smoke_image_basic";
        var client = LiterLlm.createClient("test-key", mockUrl, null, null, null);
        var result = client.imageGenerate("{\"model\":\"dall-e-3\",\"n\":1,\"prompt\":\"A white cat sitting on a windowsill\",\"size\":\"1024x1024\"}");
assertEquals(1, result.data().size(), "expected exactly 1 elements");assertFalse(java.util.Optional.ofNullable(result.data().get(0).url()).isEmpty(), "expected non-empty value");
    }


    @Test
    void testSmokeImageMultiple() throws Exception {
        // Image generation requesting multiple images
        String mockUrl = System.getenv("MOCK_SERVER_URL") + "/fixtures/smoke_image_multiple";
        var client = LiterLlm.createClient("test-key", mockUrl, null, null, null);
        var result = client.imageGenerate("{\"model\":\"dall-e-2\",\"n\":3,\"prompt\":\"A red bicycle\",\"size\":\"256x256\"}");
assertEquals(3, result.data().size(), "expected exactly 3 elements");assertFalse(java.util.Optional.ofNullable(result.data().get(0).url()).isEmpty(), "expected non-empty value");
    }


    @Test
    void testSmokeImageWithSize() throws Exception {
        // Image generation with explicit size parameter
        String mockUrl = System.getenv("MOCK_SERVER_URL") + "/fixtures/smoke_image_with_size";
        var client = LiterLlm.createClient("test-key", mockUrl, null, null, null);
        var result = client.imageGenerate("{\"model\":\"dall-e-3\",\"n\":1,\"prompt\":\"A sunset over mountains\",\"size\":\"1792x1024\"}");
assertEquals(1, result.data().size(), "expected exactly 1 elements");assertFalse(java.util.Optional.ofNullable(result.data().get(0).url()).isEmpty(), "expected non-empty value");
    }

}
