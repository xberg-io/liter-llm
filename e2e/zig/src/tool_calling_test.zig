// This file is auto-generated by alef — DO NOT EDIT.
// alef:hash:ddf3216f62b32e966370f6d7f47b5ba1dd0c041a8456a6286e2f36c25533b690
// To regenerate: alef generate
// To verify freshness: alef verify --exit-code
// Issues & docs: https://github.com/kreuzberg-dev/alef
const std = @import("std");
const testing = std.testing;
const liter_llm = @import("liter_llm");

// E2e tests for category: tool-calling

test "anthropic_tool_calling" {
    // Chat request to Anthropic provider with a tool definition; assistant responds with a tool call
    const _mock_url = try std.fmt.allocPrintSentinel(std.heap.c_allocator, "{s}/fixtures/anthropic_tool_calling", .{if (std.c.getenv("MOCK_SERVER_URL")) |v| std.mem.span(v) else "http://localhost:8080"}, 0);
    defer std.heap.c_allocator.free(_mock_url);
    var _client = try liter_llm.create_client("test-key", _mock_url, null, null, null);
    defer _client.free();
    // Note: async functions not yet fully supported; treating as sync
    const result = try _client.chat("{}");
    try testing.expectEqual(1, result.choices.len);
    try testing.expect(result.choices[0].message.tool_calls.len > 0);
    try testing.expectEqual("get_weather", result.choices[0].message.tool_calls[0].function.name);
    try testing.expectEqual("tool_calls", result.choices[0].finish_reason);
}

test "single_tool_call" {
    // Chat request with a tool definition; assistant responds with a tool call
    const _mock_url = try std.fmt.allocPrintSentinel(std.heap.c_allocator, "{s}/fixtures/single_tool_call", .{if (std.c.getenv("MOCK_SERVER_URL")) |v| std.mem.span(v) else "http://localhost:8080"}, 0);
    defer std.heap.c_allocator.free(_mock_url);
    var _client = try liter_llm.create_client("test-key", _mock_url, null, null, null);
    defer _client.free();
    // Note: async functions not yet fully supported; treating as sync
    const result = try _client.chat("{}");
    try testing.expectEqual(1, result.choices.len);
    try testing.expect(result.choices[0].message.tool_calls.len > 0);
    try testing.expectEqual("get_weather", result.choices[0].message.tool_calls[0].function.name);
    try testing.expectEqual("tool_calls", result.choices[0].finish_reason);
}
