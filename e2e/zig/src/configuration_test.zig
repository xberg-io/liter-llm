// This file is auto-generated by alef — DO NOT EDIT.
// alef:hash:a4da6d284e8869b6becb9fdab90d049e07d2e4f0e61d3d38b3faa7868a41fc54
// To regenerate: alef generate
// To verify freshness: alef verify --exit-code
// Issues & docs: https://github.com/kreuzberg-dev/alef
const std = @import("std");
const testing = std.testing;
const liter_llm = @import("liter_llm");

// E2e tests for category: configuration

test "custom_base_url" {
    // Client configured with a custom base URL routes all requests to that endpoint
    const _mock_url = try std.fmt.allocPrintSentinel(std.heap.c_allocator, "{s}/fixtures/custom_base_url", .{if (std.c.getenv("MOCK_SERVER_URL")) |v| std.mem.span(v) else "http://localhost:8080"}, 0);
    defer std.heap.c_allocator.free(_mock_url);
    var _client = try liter_llm.create_client("test-key", _mock_url, null, null, null);
    defer _client.free();
    // Note: async functions not yet fully supported; treating as sync
    const result = try _client.chat("{}");
    try testing.expectEqual(1, result.choices.len);
    try testing.expectEqual("Hi there!", result.choices[0].message.content);
    try testing.expectEqual("stop", result.choices[0].finish_reason);
    try testing.expectEqual("local-model", result.model);
}

test "extra_headers" {
    // Client configured with extra custom headers successfully completes a chat request
    const _mock_url = try std.fmt.allocPrintSentinel(std.heap.c_allocator, "{s}/fixtures/extra_headers", .{if (std.c.getenv("MOCK_SERVER_URL")) |v| std.mem.span(v) else "http://localhost:8080"}, 0);
    defer std.heap.c_allocator.free(_mock_url);
    var _client = try liter_llm.create_client("test-key", _mock_url, null, null, null);
    defer _client.free();
    // Note: async functions not yet fully supported; treating as sync
    const result = try _client.chat("{}");
    try testing.expectEqual(1, result.choices.len);
    try testing.expectEqual("stop", result.choices[0].finish_reason);
}

test "local_provider_llamacpp" {
    // llamacpp local provider routes requests via llamacpp/ model prefix with no auth
    const _mock_url = try std.fmt.allocPrintSentinel(std.heap.c_allocator, "{s}/fixtures/local_provider_llamacpp", .{if (std.c.getenv("MOCK_SERVER_URL")) |v| std.mem.span(v) else "http://localhost:8080"}, 0);
    defer std.heap.c_allocator.free(_mock_url);
    var _client = try liter_llm.create_client("test-key", _mock_url, null, null, null);
    defer _client.free();
    // Note: async functions not yet fully supported; treating as sync
    const result = try _client.chat("{}");
    try testing.expectEqual(1, result.choices.len);
    try testing.expectEqual("Hi there! I'm running locally.", result.choices[0].message.content);
    try testing.expectEqual("stop", result.choices[0].finish_reason);
    try testing.expectEqual("my-model", result.model);
}

test "local_provider_ollama" {
    // Ollama local provider routes requests via ollama/ model prefix with no auth
    const _mock_url = try std.fmt.allocPrintSentinel(std.heap.c_allocator, "{s}/fixtures/local_provider_ollama", .{if (std.c.getenv("MOCK_SERVER_URL")) |v| std.mem.span(v) else "http://localhost:8080"}, 0);
    defer std.heap.c_allocator.free(_mock_url);
    var _client = try liter_llm.create_client("test-key", _mock_url, null, null, null);
    defer _client.free();
    // Note: async functions not yet fully supported; treating as sync
    const result = try _client.chat("{}");
    try testing.expectEqual(1, result.choices.len);
    try testing.expectEqual("Hello! How can I help you today?", result.choices[0].message.content);
    try testing.expectEqual("stop", result.choices[0].finish_reason);
    try testing.expectEqual("qwen2:0.5b", result.model);
}

test "local_provider_vllm" {
    // vLLM local provider routes requests via vllm/ model prefix with no auth
    const _mock_url = try std.fmt.allocPrintSentinel(std.heap.c_allocator, "{s}/fixtures/local_provider_vllm", .{if (std.c.getenv("MOCK_SERVER_URL")) |v| std.mem.span(v) else "http://localhost:8080"}, 0);
    defer std.heap.c_allocator.free(_mock_url);
    var _client = try liter_llm.create_client("test-key", _mock_url, null, null, null);
    defer _client.free();
    // Note: async functions not yet fully supported; treating as sync
    const result = try _client.chat("{}");
    try testing.expectEqual(1, result.choices.len);
    try testing.expectEqual("Hello! How may I assist you?", result.choices[0].message.content);
    try testing.expectEqual("stop", result.choices[0].finish_reason);
    try testing.expectEqual("meta-llama/Llama-3.2-1B", result.model);
}
