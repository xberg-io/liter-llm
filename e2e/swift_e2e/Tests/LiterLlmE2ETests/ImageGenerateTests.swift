// This file is auto-generated by alef — DO NOT EDIT.
// alef:hash:47b1244d466ac644ad1074110a4583de7d15add4d2b891f6b50230ce94c3f61f
// To regenerate: alef generate
// To verify freshness: alef verify --exit-code
// Issues & docs: https://github.com/kreuzberg-dev/alef
import XCTest
import Foundation
import LiterLlm
import RustBridge

/// E2e tests for category: image-generate.
final class ImageGenerateTests: XCTestCase {
    func testEdgeImageB64Response() async throws {
        // Image generation returning base64-encoded data instead of URL
        let _request = try createImageRequestFromJson("{\"model\":\"dall-e-3\",\"n\":1,\"prompt\":\"A blue circle\",\"response_format\":\"b64_json\",\"size\":\"1024x1024\"}")
        let _client = try DefaultClient(apiKey: "test-key", baseUrl: ProcessInfo.processInfo.environment["MOCK_SERVER_URL"]! + "/fixtures/edge_image_b64_response")
        let result = try await _client.imageGenerate(_request)
        XCTAssertEqual(result.data().count, 1)
        let _vec_data_not_empty_500eca45 = result.data()
        XCTAssertNotNil(_vec_data_not_empty_500eca45[0].b64_json(), "expected non-nil value")
    }

    func testEdgeImageEmptyPrompt() async throws {
        // Image generation with an empty prompt returns 400
        do {
            let _request = try createImageRequestFromJson("{\"model\":\"dall-e-3\",\"n\":1,\"prompt\":\"\",\"size\":\"1024x1024\"}")
            let _client = try DefaultClient(apiKey: "test-key", baseUrl: ProcessInfo.processInfo.environment["MOCK_SERVER_URL"]! + "/fixtures/edge_image_empty_prompt")
            _ = try await _client.imageGenerate(_request)
            XCTFail("expected to throw")
        } catch {
            // success
        }
    }

    func testEdgeImageMultipleN() async throws {
        // Image generation requesting multiple images with n=3
        let _request = try createImageRequestFromJson("{\"model\":\"dall-e-3\",\"n\":3,\"prompt\":\"A cat\"}")
        let _client = try DefaultClient(apiKey: "test-key", baseUrl: ProcessInfo.processInfo.environment["MOCK_SERVER_URL"]! + "/fixtures/edge_image_multiple_n")
        let result = try await _client.imageGenerate(_request)
        XCTAssertEqual(result.data().count, 3)
        let _vec_data_not_empty_5b8bed51 = result.data()
        XCTAssertNotNil(_vec_data_not_empty_5b8bed51[0].url(), "expected non-nil value")
        let _vec_data_not_empty_38f0d849 = result.data()
        XCTAssertNotNil(_vec_data_not_empty_38f0d849[1].url(), "expected non-nil value")
        let _vec_data_not_empty_13db5fd7 = result.data()
        XCTAssertNotNil(_vec_data_not_empty_13db5fd7[2].url(), "expected non-nil value")
    }

    func testErrorImageAuth401() async throws {
        // 401 Unauthorized when generating images with invalid API key
        do {
            let _request = try createImageRequestFromJson("{\"model\":\"dall-e-3\",\"n\":1,\"prompt\":\"A cat\",\"size\":\"1024x1024\"}")
            let _client = try DefaultClient(apiKey: "test-key", baseUrl: ProcessInfo.processInfo.environment["MOCK_SERVER_URL"]! + "/fixtures/error_image_auth_401")
            _ = try await _client.imageGenerate(_request)
            XCTFail("expected to throw")
        } catch {
            // success
        }
    }

    func testErrorImageBadRequest() async throws {
        // 400 Bad Request when image generation parameters are invalid
        do {
            let _request = try createImageRequestFromJson("{\"model\":\"dall-e-3\",\"n\":1,\"prompt\":\"A cat\",\"size\":\"9999x9999\"}")
            let _client = try DefaultClient(apiKey: "test-key", baseUrl: ProcessInfo.processInfo.environment["MOCK_SERVER_URL"]! + "/fixtures/error_image_bad_request")
            _ = try await _client.imageGenerate(_request)
            XCTFail("expected to throw")
        } catch {
            // success
        }
    }

    func testErrorImageRateLimit() async throws {
        // 429 Rate limit exceeded for image generation
        do {
            let _request = try createImageRequestFromJson("{\"model\":\"dall-e-3\",\"n\":1,\"prompt\":\"A cat\",\"size\":\"1024x1024\"}")
            let _client = try DefaultClient(apiKey: "test-key", baseUrl: ProcessInfo.processInfo.environment["MOCK_SERVER_URL"]! + "/fixtures/error_image_rate_limit")
            _ = try await _client.imageGenerate(_request)
            XCTFail("expected to throw")
        } catch {
            // success
        }
    }

    func testSmokeImageBasic() async throws {
        // Basic image generation with a text prompt
        let _request = try createImageRequestFromJson("{\"model\":\"dall-e-3\",\"n\":1,\"prompt\":\"A white cat sitting on a windowsill\",\"size\":\"1024x1024\"}")
        let _client = try DefaultClient(apiKey: "test-key", baseUrl: ProcessInfo.processInfo.environment["MOCK_SERVER_URL"]! + "/fixtures/smoke_image_basic")
        let result = try await _client.imageGenerate(_request)
        XCTAssertEqual(result.data().count, 1)
        let _vec_data_not_empty_5b8bed51 = result.data()
        XCTAssertNotNil(_vec_data_not_empty_5b8bed51[0].url(), "expected non-nil value")
    }

    func testSmokeImageMultiple() async throws {
        // Image generation requesting multiple images
        let _request = try createImageRequestFromJson("{\"model\":\"dall-e-2\",\"n\":3,\"prompt\":\"A red bicycle\",\"size\":\"256x256\"}")
        let _client = try DefaultClient(apiKey: "test-key", baseUrl: ProcessInfo.processInfo.environment["MOCK_SERVER_URL"]! + "/fixtures/smoke_image_multiple")
        let result = try await _client.imageGenerate(_request)
        XCTAssertEqual(result.data().count, 3)
        let _vec_data_not_empty_5b8bed51 = result.data()
        XCTAssertNotNil(_vec_data_not_empty_5b8bed51[0].url(), "expected non-nil value")
    }

    func testSmokeImageWithSize() async throws {
        // Image generation with explicit size parameter
        let _request = try createImageRequestFromJson("{\"model\":\"dall-e-3\",\"n\":1,\"prompt\":\"A sunset over mountains\",\"size\":\"1792x1024\"}")
        let _client = try DefaultClient(apiKey: "test-key", baseUrl: ProcessInfo.processInfo.environment["MOCK_SERVER_URL"]! + "/fixtures/smoke_image_with_size")
        let result = try await _client.imageGenerate(_request)
        XCTAssertEqual(result.data().count, 1)
        let _vec_data_not_empty_5b8bed51 = result.data()
        XCTAssertNotNil(_vec_data_not_empty_5b8bed51[0].url(), "expected non-nil value")
    }

}
