// This file is auto-generated by alef — DO NOT EDIT.
// alef:hash:3fb07515ba02077671946cad7f111c0c7840343537bb31d1488bc316651dca95
// To regenerate: alef generate
// To verify freshness: alef verify --exit-code
// swift-format-ignore-file

import XCTest
import Foundation
#if canImport(FoundationNetworking)
import FoundationNetworking
#endif
import LiterLlm
import RustBridge

/// E2e tests for category: files.
final class FilesTests: XCTestCase {
    override class func setUp() {
        super.setUp()
    }

    func testEdgeFileEmptyList() async throws {
        // List files when no files have been uploaded
        let _client = try createClient(apiKey: "test-key", baseUrl: AlefE2EMockServer.baseURL + "/fixtures/edge_file_empty_list")
        let result = try await _client.listFiles(nil)
        XCTAssertEqual(result.data.count, 0)
    }

    func testEdgeFileLargeUpload() async throws {
        // Upload a large file successfully
        let _request = try LiterLlm.createFileRequestFromJson("{\"file\":\"eyJwcm9tcHQiOiAibGFyZ2UgdHJhaW5pbmcgZGF0YSJ9Cg==\",\"filename\":\"large_training_data.jsonl\",\"purpose\":\"fine-tune\"}")
        let _client = try createClient(apiKey: "test-key", baseUrl: AlefE2EMockServer.baseURL + "/fixtures/edge_file_large_upload")
        let result = try await _client.createFile(_request)
        XCTAssertGreaterThan(result.id.count, 0, "expected non-empty value")
    }

    func testErrorFileAuth401() async throws {
        // 401 Unauthorized when listing files with invalid API key
        do {
            let _client = try createClient(apiKey: "test-key", baseUrl: AlefE2EMockServer.baseURL + "/fixtures/error_file_auth_401")
            _ = try await _client.listFiles(nil)
            XCTFail("expected to throw")
        } catch {
            // success
        }
    }

    func testErrorFileBadPurpose() async throws {
        // 400 Bad Request when uploading a file with invalid purpose
        do {
            let _request = try LiterLlm.createFileRequestFromJson("{\"file\":\"data.jsonl\",\"purpose\":\"invalid-purpose\"}")
            let _client = try createClient(apiKey: "test-key", baseUrl: AlefE2EMockServer.baseURL + "/fixtures/error_file_bad_purpose")
            _ = try await _client.createFile(_request)
            XCTFail("expected to throw")
        } catch {
            // success
        }
    }

    func testErrorFileNotFound() async throws {
        // 404 Not Found when retrieving a nonexistent file
        do {
            let _client = try createClient(apiKey: "test-key", baseUrl: AlefE2EMockServer.baseURL + "/fixtures/error_file_not_found")
            _ = try await _client.retrieveFile("file-nonexistent")
            XCTFail("expected to throw")
        } catch {
            // success
        }
    }

    func testSmokeCreateFile() async throws {
        // Upload a file for use with the API
        let _request = try LiterLlm.createFileRequestFromJson("{\"file\":\"eyJwcm9tcHQiOiAiaGVsbG8ifQo=\",\"filename\":\"training_data.jsonl\",\"purpose\":\"fine-tune\"}")
        let _client = try createClient(apiKey: "test-key", baseUrl: AlefE2EMockServer.baseURL + "/fixtures/smoke_create_file")
        let result = try await _client.createFile(_request)
        XCTAssertGreaterThan(result.id.count, 0, "expected non-empty value")
    }

    func testSmokeDeleteFile() async throws {
        // Delete an uploaded file
        let _client = try createClient(apiKey: "test-key", baseUrl: AlefE2EMockServer.baseURL + "/fixtures/smoke_delete_file")
        let result = try await _client.deleteFile("file-abc123")
        XCTAssertGreaterThan(result.id.count, 0, "expected non-empty value")
        XCTAssertEqual(result.deleted, true)
    }

    func testSmokeFileContent() async throws {
        // Retrieve the content of an uploaded file
        let _client = try createClient(apiKey: "test-key", baseUrl: AlefE2EMockServer.baseURL + "/fixtures/smoke_file_content")
        let result = try await _client.fileContent("file-abc123")
        XCTAssertFalse(result.isEmpty, "expected non-empty value")
    }

    func testSmokeListFiles() async throws {
        // List all uploaded files
        let _client = try createClient(apiKey: "test-key", baseUrl: AlefE2EMockServer.baseURL + "/fixtures/smoke_list_files")
        let result = try await _client.listFiles(nil)
        XCTAssertEqual(result.data.count, 2)
    }

    func testSmokeRetrieveFile() async throws {
        // Retrieve metadata for an uploaded file
        let _client = try createClient(apiKey: "test-key", baseUrl: AlefE2EMockServer.baseURL + "/fixtures/smoke_retrieve_file")
        let result = try await _client.retrieveFile("file-abc123")
        XCTAssertGreaterThan(result.id.count, 0, "expected non-empty value")
    }

}
