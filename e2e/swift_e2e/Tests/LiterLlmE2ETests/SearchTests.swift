// This file is auto-generated by alef — DO NOT EDIT.
// alef:hash:f6823f4ec8be5dec6fbcb2874e0191f4b5fc1dd9b048a27a6288e7fc397cd156
// To regenerate: alef generate
// To verify freshness: alef verify --exit-code
// Issues & docs: https://github.com/sample_crate-dev/alef
// swift-format-ignore-file

import XCTest
import Foundation
#if canImport(FoundationNetworking)
import FoundationNetworking
#endif
import LiterLlm
import RustBridge

/// E2e tests for category: search.
final class SearchTests: XCTestCase {
    func testSearchBasic() async throws {
        // Basic web search request with a simple query
        let _request = try LiterLlm.searchRequestFromJson("{\"model\":\"brave/web-search\",\"query\":\"What is Rust programming language?\"}")
        let _client = try DefaultClient(apiKey: "test-key", baseUrl: AlefE2EMockServer.baseURL + "/fixtures/search_basic")
        let result = try await _client.search(_request)
    }

    func testSearchEmptyResults() async throws {
        // Web search with a query that returns no results
        let _request = try LiterLlm.searchRequestFromJson("{\"model\":\"brave/web-search\",\"query\":\"xyznonexistent12345xyz\"}")
        let _client = try DefaultClient(apiKey: "test-key", baseUrl: AlefE2EMockServer.baseURL + "/fixtures/search_empty_results")
        let result = try await _client.search(_request)
    }

    func testSearchError400() async throws {
        // 400 Bad Request error when search query is empty
        do {
            let _request = try LiterLlm.searchRequestFromJson("{\"model\":\"brave/web-search\",\"query\":\"\"}")
            let _client = try DefaultClient(apiKey: "test-key", baseUrl: AlefE2EMockServer.baseURL + "/fixtures/search_error_400")
            _ = try await _client.search(_request)
            XCTFail("expected to throw")
        } catch {
            // success
        }
    }

    func testSearchError401() async throws {
        // 401 Unauthorized error on web search due to invalid API credentials
        do {
            let _request = try LiterLlm.searchRequestFromJson("{\"model\":\"brave/web-search\",\"query\":\"test\"}")
            let _client = try DefaultClient(apiKey: "test-key", baseUrl: AlefE2EMockServer.baseURL + "/fixtures/search_error_401")
            _ = try await _client.search(_request)
            XCTFail("expected to throw")
        } catch {
            // success
        }
    }

    func testSearchWithMaxResults() async throws {
        // Search request with max_results parameter limiting response count
        let _request = try LiterLlm.searchRequestFromJson("{\"max_results\":2,\"model\":\"brave/web-search\",\"query\":\"Rust programming\"}")
        let _client = try DefaultClient(apiKey: "test-key", baseUrl: AlefE2EMockServer.baseURL + "/fixtures/search_with_max_results")
        let result = try await _client.search(_request)
        XCTAssertEqual(result.results.count, 2)
    }

}
