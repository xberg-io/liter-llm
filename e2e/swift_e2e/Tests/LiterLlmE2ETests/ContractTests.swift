// This file is auto-generated by alef — DO NOT EDIT.
// alef:hash:c88d9c84dc3555089c161a7b91b43c54df6a307f62f85f9803124d7049aebdbc
// To regenerate: alef generate
// To verify freshness: alef verify --exit-code
// Issues & docs: https://github.com/kreuzberg-dev/alef
// swift-format-ignore-file

import XCTest
import Foundation
import LiterLlm
import RustBridge

/// E2e tests for category: contract.
final class ContractTests: XCTestCase {
    func testBindingApiParity() async throws {
        // Verify all bindings expose the full API surface — constructor accepts all config options and every method exists
        let _request = try chatCompletionRequestFromJson("{\"messages\":[{\"content\":\"Contract test\",\"role\":\"user\"}],\"model\":\"openai/gpt-4o\"}")
        let _client = try DefaultClient(apiKey: "test-key", baseUrl: ProcessInfo.processInfo.environment["MOCK_SERVER_URL"]! + "/fixtures/binding_api_parity")
        let result = try await _client.chat(request: _request)
        XCTAssertEqual(result.choices.count, 1)
        XCTAssertEqual((result.choices[0].message.content ?? "").trimmingCharacters(in: CharacterSet.whitespacesAndNewlines), "OK")
        XCTAssertEqual(((result.choices[0].finishReason)?.rawValue ?? "").trimmingCharacters(in: CharacterSet.whitespacesAndNewlines), "stop")
        XCTAssertEqual(result.usage?.totalTokens, 6)
        XCTAssertEqual(result.model.trimmingCharacters(in: CharacterSet.whitespacesAndNewlines), "gpt-4o")
    }

    func testContractOcr() async throws {
        // Verify ocr() method exists in all bindings
        let _request = try ocrRequestFromJson("{\"document\":{\"type\":\"document_url\",\"url\":\"https://example.com/contract-test.pdf\"},\"model\":\"mistral/mistral-ocr-latest\"}")
        let _client = try DefaultClient(apiKey: "test-key", baseUrl: ProcessInfo.processInfo.environment["MOCK_SERVER_URL"]! + "/fixtures/contract_ocr")
        let result = try await _client.ocr(request: _request)
    }

    func testContractSearch() async throws {
        // Verify search() method exists in all bindings
        let _request = try searchRequestFromJson("{\"model\":\"brave/web-search\",\"query\":\"contract test query\"}")
        let _client = try DefaultClient(apiKey: "test-key", baseUrl: ProcessInfo.processInfo.environment["MOCK_SERVER_URL"]! + "/fixtures/contract_search")
        let result = try await _client.search(request: _request)
    }

}
