// This file is auto-generated by alef — DO NOT EDIT.
// alef:hash:6bb19c82a010ccb652c97bcf47af11690e323f84e46b4a1175dcec670ee4b8b5
// To regenerate: alef generate
// To verify freshness: alef verify --exit-code
// Issues & docs: https://github.com/kreuzberg-dev/alef
import XCTest
import Foundation
import LiterLlm
import RustBridge

/// E2e tests for category: moderate.
final class ModerateTests: XCTestCase {
    func testEdgeModerateAllCategories() async throws {
        // Moderation response with multiple categories flagged
        let _request = try moderationRequestFromJson("{\"input\":\"Extremely harmful content targeting multiple categories\",\"model\":\"omni-moderation-latest\"}")
        let _client = try DefaultClient(apiKey: "test-key", baseUrl: ProcessInfo.processInfo.environment["MOCK_SERVER_URL"]! + "/fixtures/edge_moderate_all_categories")
        let result = try await _client.moderate(_request)
        XCTAssertEqual(result.results().count, 1)
        let _vec_results_equals_66b74562 = result.results()
        XCTAssertEqual(_vec_results_equals_66b74562[0].flagged(), true)
    }

    func testEdgeModerateEmptyInput() async throws {
        // Moderation with empty string input
        let _request = try moderationRequestFromJson("{\"input\":\"\",\"model\":\"omni-moderation-latest\"}")
        let _client = try DefaultClient(apiKey: "test-key", baseUrl: ProcessInfo.processInfo.environment["MOCK_SERVER_URL"]! + "/fixtures/edge_moderate_empty_input")
        let result = try await _client.moderate(_request)
        XCTAssertEqual(result.results().count, 1)
        let _vec_results_equals_5340ed1b = result.results()
        XCTAssertEqual(_vec_results_equals_5340ed1b[0].flagged(), false)
    }

    func testErrorModerateAuth401() async throws {
        // 401 Unauthorized for moderation with invalid API key
        do {
            let _request = try moderationRequestFromJson("{\"input\":\"Hello\",\"model\":\"omni-moderation-latest\"}")
            let _client = try DefaultClient(apiKey: "test-key", baseUrl: ProcessInfo.processInfo.environment["MOCK_SERVER_URL"]! + "/fixtures/error_moderate_auth_401")
            _ = try await _client.moderate(_request)
            XCTFail("expected to throw")
        } catch {
            // success
        }
    }

    func testErrorModerateBadRequest() async throws {
        // 400 Bad Request for moderation with invalid model
        do {
            let _request = try moderationRequestFromJson("{\"input\":\"Hello\",\"model\":\"nonexistent-moderation\"}")
            let _client = try DefaultClient(apiKey: "test-key", baseUrl: ProcessInfo.processInfo.environment["MOCK_SERVER_URL"]! + "/fixtures/error_moderate_bad_request")
            _ = try await _client.moderate(_request)
            XCTFail("expected to throw")
        } catch {
            // success
        }
    }

    func testSmokeModerateBatch() async throws {
        // Moderate multiple inputs in a single request
        let _request = try moderationRequestFromJson("{\"input\":[\"Hello world\",\"Nice weather today\"],\"model\":\"omni-moderation-latest\"}")
        let _client = try DefaultClient(apiKey: "test-key", baseUrl: ProcessInfo.processInfo.environment["MOCK_SERVER_URL"]! + "/fixtures/smoke_moderate_batch")
        let result = try await _client.moderate(_request)
        XCTAssertEqual(result.results().count, 2)
        let _vec_results_equals_5340ed1b = result.results()
        XCTAssertEqual(_vec_results_equals_5340ed1b[0].flagged(), false)
    }

    func testSmokeModerateFlagged() async throws {
        // Moderation detects flagged content
        let _request = try moderationRequestFromJson("{\"input\":\"I want to hurt someone very badly\",\"model\":\"omni-moderation-latest\"}")
        let _client = try DefaultClient(apiKey: "test-key", baseUrl: ProcessInfo.processInfo.environment["MOCK_SERVER_URL"]! + "/fixtures/smoke_moderate_flagged")
        let result = try await _client.moderate(_request)
        XCTAssertEqual(result.results().count, 1)
        let _vec_results_equals_66b74562 = result.results()
        XCTAssertEqual(_vec_results_equals_66b74562[0].flagged(), true)
    }

    func testSmokeModerateSingle() async throws {
        // Moderate a single non-flagged input
        let _request = try moderationRequestFromJson("{\"input\":\"The weather is nice today.\",\"model\":\"omni-moderation-latest\"}")
        let _client = try DefaultClient(apiKey: "test-key", baseUrl: ProcessInfo.processInfo.environment["MOCK_SERVER_URL"]! + "/fixtures/smoke_moderate_single")
        let result = try await _client.moderate(_request)
        XCTAssertEqual(result.results().count, 1)
        let _vec_results_equals_5340ed1b = result.results()
        XCTAssertEqual(_vec_results_equals_5340ed1b[0].flagged(), false)
    }

}
