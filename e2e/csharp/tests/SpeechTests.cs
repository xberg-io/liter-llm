// This file is auto-generated by alef — DO NOT EDIT.
// alef:hash:9c85c8c7b7e267ee9ea72f584518c5b3879df4cbe46e331ccac7ccee477aebe2
// To regenerate: alef generate
// To verify freshness: alef verify --exit-code
// Issues & docs: https://github.com/kreuzberg-dev/alef

using System;
using System.Collections.Generic;
using System.Linq;
using System.Net.Http;
using System.Text;
using System.Text.Json;
using System.Text.Json.Serialization;
using System.Threading.Tasks;
using Xunit;
using LiterLlm;
using static LiterLlm.LiterLlmLib;

namespace LiterLlm
{
    /// <summary>E2e tests for category: speech.</summary>
    public class SpeechTests
    {
            private static readonly JsonSerializerOptions ConfigOptions = new() { Converters = { new JsonStringEnumConverter(JsonNamingPolicy.SnakeCaseLower) }, DefaultIgnoreCondition = JsonIgnoreCondition.WhenWritingDefault };

        [Fact]
        public async Task Test_EdgeSpeechAllVoices()
        {
            // Text-to-speech with specific voice selection
            var baseUrl = (System.Environment.GetEnvironmentVariable("MOCK_SERVER_URL") ?? string.Empty) + "/fixtures/edge_speech_all_voices";
            var client = LiterLlmLib.CreateClient("test-key", baseUrl, null, null, null);

            var result = await client.Speech(new CreateSpeechRequest { Input = "Hello world", Model = "tts-1", Voice = "nova" });
    Assert.False(string.IsNullOrEmpty(result.Audio?.ToString()));

        }

        [Fact]
        public async Task Test_EdgeSpeechLongInput()
        {
            // Speech generation with a very long input text
            var baseUrl = (System.Environment.GetEnvironmentVariable("MOCK_SERVER_URL") ?? string.Empty) + "/fixtures/edge_speech_long_input";
            var client = LiterLlmLib.CreateClient("test-key", baseUrl, null, null, null);

            var result = await client.Speech(new CreateSpeechRequest { Input = "This is a long input text. This is a long input text. This is a long input text. This is a long input text. This is a long input text. This is a long input text. This is a long input text. This is a long input text. This is a long input text. This is a long input text. This is a long input text. This is a long input text. This is a long input text. This is a long input text. This is a long input text. This is a long input text. This is a long input text. This is a long input text. This is a long input text. This is a long input text. This is a long input text. This is a long input text. This is a long input text. This is a long input text. This is a long input text. This is a long input text. This is a long input text. This is a long input text. This is a long input text. This is a long input text. This is a long input text. This is a long input text. This is a long input text. This is a long input text. This is a long input text. This is a long input text. This is a long input text. This is a long input text. This is a long input text. This is a long input text. This is a long input text. This is a long input text. This is a long input text. This is a long input text. This is a long input text. This is a long input text. This is a long input text. This is a long input text. This is a long input text. This is a long input text. End of input.", Model = "tts-1", Voice = "echo" });
    Assert.False(string.IsNullOrEmpty(result.Audio?.ToString()));

        }

        [Fact]
        public async Task Test_ErrorSpeechAuth401()
        {
            // 401 Unauthorized for speech generation with invalid API key
            var baseUrl = (System.Environment.GetEnvironmentVariable("MOCK_SERVER_URL") ?? string.Empty) + "/fixtures/error_speech_auth_401";
            var client = LiterLlmLib.CreateClient("test-key", baseUrl, null, null, null);

            await Assert.ThrowsAnyAsync<LiterLlmException>(() => client.Speech(new CreateSpeechRequest { Input = "Hello", Model = "tts-1", Voice = "alloy" }));
        }

        [Fact]
        public async Task Test_ErrorSpeechBadModel()
        {
            // 400 Bad Request for speech with unsupported model
            var baseUrl = (System.Environment.GetEnvironmentVariable("MOCK_SERVER_URL") ?? string.Empty) + "/fixtures/error_speech_bad_model";
            var client = LiterLlmLib.CreateClient("test-key", baseUrl, null, null, null);

            await Assert.ThrowsAnyAsync<LiterLlmException>(() => client.Speech(new CreateSpeechRequest { Input = "Hello", Model = "tts-nonexistent", Voice = "alloy" }));
        }

        [Fact]
        public async Task Test_SmokeSpeechBasic()
        {
            // Basic text-to-speech generation
            var baseUrl = (System.Environment.GetEnvironmentVariable("MOCK_SERVER_URL") ?? string.Empty) + "/fixtures/smoke_speech_basic";
            var client = LiterLlmLib.CreateClient("test-key", baseUrl, null, null, null);

            var result = await client.Speech(new CreateSpeechRequest { Input = "Hello, world!", Model = "tts-1", Voice = "alloy" });
    Assert.False(string.IsNullOrEmpty(result.Audio?.ToString()));

        }

        [Fact]
        public async Task Test_SmokeSpeechMp3Format()
        {
            // Text-to-speech with explicit MP3 response format
            var baseUrl = (System.Environment.GetEnvironmentVariable("MOCK_SERVER_URL") ?? string.Empty) + "/fixtures/smoke_speech_mp3_format";
            var client = LiterLlmLib.CreateClient("test-key", baseUrl, null, null, null);

            var result = await client.Speech(new CreateSpeechRequest { Input = "The quick brown fox jumps over the lazy dog.", Model = "tts-1-hd", ResponseFormat = "mp3", Speed = 1.0d, Voice = "nova" });
    Assert.False(string.IsNullOrEmpty(result.Audio?.ToString()));

        }


    }
}
