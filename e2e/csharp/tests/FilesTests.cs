// This file is auto-generated by alef — DO NOT EDIT.
// alef:hash:ff2dc1bd096a8371a792d9694421fa51f83849c9a7bea20ba213ad126cb5c53d
// To regenerate: alef generate
// To verify freshness: alef verify --exit-code
// Issues & docs: https://github.com/kreuzberg-dev/alef

using System;
using System.Collections.Generic;
using System.Linq;
using System.Net.Http;
using System.Text;
using System.Text.Json;
using System.Text.Json.Serialization;
using System.Threading.Tasks;
using Xunit;
using LiterLlm;
using static LiterLlm.LiterLlmLib;

namespace LiterLlm
{
    /// <summary>E2e tests for category: files.</summary>
    public class FilesTests
    {
            private static readonly JsonSerializerOptions ConfigOptions = new() { Converters = { new JsonStringEnumConverter(JsonNamingPolicy.SnakeCaseLower) }, DefaultIgnoreCondition = JsonIgnoreCondition.WhenWritingDefault };

        [Fact]
        public async Task Test_EdgeFileEmptyList()
        {
            // List files when no files have been uploaded
            var baseUrl = (System.Environment.GetEnvironmentVariable("MOCK_SERVER_URL") ?? string.Empty) + "/fixtures/edge_file_empty_list";
            var client = LiterLlmLib.CreateClient("test-key", baseUrl, null, null, null);

            var result = await client.ListFiles();
    Assert.Equal(0, result.Data.Count);

        }

        [Fact]
        public async Task Test_EdgeFileLargeUpload()
        {
            // Upload a large file successfully
            var baseUrl = (System.Environment.GetEnvironmentVariable("MOCK_SERVER_URL") ?? string.Empty) + "/fixtures/edge_file_large_upload";
            var client = LiterLlmLib.CreateClient("test-key", baseUrl, null, null, null);

            var result = await client.CreateFile(new CreateFileRequest { File = "eyJwcm9tcHQiOiAibGFyZ2UgdHJhaW5pbmcgZGF0YSJ9Cg==", Filename = "large_training_data.jsonl", Purpose = "fine-tune" });
    Assert.False(string.IsNullOrEmpty(result.Id?.ToString()));

        }

        [Fact]
        public async Task Test_ErrorFileAuth401()
        {
            // 401 Unauthorized when listing files with invalid API key
            var baseUrl = (System.Environment.GetEnvironmentVariable("MOCK_SERVER_URL") ?? string.Empty) + "/fixtures/error_file_auth_401";
            var client = LiterLlmLib.CreateClient("test-key", baseUrl, null, null, null);

            await Assert.ThrowsAnyAsync<LiterLlmException>(() => client.ListFiles());
        }

        [Fact]
        public async Task Test_ErrorFileBadPurpose()
        {
            // 400 Bad Request when uploading a file with invalid purpose
            var baseUrl = (System.Environment.GetEnvironmentVariable("MOCK_SERVER_URL") ?? string.Empty) + "/fixtures/error_file_bad_purpose";
            var client = LiterLlmLib.CreateClient("test-key", baseUrl, null, null, null);

            await Assert.ThrowsAnyAsync<LiterLlmException>(() => client.CreateFile(new CreateFileRequest { File = "data.jsonl", Purpose = "invalid-purpose" }));
        }

        [Fact]
        public async Task Test_ErrorFileNotFound()
        {
            // 404 Not Found when retrieving a nonexistent file
            var baseUrl = (System.Environment.GetEnvironmentVariable("MOCK_SERVER_URL") ?? string.Empty) + "/fixtures/error_file_not_found";
            var client = LiterLlmLib.CreateClient("test-key", baseUrl, null, null, null);

            await Assert.ThrowsAnyAsync<LiterLlmException>(() => client.RetrieveFile("file-nonexistent"));
        }

        [Fact]
        public async Task Test_SmokeCreateFile()
        {
            // Upload a file for use with the API
            var baseUrl = (System.Environment.GetEnvironmentVariable("MOCK_SERVER_URL") ?? string.Empty) + "/fixtures/smoke_create_file";
            var client = LiterLlmLib.CreateClient("test-key", baseUrl, null, null, null);

            var result = await client.CreateFile(new CreateFileRequest { File = "eyJwcm9tcHQiOiAiaGVsbG8ifQo=", Filename = "training_data.jsonl", Purpose = "fine-tune" });
    Assert.False(string.IsNullOrEmpty(result.Id?.ToString()));

        }

        [Fact]
        public async Task Test_SmokeDeleteFile()
        {
            // Delete an uploaded file
            var baseUrl = (System.Environment.GetEnvironmentVariable("MOCK_SERVER_URL") ?? string.Empty) + "/fixtures/smoke_delete_file";
            var client = LiterLlmLib.CreateClient("test-key", baseUrl, null, null, null);

            var result = await client.DeleteFile("file-abc123");
    Assert.False(string.IsNullOrEmpty(result.Id?.ToString()));
    Assert.True(result.Deleted);

        }

        [Fact]
        public async Task Test_SmokeFileContent()
        {
            // Retrieve the content of an uploaded file
            var baseUrl = (System.Environment.GetEnvironmentVariable("MOCK_SERVER_URL") ?? string.Empty) + "/fixtures/smoke_file_content";
            var client = LiterLlmLib.CreateClient("test-key", baseUrl, null, null, null);

            var result = await client.FileContent("file-abc123");
    Assert.False(string.IsNullOrEmpty(result.Content?.ToString()));

        }

        [Fact]
        public async Task Test_SmokeListFiles()
        {
            // List all uploaded files
            var baseUrl = (System.Environment.GetEnvironmentVariable("MOCK_SERVER_URL") ?? string.Empty) + "/fixtures/smoke_list_files";
            var client = LiterLlmLib.CreateClient("test-key", baseUrl, null, null, null);

            var result = await client.ListFiles();
    Assert.Equal(2, result.Data.Count);

        }

        [Fact]
        public async Task Test_SmokeRetrieveFile()
        {
            // Retrieve metadata for an uploaded file
            var baseUrl = (System.Environment.GetEnvironmentVariable("MOCK_SERVER_URL") ?? string.Empty) + "/fixtures/smoke_retrieve_file";
            var client = LiterLlmLib.CreateClient("test-key", baseUrl, null, null, null);

            var result = await client.RetrieveFile("file-abc123");
    Assert.False(string.IsNullOrEmpty(result.Id?.ToString()));

        }


    }
}
