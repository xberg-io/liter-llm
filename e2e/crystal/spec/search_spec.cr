require "./spec_helper"

describe LiterLlm do
  describe "search" do
    pending "Basic web search request with a simple query"
    pending "Web search with a query that returns no results"
    pending "400 Bad Request error when search query is empty"
    pending "401 Unauthorized error on web search due to invalid API credentials"
    pending "Search request with max_results parameter limiting response count"
  end
end
