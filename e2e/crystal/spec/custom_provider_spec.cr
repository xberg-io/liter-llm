require "./spec_helper"

describe LiterLlm do
  describe "custom_provider" do
    pending "Tests custom provider with custom auth header"
    pending "Tests that a custom provider can be registered and routes requests"
  end
end
