require "./spec_helper"

describe LiterLlm do
  describe "rerank" do
    pending "Reranking with an empty query string"
    pending "Reranking with only a single document"
    pending "401 Unauthorized for reranking with invalid API key"
    pending "400 Bad Request for reranking with invalid model"
    pending "Basic reranking of documents against a query"
    pending "Reranking with return_documents flag to include document text"
    pending "Reranking with top_n parameter to limit results"
  end
end
