require "./spec_helper"

describe LiterLlm do
  describe "list-models" do
    pending "List models response returns an empty data array when no models are available"
    pending "401 Unauthorized error on list models request when API key is invalid"
    pending "500 Internal Server Error when listing models due to server failure"
    pending "List models response with multiple model objects"
  end
end
