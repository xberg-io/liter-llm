require "./spec_helper"

describe LiterLlm do
  describe "transcribe" do
    pending "Transcription of a silent or empty audio file returns empty text"
    pending "Transcription with verbose JSON response format including timestamp segments"
    pending "401 Unauthorized for transcription with invalid API key"
    pending "400 Bad Request when audio format is unsupported"
    pending "Basic audio transcription"
    pending "Audio transcription with explicit language hint"
  end
end
