require "./spec_helper"

describe LiterLlm do
  describe "types" do
    pending "Request with all message role types (system, user, assistant, tool) to verify round-trip serialization"
    pending "User message with mixed text and image_url content parts to verify multimodal serialization"
  end
end
