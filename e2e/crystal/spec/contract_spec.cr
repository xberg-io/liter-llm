require "./spec_helper"

describe LiterLlm do
  describe "contract" do
    pending "Verify all bindings expose the full API surface — constructor accepts all config options and every method exists"
    pending "Verify ocr() method exists in all bindings"
    pending "Verify search() method exists in all bindings"
  end
end
