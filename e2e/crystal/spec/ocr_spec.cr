require "./spec_helper"

describe LiterLlm do
  describe "ocr" do
    pending "400 Bad Request error when OCR input has an invalid image format"
    pending "401 Unauthorized error on OCR request due to invalid API credentials"
    pending "OCR request returning multiple pages of document content"
    pending "OCR request with a document URL input"
  end
end
