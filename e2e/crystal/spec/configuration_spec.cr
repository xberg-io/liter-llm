require "./spec_helper"

describe LiterLlm do
  describe "configuration" do
    pending "Client configured with a custom base URL routes all requests to that endpoint"
    pending "Client configured with extra custom headers successfully completes a chat request"
    pending "llamacpp local provider routes requests via llamacpp/ model prefix with no auth"
    pending "Ollama local provider routes requests via ollama/ model prefix with no auth"
    pending "vLLM local provider routes requests via vllm/ model prefix with no auth"
  end
end
