require "./spec_helper"

describe LiterLlm do
  describe "embed" do
    pending "Embedding request with multiple input strings returns one embedding object per input"
    pending "Embedding request with multiple inputs returns multiple embedding objects"
    pending "Embedding request with empty string input returns empty data array"
    pending "Embedding request with explicit encoding_format of float returns float array embeddings"
    pending "401 Unauthorized error on embedding request when API key is invalid"
    pending "Embedding request with explicit dimensions parameter returns embeddings of the requested size"
    pending "Embedding request via Ollama local provider with all-minilm model"
  end
end
