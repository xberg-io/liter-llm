require "./spec_helper"

describe LiterLlm do
  describe "responses" do
    pending "Response completes with empty output items"
    pending "Response created with a very large input text"
    pending "401 Unauthorized when creating a response with invalid API key"
    pending "400 Bad Request when creating response with invalid model"
    pending "404 Not Found when retrieving a nonexistent response"
    pending "Cancel an in-progress response"
    pending "Create a basic response using the Responses API"
    pending "Response that includes tool call output items"
    pending "Retrieve a previously created response"
  end
end
