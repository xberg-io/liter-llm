require "./spec_helper"

describe LiterLlm do
  describe "moderate" do
    pending "Moderation response with multiple categories flagged"
    pending "Moderation with empty string input"
    pending "401 Unauthorized for moderation with invalid API key"
    pending "400 Bad Request for moderation with invalid model"
    pending "Moderate multiple inputs in a single request"
    pending "Moderation detects flagged content"
    pending "Moderate a single non-flagged input"
  end
end
