require "./spec_helper"

describe LiterLlm do
  describe "batches" do
    pending "Attempt to cancel an already-cancelled batch"
    pending "List batches when no batches exist"
    pending "401 Unauthorized when creating a batch with invalid API key"
    pending "400 Bad Request when creating a batch with invalid input file"
    pending "404 Not Found when retrieving a nonexistent batch"
    pending "Retrieve a completed batch job with output file"
    pending "Cancel a running batch job"
    pending "Create a new batch processing job"
    pending "List all batch jobs"
    pending "Retrieve the status of a batch job"
  end
end
