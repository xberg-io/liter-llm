// This file is auto-generated by alef — DO NOT EDIT.
// alef:hash:37d09de090d47599d340637316bdacf4a60161034b57399f7c45a94977d258de
// To regenerate: alef generate
// To verify freshness: alef verify --exit-code
// Issues & docs: https://github.com/kreuzberg-dev/alef
import 'package:test/test.dart';
import 'dart:io';
import 'package:liter_llm/liter_llm.dart';
import 'package:liter_llm/src/liter_llm_bridge_generated/frb_generated.dart' show RustLib;

// E2e tests for category: smoke
void main() {
  setUpAll(() async {
    await RustLib.init();
  });

  test('Basic chat completion via the Anthropic provider (claude-3-5-sonnet-20241022) with system and user messages', () async {
    final _mockUrl = "${Platform.environment["MOCK_SERVER_URL"] ?? "http://localhost:8080"}/fixtures/anthropic_chat";
        final _client = await LiterLlmBridge.createClient('test-key', baseUrl: _mockUrl);
    final result = await _client.chat();
  });

  test('Chat completion via Azure OpenAI with the azure/ prefix for provider routing — verifies the prefix is stripped before dispatching and the response is normalised to the standard OpenAI chat completion shape', () async {
    final _mockUrl = "${Platform.environment["MOCK_SERVER_URL"] ?? "http://localhost:8080"}/fixtures/azure_chat";
        final _client = await LiterLlmBridge.createClient('test-key', baseUrl: _mockUrl);
    final result = await _client.chat();
  });

  test('Embedding request via Azure OpenAI using the azure/ provider prefix — response follows the standard OpenAI embeddings shape that Azure returns unchanged', () async {
    final _mockUrl = "${Platform.environment["MOCK_SERVER_URL"] ?? "http://localhost:8080"}/fixtures/azure_embed";
        final _client = await LiterLlmBridge.createClient('test-key', baseUrl: _mockUrl);
    final result = await _client.embed();
  });

  test('Basic chat completion with a single user message', () async {
    final _mockUrl = "${Platform.environment["MOCK_SERVER_URL"] ?? "http://localhost:8080"}/fixtures/basic_chat";
        final _client = await LiterLlmBridge.createClient('test-key', baseUrl: _mockUrl);
    final result = await _client.chat();
  });

  test('Basic embedding request for a single input string', () async {
    final _mockUrl = "${Platform.environment["MOCK_SERVER_URL"] ?? "http://localhost:8080"}/fixtures/basic_embed";
        final _client = await LiterLlmBridge.createClient('test-key', baseUrl: _mockUrl);
    final result = await _client.embed();
  });

  test('List available models from the API', () async {
    final _mockUrl = "${Platform.environment["MOCK_SERVER_URL"] ?? "http://localhost:8080"}/fixtures/basic_list_models";
        final _client = await LiterLlmBridge.createClient('test-key', baseUrl: _mockUrl);
    final result = await _client.listModels();
  });

  test('Basic chat completion via the AWS Bedrock provider using the bedrock/ prefix for routing — verifies the prefix is stripped before dispatching and the Converse API response is normalised to the standard OpenAI chat completion shape', () async {
    final _mockUrl = "${Platform.environment["MOCK_SERVER_URL"] ?? "http://localhost:8080"}/fixtures/bedrock_chat";
        final _client = await LiterLlmBridge.createClient('test-key', baseUrl: _mockUrl);
    final result = await _client.chat();
  });

  test('Basic chat completion via the GitHub Copilot provider (gpt-4o) with a single user message', () async {
    final _mockUrl = "${Platform.environment["MOCK_SERVER_URL"] ?? "http://localhost:8080"}/fixtures/github_copilot_chat";
        final _client = await LiterLlmBridge.createClient('test-key', baseUrl: _mockUrl);
    final result = await _client.chat();
  });

  test('Chat completion against local Ollama with qwen2:0.5b model', () async {
    final _mockUrl = "${Platform.environment["MOCK_SERVER_URL"] ?? "http://localhost:8080"}/fixtures/local_chat_ollama";
        final _client = await LiterLlmBridge.createClient('test-key', baseUrl: _mockUrl);
    final result = await _client.chat();
  });

  test('List models from local Ollama instance', () async {
    final _mockUrl = "${Platform.environment["MOCK_SERVER_URL"] ?? "http://localhost:8080"}/fixtures/local_list_models_ollama";
        final _client = await LiterLlmBridge.createClient('test-key', baseUrl: _mockUrl);
    final result = await _client.listModels();
  });

  test('Test in-memory caching by sending identical requests twice', () async {
    final _mockUrl = "${Platform.environment["MOCK_SERVER_URL"] ?? "http://localhost:8080"}/fixtures/smoke_cache_memory";
        final _client = await LiterLlmBridge.createClient('test-key', baseUrl: _mockUrl);
    final result = await _client.chat();
  });

  test('Basic chat completion against real Anthropic API', () async {
    final _mockUrl = "${Platform.environment["MOCK_SERVER_URL"] ?? "http://localhost:8080"}/fixtures/smoke_chat_anthropic";
        final _client = await LiterLlmBridge.createClient('test-key', baseUrl: _mockUrl);
    final result = await _client.chat();
  });

  test('Basic chat completion against real Google Gemini API', () async {
    final _mockUrl = "${Platform.environment["MOCK_SERVER_URL"] ?? "http://localhost:8080"}/fixtures/smoke_chat_gemini";
        final _client = await LiterLlmBridge.createClient('test-key', baseUrl: _mockUrl);
    final result = await _client.chat();
  });

  test('Basic chat completion against real OpenAI API', () async {
    final _mockUrl = "${Platform.environment["MOCK_SERVER_URL"] ?? "http://localhost:8080"}/fixtures/smoke_chat_openai";
        final _client = await LiterLlmBridge.createClient('test-key', baseUrl: _mockUrl);
    final result = await _client.chat();
  });

  test('Embeddings request against real OpenAI API', () async {
    final _mockUrl = "${Platform.environment["MOCK_SERVER_URL"] ?? "http://localhost:8080"}/fixtures/smoke_embed_openai";
        final _client = await LiterLlmBridge.createClient('test-key', baseUrl: _mockUrl);
    final result = await _client.embed();
  });

  test('List models against real OpenAI API', () async {
    final _mockUrl = "${Platform.environment["MOCK_SERVER_URL"] ?? "http://localhost:8080"}/fixtures/smoke_list_models_openai";
        final _client = await LiterLlmBridge.createClient('test-key', baseUrl: _mockUrl);
    final result = await _client.listModels();
  });

  test('Test provider routing by sending requests to OpenAI and Anthropic', () async {
    final _mockUrl = "${Platform.environment["MOCK_SERVER_URL"] ?? "http://localhost:8080"}/fixtures/smoke_provider_routing";
        final _client = await LiterLlmBridge.createClient('test-key', baseUrl: _mockUrl);
    final result = await _client.chat();
  });

  test('Chat streaming against real OpenAI API, verifies chunks received', () async {
    final _mockUrl = "${Platform.environment["MOCK_SERVER_URL"] ?? "http://localhost:8080"}/fixtures/smoke_streaming_openai";
        final _client = await LiterLlmBridge.createClient('test-key', baseUrl: _mockUrl);
    final result = await _client.chatStream();
  });

  test('Basic chat completion via the Google Vertex AI provider using the vertex_ai/ prefix for routing — verifies the prefix is stripped before dispatching and the Gemini response is normalised to the standard OpenAI chat completion shape', () async {
    final _mockUrl = "${Platform.environment["MOCK_SERVER_URL"] ?? "http://localhost:8080"}/fixtures/vertex_chat";
        final _client = await LiterLlmBridge.createClient('test-key', baseUrl: _mockUrl);
    final result = await _client.chat();
  });

  test('Embedding request via Google Vertex AI using the vertex_ai/ provider prefix and the text-embedding-005 model — response follows the standard OpenAI embeddings shape', () async {
    final _mockUrl = "${Platform.environment["MOCK_SERVER_URL"] ?? "http://localhost:8080"}/fixtures/vertex_embed";
        final _client = await LiterLlmBridge.createClient('test-key', baseUrl: _mockUrl);
    final result = await _client.embed();
  });

}
