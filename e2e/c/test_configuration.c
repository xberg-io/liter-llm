/*
 * This file is auto-generated by alef — DO NOT EDIT.
 * alef:hash:4fe8d3411dc9c937765211959f3ac9d4fc3049ccb38d0826d86e3bdb98f705cf
 * To regenerate: alef generate
 * To verify freshness: alef verify --exit-code
 */
/* E2e tests for category: configuration */

#include <assert.h>
#include <stdint.h>
#include <string.h>
#include <stdio.h>
#include <stdlib.h>
#include "liter_llm.h"
#include "test_runner.h"

void test_custom_base_url(void) {
    /* Client configured with a custom base URL routes all requests to that endpoint */
    LITERLLMChatCompletionRequest* chat_completion_request_handle = literllm_chat_completion_request_from_json("{\"messages\":[{\"content\":\"Hello\",\"role\":\"user\"}],\"model\":\"local-model\"}");
    assert(chat_completion_request_handle != NULL && "failed to build request");
    const char* mock_base = getenv("MOCK_SERVER_URL");
    assert(mock_base != NULL && "MOCK_SERVER_URL must be set");
    char base_url[1024];
    snprintf(base_url, sizeof(base_url), "%s/fixtures/custom_base_url", mock_base);
    LITERLLMDefaultClient* client = literllm_create_client("test-key", base_url, (uint64_t)-1, (uint32_t)-1, NULL);
    assert(client != NULL && "failed to create client");
    LITERLLMChatCompletionResponse* result = literllm_default_client_chat(client, chat_completion_request_handle);
    assert(result != NULL && "expected call to succeed");
    char* choices = literllm_chat_completion_response_choices(result);
    char* choices_json = literllm_chat_completion_response_choices(result);
    assert(choices_json != NULL);
    char* choices_0_json = alef_json_array_get_index(choices_json, 0);
    char* message_json = alef_json_get_object(choices_0_json, "message");
    char* choices_0_message_content = alef_json_get_string(message_json, "content");
    char* choices_0_finish_reason = alef_json_get_string(choices_0_json, "finish_reason");
    char* model = literllm_chat_completion_response_model(result);
    {
        /* count_equals: count elements in array */
        assert(choices != NULL && "expected non-null collection JSON");
        int elem_count = alef_json_array_count(choices);
        assert(elem_count == 1 && "expected 1 elements");
    }
    assert(str_trim_eq(choices_0_message_content, "Hi there!") == 0 && "equals assertion failed");
    assert(str_trim_eq(choices_0_finish_reason, "stop") == 0 && "equals assertion failed");
    assert(str_trim_eq(model, "local-model") == 0 && "equals assertion failed");
    literllm_free_string(choices);
    free(choices_0_message_content);
    free(choices_0_finish_reason);
    literllm_free_string(model);
    free(message_json);
    free(choices_0_json);
    literllm_free_string(choices_json);
    literllm_chat_completion_response_free(result);
    literllm_chat_completion_request_free(chat_completion_request_handle);
    literllm_default_client_free(client);
}

void test_extra_headers(void) {
    /* Client configured with extra custom headers successfully completes a chat request */
    LITERLLMChatCompletionRequest* chat_completion_request_handle = literllm_chat_completion_request_from_json("{\"messages\":[{\"content\":\"Hello\",\"role\":\"user\"}],\"model\":\"gpt-4\"}");
    assert(chat_completion_request_handle != NULL && "failed to build request");
    const char* mock_base = getenv("MOCK_SERVER_URL");
    assert(mock_base != NULL && "MOCK_SERVER_URL must be set");
    char base_url[1024];
    snprintf(base_url, sizeof(base_url), "%s/fixtures/extra_headers", mock_base);
    LITERLLMDefaultClient* client = literllm_create_client("test-key", base_url, (uint64_t)-1, (uint32_t)-1, NULL);
    assert(client != NULL && "failed to create client");
    LITERLLMChatCompletionResponse* result = literllm_default_client_chat(client, chat_completion_request_handle);
    assert(result != NULL && "expected call to succeed");
    char* choices = literllm_chat_completion_response_choices(result);
    char* choices_json = literllm_chat_completion_response_choices(result);
    assert(choices_json != NULL);
    char* choices_0_json = alef_json_array_get_index(choices_json, 0);
    char* choices_0_finish_reason = alef_json_get_string(choices_0_json, "finish_reason");
    {
        /* count_equals: count elements in array */
        assert(choices != NULL && "expected non-null collection JSON");
        int elem_count = alef_json_array_count(choices);
        assert(elem_count == 1 && "expected 1 elements");
    }
    assert(str_trim_eq(choices_0_finish_reason, "stop") == 0 && "equals assertion failed");
    literllm_free_string(choices);
    free(choices_0_finish_reason);
    free(choices_0_json);
    literllm_free_string(choices_json);
    literllm_chat_completion_response_free(result);
    literllm_chat_completion_request_free(chat_completion_request_handle);
    literllm_default_client_free(client);
}

void test_local_provider_llamacpp(void) {
    /* llamacpp local provider routes requests via llamacpp/ model prefix with no auth */
    LITERLLMChatCompletionRequest* chat_completion_request_handle = literllm_chat_completion_request_from_json("{\"messages\":[{\"content\":\"Hello\",\"role\":\"user\"}],\"model\":\"llamacpp/my-model\"}");
    assert(chat_completion_request_handle != NULL && "failed to build request");
    const char* mock_base = getenv("MOCK_SERVER_URL");
    assert(mock_base != NULL && "MOCK_SERVER_URL must be set");
    char base_url[1024];
    snprintf(base_url, sizeof(base_url), "%s/fixtures/local_provider_llamacpp", mock_base);
    LITERLLMDefaultClient* client = literllm_create_client("test-key", base_url, (uint64_t)-1, (uint32_t)-1, NULL);
    assert(client != NULL && "failed to create client");
    LITERLLMChatCompletionResponse* result = literllm_default_client_chat(client, chat_completion_request_handle);
    assert(result != NULL && "expected call to succeed");
    char* choices = literllm_chat_completion_response_choices(result);
    char* choices_json = literllm_chat_completion_response_choices(result);
    assert(choices_json != NULL);
    char* choices_0_json = alef_json_array_get_index(choices_json, 0);
    char* message_json = alef_json_get_object(choices_0_json, "message");
    char* choices_0_message_content = alef_json_get_string(message_json, "content");
    char* choices_0_finish_reason = alef_json_get_string(choices_0_json, "finish_reason");
    char* model = literllm_chat_completion_response_model(result);
    {
        /* count_equals: count elements in array */
        assert(choices != NULL && "expected non-null collection JSON");
        int elem_count = alef_json_array_count(choices);
        assert(elem_count == 1 && "expected 1 elements");
    }
    assert(str_trim_eq(choices_0_message_content, "Hi there! I'm running locally.") == 0 && "equals assertion failed");
    assert(str_trim_eq(choices_0_finish_reason, "stop") == 0 && "equals assertion failed");
    assert(str_trim_eq(model, "my-model") == 0 && "equals assertion failed");
    literllm_free_string(choices);
    free(choices_0_message_content);
    free(choices_0_finish_reason);
    literllm_free_string(model);
    free(message_json);
    free(choices_0_json);
    literllm_free_string(choices_json);
    literllm_chat_completion_response_free(result);
    literllm_chat_completion_request_free(chat_completion_request_handle);
    literllm_default_client_free(client);
}

void test_local_provider_ollama(void) {
    /* Ollama local provider routes requests via ollama/ model prefix with no auth */
    LITERLLMChatCompletionRequest* chat_completion_request_handle = literllm_chat_completion_request_from_json("{\"messages\":[{\"content\":\"Hello\",\"role\":\"user\"}],\"model\":\"ollama/qwen2:0.5b\"}");
    assert(chat_completion_request_handle != NULL && "failed to build request");
    const char* mock_base = getenv("MOCK_SERVER_URL");
    assert(mock_base != NULL && "MOCK_SERVER_URL must be set");
    char base_url[1024];
    snprintf(base_url, sizeof(base_url), "%s/fixtures/local_provider_ollama", mock_base);
    LITERLLMDefaultClient* client = literllm_create_client("test-key", base_url, (uint64_t)-1, (uint32_t)-1, NULL);
    assert(client != NULL && "failed to create client");
    LITERLLMChatCompletionResponse* result = literllm_default_client_chat(client, chat_completion_request_handle);
    assert(result != NULL && "expected call to succeed");
    char* choices = literllm_chat_completion_response_choices(result);
    char* choices_json = literllm_chat_completion_response_choices(result);
    assert(choices_json != NULL);
    char* choices_0_json = alef_json_array_get_index(choices_json, 0);
    char* message_json = alef_json_get_object(choices_0_json, "message");
    char* choices_0_message_content = alef_json_get_string(message_json, "content");
    char* choices_0_finish_reason = alef_json_get_string(choices_0_json, "finish_reason");
    char* model = literllm_chat_completion_response_model(result);
    {
        /* count_equals: count elements in array */
        assert(choices != NULL && "expected non-null collection JSON");
        int elem_count = alef_json_array_count(choices);
        assert(elem_count == 1 && "expected 1 elements");
    }
    assert(str_trim_eq(choices_0_message_content, "Hello! How can I help you today?") == 0 && "equals assertion failed");
    assert(str_trim_eq(choices_0_finish_reason, "stop") == 0 && "equals assertion failed");
    assert(str_trim_eq(model, "qwen2:0.5b") == 0 && "equals assertion failed");
    literllm_free_string(choices);
    free(choices_0_message_content);
    free(choices_0_finish_reason);
    literllm_free_string(model);
    free(message_json);
    free(choices_0_json);
    literllm_free_string(choices_json);
    literllm_chat_completion_response_free(result);
    literllm_chat_completion_request_free(chat_completion_request_handle);
    literllm_default_client_free(client);
}

void test_local_provider_vllm(void) {
    /* vLLM local provider routes requests via vllm/ model prefix with no auth */
    LITERLLMChatCompletionRequest* chat_completion_request_handle = literllm_chat_completion_request_from_json("{\"messages\":[{\"content\":\"Hello\",\"role\":\"user\"}],\"model\":\"vllm/meta-llama/Llama-3.2-1B\"}");
    assert(chat_completion_request_handle != NULL && "failed to build request");
    const char* mock_base = getenv("MOCK_SERVER_URL");
    assert(mock_base != NULL && "MOCK_SERVER_URL must be set");
    char base_url[1024];
    snprintf(base_url, sizeof(base_url), "%s/fixtures/local_provider_vllm", mock_base);
    LITERLLMDefaultClient* client = literllm_create_client("test-key", base_url, (uint64_t)-1, (uint32_t)-1, NULL);
    assert(client != NULL && "failed to create client");
    LITERLLMChatCompletionResponse* result = literllm_default_client_chat(client, chat_completion_request_handle);
    assert(result != NULL && "expected call to succeed");
    char* choices = literllm_chat_completion_response_choices(result);
    char* choices_json = literllm_chat_completion_response_choices(result);
    assert(choices_json != NULL);
    char* choices_0_json = alef_json_array_get_index(choices_json, 0);
    char* message_json = alef_json_get_object(choices_0_json, "message");
    char* choices_0_message_content = alef_json_get_string(message_json, "content");
    char* choices_0_finish_reason = alef_json_get_string(choices_0_json, "finish_reason");
    char* model = literllm_chat_completion_response_model(result);
    {
        /* count_equals: count elements in array */
        assert(choices != NULL && "expected non-null collection JSON");
        int elem_count = alef_json_array_count(choices);
        assert(elem_count == 1 && "expected 1 elements");
    }
    assert(str_trim_eq(choices_0_message_content, "Hello! How may I assist you?") == 0 && "equals assertion failed");
    assert(str_trim_eq(choices_0_finish_reason, "stop") == 0 && "equals assertion failed");
    assert(str_trim_eq(model, "meta-llama/Llama-3.2-1B") == 0 && "equals assertion failed");
    literllm_free_string(choices);
    free(choices_0_message_content);
    free(choices_0_finish_reason);
    literllm_free_string(model);
    free(message_json);
    free(choices_0_json);
    literllm_free_string(choices_json);
    literllm_chat_completion_response_free(result);
    literllm_chat_completion_request_free(chat_completion_request_handle);
    literllm_default_client_free(client);
}
