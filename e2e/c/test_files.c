/*
 * This file is auto-generated by alef — DO NOT EDIT.
 * alef:hash:8146ae9f5f4d39967cc3f3f31b7931b268b44e539a36ee2dc13aaf9e3e761a66
 * To regenerate: alef generate
 * To verify freshness: alef verify --exit-code
 */
/* E2e tests for category: files */

#include <assert.h>
#include <stdint.h>
#include <string.h>
#include <stdio.h>
#include <stdlib.h>
#include "liter_llm.h"
#include "test_runner.h"

void test_edge_file_empty_list(void) {
    /* List files when no files have been uploaded */
    const char* mock_base = getenv("MOCK_SERVER_URL");
    assert(mock_base != NULL && "MOCK_SERVER_URL must be set");
    char base_url[1024];
    snprintf(base_url, sizeof(base_url), "%s/fixtures/edge_file_empty_list", mock_base);
    LITERLLMDefaultClient* client = literllm_create_client("test-key", base_url, (uint64_t)-1, (uint32_t)-1, NULL);
    assert(client != NULL && "failed to create client");
    LITERLLMFileListResponse* result = literllm_default_client_list_files(client, NULL);
    assert(result != NULL && "expected call to succeed");
    char* data = literllm_file_list_response_data(result);
    {
        /* count_equals: count elements in array */
        assert(data != NULL && "expected non-null collection JSON");
        int elem_count = alef_json_array_count(data);
        assert(elem_count == 0 && "expected 0 elements");
    }
    literllm_free_string(data);
    literllm_file_list_response_free(result);
    literllm_default_client_free(client);
}

void test_edge_file_large_upload(void) {
    /* Upload a large file successfully */
    LITERLLMCreateFileRequest* create_file_request_handle = literllm_create_file_request_from_json("{\"file\":\"eyJwcm9tcHQiOiAibGFyZ2UgdHJhaW5pbmcgZGF0YSJ9Cg==\",\"filename\":\"large_training_data.jsonl\",\"purpose\":\"fine-tune\"}");
    assert(create_file_request_handle != NULL && "failed to build request");
    const char* mock_base = getenv("MOCK_SERVER_URL");
    assert(mock_base != NULL && "MOCK_SERVER_URL must be set");
    char base_url[1024];
    snprintf(base_url, sizeof(base_url), "%s/fixtures/edge_file_large_upload", mock_base);
    LITERLLMDefaultClient* client = literllm_create_client("test-key", base_url, (uint64_t)-1, (uint32_t)-1, NULL);
    assert(client != NULL && "failed to create client");
    LITERLLMFileObject* result = literllm_default_client_create_file(client, create_file_request_handle);
    assert(result != NULL && "expected call to succeed");
    char* id = literllm_file_object_id(result);
    assert(id != NULL && strlen(id) > 0 && "expected non-empty value");
    literllm_free_string(id);
    literllm_file_object_free(result);
    literllm_create_file_request_free(create_file_request_handle);
    literllm_default_client_free(client);
}

void test_error_file_auth_401(void) {
    /* 401 Unauthorized when listing files with invalid API key */
    const char* mock_base = getenv("MOCK_SERVER_URL");
    assert(mock_base != NULL && "MOCK_SERVER_URL must be set");
    char base_url[1024];
    snprintf(base_url, sizeof(base_url), "%s/fixtures/error_file_auth_401", mock_base);
    LITERLLMDefaultClient* client = literllm_create_client("test-key", base_url, (uint64_t)-1, (uint32_t)-1, NULL);
    assert(client != NULL && "failed to create client");
    LITERLLMFileListResponse* result = literllm_default_client_list_files(client, NULL);
    literllm_default_client_free(client);
    assert(result == NULL && "expected call to fail");
}

void test_error_file_bad_purpose(void) {
    /* 400 Bad Request when uploading a file with invalid purpose */
    LITERLLMCreateFileRequest* create_file_request_handle = literllm_create_file_request_from_json("{\"file\":\"data.jsonl\",\"purpose\":\"invalid-purpose\"}");
    if (create_file_request_handle == NULL) { return; }
    const char* mock_base = getenv("MOCK_SERVER_URL");
    assert(mock_base != NULL && "MOCK_SERVER_URL must be set");
    char base_url[1024];
    snprintf(base_url, sizeof(base_url), "%s/fixtures/error_file_bad_purpose", mock_base);
    LITERLLMDefaultClient* client = literllm_create_client("test-key", base_url, (uint64_t)-1, (uint32_t)-1, NULL);
    assert(client != NULL && "failed to create client");
    LITERLLMFileObject* result = literllm_default_client_create_file(client, create_file_request_handle);
    literllm_create_file_request_free(create_file_request_handle);
    literllm_default_client_free(client);
    assert(result == NULL && "expected call to fail");
}

void test_error_file_not_found(void) {
    /* 404 Not Found when retrieving a nonexistent file */
    const char* mock_base = getenv("MOCK_SERVER_URL");
    assert(mock_base != NULL && "MOCK_SERVER_URL must be set");
    char base_url[1024];
    snprintf(base_url, sizeof(base_url), "%s/fixtures/error_file_not_found", mock_base);
    LITERLLMDefaultClient* client = literllm_create_client("test-key", base_url, (uint64_t)-1, (uint32_t)-1, NULL);
    assert(client != NULL && "failed to create client");
    LITERLLMFileObject* result = literllm_default_client_retrieve_file(client, "file-nonexistent");
    literllm_default_client_free(client);
    assert(result == NULL && "expected call to fail");
}

void test_smoke_create_file(void) {
    /* Upload a file for use with the API */
    LITERLLMCreateFileRequest* create_file_request_handle = literllm_create_file_request_from_json("{\"file\":\"eyJwcm9tcHQiOiAiaGVsbG8ifQo=\",\"filename\":\"training_data.jsonl\",\"purpose\":\"fine-tune\"}");
    assert(create_file_request_handle != NULL && "failed to build request");
    const char* mock_base = getenv("MOCK_SERVER_URL");
    assert(mock_base != NULL && "MOCK_SERVER_URL must be set");
    char base_url[1024];
    snprintf(base_url, sizeof(base_url), "%s/fixtures/smoke_create_file", mock_base);
    LITERLLMDefaultClient* client = literllm_create_client("test-key", base_url, (uint64_t)-1, (uint32_t)-1, NULL);
    assert(client != NULL && "failed to create client");
    LITERLLMFileObject* result = literllm_default_client_create_file(client, create_file_request_handle);
    assert(result != NULL && "expected call to succeed");
    char* id = literllm_file_object_id(result);
    assert(id != NULL && strlen(id) > 0 && "expected non-empty value");
    literllm_free_string(id);
    literllm_file_object_free(result);
    literllm_create_file_request_free(create_file_request_handle);
    literllm_default_client_free(client);
}

void test_smoke_delete_file(void) {
    /* Delete an uploaded file */
    const char* mock_base = getenv("MOCK_SERVER_URL");
    assert(mock_base != NULL && "MOCK_SERVER_URL must be set");
    char base_url[1024];
    snprintf(base_url, sizeof(base_url), "%s/fixtures/smoke_delete_file", mock_base);
    LITERLLMDefaultClient* client = literllm_create_client("test-key", base_url, (uint64_t)-1, (uint32_t)-1, NULL);
    assert(client != NULL && "failed to create client");
    LITERLLMDeleteResponse* result = literllm_default_client_delete_file(client, "file-abc123");
    assert(result != NULL && "expected call to succeed");
    char* id = literllm_delete_response_id(result);
    int32_t deleted = literllm_delete_response_deleted(result);
    assert(id != NULL && strlen(id) > 0 && "expected non-empty value");
    assert(deleted == 1 && "equals assertion failed");
    literllm_free_string(id);
    literllm_delete_response_free(result);
    literllm_default_client_free(client);
}

void test_smoke_file_content(void) {
    /* Retrieve the content of an uploaded file */
    const char* mock_base = getenv("MOCK_SERVER_URL");
    assert(mock_base != NULL && "MOCK_SERVER_URL must be set");
    char base_url[1024];
    snprintf(base_url, sizeof(base_url), "%s/fixtures/smoke_file_content", mock_base);
    LITERLLMDefaultClient* client = literllm_create_client("test-key", base_url, (uint64_t)-1, (uint32_t)-1, NULL);
    assert(client != NULL && "failed to create client");
    uint8_t* out_ptr = NULL;
    uintptr_t out_len = 0;
    uintptr_t out_cap = 0;
    int32_t status = literllm_default_client_file_content(client, "file-abc123", &out_ptr, &out_len, &out_cap);
    assert(status == 0 && "expected call to succeed");
    assert(out_len > 0 && "expected non-empty value");
    literllm_free_bytes(out_ptr, out_len, out_cap);
    literllm_default_client_free(client);
}

void test_smoke_list_files(void) {
    /* List all uploaded files */
    const char* mock_base = getenv("MOCK_SERVER_URL");
    assert(mock_base != NULL && "MOCK_SERVER_URL must be set");
    char base_url[1024];
    snprintf(base_url, sizeof(base_url), "%s/fixtures/smoke_list_files", mock_base);
    LITERLLMDefaultClient* client = literllm_create_client("test-key", base_url, (uint64_t)-1, (uint32_t)-1, NULL);
    assert(client != NULL && "failed to create client");
    LITERLLMFileListResponse* result = literllm_default_client_list_files(client, NULL);
    assert(result != NULL && "expected call to succeed");
    char* data = literllm_file_list_response_data(result);
    {
        /* count_equals: count elements in array */
        assert(data != NULL && "expected non-null collection JSON");
        int elem_count = alef_json_array_count(data);
        assert(elem_count == 2 && "expected 2 elements");
    }
    literllm_free_string(data);
    literllm_file_list_response_free(result);
    literllm_default_client_free(client);
}

void test_smoke_retrieve_file(void) {
    /* Retrieve metadata for an uploaded file */
    const char* mock_base = getenv("MOCK_SERVER_URL");
    assert(mock_base != NULL && "MOCK_SERVER_URL must be set");
    char base_url[1024];
    snprintf(base_url, sizeof(base_url), "%s/fixtures/smoke_retrieve_file", mock_base);
    LITERLLMDefaultClient* client = literllm_create_client("test-key", base_url, (uint64_t)-1, (uint32_t)-1, NULL);
    assert(client != NULL && "failed to create client");
    LITERLLMFileObject* result = literllm_default_client_retrieve_file(client, "file-abc123");
    assert(result != NULL && "expected call to succeed");
    char* id = literllm_file_object_id(result);
    assert(id != NULL && strlen(id) > 0 && "expected non-empty value");
    literllm_free_string(id);
    literllm_file_object_free(result);
    literllm_default_client_free(client);
}
