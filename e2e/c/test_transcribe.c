/*
 * This file is auto-generated by alef — DO NOT EDIT.
// alef:hash:f49c13fb9389780886bbf38965afc090433acd4a7ac93b8f375f70de0ed8fe77
 * To regenerate: alef generate
 * To verify freshness: alef verify --exit-code
 * Issues & docs: https://github.com/kreuzberg-dev/alef
 */
/* E2e tests for category: transcribe */

#include <assert.h>
#include <string.h>
#include <stdio.h>
#include <stdlib.h>
#include "liter_llm.h"
#include "test_runner.h"

void test_edge_transcribe_empty_audio(void) {
    /* Transcription of a silent or empty audio file returns empty text */
    LITERLLMCreateTranscriptionRequest* create_transcription_request_handle = literllm_create_transcription_request_from_json("{\"file\":\"silence.mp3\",\"model\":\"whisper-1\"}");
    assert(create_transcription_request_handle != NULL && "failed to build request");
    LITERLLMDefaultClient* client = literllm_create_client("test-key", NULL, 0, 0, NULL);
    assert(client != NULL && "failed to create client");
    LITERLLMTranscriptionResponse* result = literllm_default_client_transcribe(client, create_transcription_request_handle);
    assert(result != NULL && "expected call to succeed");
    char* text = literllm_transcription_response_text(result);
    assert(strstr(text, "") != NULL && "expected to contain substring");
    literllm_free_string(text);
    literllm_transcription_response_free(result);
    literllm_create_transcription_request_free(create_transcription_request_handle);
    literllm_default_client_free(client);
}

void test_edge_transcribe_with_timestamps(void) {
    /* Transcription with verbose JSON response format including timestamp segments */
    LITERLLMCreateTranscriptionRequest* create_transcription_request_handle = literllm_create_transcription_request_from_json("{\"file\":\"audio.mp3\",\"model\":\"whisper-1\",\"response_format\":\"verbose_json\"}");
    assert(create_transcription_request_handle != NULL && "failed to build request");
    LITERLLMDefaultClient* client = literllm_create_client("test-key", NULL, 0, 0, NULL);
    assert(client != NULL && "failed to create client");
    LITERLLMTranscriptionResponse* result = literllm_default_client_transcribe(client, create_transcription_request_handle);
    assert(result != NULL && "expected call to succeed");
    char* text = literllm_transcription_response_text(result);
    char* segments = literllm_transcription_response_segments(result);
    char* segments_json = literllm_transcription_response_segments(result);
    assert(segments_json != NULL);
    char* segments_0_id = alef_json_get_string(segments_json, "0");
    assert(text != NULL && strlen(text) > 0 && "expected non-empty value");
    {
        /* count_equals: count elements in array */
        assert(segments != NULL && "expected non-null collection JSON");
        int elem_count = alef_json_array_count(segments);
        assert(elem_count == 3 && "expected 3 elements");
    }
    assert(strcmp(segments_0_id, 0) == 0 && "equals assertion failed");
    literllm_free_string(text);
    literllm_free_string(segments);
    free(segments_0_id);
    literllm_free_string(segments_json);
    literllm_transcription_response_free(result);
    literllm_create_transcription_request_free(create_transcription_request_handle);
    literllm_default_client_free(client);
}

void test_error_transcribe_auth_401(void) {
    /* 401 Unauthorized for transcription with invalid API key */
    LITERLLMCreateTranscriptionRequest* create_transcription_request_handle = literllm_create_transcription_request_from_json("{\"file\":\"audio.mp3\",\"model\":\"whisper-1\"}");
    assert(create_transcription_request_handle != NULL && "failed to build request");
    LITERLLMDefaultClient* client = literllm_create_client("test-key", NULL, 0, 0, NULL);
    assert(client != NULL && "failed to create client");
    LITERLLMTranscriptionResponse* result = literllm_default_client_transcribe(client, create_transcription_request_handle);
    literllm_create_transcription_request_free(create_transcription_request_handle);
    literllm_default_client_free(client);
    assert(result == NULL && "expected call to fail");
}

void test_error_transcribe_bad_format(void) {
    /* 400 Bad Request when audio format is unsupported */
    LITERLLMCreateTranscriptionRequest* create_transcription_request_handle = literllm_create_transcription_request_from_json("{\"file\":\"audio.xyz\",\"model\":\"whisper-1\"}");
    assert(create_transcription_request_handle != NULL && "failed to build request");
    LITERLLMDefaultClient* client = literllm_create_client("test-key", NULL, 0, 0, NULL);
    assert(client != NULL && "failed to create client");
    LITERLLMTranscriptionResponse* result = literllm_default_client_transcribe(client, create_transcription_request_handle);
    literllm_create_transcription_request_free(create_transcription_request_handle);
    literllm_default_client_free(client);
    assert(result == NULL && "expected call to fail");
}

void test_smoke_transcribe_basic(void) {
    /* Basic audio transcription */
    LITERLLMCreateTranscriptionRequest* create_transcription_request_handle = literllm_create_transcription_request_from_json("{\"file\":\"audio.mp3\",\"model\":\"whisper-1\"}");
    assert(create_transcription_request_handle != NULL && "failed to build request");
    LITERLLMDefaultClient* client = literllm_create_client("test-key", NULL, 0, 0, NULL);
    assert(client != NULL && "failed to create client");
    LITERLLMTranscriptionResponse* result = literllm_default_client_transcribe(client, create_transcription_request_handle);
    assert(result != NULL && "expected call to succeed");
    char* text = literllm_transcription_response_text(result);
    assert(strstr(text, "Hello, this is a test transcription.") != NULL && "expected to contain substring");
    literllm_free_string(text);
    literllm_transcription_response_free(result);
    literllm_create_transcription_request_free(create_transcription_request_handle);
    literllm_default_client_free(client);
}

void test_smoke_transcribe_with_language(void) {
    /* Audio transcription with explicit language hint */
    LITERLLMCreateTranscriptionRequest* create_transcription_request_handle = literllm_create_transcription_request_from_json("{\"file\":\"audio_de.mp3\",\"language\":\"de\",\"model\":\"whisper-1\"}");
    assert(create_transcription_request_handle != NULL && "failed to build request");
    LITERLLMDefaultClient* client = literllm_create_client("test-key", NULL, 0, 0, NULL);
    assert(client != NULL && "failed to create client");
    LITERLLMTranscriptionResponse* result = literllm_default_client_transcribe(client, create_transcription_request_handle);
    assert(result != NULL && "expected call to succeed");
    char* text = literllm_transcription_response_text(result);
    assert(strstr(text, "Hallo, dies ist ein Testtranskription.") != NULL && "expected to contain substring");
    literllm_free_string(text);
    literllm_transcription_response_free(result);
    literllm_create_transcription_request_free(create_transcription_request_handle);
    literllm_default_client_free(client);
}
