/*
 * This file is auto-generated by alef — DO NOT EDIT.
 * alef:hash:ba657952768c5f5d5944344aa80ee8eea5ebdd71e9ac890aa89428c3de49085b
 * To regenerate: alef generate
 * To verify freshness: alef verify --exit-code
 */
/* E2e tests for category: image-generate */

#include <assert.h>
#include <stdint.h>
#include <string.h>
#include <stdio.h>
#include <stdlib.h>
#include "liter_llm.h"
#include "test_runner.h"

void test_edge_image_b64_response(void) {
    /* Image generation returning base64-encoded data instead of URL */
    LITERLLMCreateImageRequest* create_image_request_handle = literllm_create_image_request_from_json("{\"model\":\"dall-e-3\",\"n\":1,\"prompt\":\"A blue circle\",\"response_format\":\"b64_json\",\"size\":\"1024x1024\"}");
    assert(create_image_request_handle != NULL && "failed to build request");
    const char* mock_base = getenv("MOCK_SERVER_URL");
    assert(mock_base != NULL && "MOCK_SERVER_URL must be set");
    char base_url[1024];
    snprintf(base_url, sizeof(base_url), "%s/fixtures/edge_image_b64_response", mock_base);
    LITERLLMDefaultClient* client = literllm_create_client("test-key", base_url, (uint64_t)-1, (uint32_t)-1, NULL);
    assert(client != NULL && "failed to create client");
    LITERLLMImagesResponse* result = literllm_default_client_image_generate(client, create_image_request_handle);
    assert(result != NULL && "expected call to succeed");
    char* data = literllm_images_response_data(result);
    char* data_json = literllm_images_response_data(result);
    assert(data_json != NULL);
    char* data_0_json = alef_json_array_get_index(data_json, 0);
    char* data_0_b64_json = alef_json_get_string(data_0_json, "b64_json");
    {
        /* count_equals: count elements in array */
        assert(data != NULL && "expected non-null collection JSON");
        int elem_count = alef_json_array_count(data);
        assert(elem_count == 1 && "expected 1 elements");
    }
    assert(data_0_b64_json != NULL && strlen(data_0_b64_json) > 0 && "expected non-empty value");
    literllm_free_string(data);
    free(data_0_b64_json);
    free(data_0_json);
    literllm_free_string(data_json);
    literllm_images_response_free(result);
    literllm_create_image_request_free(create_image_request_handle);
    literllm_default_client_free(client);
}

void test_edge_image_empty_prompt(void) {
    /* Image generation with an empty prompt returns 400 */
    LITERLLMCreateImageRequest* create_image_request_handle = literllm_create_image_request_from_json("{\"model\":\"dall-e-3\",\"n\":1,\"prompt\":\"\",\"size\":\"1024x1024\"}");
    if (create_image_request_handle == NULL) { return; }
    const char* mock_base = getenv("MOCK_SERVER_URL");
    assert(mock_base != NULL && "MOCK_SERVER_URL must be set");
    char base_url[1024];
    snprintf(base_url, sizeof(base_url), "%s/fixtures/edge_image_empty_prompt", mock_base);
    LITERLLMDefaultClient* client = literllm_create_client("test-key", base_url, (uint64_t)-1, (uint32_t)-1, NULL);
    assert(client != NULL && "failed to create client");
    LITERLLMImagesResponse* result = literllm_default_client_image_generate(client, create_image_request_handle);
    literllm_create_image_request_free(create_image_request_handle);
    literllm_default_client_free(client);
    assert(result == NULL && "expected call to fail");
}

void test_edge_image_multiple_n(void) {
    /* Image generation requesting multiple images with n=3 */
    LITERLLMCreateImageRequest* create_image_request_handle = literllm_create_image_request_from_json("{\"model\":\"dall-e-3\",\"n\":3,\"prompt\":\"A cat\"}");
    assert(create_image_request_handle != NULL && "failed to build request");
    const char* mock_base = getenv("MOCK_SERVER_URL");
    assert(mock_base != NULL && "MOCK_SERVER_URL must be set");
    char base_url[1024];
    snprintf(base_url, sizeof(base_url), "%s/fixtures/edge_image_multiple_n", mock_base);
    LITERLLMDefaultClient* client = literllm_create_client("test-key", base_url, (uint64_t)-1, (uint32_t)-1, NULL);
    assert(client != NULL && "failed to create client");
    LITERLLMImagesResponse* result = literllm_default_client_image_generate(client, create_image_request_handle);
    assert(result != NULL && "expected call to succeed");
    char* data = literllm_images_response_data(result);
    char* data_json = literllm_images_response_data(result);
    assert(data_json != NULL);
    char* data_0_json = alef_json_array_get_index(data_json, 0);
    char* data_0_url = alef_json_get_string(data_0_json, "url");
    char* data_1_json = alef_json_array_get_index(data_json, 1);
    char* data_1_url = alef_json_get_string(data_1_json, "url");
    char* data_2_json = alef_json_array_get_index(data_json, 2);
    char* data_2_url = alef_json_get_string(data_2_json, "url");
    {
        /* count_equals: count elements in array */
        assert(data != NULL && "expected non-null collection JSON");
        int elem_count = alef_json_array_count(data);
        assert(elem_count == 3 && "expected 3 elements");
    }
    assert(data_0_url != NULL && strlen(data_0_url) > 0 && "expected non-empty value");
    assert(data_1_url != NULL && strlen(data_1_url) > 0 && "expected non-empty value");
    assert(data_2_url != NULL && strlen(data_2_url) > 0 && "expected non-empty value");
    literllm_free_string(data);
    free(data_0_url);
    free(data_1_url);
    free(data_2_url);
    free(data_2_json);
    free(data_1_json);
    free(data_0_json);
    literllm_free_string(data_json);
    literllm_images_response_free(result);
    literllm_create_image_request_free(create_image_request_handle);
    literllm_default_client_free(client);
}

void test_error_image_auth_401(void) {
    /* 401 Unauthorized when generating images with invalid API key */
    LITERLLMCreateImageRequest* create_image_request_handle = literllm_create_image_request_from_json("{\"model\":\"dall-e-3\",\"n\":1,\"prompt\":\"A cat\",\"size\":\"1024x1024\"}");
    if (create_image_request_handle == NULL) { return; }
    const char* mock_base = getenv("MOCK_SERVER_URL");
    assert(mock_base != NULL && "MOCK_SERVER_URL must be set");
    char base_url[1024];
    snprintf(base_url, sizeof(base_url), "%s/fixtures/error_image_auth_401", mock_base);
    LITERLLMDefaultClient* client = literllm_create_client("test-key", base_url, (uint64_t)-1, (uint32_t)-1, NULL);
    assert(client != NULL && "failed to create client");
    LITERLLMImagesResponse* result = literllm_default_client_image_generate(client, create_image_request_handle);
    literllm_create_image_request_free(create_image_request_handle);
    literllm_default_client_free(client);
    assert(result == NULL && "expected call to fail");
}

void test_error_image_bad_request(void) {
    /* 400 Bad Request when image generation parameters are invalid */
    LITERLLMCreateImageRequest* create_image_request_handle = literllm_create_image_request_from_json("{\"model\":\"dall-e-3\",\"n\":1,\"prompt\":\"A cat\",\"size\":\"9999x9999\"}");
    if (create_image_request_handle == NULL) { return; }
    const char* mock_base = getenv("MOCK_SERVER_URL");
    assert(mock_base != NULL && "MOCK_SERVER_URL must be set");
    char base_url[1024];
    snprintf(base_url, sizeof(base_url), "%s/fixtures/error_image_bad_request", mock_base);
    LITERLLMDefaultClient* client = literllm_create_client("test-key", base_url, (uint64_t)-1, (uint32_t)-1, NULL);
    assert(client != NULL && "failed to create client");
    LITERLLMImagesResponse* result = literllm_default_client_image_generate(client, create_image_request_handle);
    literllm_create_image_request_free(create_image_request_handle);
    literllm_default_client_free(client);
    assert(result == NULL && "expected call to fail");
}

void test_error_image_rate_limit(void) {
    /* 429 Rate limit exceeded for image generation */
    LITERLLMCreateImageRequest* create_image_request_handle = literllm_create_image_request_from_json("{\"model\":\"dall-e-3\",\"n\":1,\"prompt\":\"A cat\",\"size\":\"1024x1024\"}");
    if (create_image_request_handle == NULL) { return; }
    const char* mock_base = getenv("MOCK_SERVER_URL");
    assert(mock_base != NULL && "MOCK_SERVER_URL must be set");
    char base_url[1024];
    snprintf(base_url, sizeof(base_url), "%s/fixtures/error_image_rate_limit", mock_base);
    LITERLLMDefaultClient* client = literllm_create_client("test-key", base_url, (uint64_t)-1, (uint32_t)-1, NULL);
    assert(client != NULL && "failed to create client");
    LITERLLMImagesResponse* result = literllm_default_client_image_generate(client, create_image_request_handle);
    literllm_create_image_request_free(create_image_request_handle);
    literllm_default_client_free(client);
    assert(result == NULL && "expected call to fail");
}

void test_smoke_image_basic(void) {
    /* Basic image generation with a text prompt */
    LITERLLMCreateImageRequest* create_image_request_handle = literllm_create_image_request_from_json("{\"model\":\"dall-e-3\",\"n\":1,\"prompt\":\"A white cat sitting on a windowsill\",\"size\":\"1024x1024\"}");
    assert(create_image_request_handle != NULL && "failed to build request");
    const char* mock_base = getenv("MOCK_SERVER_URL");
    assert(mock_base != NULL && "MOCK_SERVER_URL must be set");
    char base_url[1024];
    snprintf(base_url, sizeof(base_url), "%s/fixtures/smoke_image_basic", mock_base);
    LITERLLMDefaultClient* client = literllm_create_client("test-key", base_url, (uint64_t)-1, (uint32_t)-1, NULL);
    assert(client != NULL && "failed to create client");
    LITERLLMImagesResponse* result = literllm_default_client_image_generate(client, create_image_request_handle);
    assert(result != NULL && "expected call to succeed");
    char* data = literllm_images_response_data(result);
    char* data_json = literllm_images_response_data(result);
    assert(data_json != NULL);
    char* data_0_json = alef_json_array_get_index(data_json, 0);
    char* data_0_url = alef_json_get_string(data_0_json, "url");
    {
        /* count_equals: count elements in array */
        assert(data != NULL && "expected non-null collection JSON");
        int elem_count = alef_json_array_count(data);
        assert(elem_count == 1 && "expected 1 elements");
    }
    assert(data_0_url != NULL && strlen(data_0_url) > 0 && "expected non-empty value");
    literllm_free_string(data);
    free(data_0_url);
    free(data_0_json);
    literllm_free_string(data_json);
    literllm_images_response_free(result);
    literllm_create_image_request_free(create_image_request_handle);
    literllm_default_client_free(client);
}

void test_smoke_image_multiple(void) {
    /* Image generation requesting multiple images */
    LITERLLMCreateImageRequest* create_image_request_handle = literllm_create_image_request_from_json("{\"model\":\"dall-e-2\",\"n\":3,\"prompt\":\"A red bicycle\",\"size\":\"256x256\"}");
    assert(create_image_request_handle != NULL && "failed to build request");
    const char* mock_base = getenv("MOCK_SERVER_URL");
    assert(mock_base != NULL && "MOCK_SERVER_URL must be set");
    char base_url[1024];
    snprintf(base_url, sizeof(base_url), "%s/fixtures/smoke_image_multiple", mock_base);
    LITERLLMDefaultClient* client = literllm_create_client("test-key", base_url, (uint64_t)-1, (uint32_t)-1, NULL);
    assert(client != NULL && "failed to create client");
    LITERLLMImagesResponse* result = literllm_default_client_image_generate(client, create_image_request_handle);
    assert(result != NULL && "expected call to succeed");
    char* data = literllm_images_response_data(result);
    char* data_json = literllm_images_response_data(result);
    assert(data_json != NULL);
    char* data_0_json = alef_json_array_get_index(data_json, 0);
    char* data_0_url = alef_json_get_string(data_0_json, "url");
    {
        /* count_equals: count elements in array */
        assert(data != NULL && "expected non-null collection JSON");
        int elem_count = alef_json_array_count(data);
        assert(elem_count == 3 && "expected 3 elements");
    }
    assert(data_0_url != NULL && strlen(data_0_url) > 0 && "expected non-empty value");
    literllm_free_string(data);
    free(data_0_url);
    free(data_0_json);
    literllm_free_string(data_json);
    literllm_images_response_free(result);
    literllm_create_image_request_free(create_image_request_handle);
    literllm_default_client_free(client);
}

void test_smoke_image_with_size(void) {
    /* Image generation with explicit size parameter */
    LITERLLMCreateImageRequest* create_image_request_handle = literllm_create_image_request_from_json("{\"model\":\"dall-e-3\",\"n\":1,\"prompt\":\"A sunset over mountains\",\"size\":\"1792x1024\"}");
    assert(create_image_request_handle != NULL && "failed to build request");
    const char* mock_base = getenv("MOCK_SERVER_URL");
    assert(mock_base != NULL && "MOCK_SERVER_URL must be set");
    char base_url[1024];
    snprintf(base_url, sizeof(base_url), "%s/fixtures/smoke_image_with_size", mock_base);
    LITERLLMDefaultClient* client = literllm_create_client("test-key", base_url, (uint64_t)-1, (uint32_t)-1, NULL);
    assert(client != NULL && "failed to create client");
    LITERLLMImagesResponse* result = literllm_default_client_image_generate(client, create_image_request_handle);
    assert(result != NULL && "expected call to succeed");
    char* data = literllm_images_response_data(result);
    char* data_json = literllm_images_response_data(result);
    assert(data_json != NULL);
    char* data_0_json = alef_json_array_get_index(data_json, 0);
    char* data_0_url = alef_json_get_string(data_0_json, "url");
    {
        /* count_equals: count elements in array */
        assert(data != NULL && "expected non-null collection JSON");
        int elem_count = alef_json_array_count(data);
        assert(elem_count == 1 && "expected 1 elements");
    }
    assert(data_0_url != NULL && strlen(data_0_url) > 0 && "expected non-empty value");
    literllm_free_string(data);
    free(data_0_url);
    free(data_0_json);
    literllm_free_string(data_json);
    literllm_images_response_free(result);
    literllm_create_image_request_free(create_image_request_handle);
    literllm_default_client_free(client);
}
