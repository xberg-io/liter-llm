// This file is auto-generated by alef — DO NOT EDIT.
// alef:hash:ebd07be505b12da268a3b0f5adc65bb22a4e8675ac9d85d6495a7c4c7e76381e
// To regenerate: alef generate
// To verify freshness: alef verify --exit-code
// Issues & docs: https://github.com/kreuzberg-dev/alef
import gleeunit
import gleeunit/should
import liter_llm
import e2e_gleam

import gleam/list
import gleam/option
import gleam/string

// Response completes with empty output items
pub fn edge_response_empty_output_test() {
  // skipped: json_object arg requires typed record construction not yet supported in Gleam e2e
  Nil
}

// Response created with a very large input text
pub fn edge_response_large_input_test() {
  // skipped: json_object arg requires typed record construction not yet supported in Gleam e2e
  Nil
}

// 401 Unauthorized when creating a response with invalid API key
pub fn error_response_auth_401_test() {
  // skipped: json_object arg requires typed record construction not yet supported in Gleam e2e
  Nil
}

// 400 Bad Request when creating response with invalid model
pub fn error_response_bad_request_test() {
  // skipped: json_object arg requires typed record construction not yet supported in Gleam e2e
  Nil
}

// 404 Not Found when retrieving a nonexistent response
pub fn error_response_not_found_test() {
  let assert Ok(client) = liter_llm.create_client("test-key", option.None, option.None, option.None, option.None)
    let _ = client
  liter_llm.retrieve_response(client, "") |> should.be_error()
}

// Cancel an in-progress response
pub fn smoke_cancel_response_test() {
  let assert Ok(client) = liter_llm.create_client("test-key", option.None, option.None, option.None, option.None)
    let _ = client
  let result = liter_llm.cancel_response(client, "")
  result |> should.be_ok()
  let assert Ok(r) = result
  r.id |> string.is_empty |> should.equal(False)
  r.status |> should.equal("cancelled")
  r.output |> list.length |> should.equal(0)
}

// Create a basic response using the Responses API
pub fn smoke_create_response_test() {
  // skipped: json_object arg requires typed record construction not yet supported in Gleam e2e
  Nil
}

// Response that includes tool call output items
pub fn smoke_response_with_tools_test() {
  // skipped: json_object arg requires typed record construction not yet supported in Gleam e2e
  Nil
}

// Retrieve a previously created response
pub fn smoke_retrieve_response_test() {
  let assert Ok(client) = liter_llm.create_client("test-key", option.None, option.None, option.None, option.None)
    let _ = client
  let result = liter_llm.retrieve_response(client, "")
  result |> should.be_ok()
  let assert Ok(r) = result
  r.id |> string.is_empty |> should.equal(False)
  r.status |> should.equal("completed")
}
