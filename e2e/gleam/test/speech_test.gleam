// This file is auto-generated by alef — DO NOT EDIT.
// alef:hash:a4b53439f8be2a119a40c354ec19606bbcf6ff2d613b6a1fb0c42e1b77e68ce3
// To regenerate: alef generate
// To verify freshness: alef verify --exit-code
// Issues & docs: https://github.com/kreuzberg-dev/alef
import gleeunit
import gleeunit/should
import liter_llm
import e2e_gleam

import gleam/option
import gleam/string

// Text-to-speech with specific voice selection
pub fn edge_speech_all_voices_test() {
  let assert Ok(client) = liter_llm.create_client("test-key", "")
    let _ = client
  let result = liter_llm.speech(client, "Hello world")
  result |> should.be_ok()
  let assert Ok(r) = result
  r.audio |> string.is_empty |> should.equal(False)
}

// Speech generation with a very long input text
pub fn edge_speech_long_input_test() {
  let assert Ok(client) = liter_llm.create_client("test-key", "")
    let _ = client
  let result = liter_llm.speech(client, "This is a long input text. This is a long input text. This is a long input text. This is a long input text. This is a long input text. This is a long input text. This is a long input text. This is a long input text. This is a long input text. This is a long input text. This is a long input text. This is a long input text. This is a long input text. This is a long input text. This is a long input text. This is a long input text. This is a long input text. This is a long input text. This is a long input text. This is a long input text. This is a long input text. This is a long input text. This is a long input text. This is a long input text. This is a long input text. This is a long input text. This is a long input text. This is a long input text. This is a long input text. This is a long input text. This is a long input text. This is a long input text. This is a long input text. This is a long input text. This is a long input text. This is a long input text. This is a long input text. This is a long input text. This is a long input text. This is a long input text. This is a long input text. This is a long input text. This is a long input text. This is a long input text. This is a long input text. This is a long input text. This is a long input text. This is a long input text. This is a long input text. This is a long input text. End of input.")
  result |> should.be_ok()
  let assert Ok(r) = result
  r.audio |> string.is_empty |> should.equal(False)
}

// 401 Unauthorized for speech generation with invalid API key
pub fn error_speech_auth_401_test() {
  let assert Ok(client) = liter_llm.create_client("test-key", "")
    let _ = client
  liter_llm.speech(client, "Hello") |> should.be_error()
}

// 400 Bad Request for speech with unsupported model
pub fn error_speech_bad_model_test() {
  let assert Ok(client) = liter_llm.create_client("test-key", "")
    let _ = client
  liter_llm.speech(client, "Hello") |> should.be_error()
}

// Basic text-to-speech generation
pub fn smoke_speech_basic_test() {
  let assert Ok(client) = liter_llm.create_client("test-key", "")
    let _ = client
  let result = liter_llm.speech(client, "Hello, world!")
  result |> should.be_ok()
  let assert Ok(r) = result
  r.audio |> string.is_empty |> should.equal(False)
}

// Text-to-speech with explicit MP3 response format
pub fn smoke_speech_mp3_format_test() {
  let assert Ok(client) = liter_llm.create_client("test-key", "")
    let _ = client
  let result = liter_llm.speech(client, "The quick brown fox jumps over the lazy dog.")
  result |> should.be_ok()
  let assert Ok(r) = result
  r.audio |> string.is_empty |> should.equal(False)
}
