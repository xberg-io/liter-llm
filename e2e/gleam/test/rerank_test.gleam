// This file is auto-generated by alef — DO NOT EDIT.
// alef:hash:9569bd15217917e4a87f9ff7887465e29791601e174e5d65f817b84ab59ba479
// To regenerate: alef generate
// To verify freshness: alef verify --exit-code
// Issues & docs: https://github.com/kreuzberg-dev/alef
import gleeunit
import gleeunit/should
import liter_llm
import e2e_gleam

import gleam/list
import gleam/option

// Reranking with an empty query string
pub fn edge_rerank_empty_query_test() {
  let assert Ok(client) = liter_llm.create_client("test-key", "")
    let _ = client
  let result = liter_llm.rerank(client, "{}")
  result |> should.be_ok()
  let assert Ok(r) = result
  r.results |> list.length |> should.equal(2)
}

// Reranking with only a single document
pub fn edge_rerank_single_doc_test() {
  let assert Ok(client) = liter_llm.create_client("test-key", "")
    let _ = client
  let result = liter_llm.rerank(client, "{}")
  result |> should.be_ok()
  let assert Ok(r) = result
  r.results |> list.length |> should.equal(1)
  // skipped: array element field 'results[0].relevance_score' not yet supported in Gleam e2e
}

// 401 Unauthorized for reranking with invalid API key
pub fn error_rerank_auth_401_test() {
  let assert Ok(client) = liter_llm.create_client("test-key", "")
    let _ = client
  liter_llm.rerank(client, "{}") |> should.be_error()
}

// 400 Bad Request for reranking with invalid model
pub fn error_rerank_bad_request_test() {
  let assert Ok(client) = liter_llm.create_client("test-key", "")
    let _ = client
  liter_llm.rerank(client, "{}") |> should.be_error()
}

// Basic reranking of documents against a query
pub fn smoke_rerank_basic_test() {
  let assert Ok(client) = liter_llm.create_client("test-key", "")
    let _ = client
  let result = liter_llm.rerank(client, "{}")
  result |> should.be_ok()
  let assert Ok(r) = result
  r.results |> list.length |> should.equal(3)
  // skipped: array element field 'results[0].relevance_score' not yet supported in Gleam e2e
}

// Reranking with return_documents flag to include document text
pub fn smoke_rerank_return_docs_test() {
  let assert Ok(client) = liter_llm.create_client("test-key", "")
    let _ = client
  let result = liter_llm.rerank(client, "{}")
  result |> should.be_ok()
  let assert Ok(r) = result
  r.results |> list.length |> should.equal(2)
  // skipped: array element field 'results[0].document' not yet supported in Gleam e2e
  // skipped: array element field 'results[0].relevance_score' not yet supported in Gleam e2e
}

// Reranking with top_n parameter to limit results
pub fn smoke_rerank_with_top_n_test() {
  let assert Ok(client) = liter_llm.create_client("test-key", "")
    let _ = client
  let result = liter_llm.rerank(client, "{}")
  result |> should.be_ok()
  let assert Ok(r) = result
  r.results |> list.length |> should.equal(2)
  // skipped: array element field 'results[0].relevance_score' not yet supported in Gleam e2e
}
