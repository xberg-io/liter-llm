// This file is auto-generated by alef — DO NOT EDIT.
// alef:hash:9c7535f59b5946d77e8fa1f4d1ebbed53903dc992c51938d18b2083e1004eb78
// To regenerate: alef generate
// To verify freshness: alef verify --exit-code
// Issues & docs: https://github.com/kreuzberg-dev/alef
import gleeunit
import gleeunit/should
import liter_llm
import e2e_gleam

import gleam/list
import gleam/option
import gleam/string

// Transcription of a silent or empty audio file returns empty text
pub fn edge_transcribe_empty_audio_test() {
  let assert Ok(request_json__) = liter_llm.create_transcription_request_from_json("{}")
  let assert Ok(client) = liter_llm.create_client("test-key", option.None, option.None, option.None, option.None)
    let _ = client
  let result = liter_llm.transcribe(client, request_json__)
  result |> should.be_ok()
  let assert Ok(r) = result
  r.text |> string.contains("") |> should.equal(True)
}

// Transcription with verbose JSON response format including timestamp segments
pub fn edge_transcribe_with_timestamps_test() {
  let assert Ok(request_json__) = liter_llm.create_transcription_request_from_json("{}")
  let assert Ok(client) = liter_llm.create_client("test-key", option.None, option.None, option.None, option.None)
    let _ = client
  let result = liter_llm.transcribe(client, request_json__)
  result |> should.be_ok()
  let assert Ok(r) = result
  r.text |> string.is_empty |> should.equal(False)
  r.segments |> list.length |> should.equal(3)
  // skipped: array element field 'segments[0].id' not yet supported in Gleam e2e
}

// 401 Unauthorized for transcription with invalid API key
pub fn error_transcribe_auth_401_test() {
  let assert Ok(request_json__) = liter_llm.create_transcription_request_from_json("{}")
  let assert Ok(client) = liter_llm.create_client("test-key", option.None, option.None, option.None, option.None)
    let _ = client
  liter_llm.transcribe(client, request_json__) |> should.be_error()
}

// 400 Bad Request when audio format is unsupported
pub fn error_transcribe_bad_format_test() {
  let assert Ok(request_json__) = liter_llm.create_transcription_request_from_json("{}")
  let assert Ok(client) = liter_llm.create_client("test-key", option.None, option.None, option.None, option.None)
    let _ = client
  liter_llm.transcribe(client, request_json__) |> should.be_error()
}

// Basic audio transcription
pub fn smoke_transcribe_basic_test() {
  let assert Ok(request_json__) = liter_llm.create_transcription_request_from_json("{}")
  let assert Ok(client) = liter_llm.create_client("test-key", option.None, option.None, option.None, option.None)
    let _ = client
  let result = liter_llm.transcribe(client, request_json__)
  result |> should.be_ok()
  let assert Ok(r) = result
  r.text |> string.contains("Hello, this is a test transcription.") |> should.equal(True)
}

// Audio transcription with explicit language hint
pub fn smoke_transcribe_with_language_test() {
  let assert Ok(request_json__) = liter_llm.create_transcription_request_from_json("{}")
  let assert Ok(client) = liter_llm.create_client("test-key", option.None, option.None, option.None, option.None)
    let _ = client
  let result = liter_llm.transcribe(client, request_json__)
  result |> should.be_ok()
  let assert Ok(r) = result
  r.text |> string.contains("Hallo, dies ist ein Testtranskription.") |> should.equal(True)
}
