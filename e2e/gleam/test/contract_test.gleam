// This file is auto-generated by alef — DO NOT EDIT.
// alef:hash:a3b7c2cc9988ff5ddbcbb788ed4a9888d1504862bb83707dea9f6ccbe27947d8
// To regenerate: alef generate
// To verify freshness: alef verify --exit-code
// Issues & docs: https://github.com/kreuzberg-dev/alef
import gleeunit
import gleeunit/should
import liter_llm
import e2e_gleam

import gleam/list
import gleam/option

// Verify all bindings expose the full API surface — constructor accepts all config options and every method exists
pub fn binding_api_parity_test() {
  let assert Ok(client) = liter_llm.create_client("test-key", "")
    let _ = client
  let result = liter_llm.chat(client, "{}")
  result |> should.be_ok()
  let assert Ok(r) = result
  r.choices |> list.length |> should.equal(1)
  // skipped: array element field 'choices[0].message.content' not yet supported in Gleam e2e
  // skipped: array element field 'choices[0].finish_reason' not yet supported in Gleam e2e
  case r.usage {
    option.Some(opt_inner__) -> {
      // optional-prefix assertion 'equals' not yet implemented for Gleam
    }
    option.None -> should.fail()
  }
  r.model |> should.equal("gpt-4o")
}

// Verify ocr() method exists in all bindings
pub fn contract_ocr_test() {
  let assert Ok(client) = liter_llm.create_client("test-key", "")
    let _ = client
  let result = liter_llm.ocr(client, "{}")
  result |> should.be_ok()
  let assert Ok(r) = result
}

// Verify search() method exists in all bindings
pub fn contract_search_test() {
  let assert Ok(client) = liter_llm.create_client("test-key", "")
    let _ = client
  let result = liter_llm.search(client, "{}")
  result |> should.be_ok()
  let assert Ok(r) = result
}
