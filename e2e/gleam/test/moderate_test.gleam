// This file is auto-generated by alef — DO NOT EDIT.
// alef:hash:2e9306e2ec2007d83682b11e1c2a28e9ba306d94baf0a618b58c7149cdec7783
// To regenerate: alef generate
// To verify freshness: alef verify --exit-code
// Issues & docs: https://github.com/kreuzberg-dev/alef
import gleeunit
import gleeunit/should
import liter_llm
import e2e_gleam

import gleam/list
import gleam/option

// Moderation response with multiple categories flagged
pub fn edge_moderate_all_categories_test() {
  let assert Ok(client) = liter_llm.create_client("test-key", "")
    let _ = client
  let result = liter_llm.moderate(client, "Extremely harmful content targeting multiple categories")
  result |> should.be_ok()
  let assert Ok(r) = result
  r.results |> list.length |> should.equal(1)
  // skipped: array element field 'results[0].flagged' not yet supported in Gleam e2e
}

// Moderation with empty string input
pub fn edge_moderate_empty_input_test() {
  let assert Ok(client) = liter_llm.create_client("test-key", "")
    let _ = client
  let result = liter_llm.moderate(client, "")
  result |> should.be_ok()
  let assert Ok(r) = result
  r.results |> list.length |> should.equal(1)
  // skipped: array element field 'results[0].flagged' not yet supported in Gleam e2e
}

// 401 Unauthorized for moderation with invalid API key
pub fn error_moderate_auth_401_test() {
  let assert Ok(client) = liter_llm.create_client("test-key", "")
    let _ = client
  liter_llm.moderate(client, "Hello") |> should.be_error()
}

// 400 Bad Request for moderation with invalid model
pub fn error_moderate_bad_request_test() {
  let assert Ok(client) = liter_llm.create_client("test-key", "")
    let _ = client
  liter_llm.moderate(client, "Hello") |> should.be_error()
}

// Moderate multiple inputs in a single request
pub fn smoke_moderate_batch_test() {
  let assert Ok(client) = liter_llm.create_client("test-key", "")
    let _ = client
  let result = liter_llm.moderate(client, ["Hello world", "Nice weather today"])
  result |> should.be_ok()
  let assert Ok(r) = result
  r.results |> list.length |> should.equal(2)
  // skipped: array element field 'results[0].flagged' not yet supported in Gleam e2e
}

// Moderation detects flagged content
pub fn smoke_moderate_flagged_test() {
  let assert Ok(client) = liter_llm.create_client("test-key", "")
    let _ = client
  let result = liter_llm.moderate(client, "I want to hurt someone very badly")
  result |> should.be_ok()
  let assert Ok(r) = result
  r.results |> list.length |> should.equal(1)
  // skipped: array element field 'results[0].flagged' not yet supported in Gleam e2e
}

// Moderate a single non-flagged input
pub fn smoke_moderate_single_test() {
  let assert Ok(client) = liter_llm.create_client("test-key", "")
    let _ = client
  let result = liter_llm.moderate(client, "The weather is nice today.")
  result |> should.be_ok()
  let assert Ok(r) = result
  r.results |> list.length |> should.equal(1)
  // skipped: array element field 'results[0].flagged' not yet supported in Gleam e2e
}
