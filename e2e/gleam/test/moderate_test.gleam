// This file is auto-generated by alef — DO NOT EDIT.
// alef:hash:b427484fdf72b60209629138d714a596616e8ccffd4b21c072b5a21ca5f835ce
// To regenerate: alef generate
// To verify freshness: alef verify --exit-code
// Issues & docs: https://github.com/kreuzberg-dev/alef
import gleeunit
import gleeunit/should
import liter_llm
import e2e_gleam

import gleam/list
import gleam/option

// Moderation response with multiple categories flagged
pub fn edge_moderate_all_categories_test() {
  let assert Ok(request_json__) = liter_llm.moderation_request_from_json("\"Extremely harmful content targeting multiple categories\"")
  let assert Ok(client) = liter_llm.create_client("test-key", option.None, option.None, option.None, option.None)
    let _ = client
  let result = liter_llm.moderate(client, request_json__)
  result |> should.be_ok()
  let assert Ok(r) = result
  r.results |> list.length |> should.equal(1)
  // skipped: array element field 'results[0].flagged' not yet supported in Gleam e2e
}

// Moderation with empty string input
pub fn edge_moderate_empty_input_test() {
  let assert Ok(request_json__) = liter_llm.moderation_request_from_json("\"\"")
  let assert Ok(client) = liter_llm.create_client("test-key", option.None, option.None, option.None, option.None)
    let _ = client
  let result = liter_llm.moderate(client, request_json__)
  result |> should.be_ok()
  let assert Ok(r) = result
  r.results |> list.length |> should.equal(1)
  // skipped: array element field 'results[0].flagged' not yet supported in Gleam e2e
}

// 401 Unauthorized for moderation with invalid API key
pub fn error_moderate_auth_401_test() {
  let assert Ok(request_json__) = liter_llm.moderation_request_from_json("\"Hello\"")
  let assert Ok(client) = liter_llm.create_client("test-key", option.None, option.None, option.None, option.None)
    let _ = client
  liter_llm.moderate(client, request_json__) |> should.be_error()
}

// 400 Bad Request for moderation with invalid model
pub fn error_moderate_bad_request_test() {
  let assert Ok(request_json__) = liter_llm.moderation_request_from_json("\"Hello\"")
  let assert Ok(client) = liter_llm.create_client("test-key", option.None, option.None, option.None, option.None)
    let _ = client
  liter_llm.moderate(client, request_json__) |> should.be_error()
}

// Moderate multiple inputs in a single request
pub fn smoke_moderate_batch_test() {
  let assert Ok(request_json__) = liter_llm.moderation_request_from_json("[\"Hello world\",\"Nice weather today\"]")
  let assert Ok(client) = liter_llm.create_client("test-key", option.None, option.None, option.None, option.None)
    let _ = client
  let result = liter_llm.moderate(client, request_json__)
  result |> should.be_ok()
  let assert Ok(r) = result
  r.results |> list.length |> should.equal(2)
  // skipped: array element field 'results[0].flagged' not yet supported in Gleam e2e
}

// Moderation detects flagged content
pub fn smoke_moderate_flagged_test() {
  let assert Ok(request_json__) = liter_llm.moderation_request_from_json("\"I want to hurt someone very badly\"")
  let assert Ok(client) = liter_llm.create_client("test-key", option.None, option.None, option.None, option.None)
    let _ = client
  let result = liter_llm.moderate(client, request_json__)
  result |> should.be_ok()
  let assert Ok(r) = result
  r.results |> list.length |> should.equal(1)
  // skipped: array element field 'results[0].flagged' not yet supported in Gleam e2e
}

// Moderate a single non-flagged input
pub fn smoke_moderate_single_test() {
  let assert Ok(request_json__) = liter_llm.moderation_request_from_json("\"The weather is nice today.\"")
  let assert Ok(client) = liter_llm.create_client("test-key", option.None, option.None, option.None, option.None)
    let _ = client
  let result = liter_llm.moderate(client, request_json__)
  result |> should.be_ok()
  let assert Ok(r) = result
  r.results |> list.length |> should.equal(1)
  // skipped: array element field 'results[0].flagged' not yet supported in Gleam e2e
}
