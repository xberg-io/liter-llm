%% Generated by alef. Do not edit by hand.
%% Starts the liter_llm OTP application and all its dependencies.
%% Called by e2e_gleam_test.main/0 before gleeunit.main/0.
-module(e2e_startup).
-export([start_app/0]).

start_app() ->
    %% Elixir runtime must be started before liter_llm NIF init
    %% because Rustler uses Elixir.Application.app_dir/2 to locate the .so.
    %% Gracefully fall back when Elixir is not a runtime dependency.
    case application:ensure_all_started(elixir) of
        {ok, _} -> ok;
        {error, _} -> ok
    end,
    {ok, _} = application:ensure_all_started(liter_llm),
    nil.
