# This file is auto-generated by alef — DO NOT EDIT.
# alef:hash:a0b51aaa3077ce441d26ae96194817c1180a72558a5fe427db923450d60a1dde
# To regenerate: alef generate
# To verify freshness: alef verify --exit-code
# Issues & docs: https://github.com/kreuzberg-dev/alef
# frozen_string_literal: true

require 'liter_llm'
require 'json'
require 'spec_helper'

RSpec.describe 'configuration' do
  it 'custom_base_url: Client configured with a custom base URL routes all requests to that endpoint' do
    client = LiterLlm.create_client('test-key', ENV.fetch('MOCK_SERVER_URL') + '/fixtures/custom_base_url')
    result = client.chat_async(LiterLlm::ChatCompletionRequest.new(messages: [{ 'content' => 'Hello', 'role' => 'user' }], model: 'local-model'))
    expect(result.choices.length).to eq(1)
    expect(result.choices[0].message.content).to eq('Hi there!')
    expect(result.choices[0].finish_reason.to_s).to eq('stop')
    expect(result.model).to eq('local-model')

  end

  it 'extra_headers: Client configured with extra custom headers successfully completes a chat request' do
    client = LiterLlm.create_client('test-key', ENV.fetch('MOCK_SERVER_URL') + '/fixtures/extra_headers')
    result = client.chat_async(LiterLlm::ChatCompletionRequest.new(messages: [{ 'content' => 'Hello', 'role' => 'user' }], model: 'gpt-4'))
    expect(result.choices.length).to eq(1)
    expect(result.choices[0].finish_reason.to_s).to eq('stop')

  end

  it 'local_provider_llamacpp: llamacpp local provider routes requests via llamacpp/ model prefix with no auth' do
    client = LiterLlm.create_client('test-key', ENV.fetch('MOCK_SERVER_URL') + '/fixtures/local_provider_llamacpp')
    result = client.chat_async(LiterLlm::ChatCompletionRequest.new(messages: [{ 'content' => 'Hello', 'role' => 'user' }], model: 'llamacpp/my-model'))
    expect(result.choices.length).to eq(1)
    expect(result.choices[0].message.content).to eq('Hi there! I\'m running locally.')
    expect(result.choices[0].finish_reason.to_s).to eq('stop')
    expect(result.model).to eq('my-model')

  end

  it 'local_provider_ollama: Ollama local provider routes requests via ollama/ model prefix with no auth' do
    client = LiterLlm.create_client('test-key', ENV.fetch('MOCK_SERVER_URL') + '/fixtures/local_provider_ollama')
    result = client.chat_async(LiterLlm::ChatCompletionRequest.new(messages: [{ 'content' => 'Hello', 'role' => 'user' }], model: 'ollama/qwen2:0.5b'))
    expect(result.choices.length).to eq(1)
    expect(result.choices[0].message.content).to eq('Hello! How can I help you today?')
    expect(result.choices[0].finish_reason.to_s).to eq('stop')
    expect(result.model).to eq('qwen2:0.5b')

  end

  it 'local_provider_vllm: vLLM local provider routes requests via vllm/ model prefix with no auth' do
    client = LiterLlm.create_client('test-key', ENV.fetch('MOCK_SERVER_URL') + '/fixtures/local_provider_vllm')
    result = client.chat_async(LiterLlm::ChatCompletionRequest.new(messages: [{ 'content' => 'Hello', 'role' => 'user' }], model: 'vllm/meta-llama/Llama-3.2-1B'))
    expect(result.choices.length).to eq(1)
    expect(result.choices[0].message.content).to eq('Hello! How may I assist you?')
    expect(result.choices[0].finish_reason.to_s).to eq('stop')
    expect(result.model).to eq('meta-llama/Llama-3.2-1B')

  end
end
