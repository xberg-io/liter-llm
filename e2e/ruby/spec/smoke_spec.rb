# This file is auto-generated by alef — DO NOT EDIT.
# alef:hash:01ccde93a878d88c0540b27dba5c50a0d62baf78a0d96b885f4251b901ec6cc7
# To regenerate: alef generate
# To verify freshness: alef verify --exit-code
# Issues & docs: https://github.com/kreuzberg-dev/alef
# frozen_string_literal: true

require 'liter_llm'
require 'json'

RSpec.describe 'smoke' do
  it 'anthropic_chat: Basic chat completion via the Anthropic provider (claude-3-5-sonnet-20241022) with system and user messages' do
    client = LiterLlm.create_client('test-key', ENV.fetch('MOCK_SERVER_URL') + '/fixtures/anthropic_chat')
    result = client.chat(LiterLlm::ChatCompletionRequest.new(max_tokens: 16, messages: [{ 'content' => 'You are a helpful assistant.', 'role' => 'system' }, { 'content' => 'Say hello in one word.', 'role' => 'user' }], model: 'anthropic/claude-3-5-sonnet-20241022', temperature: 0))
    expect(result.choices.length).to eq(1)
    expect(result.choices[0].message.content).to eq('Hello!')
    expect(result.choices[0].finish_reason).to eq('stop')
    expect(result.model).to eq('claude-3-5-sonnet-20241022')
    expect(result.usage.total_tokens).to eq(23)

  end

  it 'azure_chat: Chat completion via Azure OpenAI with the azure/ prefix for provider routing — verifies the prefix is stripped before dispatching and the response is normalised to the standard OpenAI chat completion shape' do
    client = LiterLlm.create_client('test-key', ENV.fetch('MOCK_SERVER_URL') + '/fixtures/azure_chat')
    result = client.chat(LiterLlm::ChatCompletionRequest.new(max_tokens: 16, messages: [{ 'content' => 'Say hello', 'role' => 'user' }], model: 'azure/gpt-4', temperature: 0))
    expect(result.choices.length).to eq(1)
    expect(result.choices[0].message.content).to eq('Hello!')
    expect(result.choices[0].finish_reason).to eq('stop')
    expect(result.model).to eq('gpt-4')
    expect(result.usage.total_tokens).to eq(13)

  end

  it 'azure_embed: Embedding request via Azure OpenAI using the azure/ provider prefix — response follows the standard OpenAI embeddings shape that Azure returns unchanged' do
    client = LiterLlm.create_client('test-key', ENV.fetch('MOCK_SERVER_URL') + '/fixtures/azure_embed')
    result = client.embed(LiterLlm::EmbeddingRequest.new(input: 'Hello world', model: 'azure/text-embedding-ada-002'))
    expect(result.data.length).to eq(1)
    expect(result.data[0].embedding.length).to eq(1480)

  end

  it 'basic_chat: Basic chat completion with a single user message' do
    client = LiterLlm.create_client('test-key', ENV.fetch('MOCK_SERVER_URL') + '/fixtures/basic_chat')
    result = client.chat(LiterLlm::ChatCompletionRequest.new(messages: [{ 'content' => 'Say hello', 'role' => 'user' }], model: 'gpt-4', temperature: 0))
    expect(result.choices.length).to eq(1)
    expect(result.choices[0].message.content).to eq('Hello!')
    expect(result.choices[0].finish_reason).to eq('stop')
    expect(result.usage.total_tokens).to eq(15)
    expect(result.model).to eq('gpt-4')

  end

  it 'basic_embed: Basic embedding request for a single input string' do
    client = LiterLlm.create_client('test-key', ENV.fetch('MOCK_SERVER_URL') + '/fixtures/basic_embed')
    result = client.embed(LiterLlm::EmbeddingRequest.new(input: 'Hello world', model: 'text-embedding-3-small'))
    expect(result.data.length).to eq(1)
    expect(result.data[0].embedding.length).to eq(5)

  end

  it 'basic_list_models: List available models from the API' do
    client = LiterLlm.create_client('test-key', ENV.fetch('MOCK_SERVER_URL') + '/fixtures/basic_list_models')
    result = client.list_models()
    expect(result.data.length).to be >= 1

  end

  it 'bedrock_chat: Basic chat completion via the AWS Bedrock provider using the bedrock/ prefix for routing — verifies the prefix is stripped before dispatching and the Converse API response is normalised to the standard OpenAI chat completion shape' do
    client = LiterLlm.create_client('test-key', ENV.fetch('MOCK_SERVER_URL') + '/fixtures/bedrock_chat')
    result = client.chat(LiterLlm::ChatCompletionRequest.new(max_tokens: 16, messages: [{ 'content' => 'Say hello in one word.', 'role' => 'user' }], model: 'bedrock/anthropic.claude-3-sonnet-20240229-v1:0', temperature: 0))
    expect(result.choices.length).to eq(1)
    expect(result.choices[0].message.content).to eq('Hello!')
    expect(result.choices[0].finish_reason).to eq('stop')
    expect(result.model).to eq('anthropic.claude-3-sonnet-20240229-v1:0')
    expect(result.usage.total_tokens).to eq(15)

  end

  it 'github_copilot_chat: Basic chat completion via the GitHub Copilot provider (gpt-4o) with a single user message' do
    client = LiterLlm.create_client('test-key', ENV.fetch('MOCK_SERVER_URL') + '/fixtures/github_copilot_chat')
    result = client.chat(LiterLlm::ChatCompletionRequest.new(max_tokens: 16, messages: [{ 'content' => 'Say hello in one word.', 'role' => 'user' }], model: 'github_copilot/gpt-4o', temperature: 0))
    expect(result.choices.length).to eq(1)
    expect(result.choices[0].message.content).to eq('Hello!')
    expect(result.choices[0].finish_reason).to eq('stop')
    expect(result.model).to eq('gpt-4o')
    expect(result.usage.total_tokens).to eq(14)

  end

  it 'local_chat_ollama: Chat completion against local Ollama with qwen2:0.5b model' do
    client = LiterLlm.create_client('test-key', ENV.fetch('MOCK_SERVER_URL') + '/fixtures/local_chat_ollama')
    result = client.chat(LiterLlm::ChatCompletionRequest.new(max_tokens: 10, messages: [{ 'content' => 'Say hello in one word.', 'role' => 'user' }], model: 'ollama/qwen2:0.5b'))
    expect(result.choices.to_s).not_to be_empty

  end

  it 'local_list_models_ollama: List models from local Ollama instance' do
    client = LiterLlm.create_client('test-key', ENV.fetch('MOCK_SERVER_URL') + '/fixtures/local_list_models_ollama')
    result = client.list_models({ 'model' => 'ollama/any' })
    expect(result.data.length).to be >= 1

  end

  it 'smoke_cache_memory: Test in-memory caching by sending identical requests twice' do
    client = LiterLlm.create_client('test-key', ENV.fetch('MOCK_SERVER_URL') + '/fixtures/smoke_cache_memory')
    result = client.chat(LiterLlm::ChatCompletionRequest.new(max_tokens: 5, messages: [{ 'content' => 'What is 2+2? Answer with just the number.', 'role' => 'user' }], model: 'openai/gpt-4o-mini'))
    expect(result.choices.to_s).not_to be_empty

  end

  it 'smoke_chat_anthropic: Basic chat completion against real Anthropic API' do
    client = LiterLlm.create_client('test-key', ENV.fetch('MOCK_SERVER_URL') + '/fixtures/smoke_chat_anthropic')
    result = client.chat(LiterLlm::ChatCompletionRequest.new(max_tokens: 10, messages: [{ 'content' => 'Say hello in exactly one word.', 'role' => 'user' }], model: 'anthropic/claude-sonnet-4-20250514'))
    expect(result.choices.to_s).not_to be_empty
    expect(result.usage.to_s).not_to be_empty

  end

  it 'smoke_chat_gemini: Basic chat completion against real Google Gemini API' do
    client = LiterLlm.create_client('test-key', ENV.fetch('MOCK_SERVER_URL') + '/fixtures/smoke_chat_gemini')
    result = client.chat(LiterLlm::ChatCompletionRequest.new(max_tokens: 10, messages: [{ 'content' => 'Say hello in exactly one word.', 'role' => 'user' }], model: 'gemini/gemini-2.5-flash-lite'))
    expect(result.choices.to_s).not_to be_empty
    expect(result.usage.to_s).not_to be_empty

  end

  it 'smoke_chat_openai: Basic chat completion against real OpenAI API' do
    client = LiterLlm.create_client('test-key', ENV.fetch('MOCK_SERVER_URL') + '/fixtures/smoke_chat_openai')
    result = client.chat(LiterLlm::ChatCompletionRequest.new(max_tokens: 10, messages: [{ 'content' => 'Say hello in exactly one word.', 'role' => 'user' }], model: 'openai/gpt-4o-mini'))
    expect(result.choices.to_s).not_to be_empty
    expect(result.usage.to_s).not_to be_empty

  end

  it 'smoke_embed_openai: Embeddings request against real OpenAI API' do
    client = LiterLlm.create_client('test-key', ENV.fetch('MOCK_SERVER_URL') + '/fixtures/smoke_embed_openai')
    result = client.embed(LiterLlm::EmbeddingRequest.new(input: ['Hello world'], model: 'openai/text-embedding-3-small'))
    expect(result.data.to_s).not_to be_empty

  end

  it 'smoke_list_models_openai: List models against real OpenAI API' do
    client = LiterLlm.create_client('test-key', ENV.fetch('MOCK_SERVER_URL') + '/fixtures/smoke_list_models_openai')
    result = client.list_models({ 'model' => 'openai/gpt-4o-mini' })
    expect(result.data.length).to be >= 1

  end

  it 'smoke_provider_routing: Test provider routing by sending requests to OpenAI and Anthropic' do
    client = LiterLlm.create_client('test-key', ENV.fetch('MOCK_SERVER_URL') + '/fixtures/smoke_provider_routing')
    result = client.chat(LiterLlm::ChatCompletionRequest.new(max_tokens: 5, messages: [{ 'content' => 'Say hi.', 'role' => 'user' }], model: 'openai/gpt-4o-mini'))
    expect(result.choices.to_s).not_to be_empty

  end

  it 'smoke_streaming_openai: Chat streaming against real OpenAI API, verifies chunks received' do
    skip 'Non-HTTP fixture cannot be tested via Net::HTTP'
  end

  it 'vertex_chat: Basic chat completion via the Google Vertex AI provider using the vertex_ai/ prefix for routing — verifies the prefix is stripped before dispatching and the Gemini response is normalised to the standard OpenAI chat completion shape' do
    client = LiterLlm.create_client('test-key', ENV.fetch('MOCK_SERVER_URL') + '/fixtures/vertex_chat')
    result = client.chat(LiterLlm::ChatCompletionRequest.new(max_tokens: 16, messages: [{ 'content' => 'Say hello in one word.', 'role' => 'user' }], model: 'vertex_ai/gemini-2.0-flash', temperature: 0))
    expect(result.choices.length).to eq(1)
    expect(result.choices[0].message.content).to eq('Hello!')
    expect(result.choices[0].finish_reason).to eq('stop')
    expect(result.model).to eq('gemini-2.0-flash')
    expect(result.usage.total_tokens).to eq(13)

  end

  it 'vertex_embed: Embedding request via Google Vertex AI using the vertex_ai/ provider prefix and the text-embedding-005 model — response follows the standard OpenAI embeddings shape' do
    client = LiterLlm.create_client('test-key', ENV.fetch('MOCK_SERVER_URL') + '/fixtures/vertex_embed')
    result = client.embed(LiterLlm::EmbeddingRequest.new(input: 'Hello', model: 'vertex_ai/text-embedding-005'))
    expect(result.data.length).to eq(1)
    expect(result.data[0].embedding.length).to eq(160)

  end
end
