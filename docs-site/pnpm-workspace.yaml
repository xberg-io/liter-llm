# Standalone workspace root so `pnpm install` here does not join the repo-root
# napi workspace; docs-site keeps its own isolated pnpm-lock.yaml.
allowBuilds:
  esbuild: true
  sharp: true
minimumReleaseAgeExclude:
  - "@xberg-io/docs-theme@0.1.0 || 0.2.0"
