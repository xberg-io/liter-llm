// This file is auto-generated by alef — DO NOT EDIT.
// alef:hash:de9738d1a2c1e8c5eacecbd4da06878683f3650e2b3bcc2ce0cb163d94f425f0
// To regenerate: alef generate
// To verify freshness: alef verify --exit-code
// Issues & docs: https://github.com/sample_crate-dev/alef
//! E2e tests for category: tool-calling

use liter_llm::BatchClient;
use liter_llm::FileClient;
use liter_llm::LlmClient;
use liter_llm::ResponseClient;
mod common;
mod mock_server;
#[allow(unused_imports)]
use mock_server::{MockRoute, MockServer};

#[tokio::test]
async fn test_anthropic_tool_calling() {
    // Chat request to Anthropic provider with a tool definition; assistant responds with a tool call
    let mock_route = MockRoute {
        path: "/chat/completions",
        method: "POST",
        status: 200,
        body: r#"{"choices":[{"finish_reason":"tool_calls","index":0,"message":{"content":null,"role":"assistant","tool_calls":[{"function":{"arguments":"{\"location\": \"London, UK\", \"unit\": \"celsius\"}","name":"get_weather"},"id":"toolu_01abc123","type":"function"}]}}],"created":1711000300,"id":"chatcmpl-anthropic-tool001","model":"claude-3-5-sonnet-20241022","object":"chat.completion","usage":{"completion_tokens":22,"prompt_tokens":95,"total_tokens":117}}"#.to_string(),
        is_streaming: false,
        stream_chunks: vec![],
        headers: vec![
        ],
        delay_ms: None,
    };
    let mock_server = MockServer::start(vec![mock_route]).await;
    let request_json: serde_json::Value = serde_json::from_str(r#"{"max_tokens":256,"messages":[{"content":"What is the weather in London?","role":"user"}],"model":"anthropic/claude-3-5-sonnet-20241022","tool_choice":"auto","tools":[{"function":{"description":"Get the current weather for a given location","name":"get_weather","parameters":{"properties":{"location":{"description":"The city and country, e.g. London, UK","type":"string"},"unit":{"description":"The temperature unit to use","enum":["celsius","fahrenheit"],"type":"string"}},"required":["location"],"type":"object"}},"type":"function"}]}"#).unwrap();
    let request = serde_json::from_value(request_json).unwrap();
    let client =
        liter_llm::create_client("test-key".to_string(), Some(mock_server.url.clone()), None, None, None).unwrap();
    let result = client.chat(request).await.expect("should succeed");
    let _choices_0_finish_reason = result.choices[0]
        .finish_reason
        .as_ref()
        .map(|v| v.to_string())
        .unwrap_or_default();
    assert_eq!(
        result.choices.len(),
        1,
        "expected exactly 1 elements, got {}",
        result.choices.len()
    );
    assert!(
        result.choices[0]
            .message
            .tool_calls
            .as_ref()
            .is_some_and(|v| !v.is_empty()),
        "expected choices[0].message.tool_calls to be present and non-empty"
    );
    assert_eq!(
        result.choices[0].message.tool_calls.as_ref().unwrap()[0]
            .function
            .name
            .to_string()
            .as_str()
            .trim(),
        r#"get_weather"#,
        "equals assertion failed"
    );
    assert_eq!(
        _choices_0_finish_reason.to_string().as_str().trim(),
        r#"tool_calls"#,
        "equals assertion failed"
    );
}

#[tokio::test]
async fn test_single_tool_call() {
    // Chat request with a tool definition; assistant responds with a tool call
    let mock_route = MockRoute {
        path: "/chat/completions",
        method: "POST",
        status: 200,
        body: r#"{"choices":[{"finish_reason":"tool_calls","index":0,"message":{"content":null,"role":"assistant","tool_calls":[{"function":{"arguments":"{\"location\": \"San Francisco, CA\", \"unit\": \"fahrenheit\"}","name":"get_weather"},"id":"call_abc123","type":"function"}]}}],"created":1711000002,"id":"chatcmpl-tool001","model":"gpt-4","object":"chat.completion","usage":{"completion_tokens":17,"prompt_tokens":82,"total_tokens":99}}"#.to_string(),
        is_streaming: false,
        stream_chunks: vec![],
        headers: vec![
        ],
        delay_ms: None,
    };
    let mock_server = MockServer::start(vec![mock_route]).await;
    let request_json: serde_json::Value = serde_json::from_str(r#"{"messages":[{"content":"What is the weather in San Francisco?","role":"user"}],"model":"gpt-4","tool_choice":"auto","tools":[{"function":{"description":"Get the current weather for a given location","name":"get_weather","parameters":{"properties":{"location":{"description":"The city and state, e.g. San Francisco, CA","type":"string"},"unit":{"description":"The temperature unit to use","enum":["celsius","fahrenheit"],"type":"string"}},"required":["location"],"type":"object"}},"type":"function"}]}"#).unwrap();
    let request = serde_json::from_value(request_json).unwrap();
    let client =
        liter_llm::create_client("test-key".to_string(), Some(mock_server.url.clone()), None, None, None).unwrap();
    let result = client.chat(request).await.expect("should succeed");
    let _choices_0_finish_reason = result.choices[0]
        .finish_reason
        .as_ref()
        .map(|v| v.to_string())
        .unwrap_or_default();
    assert_eq!(
        result.choices.len(),
        1,
        "expected exactly 1 elements, got {}",
        result.choices.len()
    );
    assert!(
        result.choices[0]
            .message
            .tool_calls
            .as_ref()
            .is_some_and(|v| !v.is_empty()),
        "expected choices[0].message.tool_calls to be present and non-empty"
    );
    assert_eq!(
        result.choices[0].message.tool_calls.as_ref().unwrap()[0]
            .function
            .name
            .to_string()
            .as_str()
            .trim(),
        r#"get_weather"#,
        "equals assertion failed"
    );
    assert_eq!(
        _choices_0_finish_reason.to_string().as_str().trim(),
        r#"tool_calls"#,
        "equals assertion failed"
    );
}
