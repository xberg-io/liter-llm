// This file is auto-generated by alef — DO NOT EDIT.
// alef:hash:1c1a4f40437bd3664405b1b60a53b4dc867f8b6f298baac7d0bf32a63ff9e517
// To regenerate: alef generate
// To verify freshness: alef verify --exit-code
//! E2e tests for category: search

use liter_llm::BatchClient;
use liter_llm::FileClient;
use liter_llm::LlmClient;
use liter_llm::ResponseClient;
mod common;
mod mock_server;
#[allow(unused_imports)]
use mock_server::{MockRoute, MockServer};

#[tokio::test]
async fn test_search_basic() {
    // Basic web search request with a simple query
    let mock_route = MockRoute {
        path: "/search",
        method: "POST",
        status: 200,
        body: r#"{"model":"brave/web-search","results":[{"snippet":"A language empowering everyone to build reliable and efficient software.","title":"Rust Programming Language","url":"https://www.rust-lang.org"}]}"#.to_string(),
        is_streaming: false,
        stream_chunks: vec![],
        headers: vec![
        ],
        delay_ms: None,
    };
    let mock_server = MockServer::start(vec![mock_route]).await;
    let request_json: serde_json::Value =
        serde_json::from_str(r#"{"model":"brave/web-search","query":"What is Rust programming language?"}"#).unwrap();
    let request = serde_json::from_value(request_json).unwrap();
    let client =
        liter_llm::create_client("test-key".to_string(), Some(mock_server.url.clone()), None, None, None).unwrap();
    let _ = client.search(request).await.expect("should succeed");
}

#[tokio::test]
async fn test_search_empty_results() {
    // Web search with a query that returns no results
    let mock_route = MockRoute {
        path: "/search",
        method: "POST",
        status: 200,
        body: r#"{"model":"brave/web-search","results":[]}"#.to_string(),
        is_streaming: false,
        stream_chunks: vec![],
        headers: vec![],
        delay_ms: None,
    };
    let mock_server = MockServer::start(vec![mock_route]).await;
    let request_json: serde_json::Value =
        serde_json::from_str(r#"{"model":"brave/web-search","query":"xyznonexistent12345xyz"}"#).unwrap();
    let request = serde_json::from_value(request_json).unwrap();
    let client =
        liter_llm::create_client("test-key".to_string(), Some(mock_server.url.clone()), None, None, None).unwrap();
    let _ = client.search(request).await.expect("should succeed");
}

#[tokio::test]
async fn test_search_error_400() {
    // 400 Bad Request error when search query is empty
    let mock_route = MockRoute {
        path: "/search",
        method: "POST",
        status: 400,
        body: r#"{"error":{"code":"invalid_query","message":"Query cannot be empty","type":"invalid_request_error"}}"#
            .to_string(),
        is_streaming: false,
        stream_chunks: vec![],
        headers: vec![],
        delay_ms: None,
    };
    let mock_server = MockServer::start(vec![mock_route]).await;
    let request_json: serde_json::Value = serde_json::from_str(r#"{"model":"brave/web-search","query":""}"#).unwrap();
    let request = serde_json::from_value(request_json).unwrap();
    let client =
        liter_llm::create_client("test-key".to_string(), Some(mock_server.url.clone()), None, None, None).unwrap();
    let result = client.search(request).await;
    assert!(result.is_err(), "expected call to fail");
    {
        let __e = result.as_ref().err().unwrap();
        assert!(
            format!("{:?}", __e).contains("BadRequest") || __e.to_string().contains("BadRequest"),
            "error message mismatch"
        );
    }
    assert_eq!(
        {
            let __err = result.as_ref().err().unwrap();
            __err.status_code()
        },
        400,
        "equals assertion failed"
    );
}

#[tokio::test]
async fn test_search_error_401() {
    // 401 Unauthorized error on web search due to invalid API credentials
    let mock_route = MockRoute {
        path: "/search",
        method: "POST",
        status: 401,
        body: r#"{"error":{"code":"invalid_api_key","message":"Invalid API key","type":"invalid_request_error"}}"#
            .to_string(),
        is_streaming: false,
        stream_chunks: vec![],
        headers: vec![],
        delay_ms: None,
    };
    let mock_server = MockServer::start(vec![mock_route]).await;
    let request_json: serde_json::Value =
        serde_json::from_str(r#"{"model":"brave/web-search","query":"test"}"#).unwrap();
    let request = serde_json::from_value(request_json).unwrap();
    let client =
        liter_llm::create_client("test-key".to_string(), Some(mock_server.url.clone()), None, None, None).unwrap();
    let result = client.search(request).await;
    assert!(result.is_err(), "expected call to fail");
    {
        let __e = result.as_ref().err().unwrap();
        assert!(
            format!("{:?}", __e).contains("Authentication") || __e.to_string().contains("Authentication"),
            "error message mismatch"
        );
    }
    assert_eq!(
        {
            let __err = result.as_ref().err().unwrap();
            __err.status_code()
        },
        401,
        "equals assertion failed"
    );
}

#[tokio::test]
async fn test_search_with_max_results() {
    // Search request with max_results parameter limiting response count
    let mock_route = MockRoute {
        path: "/search",
        method: "POST",
        status: 200,
        body: r#"{"model":"brave/web-search","results":[{"snippet":"Empowering everyone to build reliable and efficient software.","title":"The Rust Programming Language","url":"https://www.rust-lang.org"},{"snippet":"Learn Rust through practical examples.","title":"Rust by Example","url":"https://doc.rust-lang.org/rust-by-example"}]}"#.to_string(),
        is_streaming: false,
        stream_chunks: vec![],
        headers: vec![
        ],
        delay_ms: None,
    };
    let mock_server = MockServer::start(vec![mock_route]).await;
    let request_json: serde_json::Value =
        serde_json::from_str(r#"{"max_results":2,"model":"brave/web-search","query":"Rust programming"}"#).unwrap();
    let request = serde_json::from_value(request_json).unwrap();
    let client =
        liter_llm::create_client("test-key".to_string(), Some(mock_server.url.clone()), None, None, None).unwrap();
    let result = client.search(request).await.expect("should succeed");
    assert_eq!(
        result.results.len(),
        2,
        "expected exactly 2 elements, got {}",
        result.results.len()
    );
}
