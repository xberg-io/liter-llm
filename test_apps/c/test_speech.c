/*
 * This file is auto-generated by alef — DO NOT EDIT.
 * alef:hash:5f17ebe676822cfa77ea15c5d293e0cac18cf5c5fd2e4c3c2b8974a130abfd16
 * To regenerate: alef generate
 * To verify freshness: alef verify --exit-code
 */
/* E2e tests for category: speech */

#include <assert.h>
#include <stdint.h>
#include <string.h>
#include <stdio.h>
#include <stdlib.h>
#include "liter_llm.h"
#include "test_runner.h"

void test_edge_speech_all_voices(void) {
    /* Text-to-speech with specific voice selection */
    LITERLLMCreateSpeechRequest* create_speech_request_handle = literllm_create_speech_request_from_json("{\"input\":\"Hello world\",\"model\":\"tts-1\",\"voice\":\"nova\"}");
    assert(create_speech_request_handle != NULL && "failed to build request");
    const char* mock_base = getenv("MOCK_SERVER_URL");
    assert(mock_base != NULL && "MOCK_SERVER_URL must be set");
    char base_url[1024];
    snprintf(base_url, sizeof(base_url), "%s/fixtures/edge_speech_all_voices", mock_base);
    LITERLLMDefaultClient* client = literllm_create_client("test-key", base_url, (uint64_t)-1, (uint32_t)-1, NULL);
    assert(client != NULL && "failed to create client");
    uint8_t* out_ptr = NULL;
    uintptr_t out_len = 0;
    uintptr_t out_cap = 0;
    int32_t status = literllm_default_client_speech(client, create_speech_request_handle, &out_ptr, &out_len, &out_cap);
    assert(status == 0 && "expected call to succeed");
    assert(out_len > 0 && "expected non-empty value");
    literllm_free_bytes(out_ptr, out_len, out_cap);
    literllm_create_speech_request_free(create_speech_request_handle);
    literllm_default_client_free(client);
}

void test_edge_speech_long_input(void) {
    /* Speech generation with a very long input text */
    LITERLLMCreateSpeechRequest* create_speech_request_handle = literllm_create_speech_request_from_json("{\"input\":\"This is a long input text. This is a long input text. This is a long input text. This is a long input text. This is a long input text. This is a long input text. This is a long input text. This is a long input text. This is a long input text. This is a long input text. This is a long input text. This is a long input text. This is a long input text. This is a long input text. This is a long input text. This is a long input text. This is a long input text. This is a long input text. This is a long input text. This is a long input text. This is a long input text. This is a long input text. This is a long input text. This is a long input text. This is a long input text. This is a long input text. This is a long input text. This is a long input text. This is a long input text. This is a long input text. This is a long input text. This is a long input text. This is a long input text. This is a long input text. This is a long input text. This is a long input text. This is a long input text. This is a long input text. This is a long input text. This is a long input text. This is a long input text. This is a long input text. This is a long input text. This is a long input text. This is a long input text. This is a long input text. This is a long input text. This is a long input text. This is a long input text. This is a long input text. End of input.\",\"model\":\"tts-1\",\"voice\":\"echo\"}");
    assert(create_speech_request_handle != NULL && "failed to build request");
    const char* mock_base = getenv("MOCK_SERVER_URL");
    assert(mock_base != NULL && "MOCK_SERVER_URL must be set");
    char base_url[1024];
    snprintf(base_url, sizeof(base_url), "%s/fixtures/edge_speech_long_input", mock_base);
    LITERLLMDefaultClient* client = literllm_create_client("test-key", base_url, (uint64_t)-1, (uint32_t)-1, NULL);
    assert(client != NULL && "failed to create client");
    uint8_t* out_ptr = NULL;
    uintptr_t out_len = 0;
    uintptr_t out_cap = 0;
    int32_t status = literllm_default_client_speech(client, create_speech_request_handle, &out_ptr, &out_len, &out_cap);
    assert(status == 0 && "expected call to succeed");
    assert(out_len > 0 && "expected non-empty value");
    literllm_free_bytes(out_ptr, out_len, out_cap);
    literllm_create_speech_request_free(create_speech_request_handle);
    literllm_default_client_free(client);
}

void test_error_speech_auth_401(void) {
    /* 401 Unauthorized for speech generation with invalid API key */
    LITERLLMCreateSpeechRequest* create_speech_request_handle = literllm_create_speech_request_from_json("{\"input\":\"Hello\",\"model\":\"tts-1\",\"voice\":\"alloy\"}");
    if (create_speech_request_handle == NULL) { return; }
    const char* mock_base = getenv("MOCK_SERVER_URL");
    assert(mock_base != NULL && "MOCK_SERVER_URL must be set");
    char base_url[1024];
    snprintf(base_url, sizeof(base_url), "%s/fixtures/error_speech_auth_401", mock_base);
    LITERLLMDefaultClient* client = literllm_create_client("test-key", base_url, (uint64_t)-1, (uint32_t)-1, NULL);
    assert(client != NULL && "failed to create client");
    uint8_t* out_ptr = NULL;
    uintptr_t out_len = 0;
    uintptr_t out_cap = 0;
    int32_t status = literllm_default_client_speech(client, create_speech_request_handle, &out_ptr, &out_len, &out_cap);
    literllm_create_speech_request_free(create_speech_request_handle);
    literllm_default_client_free(client);
    assert(status != 0 && "expected call to fail");
    literllm_free_bytes(out_ptr, out_len, out_cap);
}

void test_error_speech_bad_model(void) {
    /* 400 Bad Request for speech with unsupported model */
    LITERLLMCreateSpeechRequest* create_speech_request_handle = literllm_create_speech_request_from_json("{\"input\":\"Hello\",\"model\":\"tts-nonexistent\",\"voice\":\"alloy\"}");
    if (create_speech_request_handle == NULL) { return; }
    const char* mock_base = getenv("MOCK_SERVER_URL");
    assert(mock_base != NULL && "MOCK_SERVER_URL must be set");
    char base_url[1024];
    snprintf(base_url, sizeof(base_url), "%s/fixtures/error_speech_bad_model", mock_base);
    LITERLLMDefaultClient* client = literllm_create_client("test-key", base_url, (uint64_t)-1, (uint32_t)-1, NULL);
    assert(client != NULL && "failed to create client");
    uint8_t* out_ptr = NULL;
    uintptr_t out_len = 0;
    uintptr_t out_cap = 0;
    int32_t status = literllm_default_client_speech(client, create_speech_request_handle, &out_ptr, &out_len, &out_cap);
    literllm_create_speech_request_free(create_speech_request_handle);
    literllm_default_client_free(client);
    assert(status != 0 && "expected call to fail");
    literllm_free_bytes(out_ptr, out_len, out_cap);
}

void test_smoke_speech_basic(void) {
    /* Basic text-to-speech generation */
    LITERLLMCreateSpeechRequest* create_speech_request_handle = literllm_create_speech_request_from_json("{\"input\":\"Hello, world!\",\"model\":\"tts-1\",\"voice\":\"alloy\"}");
    assert(create_speech_request_handle != NULL && "failed to build request");
    const char* mock_base = getenv("MOCK_SERVER_URL");
    assert(mock_base != NULL && "MOCK_SERVER_URL must be set");
    char base_url[1024];
    snprintf(base_url, sizeof(base_url), "%s/fixtures/smoke_speech_basic", mock_base);
    LITERLLMDefaultClient* client = literllm_create_client("test-key", base_url, (uint64_t)-1, (uint32_t)-1, NULL);
    assert(client != NULL && "failed to create client");
    uint8_t* out_ptr = NULL;
    uintptr_t out_len = 0;
    uintptr_t out_cap = 0;
    int32_t status = literllm_default_client_speech(client, create_speech_request_handle, &out_ptr, &out_len, &out_cap);
    assert(status == 0 && "expected call to succeed");
    assert(out_len > 0 && "expected non-empty value");
    literllm_free_bytes(out_ptr, out_len, out_cap);
    literllm_create_speech_request_free(create_speech_request_handle);
    literllm_default_client_free(client);
}

void test_smoke_speech_mp3_format(void) {
    /* Text-to-speech with explicit MP3 response format */
    LITERLLMCreateSpeechRequest* create_speech_request_handle = literllm_create_speech_request_from_json("{\"input\":\"The quick brown fox jumps over the lazy dog.\",\"model\":\"tts-1-hd\",\"response_format\":\"mp3\",\"speed\":1.0,\"voice\":\"nova\"}");
    assert(create_speech_request_handle != NULL && "failed to build request");
    const char* mock_base = getenv("MOCK_SERVER_URL");
    assert(mock_base != NULL && "MOCK_SERVER_URL must be set");
    char base_url[1024];
    snprintf(base_url, sizeof(base_url), "%s/fixtures/smoke_speech_mp3_format", mock_base);
    LITERLLMDefaultClient* client = literllm_create_client("test-key", base_url, (uint64_t)-1, (uint32_t)-1, NULL);
    assert(client != NULL && "failed to create client");
    uint8_t* out_ptr = NULL;
    uintptr_t out_len = 0;
    uintptr_t out_cap = 0;
    int32_t status = literllm_default_client_speech(client, create_speech_request_handle, &out_ptr, &out_len, &out_cap);
    assert(status == 0 && "expected call to succeed");
    assert(out_len > 0 && "expected non-empty value");
    literllm_free_bytes(out_ptr, out_len, out_cap);
    literllm_create_speech_request_free(create_speech_request_handle);
    literllm_default_client_free(client);
}
