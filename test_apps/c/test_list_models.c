/*
 * This file is auto-generated by alef — DO NOT EDIT.
 * alef:hash:b34f5dac0a6af082b71cab3b75e126050fd38402a73d6d3ff06130b6fe7d8875
 * To regenerate: alef generate
 * To verify freshness: alef verify --exit-code
 */
/* E2e tests for category: list-models */

#include <assert.h>
#include <stdint.h>
#include <string.h>
#include <stdio.h>
#include <stdlib.h>
#include "liter_llm.h"
#include "test_runner.h"

void test_empty_model_list(void) {
    /* List models response returns an empty data array when no models are available */
    const char* mock_base = getenv("MOCK_SERVER_URL");
    assert(mock_base != NULL && "MOCK_SERVER_URL must be set");
    char base_url[1024];
    snprintf(base_url, sizeof(base_url), "%s/fixtures/empty_model_list", mock_base);
    LITERLLMDefaultClient* client = literllm_create_client("test-key", base_url, (uint64_t)-1, (uint32_t)-1, NULL);
    assert(client != NULL && "failed to create client");
    LITERLLMModelsListResponse* result = literllm_default_client_list_models(client);
    assert(result != NULL && "expected call to succeed");
    char* data = literllm_models_list_response_data(result);
    {
        /* count_min: count top-level JSON array elements */
        assert(data != NULL && "expected non-null collection JSON");
        int elem_count = alef_json_array_count(data);
        assert(elem_count >= 0 && "expected at least 0 elements");
    }
    {
        /* count_equals: count elements in array */
        assert(data != NULL && "expected non-null collection JSON");
        int elem_count = alef_json_array_count(data);
        assert(elem_count == 0 && "expected 0 elements");
    }
    literllm_free_string(data);
    literllm_models_list_response_free(result);
    literllm_default_client_free(client);
}

void test_list_models_error_401(void) {
    /* 401 Unauthorized error on list models request when API key is invalid */
    const char* mock_base = getenv("MOCK_SERVER_URL");
    assert(mock_base != NULL && "MOCK_SERVER_URL must be set");
    char base_url[1024];
    snprintf(base_url, sizeof(base_url), "%s/fixtures/list_models_error_401", mock_base);
    LITERLLMDefaultClient* client = literllm_create_client("test-key", base_url, (uint64_t)-1, (uint32_t)-1, NULL);
    assert(client != NULL && "failed to create client");
    LITERLLMModelsListResponse* result = literllm_default_client_list_models(client);
    literllm_default_client_free(client);
    assert(result == NULL && "expected call to fail");
}

void test_list_models_error_500(void) {
    /* 500 Internal Server Error when listing models due to server failure */
    const char* mock_base = getenv("MOCK_SERVER_URL");
    assert(mock_base != NULL && "MOCK_SERVER_URL must be set");
    char base_url[1024];
    snprintf(base_url, sizeof(base_url), "%s/fixtures/list_models_error_500", mock_base);
    LITERLLMDefaultClient* client = literllm_create_client("test-key", base_url, (uint64_t)-1, (uint32_t)-1, NULL);
    assert(client != NULL && "failed to create client");
    LITERLLMModelsListResponse* result = literllm_default_client_list_models(client);
    literllm_default_client_free(client);
    assert(result == NULL && "expected call to fail");
}

void test_list_models_filtered(void) {
    /* List models response with multiple model objects */
    const char* mock_base = getenv("MOCK_SERVER_URL");
    assert(mock_base != NULL && "MOCK_SERVER_URL must be set");
    char base_url[1024];
    snprintf(base_url, sizeof(base_url), "%s/fixtures/list_models_filtered", mock_base);
    LITERLLMDefaultClient* client = literllm_create_client("test-key", base_url, (uint64_t)-1, (uint32_t)-1, NULL);
    assert(client != NULL && "failed to create client");
    LITERLLMModelsListResponse* result = literllm_default_client_list_models(client);
    assert(result != NULL && "expected call to succeed");
    char* data = literllm_models_list_response_data(result);
    char* data_json = literllm_models_list_response_data(result);
    assert(data_json != NULL);
    char* data_0_json = alef_json_array_get_index(data_json, 0);
    char* data_0_object = alef_json_get_string(data_0_json, "object");
    {
        /* count_min: count top-level JSON array elements */
        assert(data != NULL && "expected non-null collection JSON");
        int elem_count = alef_json_array_count(data);
        assert(elem_count >= 5 && "expected at least 5 elements");
    }
    assert(str_trim_eq(data_0_object, "model") == 0 && "equals assertion failed");
    literllm_free_string(data);
    free(data_0_object);
    free(data_0_json);
    literllm_free_string(data_json);
    literllm_models_list_response_free(result);
    literllm_default_client_free(client);
}
