// This file is auto-generated by alef — DO NOT EDIT.
// alef:hash:8cdff7ccce443d1fc90432984007ad265b45f4b1399e6a8713ec17d0541866e9
// To regenerate: alef generate
// To verify freshness: alef verify --exit-code
// Issues & docs: https://github.com/sample_crate-dev/alef

import { describe, expect, it } from 'vitest';import { createClient } from '@kreuzberg/liter-llm-node';
function _alefE2eText(value: unknown): string {
  return value == null ? "" : String(value);
}

function _alefE2eItemTexts(item: unknown): string[] {
  if (item == null || typeof item !== "object") {
    return [_alefE2eText(item)];
  }
  const record = item as Record<string, unknown>;
  const itemsText = Array.isArray(record.items) ? record.items.map(_alefE2eText).join(" ") : "";
  return [_alefE2eText(item), _alefE2eText(record.kind), _alefE2eText(record.name), _alefE2eText(record.source), _alefE2eText(record.alias), _alefE2eText(record.text), _alefE2eText(record.signature), itemsText];
}

function _alefE2eFormatMetadataDisplay(fm: unknown): string {
  if (fm == null) return "";
  if (typeof fm !== "object") return String(fm);
  const record = fm as Record<string, unknown>;
  const formatType = record.format_type;

  // FormatMetadata is a tagged union: { format_type: 'image', image: { format: 'PNG', ... }, ... }
  // Extract the display string based on the variant type
  if (formatType === "image" && typeof record.image === "object") {
    const imageData = record.image as Record<string, unknown>;
    if (typeof imageData.format === "string") return imageData.format;
  }

  // Fallback: return format_type variant name
  if (typeof record.format_type === "string") return record.format_type;
  return "";
}


describe('rerank', () => {  it('edge_rerank_empty_query: Reranking with an empty query string', async () => {    const client = createClient('test-key', `${process.env.MOCK_SERVER_URL}/fixtures/edge_rerank_empty_query`);    const result = await client.rerank({ documents: ["Some document", "Another document"], model: "rerank-v3.5", query: "" });    expect(result.results.length).toBe(2);

  }, 30000);
  it('edge_rerank_single_doc: Reranking with only a single document', async () => {    const client = createClient('test-key', `${process.env.MOCK_SERVER_URL}/fixtures/edge_rerank_single_doc`);    const result = await client.rerank({ documents: ["Artificial intelligence is the simulation of human intelligence."], model: "rerank-v3.5", query: "What is AI?" });    expect(result.results.length).toBe(1);
    expect(result.results[0].relevanceScore).toBeGreaterThan(0.9);

  }, 30000);
  it('error_rerank_auth_401: 401 Unauthorized for reranking with invalid API key', async () => {    const client = createClient('test-key', `${process.env.MOCK_SERVER_URL}/fixtures/error_rerank_auth_401`);    await expect(async () => {      await client.rerank({ documents: ["doc1"], model: "rerank-v3.5", query: "test" });    }).rejects.toThrow();
  }, 30000);
  it('error_rerank_bad_request: 400 Bad Request for reranking with invalid model', async () => {    const client = createClient('test-key', `${process.env.MOCK_SERVER_URL}/fixtures/error_rerank_bad_request`);    await expect(async () => {      await client.rerank({ documents: ["doc1"], model: "nonexistent-rerank", query: "test" });    }).rejects.toThrow();
  }, 30000);
  it('smoke_rerank_basic: Basic reranking of documents against a query', async () => {    const client = createClient('test-key', `${process.env.MOCK_SERVER_URL}/fixtures/smoke_rerank_basic`);    const result = await client.rerank({ documents: ["Machine learning is a subset of AI.", "The weather is sunny today.", "Deep learning uses neural networks."], model: "rerank-v3.5", query: "What is machine learning?" });    expect(result.results.length).toBe(3);
    expect(result.results[0].relevanceScore).toBeGreaterThan(0.9);

  }, 30000);
  it('smoke_rerank_return_docs: Reranking with return_documents flag to include document text', async () => {    const client = createClient('test-key', `${process.env.MOCK_SERVER_URL}/fixtures/smoke_rerank_return_docs`);    const result = await client.rerank({ documents: ["Rust is a systems programming language.", "Iron rusts when exposed to water."], model: "rerank-v3.5", query: "What is Rust?", returnDocuments: true });    expect(result.results.length).toBe(2);
    {
        const _v = result.results[0].document;
        if (typeof _v === "string" || Array.isArray(_v)) {
            expect(_v.length).toBeGreaterThan(0);
        } else {
            expect(_v).toBeDefined();
            expect(_v).not.toBeNull();
        }
    }
    expect(result.results[0].relevanceScore).toBeGreaterThan(0.9);

  }, 30000);
  it('smoke_rerank_with_top_n: Reranking with top_n parameter to limit results', async () => {    const client = createClient('test-key', `${process.env.MOCK_SERVER_URL}/fixtures/smoke_rerank_with_top_n`);    const result = await client.rerank({ documents: ["Python is a programming language.", "Cats are cute animals.", "Python was created by Guido van Rossum.", "The sun is a star."], model: "rerank-v3.5", query: "What is Python?", topN: 2 });    expect(result.results.length).toBe(2);
    expect(result.results[0].relevanceScore).toBeGreaterThan(0.9);

  }, 30000);
});
