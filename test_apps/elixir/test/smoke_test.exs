# This file is auto-generated by alef — DO NOT EDIT.
# alef:hash:4e914840e6cf80f28d3c62f8f0f455be1c6a6fa50daa8203c700d8f290589302
# To regenerate: alef generate
# To verify freshness: alef verify --exit-code
# E2e tests for category: smoke
defmodule E2e.SmokeTest do
  use ExUnit.Case, async: false

  describe "anthropic_chat" do
    test "anthropic_chat" do
      {:ok, client} = LiterLlm.create_client("test-key", base_url: (System.get_env("MOCK_SERVER_URL") || "") <> "/fixtures/anthropic_chat")
      {:ok, result} = LiterLlm.defaultclient_chat_async(client, "{\"max_tokens\":16,\"messages\":[{\"content\":\"You are a helpful assistant.\",\"role\":\"system\"},{\"content\":\"Say hello in one word.\",\"role\":\"user\"}],\"model\":\"anthropic/claude-3-5-sonnet-20241022\",\"temperature\":0}")
      assert length(result.choices) == 1
      assert String.trim(Enum.at(result.choices, 0).message.content) == "Hello!"
      assert String.trim(to_string(Enum.at(result.choices, 0).finish_reason)) == "stop"
      assert String.trim(result.model) == "claude-3-5-sonnet-20241022"
      assert result.usage.total_tokens == 23
    end
  end

  describe "azure_chat" do
    test "azure_chat" do
      {:ok, client} = LiterLlm.create_client("test-key", base_url: (System.get_env("MOCK_SERVER_URL") || "") <> "/fixtures/azure_chat")
      {:ok, result} = LiterLlm.defaultclient_chat_async(client, "{\"max_tokens\":16,\"messages\":[{\"content\":\"Say hello\",\"role\":\"user\"}],\"model\":\"azure/gpt-4\",\"temperature\":0}")
      assert length(result.choices) == 1
      assert String.trim(Enum.at(result.choices, 0).message.content) == "Hello!"
      assert String.trim(to_string(Enum.at(result.choices, 0).finish_reason)) == "stop"
      assert String.trim(result.model) == "gpt-4"
      assert result.usage.total_tokens == 13
    end
  end

  describe "azure_embed" do
    test "azure_embed" do
      {:ok, client} = LiterLlm.create_client("test-key", base_url: (System.get_env("MOCK_SERVER_URL") || "") <> "/fixtures/azure_embed")
      {:ok, result} = LiterLlm.defaultclient_embed_async(client, "{\"input\":\"Hello world\",\"model\":\"azure/text-embedding-ada-002\"}")
      assert length(result.data) == 1
      assert length(Enum.at(result.data, 0).embedding) == 1480
    end
  end

  describe "basic_chat" do
    test "basic_chat" do
      {:ok, client} = LiterLlm.create_client("test-key", base_url: (System.get_env("MOCK_SERVER_URL") || "") <> "/fixtures/basic_chat")
      {:ok, result} = LiterLlm.defaultclient_chat_async(client, "{\"messages\":[{\"content\":\"Say hello\",\"role\":\"user\"}],\"model\":\"gpt-4\",\"temperature\":0}")
      assert length(result.choices) == 1
      assert String.trim(Enum.at(result.choices, 0).message.content) == "Hello!"
      assert String.trim(to_string(Enum.at(result.choices, 0).finish_reason)) == "stop"
      assert result.usage.total_tokens == 15
      assert String.trim(result.model) == "gpt-4"
    end
  end

  describe "basic_embed" do
    test "basic_embed" do
      {:ok, client} = LiterLlm.create_client("test-key", base_url: (System.get_env("MOCK_SERVER_URL") || "") <> "/fixtures/basic_embed")
      {:ok, result} = LiterLlm.defaultclient_embed_async(client, "{\"input\":\"Hello world\",\"model\":\"text-embedding-3-small\"}")
      assert length(result.data) == 1
      assert length(Enum.at(result.data, 0).embedding) == 5
    end
  end

  describe "basic_list_models" do
    test "basic_list_models" do
      {:ok, client} = LiterLlm.create_client("test-key", base_url: (System.get_env("MOCK_SERVER_URL") || "") <> "/fixtures/basic_list_models")
      {:ok, result} = LiterLlm.defaultclient_list_models_async(client)
      assert length(result.data) >= 1
    end
  end

  describe "bedrock_chat" do
    test "bedrock_chat" do
      {:ok, client} = LiterLlm.create_client("test-key", base_url: (System.get_env("MOCK_SERVER_URL") || "") <> "/fixtures/bedrock_chat")
      {:ok, result} = LiterLlm.defaultclient_chat_async(client, "{\"max_tokens\":16,\"messages\":[{\"content\":\"Say hello in one word.\",\"role\":\"user\"}],\"model\":\"bedrock/anthropic.claude-3-sonnet-20240229-v1:0\",\"temperature\":0}")
      assert length(result.choices) == 1
      assert String.trim(Enum.at(result.choices, 0).message.content) == "Hello!"
      assert String.trim(to_string(Enum.at(result.choices, 0).finish_reason)) == "stop"
      assert String.trim(result.model) == "anthropic.claude-3-sonnet-20240229-v1:0"
      assert result.usage.total_tokens == 15
    end
  end

  describe "github_copilot_chat" do
    test "github_copilot_chat" do
      {:ok, client} = LiterLlm.create_client("test-key", base_url: (System.get_env("MOCK_SERVER_URL") || "") <> "/fixtures/github_copilot_chat")
      {:ok, result} = LiterLlm.defaultclient_chat_async(client, "{\"max_tokens\":16,\"messages\":[{\"content\":\"Say hello in one word.\",\"role\":\"user\"}],\"model\":\"github_copilot/gpt-4o\",\"temperature\":0}")
      assert length(result.choices) == 1
      assert String.trim(Enum.at(result.choices, 0).message.content) == "Hello!"
      assert String.trim(to_string(Enum.at(result.choices, 0).finish_reason)) == "stop"
      assert String.trim(result.model) == "gpt-4o"
      assert result.usage.total_tokens == 14
    end
  end

  describe "smoke_cache_memory" do
    test "smoke_cache_memory" do
      api_key_val = System.get_env("OPENAI_API_KEY")
      {api_key_val, client_opts} = if api_key_val && api_key_val != "" do
        IO.puts("smoke_cache_memory: using real API (OPENAI_API_KEY is set)")
        {api_key_val, []}
      else
        IO.puts("smoke_cache_memory: using mock server (OPENAI_API_KEY not set)")
        {"test-key", [base_url: (System.get_env("MOCK_SERVER_URL") || "") <> "/fixtures/smoke_cache_memory"]}
      end
      {:ok, client} = LiterLlm.create_client(api_key_val, client_opts)
      {:ok, result} = LiterLlm.defaultclient_chat_async(client, "{\"max_tokens\":5,\"messages\":[{\"content\":\"What is 2+2? Answer with just the number.\",\"role\":\"user\"}],\"model\":\"openai/gpt-4o-mini\"}")
      assert result.choices != ""
    end
  end

  describe "smoke_chat_anthropic" do
    test "smoke_chat_anthropic" do
      api_key_val = System.get_env("ANTHROPIC_API_KEY")
      {api_key_val, client_opts} = if api_key_val && api_key_val != "" do
        IO.puts("smoke_chat_anthropic: using real API (ANTHROPIC_API_KEY is set)")
        {api_key_val, []}
      else
        IO.puts("smoke_chat_anthropic: using mock server (ANTHROPIC_API_KEY not set)")
        {"test-key", [base_url: (System.get_env("MOCK_SERVER_URL") || "") <> "/fixtures/smoke_chat_anthropic"]}
      end
      {:ok, client} = LiterLlm.create_client(api_key_val, client_opts)
      {:ok, result} = LiterLlm.defaultclient_chat_async(client, "{\"max_tokens\":10,\"messages\":[{\"content\":\"Say hello in exactly one word.\",\"role\":\"user\"}],\"model\":\"anthropic/claude-sonnet-4-20250514\"}")
      assert result.choices != ""
      assert result.usage != ""
    end
  end

  describe "smoke_chat_gemini" do
    test "smoke_chat_gemini" do
      api_key_val = System.get_env("GEMINI_API_KEY")
      {api_key_val, client_opts} = if api_key_val && api_key_val != "" do
        IO.puts("smoke_chat_gemini: using real API (GEMINI_API_KEY is set)")
        {api_key_val, []}
      else
        IO.puts("smoke_chat_gemini: using mock server (GEMINI_API_KEY not set)")
        {"test-key", [base_url: (System.get_env("MOCK_SERVER_URL") || "") <> "/fixtures/smoke_chat_gemini"]}
      end
      {:ok, client} = LiterLlm.create_client(api_key_val, client_opts)
      {:ok, result} = LiterLlm.defaultclient_chat_async(client, "{\"max_tokens\":10,\"messages\":[{\"content\":\"Say hello in exactly one word.\",\"role\":\"user\"}],\"model\":\"gemini/gemini-2.5-flash-lite\"}")
      assert result.choices != ""
      assert result.usage != ""
    end
  end

  describe "smoke_chat_openai" do
    test "smoke_chat_openai" do
      api_key_val = System.get_env("OPENAI_API_KEY")
      {api_key_val, client_opts} = if api_key_val && api_key_val != "" do
        IO.puts("smoke_chat_openai: using real API (OPENAI_API_KEY is set)")
        {api_key_val, []}
      else
        IO.puts("smoke_chat_openai: using mock server (OPENAI_API_KEY not set)")
        {"test-key", [base_url: (System.get_env("MOCK_SERVER_URL") || "") <> "/fixtures/smoke_chat_openai"]}
      end
      {:ok, client} = LiterLlm.create_client(api_key_val, client_opts)
      {:ok, result} = LiterLlm.defaultclient_chat_async(client, "{\"max_tokens\":10,\"messages\":[{\"content\":\"Say hello in exactly one word.\",\"role\":\"user\"}],\"model\":\"openai/gpt-4o-mini\"}")
      assert result.choices != ""
      assert result.usage != ""
    end
  end

  describe "smoke_embed_openai" do
    test "smoke_embed_openai" do
      api_key_val = System.get_env("OPENAI_API_KEY")
      {api_key_val, client_opts} = if api_key_val && api_key_val != "" do
        IO.puts("smoke_embed_openai: using real API (OPENAI_API_KEY is set)")
        {api_key_val, []}
      else
        IO.puts("smoke_embed_openai: using mock server (OPENAI_API_KEY not set)")
        {"test-key", [base_url: (System.get_env("MOCK_SERVER_URL") || "") <> "/fixtures/smoke_embed_openai"]}
      end
      {:ok, client} = LiterLlm.create_client(api_key_val, client_opts)
      {:ok, result} = LiterLlm.defaultclient_embed_async(client, "{\"input\":[\"Hello world\"],\"model\":\"openai/text-embedding-3-small\"}")
      assert result.data != ""
    end
  end

  describe "smoke_list_models_openai" do
    test "smoke_list_models_openai" do
      api_key_val = System.get_env("OPENAI_API_KEY")
      {api_key_val, client_opts} = if api_key_val && api_key_val != "" do
        IO.puts("smoke_list_models_openai: using real API (OPENAI_API_KEY is set)")
        {api_key_val, []}
      else
        IO.puts("smoke_list_models_openai: using mock server (OPENAI_API_KEY not set)")
        {"test-key", [base_url: (System.get_env("MOCK_SERVER_URL") || "") <> "/fixtures/smoke_list_models_openai"]}
      end
      {:ok, client} = LiterLlm.create_client(api_key_val, client_opts)
      {:ok, result} = LiterLlm.defaultclient_list_models_async(client)
      assert length(result.data) >= 1
    end
  end

  describe "smoke_provider_routing" do
    test "smoke_provider_routing" do
      api_key_val = System.get_env("OPENAI_API_KEY")
      {api_key_val, client_opts} = if api_key_val && api_key_val != "" do
        IO.puts("smoke_provider_routing: using real API (OPENAI_API_KEY is set)")
        {api_key_val, []}
      else
        IO.puts("smoke_provider_routing: using mock server (OPENAI_API_KEY not set)")
        {"test-key", [base_url: (System.get_env("MOCK_SERVER_URL") || "") <> "/fixtures/smoke_provider_routing"]}
      end
      {:ok, client} = LiterLlm.create_client(api_key_val, client_opts)
      {:ok, result} = LiterLlm.defaultclient_chat_async(client, "{\"max_tokens\":5,\"messages\":[{\"content\":\"Say hi.\",\"role\":\"user\"}],\"model\":\"openai/gpt-4o-mini\"}")
      assert result.choices != ""
    end
  end

  describe "smoke_streaming_openai" do
    test "smoke_streaming_openai" do
      api_key_val = System.get_env("OPENAI_API_KEY")
      {api_key_val, client_opts} = if api_key_val && api_key_val != "" do
        IO.puts("smoke_streaming_openai: using real API (OPENAI_API_KEY is set)")
        {api_key_val, []}
      else
        IO.puts("smoke_streaming_openai: using mock server (OPENAI_API_KEY not set)")
        {"test-key", [base_url: (System.get_env("MOCK_SERVER_URL") || "") <> "/fixtures/smoke_streaming_openai"]}
      end
      {:ok, client} = LiterLlm.create_client(api_key_val, client_opts)
      {:ok, result} = LiterLlm.chat_stream(client, "{\"max_tokens\":50,\"messages\":[{\"content\":\"Count from 1 to 5.\",\"role\":\"user\"}],\"model\":\"openai/gpt-4o-mini\"}")
      chunks = Enum.to_list(result)
      assert length(chunks) >= 1
    end
  end

  describe "vertex_chat" do
    test "vertex_chat" do
      {:ok, client} = LiterLlm.create_client("test-key", base_url: (System.get_env("MOCK_SERVER_URL") || "") <> "/fixtures/vertex_chat")
      {:ok, result} = LiterLlm.defaultclient_chat_async(client, "{\"max_tokens\":16,\"messages\":[{\"content\":\"Say hello in one word.\",\"role\":\"user\"}],\"model\":\"vertex_ai/gemini-2.0-flash\",\"temperature\":0}")
      assert length(result.choices) == 1
      assert String.trim(Enum.at(result.choices, 0).message.content) == "Hello!"
      assert String.trim(to_string(Enum.at(result.choices, 0).finish_reason)) == "stop"
      assert String.trim(result.model) == "gemini-2.0-flash"
      assert result.usage.total_tokens == 13
    end
  end

  describe "vertex_embed" do
    test "vertex_embed" do
      {:ok, client} = LiterLlm.create_client("test-key", base_url: (System.get_env("MOCK_SERVER_URL") || "") <> "/fixtures/vertex_embed")
      {:ok, result} = LiterLlm.defaultclient_embed_async(client, "{\"input\":\"Hello\",\"model\":\"vertex_ai/text-embedding-005\"}")
      assert length(result.data) == 1
      assert length(Enum.at(result.data, 0).embedding) == 160
    end
  end
end
