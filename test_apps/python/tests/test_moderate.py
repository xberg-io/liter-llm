# This file is auto-generated by alef — DO NOT EDIT.
# alef:hash:fe64a8a06beeb01b5344005fa07dcdfdf3e244d772ace0f89dbba84b0541ca2d
# To regenerate: alef generate
# To verify freshness: alef verify --exit-code

"""E2e tests for category: moderate."""

import os
import pytest  # noqa: F401
from liter_llm import create_client, ModerationRequest, ModerationInput
from liter_llm._internal_bindings import ModerationRequest


def _alef_e2e_text(value: object) -> str:
    return "" if value is None else str(value)


def _alef_e2e_item_texts(item: object) -> tuple[str, ...]:
    raw_items = getattr(item, "items", None)
    items_text = " ".join(str(value) for value in raw_items) if isinstance(raw_items, list) else ""
    return (
        _alef_e2e_text(item),
        _alef_e2e_text(getattr(item, "kind", None)),
        _alef_e2e_text(getattr(item, "name", None)),
        _alef_e2e_text(getattr(item, "source", None)),
        _alef_e2e_text(getattr(item, "alias", None)),
        _alef_e2e_text(getattr(item, "text", None)),
        _alef_e2e_text(getattr(item, "signature", None)),
        items_text,
    )


@pytest.mark.asyncio

async def test_edge_moderate_all_categories() -> None:
    """Moderation response with multiple categories flagged."""
    client = create_client(api_key="test-key", base_url=os.environ["MOCK_SERVER_URL"] + "/fixtures/edge_moderate_all_categories")
    request = ModerationRequest.from_json("{\"input\":\"Extremely harmful content targeting multiple categories\",\"model\":\"omni-moderation-latest\"}")

    result = await client.moderate(request)
    assert len(result.results) == 1  # noqa: S101
    assert result.results[0].flagged is True  # noqa: S101


@pytest.mark.asyncio

async def test_edge_moderate_empty_input() -> None:
    """Moderation with empty string input."""
    client = create_client(api_key="test-key", base_url=os.environ["MOCK_SERVER_URL"] + "/fixtures/edge_moderate_empty_input")
    request = ModerationRequest.from_json("{\"input\":\"\",\"model\":\"omni-moderation-latest\"}")

    result = await client.moderate(request)
    assert len(result.results) == 1  # noqa: S101
    assert result.results[0].flagged is False  # noqa: S101


@pytest.mark.asyncio

async def test_error_moderate_auth_401() -> None:
    """401 Unauthorized for moderation with invalid API key."""
    with pytest.raises(Exception) as exc_info:  # noqa: B017
        client = create_client(api_key="test-key", base_url=os.environ["MOCK_SERVER_URL"] + "/fixtures/error_moderate_auth_401")
        request = ModerationRequest.from_json("{\"input\":\"Hello\",\"model\":\"omni-moderation-latest\"}")
        await client.moderate(request)
    assert "Authentication" in str(exc_info.value) or "Authentication" in type(exc_info.value).__name__  # noqa: S101


@pytest.mark.asyncio

async def test_error_moderate_bad_request() -> None:
    """400 Bad Request for moderation with invalid model."""
    with pytest.raises(Exception) as exc_info:  # noqa: B017
        client = create_client(api_key="test-key", base_url=os.environ["MOCK_SERVER_URL"] + "/fixtures/error_moderate_bad_request")
        request = ModerationRequest.from_json("{\"input\":\"Hello\",\"model\":\"nonexistent-moderation\"}")
        await client.moderate(request)
    assert "BadRequest" in str(exc_info.value) or "BadRequest" in type(exc_info.value).__name__  # noqa: S101


@pytest.mark.asyncio

async def test_smoke_moderate_batch() -> None:
    """Moderate multiple inputs in a single request."""
    client = create_client(api_key="test-key", base_url=os.environ["MOCK_SERVER_URL"] + "/fixtures/smoke_moderate_batch")
    request = ModerationRequest.from_json("{\"input\":[\"Hello world\",\"Nice weather today\"],\"model\":\"omni-moderation-latest\"}")

    result = await client.moderate(request)
    assert len(result.results) == 2  # noqa: S101
    assert result.results[0].flagged is False  # noqa: S101


@pytest.mark.asyncio

async def test_smoke_moderate_flagged() -> None:
    """Moderation detects flagged content."""
    client = create_client(api_key="test-key", base_url=os.environ["MOCK_SERVER_URL"] + "/fixtures/smoke_moderate_flagged")
    request = ModerationRequest.from_json("{\"input\":\"I want to hurt someone very badly\",\"model\":\"omni-moderation-latest\"}")

    result = await client.moderate(request)
    assert len(result.results) == 1  # noqa: S101
    assert result.results[0].flagged is True  # noqa: S101


@pytest.mark.asyncio

async def test_smoke_moderate_single() -> None:
    """Moderate a single non-flagged input."""
    client = create_client(api_key="test-key", base_url=os.environ["MOCK_SERVER_URL"] + "/fixtures/smoke_moderate_single")
    request = ModerationRequest.from_json("{\"input\":\"The weather is nice today.\",\"model\":\"omni-moderation-latest\"}")

    result = await client.moderate(request)
    assert len(result.results) == 1  # noqa: S101
    assert result.results[0].flagged is False  # noqa: S101
