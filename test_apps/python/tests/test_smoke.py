# This file is auto-generated by alef — DO NOT EDIT.
# alef:hash:f2d02d39ed23b63568387f22c991c70fd7b0d5c8f2f92cc028dcbaff825ca583
# To regenerate: alef generate
# To verify freshness: alef verify --exit-code

"""E2e tests for category: smoke."""

import os
import pytest  # noqa: F401
from liter_llm import create_client, ChatCompletionRequest, EmbeddingRequest, EmbeddingInput, Message
from liter_llm._internal_bindings import ChatCompletionRequest
from liter_llm._internal_bindings import EmbeddingRequest


def _alef_e2e_text(value: object) -> str:
    return "" if value is None else str(value)


def _alef_e2e_item_texts(item: object) -> tuple[str, ...]:
    raw_items = getattr(item, "items", None)
    items_text = " ".join(str(value) for value in raw_items) if isinstance(raw_items, list) else ""
    return (
        _alef_e2e_text(item),
        _alef_e2e_text(getattr(item, "kind", None)),
        _alef_e2e_text(getattr(item, "name", None)),
        _alef_e2e_text(getattr(item, "source", None)),
        _alef_e2e_text(getattr(item, "alias", None)),
        _alef_e2e_text(getattr(item, "text", None)),
        _alef_e2e_text(getattr(item, "signature", None)),
        items_text,
    )


@pytest.mark.asyncio

async def test_anthropic_chat() -> None:
    """Basic chat completion via the Anthropic provider (claude-3-5-sonnet-20241022) with system and user messages."""
    client = create_client(api_key="test-key", base_url=os.environ["MOCK_SERVER_URL"] + "/fixtures/anthropic_chat")
    request = ChatCompletionRequest.from_json("{\"max_tokens\":16,\"messages\":[{\"content\":\"You are a helpful assistant.\",\"role\":\"system\"},{\"content\":\"Say hello in one word.\",\"role\":\"user\"}],\"model\":\"anthropic/claude-3-5-sonnet-20241022\",\"temperature\":0}")

    result = await client.chat(request)
    assert len(result.choices) == 1  # noqa: S101
    assert _alef_e2e_text(result.choices[0].message.content).lower() == "Hello!".lower()  # noqa: S101
    assert _alef_e2e_text(result.choices[0].finish_reason).lower() == "stop".lower()  # noqa: S101
    assert result.model.strip() == "claude-3-5-sonnet-20241022"  # noqa: S101
    assert result.usage.total_tokens == 23  # noqa: S101


@pytest.mark.asyncio

async def test_azure_chat() -> None:
    """Chat completion via Azure OpenAI with the azure/ prefix for provider routing — verifies the prefix is stripped before dispatching and the response is normalised to the standard OpenAI chat completion shape."""
    client = create_client(api_key="test-key", base_url=os.environ["MOCK_SERVER_URL"] + "/fixtures/azure_chat")
    request = ChatCompletionRequest.from_json("{\"max_tokens\":16,\"messages\":[{\"content\":\"Say hello\",\"role\":\"user\"}],\"model\":\"azure/gpt-4\",\"temperature\":0}")

    result = await client.chat(request)
    assert len(result.choices) == 1  # noqa: S101
    assert _alef_e2e_text(result.choices[0].message.content).lower() == "Hello!".lower()  # noqa: S101
    assert _alef_e2e_text(result.choices[0].finish_reason).lower() == "stop".lower()  # noqa: S101
    assert result.model.strip() == "gpt-4"  # noqa: S101
    assert result.usage.total_tokens == 13  # noqa: S101


@pytest.mark.asyncio

async def test_azure_embed() -> None:
    """Embedding request via Azure OpenAI using the azure/ provider prefix — response follows the standard OpenAI embeddings shape that Azure returns unchanged."""
    client = create_client(api_key="test-key", base_url=os.environ["MOCK_SERVER_URL"] + "/fixtures/azure_embed")
    request = EmbeddingRequest.from_json("{\"input\":\"Hello world\",\"model\":\"azure/text-embedding-ada-002\"}")

    result = await client.embed(request)
    assert len(result.data) == 1  # noqa: S101
    assert len(result.data[0].embedding) == 1480  # noqa: S101


@pytest.mark.asyncio

async def test_basic_chat() -> None:
    """Basic chat completion with a single user message."""
    client = create_client(api_key="test-key", base_url=os.environ["MOCK_SERVER_URL"] + "/fixtures/basic_chat")
    request = ChatCompletionRequest.from_json("{\"messages\":[{\"content\":\"Say hello\",\"role\":\"user\"}],\"model\":\"gpt-4\",\"temperature\":0}")

    result = await client.chat(request)
    assert len(result.choices) == 1  # noqa: S101
    assert _alef_e2e_text(result.choices[0].message.content).lower() == "Hello!".lower()  # noqa: S101
    assert _alef_e2e_text(result.choices[0].finish_reason).lower() == "stop".lower()  # noqa: S101
    assert result.usage.total_tokens == 15  # noqa: S101
    assert result.model.strip() == "gpt-4"  # noqa: S101


@pytest.mark.asyncio

async def test_basic_embed() -> None:
    """Basic embedding request for a single input string."""
    client = create_client(api_key="test-key", base_url=os.environ["MOCK_SERVER_URL"] + "/fixtures/basic_embed")
    request = EmbeddingRequest.from_json("{\"input\":\"Hello world\",\"model\":\"text-embedding-3-small\"}")

    result = await client.embed(request)
    assert len(result.data) == 1  # noqa: S101
    assert len(result.data[0].embedding) == 5  # noqa: S101


@pytest.mark.asyncio

async def test_basic_list_models() -> None:
    """List available models from the API."""
    client = create_client(api_key="test-key", base_url=os.environ["MOCK_SERVER_URL"] + "/fixtures/basic_list_models")

    result = await client.list_models()
    assert len(result.data) >= 1  # noqa: S101


@pytest.mark.asyncio

async def test_bedrock_chat() -> None:
    """Basic chat completion via the AWS Bedrock provider using the bedrock/ prefix for routing — verifies the prefix is stripped before dispatching and the Converse API response is normalised to the standard OpenAI chat completion shape."""
    client = create_client(api_key="test-key", base_url=os.environ["MOCK_SERVER_URL"] + "/fixtures/bedrock_chat")
    request = ChatCompletionRequest.from_json("{\"max_tokens\":16,\"messages\":[{\"content\":\"Say hello in one word.\",\"role\":\"user\"}],\"model\":\"bedrock/anthropic.claude-3-sonnet-20240229-v1:0\",\"temperature\":0}")

    result = await client.chat(request)
    assert len(result.choices) == 1  # noqa: S101
    assert _alef_e2e_text(result.choices[0].message.content).lower() == "Hello!".lower()  # noqa: S101
    assert _alef_e2e_text(result.choices[0].finish_reason).lower() == "stop".lower()  # noqa: S101
    assert result.model.strip() == "anthropic.claude-3-sonnet-20240229-v1:0"  # noqa: S101
    assert result.usage.total_tokens == 15  # noqa: S101


@pytest.mark.asyncio

async def test_github_copilot_chat() -> None:
    """Basic chat completion via the GitHub Copilot provider (gpt-4o) with a single user message."""
    client = create_client(api_key="test-key", base_url=os.environ["MOCK_SERVER_URL"] + "/fixtures/github_copilot_chat")
    request = ChatCompletionRequest.from_json("{\"max_tokens\":16,\"messages\":[{\"content\":\"Say hello in one word.\",\"role\":\"user\"}],\"model\":\"github_copilot/gpt-4o\",\"temperature\":0}")

    result = await client.chat(request)
    assert len(result.choices) == 1  # noqa: S101
    assert _alef_e2e_text(result.choices[0].message.content).lower() == "Hello!".lower()  # noqa: S101
    assert _alef_e2e_text(result.choices[0].finish_reason).lower() == "stop".lower()  # noqa: S101
    assert result.model.strip() == "gpt-4o"  # noqa: S101
    assert result.usage.total_tokens == 14  # noqa: S101


@pytest.mark.asyncio

async def test_smoke_cache_memory() -> None:
    """Test in-memory caching by sending identical requests twice."""
    api_key = os.environ.get("OPENAI_API_KEY")
    if api_key:
        print("smoke_cache_memory: using real API (OPENAI_API_KEY is set)", flush=True)  # noqa: T201
        client = create_client(api_key=api_key)
    else:
        print("smoke_cache_memory: using mock server (OPENAI_API_KEY not set)", flush=True)  # noqa: T201
        client = create_client(api_key="test-key", base_url=os.environ["MOCK_SERVER_URL"] + "/fixtures/smoke_cache_memory")
    request = ChatCompletionRequest.from_json("{\"max_tokens\":5,\"messages\":[{\"content\":\"What is 2+2? Answer with just the number.\",\"role\":\"user\"}],\"model\":\"openai/gpt-4o-mini\"}")

    result = await client.chat(request)
    assert result.choices  # noqa: S101


@pytest.mark.asyncio

async def test_smoke_chat_anthropic() -> None:
    """Basic chat completion against real Anthropic API."""
    api_key = os.environ.get("ANTHROPIC_API_KEY")
    if api_key:
        print("smoke_chat_anthropic: using real API (ANTHROPIC_API_KEY is set)", flush=True)  # noqa: T201
        client = create_client(api_key=api_key)
    else:
        print("smoke_chat_anthropic: using mock server (ANTHROPIC_API_KEY not set)", flush=True)  # noqa: T201
        client = create_client(api_key="test-key", base_url=os.environ["MOCK_SERVER_URL"] + "/fixtures/smoke_chat_anthropic")
    request = ChatCompletionRequest.from_json("{\"max_tokens\":10,\"messages\":[{\"content\":\"Say hello in exactly one word.\",\"role\":\"user\"}],\"model\":\"anthropic/claude-sonnet-4-20250514\"}")

    result = await client.chat(request)
    assert result.choices  # noqa: S101
    assert result.usage  # noqa: S101


@pytest.mark.asyncio

async def test_smoke_chat_gemini() -> None:
    """Basic chat completion against real Google Gemini API."""
    api_key = os.environ.get("GEMINI_API_KEY")
    if api_key:
        print("smoke_chat_gemini: using real API (GEMINI_API_KEY is set)", flush=True)  # noqa: T201
        client = create_client(api_key=api_key)
    else:
        print("smoke_chat_gemini: using mock server (GEMINI_API_KEY not set)", flush=True)  # noqa: T201
        client = create_client(api_key="test-key", base_url=os.environ["MOCK_SERVER_URL"] + "/fixtures/smoke_chat_gemini")
    request = ChatCompletionRequest.from_json("{\"max_tokens\":10,\"messages\":[{\"content\":\"Say hello in exactly one word.\",\"role\":\"user\"}],\"model\":\"gemini/gemini-2.5-flash-lite\"}")

    result = await client.chat(request)
    assert result.choices  # noqa: S101
    assert result.usage  # noqa: S101


@pytest.mark.asyncio

async def test_smoke_chat_openai() -> None:
    """Basic chat completion against real OpenAI API."""
    api_key = os.environ.get("OPENAI_API_KEY")
    if api_key:
        print("smoke_chat_openai: using real API (OPENAI_API_KEY is set)", flush=True)  # noqa: T201
        client = create_client(api_key=api_key)
    else:
        print("smoke_chat_openai: using mock server (OPENAI_API_KEY not set)", flush=True)  # noqa: T201
        client = create_client(api_key="test-key", base_url=os.environ["MOCK_SERVER_URL"] + "/fixtures/smoke_chat_openai")
    request = ChatCompletionRequest.from_json("{\"max_tokens\":10,\"messages\":[{\"content\":\"Say hello in exactly one word.\",\"role\":\"user\"}],\"model\":\"openai/gpt-4o-mini\"}")

    result = await client.chat(request)
    assert result.choices  # noqa: S101
    assert result.usage  # noqa: S101


@pytest.mark.asyncio

async def test_smoke_embed_openai() -> None:
    """Embeddings request against real OpenAI API."""
    api_key = os.environ.get("OPENAI_API_KEY")
    if api_key:
        print("smoke_embed_openai: using real API (OPENAI_API_KEY is set)", flush=True)  # noqa: T201
        client = create_client(api_key=api_key)
    else:
        print("smoke_embed_openai: using mock server (OPENAI_API_KEY not set)", flush=True)  # noqa: T201
        client = create_client(api_key="test-key", base_url=os.environ["MOCK_SERVER_URL"] + "/fixtures/smoke_embed_openai")
    request = EmbeddingRequest.from_json("{\"input\":[\"Hello world\"],\"model\":\"openai/text-embedding-3-small\"}")

    result = await client.embed(request)
    assert result.data  # noqa: S101


@pytest.mark.asyncio

async def test_smoke_list_models_openai() -> None:
    """List models against real OpenAI API."""
    api_key = os.environ.get("OPENAI_API_KEY")
    if api_key:
        print("smoke_list_models_openai: using real API (OPENAI_API_KEY is set)", flush=True)  # noqa: T201
        client = create_client(api_key=api_key)
    else:
        print("smoke_list_models_openai: using mock server (OPENAI_API_KEY not set)", flush=True)  # noqa: T201
        client = create_client(api_key="test-key", base_url=os.environ["MOCK_SERVER_URL"] + "/fixtures/smoke_list_models_openai")

    result = await client.list_models()
    assert len(result.data) >= 1  # noqa: S101


@pytest.mark.asyncio

async def test_smoke_provider_routing() -> None:
    """Test provider routing by sending requests to OpenAI and Anthropic."""
    api_key = os.environ.get("OPENAI_API_KEY")
    if api_key:
        print("smoke_provider_routing: using real API (OPENAI_API_KEY is set)", flush=True)  # noqa: T201
        client = create_client(api_key=api_key)
    else:
        print("smoke_provider_routing: using mock server (OPENAI_API_KEY not set)", flush=True)  # noqa: T201
        client = create_client(api_key="test-key", base_url=os.environ["MOCK_SERVER_URL"] + "/fixtures/smoke_provider_routing")
    request = ChatCompletionRequest.from_json("{\"max_tokens\":5,\"messages\":[{\"content\":\"Say hi.\",\"role\":\"user\"}],\"model\":\"openai/gpt-4o-mini\"}")

    result = await client.chat(request)
    assert result.choices  # noqa: S101


@pytest.mark.asyncio

async def test_smoke_streaming_openai() -> None:
    """Chat streaming against real OpenAI API, verifies chunks received."""
    api_key = os.environ.get("OPENAI_API_KEY")
    if api_key:
        print("smoke_streaming_openai: using real API (OPENAI_API_KEY is set)", flush=True)  # noqa: T201
        client = create_client(api_key=api_key)
    else:
        print("smoke_streaming_openai: using mock server (OPENAI_API_KEY not set)", flush=True)  # noqa: T201
        client = create_client(api_key="test-key", base_url=os.environ["MOCK_SERVER_URL"] + "/fixtures/smoke_streaming_openai")
    request = ChatCompletionRequest.from_json("{\"max_tokens\":50,\"messages\":[{\"content\":\"Count from 1 to 5.\",\"role\":\"user\"}],\"model\":\"openai/gpt-4o-mini\"}")

    result = client.chat_stream(request)
    chunks = []
    async for chunk in result:
        chunks.append(chunk)
    assert len(chunks) >= 1  # noqa: S101


@pytest.mark.asyncio

async def test_vertex_chat() -> None:
    """Basic chat completion via the Google Vertex AI provider using the vertex_ai/ prefix for routing — verifies the prefix is stripped before dispatching and the Gemini response is normalised to the standard OpenAI chat completion shape."""
    client = create_client(api_key="test-key", base_url=os.environ["MOCK_SERVER_URL"] + "/fixtures/vertex_chat")
    request = ChatCompletionRequest.from_json("{\"max_tokens\":16,\"messages\":[{\"content\":\"Say hello in one word.\",\"role\":\"user\"}],\"model\":\"vertex_ai/gemini-2.0-flash\",\"temperature\":0}")

    result = await client.chat(request)
    assert len(result.choices) == 1  # noqa: S101
    assert _alef_e2e_text(result.choices[0].message.content).lower() == "Hello!".lower()  # noqa: S101
    assert _alef_e2e_text(result.choices[0].finish_reason).lower() == "stop".lower()  # noqa: S101
    assert result.model.strip() == "gemini-2.0-flash"  # noqa: S101
    assert result.usage.total_tokens == 13  # noqa: S101


@pytest.mark.asyncio

async def test_vertex_embed() -> None:
    """Embedding request via Google Vertex AI using the vertex_ai/ provider prefix and the text-embedding-005 model — response follows the standard OpenAI embeddings shape."""
    client = create_client(api_key="test-key", base_url=os.environ["MOCK_SERVER_URL"] + "/fixtures/vertex_embed")
    request = EmbeddingRequest.from_json("{\"input\":\"Hello\",\"model\":\"vertex_ai/text-embedding-005\"}")

    result = await client.embed(request)
    assert len(result.data) == 1  # noqa: S101
    assert len(result.data[0].embedding) == 160  # noqa: S101
