// This file is auto-generated by alef — DO NOT EDIT.
// alef:hash:a4d454c0756a15ceddae0a8c1fb84ef9dcd2872078b108f8ce5789cc78893a8d
// To regenerate: alef generate
// To verify freshness: alef verify --exit-code

package io.xberg.literllm.e2e;

import org.junit.jupiter.api.Test;
import static org.junit.jupiter.api.Assertions.*;
import io.xberg.literllm.LiterLlm;
import io.xberg.literllm.EmbeddingRequest;
import java.util.Optional;
import io.xberg.literllm.JsonUtil;

/** E2e tests for category: embed. */
public class EmbedTest {

    @Test
    void testBatchEmbed() throws Exception {
        // Embedding request with multiple input strings returns one embedding object per input
        var request = JsonUtil.fromJson("{\"input\":[\"Hello\",\"World\"],\"model\":\"text-embedding-3-small\"}", EmbeddingRequest.class);

        String mockServerUrl = System.getProperty("mockServerUrl"); if (mockServerUrl == null) { mockServerUrl = System.getenv("MOCK_SERVER_URL"); } String mockUrl = (mockServerUrl != null ? mockServerUrl : "http://localhost:8000") + "/fixtures/batch_embed";
        var client = LiterLlm.createClient("test-key", mockUrl, null, null, null);
        var result = client.embed(request);
assertEquals(2, result.data().size(), "expected exactly 2 elements");assertEquals(5, result.data().get(0).embedding().size(), "expected exactly 5 elements");

    }


    @Test
    void testEdgeEmbedBatchInput() throws Exception {
        // Embedding request with multiple inputs returns multiple embedding objects
        var request = JsonUtil.fromJson("{\"input\":[\"Hello world\",\"Goodbye world\"],\"model\":\"text-embedding-3-small\"}", EmbeddingRequest.class);

        String mockServerUrl = System.getProperty("mockServerUrl"); if (mockServerUrl == null) { mockServerUrl = System.getenv("MOCK_SERVER_URL"); } String mockUrl = (mockServerUrl != null ? mockServerUrl : "http://localhost:8000") + "/fixtures/edge_embed_batch_input";
        var client = LiterLlm.createClient("test-key", mockUrl, null, null, null);
        var result = client.embed(request);
assertEquals(2, result.data().size(), "expected exactly 2 elements");assertEquals(0, result.data().get(0).index());assertEquals(1, result.data().get(1).index());

    }


    @Test
    void testEdgeEmbedEmptyInput() throws Exception {
        // Embedding request with empty string input returns empty data array
        var request = JsonUtil.fromJson("{\"input\":\"\",\"model\":\"text-embedding-3-small\"}", EmbeddingRequest.class);

        String mockServerUrl = System.getProperty("mockServerUrl"); if (mockServerUrl == null) { mockServerUrl = System.getenv("MOCK_SERVER_URL"); } String mockUrl = (mockServerUrl != null ? mockServerUrl : "http://localhost:8000") + "/fixtures/edge_embed_empty_input";
        var client = LiterLlm.createClient("test-key", mockUrl, null, null, null);
        var result = client.embed(request);
assertEquals(0, result.data().size(), "expected exactly 0 elements");

    }


    @Test
    void testEmbedEncodingFormat() throws Exception {
        // Embedding request with explicit encoding_format of float returns float array embeddings
        var request = JsonUtil.fromJson("{\"encoding_format\":\"float\",\"input\":\"Test input\",\"model\":\"text-embedding-3-small\"}", EmbeddingRequest.class);

        String mockServerUrl = System.getProperty("mockServerUrl"); if (mockServerUrl == null) { mockServerUrl = System.getenv("MOCK_SERVER_URL"); } String mockUrl = (mockServerUrl != null ? mockServerUrl : "http://localhost:8000") + "/fixtures/embed_encoding_format";
        var client = LiterLlm.createClient("test-key", mockUrl, null, null, null);
        var result = client.embed(request);
assertEquals(1, result.data().size(), "expected exactly 1 elements");assertEquals(5, result.data().get(0).embedding().size(), "expected exactly 5 elements");

    }


    @Test
    void testEmbedError401() throws Exception {
        // 401 Unauthorized error on embedding request when API key is invalid
        // Wrap setup_lines + call_expr inside the lambda so error fixtures
        // catch failures at *any* step — including `<Type>.fromJson(...)`
        // calls that throw on malformed JSON (e.g. error fixtures with an
        // invalid enum value like `"purpose":"invalid-purpose"`). Mirrors
        // the C# `Assert.ThrowsAnyAsync(() => client.X(Type.FromJson(...)))`
        // pattern.
        assertThrows(Exception.class, () -> {
        var request = JsonUtil.fromJson("{\"input\":\"Hello world\",\"model\":\"text-embedding-3-small\"}", EmbeddingRequest.class);

            String mockServerUrl = System.getProperty("mockServerUrl"); if (mockServerUrl == null) { mockServerUrl = System.getenv("MOCK_SERVER_URL"); } String mockUrl = (mockServerUrl != null ? mockServerUrl : "http://localhost:8000") + "/fixtures/embed_error_401";
            var client = LiterLlm.createClient("test-key", mockUrl, null, null, null);
            client.embed(request);
        });

    }


    @Test
    void testEmbedWithDimensions() throws Exception {
        // Embedding request with explicit dimensions parameter returns embeddings of the requested size
        var request = JsonUtil.fromJson("{\"dimensions\":256,\"input\":\"Hello world\",\"model\":\"text-embedding-3-small\"}", EmbeddingRequest.class);

        String mockServerUrl = System.getProperty("mockServerUrl"); if (mockServerUrl == null) { mockServerUrl = System.getenv("MOCK_SERVER_URL"); } String mockUrl = (mockServerUrl != null ? mockServerUrl : "http://localhost:8000") + "/fixtures/embed_with_dimensions";
        var client = LiterLlm.createClient("test-key", mockUrl, null, null, null);
        var result = client.embed(request);
assertEquals(1, result.data().size(), "expected exactly 1 elements");assertEquals(8, result.data().get(0).embedding().size(), "expected exactly 8 elements");

    }


    @Test
    void testLocalEmbedOllama() throws Exception {
        // Embedding request via Ollama local provider with all-minilm model
        var request = JsonUtil.fromJson("{\"input\":\"The quick brown fox jumps over the lazy dog\",\"model\":\"ollama/all-minilm\"}", EmbeddingRequest.class);

        String mockServerUrl = System.getProperty("mockServerUrl"); if (mockServerUrl == null) { mockServerUrl = System.getenv("MOCK_SERVER_URL"); } String mockUrl = (mockServerUrl != null ? mockServerUrl : "http://localhost:8000") + "/fixtures/local_embed_ollama";
        var client = LiterLlm.createClient("test-key", mockUrl, null, null, null);
        var result = client.embed(request);
assertEquals(1, result.data().size(), "expected exactly 1 elements");assertEquals(32, result.data().get(0).embedding().size(), "expected exactly 32 elements");

    }

}
