// This file is auto-generated by alef — DO NOT EDIT.
// alef:hash:fe64a8a06beeb01b5344005fa07dcdfdf3e244d772ace0f89dbba84b0541ca2d
// To regenerate: alef generate
// To verify freshness: alef verify --exit-code

package dev.kreuzberg.literllm.e2e;

import org.junit.jupiter.api.Test;
import static org.junit.jupiter.api.Assertions.*;
import dev.kreuzberg.literllm.LiterLlm;
import dev.kreuzberg.literllm.ChatCompletionRequest;
import java.util.Optional;
import dev.kreuzberg.literllm.JsonUtil;
import dev.kreuzberg.literllm.ChatCompletionChunk;
import dev.kreuzberg.literllm.ChatCompletionRequest;

/** E2e tests for category: streaming. */
public class StreamingTest {

    @Test
    void testAnthropicStream() throws Exception {
        // Streaming chat completion via the Anthropic provider (claude-3-5-sonnet-20241022) yielding multiple SSE chunks
        var request = JsonUtil.fromJson("{\"max_tokens\":32,\"messages\":[{\"content\":\"Count to three, one word per response.\",\"role\":\"user\"}],\"model\":\"anthropic/claude-3-5-sonnet-20241022\",\"stream\":true}", ChatCompletionRequest.class);

        String mockServerUrl = System.getProperty("mockServerUrl"); if (mockServerUrl == null) { mockServerUrl = System.getenv("MOCK_SERVER_URL"); } String mockUrl = (mockServerUrl != null ? mockServerUrl : "http://localhost:8000") + "/fixtures/anthropic_stream";
        var client = LiterLlm.createClient("test-key", mockUrl, null, null, null);
        var result = client.chatStream(request);
        var chunks = new java.util.ArrayList<ChatCompletionChunk>();
        var _it = result.iterator();
        while (_it.hasNext()) { chunks.add(_it.next()); }
        assertTrue(chunks.size() >= 3, "expected >= 3 chunks");
        assertEquals("One Two Three", chunks.stream().map(c -> c.choices().stream().findFirst().map(ch -> ch.delta().content() != null ? ch.delta().content() : "").orElse("")).collect(java.util.stream.Collectors.joining()));
        assertTrue(!chunks.isEmpty() && chunks.get(chunks.size()-1).choices().stream().findFirst().flatMap(ch -> java.util.Optional.ofNullable(ch.finishReason())).isPresent(), "expected true");


    }


    @Test
    void testAzureStream() throws Exception {
        // Streaming chat completion via Azure OpenAI — verifies the azure/ prefix routes correctly and SSE chunks are delivered in the standard OpenAI chat.completion.chunk shape
        var request = JsonUtil.fromJson("{\"messages\":[{\"content\":\"Count to 3\",\"role\":\"user\"}],\"model\":\"azure/gpt-4\",\"stream\":true,\"temperature\":0}", ChatCompletionRequest.class);

        String mockServerUrl = System.getProperty("mockServerUrl"); if (mockServerUrl == null) { mockServerUrl = System.getenv("MOCK_SERVER_URL"); } String mockUrl = (mockServerUrl != null ? mockServerUrl : "http://localhost:8000") + "/fixtures/azure_stream";
        var client = LiterLlm.createClient("test-key", mockUrl, null, null, null);
        var result = client.chatStream(request);
        var chunks = new java.util.ArrayList<ChatCompletionChunk>();
        var _it = result.iterator();
        while (_it.hasNext()) { chunks.add(_it.next()); }
        assertTrue(chunks.size() >= 3, "expected >= 3 chunks");
        assertEquals("1 2 3", chunks.stream().map(c -> c.choices().stream().findFirst().map(ch -> ch.delta().content() != null ? ch.delta().content() : "").orElse("")).collect(java.util.stream.Collectors.joining()));
        assertTrue(!chunks.isEmpty() && chunks.get(chunks.size()-1).choices().stream().findFirst().flatMap(ch -> java.util.Optional.ofNullable(ch.finishReason())).isPresent(), "expected true");


    }


    @Test
    void testBasicStream() throws Exception {
        // Streaming chat completion that produces content across multiple SSE chunks
        var request = JsonUtil.fromJson("{\"messages\":[{\"content\":\"Count to 3\",\"role\":\"user\"}],\"model\":\"gpt-4\",\"stream\":true}", ChatCompletionRequest.class);

        String mockServerUrl = System.getProperty("mockServerUrl"); if (mockServerUrl == null) { mockServerUrl = System.getenv("MOCK_SERVER_URL"); } String mockUrl = (mockServerUrl != null ? mockServerUrl : "http://localhost:8000") + "/fixtures/basic_stream";
        var client = LiterLlm.createClient("test-key", mockUrl, null, null, null);
        var result = client.chatStream(request);
        var chunks = new java.util.ArrayList<ChatCompletionChunk>();
        var _it = result.iterator();
        while (_it.hasNext()) { chunks.add(_it.next()); }
        assertTrue(chunks.size() >= 3, "expected >= 3 chunks");
        assertEquals("1 2 3", chunks.stream().map(c -> c.choices().stream().findFirst().map(ch -> ch.delta().content() != null ? ch.delta().content() : "").orElse("")).collect(java.util.stream.Collectors.joining()));


    }


    @Test
    void testBedrockStream() throws Exception {
        // Streaming chat completion via the AWS Bedrock provider using the bedrock/ prefix — verifies SSE chunks are yielded and assembled correctly from the Converse streaming API
        var request = JsonUtil.fromJson("{\"max_tokens\":32,\"messages\":[{\"content\":\"Count to three, one word per response.\",\"role\":\"user\"}],\"model\":\"bedrock/anthropic.claude-3-sonnet-20240229-v1:0\",\"stream\":true}", ChatCompletionRequest.class);

        String mockServerUrl = System.getProperty("mockServerUrl"); if (mockServerUrl == null) { mockServerUrl = System.getenv("MOCK_SERVER_URL"); } String mockUrl = (mockServerUrl != null ? mockServerUrl : "http://localhost:8000") + "/fixtures/bedrock_stream";
        var client = LiterLlm.createClient("test-key", mockUrl, null, null, null);
        var result = client.chatStream(request);
        var chunks = new java.util.ArrayList<ChatCompletionChunk>();
        var _it = result.iterator();
        while (_it.hasNext()) { chunks.add(_it.next()); }
        assertTrue(chunks.size() >= 2, "expected >= 2 chunks");
        assertEquals("One Two Three", chunks.stream().map(c -> c.choices().stream().findFirst().map(ch -> ch.delta().content() != null ? ch.delta().content() : "").orElse("")).collect(java.util.stream.Collectors.joining()));
        assertTrue(!chunks.isEmpty() && chunks.get(chunks.size()-1).choices().stream().findFirst().flatMap(ch -> java.util.Optional.ofNullable(ch.finishReason())).isPresent(), "expected true");


    }


    @Test
    void testEdgeStreamFunctionCall() throws Exception {
        // Streaming chat completion with tool/function call chunks
        var request = JsonUtil.fromJson("{\"messages\":[{\"content\":\"What's the weather?\",\"role\":\"user\"}],\"model\":\"gpt-4\",\"tools\":[{\"function\":{\"name\":\"get_weather\",\"parameters\":{\"properties\":{\"city\":{\"type\":\"string\"}},\"type\":\"object\"}},\"type\":\"function\"}]}", ChatCompletionRequest.class);

        String mockServerUrl = System.getProperty("mockServerUrl"); if (mockServerUrl == null) { mockServerUrl = System.getenv("MOCK_SERVER_URL"); } String mockUrl = (mockServerUrl != null ? mockServerUrl : "http://localhost:8000") + "/fixtures/edge_stream_function_call";
        var client = LiterLlm.createClient("test-key", mockUrl, null, null, null);
        var result = client.chatStream(request);
        var chunks = new java.util.ArrayList<ChatCompletionChunk>();
        var _it = result.iterator();
        while (_it.hasNext()) { chunks.add(_it.next()); }
        assertTrue(chunks.size() >= 2, "expected >= 2 chunks");


    }


    @Test
    void testEmptyStream() throws Exception {
        // Streaming chat completion that produces no content chunks before the DONE signal
        var request = JsonUtil.fromJson("{\"messages\":[{\"content\":\"Say nothing\",\"role\":\"user\"}],\"model\":\"gpt-4\",\"stream\":true}", ChatCompletionRequest.class);

        String mockServerUrl = System.getProperty("mockServerUrl"); if (mockServerUrl == null) { mockServerUrl = System.getenv("MOCK_SERVER_URL"); } String mockUrl = (mockServerUrl != null ? mockServerUrl : "http://localhost:8000") + "/fixtures/empty_stream";
        var client = LiterLlm.createClient("test-key", mockUrl, null, null, null);
        var result = client.chatStream(request);
        var chunks = new java.util.ArrayList<ChatCompletionChunk>();
        var _it = result.iterator();
        while (_it.hasNext()) { chunks.add(_it.next()); }
        assertTrue(chunks.size() >= 0, "expected >= 0 chunks");
        assertEquals("", chunks.stream().map(c -> c.choices().stream().findFirst().map(ch -> ch.delta().content() != null ? ch.delta().content() : "").orElse("")).collect(java.util.stream.Collectors.joining()));


    }


    @Test
    void testLocalStreamOllama() throws Exception {
        // Streaming chat completion via Ollama local provider with SSE chunks
        var request = JsonUtil.fromJson("{\"messages\":[{\"content\":\"Count to 3\",\"role\":\"user\"}],\"model\":\"ollama/qwen2:0.5b\",\"stream\":true}", ChatCompletionRequest.class);

        String mockServerUrl = System.getProperty("mockServerUrl"); if (mockServerUrl == null) { mockServerUrl = System.getenv("MOCK_SERVER_URL"); } String mockUrl = (mockServerUrl != null ? mockServerUrl : "http://localhost:8000") + "/fixtures/local_stream_ollama";
        var client = LiterLlm.createClient("test-key", mockUrl, null, null, null);
        var result = client.chatStream(request);
        var chunks = new java.util.ArrayList<ChatCompletionChunk>();
        var _it = result.iterator();
        while (_it.hasNext()) { chunks.add(_it.next()); }
        assertTrue(chunks.size() >= 3, "expected >= 3 chunks");
        assertEquals("1 2 3", chunks.stream().map(c -> c.choices().stream().findFirst().map(ch -> ch.delta().content() != null ? ch.delta().content() : "").orElse("")).collect(java.util.stream.Collectors.joining()));
        assertTrue(!chunks.isEmpty() && chunks.get(chunks.size()-1).choices().stream().findFirst().flatMap(ch -> java.util.Optional.ofNullable(ch.finishReason())).isPresent(), "expected true");


    }


    @Test
    void testStreamContentPolicyError() throws Exception {
        // 400 Bad Request error on stream due to content policy violation
        // Wrap setup_lines + call_expr inside the lambda so error fixtures
        // catch failures at *any* step — including `<Type>.fromJson(...)`
        // calls that throw on malformed JSON (e.g. error fixtures with an
        // invalid enum value like `"purpose":"invalid-purpose"`). Mirrors
        // the C# `Assert.ThrowsAnyAsync(() => client.X(Type.FromJson(...)))`
        // pattern.
        assertThrows(Exception.class, () -> {
        var request = JsonUtil.fromJson("{\"messages\":[{\"content\":\"Generate harmful content\",\"role\":\"user\"}],\"model\":\"gpt-4o\",\"stream\":true}", ChatCompletionRequest.class);

            String mockServerUrl = System.getProperty("mockServerUrl"); if (mockServerUrl == null) { mockServerUrl = System.getenv("MOCK_SERVER_URL"); } String mockUrl = (mockServerUrl != null ? mockServerUrl : "http://localhost:8000") + "/fixtures/stream_content_policy_error";
            var client = LiterLlm.createClient("test-key", mockUrl, null, null, null);
            client.chatStream(request);
        });

    }


    @Test
    void testStreamDoneSignal() throws Exception {
        // Verify that the [DONE] sentinel signal properly terminates the stream
        var request = JsonUtil.fromJson("{\"messages\":[{\"content\":\"Say done\",\"role\":\"user\"}],\"model\":\"gpt-4\",\"stream\":true}", ChatCompletionRequest.class);

        String mockServerUrl = System.getProperty("mockServerUrl"); if (mockServerUrl == null) { mockServerUrl = System.getenv("MOCK_SERVER_URL"); } String mockUrl = (mockServerUrl != null ? mockServerUrl : "http://localhost:8000") + "/fixtures/stream_done_signal";
        var client = LiterLlm.createClient("test-key", mockUrl, null, null, null);
        var result = client.chatStream(request);
        var chunks = new java.util.ArrayList<ChatCompletionChunk>();
        var _it = result.iterator();
        while (_it.hasNext()) { chunks.add(_it.next()); }
        assertTrue(!chunks.isEmpty() && chunks.get(chunks.size()-1).choices().stream().findFirst().flatMap(ch -> java.util.Optional.ofNullable(ch.finishReason())).isPresent(), "expected true");
        assertEquals("Done", chunks.stream().map(c -> c.choices().stream().findFirst().map(ch -> ch.delta().content() != null ? ch.delta().content() : "").orElse("")).collect(java.util.stream.Collectors.joining()));
        assertTrue(true, "expected true");


    }


    @Test
    void testStreamError401() throws Exception {
        // 401 Unauthorized error on stream initiation before any chunks are received
        // Wrap setup_lines + call_expr inside the lambda so error fixtures
        // catch failures at *any* step — including `<Type>.fromJson(...)`
        // calls that throw on malformed JSON (e.g. error fixtures with an
        // invalid enum value like `"purpose":"invalid-purpose"`). Mirrors
        // the C# `Assert.ThrowsAnyAsync(() => client.X(Type.FromJson(...)))`
        // pattern.
        assertThrows(Exception.class, () -> {
        var request = JsonUtil.fromJson("{\"messages\":[{\"content\":\"Hello\",\"role\":\"user\"}],\"model\":\"gpt-4\",\"stream\":true}", ChatCompletionRequest.class);

            String mockServerUrl = System.getProperty("mockServerUrl"); if (mockServerUrl == null) { mockServerUrl = System.getenv("MOCK_SERVER_URL"); } String mockUrl = (mockServerUrl != null ? mockServerUrl : "http://localhost:8000") + "/fixtures/stream_error_401";
            var client = LiterLlm.createClient("test-key", mockUrl, null, null, null);
            client.chatStream(request);
        });

    }


    @Test
    void testStreamMultipleChoices() throws Exception {
        // Streaming chat completion with multiple choice outputs (n > 1)
        var request = JsonUtil.fromJson("{\"messages\":[{\"content\":\"Hello\",\"role\":\"user\"}],\"model\":\"gpt-4o\",\"n\":2,\"stream\":true}", ChatCompletionRequest.class);

        String mockServerUrl = System.getProperty("mockServerUrl"); if (mockServerUrl == null) { mockServerUrl = System.getenv("MOCK_SERVER_URL"); } String mockUrl = (mockServerUrl != null ? mockServerUrl : "http://localhost:8000") + "/fixtures/stream_multiple_choices";
        var client = LiterLlm.createClient("test-key", mockUrl, null, null, null);
        var result = client.chatStream(request);
        var chunks = new java.util.ArrayList<ChatCompletionChunk>();
        var _it = result.iterator();
        while (_it.hasNext()) { chunks.add(_it.next()); }


    }


    @Test
    void testStreamWithToolCalls() throws Exception {
        // Streaming chat completion where the assistant responds with a tool call across multiple chunks
        var request = JsonUtil.fromJson("{\"messages\":[{\"content\":\"What is the weather in NYC?\",\"role\":\"user\"}],\"model\":\"gpt-4\",\"stream\":true,\"tools\":[{\"function\":{\"description\":\"Get the current weather for a given location\",\"name\":\"get_weather\",\"parameters\":{\"properties\":{\"location\":{\"description\":\"The city and state, e.g. New York, NY\",\"type\":\"string\"}},\"required\":[\"location\"],\"type\":\"object\"}},\"type\":\"function\"}]}", ChatCompletionRequest.class);

        String mockServerUrl = System.getProperty("mockServerUrl"); if (mockServerUrl == null) { mockServerUrl = System.getenv("MOCK_SERVER_URL"); } String mockUrl = (mockServerUrl != null ? mockServerUrl : "http://localhost:8000") + "/fixtures/stream_with_tool_calls";
        var client = LiterLlm.createClient("test-key", mockUrl, null, null, null);
        var result = client.chatStream(request);
        var chunks = new java.util.ArrayList<ChatCompletionChunk>();
        var _it = result.iterator();
        while (_it.hasNext()) { chunks.add(_it.next()); }
        assertTrue(chunks.size() >= 4, "expected >= 4 chunks");
        assertEquals("tool_calls", (chunks.isEmpty() ? null : chunks.get(chunks.size()-1).choices().stream().findFirst().map(ch -> ch.finishReason() == null ? null : ch.finishReason().getValue()).orElse(null)));
        assertFalse(chunks.stream().flatMap(c -> c.choices().stream()).flatMap(ch -> ch.delta().toolCalls() != null ? ch.delta().toolCalls().stream() : java.util.stream.Stream.empty()).toList().isEmpty(), "expected non-empty");
        assertEquals("get_weather", (chunks.stream().flatMap(c -> c.choices().stream()).flatMap(ch -> ch.delta().toolCalls() != null ? ch.delta().toolCalls().stream() : java.util.stream.Stream.empty()).toList()).get(0).function().name());


    }


    @Test
    void testVertexStream() throws Exception {
        // Streaming chat completion via the Google Vertex AI provider using the vertex_ai/ prefix — verifies SSE chunks from the Gemini streaming endpoint are yielded and assembled correctly
        var request = JsonUtil.fromJson("{\"max_tokens\":32,\"messages\":[{\"content\":\"Count to three, one word per response.\",\"role\":\"user\"}],\"model\":\"vertex_ai/gemini-2.0-flash\",\"stream\":true}", ChatCompletionRequest.class);

        String mockServerUrl = System.getProperty("mockServerUrl"); if (mockServerUrl == null) { mockServerUrl = System.getenv("MOCK_SERVER_URL"); } String mockUrl = (mockServerUrl != null ? mockServerUrl : "http://localhost:8000") + "/fixtures/vertex_stream";
        var client = LiterLlm.createClient("test-key", mockUrl, null, null, null);
        var result = client.chatStream(request);
        var chunks = new java.util.ArrayList<ChatCompletionChunk>();
        var _it = result.iterator();
        while (_it.hasNext()) { chunks.add(_it.next()); }
        assertTrue(chunks.size() >= 2, "expected >= 2 chunks");
        assertEquals("One Two Three", chunks.stream().map(c -> c.choices().stream().findFirst().map(ch -> ch.delta().content() != null ? ch.delta().content() : "").orElse("")).collect(java.util.stream.Collectors.joining()));
        assertTrue(!chunks.isEmpty() && chunks.get(chunks.size()-1).choices().stream().findFirst().flatMap(ch -> java.util.Optional.ofNullable(ch.finishReason())).isPresent(), "expected true");


    }

}
