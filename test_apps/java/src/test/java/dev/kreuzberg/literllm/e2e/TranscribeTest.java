// This file is auto-generated by alef — DO NOT EDIT.
// alef:hash:ed71254ddce5aee3d0319726a18ab5c3f9b4dae16d1a48b2ae9903ba49c333bc
// To regenerate: alef generate
// To verify freshness: alef verify --exit-code
// Issues & docs: https://github.com/kreuzberg-dev/alef

package dev.kreuzberg.literllm.e2e;

import org.junit.jupiter.api.Test;
import static org.junit.jupiter.api.Assertions.*;
import dev.kreuzberg.literllm.LiterLlm;
import dev.kreuzberg.literllm.CreateTranscriptionRequest;
import java.util.Optional;

/** E2e tests for category: transcribe. */
class TranscribeTest {

    @Test
    void testEdgeTranscribeEmptyAudio() throws Exception {
        // Transcription of a silent or empty audio file returns empty text
        var request = CreateTranscriptionRequest.fromJson("{\"file\":\"silence.mp3\",\"model\":\"whisper-1\"}");

        String mockUrl = System.getProperty("mockServerUrl", System.getenv("MOCK_SERVER_URL")) + "/fixtures/edge_transcribe_empty_audio";
        var client = LiterLlm.createClient("test-key", mockUrl, null, null, null);
        var result = client.transcribe(request);
assertTrue(result.text().contains(""), "expected to contain: " + "");
    }


    @Test
    void testEdgeTranscribeWithTimestamps() throws Exception {
        // Transcription with verbose JSON response format including timestamp segments
        var request = CreateTranscriptionRequest.fromJson("{\"file\":\"audio.mp3\",\"model\":\"whisper-1\",\"response_format\":\"verbose_json\"}");

        String mockUrl = System.getProperty("mockServerUrl", System.getenv("MOCK_SERVER_URL")) + "/fixtures/edge_transcribe_with_timestamps";
        var client = LiterLlm.createClient("test-key", mockUrl, null, null, null);
        var result = client.transcribe(request);
assertFalse(result.text().isEmpty(), "expected non-empty value");assertEquals(3, java.util.Optional.ofNullable(result.segments()).orElse(java.util.List.of()).size(), "expected exactly 3 elements");assertEquals(0, result.segments().get(0).id());
    }


    @Test
    void testErrorTranscribeAuth401() throws Exception {
        // 401 Unauthorized for transcription with invalid API key
        // Wrap setup_lines + call_expr inside the lambda so error fixtures
        // catch failures at *any* step — including `<Type>.fromJson(...)`
        // calls that throw on malformed JSON (e.g. error fixtures with an
        // invalid enum value like `"purpose":"invalid-purpose"`). Mirrors
        // the C# `Assert.ThrowsAnyAsync(() => client.X(Type.FromJson(...)))`
        // pattern.
        assertThrows(Exception.class, () -> {
        var request = CreateTranscriptionRequest.fromJson("{\"file\":\"audio.mp3\",\"model\":\"whisper-1\"}");

            String mockUrl = System.getProperty("mockServerUrl", System.getenv("MOCK_SERVER_URL")) + "/fixtures/error_transcribe_auth_401";
            var client = LiterLlm.createClient("test-key", mockUrl, null, null, null);
            client.transcribe(request);
        });
    }


    @Test
    void testErrorTranscribeBadFormat() throws Exception {
        // 400 Bad Request when audio format is unsupported
        // Wrap setup_lines + call_expr inside the lambda so error fixtures
        // catch failures at *any* step — including `<Type>.fromJson(...)`
        // calls that throw on malformed JSON (e.g. error fixtures with an
        // invalid enum value like `"purpose":"invalid-purpose"`). Mirrors
        // the C# `Assert.ThrowsAnyAsync(() => client.X(Type.FromJson(...)))`
        // pattern.
        assertThrows(Exception.class, () -> {
        var request = CreateTranscriptionRequest.fromJson("{\"file\":\"audio.xyz\",\"model\":\"whisper-1\"}");

            String mockUrl = System.getProperty("mockServerUrl", System.getenv("MOCK_SERVER_URL")) + "/fixtures/error_transcribe_bad_format";
            var client = LiterLlm.createClient("test-key", mockUrl, null, null, null);
            client.transcribe(request);
        });
    }


    @Test
    void testSmokeTranscribeBasic() throws Exception {
        // Basic audio transcription
        var request = CreateTranscriptionRequest.fromJson("{\"file\":\"audio.mp3\",\"model\":\"whisper-1\"}");

        String mockUrl = System.getProperty("mockServerUrl", System.getenv("MOCK_SERVER_URL")) + "/fixtures/smoke_transcribe_basic";
        var client = LiterLlm.createClient("test-key", mockUrl, null, null, null);
        var result = client.transcribe(request);
assertTrue(result.text().contains("Hello, this is a test transcription."), "expected to contain: " + "Hello, this is a test transcription.");
    }


    @Test
    void testSmokeTranscribeWithLanguage() throws Exception {
        // Audio transcription with explicit language hint
        var request = CreateTranscriptionRequest.fromJson("{\"file\":\"audio_de.mp3\",\"language\":\"de\",\"model\":\"whisper-1\"}");

        String mockUrl = System.getProperty("mockServerUrl", System.getenv("MOCK_SERVER_URL")) + "/fixtures/smoke_transcribe_with_language";
        var client = LiterLlm.createClient("test-key", mockUrl, null, null, null);
        var result = client.transcribe(request);
assertTrue(result.text().contains("Hallo, dies ist ein Testtranskription."), "expected to contain: " + "Hallo, dies ist ein Testtranskription.");
    }

}
