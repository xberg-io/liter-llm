// This file is auto-generated by alef — DO NOT EDIT.
// alef:hash:419204b66f16f0a46756e52ee2a70660e87d41eea851478a78189354fad3dc5d
// To regenerate: alef generate
// To verify freshness: alef verify --exit-code
// Issues & docs: https://github.com/sample_crate-dev/alef

package dev.kreuzberg.literllm.e2e;

import org.junit.jupiter.api.Test;
import static org.junit.jupiter.api.Assertions.*;
import dev.kreuzberg.literllm.LiterLlm;
import dev.kreuzberg.literllm.ChatCompletionRequest;
import dev.kreuzberg.literllm.EmbeddingRequest;
import java.util.Optional;
import dev.kreuzberg.literllm.JsonUtil;
import dev.kreuzberg.literllm.ChatCompletionChunk;
import dev.kreuzberg.literllm.ChatCompletionRequest;

/** E2e tests for category: smoke. */
class SmokeTest {

    @Test
    void testAnthropicChat() throws Exception {
        // Basic chat completion via the Anthropic provider (claude-3-5-sonnet-20241022) with system and user messages
        var request = JsonUtil.fromJson("{\"max_tokens\":16,\"messages\":[{\"content\":\"You are a helpful assistant.\",\"role\":\"system\"},{\"content\":\"Say hello in one word.\",\"role\":\"user\"}],\"model\":\"anthropic/claude-3-5-sonnet-20241022\",\"temperature\":0}", ChatCompletionRequest.class);

        String mockServerUrl = System.getProperty("mockServerUrl"); if (mockServerUrl == null) { mockServerUrl = System.getenv("MOCK_SERVER_URL"); } String mockUrl = (mockServerUrl != null ? mockServerUrl : "http://localhost:8000") + "/fixtures/anthropic_chat";
        var client = LiterLlm.createClient("test-key", mockUrl, null, null, null);
        var result = client.chat(request);
assertEquals(1, result.choices().size(), "expected exactly 1 elements");assertEquals("Hello!", java.util.Optional.ofNullable(result.choices().get(0).message().content()).map(java.util.Objects::toString).orElse("").trim());assertEquals("stop", java.util.Optional.ofNullable(result.choices().get(0).finishReason()).map(v -> v.getValue()).orElse("").trim());assertEquals("claude-3-5-sonnet-20241022", result.model().trim());assertEquals(23, result.usage().totalTokens());
    }


    @Test
    void testAzureChat() throws Exception {
        // Chat completion via Azure OpenAI with the azure/ prefix for provider routing — verifies the prefix is stripped before dispatching and the response is normalised to the standard OpenAI chat completion shape
        var request = JsonUtil.fromJson("{\"max_tokens\":16,\"messages\":[{\"content\":\"Say hello\",\"role\":\"user\"}],\"model\":\"azure/gpt-4\",\"temperature\":0}", ChatCompletionRequest.class);

        String mockServerUrl = System.getProperty("mockServerUrl"); if (mockServerUrl == null) { mockServerUrl = System.getenv("MOCK_SERVER_URL"); } String mockUrl = (mockServerUrl != null ? mockServerUrl : "http://localhost:8000") + "/fixtures/azure_chat";
        var client = LiterLlm.createClient("test-key", mockUrl, null, null, null);
        var result = client.chat(request);
assertEquals(1, result.choices().size(), "expected exactly 1 elements");assertEquals("Hello!", java.util.Optional.ofNullable(result.choices().get(0).message().content()).map(java.util.Objects::toString).orElse("").trim());assertEquals("stop", java.util.Optional.ofNullable(result.choices().get(0).finishReason()).map(v -> v.getValue()).orElse("").trim());assertEquals("gpt-4", result.model().trim());assertEquals(13, result.usage().totalTokens());
    }


    @Test
    void testAzureEmbed() throws Exception {
        // Embedding request via Azure OpenAI using the azure/ provider prefix — response follows the standard OpenAI embeddings shape that Azure returns unchanged
        var request = JsonUtil.fromJson("{\"input\":\"Hello world\",\"model\":\"azure/text-embedding-ada-002\"}", EmbeddingRequest.class);

        String mockServerUrl = System.getProperty("mockServerUrl"); if (mockServerUrl == null) { mockServerUrl = System.getenv("MOCK_SERVER_URL"); } String mockUrl = (mockServerUrl != null ? mockServerUrl : "http://localhost:8000") + "/fixtures/azure_embed";
        var client = LiterLlm.createClient("test-key", mockUrl, null, null, null);
        var result = client.embed(request);
assertEquals(1, result.data().size(), "expected exactly 1 elements");assertEquals(1480, result.data().get(0).embedding().size(), "expected exactly 1480 elements");
    }


    @Test
    void testBasicChat() throws Exception {
        // Basic chat completion with a single user message
        var request = JsonUtil.fromJson("{\"messages\":[{\"content\":\"Say hello\",\"role\":\"user\"}],\"model\":\"gpt-4\",\"temperature\":0}", ChatCompletionRequest.class);

        String mockServerUrl = System.getProperty("mockServerUrl"); if (mockServerUrl == null) { mockServerUrl = System.getenv("MOCK_SERVER_URL"); } String mockUrl = (mockServerUrl != null ? mockServerUrl : "http://localhost:8000") + "/fixtures/basic_chat";
        var client = LiterLlm.createClient("test-key", mockUrl, null, null, null);
        var result = client.chat(request);
assertEquals(1, result.choices().size(), "expected exactly 1 elements");assertEquals("Hello!", java.util.Optional.ofNullable(result.choices().get(0).message().content()).map(java.util.Objects::toString).orElse("").trim());assertEquals("stop", java.util.Optional.ofNullable(result.choices().get(0).finishReason()).map(v -> v.getValue()).orElse("").trim());assertEquals(15, result.usage().totalTokens());assertEquals("gpt-4", result.model().trim());
    }


    @Test
    void testBasicEmbed() throws Exception {
        // Basic embedding request for a single input string
        var request = JsonUtil.fromJson("{\"input\":\"Hello world\",\"model\":\"text-embedding-3-small\"}", EmbeddingRequest.class);

        String mockServerUrl = System.getProperty("mockServerUrl"); if (mockServerUrl == null) { mockServerUrl = System.getenv("MOCK_SERVER_URL"); } String mockUrl = (mockServerUrl != null ? mockServerUrl : "http://localhost:8000") + "/fixtures/basic_embed";
        var client = LiterLlm.createClient("test-key", mockUrl, null, null, null);
        var result = client.embed(request);
assertEquals(1, result.data().size(), "expected exactly 1 elements");assertEquals(5, result.data().get(0).embedding().size(), "expected exactly 5 elements");
    }


    @Test
    void testBasicListModels() throws Exception {
        // List available models from the API
        String mockServerUrl = System.getProperty("mockServerUrl"); if (mockServerUrl == null) { mockServerUrl = System.getenv("MOCK_SERVER_URL"); } String mockUrl = (mockServerUrl != null ? mockServerUrl : "http://localhost:8000") + "/fixtures/basic_list_models";
        var client = LiterLlm.createClient("test-key", mockUrl, null, null, null);
        var result = client.listModels();
assertTrue(result.data().size() >= 1, "expected at least 1 elements");
    }


    @Test
    void testBedrockChat() throws Exception {
        // Basic chat completion via the AWS Bedrock provider using the bedrock/ prefix for routing — verifies the prefix is stripped before dispatching and the Converse API response is normalised to the standard OpenAI chat completion shape
        var request = JsonUtil.fromJson("{\"max_tokens\":16,\"messages\":[{\"content\":\"Say hello in one word.\",\"role\":\"user\"}],\"model\":\"bedrock/anthropic.claude-3-sonnet-20240229-v1:0\",\"temperature\":0}", ChatCompletionRequest.class);

        String mockServerUrl = System.getProperty("mockServerUrl"); if (mockServerUrl == null) { mockServerUrl = System.getenv("MOCK_SERVER_URL"); } String mockUrl = (mockServerUrl != null ? mockServerUrl : "http://localhost:8000") + "/fixtures/bedrock_chat";
        var client = LiterLlm.createClient("test-key", mockUrl, null, null, null);
        var result = client.chat(request);
assertEquals(1, result.choices().size(), "expected exactly 1 elements");assertEquals("Hello!", java.util.Optional.ofNullable(result.choices().get(0).message().content()).map(java.util.Objects::toString).orElse("").trim());assertEquals("stop", java.util.Optional.ofNullable(result.choices().get(0).finishReason()).map(v -> v.getValue()).orElse("").trim());assertEquals("anthropic.claude-3-sonnet-20240229-v1:0", result.model().trim());assertEquals(15, result.usage().totalTokens());
    }


    @Test
    void testGithubCopilotChat() throws Exception {
        // Basic chat completion via the GitHub Copilot provider (gpt-4o) with a single user message
        var request = JsonUtil.fromJson("{\"max_tokens\":16,\"messages\":[{\"content\":\"Say hello in one word.\",\"role\":\"user\"}],\"model\":\"github_copilot/gpt-4o\",\"temperature\":0}", ChatCompletionRequest.class);

        String mockServerUrl = System.getProperty("mockServerUrl"); if (mockServerUrl == null) { mockServerUrl = System.getenv("MOCK_SERVER_URL"); } String mockUrl = (mockServerUrl != null ? mockServerUrl : "http://localhost:8000") + "/fixtures/github_copilot_chat";
        var client = LiterLlm.createClient("test-key", mockUrl, null, null, null);
        var result = client.chat(request);
assertEquals(1, result.choices().size(), "expected exactly 1 elements");assertEquals("Hello!", java.util.Optional.ofNullable(result.choices().get(0).message().content()).map(java.util.Objects::toString).orElse("").trim());assertEquals("stop", java.util.Optional.ofNullable(result.choices().get(0).finishReason()).map(v -> v.getValue()).orElse("").trim());assertEquals("gpt-4o", result.model().trim());assertEquals(14, result.usage().totalTokens());
    }


    @Test
    void testSmokeCacheMemory() throws Exception {
        // Test in-memory caching by sending identical requests twice
        var request = JsonUtil.fromJson("{\"max_tokens\":5,\"messages\":[{\"content\":\"What is 2+2? Answer with just the number.\",\"role\":\"user\"}],\"model\":\"openai/gpt-4o-mini\"}", ChatCompletionRequest.class);

        String apiKey = System.getenv("OPENAI_API_KEY");
        String mockServerUrl = System.getProperty("mockServerUrl"); if (mockServerUrl == null) { mockServerUrl = System.getenv("MOCK_SERVER_URL"); } String baseUrl = (apiKey != null && !apiKey.isEmpty()) ? null : (mockServerUrl != null ? mockServerUrl + "/fixtures/smoke_cache_memory" : "http://localhost:8000/fixtures/smoke_cache_memory");
        System.out.println("smoke_cache_memory: " + (baseUrl == null ? "using real API (OPENAI_API_KEY is set)" : "using mock server (OPENAI_API_KEY not set)"));
        var client = LiterLlm.createClient(baseUrl == null ? apiKey : "test-key", baseUrl, null, null, null);
        var result = client.chat(request);
assertFalse(result.choices().isEmpty(), "expected non-empty value");
    }


    @Test
    void testSmokeChatAnthropic() throws Exception {
        // Basic chat completion against real Anthropic API
        var request = JsonUtil.fromJson("{\"max_tokens\":10,\"messages\":[{\"content\":\"Say hello in exactly one word.\",\"role\":\"user\"}],\"model\":\"anthropic/claude-sonnet-4-20250514\"}", ChatCompletionRequest.class);

        String apiKey = System.getenv("ANTHROPIC_API_KEY");
        String mockServerUrl = System.getProperty("mockServerUrl"); if (mockServerUrl == null) { mockServerUrl = System.getenv("MOCK_SERVER_URL"); } String baseUrl = (apiKey != null && !apiKey.isEmpty()) ? null : (mockServerUrl != null ? mockServerUrl + "/fixtures/smoke_chat_anthropic" : "http://localhost:8000/fixtures/smoke_chat_anthropic");
        System.out.println("smoke_chat_anthropic: " + (baseUrl == null ? "using real API (ANTHROPIC_API_KEY is set)" : "using mock server (ANTHROPIC_API_KEY not set)"));
        var client = LiterLlm.createClient(baseUrl == null ? apiKey : "test-key", baseUrl, null, null, null);
        var result = client.chat(request);
assertFalse(result.choices().isEmpty(), "expected non-empty value");assertFalse(java.util.Optional.ofNullable(result.usage()).isEmpty(), "expected non-empty value");
    }


    @Test
    void testSmokeChatGemini() throws Exception {
        // Basic chat completion against real Google Gemini API
        var request = JsonUtil.fromJson("{\"max_tokens\":10,\"messages\":[{\"content\":\"Say hello in exactly one word.\",\"role\":\"user\"}],\"model\":\"gemini/gemini-2.5-flash-lite\"}", ChatCompletionRequest.class);

        String apiKey = System.getenv("GEMINI_API_KEY");
        String mockServerUrl = System.getProperty("mockServerUrl"); if (mockServerUrl == null) { mockServerUrl = System.getenv("MOCK_SERVER_URL"); } String baseUrl = (apiKey != null && !apiKey.isEmpty()) ? null : (mockServerUrl != null ? mockServerUrl + "/fixtures/smoke_chat_gemini" : "http://localhost:8000/fixtures/smoke_chat_gemini");
        System.out.println("smoke_chat_gemini: " + (baseUrl == null ? "using real API (GEMINI_API_KEY is set)" : "using mock server (GEMINI_API_KEY not set)"));
        var client = LiterLlm.createClient(baseUrl == null ? apiKey : "test-key", baseUrl, null, null, null);
        var result = client.chat(request);
assertFalse(result.choices().isEmpty(), "expected non-empty value");assertFalse(java.util.Optional.ofNullable(result.usage()).isEmpty(), "expected non-empty value");
    }


    @Test
    void testSmokeChatOpenai() throws Exception {
        // Basic chat completion against real OpenAI API
        var request = JsonUtil.fromJson("{\"max_tokens\":10,\"messages\":[{\"content\":\"Say hello in exactly one word.\",\"role\":\"user\"}],\"model\":\"openai/gpt-4o-mini\"}", ChatCompletionRequest.class);

        String apiKey = System.getenv("OPENAI_API_KEY");
        String mockServerUrl = System.getProperty("mockServerUrl"); if (mockServerUrl == null) { mockServerUrl = System.getenv("MOCK_SERVER_URL"); } String baseUrl = (apiKey != null && !apiKey.isEmpty()) ? null : (mockServerUrl != null ? mockServerUrl + "/fixtures/smoke_chat_openai" : "http://localhost:8000/fixtures/smoke_chat_openai");
        System.out.println("smoke_chat_openai: " + (baseUrl == null ? "using real API (OPENAI_API_KEY is set)" : "using mock server (OPENAI_API_KEY not set)"));
        var client = LiterLlm.createClient(baseUrl == null ? apiKey : "test-key", baseUrl, null, null, null);
        var result = client.chat(request);
assertFalse(result.choices().isEmpty(), "expected non-empty value");assertFalse(java.util.Optional.ofNullable(result.usage()).isEmpty(), "expected non-empty value");
    }


    @Test
    void testSmokeEmbedOpenai() throws Exception {
        // Embeddings request against real OpenAI API
        var request = JsonUtil.fromJson("{\"input\":[\"Hello world\"],\"model\":\"openai/text-embedding-3-small\"}", EmbeddingRequest.class);

        String apiKey = System.getenv("OPENAI_API_KEY");
        String mockServerUrl = System.getProperty("mockServerUrl"); if (mockServerUrl == null) { mockServerUrl = System.getenv("MOCK_SERVER_URL"); } String baseUrl = (apiKey != null && !apiKey.isEmpty()) ? null : (mockServerUrl != null ? mockServerUrl + "/fixtures/smoke_embed_openai" : "http://localhost:8000/fixtures/smoke_embed_openai");
        System.out.println("smoke_embed_openai: " + (baseUrl == null ? "using real API (OPENAI_API_KEY is set)" : "using mock server (OPENAI_API_KEY not set)"));
        var client = LiterLlm.createClient(baseUrl == null ? apiKey : "test-key", baseUrl, null, null, null);
        var result = client.embed(request);
assertFalse(result.data().isEmpty(), "expected non-empty value");
    }


    @Test
    void testSmokeListModelsOpenai() throws Exception {
        // List models against real OpenAI API
        String apiKey = System.getenv("OPENAI_API_KEY");
        String mockServerUrl = System.getProperty("mockServerUrl"); if (mockServerUrl == null) { mockServerUrl = System.getenv("MOCK_SERVER_URL"); } String baseUrl = (apiKey != null && !apiKey.isEmpty()) ? null : (mockServerUrl != null ? mockServerUrl + "/fixtures/smoke_list_models_openai" : "http://localhost:8000/fixtures/smoke_list_models_openai");
        System.out.println("smoke_list_models_openai: " + (baseUrl == null ? "using real API (OPENAI_API_KEY is set)" : "using mock server (OPENAI_API_KEY not set)"));
        var client = LiterLlm.createClient(baseUrl == null ? apiKey : "test-key", baseUrl, null, null, null);
        var result = client.listModels();
assertTrue(result.data().size() >= 1, "expected at least 1 elements");
    }


    @Test
    void testSmokeProviderRouting() throws Exception {
        // Test provider routing by sending requests to OpenAI and Anthropic
        var request = JsonUtil.fromJson("{\"max_tokens\":5,\"messages\":[{\"content\":\"Say hi.\",\"role\":\"user\"}],\"model\":\"openai/gpt-4o-mini\"}", ChatCompletionRequest.class);

        String apiKey = System.getenv("OPENAI_API_KEY");
        String mockServerUrl = System.getProperty("mockServerUrl"); if (mockServerUrl == null) { mockServerUrl = System.getenv("MOCK_SERVER_URL"); } String baseUrl = (apiKey != null && !apiKey.isEmpty()) ? null : (mockServerUrl != null ? mockServerUrl + "/fixtures/smoke_provider_routing" : "http://localhost:8000/fixtures/smoke_provider_routing");
        System.out.println("smoke_provider_routing: " + (baseUrl == null ? "using real API (OPENAI_API_KEY is set)" : "using mock server (OPENAI_API_KEY not set)"));
        var client = LiterLlm.createClient(baseUrl == null ? apiKey : "test-key", baseUrl, null, null, null);
        var result = client.chat(request);
assertFalse(result.choices().isEmpty(), "expected non-empty value");
    }


    @Test
    void testSmokeStreamingOpenai() throws Exception {
        // Chat streaming against real OpenAI API, verifies chunks received
        var request = JsonUtil.fromJson("{\"max_tokens\":50,\"messages\":[{\"content\":\"Count from 1 to 5.\",\"role\":\"user\"}],\"model\":\"openai/gpt-4o-mini\"}", ChatCompletionRequest.class);

        String apiKey = System.getenv("OPENAI_API_KEY");
        String mockServerUrl = System.getProperty("mockServerUrl"); if (mockServerUrl == null) { mockServerUrl = System.getenv("MOCK_SERVER_URL"); } String baseUrl = (apiKey != null && !apiKey.isEmpty()) ? null : (mockServerUrl != null ? mockServerUrl + "/fixtures/smoke_streaming_openai" : "http://localhost:8000/fixtures/smoke_streaming_openai");
        System.out.println("smoke_streaming_openai: " + (baseUrl == null ? "using real API (OPENAI_API_KEY is set)" : "using mock server (OPENAI_API_KEY not set)"));
        var client = LiterLlm.createClient(baseUrl == null ? apiKey : "test-key", baseUrl, null, null, null);
        var result = client.chatStream(request);
        var chunks = new java.util.ArrayList<ChatCompletionChunk>();
        var _it = result.iterator();
        while (_it.hasNext()) { chunks.add(_it.next()); }
        assertTrue(chunks.size() >= 1, "expected >= 1 chunks");

    }


    @Test
    void testVertexChat() throws Exception {
        // Basic chat completion via the Google Vertex AI provider using the vertex_ai/ prefix for routing — verifies the prefix is stripped before dispatching and the Gemini response is normalised to the standard OpenAI chat completion shape
        var request = JsonUtil.fromJson("{\"max_tokens\":16,\"messages\":[{\"content\":\"Say hello in one word.\",\"role\":\"user\"}],\"model\":\"vertex_ai/gemini-2.0-flash\",\"temperature\":0}", ChatCompletionRequest.class);

        String mockServerUrl = System.getProperty("mockServerUrl"); if (mockServerUrl == null) { mockServerUrl = System.getenv("MOCK_SERVER_URL"); } String mockUrl = (mockServerUrl != null ? mockServerUrl : "http://localhost:8000") + "/fixtures/vertex_chat";
        var client = LiterLlm.createClient("test-key", mockUrl, null, null, null);
        var result = client.chat(request);
assertEquals(1, result.choices().size(), "expected exactly 1 elements");assertEquals("Hello!", java.util.Optional.ofNullable(result.choices().get(0).message().content()).map(java.util.Objects::toString).orElse("").trim());assertEquals("stop", java.util.Optional.ofNullable(result.choices().get(0).finishReason()).map(v -> v.getValue()).orElse("").trim());assertEquals("gemini-2.0-flash", result.model().trim());assertEquals(13, result.usage().totalTokens());
    }


    @Test
    void testVertexEmbed() throws Exception {
        // Embedding request via Google Vertex AI using the vertex_ai/ provider prefix and the text-embedding-005 model — response follows the standard OpenAI embeddings shape
        var request = JsonUtil.fromJson("{\"input\":\"Hello\",\"model\":\"vertex_ai/text-embedding-005\"}", EmbeddingRequest.class);

        String mockServerUrl = System.getProperty("mockServerUrl"); if (mockServerUrl == null) { mockServerUrl = System.getenv("MOCK_SERVER_URL"); } String mockUrl = (mockServerUrl != null ? mockServerUrl : "http://localhost:8000") + "/fixtures/vertex_embed";
        var client = LiterLlm.createClient("test-key", mockUrl, null, null, null);
        var result = client.embed(request);
assertEquals(1, result.data().size(), "expected exactly 1 elements");assertEquals(160, result.data().get(0).embedding().size(), "expected exactly 160 elements");
    }

}
