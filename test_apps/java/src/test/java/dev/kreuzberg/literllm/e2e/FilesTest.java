// This file is auto-generated by alef — DO NOT EDIT.
// alef:hash:b08e3719281f4d798d3eae56cf3af4b6b080ee7c65490b4354f287f8f1fbc9da
// To regenerate: alef generate
// To verify freshness: alef verify --exit-code
// Issues & docs: https://github.com/kreuzberg-dev/alef

package dev.kreuzberg.literllm.e2e;

import org.junit.jupiter.api.Test;
import static org.junit.jupiter.api.Assertions.*;
import dev.kreuzberg.literllm.LiterLlm;
import dev.kreuzberg.literllm.CreateFileRequest;
import java.util.Optional;

/** E2e tests for category: files. */
class FilesTest {

    @Test
    void testEdgeFileEmptyList() throws Exception {
        // List files when no files have been uploaded
        String mockUrl = System.getProperty("mockServerUrl", System.getenv("MOCK_SERVER_URL")) + "/fixtures/edge_file_empty_list";
        var client = LiterLlm.createClient("test-key", mockUrl, null, null, null);
        var result = client.listFiles(null);
assertEquals(0, result.data().size(), "expected exactly 0 elements");
    }


    @Test
    void testEdgeFileLargeUpload() throws Exception {
        // Upload a large file successfully
        var request = CreateFileRequest.fromJson("{\"file\":\"eyJwcm9tcHQiOiAibGFyZ2UgdHJhaW5pbmcgZGF0YSJ9Cg==\",\"filename\":\"large_training_data.jsonl\",\"purpose\":\"fine-tune\"}");

        String mockUrl = System.getProperty("mockServerUrl", System.getenv("MOCK_SERVER_URL")) + "/fixtures/edge_file_large_upload";
        var client = LiterLlm.createClient("test-key", mockUrl, null, null, null);
        var result = client.createFile(request);
assertFalse(result.id().isEmpty(), "expected non-empty value");
    }


    @Test
    void testErrorFileAuth401() throws Exception {
        // 401 Unauthorized when listing files with invalid API key
        // Wrap setup_lines + call_expr inside the lambda so error fixtures
        // catch failures at *any* step — including `<Type>.fromJson(...)`
        // calls that throw on malformed JSON (e.g. error fixtures with an
        // invalid enum value like `"purpose":"invalid-purpose"`). Mirrors
        // the C# `Assert.ThrowsAnyAsync(() => client.X(Type.FromJson(...)))`
        // pattern.
        assertThrows(Exception.class, () -> {
            String mockUrl = System.getProperty("mockServerUrl", System.getenv("MOCK_SERVER_URL")) + "/fixtures/error_file_auth_401";
            var client = LiterLlm.createClient("test-key", mockUrl, null, null, null);
            client.listFiles(null);
        });
    }


    @Test
    void testErrorFileBadPurpose() throws Exception {
        // 400 Bad Request when uploading a file with invalid purpose
        // Wrap setup_lines + call_expr inside the lambda so error fixtures
        // catch failures at *any* step — including `<Type>.fromJson(...)`
        // calls that throw on malformed JSON (e.g. error fixtures with an
        // invalid enum value like `"purpose":"invalid-purpose"`). Mirrors
        // the C# `Assert.ThrowsAnyAsync(() => client.X(Type.FromJson(...)))`
        // pattern.
        assertThrows(Exception.class, () -> {
        var request = CreateFileRequest.fromJson("{\"file\":\"data.jsonl\",\"purpose\":\"invalid-purpose\"}");

            String mockUrl = System.getProperty("mockServerUrl", System.getenv("MOCK_SERVER_URL")) + "/fixtures/error_file_bad_purpose";
            var client = LiterLlm.createClient("test-key", mockUrl, null, null, null);
            client.createFile(request);
        });
    }


    @Test
    void testErrorFileNotFound() throws Exception {
        // 404 Not Found when retrieving a nonexistent file
        // Wrap setup_lines + call_expr inside the lambda so error fixtures
        // catch failures at *any* step — including `<Type>.fromJson(...)`
        // calls that throw on malformed JSON (e.g. error fixtures with an
        // invalid enum value like `"purpose":"invalid-purpose"`). Mirrors
        // the C# `Assert.ThrowsAnyAsync(() => client.X(Type.FromJson(...)))`
        // pattern.
        assertThrows(Exception.class, () -> {
            String mockUrl = System.getProperty("mockServerUrl", System.getenv("MOCK_SERVER_URL")) + "/fixtures/error_file_not_found";
            var client = LiterLlm.createClient("test-key", mockUrl, null, null, null);
            client.retrieveFile("file-nonexistent");
        });
    }


    @Test
    void testSmokeCreateFile() throws Exception {
        // Upload a file for use with the API
        var request = CreateFileRequest.fromJson("{\"file\":\"eyJwcm9tcHQiOiAiaGVsbG8ifQo=\",\"filename\":\"training_data.jsonl\",\"purpose\":\"fine-tune\"}");

        String mockUrl = System.getProperty("mockServerUrl", System.getenv("MOCK_SERVER_URL")) + "/fixtures/smoke_create_file";
        var client = LiterLlm.createClient("test-key", mockUrl, null, null, null);
        var result = client.createFile(request);
assertFalse(result.id().isEmpty(), "expected non-empty value");
    }


    @Test
    void testSmokeDeleteFile() throws Exception {
        // Delete an uploaded file
        String mockUrl = System.getProperty("mockServerUrl", System.getenv("MOCK_SERVER_URL")) + "/fixtures/smoke_delete_file";
        var client = LiterLlm.createClient("test-key", mockUrl, null, null, null);
        var result = client.deleteFile("file-abc123");
assertFalse(result.id().isEmpty(), "expected non-empty value");assertEquals(true, result.deleted());
    }


    @Test
    void testSmokeFileContent() throws Exception {
        // Retrieve the content of an uploaded file
        String mockUrl = System.getProperty("mockServerUrl", System.getenv("MOCK_SERVER_URL")) + "/fixtures/smoke_file_content";
        var client = LiterLlm.createClient("test-key", mockUrl, null, null, null);
        var result = client.fileContent("file-abc123");
        assertNotNull(result, "expected non-null byte[] response");
        assertTrue(result.length > 0, "expected non-empty value");

    }


    @Test
    void testSmokeListFiles() throws Exception {
        // List all uploaded files
        String mockUrl = System.getProperty("mockServerUrl", System.getenv("MOCK_SERVER_URL")) + "/fixtures/smoke_list_files";
        var client = LiterLlm.createClient("test-key", mockUrl, null, null, null);
        var result = client.listFiles(null);
assertEquals(2, result.data().size(), "expected exactly 2 elements");
    }


    @Test
    void testSmokeRetrieveFile() throws Exception {
        // Retrieve metadata for an uploaded file
        String mockUrl = System.getProperty("mockServerUrl", System.getenv("MOCK_SERVER_URL")) + "/fixtures/smoke_retrieve_file";
        var client = LiterLlm.createClient("test-key", mockUrl, null, null, null);
        var result = client.retrieveFile("file-abc123");
assertFalse(result.id().isEmpty(), "expected non-empty value");
    }

}
