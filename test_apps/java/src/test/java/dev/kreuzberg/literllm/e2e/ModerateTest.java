// This file is auto-generated by alef — DO NOT EDIT.
// alef:hash:33a4cc487901cd0e6511bf0940b2e59d8f26bd59a8719738f26d73b906bddd5c
// To regenerate: alef generate
// To verify freshness: alef verify --exit-code

package dev.kreuzberg.literllm.e2e;

import org.junit.jupiter.api.Test;
import static org.junit.jupiter.api.Assertions.*;
import dev.kreuzberg.literllm.LiterLlm;
import dev.kreuzberg.literllm.ModerationRequest;
import java.util.Optional;
import dev.kreuzberg.literllm.JsonUtil;

/** E2e tests for category: moderate. */
public class ModerateTest {

    @Test
    void testEdgeModerateAllCategories() throws Exception {
        // Moderation response with multiple categories flagged
        var request = JsonUtil.fromJson("{\"input\":\"Extremely harmful content targeting multiple categories\",\"model\":\"omni-moderation-latest\"}", ModerationRequest.class);

        String mockServerUrl = System.getProperty("mockServerUrl"); if (mockServerUrl == null) { mockServerUrl = System.getenv("MOCK_SERVER_URL"); } String mockUrl = (mockServerUrl != null ? mockServerUrl : "http://localhost:8000") + "/fixtures/edge_moderate_all_categories";
        var client = LiterLlm.createClient("test-key", mockUrl, null, null, null);
        var result = client.moderate(request);
assertEquals(1, result.results().size(), "expected exactly 1 elements");assertEquals(true, result.results().get(0).flagged());

    }


    @Test
    void testEdgeModerateEmptyInput() throws Exception {
        // Moderation with empty string input
        var request = JsonUtil.fromJson("{\"input\":\"\",\"model\":\"omni-moderation-latest\"}", ModerationRequest.class);

        String mockServerUrl = System.getProperty("mockServerUrl"); if (mockServerUrl == null) { mockServerUrl = System.getenv("MOCK_SERVER_URL"); } String mockUrl = (mockServerUrl != null ? mockServerUrl : "http://localhost:8000") + "/fixtures/edge_moderate_empty_input";
        var client = LiterLlm.createClient("test-key", mockUrl, null, null, null);
        var result = client.moderate(request);
assertEquals(1, result.results().size(), "expected exactly 1 elements");assertEquals(false, result.results().get(0).flagged());

    }


    @Test
    void testErrorModerateAuth401() throws Exception {
        // 401 Unauthorized for moderation with invalid API key
        // Wrap setup_lines + call_expr inside the lambda so error fixtures
        // catch failures at *any* step — including `<Type>.fromJson(...)`
        // calls that throw on malformed JSON (e.g. error fixtures with an
        // invalid enum value like `"purpose":"invalid-purpose"`). Mirrors
        // the C# `Assert.ThrowsAnyAsync(() => client.X(Type.FromJson(...)))`
        // pattern.
        assertThrows(Exception.class, () -> {
        var request = JsonUtil.fromJson("{\"input\":\"Hello\",\"model\":\"omni-moderation-latest\"}", ModerationRequest.class);

            String mockServerUrl = System.getProperty("mockServerUrl"); if (mockServerUrl == null) { mockServerUrl = System.getenv("MOCK_SERVER_URL"); } String mockUrl = (mockServerUrl != null ? mockServerUrl : "http://localhost:8000") + "/fixtures/error_moderate_auth_401";
            var client = LiterLlm.createClient("test-key", mockUrl, null, null, null);
            client.moderate(request);
        });

    }


    @Test
    void testErrorModerateBadRequest() throws Exception {
        // 400 Bad Request for moderation with invalid model
        // Wrap setup_lines + call_expr inside the lambda so error fixtures
        // catch failures at *any* step — including `<Type>.fromJson(...)`
        // calls that throw on malformed JSON (e.g. error fixtures with an
        // invalid enum value like `"purpose":"invalid-purpose"`). Mirrors
        // the C# `Assert.ThrowsAnyAsync(() => client.X(Type.FromJson(...)))`
        // pattern.
        assertThrows(Exception.class, () -> {
        var request = JsonUtil.fromJson("{\"input\":\"Hello\",\"model\":\"nonexistent-moderation\"}", ModerationRequest.class);

            String mockServerUrl = System.getProperty("mockServerUrl"); if (mockServerUrl == null) { mockServerUrl = System.getenv("MOCK_SERVER_URL"); } String mockUrl = (mockServerUrl != null ? mockServerUrl : "http://localhost:8000") + "/fixtures/error_moderate_bad_request";
            var client = LiterLlm.createClient("test-key", mockUrl, null, null, null);
            client.moderate(request);
        });

    }


    @Test
    void testSmokeModerateBatch() throws Exception {
        // Moderate multiple inputs in a single request
        var request = JsonUtil.fromJson("{\"input\":[\"Hello world\",\"Nice weather today\"],\"model\":\"omni-moderation-latest\"}", ModerationRequest.class);

        String mockServerUrl = System.getProperty("mockServerUrl"); if (mockServerUrl == null) { mockServerUrl = System.getenv("MOCK_SERVER_URL"); } String mockUrl = (mockServerUrl != null ? mockServerUrl : "http://localhost:8000") + "/fixtures/smoke_moderate_batch";
        var client = LiterLlm.createClient("test-key", mockUrl, null, null, null);
        var result = client.moderate(request);
assertEquals(2, result.results().size(), "expected exactly 2 elements");assertEquals(false, result.results().get(0).flagged());

    }


    @Test
    void testSmokeModerateFlagged() throws Exception {
        // Moderation detects flagged content
        var request = JsonUtil.fromJson("{\"input\":\"I want to hurt someone very badly\",\"model\":\"omni-moderation-latest\"}", ModerationRequest.class);

        String mockServerUrl = System.getProperty("mockServerUrl"); if (mockServerUrl == null) { mockServerUrl = System.getenv("MOCK_SERVER_URL"); } String mockUrl = (mockServerUrl != null ? mockServerUrl : "http://localhost:8000") + "/fixtures/smoke_moderate_flagged";
        var client = LiterLlm.createClient("test-key", mockUrl, null, null, null);
        var result = client.moderate(request);
assertEquals(1, result.results().size(), "expected exactly 1 elements");assertEquals(true, result.results().get(0).flagged());

    }


    @Test
    void testSmokeModerateSingle() throws Exception {
        // Moderate a single non-flagged input
        var request = JsonUtil.fromJson("{\"input\":\"The weather is nice today.\",\"model\":\"omni-moderation-latest\"}", ModerationRequest.class);

        String mockServerUrl = System.getProperty("mockServerUrl"); if (mockServerUrl == null) { mockServerUrl = System.getenv("MOCK_SERVER_URL"); } String mockUrl = (mockServerUrl != null ? mockServerUrl : "http://localhost:8000") + "/fixtures/smoke_moderate_single";
        var client = LiterLlm.createClient("test-key", mockUrl, null, null, null);
        var result = client.moderate(request);
assertEquals(1, result.results().size(), "expected exactly 1 elements");assertEquals(false, result.results().get(0).flagged());

    }

}
