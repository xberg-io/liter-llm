// This file is auto-generated by alef — DO NOT EDIT.
// alef:hash:8cdff7ccce443d1fc90432984007ad265b45f4b1399e6a8713ec17d0541866e9
// To regenerate: alef generate
// To verify freshness: alef verify --exit-code
// Issues & docs: https://github.com/sample_crate-dev/alef

import { describe, expect, it } from 'vitest';import { createClient, WasmChatCompletionRequest, WasmCreateBatchRequest, WasmChatCompletionTool, WasmFunctionDefinition, WasmStreamOptions, WasmBatchStatus } from '@kreuzberg/liter-llm-wasm';
function _alefE2eText(value: unknown): string {
  return value == null ? "" : String(value);
}

function _alefE2eItemTexts(item: unknown): string[] {
  if (item == null || typeof item !== "object") {
    return [_alefE2eText(item)];
  }
  const record = item as Record<string, unknown>;
  const itemsText = Array.isArray(record.items) ? record.items.map(_alefE2eText).join(" ") : "";
  return [_alefE2eText(item), _alefE2eText(record.kind), _alefE2eText(record.name), _alefE2eText(record.source), _alefE2eText(record.alias), _alefE2eText(record.text), _alefE2eText(record.signature), itemsText];
}

function _alefE2eFormatMetadataDisplay(fm: unknown): string {
  if (fm == null) return "";
  if (typeof fm !== "object") return String(fm);
  const record = fm as Record<string, unknown>;
  const formatType = record.format_type;

  // FormatMetadata is a tagged union: { format_type: 'image', image: { format: 'PNG', ... }, ... }
  // Extract the display string based on the variant type
  if (formatType === "image" && typeof record.image === "object") {
    const imageData = record.image as Record<string, unknown>;
    if (typeof imageData.format === "string") return imageData.format;
  }

  // Fallback: return format_type variant name
  if (typeof record.format_type === "string") return record.format_type;
  return "";
}


describe('batches', () => {  it('edge_batch_already_cancelled: Attempt to cancel an already-cancelled batch', async () => {    const client = createClient('test-key', `${process.env.MOCK_SERVER_URL}/fixtures/edge_batch_already_cancelled`);    await expect(async () => {      await client.cancelBatch("batch-cancelled001");    }).rejects.toThrow();
  }, 30000);
  it('edge_batch_empty_list: List batches when no batches exist', async () => {    const client = createClient('test-key', `${process.env.MOCK_SERVER_URL}/fixtures/edge_batch_empty_list`);    const result = await client.listBatches(undefined);    expect(result.data.length).toBe(0);

  }, 30000);
  it('error_batch_auth_401: 401 Unauthorized when creating a batch with invalid API key', async () => {    const client = createClient('test-key', `${process.env.MOCK_SERVER_URL}/fixtures/error_batch_auth_401`);    await expect(async () => {      await client.createBatch((() => { const _u0 = WasmCreateBatchRequest.default(); _u0.completionWindow = "24h"; _u0.endpoint = "/v1/chat/completions"; _u0.inputFileId = "file-abc123"; return _u0; })());    }).rejects.toThrow();
  }, 30000);
  it('error_batch_invalid_file: 400 Bad Request when creating a batch with invalid input file', async () => {    const client = createClient('test-key', `${process.env.MOCK_SERVER_URL}/fixtures/error_batch_invalid_file`);    await expect(async () => {      await client.createBatch((() => { const _u0 = WasmCreateBatchRequest.default(); _u0.completionWindow = "24h"; _u0.endpoint = "/v1/chat/completions"; _u0.inputFileId = "file-wrong-purpose"; return _u0; })());    }).rejects.toThrow();
  }, 30000);
  it('error_batch_not_found: 404 Not Found when retrieving a nonexistent batch', async () => {    const client = createClient('test-key', `${process.env.MOCK_SERVER_URL}/fixtures/error_batch_not_found`);    await expect(async () => {      await client.retrieveBatch("batch-nonexistent");    }).rejects.toThrow();
  }, 30000);
  it('smoke_batch_completed: Retrieve a completed batch job with output file', async () => {    const client = createClient('test-key', `${process.env.MOCK_SERVER_URL}/fixtures/smoke_batch_completed`);    const result = await client.retrieveBatch("batch-ghi789");    {
        const _v = result.id;
        if (typeof _v === "string" || Array.isArray(_v)) {
            expect(_v.length).toBeGreaterThan(0);
        } else {
            expect(_v).toBeDefined();
            expect(_v).not.toBeNull();
        }
    }
    expect(result.status).toBe("completed");

  }, 30000);
  it('smoke_cancel_batch: Cancel a running batch job', async () => {    const client = createClient('test-key', `${process.env.MOCK_SERVER_URL}/fixtures/smoke_cancel_batch`);    const result = await client.cancelBatch("batch-def456");    {
        const _v = result.id;
        if (typeof _v === "string" || Array.isArray(_v)) {
            expect(_v.length).toBeGreaterThan(0);
        } else {
            expect(_v).toBeDefined();
            expect(_v).not.toBeNull();
        }
    }
    expect(result.status).toBe("cancelling");

  }, 30000);
  it('smoke_create_batch: Create a new batch processing job', async () => {    const client = createClient('test-key', `${process.env.MOCK_SERVER_URL}/fixtures/smoke_create_batch`);    const result = await client.createBatch((() => { const _u0 = WasmCreateBatchRequest.default(); _u0.completionWindow = "24h"; _u0.endpoint = "/v1/chat/completions"; _u0.inputFileId = "file-abc123"; return _u0; })());    {
        const _v = result.id;
        if (typeof _v === "string" || Array.isArray(_v)) {
            expect(_v.length).toBeGreaterThan(0);
        } else {
            expect(_v).toBeDefined();
            expect(_v).not.toBeNull();
        }
    }
    expect(result.status).toBe("validating");

  }, 30000);
  it('smoke_list_batches: List all batch jobs', async () => {    const client = createClient('test-key', `${process.env.MOCK_SERVER_URL}/fixtures/smoke_list_batches`);    const result = await client.listBatches(undefined);    expect(result.data.length).toBe(2);

  }, 30000);
  it('smoke_retrieve_batch: Retrieve the status of a batch job', async () => {    const client = createClient('test-key', `${process.env.MOCK_SERVER_URL}/fixtures/smoke_retrieve_batch`);    const result = await client.retrieveBatch("batch-abc123");    {
        const _v = result.id;
        if (typeof _v === "string" || Array.isArray(_v)) {
            expect(_v.length).toBeGreaterThan(0);
        } else {
            expect(_v).toBeDefined();
            expect(_v).not.toBeNull();
        }
    }
    expect(result.status).toBe("in_progress");

  }, 30000);
});
