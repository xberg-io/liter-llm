// This file is auto-generated by alef — DO NOT EDIT.
// alef:hash:921b2ad43a718ffb9c525b98cb457516c4be520d3c54c28631223c5955a29e56
// To regenerate: alef generate
// To verify freshness: alef verify --exit-code
// Issues & docs: https://github.com/sample_crate-dev/alef

import { describe, expect, it } from 'vitest';import { createClient, WasmChatCompletionRequest, WasmModerationRequest, WasmChatCompletionTool, WasmFunctionDefinition, WasmStreamOptions } from '@kreuzberg/liter-llm-wasm';
function _alefE2eText(value: unknown): string {
  return value == null ? "" : String(value);
}

function _alefE2eItemTexts(item: unknown): string[] {
  if (item == null || typeof item !== "object") {
    return [_alefE2eText(item)];
  }
  const record = item as Record<string, unknown>;
  const itemsText = Array.isArray(record.items) ? record.items.map(_alefE2eText).join(" ") : "";
  return [_alefE2eText(item), _alefE2eText(record.kind), _alefE2eText(record.name), _alefE2eText(record.source), _alefE2eText(record.alias), _alefE2eText(record.text), _alefE2eText(record.signature), itemsText];
}

function _alefE2eFormatMetadataDisplay(fm: unknown): string {
  if (fm == null) return "";
  if (typeof fm !== "object") return String(fm);
  const record = fm as Record<string, unknown>;
  const formatType = record.format_type;

  // FormatMetadata is a tagged union: { format_type: 'image', image: { format: 'PNG', ... }, ... }
  // Extract the display string based on the variant type
  if (formatType === "image" && typeof record.image === "object") {
    const imageData = record.image as Record<string, unknown>;
    if (typeof imageData.format === "string") return imageData.format;
  }

  // Fallback: return format_type variant name
  if (typeof record.format_type === "string") return record.format_type;
  return "";
}


describe('moderate', () => {  it('edge_moderate_all_categories: Moderation response with multiple categories flagged', async () => {    const client = createClient('test-key', `${process.env.MOCK_SERVER_URL}/fixtures/edge_moderate_all_categories`);    const result = await client.moderate((() => { const _u = WasmModerationRequest.default(); _u.input = "Extremely harmful content targeting multiple categories"; _u.model = "omni-moderation-latest"; return _u; })());    expect(result.results.length).toBe(1);
    expect(result.results[0].flagged).toBe(true);

  }, 30000);
  it('edge_moderate_empty_input: Moderation with empty string input', async () => {    const client = createClient('test-key', `${process.env.MOCK_SERVER_URL}/fixtures/edge_moderate_empty_input`);    const result = await client.moderate((() => { const _u = WasmModerationRequest.default(); _u.input = ""; _u.model = "omni-moderation-latest"; return _u; })());    expect(result.results.length).toBe(1);
    expect(result.results[0].flagged).toBe(false);

  }, 30000);
  it('error_moderate_auth_401: 401 Unauthorized for moderation with invalid API key', async () => {    const client = createClient('test-key', `${process.env.MOCK_SERVER_URL}/fixtures/error_moderate_auth_401`);    await expect(async () => {      await client.moderate((() => { const _u = WasmModerationRequest.default(); _u.input = "Hello"; _u.model = "omni-moderation-latest"; return _u; })());    }).rejects.toThrow();
  }, 30000);
  it('error_moderate_bad_request: 400 Bad Request for moderation with invalid model', async () => {    const client = createClient('test-key', `${process.env.MOCK_SERVER_URL}/fixtures/error_moderate_bad_request`);    await expect(async () => {      await client.moderate((() => { const _u = WasmModerationRequest.default(); _u.input = "Hello"; _u.model = "nonexistent-moderation"; return _u; })());    }).rejects.toThrow();
  }, 30000);
  it('smoke_moderate_batch: Moderate multiple inputs in a single request', async () => {    const client = createClient('test-key', `${process.env.MOCK_SERVER_URL}/fixtures/smoke_moderate_batch`);    const result = await client.moderate((() => { const _u = WasmModerationRequest.default(); _u.input = ["Hello world", "Nice weather today"]; _u.model = "omni-moderation-latest"; return _u; })());    expect(result.results.length).toBe(2);
    expect(result.results[0].flagged).toBe(false);

  }, 30000);
  it('smoke_moderate_flagged: Moderation detects flagged content', async () => {    const client = createClient('test-key', `${process.env.MOCK_SERVER_URL}/fixtures/smoke_moderate_flagged`);    const result = await client.moderate((() => { const _u = WasmModerationRequest.default(); _u.input = "I want to hurt someone very badly"; _u.model = "omni-moderation-latest"; return _u; })());    expect(result.results.length).toBe(1);
    expect(result.results[0].flagged).toBe(true);

  }, 30000);
  it('smoke_moderate_single: Moderate a single non-flagged input', async () => {    const client = createClient('test-key', `${process.env.MOCK_SERVER_URL}/fixtures/smoke_moderate_single`);    const result = await client.moderate((() => { const _u = WasmModerationRequest.default(); _u.input = "The weather is nice today."; _u.model = "omni-moderation-latest"; return _u; })());    expect(result.results.length).toBe(1);
    expect(result.results[0].flagged).toBe(false);

  }, 30000);
});
