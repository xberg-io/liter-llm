// This file is auto-generated by alef — DO NOT EDIT.
// alef:hash:8c6394307ee786542760e258bcc70a27da623ca905384e09eb87f8ddb8d7e534
// To regenerate: alef generate
// To verify freshness: alef verify --exit-code

import { describe, expect, it } from "vitest";
import { createClient, WasmChatCompletionRequest, WasmChatCompletionTool, WasmFunctionDefinition, WasmStreamOptions, WasmFinishReason } from "@xberg-io/liter-llm-wasm";

async function _alefE2eDecompressAndParseJson(response: Response): Promise<unknown> {
	const contentEncoding = response.headers.get("content-encoding");
	let buffer = await response.arrayBuffer();

	if (contentEncoding === "gzip") {
		const { gunzipSync } = await import("zlib");
		buffer = gunzipSync(new Uint8Array(buffer)).buffer;
	} else if (contentEncoding === "br") {
		const { brotliDecompressSync } = await import("zlib");
		buffer = brotliDecompressSync(new Uint8Array(buffer)).buffer;
	}

	const text = new TextDecoder().decode(buffer);
	return JSON.parse(text);
}

function _alefE2eText(value: unknown): string {
	return value == null ? "" : String(value);
}

function _alefE2eItemTexts(item: unknown): string[] {
	if (item == null || typeof item !== "object") {
		return [_alefE2eText(item)];
	}
	const record = item as Record<string, unknown>;
	const itemsText = Array.isArray(record.items) ? record.items.map(_alefE2eText).join(" ") : "";
	return [_alefE2eText(item), _alefE2eText(record.kind), _alefE2eText(record.name), _alefE2eText(record.source), _alefE2eText(record.alias), _alefE2eText(record.text), _alefE2eText(record.signature), itemsText];
}

function _alefE2eFormatMetadataDisplay(fm: unknown): string {
	if (fm == null) return "";
	if (typeof fm !== "object") return String(fm);
	const record = fm as Record<string, unknown>;
	const formatType = record.format_type;

	// FormatMetadata is a tagged union: { format_type: 'image', image: { format: 'PNG', ... }, ... }
	// Extract the display string based on the variant type
	if (formatType === "image" && typeof record.image === "object") {
		const imageData = record.image as Record<string, unknown>;
		if (typeof imageData.format === "string") return imageData.format;
	}

	// Fallback: return format_type variant name
	if (typeof record.format_type === "string") return record.format_type;
	return "";
}


describe("error-handling", () => {

	it("anthropic_error_auth: 401 Authentication error returned by the Anthropic API when the API key is invalid", async () => {
		const client = createClient('test-key', `${process.env.MOCK_SERVER_URL}/fixtures/anthropic_error_auth`);
		await expect(async () => {
			await client.chat((() => { const _u0 = WasmChatCompletionRequest.default(); _u0.messages = [{ content: "Hello", role: "user" }]; _u0.model = "anthropic/claude-3-5-sonnet-20241022"; return _u0; })());
		}).rejects.toThrow();
	}, 30000);
	it("auth_401: 401 Unauthorized error when API key is invalid or missing", async () => {
		const client = createClient('test-key', `${process.env.MOCK_SERVER_URL}/fixtures/auth_401`);
		await expect(async () => {
			await client.chat((() => { const _u0 = WasmChatCompletionRequest.default(); _u0.messages = [{ content: "Hello", role: "user" }]; _u0.model = "gpt-4"; return _u0; })());
		}).rejects.toThrow();
	}, 30000);
	it("azure_error_auth: Azure OpenAI returns a 401 Unauthorized error when the API key is missing or invalid — uses Azure's error envelope shape with code AccessDenied", async () => {
		const client = createClient('test-key', `${process.env.MOCK_SERVER_URL}/fixtures/azure_error_auth`);
		await expect(async () => {
			await client.chat((() => { const _u0 = WasmChatCompletionRequest.default(); _u0.messages = [{ content: "Hello", role: "user" }]; _u0.model = "azure/gpt-4"; return _u0; })());
		}).rejects.toThrow();
	}, 30000);
	it("bad_request_400: 400 Bad Request error when a parameter value is invalid", async () => {
		const client = createClient('test-key', `${process.env.MOCK_SERVER_URL}/fixtures/bad_request_400`);
		await expect(async () => {
			await client.chat((() => { const _u0 = WasmChatCompletionRequest.default(); _u0.messages = [{ content: "Hello", role: "user" }]; _u0.model = "gpt-4"; _u0.temperature = 5.0; return _u0; })());
		}).rejects.toThrow();
	}, 30000);
	it("bedrock_error_auth: AWS Bedrock returns 403 Forbidden (not 401) when credentials are missing, expired, or the IAM role lacks bedrock:InvokeModel permission — verifies the error is mapped to Authentication", async () => {
		const client = createClient('test-key', `${process.env.MOCK_SERVER_URL}/fixtures/bedrock_error_auth`);
		await expect(async () => {
			await client.chat((() => { const _u0 = WasmChatCompletionRequest.default(); _u0.messages = [{ content: "Hello", role: "user" }]; _u0.model = "bedrock/anthropic.claude-3-sonnet-20240229-v1:0"; return _u0; })());
		}).rejects.toThrow();
	}, 30000);
	it("content_policy_violation: 400 error when a request is rejected due to content policy", async () => {
		const client = createClient('test-key', `${process.env.MOCK_SERVER_URL}/fixtures/content_policy_violation`);
		await expect(async () => {
			await client.chat((() => { const _u0 = WasmChatCompletionRequest.default(); _u0.messages = [{ content: "Generate harmful content", role: "user" }]; _u0.model = "gpt-4"; return _u0; })());
		}).rejects.toThrow();
	}, 30000);
	it("context_window_exceeded: 400 error when the prompt exceeds the model's maximum context length", async () => {
		const client = createClient('test-key', `${process.env.MOCK_SERVER_URL}/fixtures/context_window_exceeded`);
		await expect(async () => {
			await client.chat((() => { const _u0 = WasmChatCompletionRequest.default(); _u0.messages = [{ content: "Very long prompt that exceeds the context window...", role: "user" }]; _u0.model = "gpt-4"; return _u0; })());
		}).rejects.toThrow();
	}, 30000);
	it("empty_response_body: 200 OK response with an empty JSON object body, missing required fields", async () => {
		const client = createClient('test-key', `${process.env.MOCK_SERVER_URL}/fixtures/empty_response_body`);
		await expect(async () => {
			await client.chat((() => { const _u0 = WasmChatCompletionRequest.default(); _u0.messages = [{ content: "Hello", role: "user" }]; _u0.model = "gpt-4"; return _u0; })());
		}).rejects.toThrow();
	}, 30000);
	it("forbidden_403: 403 Forbidden error when the API key does not have access to the requested resource", async () => {
		const client = createClient('test-key', `${process.env.MOCK_SERVER_URL}/fixtures/forbidden_403`);
		await expect(async () => {
			await client.chat((() => { const _u0 = WasmChatCompletionRequest.default(); _u0.messages = [{ content: "Hello", role: "user" }]; _u0.model = "gpt-4"; return _u0; })());
		}).rejects.toThrow();
	}, 30000);
	it("gateway_timeout_504: 504 Gateway Timeout error when the upstream service times out", async () => {
		const client = createClient('test-key', `${process.env.MOCK_SERVER_URL}/fixtures/gateway_timeout_504`);
		await expect(async () => {
			await client.chat((() => { const _u0 = WasmChatCompletionRequest.default(); _u0.messages = [{ content: "Hello", role: "user" }]; _u0.model = "gpt-4"; return _u0; })());
		}).rejects.toThrow();
	}, 30000);
	it("github_copilot_error_auth: 401 Authentication error returned by the GitHub Copilot API when the token is invalid or expired", async () => {
		const client = createClient('test-key', `${process.env.MOCK_SERVER_URL}/fixtures/github_copilot_error_auth`);
		await expect(async () => {
			await client.chat((() => { const _u0 = WasmChatCompletionRequest.default(); _u0.messages = [{ content: "Hello", role: "user" }]; _u0.model = "github_copilot/gpt-4o"; return _u0; })());
		}).rejects.toThrow();
	}, 30000);
	it("not_found_404: 404 Not Found error when requesting a model that does not exist", async () => {
		const client = createClient('test-key', `${process.env.MOCK_SERVER_URL}/fixtures/not_found_404`);
		await expect(async () => {
			await client.chat((() => { const _u0 = WasmChatCompletionRequest.default(); _u0.messages = [{ content: "Hello", role: "user" }]; _u0.model = "gpt-99"; return _u0; })());
		}).rejects.toThrow();
	}, 30000);
	it("rate_limit_429: 429 Too Many Requests error when the rate limit is exceeded", async () => {
		const client = createClient('test-key', `${process.env.MOCK_SERVER_URL}/fixtures/rate_limit_429`);
		await expect(async () => {
			await client.chat((() => { const _u0 = WasmChatCompletionRequest.default(); _u0.messages = [{ content: "Hello", role: "user" }]; _u0.model = "gpt-4"; return _u0; })());
		}).rejects.toThrow();
	}, 30000);
	it("server_error_500: 500 Internal Server Error from the upstream API", async () => {
		const client = createClient('test-key', `${process.env.MOCK_SERVER_URL}/fixtures/server_error_500`);
		await expect(async () => {
			await client.chat((() => { const _u0 = WasmChatCompletionRequest.default(); _u0.messages = [{ content: "Hello", role: "user" }]; _u0.model = "gpt-4"; return _u0; })());
		}).rejects.toThrow();
	}, 30000);
	it("service_unavailable_502: 502 Bad Gateway error when the upstream service is unavailable", async () => {
		const client = createClient('test-key', `${process.env.MOCK_SERVER_URL}/fixtures/service_unavailable_502`);
		await expect(async () => {
			await client.chat((() => { const _u0 = WasmChatCompletionRequest.default(); _u0.messages = [{ content: "Hello", role: "user" }]; _u0.model = "gpt-4"; return _u0; })());
		}).rejects.toThrow();
	}, 30000);
	it("timeout_error: 408 Request Timeout error when the API request takes too long to complete", async () => {
		const client = createClient('test-key', `${process.env.MOCK_SERVER_URL}/fixtures/timeout_error`);
		await expect(async () => {
			await client.chat((() => { const _u0 = WasmChatCompletionRequest.default(); _u0.messages = [{ content: "Hello", role: "user" }]; _u0.model = "gpt-4"; return _u0; })());
		}).rejects.toThrow();
	}, 30000);
	it("vertex_error_auth: Google Vertex AI returns 401 Unauthorized when the OAuth2 token is missing, expired, or the service account lacks aiplatform.endpoints.predict permission — verifies the error is mapped to Authentication", async () => {
		const client = createClient('test-key', `${process.env.MOCK_SERVER_URL}/fixtures/vertex_error_auth`);
		await expect(async () => {
			await client.chat((() => { const _u0 = WasmChatCompletionRequest.default(); _u0.messages = [{ content: "Hello", role: "user" }]; _u0.model = "vertex_ai/gemini-2.0-flash"; return _u0; })());
		}).rejects.toThrow();
	}, 30000);
});
