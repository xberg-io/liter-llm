// This file is auto-generated by alef — DO NOT EDIT.
// alef:hash:8f936f48081d739f9fd9131c56efc5f0980e9efa27345424d2add16ff2c63a98
// To regenerate: alef generate
// To verify freshness: alef verify --exit-code
// Issues & docs: https://github.com/kreuzberg-dev/alef

import { describe, expect, it } from "vitest";
import {
  createClient,
  WasmChatCompletionRequest,
  WasmOcrRequest,
  WasmStreamOptions,
  WasmChatCompletionTool,
  WasmFunctionDefinition,
} from "@kreuzberg/liter-llm-wasm";
function _alefE2eText(value: unknown): string {
  return value == null ? "" : String(value);
}

function _alefE2eItemTexts(item: unknown): string[] {
  if (item == null || typeof item !== "object") {
    return [_alefE2eText(item)];
  }
  const record = item as Record<string, unknown>;
  const itemsText = Array.isArray(record.items) ? record.items.map(_alefE2eText).join(" ") : "";
  return [
    _alefE2eText(item),
    _alefE2eText(record.kind),
    _alefE2eText(record.name),
    _alefE2eText(record.source),
    _alefE2eText(record.alias),
    _alefE2eText(record.text),
    _alefE2eText(record.signature),
    itemsText,
  ];
}

describe("ocr", () => {
  it("ocr_error_400: 400 Bad Request error when OCR input has an invalid image format", async () => {
    const client = createClient(
      "test-key",
      `${process.env.MOCK_SERVER_URL}/fixtures/ocr_error_400`,
    );
    await expect(async () => {
      await client.ocr(
        (() => {
          const _u = WasmOcrRequest.default();
          _u.document = { type: "document_url", url: "invalid://url" };
          _u.model = "mistral/mistral-ocr-latest";
          return _u;
        })(),
      );
    }).rejects.toThrow();
  });
  it("ocr_error_401: 401 Unauthorized error on OCR request due to invalid API credentials", async () => {
    const client = createClient(
      "test-key",
      `${process.env.MOCK_SERVER_URL}/fixtures/ocr_error_401`,
    );
    await expect(async () => {
      await client.ocr(
        (() => {
          const _u = WasmOcrRequest.default();
          _u.document = { type: "document_url", url: "https://example.com/doc.pdf" };
          _u.model = "mistral/mistral-ocr-latest";
          return _u;
        })(),
      );
    }).rejects.toThrow();
  });
  it("ocr_multi_page: OCR request returning multiple pages of document content", async () => {
    const client = createClient(
      "test-key",
      `${process.env.MOCK_SERVER_URL}/fixtures/ocr_multi_page`,
    );
    const result = await client.ocr(
      (() => {
        const _u = WasmOcrRequest.default();
        _u.document = { type: "document_url", url: "https://example.com/multipage.pdf" };
        _u.model = "mistral/mistral-ocr-latest";
        return _u;
      })(),
    );
    expect(result.pages.length).toBe(2);
    expect(Number(result.pages[0].index)).toBe(0);
    expect(Number(result.pages[1].index)).toBe(1);
  });
  it("ocr_url_document: OCR request with a document URL input", async () => {
    const client = createClient(
      "test-key",
      `${process.env.MOCK_SERVER_URL}/fixtures/ocr_url_document`,
    );
    await client.ocr(
      (() => {
        const _u = WasmOcrRequest.default();
        _u.document = { type: "document_url", url: "https://example.com/doc.pdf" };
        _u.model = "mistral/mistral-ocr-latest";
        return _u;
      })(),
    );
  });
});
