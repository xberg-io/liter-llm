// This file is auto-generated by alef — DO NOT EDIT.
// To regenerate: alef generate
// To verify freshness: alef verify --exit-code
// Issues & docs: https://github.com/kreuzberg-dev/alef

import { describe, expect, it } from "vitest";
import {
  createClient,
  WasmChatCompletionRequest,
  WasmCreateImageRequest,
  WasmChatCompletionTool,
  WasmFunctionDefinition,
  WasmStreamOptions,
} from "@kreuzberg/liter-llm-wasm";
function _alefE2eText(value: unknown): string {
  return value == null ? "" : String(value);
}

function _alefE2eItemTexts(item: unknown): string[] {
  if (item == null || typeof item !== "object") {
    return [_alefE2eText(item)];
  }
  const record = item as Record<string, unknown>;
  const itemsText = Array.isArray(record.items) ? record.items.map(_alefE2eText).join(" ") : "";
  return [
    _alefE2eText(item),
    _alefE2eText(record.kind),
    _alefE2eText(record.name),
    _alefE2eText(record.source),
    _alefE2eText(record.alias),
    _alefE2eText(record.text),
    _alefE2eText(record.signature),
    itemsText,
  ];
}

function _alefE2eFormatMetadataDisplay(fm: unknown): string {
  if (fm == null) return "";
  if (typeof fm !== "object") return String(fm);
  const record = fm as Record<string, unknown>;
  const formatType = record.format_type;

  // FormatMetadata is a tagged union: { format_type: 'image', image: { format: 'PNG', ... }, ... }
  // Extract the display string based on the variant type
  if (formatType === "image" && typeof record.image === "object") {
    const imageData = record.image as Record<string, unknown>;
    if (typeof imageData.format === "string") return imageData.format;
  }

  // Fallback: return format_type variant name
  if (typeof record.format_type === "string") return record.format_type;
  return "";
}

describe("image-generate", () => {
  it("edge_image_b64_response: Image generation returning base64-encoded data instead of URL", async () => {
    const client = createClient(
      "test-key",
      `${process.env.MOCK_SERVER_URL}/fixtures/edge_image_b64_response`,
    );
    const result = await client.imageGenerate(
      (() => {
        const _u = WasmCreateImageRequest.default();
        _u.model = "dall-e-3";
        _u.n = 1;
        _u.prompt = "A blue circle";
        _u.responseFormat = "b64_json";
        _u.size = "1024x1024";
        return _u;
      })(),
    );
    expect(result.data.length).toBe(1);
    {
      const _v = result.data[0].b64Json;
      if (typeof _v === "string" || Array.isArray(_v)) {
        expect(_v.length).toBeGreaterThan(0);
      } else {
        expect(_v).toBeDefined();
        expect(_v).not.toBeNull();
      }
    }
  }, 30000);
  it("edge_image_empty_prompt: Image generation with an empty prompt returns 400", async () => {
    const client = createClient(
      "test-key",
      `${process.env.MOCK_SERVER_URL}/fixtures/edge_image_empty_prompt`,
    );
    await expect(async () => {
      await client.imageGenerate(
        (() => {
          const _u = WasmCreateImageRequest.default();
          _u.model = "dall-e-3";
          _u.n = 1;
          _u.prompt = "";
          _u.size = "1024x1024";
          return _u;
        })(),
      );
    }).rejects.toThrow();
  }, 30000);
  it("edge_image_multiple_n: Image generation requesting multiple images with n=3", async () => {
    const client = createClient(
      "test-key",
      `${process.env.MOCK_SERVER_URL}/fixtures/edge_image_multiple_n`,
    );
    const result = await client.imageGenerate(
      (() => {
        const _u = WasmCreateImageRequest.default();
        _u.model = "dall-e-3";
        _u.n = 3;
        _u.prompt = "A cat";
        return _u;
      })(),
    );
    expect(result.data.length).toBe(3);
    {
      const _v = result.data[0].url;
      if (typeof _v === "string" || Array.isArray(_v)) {
        expect(_v.length).toBeGreaterThan(0);
      } else {
        expect(_v).toBeDefined();
        expect(_v).not.toBeNull();
      }
    }
    {
      const _v = result.data[1].url;
      if (typeof _v === "string" || Array.isArray(_v)) {
        expect(_v.length).toBeGreaterThan(0);
      } else {
        expect(_v).toBeDefined();
        expect(_v).not.toBeNull();
      }
    }
    {
      const _v = result.data[2].url;
      if (typeof _v === "string" || Array.isArray(_v)) {
        expect(_v.length).toBeGreaterThan(0);
      } else {
        expect(_v).toBeDefined();
        expect(_v).not.toBeNull();
      }
    }
  }, 30000);
  it("error_image_auth_401: 401 Unauthorized when generating images with invalid API key", async () => {
    const client = createClient(
      "test-key",
      `${process.env.MOCK_SERVER_URL}/fixtures/error_image_auth_401`,
    );
    await expect(async () => {
      await client.imageGenerate(
        (() => {
          const _u = WasmCreateImageRequest.default();
          _u.model = "dall-e-3";
          _u.n = 1;
          _u.prompt = "A cat";
          _u.size = "1024x1024";
          return _u;
        })(),
      );
    }).rejects.toThrow();
  }, 30000);
  it("error_image_bad_request: 400 Bad Request when image generation parameters are invalid", async () => {
    const client = createClient(
      "test-key",
      `${process.env.MOCK_SERVER_URL}/fixtures/error_image_bad_request`,
    );
    await expect(async () => {
      await client.imageGenerate(
        (() => {
          const _u = WasmCreateImageRequest.default();
          _u.model = "dall-e-3";
          _u.n = 1;
          _u.prompt = "A cat";
          _u.size = "9999x9999";
          return _u;
        })(),
      );
    }).rejects.toThrow();
  }, 30000);
  it("error_image_rate_limit: 429 Rate limit exceeded for image generation", async () => {
    const client = createClient(
      "test-key",
      `${process.env.MOCK_SERVER_URL}/fixtures/error_image_rate_limit`,
    );
    await expect(async () => {
      await client.imageGenerate(
        (() => {
          const _u = WasmCreateImageRequest.default();
          _u.model = "dall-e-3";
          _u.n = 1;
          _u.prompt = "A cat";
          _u.size = "1024x1024";
          return _u;
        })(),
      );
    }).rejects.toThrow();
  }, 30000);
  it("smoke_image_basic: Basic image generation with a text prompt", async () => {
    const client = createClient(
      "test-key",
      `${process.env.MOCK_SERVER_URL}/fixtures/smoke_image_basic`,
    );
    const result = await client.imageGenerate(
      (() => {
        const _u = WasmCreateImageRequest.default();
        _u.model = "dall-e-3";
        _u.n = 1;
        _u.prompt = "A white cat sitting on a windowsill";
        _u.size = "1024x1024";
        return _u;
      })(),
    );
    expect(result.data.length).toBe(1);
    {
      const _v = result.data[0].url;
      if (typeof _v === "string" || Array.isArray(_v)) {
        expect(_v.length).toBeGreaterThan(0);
      } else {
        expect(_v).toBeDefined();
        expect(_v).not.toBeNull();
      }
    }
  }, 30000);
  it("smoke_image_multiple: Image generation requesting multiple images", async () => {
    const client = createClient(
      "test-key",
      `${process.env.MOCK_SERVER_URL}/fixtures/smoke_image_multiple`,
    );
    const result = await client.imageGenerate(
      (() => {
        const _u = WasmCreateImageRequest.default();
        _u.model = "dall-e-2";
        _u.n = 3;
        _u.prompt = "A red bicycle";
        _u.size = "256x256";
        return _u;
      })(),
    );
    expect(result.data.length).toBe(3);
    {
      const _v = result.data[0].url;
      if (typeof _v === "string" || Array.isArray(_v)) {
        expect(_v.length).toBeGreaterThan(0);
      } else {
        expect(_v).toBeDefined();
        expect(_v).not.toBeNull();
      }
    }
  }, 30000);
  it("smoke_image_with_size: Image generation with explicit size parameter", async () => {
    const client = createClient(
      "test-key",
      `${process.env.MOCK_SERVER_URL}/fixtures/smoke_image_with_size`,
    );
    const result = await client.imageGenerate(
      (() => {
        const _u = WasmCreateImageRequest.default();
        _u.model = "dall-e-3";
        _u.n = 1;
        _u.prompt = "A sunset over mountains";
        _u.size = "1792x1024";
        return _u;
      })(),
    );
    expect(result.data.length).toBe(1);
    {
      const _v = result.data[0].url;
      if (typeof _v === "string" || Array.isArray(_v)) {
        expect(_v.length).toBeGreaterThan(0);
      } else {
        expect(_v).toBeDefined();
        expect(_v).not.toBeNull();
      }
    }
  }, 30000);
});
