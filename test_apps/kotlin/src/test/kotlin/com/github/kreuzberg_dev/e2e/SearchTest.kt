// This file is auto-generated by alef — DO NOT EDIT.
// alef:hash:39a159a4a8caf376823cff2c045331d914298842ac2554bebc6d0c96cba7d317
// To regenerate: alef generate
// To verify freshness: alef verify --exit-code
// Issues & docs: https://github.com/kreuzberg-dev/alef
package com.github.kreuzberg_dev.e2e

import org.junit.jupiter.api.Test
import kotlin.test.assertEquals
import kotlin.test.assertTrue
import kotlin.test.assertFalse
import kotlin.test.assertFailsWith
import com.fasterxml.jackson.databind.ObjectMapper
import com.fasterxml.jackson.datatype.jdk8.Jdk8Module
import SearchRequest

/** E2e tests for category: search. */
class SearchTest {

    companion object {
        private val MAPPER = ObjectMapper().registerModule(Jdk8Module())
    }
    @Test
    fun testSearchBasic() {
        // Basic web search request with a simple query
        val request = MAPPER.readValue("{\"model\":\"brave/web-search\",\"query\":\"What is Rust programming language?\"}", SearchRequest::class.java)
        val client = LiterLlm.createClient(apiKey = "test-key", baseUrl = System.getenv("MOCK_SERVER_URL") + "/fixtures/search_basic")
        val result = client.search(request)
        client.close()
    }

    @Test
    fun testSearchEmptyResults() {
        // Web search with a query that returns no results
        val request = MAPPER.readValue("{\"model\":\"brave/web-search\",\"query\":\"xyznonexistent12345xyz\"}", SearchRequest::class.java)
        val client = LiterLlm.createClient(apiKey = "test-key", baseUrl = System.getenv("MOCK_SERVER_URL") + "/fixtures/search_empty_results")
        val result = client.search(request)
        client.close()
    }

    @Test
    fun testSearchError400() {
        // 400 Bad Request error when search query is empty
        val request = MAPPER.readValue("{\"model\":\"brave/web-search\",\"query\":\"\"}", SearchRequest::class.java)
        val client = LiterLlm.createClient(apiKey = "test-key", baseUrl = System.getenv("MOCK_SERVER_URL") + "/fixtures/search_error_400")
        assertFailsWith<Exception> {
            client.search(request)
        }
        client.close()
    }

    @Test
    fun testSearchError401() {
        // 401 Unauthorized error on web search due to invalid API credentials
        val request = MAPPER.readValue("{\"model\":\"brave/web-search\",\"query\":\"test\"}", SearchRequest::class.java)
        val client = LiterLlm.createClient(apiKey = "test-key", baseUrl = System.getenv("MOCK_SERVER_URL") + "/fixtures/search_error_401")
        assertFailsWith<Exception> {
            client.search(request)
        }
        client.close()
    }

    @Test
    fun testSearchWithMaxResults() {
        // Search request with max_results parameter limiting response count
        val request = MAPPER.readValue("{\"max_results\":2,\"model\":\"brave/web-search\",\"query\":\"Rust programming\"}", SearchRequest::class.java)
        val client = LiterLlm.createClient(apiKey = "test-key", baseUrl = System.getenv("MOCK_SERVER_URL") + "/fixtures/search_with_max_results")
        val result = client.search(request)
        assertEquals(2, result.results().size, "expected exactly 2 elements")
        client.close()
    }

}
