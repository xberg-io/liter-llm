// This file is auto-generated by alef — DO NOT EDIT.
// alef:hash:9b111184530d7e3708623eb93e5066fe9b162dbffcf0c8e8681564fe28d69f96
// To regenerate: alef generate
// To verify freshness: alef verify --exit-code
// Issues & docs: https://github.com/kreuzberg-dev/alef
package com.github.kreuzberg_dev.e2e

import org.junit.jupiter.api.Test
import kotlin.test.assertEquals
import kotlin.test.assertTrue
import kotlin.test.assertFalse
import kotlin.test.assertFailsWith
import com.fasterxml.jackson.databind.ObjectMapper
import com.fasterxml.jackson.datatype.jdk8.Jdk8Module
import ChatCompletionRequest

/** E2e tests for category: configuration. */
class ConfigurationTest {

    companion object {
        private val MAPPER = ObjectMapper().registerModule(Jdk8Module())
    }
    @Test
    fun testCustomBaseUrl() {
        // Client configured with a custom base URL routes all requests to that endpoint
        val request = MAPPER.readValue("{\"messages\":[{\"content\":\"Hello\",\"role\":\"user\"}],\"model\":\"local-model\"}", ChatCompletionRequest::class.java)
        val client = LiterLlm.createClient(apiKey = "test-key", baseUrl = System.getenv("MOCK_SERVER_URL") + "/fixtures/custom_base_url")
        val result = client.chat(request)
        assertEquals(1, result.choices().size, "expected exactly 1 elements")
        assertEquals("Hi there!", result.choices().first().message().content().orEmpty().trim())
        assertEquals("stop", result.choices().first().finishReason().orEmpty().getValue().trim())
        assertEquals("local-model", result.model().trim())
        client.close()
    }

    @Test
    fun testExtraHeaders() {
        // Client configured with extra custom headers successfully completes a chat request
        val request = MAPPER.readValue("{\"messages\":[{\"content\":\"Hello\",\"role\":\"user\"}],\"model\":\"gpt-4\"}", ChatCompletionRequest::class.java)
        val client = LiterLlm.createClient(apiKey = "test-key", baseUrl = System.getenv("MOCK_SERVER_URL") + "/fixtures/extra_headers")
        val result = client.chat(request)
        assertEquals(1, result.choices().size, "expected exactly 1 elements")
        assertEquals("stop", result.choices().first().finishReason().orEmpty().getValue().trim())
        client.close()
    }

    @Test
    fun testLocalProviderLlamacpp() {
        // llamacpp local provider routes requests via llamacpp/ model prefix with no auth
        val request = MAPPER.readValue("{\"messages\":[{\"content\":\"Hello\",\"role\":\"user\"}],\"model\":\"llamacpp/my-model\"}", ChatCompletionRequest::class.java)
        val client = LiterLlm.createClient(apiKey = "test-key", baseUrl = System.getenv("MOCK_SERVER_URL") + "/fixtures/local_provider_llamacpp")
        val result = client.chat(request)
        assertEquals(1, result.choices().size, "expected exactly 1 elements")
        assertEquals("Hi there! I'm running locally.", result.choices().first().message().content().orEmpty().trim())
        assertEquals("stop", result.choices().first().finishReason().orEmpty().getValue().trim())
        assertEquals("my-model", result.model().trim())
        client.close()
    }

    @Test
    fun testLocalProviderOllama() {
        // Ollama local provider routes requests via ollama/ model prefix with no auth
        val request = MAPPER.readValue("{\"messages\":[{\"content\":\"Hello\",\"role\":\"user\"}],\"model\":\"ollama/qwen2:0.5b\"}", ChatCompletionRequest::class.java)
        val client = LiterLlm.createClient(apiKey = "test-key", baseUrl = System.getenv("MOCK_SERVER_URL") + "/fixtures/local_provider_ollama")
        val result = client.chat(request)
        assertEquals(1, result.choices().size, "expected exactly 1 elements")
        assertEquals("Hello! How can I help you today?", result.choices().first().message().content().orEmpty().trim())
        assertEquals("stop", result.choices().first().finishReason().orEmpty().getValue().trim())
        assertEquals("qwen2:0.5b", result.model().trim())
        client.close()
    }

    @Test
    fun testLocalProviderVllm() {
        // vLLM local provider routes requests via vllm/ model prefix with no auth
        val request = MAPPER.readValue("{\"messages\":[{\"content\":\"Hello\",\"role\":\"user\"}],\"model\":\"vllm/meta-llama/Llama-3.2-1B\"}", ChatCompletionRequest::class.java)
        val client = LiterLlm.createClient(apiKey = "test-key", baseUrl = System.getenv("MOCK_SERVER_URL") + "/fixtures/local_provider_vllm")
        val result = client.chat(request)
        assertEquals(1, result.choices().size, "expected exactly 1 elements")
        assertEquals("Hello! How may I assist you?", result.choices().first().message().content().orEmpty().trim())
        assertEquals("stop", result.choices().first().finishReason().orEmpty().getValue().trim())
        assertEquals("meta-llama/Llama-3.2-1B", result.model().trim())
        client.close()
    }

}
