// This file is auto-generated by alef — DO NOT EDIT.
// alef:hash:60c1a899c8bd630dec0c21eb52e64096890d958bdb1e3dd3c7b5a21e48a67812
// To regenerate: alef generate
// To verify freshness: alef verify --exit-code
// Issues & docs: https://github.com/kreuzberg-dev/alef
package com.github.kreuzberg_dev.e2e

import org.junit.jupiter.api.Test
import kotlin.test.assertEquals
import kotlin.test.assertTrue
import kotlin.test.assertFalse
import kotlin.test.assertFailsWith
import com.fasterxml.jackson.databind.ObjectMapper
import com.fasterxml.jackson.datatype.jdk8.Jdk8Module
import CreateFileRequest

/** E2e tests for category: files. */
class FilesTest {

    companion object {
        private val MAPPER = ObjectMapper().registerModule(Jdk8Module())
    }
    @Test
    fun testEdgeFileEmptyList() {
        // List files when no files have been uploaded
        val client = LiterLlm.createClient(apiKey = "test-key", baseUrl = System.getenv("MOCK_SERVER_URL") + "/fixtures/edge_file_empty_list")
        val result = client.listFiles()
        assertEquals(0, result.data().size, "expected exactly 0 elements")
        client.close()
    }

    @Test
    fun testEdgeFileLargeUpload() {
        // Upload a large file successfully
        val request = MAPPER.readValue("{\"file\":\"eyJwcm9tcHQiOiAibGFyZ2UgdHJhaW5pbmcgZGF0YSJ9Cg==\",\"filename\":\"large_training_data.jsonl\",\"purpose\":\"fine-tune\"}", CreateFileRequest::class.java)
        val client = LiterLlm.createClient(apiKey = "test-key", baseUrl = System.getenv("MOCK_SERVER_URL") + "/fixtures/edge_file_large_upload")
        val result = client.createFile(request)
        assertFalse(result.id().isEmpty(), "expected non-empty value")
        client.close()
    }

    @Test
    fun testErrorFileAuth401() {
        // 401 Unauthorized when listing files with invalid API key
        val client = LiterLlm.createClient(apiKey = "test-key", baseUrl = System.getenv("MOCK_SERVER_URL") + "/fixtures/error_file_auth_401")
        assertFailsWith<Exception> {
            client.listFiles()
        }
        client.close()
    }

    @Test
    fun testErrorFileBadPurpose() {
        // 400 Bad Request when uploading a file with invalid purpose
        val request = MAPPER.readValue("{\"file\":\"data.jsonl\",\"purpose\":\"invalid-purpose\"}", CreateFileRequest::class.java)
        val client = LiterLlm.createClient(apiKey = "test-key", baseUrl = System.getenv("MOCK_SERVER_URL") + "/fixtures/error_file_bad_purpose")
        assertFailsWith<Exception> {
            client.createFile(request)
        }
        client.close()
    }

    @Test
    fun testErrorFileNotFound() {
        // 404 Not Found when retrieving a nonexistent file
        val client = LiterLlm.createClient(apiKey = "test-key", baseUrl = System.getenv("MOCK_SERVER_URL") + "/fixtures/error_file_not_found")
        assertFailsWith<Exception> {
            client.retrieveFile("file-nonexistent")
        }
        client.close()
    }

    @Test
    fun testSmokeCreateFile() {
        // Upload a file for use with the API
        val request = MAPPER.readValue("{\"file\":\"eyJwcm9tcHQiOiAiaGVsbG8ifQo=\",\"filename\":\"training_data.jsonl\",\"purpose\":\"fine-tune\"}", CreateFileRequest::class.java)
        val client = LiterLlm.createClient(apiKey = "test-key", baseUrl = System.getenv("MOCK_SERVER_URL") + "/fixtures/smoke_create_file")
        val result = client.createFile(request)
        assertFalse(result.id().isEmpty(), "expected non-empty value")
        client.close()
    }

    @Test
    fun testSmokeDeleteFile() {
        // Delete an uploaded file
        val client = LiterLlm.createClient(apiKey = "test-key", baseUrl = System.getenv("MOCK_SERVER_URL") + "/fixtures/smoke_delete_file")
        val result = client.deleteFile("file-abc123")
        assertFalse(result.id().isEmpty(), "expected non-empty value")
        assertEquals(true, result.deleted())
        client.close()
    }

    @Test
    fun testSmokeFileContent() {
        // Retrieve the content of an uploaded file
        val client = LiterLlm.createClient(apiKey = "test-key", baseUrl = System.getenv("MOCK_SERVER_URL") + "/fixtures/smoke_file_content")
        val result = client.fileContent("file-abc123")
        assertFalse(result.content().isEmpty(), "expected non-empty value")
        client.close()
    }

    @Test
    fun testSmokeListFiles() {
        // List all uploaded files
        val client = LiterLlm.createClient(apiKey = "test-key", baseUrl = System.getenv("MOCK_SERVER_URL") + "/fixtures/smoke_list_files")
        val result = client.listFiles()
        assertEquals(2, result.data().size, "expected exactly 2 elements")
        client.close()
    }

    @Test
    fun testSmokeRetrieveFile() {
        // Retrieve metadata for an uploaded file
        val client = LiterLlm.createClient(apiKey = "test-key", baseUrl = System.getenv("MOCK_SERVER_URL") + "/fixtures/smoke_retrieve_file")
        val result = client.retrieveFile("file-abc123")
        assertFalse(result.id().isEmpty(), "expected non-empty value")
        client.close()
    }

}
