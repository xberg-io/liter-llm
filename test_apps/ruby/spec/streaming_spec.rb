# This file is auto-generated by alef — DO NOT EDIT.
# alef:hash:318c58a133a4b0cf29ba0b9369fa09fc9239a2be870dbe6de9d73eef61fda91d
# To regenerate: alef generate
# To verify freshness: alef verify --exit-code
# Issues & docs: https://github.com/sample_crate-dev/alef
# frozen_string_literal: true

require 'liter_llm'
require 'json'
require 'spec_helper'

RSpec.describe 'streaming' do
  it 'anthropic_stream: Streaming chat completion via the Anthropic provider (claude-3-5-sonnet-20241022) yielding multiple SSE chunks' do
    client = LiterLlm.create_client('test-key', ENV.fetch('MOCK_SERVER_URL') + '/fixtures/anthropic_stream')
    chunks = []
    stream_complete = false
    client.chat_stream(LiterLlm::ChatCompletionRequest.new(max_tokens: 32, messages: [{ 'content' => 'Count to three, one word per response.', 'role' => 'user' }], model: 'anthropic/claude-3-5-sonnet-20241022', stream: true)) do |chunk|
      chunks << chunk
    end
    stream_complete = true
    expect(chunks.length).to be >= 3
    expect(chunks.map { |c| c.choices.first&.delta&.content || '' }.join.to_s.strip).to eq('One Two Three'.strip)
    # skipped: streaming assertion on unsupported field 'stream_complete'
  end

  it 'azure_stream: Streaming chat completion via Azure OpenAI — verifies the azure/ prefix routes correctly and SSE chunks are delivered in the standard OpenAI chat.completion.chunk shape' do
    client = LiterLlm.create_client('test-key', ENV.fetch('MOCK_SERVER_URL') + '/fixtures/azure_stream')
    chunks = []
    stream_complete = false
    client.chat_stream(LiterLlm::ChatCompletionRequest.new(messages: [{ 'content' => 'Count to 3', 'role' => 'user' }], model: 'azure/gpt-4', stream: true, temperature: 0)) do |chunk|
      chunks << chunk
    end
    stream_complete = true
    expect(chunks.length).to be >= 3
    expect(chunks.map { |c| c.choices.first&.delta&.content || '' }.join.to_s.strip).to eq('1 2 3'.strip)
    # skipped: streaming assertion on unsupported field 'stream_complete'
  end

  it 'basic_stream: Streaming chat completion that produces content across multiple SSE chunks' do
    client = LiterLlm.create_client('test-key', ENV.fetch('MOCK_SERVER_URL') + '/fixtures/basic_stream')
    chunks = []
    stream_complete = false
    client.chat_stream(LiterLlm::ChatCompletionRequest.new(messages: [{ 'content' => 'Count to 3', 'role' => 'user' }], model: 'gpt-4', stream: true)) do |chunk|
      chunks << chunk
    end
    stream_complete = true
    expect(chunks.length).to be >= 3
    expect(chunks.map { |c| c.choices.first&.delta&.content || '' }.join.to_s.strip).to eq('1 2 3'.strip)
    expect(stream_complete).to be(true)
  end

  it 'bedrock_stream: Streaming chat completion via the AWS Bedrock provider using the bedrock/ prefix — verifies SSE chunks are yielded and assembled correctly from the Converse streaming API' do
    client = LiterLlm.create_client('test-key', ENV.fetch('MOCK_SERVER_URL') + '/fixtures/bedrock_stream')
    chunks = []
    stream_complete = false
    client.chat_stream(LiterLlm::ChatCompletionRequest.new(max_tokens: 32, messages: [{ 'content' => 'Count to three, one word per response.', 'role' => 'user' }], model: 'bedrock/anthropic.claude-3-sonnet-20240229-v1:0', stream: true)) do |chunk|
      chunks << chunk
    end
    stream_complete = true
    expect(chunks.length).to be >= 2
    expect(chunks.map { |c| c.choices.first&.delta&.content || '' }.join.to_s.strip).to eq('One Two Three'.strip)
    # skipped: streaming assertion on unsupported field 'stream_complete'
  end

  it 'edge_stream_function_call: Streaming chat completion with tool/function call chunks' do
    client = LiterLlm.create_client('test-key', ENV.fetch('MOCK_SERVER_URL') + '/fixtures/edge_stream_function_call')
    chunks = []
    stream_complete = false
    client.chat_stream(LiterLlm::ChatCompletionRequest.new(messages: [{ 'content' => 'What\'s the weather?', 'role' => 'user' }], model: 'gpt-4', tools: [{ 'function' => { 'name' => 'get_weather', 'parameters' => { 'properties' => { 'city' => { 'type' => 'string' } }, 'type' => 'object' } }, 'type' => 'function' }])) do |chunk|
      chunks << chunk
    end
    stream_complete = true
    expect(chunks.length).to be >= 2
    expect(stream_complete).to be(true)
  end

  it 'empty_stream: Streaming chat completion that produces no content chunks before the DONE signal' do
    client = LiterLlm.create_client('test-key', ENV.fetch('MOCK_SERVER_URL') + '/fixtures/empty_stream')
    chunks = []
    stream_complete = false
    client.chat_stream(LiterLlm::ChatCompletionRequest.new(messages: [{ 'content' => 'Say nothing', 'role' => 'user' }], model: 'gpt-4', stream: true)) do |chunk|
      chunks << chunk
    end
    stream_complete = true
    expect(chunks.length).to be >= 0
    expect(chunks.map { |c| c.choices.first&.delta&.content || '' }.join.to_s.strip).to eq(''.strip)
    expect(stream_complete).to be(true)
  end

  it 'local_stream_ollama: Streaming chat completion via Ollama local provider with SSE chunks' do
    client = LiterLlm.create_client('test-key', ENV.fetch('MOCK_SERVER_URL') + '/fixtures/local_stream_ollama')
    chunks = []
    stream_complete = false
    client.chat_stream(LiterLlm::ChatCompletionRequest.new(messages: [{ 'content' => 'Count to 3', 'role' => 'user' }], model: 'ollama/qwen2:0.5b', stream: true)) do |chunk|
      chunks << chunk
    end
    stream_complete = true
    expect(chunks.length).to be >= 3
    expect(chunks.map { |c| c.choices.first&.delta&.content || '' }.join.to_s.strip).to eq('1 2 3'.strip)
    # skipped: streaming assertion on unsupported field 'stream_complete'
  end

  it 'stream_content_policy_error: 400 Bad Request error on stream due to content policy violation' do
    client = LiterLlm.create_client('test-key', ENV.fetch('MOCK_SERVER_URL') + '/fixtures/stream_content_policy_error')
    expect { client.chat_stream(LiterLlm::ChatCompletionRequest.new(messages: [{ 'content' => 'Generate harmful content', 'role' => 'user' }], model: 'gpt-4o', stream: true)) { |_chunk| } }.to raise_error
  end

  it 'stream_done_signal: Verify that the [DONE] sentinel signal properly terminates the stream' do
    client = LiterLlm.create_client('test-key', ENV.fetch('MOCK_SERVER_URL') + '/fixtures/stream_done_signal')
    chunks = []
    stream_complete = false
    client.chat_stream(LiterLlm::ChatCompletionRequest.new(messages: [{ 'content' => 'Say done', 'role' => 'user' }], model: 'gpt-4', stream: true)) do |chunk|
      chunks << chunk
    end
    stream_complete = true
    # skipped: streaming assertion on unsupported field 'stream_complete'
    expect(chunks.map { |c| c.choices.first&.delta&.content || '' }.join.to_s.strip).to eq('Done'.strip)
    # skipped: streaming assertion on unsupported field 'no_chunks_after_done'
  end

  it 'stream_error_401: 401 Unauthorized error on stream initiation before any chunks are received' do
    client = LiterLlm.create_client('test-key', ENV.fetch('MOCK_SERVER_URL') + '/fixtures/stream_error_401')
    expect { client.chat_stream(LiterLlm::ChatCompletionRequest.new(messages: [{ 'content' => 'Hello', 'role' => 'user' }], model: 'gpt-4', stream: true)) { |_chunk| } }.to raise_error
  end

  it 'stream_multiple_choices: Streaming chat completion with multiple choice outputs (n > 1)' do
    client = LiterLlm.create_client('test-key', ENV.fetch('MOCK_SERVER_URL') + '/fixtures/stream_multiple_choices')
    chunks = []
    stream_complete = false
    client.chat_stream(LiterLlm::ChatCompletionRequest.new(messages: [{ 'content' => 'Hello', 'role' => 'user' }], model: 'gpt-4o', n: 2, stream: true)) do |chunk|
      chunks << chunk
    end
    stream_complete = true
    expect(stream_complete).to be(true)
  end

  it 'stream_with_tool_calls: Streaming chat completion where the assistant responds with a tool call across multiple chunks' do
    client = LiterLlm.create_client('test-key', ENV.fetch('MOCK_SERVER_URL') + '/fixtures/stream_with_tool_calls')
    chunks = []
    stream_complete = false
    client.chat_stream(LiterLlm::ChatCompletionRequest.new(messages: [{ 'content' => 'What is the weather in NYC?', 'role' => 'user' }], model: 'gpt-4', stream: true, tools: [{ 'function' => { 'description' => 'Get the current weather for a given location', 'name' => 'get_weather', 'parameters' => { 'properties' => { 'location' => { 'description' => 'The city and state, e.g. New York, NY', 'type' => 'string' } }, 'required' => ['location'], 'type' => 'object' } }, 'type' => 'function' }])) do |chunk|
      chunks << chunk
    end
    stream_complete = true
    expect(chunks.length).to be >= 4
    expect((chunks.empty? ? nil : chunks.last&.choices&.first&.finish_reason&.to_s).to_s.strip).to eq('tool_calls'.strip)
    expect(chunks.flat_map { |c| c.choices&.first&.delta&.tool_calls || [] }).not_to be_nil
    expect((chunks.flat_map { |c| c.choices&.first&.delta&.tool_calls || [] })[0].function.name.to_s.strip).to eq('get_weather'.strip)
    expect(stream_complete).to be(true)
  end

  it 'vertex_stream: Streaming chat completion via the Google Vertex AI provider using the vertex_ai/ prefix — verifies SSE chunks from the Gemini streaming endpoint are yielded and assembled correctly' do
    client = LiterLlm.create_client('test-key', ENV.fetch('MOCK_SERVER_URL') + '/fixtures/vertex_stream')
    chunks = []
    stream_complete = false
    client.chat_stream(LiterLlm::ChatCompletionRequest.new(max_tokens: 32, messages: [{ 'content' => 'Count to three, one word per response.', 'role' => 'user' }], model: 'vertex_ai/gemini-2.0-flash', stream: true)) do |chunk|
      chunks << chunk
    end
    stream_complete = true
    expect(chunks.length).to be >= 2
    expect(chunks.map { |c| c.choices.first&.delta&.content || '' }.join.to_s.strip).to eq('One Two Three'.strip)
    # skipped: streaming assertion on unsupported field 'stream_complete'
  end
end
