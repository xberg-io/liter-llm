// This file is auto-generated by alef — DO NOT EDIT.
// alef:hash:2b37ed2e01ef17269aeb11fed4a579edfa109e72280acbba8097704ffe82445f
// To regenerate: alef generate
// To verify freshness: alef verify --exit-code

// E2e tests for category: rerank
package e2e_test

import (
	"encoding/json"
	"os"
	"testing"

	"github.com/stretchr/testify/assert"

	pkg "github.com/kreuzberg-dev/liter-llm/packages/go"
)

func Test_EdgeRerankEmptyQuery(t *testing.T) {
	// Reranking with an empty query string
	var request pkg.RerankRequest
	if err := json.Unmarshal([]byte(`{"documents":["Some document","Another document"],"model":"rerank-v3.5","query":""}`), &request); err != nil {
		t.Fatalf("config parse failed: %v", err)
	}
	mockURL := os.Getenv("MOCK_SERVER_URL") + "/fixtures/edge_rerank_empty_query"
	client, clientErr := pkg.CreateClient("test-key", &mockURL, nil, nil, nil)
	if clientErr != nil {
		t.Fatalf("create client failed: %v", clientErr)
	}
	result, err := client.Rerank(request)
	if err != nil {
		t.Fatalf("call failed: %v", err)
	}
	assert.Equal(t, len(result.Results), 2, "expected exactly 2 elements")
}

func Test_EdgeRerankSingleDoc(t *testing.T) {
	// Reranking with only a single document
	var request pkg.RerankRequest
	if err := json.Unmarshal([]byte(`{"documents":["Artificial intelligence is the simulation of human intelligence."],"model":"rerank-v3.5","query":"What is AI?"}`), &request); err != nil {
		t.Fatalf("config parse failed: %v", err)
	}
	mockURL := os.Getenv("MOCK_SERVER_URL") + "/fixtures/edge_rerank_single_doc"
	client, clientErr := pkg.CreateClient("test-key", &mockURL, nil, nil, nil)
	if clientErr != nil {
		t.Fatalf("create client failed: %v", clientErr)
	}
	result, err := client.Rerank(request)
	if err != nil {
		t.Fatalf("call failed: %v", err)
	}
	assert.Equal(t, len(result.Results), 1, "expected exactly 1 elements")
	if len(result.Results) > 0 {
		if result.Results[0].RelevanceScore <= 0.9 {
			t.Errorf("expected > 0.9, got %v", result.Results[0].RelevanceScore)
		}
	}
}

func Test_ErrorRerankAuth401(t *testing.T) {
	// 401 Unauthorized for reranking with invalid API key
	var request pkg.RerankRequest
	if err := json.Unmarshal([]byte(`{"documents":["doc1"],"model":"rerank-v3.5","query":"test"}`), &request); err != nil {
		t.Fatalf("config parse failed: %v", err)
	}
	mockURL := os.Getenv("MOCK_SERVER_URL") + "/fixtures/error_rerank_auth_401"
	client, clientErr := pkg.CreateClient("test-key", &mockURL, nil, nil, nil)
	if clientErr != nil {
		t.Fatalf("create client failed: %v", clientErr)
	}
	_, err := client.Rerank(request)
	if err == nil {
		t.Errorf("expected an error, but call succeeded")
	}
}

func Test_ErrorRerankBadRequest(t *testing.T) {
	// 400 Bad Request for reranking with invalid model
	var request pkg.RerankRequest
	if err := json.Unmarshal([]byte(`{"documents":["doc1"],"model":"nonexistent-rerank","query":"test"}`), &request); err != nil {
		t.Fatalf("config parse failed: %v", err)
	}
	mockURL := os.Getenv("MOCK_SERVER_URL") + "/fixtures/error_rerank_bad_request"
	client, clientErr := pkg.CreateClient("test-key", &mockURL, nil, nil, nil)
	if clientErr != nil {
		t.Fatalf("create client failed: %v", clientErr)
	}
	_, err := client.Rerank(request)
	if err == nil {
		t.Errorf("expected an error, but call succeeded")
	}
}

func Test_SmokeRerankBasic(t *testing.T) {
	// Basic reranking of documents against a query
	var request pkg.RerankRequest
	if err := json.Unmarshal([]byte(`{"documents":["Machine learning is a subset of AI.","The weather is sunny today.","Deep learning uses neural networks."],"model":"rerank-v3.5","query":"What is machine learning?"}`), &request); err != nil {
		t.Fatalf("config parse failed: %v", err)
	}
	mockURL := os.Getenv("MOCK_SERVER_URL") + "/fixtures/smoke_rerank_basic"
	client, clientErr := pkg.CreateClient("test-key", &mockURL, nil, nil, nil)
	if clientErr != nil {
		t.Fatalf("create client failed: %v", clientErr)
	}
	result, err := client.Rerank(request)
	if err != nil {
		t.Fatalf("call failed: %v", err)
	}
	assert.Equal(t, len(result.Results), 3, "expected exactly 3 elements")
	if len(result.Results) > 0 {
		if result.Results[0].RelevanceScore <= 0.9 {
			t.Errorf("expected > 0.9, got %v", result.Results[0].RelevanceScore)
		}
	}
}

func Test_SmokeRerankReturnDocs(t *testing.T) {
	// Reranking with return_documents flag to include document text
	var request pkg.RerankRequest
	if err := json.Unmarshal([]byte(`{"documents":["Rust is a systems programming language.","Iron rusts when exposed to water."],"model":"rerank-v3.5","query":"What is Rust?","return_documents":true}`), &request); err != nil {
		t.Fatalf("config parse failed: %v", err)
	}
	mockURL := os.Getenv("MOCK_SERVER_URL") + "/fixtures/smoke_rerank_return_docs"
	client, clientErr := pkg.CreateClient("test-key", &mockURL, nil, nil, nil)
	if clientErr != nil {
		t.Fatalf("create client failed: %v", clientErr)
	}
	result, err := client.Rerank(request)
	if err != nil {
		t.Fatalf("call failed: %v", err)
	}
	assert.Equal(t, len(result.Results), 2, "expected exactly 2 elements")
	if len(result.Results) > 0 {
		if result.Results[0].Document == nil {
			t.Errorf("expected non-empty value")
		}
	}
	if len(result.Results) > 0 {
		if result.Results[0].RelevanceScore <= 0.9 {
			t.Errorf("expected > 0.9, got %v", result.Results[0].RelevanceScore)
		}
	}
}

func Test_SmokeRerankWithTopN(t *testing.T) {
	// Reranking with top_n parameter to limit results
	var request pkg.RerankRequest
	if err := json.Unmarshal([]byte(`{"documents":["Python is a programming language.","Cats are cute animals.","Python was created by Guido van Rossum.","The sun is a star."],"model":"rerank-v3.5","query":"What is Python?","top_n":2}`), &request); err != nil {
		t.Fatalf("config parse failed: %v", err)
	}
	mockURL := os.Getenv("MOCK_SERVER_URL") + "/fixtures/smoke_rerank_with_top_n"
	client, clientErr := pkg.CreateClient("test-key", &mockURL, nil, nil, nil)
	if clientErr != nil {
		t.Fatalf("create client failed: %v", clientErr)
	}
	result, err := client.Rerank(request)
	if err != nil {
		t.Fatalf("call failed: %v", err)
	}
	assert.Equal(t, len(result.Results), 2, "expected exactly 2 elements")
	if len(result.Results) > 0 {
		if result.Results[0].RelevanceScore <= 0.9 {
			t.Errorf("expected > 0.9, got %v", result.Results[0].RelevanceScore)
		}
	}
}
