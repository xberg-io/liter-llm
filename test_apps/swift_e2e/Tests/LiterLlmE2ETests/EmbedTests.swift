// This file is auto-generated by alef — DO NOT EDIT.
// alef:hash:c10fe61156d318b7f7b8255dbc410b8778efde8c48c482eb7beda5044f8546fd
// To regenerate: alef generate
// To verify freshness: alef verify --exit-code
// Issues & docs: https://github.com/kreuzberg-dev/alef
import XCTest
import Foundation
import LiterLlm
import RustBridge

/// E2e tests for category: embed.
final class EmbedTests: XCTestCase {
    func testBatchEmbed() async throws {
        // Embedding request with multiple input strings returns one embedding object per input
        let _request = try embeddingRequestFromJson("{\"input\":[\"Hello\",\"World\"],\"model\":\"text-embedding-3-small\"}")
        let _client = try DefaultClient(apiKey: "test-key", baseUrl: ProcessInfo.processInfo.environment["MOCK_SERVER_URL"]! + "/fixtures/batch_embed")
        let result = try await _client.embed(_request)
        XCTAssertEqual(result.data().count, 2)
        let _vec_data_count_equals_43041d6f = result.data()
        XCTAssertEqual(result.data()[0].embedding().count, 5)
    }

    func testEdgeEmbedBatchInput() async throws {
        // Embedding request with multiple inputs returns multiple embedding objects
        let _request = try embeddingRequestFromJson("{\"input\":[\"Hello world\",\"Goodbye world\"],\"model\":\"text-embedding-3-small\"}")
        let _client = try DefaultClient(apiKey: "test-key", baseUrl: ProcessInfo.processInfo.environment["MOCK_SERVER_URL"]! + "/fixtures/edge_embed_batch_input")
        let result = try await _client.embed(_request)
        XCTAssertEqual(result.data().count, 2)
        let _vec_data_equals_a12b7cc1 = result.data()
        XCTAssertEqual(_vec_data_equals_a12b7cc1[0].index(), 0)
        let _vec_data_equals_5c0b5df0 = result.data()
        XCTAssertEqual(_vec_data_equals_5c0b5df0[1].index(), 1)
    }

    func testEdgeEmbedEmptyInput() async throws {
        // Embedding request with empty string input returns empty data array
        let _request = try embeddingRequestFromJson("{\"input\":\"\",\"model\":\"text-embedding-3-small\"}")
        let _client = try DefaultClient(apiKey: "test-key", baseUrl: ProcessInfo.processInfo.environment["MOCK_SERVER_URL"]! + "/fixtures/edge_embed_empty_input")
        let result = try await _client.embed(_request)
        XCTAssertEqual(result.data().count, 0)
    }

    func testEmbedEncodingFormat() async throws {
        // Embedding request with explicit encoding_format of float returns float array embeddings
        let _request = try embeddingRequestFromJson("{\"encoding_format\":\"float\",\"input\":\"Test input\",\"model\":\"text-embedding-3-small\"}")
        let _client = try DefaultClient(apiKey: "test-key", baseUrl: ProcessInfo.processInfo.environment["MOCK_SERVER_URL"]! + "/fixtures/embed_encoding_format")
        let result = try await _client.embed(_request)
        XCTAssertEqual(result.data().count, 1)
        let _vec_data_count_equals_43041d6f = result.data()
        XCTAssertEqual(result.data()[0].embedding().count, 5)
    }

    func testEmbedError401() async throws {
        // 401 Unauthorized error on embedding request when API key is invalid
        do {
            let _request = try embeddingRequestFromJson("{\"input\":\"Hello world\",\"model\":\"text-embedding-3-small\"}")
            let _client = try DefaultClient(apiKey: "test-key", baseUrl: ProcessInfo.processInfo.environment["MOCK_SERVER_URL"]! + "/fixtures/embed_error_401")
            _ = try await _client.embed(_request)
            XCTFail("expected to throw")
        } catch {
            // success
        }
    }

    func testEmbedWithDimensions() async throws {
        // Embedding request with explicit dimensions parameter returns embeddings of the requested size
        let _request = try embeddingRequestFromJson("{\"dimensions\":256,\"input\":\"Hello world\",\"model\":\"text-embedding-3-small\"}")
        let _client = try DefaultClient(apiKey: "test-key", baseUrl: ProcessInfo.processInfo.environment["MOCK_SERVER_URL"]! + "/fixtures/embed_with_dimensions")
        let result = try await _client.embed(_request)
        XCTAssertEqual(result.data().count, 1)
        let _vec_data_count_equals_2d520974 = result.data()
        XCTAssertEqual(result.data()[0].embedding().count, 8)
    }

    func testLocalEmbedOllama() async throws {
        // Embedding request via Ollama local provider with all-minilm model
        let _request = try embeddingRequestFromJson("{\"input\":\"The quick brown fox jumps over the lazy dog\",\"model\":\"ollama/all-minilm\"}")
        let _client = try DefaultClient(apiKey: "test-key", baseUrl: ProcessInfo.processInfo.environment["MOCK_SERVER_URL"]! + "/fixtures/local_embed_ollama")
        let result = try await _client.embed(_request)
        XCTAssertEqual(result.data().count, 1)
        let _vec_data_count_equals_c2eca63c = result.data()
        XCTAssertEqual(result.data()[0].embedding().count, 32)
    }

}
