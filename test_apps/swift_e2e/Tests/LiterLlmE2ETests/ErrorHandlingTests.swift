// This file is auto-generated by alef — DO NOT EDIT.
// alef:hash:8cdff7ccce443d1fc90432984007ad265b45f4b1399e6a8713ec17d0541866e9
// To regenerate: alef generate
// To verify freshness: alef verify --exit-code
// Issues & docs: https://github.com/sample_crate-dev/alef
// swift-format-ignore-file

import XCTest
import Foundation
#if canImport(FoundationNetworking)
import FoundationNetworking
#endif
import LiterLlm
import RustBridge

/// E2e tests for category: error-handling.
final class ErrorHandlingTests: XCTestCase {
    func testAnthropicErrorAuth() async throws {
        // 401 Authentication error returned by the Anthropic API when the API key is invalid
        do {
            let _request = try LiterLlm.chatCompletionRequestFromJson("{\"messages\":[{\"content\":\"Hello\",\"role\":\"user\"}],\"model\":\"anthropic/claude-3-5-sonnet-20241022\"}")
            let _client = try DefaultClient(apiKey: "test-key", baseUrl: AlefE2EMockServer.baseURL + "/fixtures/anthropic_error_auth")
            _ = try await _client.chat(_request)
            XCTFail("expected to throw")
        } catch {
            // success
        }
    }

    func testAuth401() async throws {
        // 401 Unauthorized error when API key is invalid or missing
        do {
            let _request = try LiterLlm.chatCompletionRequestFromJson("{\"messages\":[{\"content\":\"Hello\",\"role\":\"user\"}],\"model\":\"gpt-4\"}")
            let _client = try DefaultClient(apiKey: "test-key", baseUrl: AlefE2EMockServer.baseURL + "/fixtures/auth_401")
            _ = try await _client.chat(_request)
            XCTFail("expected to throw")
        } catch {
            // success
        }
    }

    func testAzureErrorAuth() async throws {
        // Azure OpenAI returns a 401 Unauthorized error when the API key is missing or invalid — uses Azure's error envelope shape with code AccessDenied
        do {
            let _request = try LiterLlm.chatCompletionRequestFromJson("{\"messages\":[{\"content\":\"Hello\",\"role\":\"user\"}],\"model\":\"azure/gpt-4\"}")
            let _client = try DefaultClient(apiKey: "test-key", baseUrl: AlefE2EMockServer.baseURL + "/fixtures/azure_error_auth")
            _ = try await _client.chat(_request)
            XCTFail("expected to throw")
        } catch {
            // success
        }
    }

    func testBadRequest400() async throws {
        // 400 Bad Request error when a parameter value is invalid
        do {
            let _request = try LiterLlm.chatCompletionRequestFromJson("{\"messages\":[{\"content\":\"Hello\",\"role\":\"user\"}],\"model\":\"gpt-4\",\"temperature\":5.0}")
            let _client = try DefaultClient(apiKey: "test-key", baseUrl: AlefE2EMockServer.baseURL + "/fixtures/bad_request_400")
            _ = try await _client.chat(_request)
            XCTFail("expected to throw")
        } catch {
            // success
        }
    }

    func testBedrockErrorAuth() async throws {
        // AWS Bedrock returns 403 Forbidden (not 401) when credentials are missing, expired, or the IAM role lacks bedrock:InvokeModel permission — verifies the error is mapped to Authentication
        do {
            let _request = try LiterLlm.chatCompletionRequestFromJson("{\"messages\":[{\"content\":\"Hello\",\"role\":\"user\"}],\"model\":\"bedrock/anthropic.claude-3-sonnet-20240229-v1:0\"}")
            let _client = try DefaultClient(apiKey: "test-key", baseUrl: AlefE2EMockServer.baseURL + "/fixtures/bedrock_error_auth")
            _ = try await _client.chat(_request)
            XCTFail("expected to throw")
        } catch {
            // success
        }
    }

    func testContentPolicyViolation() async throws {
        // 400 error when a request is rejected due to content policy
        do {
            let _request = try LiterLlm.chatCompletionRequestFromJson("{\"messages\":[{\"content\":\"Generate harmful content\",\"role\":\"user\"}],\"model\":\"gpt-4\"}")
            let _client = try DefaultClient(apiKey: "test-key", baseUrl: AlefE2EMockServer.baseURL + "/fixtures/content_policy_violation")
            _ = try await _client.chat(_request)
            XCTFail("expected to throw")
        } catch {
            // success
        }
    }

    func testContextWindowExceeded() async throws {
        // 400 error when the prompt exceeds the model's maximum context length
        do {
            let _request = try LiterLlm.chatCompletionRequestFromJson("{\"messages\":[{\"content\":\"Very long prompt that exceeds the context window...\",\"role\":\"user\"}],\"model\":\"gpt-4\"}")
            let _client = try DefaultClient(apiKey: "test-key", baseUrl: AlefE2EMockServer.baseURL + "/fixtures/context_window_exceeded")
            _ = try await _client.chat(_request)
            XCTFail("expected to throw")
        } catch {
            // success
        }
    }

    func testEmptyResponseBody() async throws {
        // 200 OK response with an empty JSON object body, missing required fields
        do {
            let _request = try LiterLlm.chatCompletionRequestFromJson("{\"messages\":[{\"content\":\"Hello\",\"role\":\"user\"}],\"model\":\"gpt-4\"}")
            let _client = try DefaultClient(apiKey: "test-key", baseUrl: AlefE2EMockServer.baseURL + "/fixtures/empty_response_body")
            _ = try await _client.chat(_request)
            XCTFail("expected to throw")
        } catch {
            // success
        }
    }

    func testForbidden403() async throws {
        // 403 Forbidden error when the API key does not have access to the requested resource
        do {
            let _request = try LiterLlm.chatCompletionRequestFromJson("{\"messages\":[{\"content\":\"Hello\",\"role\":\"user\"}],\"model\":\"gpt-4\"}")
            let _client = try DefaultClient(apiKey: "test-key", baseUrl: AlefE2EMockServer.baseURL + "/fixtures/forbidden_403")
            _ = try await _client.chat(_request)
            XCTFail("expected to throw")
        } catch {
            // success
        }
    }

    func testGatewayTimeout504() async throws {
        // 504 Gateway Timeout error when the upstream service times out
        do {
            let _request = try LiterLlm.chatCompletionRequestFromJson("{\"messages\":[{\"content\":\"Hello\",\"role\":\"user\"}],\"model\":\"gpt-4\"}")
            let _client = try DefaultClient(apiKey: "test-key", baseUrl: AlefE2EMockServer.baseURL + "/fixtures/gateway_timeout_504")
            _ = try await _client.chat(_request)
            XCTFail("expected to throw")
        } catch {
            // success
        }
    }

    func testGithubCopilotErrorAuth() async throws {
        // 401 Authentication error returned by the GitHub Copilot API when the token is invalid or expired
        do {
            let _request = try LiterLlm.chatCompletionRequestFromJson("{\"messages\":[{\"content\":\"Hello\",\"role\":\"user\"}],\"model\":\"github_copilot/gpt-4o\"}")
            let _client = try DefaultClient(apiKey: "test-key", baseUrl: AlefE2EMockServer.baseURL + "/fixtures/github_copilot_error_auth")
            _ = try await _client.chat(_request)
            XCTFail("expected to throw")
        } catch {
            // success
        }
    }

    func testNotFound404() async throws {
        // 404 Not Found error when requesting a model that does not exist
        do {
            let _request = try LiterLlm.chatCompletionRequestFromJson("{\"messages\":[{\"content\":\"Hello\",\"role\":\"user\"}],\"model\":\"gpt-99\"}")
            let _client = try DefaultClient(apiKey: "test-key", baseUrl: AlefE2EMockServer.baseURL + "/fixtures/not_found_404")
            _ = try await _client.chat(_request)
            XCTFail("expected to throw")
        } catch {
            // success
        }
    }

    func testRateLimit429() async throws {
        // 429 Too Many Requests error when the rate limit is exceeded
        do {
            let _request = try LiterLlm.chatCompletionRequestFromJson("{\"messages\":[{\"content\":\"Hello\",\"role\":\"user\"}],\"model\":\"gpt-4\"}")
            let _client = try DefaultClient(apiKey: "test-key", baseUrl: AlefE2EMockServer.baseURL + "/fixtures/rate_limit_429")
            _ = try await _client.chat(_request)
            XCTFail("expected to throw")
        } catch {
            // success
        }
    }

    func testServerError500() async throws {
        // 500 Internal Server Error from the upstream API
        do {
            let _request = try LiterLlm.chatCompletionRequestFromJson("{\"messages\":[{\"content\":\"Hello\",\"role\":\"user\"}],\"model\":\"gpt-4\"}")
            let _client = try DefaultClient(apiKey: "test-key", baseUrl: AlefE2EMockServer.baseURL + "/fixtures/server_error_500")
            _ = try await _client.chat(_request)
            XCTFail("expected to throw")
        } catch {
            // success
        }
    }

    func testServiceUnavailable502() async throws {
        // 502 Bad Gateway error when the upstream service is unavailable
        do {
            let _request = try LiterLlm.chatCompletionRequestFromJson("{\"messages\":[{\"content\":\"Hello\",\"role\":\"user\"}],\"model\":\"gpt-4\"}")
            let _client = try DefaultClient(apiKey: "test-key", baseUrl: AlefE2EMockServer.baseURL + "/fixtures/service_unavailable_502")
            _ = try await _client.chat(_request)
            XCTFail("expected to throw")
        } catch {
            // success
        }
    }

    func testTimeoutError() async throws {
        // 408 Request Timeout error when the API request takes too long to complete
        do {
            let _request = try LiterLlm.chatCompletionRequestFromJson("{\"messages\":[{\"content\":\"Hello\",\"role\":\"user\"}],\"model\":\"gpt-4\"}")
            let _client = try DefaultClient(apiKey: "test-key", baseUrl: AlefE2EMockServer.baseURL + "/fixtures/timeout_error")
            _ = try await _client.chat(_request)
            XCTFail("expected to throw")
        } catch {
            // success
        }
    }

    func testVertexErrorAuth() async throws {
        // Google Vertex AI returns 401 Unauthorized when the OAuth2 token is missing, expired, or the service account lacks aiplatform.endpoints.predict permission — verifies the error is mapped to Authentication
        do {
            let _request = try LiterLlm.chatCompletionRequestFromJson("{\"messages\":[{\"content\":\"Hello\",\"role\":\"user\"}],\"model\":\"vertex_ai/gemini-2.0-flash\"}")
            let _client = try DefaultClient(apiKey: "test-key", baseUrl: AlefE2EMockServer.baseURL + "/fixtures/vertex_error_auth")
            _ = try await _client.chat(_request)
            XCTFail("expected to throw")
        } catch {
            // success
        }
    }

}
