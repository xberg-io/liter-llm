// This file is auto-generated by alef — DO NOT EDIT.
// alef:hash:bcc4f22c44e142dd276473383117978d89ca2987df5cadb3e37f80ed270b6128
// To regenerate: alef generate
// To verify freshness: alef verify --exit-code
// Issues & docs: https://github.com/kreuzberg-dev/alef
import XCTest
import Foundation
import LiterLlm
import RustBridge

/// E2e tests for category: configuration.
final class ConfigurationTests: XCTestCase {
    func testCustomBaseUrl() async throws {
        // Client configured with a custom base URL routes all requests to that endpoint
        let _request = try chatCompletionRequestFromJson("{\"messages\":[{\"content\":\"Hello\",\"role\":\"user\"}],\"model\":\"local-model\"}")
        let _client = try DefaultClient(apiKey: "test-key", baseUrl: ProcessInfo.processInfo.environment["MOCK_SERVER_URL"]! + "/fixtures/custom_base_url")
        let result = try await _client.chat(_request)
        XCTAssertEqual(result.choices().count, 1)
        let _vec_choices_equals_d1957db = result.choices()
        XCTAssertEqual((_vec_choices_equals_d1957db[0].message().content()?.toString() ?? "").trimmingCharacters(in: CharacterSet.whitespacesAndNewlines), "Hi there!")
        let _vec_choices_equals_f15f117a = result.choices()
        XCTAssertEqual((_vec_choices_equals_f15f117a[0].finish_reason()?.toString() ?? "").trimmingCharacters(in: CharacterSet.whitespacesAndNewlines), "stop")
        XCTAssertEqual(result.model().toString().trimmingCharacters(in: CharacterSet.whitespacesAndNewlines), "local-model")
    }

    func testExtraHeaders() async throws {
        // Client configured with extra custom headers successfully completes a chat request
        let _request = try chatCompletionRequestFromJson("{\"messages\":[{\"content\":\"Hello\",\"role\":\"user\"}],\"model\":\"gpt-4\"}")
        let _client = try DefaultClient(apiKey: "test-key", baseUrl: ProcessInfo.processInfo.environment["MOCK_SERVER_URL"]! + "/fixtures/extra_headers")
        let result = try await _client.chat(_request)
        XCTAssertEqual(result.choices().count, 1)
        let _vec_choices_equals_f15f117a = result.choices()
        XCTAssertEqual((_vec_choices_equals_f15f117a[0].finish_reason()?.toString() ?? "").trimmingCharacters(in: CharacterSet.whitespacesAndNewlines), "stop")
    }

    func testLocalProviderLlamacpp() async throws {
        // llamacpp local provider routes requests via llamacpp/ model prefix with no auth
        let _request = try chatCompletionRequestFromJson("{\"messages\":[{\"content\":\"Hello\",\"role\":\"user\"}],\"model\":\"llamacpp/my-model\"}")
        let _client = try DefaultClient(apiKey: "test-key", baseUrl: ProcessInfo.processInfo.environment["MOCK_SERVER_URL"]! + "/fixtures/local_provider_llamacpp")
        let result = try await _client.chat(_request)
        XCTAssertEqual(result.choices().count, 1)
        let _vec_choices_equals_6efdaed5 = result.choices()
        XCTAssertEqual((_vec_choices_equals_6efdaed5[0].message().content()?.toString() ?? "").trimmingCharacters(in: CharacterSet.whitespacesAndNewlines), "Hi there! I'm running locally.")
        let _vec_choices_equals_f15f117a = result.choices()
        XCTAssertEqual((_vec_choices_equals_f15f117a[0].finish_reason()?.toString() ?? "").trimmingCharacters(in: CharacterSet.whitespacesAndNewlines), "stop")
        XCTAssertEqual(result.model().toString().trimmingCharacters(in: CharacterSet.whitespacesAndNewlines), "my-model")
    }

    func testLocalProviderOllama() async throws {
        // Ollama local provider routes requests via ollama/ model prefix with no auth
        let _request = try chatCompletionRequestFromJson("{\"messages\":[{\"content\":\"Hello\",\"role\":\"user\"}],\"model\":\"ollama/qwen2:0.5b\"}")
        let _client = try DefaultClient(apiKey: "test-key", baseUrl: ProcessInfo.processInfo.environment["MOCK_SERVER_URL"]! + "/fixtures/local_provider_ollama")
        let result = try await _client.chat(_request)
        XCTAssertEqual(result.choices().count, 1)
        let _vec_choices_equals_3b8560b3 = result.choices()
        XCTAssertEqual((_vec_choices_equals_3b8560b3[0].message().content()?.toString() ?? "").trimmingCharacters(in: CharacterSet.whitespacesAndNewlines), "Hello! How can I help you today?")
        let _vec_choices_equals_f15f117a = result.choices()
        XCTAssertEqual((_vec_choices_equals_f15f117a[0].finish_reason()?.toString() ?? "").trimmingCharacters(in: CharacterSet.whitespacesAndNewlines), "stop")
        XCTAssertEqual(result.model().toString().trimmingCharacters(in: CharacterSet.whitespacesAndNewlines), "qwen2:0.5b")
    }

    func testLocalProviderVllm() async throws {
        // vLLM local provider routes requests via vllm/ model prefix with no auth
        let _request = try chatCompletionRequestFromJson("{\"messages\":[{\"content\":\"Hello\",\"role\":\"user\"}],\"model\":\"vllm/meta-llama/Llama-3.2-1B\"}")
        let _client = try DefaultClient(apiKey: "test-key", baseUrl: ProcessInfo.processInfo.environment["MOCK_SERVER_URL"]! + "/fixtures/local_provider_vllm")
        let result = try await _client.chat(_request)
        XCTAssertEqual(result.choices().count, 1)
        let _vec_choices_equals_64a5e79c = result.choices()
        XCTAssertEqual((_vec_choices_equals_64a5e79c[0].message().content()?.toString() ?? "").trimmingCharacters(in: CharacterSet.whitespacesAndNewlines), "Hello! How may I assist you?")
        let _vec_choices_equals_f15f117a = result.choices()
        XCTAssertEqual((_vec_choices_equals_f15f117a[0].finish_reason()?.toString() ?? "").trimmingCharacters(in: CharacterSet.whitespacesAndNewlines), "stop")
        XCTAssertEqual(result.model().toString().trimmingCharacters(in: CharacterSet.whitespacesAndNewlines), "meta-llama/Llama-3.2-1B")
    }

}
