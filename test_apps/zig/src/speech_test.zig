// This file is auto-generated by alef — DO NOT EDIT.
// alef:hash:7ef0cdb8f1e5cd6e00ad66a7ff1d010e60a1cf1349c16e197e2fc1e4bb44872a
// To regenerate: alef generate
// To verify freshness: alef verify --exit-code
// Issues & docs: https://github.com/kreuzberg-dev/alef
const std = @import("std");
const testing = std.testing;
const liter_llm = @import("liter_llm");

// E2e tests for category: speech

test "edge_speech_all_voices" {
    // Text-to-speech with specific voice selection
    // Note: async functions not yet fully supported; treating as sync
    const result = try liter_llm.speech("\"Hello world\"");
    try testing.expect(result.audio.len > 0);
}

test "edge_speech_long_input" {
    // Speech generation with a very long input text
    // Note: async functions not yet fully supported; treating as sync
    const result = try liter_llm.speech("\"This is a long input text. This is a long input text. This is a long input text. This is a long input text. This is a long input text. This is a long input text. This is a long input text. This is a long input text. This is a long input text. This is a long input text. This is a long input text. This is a long input text. This is a long input text. This is a long input text. This is a long input text. This is a long input text. This is a long input text. This is a long input text. This is a long input text. This is a long input text. This is a long input text. This is a long input text. This is a long input text. This is a long input text. This is a long input text. This is a long input text. This is a long input text. This is a long input text. This is a long input text. This is a long input text. This is a long input text. This is a long input text. This is a long input text. This is a long input text. This is a long input text. This is a long input text. This is a long input text. This is a long input text. This is a long input text. This is a long input text. This is a long input text. This is a long input text. This is a long input text. This is a long input text. This is a long input text. This is a long input text. This is a long input text. This is a long input text. This is a long input text. This is a long input text. End of input.\"");
    try testing.expect(result.audio.len > 0);
}

test "error_speech_auth_401" {
    // 401 Unauthorized for speech generation with invalid API key
    // Note: async functions not yet fully supported; treating as sync
    const result = liter_llm.speech("\"Hello\"") catch {
        try testing.expect(true); // Error occurred as expected
        return;
    };
    _ = result;
}

test "error_speech_bad_model" {
    // 400 Bad Request for speech with unsupported model
    // Note: async functions not yet fully supported; treating as sync
    const result = liter_llm.speech("\"Hello\"") catch {
        try testing.expect(true); // Error occurred as expected
        return;
    };
    _ = result;
}

test "smoke_speech_basic" {
    // Basic text-to-speech generation
    // Note: async functions not yet fully supported; treating as sync
    const result = try liter_llm.speech("\"Hello, world!\"");
    try testing.expect(result.audio.len > 0);
}

test "smoke_speech_mp3_format" {
    // Text-to-speech with explicit MP3 response format
    // Note: async functions not yet fully supported; treating as sync
    const result = try liter_llm.speech("\"The quick brown fox jumps over the lazy dog.\"");
    try testing.expect(result.audio.len > 0);
}
